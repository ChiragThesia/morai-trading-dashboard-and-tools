"""Money vocabulary: units (`units.py`) and the API field type that carries them
across the wire (`api_types.py`). Every later phase's money signatures are written
against these two modules (D-01, D-03)."""
