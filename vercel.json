{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "bun install",
  "buildCommand": "rm -rf apps/web/dist apps/web/node_modules/.vite node_modules/.vite node_modules/.cache && bun run --filter @morai/web build",
  "outputDirectory": "apps/web/dist",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
