# TOS Analyze → Risk Profile — design references (Phase 17.1 / OVW-04, OVW-05)

Drop the two Thinkorswim screenshots here (they were pasted in-chat but their macOS
temp files were cleared before they could be saved):

- `tos-risk-profile-combined-graph.png` — the combined Risk Profile graph the Overview
  payoff hero must emulate (smooth magenta "date" curve + cyan @exp curve, TOS axis
  ticks/scaling). Target for OVW-04.
- `tos-analyze-date-picker.png` — the Analyze tab with the "Date:" calendar-picker widget
  the Overview should gain (project the curve to a chosen future date). Target for OVW-05.
