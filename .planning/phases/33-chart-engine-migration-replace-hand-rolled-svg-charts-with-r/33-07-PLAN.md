---
phase: 33-chart-engine-migration-replace-hand-rolled-svg-charts-with-r
plan: 07
type: execute
wave: 4
depends_on: [33-03, 33-04, 33-05, 33-06]
files_modified:
  - apps/web/src/components/charts/PayoffChart.tsx
  - apps/web/src/components/charts/GammaProfile.tsx
  - apps/web/src/components/charts/GexBars.tsx
  - apps/web/src/components/picker/TermStructureChart.tsx
autonomous: true
requirements: [CHART-06]
user_setup: []
must_haves:
  truths:
    - "Dead SVG/scale helpers that the migration made unreachable are removed from the four migrated charts, and no other component imported them (verified before deletion)"
    - "@visx/*, echarts, and echarts-for-react remain in apps/web/package.json — the out-of-scope charts (LifecycleChart, EquityCurve, MiniLine, GexByExpiry) still import them"
    - "The full web workspace test suite, typecheck, and lint are all green after the migration"
  artifacts: []
  key_links:
    - "grep proves no importer outside the four chart files references a symbol before it is deleted"
    - "grep proves @visx/*, echarts, echarts-for-react are still declared deps AND still imported by the four out-of-scope charts"
---

<objective>
Close the phase: remove the now-dead SVG/scale internals from the four migrated charts (only the helpers
the Recharts swap made unreachable), prove nothing else imported them, confirm the out-of-scope charts'
libraries are untouched, and run the full green gate (test + typecheck + lint). No behavior change — this
is the cleanup + verification wave.

Purpose: The migrations deleted the crosshair block and wall clamp inline, but each chart may still carry
orphaned helpers (path builders, hand-rolled scale functions, unused re-exports) that were replaced by
Recharts primitives. Removing them is a refactor with green tests on both sides. The one hard guardrail:
do NOT remove @visx/*, echarts, or echarts-for-react — four out-of-scope charts still depend on them
(RESEARCH D-13 / Pitfall 7); removing them breaks the build.

Output: leaner chart files (dead code gone), a full green suite, and a verified dependency floor.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33-chart-engine-migration-replace-hand-rolled-svg-charts-with-r/33-CONTEXT.md
@.planning/phases/33-chart-engine-migration-replace-hand-rolled-svg-charts-with-r/33-RESEARCH.md
@apps/web/package.json
@apps/web/src/components/charts/PayoffChart.tsx
@apps/web/src/components/charts/GammaProfile.tsx
@apps/web/src/components/charts/GexBars.tsx
@apps/web/src/components/picker/TermStructureChart.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dead-code sweep for the four migrated charts (verify-before-delete; deps retained)</name>
  <files>apps/web/src/components/charts/PayoffChart.tsx, apps/web/src/components/charts/GammaProfile.tsx, apps/web/src/components/charts/GexBars.tsx, apps/web/src/components/picker/TermStructureChart.tsx</files>
  <read_first>
    - The four migrated chart files — identify helpers the Recharts swap made unreachable: e.g. in
      PayoffChart, the buildProfitZonePath / buildFillPath / buildYScale path builders and the trailing
      "AreaClosed/AreaStack imported for potential future use" comment if their visx basis is gone; in
      TermStructureChart, buildTermLinePath and the exported xScale/yScale if no longer referenced; in
      GexBars, any leftover buildOption remnants; in GammaProfile, any orphaned scale helper.
    - 33-RESEARCH.md Pitfall 7 / D-13 — @visx/*, echarts, echarts-for-react MUST stay (out-of-scope charts).
    - The other charts that still use those libraries: LifecycleChart.tsx, EquityCurve.tsx, MiniLine.tsx
      (visx); GexByExpiry.tsx (echarts-for-react).
  </read_first>
  <action>
    For each candidate dead symbol in the four migrated charts, grep the whole apps/web/src tree for
    importers BEFORE deleting; only remove a symbol when the sole references are inside the chart file
    itself (and its own test) and the Recharts version no longer uses it. Remove only what the task made
    unused — no drive-by refactors of adjacent code (change hygiene). Preserve exports that the migrated
    tests still import (e.g. any pure helper still referenced by a test). Do NOT touch package.json's
    @visx/*, echarts, or echarts-for-react entries. After deletions, run the four charts' tests plus
    typecheck to confirm nothing broke.
  </action>
  <verify>
    <automated>rg -q '"@visx/shape"' apps/web/package.json && rg -q '"echarts"' apps/web/package.json && rg -q '"echarts-for-react"' apps/web/package.json && bun run test -- apps/web/src/components/charts apps/web/src/components/picker/TermStructureChart.test.tsx && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - source: removed symbols have zero remaining importers across apps/web/src (proven by grep before deletion).
    - source: apps/web/package.json still declares @visx/*, echarts, and echarts-for-react.
    - behavior: the four migrated charts' tests + typecheck stay green after the sweep (green on both sides of the refactor).
    - test-command: the four chart test paths + `bun run typecheck` green.
  </acceptance_criteria>
  <done>Orphaned SVG/scale helpers removed from the four migrated charts with no lost importers; out-of-scope libraries retained.</done>
</task>

<task type="auto">
  <name>Task 2: Full phase green gate (test + typecheck + lint)</name>
  <files>apps/web/src/components/charts/PayoffChart.tsx</files>
  <read_first>
    - Root package.json scripts: test (`vitest run`), typecheck (`tsc --build --force`), lint (`eslint .`).
    - .claude/rules/workflow.md "Verification Before Done" — show the passing proof, do not claim "should work".
  </read_first>
  <action>
    Run the full workspace suite, typecheck, and lint. Fix any cross-file fallout the migration surfaced
    (e.g. a screen that passed a now-removed prop, an import path that shifted). Keep fixes minimal and
    scoped to migration fallout — no unrelated changes (the working tree has unrelated dirty files; stage
    only the paths this plan touches, never `git add -A`). This is the phase gate: it must be fully green
    before verification.
  </action>
  <verify>
    <automated>bun run test && bun run typecheck && bun run lint</automated>
  </verify>
  <acceptance_criteria>
    - behavior: `bun run test` (full workspace) passes; `bun run typecheck` clean; `bun run lint` clean.
    - source: any fixes are scoped to migration fallout; commits stage explicit paths only (no `git add -A`).
    - test-command: `bun run test && bun run typecheck && bun run lint` all green.
  </acceptance_criteria>
  <done>Full web workspace test suite, typecheck, and lint are green after the chart migration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Cleanup + verification only; no new runtime surface, no new input parsing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-08 | Tampering | dependency floor | medium | mitigate | Verify (grep) that @visx/*, echarts, echarts-for-react remain declared + imported by out-of-scope charts before finalizing — prevents a build-breaking "unused dep" removal (Pitfall 7) |
| T-33-SC | Tampering | npm installs | low | accept | No new dependency in this plan |
</threat_model>

<verification>
- `bun run test` (full workspace) + `bun run typecheck` + `bun run lint` all green.
- Removed chart helpers have zero remaining importers (grep before deletion).
- @visx/*, echarts, echarts-for-react still in apps/web/package.json and still imported by LifecycleChart/EquityCurve/MiniLine/GexByExpiry.
</verification>

<success_criteria>
- Dead SVG/scale code removed from the four migrated charts only, with no lost importers.
- Out-of-scope chart libraries (visx/echarts) retained; build intact.
- Full phase green gate passes (test + typecheck + lint).
</success_criteria>

<artifacts>
## Artifacts this plan produces (Plan 07)
- Leaner PayoffChart/GammaProfile/GexBars/TermStructureChart (orphaned helpers removed)
- Verified dependency floor (@visx/*, echarts, echarts-for-react retained)
- Full green suite as the phase gate
</artifacts>

<output>
Create `.planning/phases/33-chart-engine-migration-replace-hand-rolled-svg-charts-with-r/33-07-SUMMARY.md` when done
</output>
