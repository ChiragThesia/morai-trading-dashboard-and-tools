---
phase: 14-fred-expansion
plan: 06
type: tdd
wave: 3
depends_on: [14-01, 14-03, 14-04]
files_modified:
  - apps/server/src/adapters/http/analytics.routes.ts
  - apps/server/src/adapters/http/analytics.routes.test.ts
  - apps/server/src/adapters/mcp/tools.ts
  - apps/server/src/adapters/mcp/server.ts
  - apps/server/src/adapters/mcp/mcp.test.ts
  - apps/server/src/main.ts
autonomous: true
requirements: [MAC-02]

must_haves:
  truths:
    - "GET /api/analytics/macro returns the macroResponse-shaped map, validates days/series query params (400 on invalid), maps use-case errors to a flat {error:'internal'} 500, and inherits the Supabase JWT gate by apiRouter placement"
    - "MCP get_macro returns the same macroResponse payload as the HTTP route over the ONE shared contract, accepting optional {days, series}"
    - "The route + MCP tool are wired from one getMacro use-case in the server composition root"
  artifacts:
    - path: "apps/server/src/adapters/http/analytics.routes.ts"
      provides: "GET /analytics/macro (query-param validated, contract-parsed)"
      contains: "/analytics/macro"
    - path: "apps/server/src/adapters/mcp/tools.ts"
      provides: "registerGetMacroTool (get_macro over macroResponse, {days?,series?} input)"
      contains: "registerGetMacroTool"
  key_links:
    - from: "apps/server/src/main.ts"
      to: "packages/core/src/journal/application/getMacro.ts"
      via: "makeGetMacroUseCase wired into analyticsRoutes + the MCP router"
      pattern: "makeGetMacroUseCase"
    - from: "apps/server/src/adapters/http/analytics.routes.ts"
      to: "packages/contracts/src/macro.ts"
      via: "macroResponse.parse before send + macroQuery for input validation"
      pattern: "macroResponse"
---

<objective>
Expose the macro series over HTTP + MCP (MAC-02 / MCP-02): add `GET /api/analytics/macro` (with `days`/`series`
query-param validation) and the `get_macro` MCP tool, both backed by one `getMacro` use-case and one
`macroResponse` contract, wired in `apps/server/src/main.ts`.

Purpose: MAC-02's two adapters must ship in the same change over one Zod schema (MCP-02). The route inherits the
existing Supabase JWT gate by placement inside `apiRouter`/`authReadGroup` — no new auth code (RESEARCH-verified).
Unlike COT, this route takes user input (D-11), so `days`/`series` are validated at the boundary.
Output: the macro HTTP route + MCP tool over the shared contract.

Implements MAC-02 (route + MCP), per D-10/D-11 and MCP-02.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-fred-expansion/14-CONTEXT.md
@.planning/phases/14-fred-expansion/14-RESEARCH.md
@.planning/phases/14-fred-expansion/14-PATTERNS.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/tdd.md
@.claude/rules/typescript.md

# Files this plan extends + analogs:
@apps/server/src/adapters/http/analytics.routes.ts
@apps/server/src/adapters/mcp/tools.ts
@apps/server/src/adapters/mcp/server.ts
</context>

## Artifacts this phase produces (this plan)

| Symbol / file | Layer |
|---|---|
| `GET /api/analytics/macro` route (query-validated) — `apps/server/src/adapters/http/analytics.routes.ts` | server |
| `registerGetMacroTool` (`get_macro`) + MCP wiring — `apps/server/src/adapters/mcp/tools.ts` + `server.ts` | server |
| server composition wiring (macro repo + getMacro into route + MCP router) — `apps/server/src/main.ts` | server |

Consumes: `macroResponse`/`macroQuery`/`MACRO_SERIES_IDS` (14-01); `makePostgresMacroObservationsRepo` (14-03); `makeGetMacroUseCase`/`ForRunningGetMacro` (14-04). See 14-01 for the full phase manifest.

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: GET /api/analytics/macro route (query-validated, contract-parsed)</name>
  <files>apps/server/src/adapters/http/analytics.routes.ts, apps/server/src/adapters/http/analytics.routes.test.ts</files>
  <read_first>
    - apps/server/src/adapters/http/analytics.routes.ts — the `analyticsRoutes(getTermStructure, getSkew, getCot)` factory; the `/analytics/cot` handler (call use-case → err→flat `{error:"internal"}` 500 → `cotResponse.parse` → `c.json`) and the `/analytics/skew` handler which DOES read optional query params via `c.req.query(...)` (the input-parsing pattern to follow, since macro takes input unlike cot).
    - apps/server/src/adapters/http/analytics.routes.test.ts — existing route test structure (how getTermStructure/getSkew/getCot fakes are injected; how JSON body + status are asserted).
    - packages/contracts/src/macro.ts — `macroResponse` (parse before send) + `macroQuery` (validate days/series).
    - .planning/phases/14-fred-expansion/14-CONTEXT.md D-11 (days max 1825, series CSV filter of known ids); 14-RESEARCH.md Security Domain (map errors to flat body; validate input) + Pattern 4.
  </read_first>
  <behavior>
    - GET /analytics/macro with no query params returns 200 and a macroResponse-shaped map (default 90-day window applied by the use-case).
    - `?days=365&series=DFF,VVIX` is parsed via macroQuery and passed to getMacro; the response contains only the requested series.
    - An invalid param (`days=99999` or `series=BOGUS`) returns 400 (rejected by macroQuery) and does NOT call the use-case.
    - A use-case StorageError maps to a flat `{ error: "internal" }` 500 body — no DB internals leaked.
    - The response is always `macroResponse.parse(...)`'d before `c.json` (empty store → `{}`).
  </behavior>
  <action>
    Add `getMacro: ForRunningGetMacro` as a fourth parameter to the `analyticsRoutes(...)` factory. Add
    `router.get("/analytics/macro", ...)`: read `c.req.query("days")` and `c.req.query("series")`, build a raw
    params object (omit absent keys), and validate it through `macroQuery.safeParse(...)`; on failure return
    `c.json({ error: "invalid query" }, 400)` (T-14-01 — do NOT call the use-case). On success call
    `getMacro({ days, series })` with the parsed values (series as the validated string array); on Result err return
    `c.json({ error: "internal" }, 500)` (T-06-08 discipline — never expose DB internals). On ok return
    `c.json(macroResponse.parse(result.value))` (empty store → `{}`). Keep the handler thin (architecture-boundaries
    §3). Import `macroResponse` + `macroQuery` from `@morai/contracts` and `ForRunningGetMacro` from `@morai/core`.
    Write the RED tests FIRST (extend analytics.routes.test.ts: default-window 200, filtered params, invalid→400,
    err→500) and watch them fail before implementing.
  </action>
  <verify>
    <automated>bun run test -- --project server analytics.routes</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test -- --project server analytics.routes` passes: default 200, days/series filter, invalid-param→400, StorageError→500.
    - `rg -n "/analytics/macro" apps/server/src/adapters/http/analytics.routes.ts` matches.
    - The invalid-param test proves the use-case is NOT called on a 400 (input validated at the boundary, T-14-01).
    - `rg -n "macroResponse.parse" apps/server/src/adapters/http/analytics.routes.ts` matches (output contract-parsed).
    - `bun run typecheck` clean.
  </acceptance_criteria>
  <done>GET /api/analytics/macro returns the contract-shaped map, validates days/series (400 on bad input), and hides DB errors behind a flat 500.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: get_macro MCP tool (shared contract, {days?,series?})</name>
  <files>apps/server/src/adapters/mcp/tools.ts, apps/server/src/adapters/mcp/server.ts, apps/server/src/adapters/mcp/mcp.test.ts</files>
  <read_first>
    - apps/server/src/adapters/mcp/tools.ts — `registerGetCotTool` (server.registerTool → call use-case → err→flat text → `cotResponse.parse` → content) as the exact shape to mirror; note its empty `inputSchema: {}` (macro differs — it takes optional params).
    - apps/server/src/adapters/mcp/server.ts — the `getCot?: ForRunningGetCot` optional param + the `if (getCot !== undefined) registerGetCotTool(server, getCot)` wiring (the optional-injection pattern to mirror for backward compat).
    - apps/server/src/adapters/mcp/mcp.test.ts — how MCP tools are exercised and their payloads asserted; used to prove get_macro returns the same payload as the HTTP route.
    - packages/contracts/src/macro.ts — `macroResponse` (parse before return) + `macroQuery` (validate the tool's {days,series} input); note the MCP `inputSchema` uses a Zod raw-shape object (see how other parameterized MCP tools declare inputSchema).
    - .planning/phases/14-fred-expansion/14-CONTEXT.md D-10/D-11 (get_macro accepts the same {days?, series?}).
  </read_first>
  <behavior>
    - get_macro with no args returns the full default-window macroResponse payload (same shape the HTTP route returns).
    - get_macro with {days, series} filters the payload identically to the HTTP route (parity assertion).
    - Invalid args are rejected/normalized via macroQuery before the use-case is called (no unbounded request).
    - A use-case error returns a flat MCP error content (no DB internals), mirroring get_cot.
    - The tool is only registered when the getMacro use-case is injected (optional-injection backward compat).
  </behavior>
  <action>
    Add `registerGetMacroTool(server: McpServer, getMacro: ForRunningGetMacro): void` to `tools.ts`, mirroring
    `registerGetCotTool` but with an `inputSchema` exposing optional `days` (number) and `series` (string CSV) —
    validate the incoming args through `macroQuery` (or the macro param schema) inside the handler, call
    `getMacro({ days, series })`, map err → flat error content, and return
    `macroResponse.parse(result.value)` serialized as text content. Update `server.ts` (makeMcpServer): add an
    optional `getMacro?: ForRunningGetMacro` parameter and, mirroring the getCot block, call
    `if (getMacro !== undefined) registerGetMacroTool(server, getMacro)`. Thread `getMacro` through whatever the MCP
    composition chain is that currently threads `getCot` (the `makeMcpRouter` call in main.ts wires it in Task 3).
    Keep both adapters on the ONE `macroResponse` schema (MCP-02 — a one-sided field change fails typecheck). Write
    the RED tests FIRST (extend mcp.test.ts: get_macro default payload, filtered payload, HTTP-route parity) and
    watch them fail before implementing.
  </action>
  <verify>
    <automated>bun run test -- --project server mcp</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test -- --project server mcp` passes: get_macro default payload, {days,series} filter, and parity with the HTTP route response.
    - `rg -n "registerGetMacroTool" apps/server/src/adapters/mcp/tools.ts` and `apps/server/src/adapters/mcp/server.ts` both match.
    - The MCP tool and HTTP route both parse through `macroResponse` (MCP-02 shared schema — asserted by the parity test).
    - `bun run typecheck` clean.
  </acceptance_criteria>
  <done>get_macro returns the same macroResponse payload as the HTTP route over one shared contract, accepting {days?,series?}.</done>
</task>

<task type="execute">
  <name>Task 3: server composition wiring (macro repo + getMacro into route + MCP router)</name>
  <files>apps/server/src/main.ts</files>
  <read_first>
    - apps/server/src/main.ts — the getCot wiring block (`makePostgresCotObservationsRepo(db)` + `makeGetCotUseCase({ readCotObservations: ... })`, ~line 138-143), the `analyticsRoutes(getTermStructure, getSkew, getCot)` mount (~line 229), and the `makeMcpRouter(config, ..., getCot, getPositions, ...)` call (~line 273-289) — mirror these three sites for macro.
    - packages/adapters/src/index.ts — `makePostgresMacroObservationsRepo`; packages/core/src/index.ts — `makeGetMacroUseCase`.
  </read_first>
  <action>
    In the server composition root (pure wiring — architecture-boundaries §6; TDD-exempt), near the getCot block:
    construct `const macroObservationsRepo = makePostgresMacroObservationsRepo(db)` and `const getMacro =
    makeGetMacroUseCase({ readMacroObservations: macroObservationsRepo.readMacroObservations })`. Pass `getMacro` as
    the new fourth argument to `analyticsRoutes(getTermStructure, getSkew, getCot, getMacro)`. Thread `getMacro` into
    the `makeMcpRouter(...)` call next to `getCot` (matching the parameter position the MCP router/server expects
    from Task 2). Add the imports from `@morai/adapters` + `@morai/core`. Do not change any other wiring.
  </action>
  <verify>
    <automated>bun run typecheck && bun run test -- --project server</automated>
  </verify>
  <acceptance_criteria>
    - `bun run typecheck` clean; `bun run test -- --project server` green (whole server suite).
    - `rg -n "makeGetMacroUseCase|makePostgresMacroObservationsRepo" apps/server/src/main.ts` shows both wired.
    - `rg -n "analyticsRoutes\(" apps/server/src/main.ts` shows getMacro passed to the route factory.
    - `getMacro` is threaded into the MCP router call alongside `getCot`.
  </acceptance_criteria>
  <done>Server composes the macro repo + getMacro use-case and wires it into both the HTTP route and the MCP router from one source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser/client → GET /api/analytics/macro | User-controlled `days`/`series` cross here; validated by macroQuery before the use-case |
| Claude Code → MCP get_macro | Bearer-gated `/mcp/*`; tool args validated before the use-case |
| server edge → read endpoint | Supabase JWT gate inherited by apiRouter placement (no new auth code) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Tampering | macro route/MCP `series`/`days` input | medium | mitigate | macroQuery (z.enum + max 1825) validates at the boundary; invalid → 400 (route) / rejected (MCP) before the use-case runs |
| T-14-14 | Information Disclosure | DB error leaking through the route/tool | medium | mitigate | Map all use-case errors to a flat `{error:"internal"}` 500 (HTTP) / flat error content (MCP) — never the raw StorageError.message (T-06-08/T-13-06-INJ) |
| T-14-15 | Spoofing | unauthenticated access to macro data | low | mitigate | Route inherits the Supabase JWT gate by apiRouter/authReadGroup placement; MCP inherits the bearer gate on /mcp/* — no new auth code, verified in RESEARCH |
| T-14-03 | Information Disclosure | route/MCP contract drift | low | mitigate | ONE macroResponse schema for both adapters; parity test + typecheck fail on a one-sided change (MCP-02) |
| T-14-SC | Tampering | npm/bun installs | low | accept | Zero new dependencies — no legitimacy gate |
</threat_model>

<verification>
- `bun run test -- --project server` green (analytics.routes + mcp + whole server suite).
- `bun run typecheck` + `bun run lint` clean.
- Route + MCP parity proven against one macroResponse contract (MCP-02).
</verification>

<success_criteria>
- GET /api/analytics/macro returns the contract map, validates days/series (400 on bad input), hides DB errors, inherits auth.
- MCP get_macro returns the identical payload over the shared contract, accepting {days?,series?}.
- Both wired from one getMacro use-case in the server composition root.
</success_criteria>

<output>
Create `.planning/phases/14-fred-expansion/14-06-SUMMARY.md` when done.
</output>
