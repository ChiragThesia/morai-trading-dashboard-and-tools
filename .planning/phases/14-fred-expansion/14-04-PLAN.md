---
phase: 14-fred-expansion
plan: 04
type: tdd
wave: 2
depends_on: [14-01]
files_modified:
  - packages/core/src/journal/application/fetchMacroSeries.ts
  - packages/core/src/journal/application/fetchMacroSeries.test.ts
  - packages/core/src/journal/application/getMacro.ts
  - packages/core/src/journal/application/getMacro.test.ts
  - packages/core/src/journal/index.ts
  - packages/core/src/index.ts
autonomous: true
requirements: [MAC-01, MAC-02]

must_haves:
  truths:
    - "fetchMacroSeries fetches 7 FRED series + VVIX via Promise.allSettled, persists every success independently, then returns err naming the failed series if ANY failed (best-effort + fail-loud finish, D-07)"
    - "getMacro groups rows by seriesId into { [seriesId]: [{time, value}] } ascending by date, applies the default 90-day window and optional days/series filters (D-10/D-11), and returns ok({}) on empty store"
    - "Neither use-case imports pg-boss, Hono, process.env, or node I/O (hexagon pure)"
  artifacts:
    - path: "packages/core/src/journal/application/fetchMacroSeries.ts"
      provides: "makeFetchMacroSeries + ForRunningFetchMacroSeries (per-series independence, fail-loud finish)"
      contains: "makeFetchMacroSeries"
    - path: "packages/core/src/journal/application/getMacro.ts"
      provides: "makeGetMacroUseCase + ForRunningGetMacro + MacroSeriesQuery (group + window)"
      contains: "makeGetMacroUseCase"
  key_links:
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/journal/application/getMacro.ts"
      via: "barrel re-export of makeGetMacroUseCase + ForRunningGetMacro"
      pattern: "ForRunningGetMacro"
    - from: "packages/core/src/journal/application/fetchMacroSeries.ts"
      to: "packages/core/src/journal/application/ports.ts"
      via: "consumes ForFetchingFredSeries + ForFetchingVvixQuote + ForPersistingMacroObservation"
      pattern: "ForFetchingFredSeries"
---

<objective>
Build the two core macro use-cases: `makeFetchMacroSeries` (orchestrate 7 FRED series + VVIX with per-series
independent failure and a fail-loud finish) and `makeGetMacroUseCase` (group stored rows into the
`{ [seriesId]: [{time, value}] }` map with the default 90-day window + optional filters).

Purpose: pure orchestration in the hexagon (no I/O). fetchMacroSeries is the MAC-01 ingestion brain;
getMacro is the MAC-02 read brain. Both are function-type factories consumed by the worker (14-05) and
server (14-06) composition roots. Same file group so the core barrels are edited once.
Output: fetchMacroSeries + getMacro use-cases with driver-port types.

Implements MAC-01 (ingestion orchestration) + MAC-02 (read/group), per D-05/D-07/D-09/D-10/D-11.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-fred-expansion/14-CONTEXT.md
@.planning/phases/14-fred-expansion/14-RESEARCH.md
@.planning/phases/14-fred-expansion/14-PATTERNS.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/tdd.md
@.claude/rules/typescript.md

# Direct analogs to mirror:
@packages/core/src/journal/application/fetchCot.ts
@packages/core/src/journal/application/getCot.ts
@packages/core/src/journal/application/ports.ts
</context>

## Artifacts this phase produces (this plan)

| Symbol / file | Layer |
|---|---|
| `makeFetchMacroSeries` + `ForRunningFetchMacroSeries` — `packages/core/src/journal/application/fetchMacroSeries.ts` | core |
| `makeGetMacroUseCase` + `ForRunningGetMacro` + `MacroSeriesQuery` — `packages/core/src/journal/application/getMacro.ts` | core |
| barrel re-exports in `packages/core/src/journal/index.ts` + `packages/core/src/index.ts` | core |

Consumes: the four macro ports + `MacroObservationRow` (from 14-01). See 14-01 for the full phase manifest.

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: fetchMacroSeries use-case (per-series independence + fail-loud finish)</name>
  <files>packages/core/src/journal/application/fetchMacroSeries.ts, packages/core/src/journal/application/fetchMacroSeries.test.ts, packages/core/src/journal/index.ts, packages/core/src/index.ts</files>
  <read_first>
    - packages/core/src/journal/application/fetchCot.ts — the single-fetch factory shape (`make...` → driver returning `Result<void, FetchError | StorageError>`, fetch → stamp → persist, no fabricated fallback).
    - packages/core/src/journal/application/ports.ts — `ForFetchingFredSeries`, `ForFetchingVvixQuote`, `ForPersistingMacroObservation`, `MacroObservationRow`, `FetchError`, `StorageError` (added in 14-01).
    - packages/core/src/index.ts + journal/index.ts — where use-case factories + driver-port types are re-exported (see the fetchCot exports).
    - .planning/phases/14-fred-expansion/14-CONTEXT.md D-07 (best-effort per series, fail-loud finish naming failed series), D-09 (missing key is a hard fail surfaced by the adapter's err); 14-RESEARCH.md Pattern 3 (Promise.allSettled, refresh-tokens JOB-02 precedent); D-04/D-05 (self-healing incremental is adapter/config-driven — the use-case just fetches "the latest" per series via the injected ports).
  </read_first>
  <behavior>
    - With all 8 fetches succeeding, every returned row is persisted and the driver returns ok(void).
    - When 2 series fetches fail (reject or Result.err) and 6 succeed, the 6 successes are STILL persisted, and the driver returns err whose message names the failed series (e.g. contains "DFF" and "SOFR").
    - A persist failure on one series does not prevent attempting the others; the finish still returns err.
    - Fetches run via Promise.allSettled (one series' rejection does not short-circuit the batch).
    - The default series set is the 7 FRED ids (DFF, DGS1MO, DGS3MO, SOFR, T10Y2Y, T10Y3M, VIXCLS) plus VVIX via the VVIX port.
  </behavior>
  <action>
    Create `makeFetchMacroSeries(deps: { fetchFredSeries: ForFetchingFredSeries; fetchVvixQuote:
    ForFetchingVvixQuote; persistMacroObservation: ForPersistingMacroObservation; fredSeriesIds?: ReadonlyArray<
    string> })` returning `ForRunningFetchMacroSeries = () => Promise<Result<void, FetchError>>`. Default
    `fredSeriesIds` to the seven FRED ids `["DFF","DGS1MO","DGS3MO","SOFR","T10Y2Y","T10Y3M","VIXCLS"]` (domain
    constant in this file — core cannot import contracts; this mirrors fetchCot's default contractCode). Build the
    batch: for each FRED id a fetch task calling `fetchFredSeries(id)`, plus one task calling `fetchVvixQuote()`.
    Run them with `Promise.allSettled`. For each settled result that is fulfilled AND `Result.ok`, call
    `persistMacroObservation(row)` (await; collect persist failures too). Collect the names of every series that
    failed to fetch (rejected or `Result.err`) OR failed to persist. After ALL persists are attempted (best-effort,
    D-07): if the failed-series list is empty return `ok(undefined)`; otherwise return `err({ kind: "fetch-error",
    message: "macro fetch failed for: <comma-joined series ids>" })` (fail-loud finish, D-07 — so pg-boss marks the
    job failed and /api/status shows lastErr while every success is already persisted; next run self-heals). Use
    `ok`/`err` from `@morai/shared`; never throw (core is Result-based). Re-export `makeFetchMacroSeries` +
    `ForRunningFetchMacroSeries` through journal/index.ts + core/index.ts. Write the RED tests FIRST (inject fake
    ports: all-ok, mixed-failure asserting successes persisted + err names failed series, persist-failure) and watch
    them fail before implementing.
  </action>
  <verify>
    <automated>bun run test -- packages/core/src/journal/application/fetchMacroSeries.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test -- packages/core/src/journal/application/fetchMacroSeries.test.ts` passes: all-success→ok, mixed-failure→6 persisted + err naming failed series, persist-failure counted in the finish.
    - The mixed-failure test proves successes are persisted even though the overall Result is err (best-effort + fail-loud, D-07).
    - `rg -n "allSettled" packages/core/src/journal/application/fetchMacroSeries.ts` matches (per-series independence).
    - `rg -n "makeFetchMacroSeries|ForRunningFetchMacroSeries" packages/core/src/index.ts` shows the barrel exports.
    - `bun run typecheck` clean; the use-case imports only from `./ports.ts` and `@morai/shared` (no adapters/framework).
  </acceptance_criteria>
  <done>fetchMacroSeries fetches 8 series independently, persists all successes, and fails loud naming any failed series.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: getMacro use-case (group by series + window/filter)</name>
  <files>packages/core/src/journal/application/getMacro.ts, packages/core/src/journal/application/getMacro.test.ts, packages/core/src/journal/index.ts, packages/core/src/index.ts</files>
  <read_first>
    - packages/core/src/journal/application/getCot.ts — the read use-case shape (`make...UseCase({ read... })` → driver returning `Result<..., StorageError>`, read → map, empty store → ok([]); domain shape defined locally, NOT imported from contracts, to keep the hexagon pure).
    - packages/core/src/journal/application/ports.ts — `ForReadingMacroObservations`, `MacroObservationRow`, `StorageError` (added in 14-01).
    - .planning/phases/14-fred-expansion/14-CONTEXT.md D-10 (response `{ [seriesId]: [{time,value}] }`, time-ordered ASCENDING) + D-11 (default 90-day window; optional `days` max 1825; optional `series` filter — a list of series ids); 14-PATTERNS.md "getMacro.ts" section (group-by-seriesId reduction; keep filtering pure in the use-case).
  </read_first>
  <behavior>
    - getMacro() with rows for DFF + VVIX returns a map keyed by seriesId; each series array is ascending by date; `time` is the YYYY-MM-DD string; `value` is the raw number.
    - Empty store returns ok({}) (not an error).
    - Default window: with no params, rows older than 90 days before the injected `now` are excluded; rows within 90 days are included.
    - `days: 365` widens the window to 365 days; a value is clamped to the 1825 max upstream (contract) so the use-case can trust the passed number.
    - `series: ["VVIX"]` returns only the VVIX key (other series filtered out).
    - Combined `{ days, series }` applies both filters.
  </behavior>
  <action>
    Define a local `MacroSeriesQuery` type (`{ readonly days?: number; readonly series?: ReadonlyArray<string> }`) and
    a local `MacroSeriesPointOut` shape (`{ time: string; value: number }`) — defined here, NOT imported from
    contracts (hexagon purity, getCot precedent). Create `makeGetMacroUseCase(deps: { readMacroObservations:
    ForReadingMacroObservations; now?: () => Date })` returning `ForRunningGetMacro = (query?: MacroSeriesQuery) =>
    Promise<Result<Record<string, ReadonlyArray<MacroSeriesPointOut>>, StorageError>>`. In the driver: read all rows
    via `readMacroObservations()`; on err propagate. Compute the window cutoff date = (`deps.now?.() ?? new Date()`)
    minus `query?.days ?? 90` days, formatted YYYY-MM-DD; keep only rows with `date >= cutoff` (YYYY-MM-DD strings
    compare lexicographically = chronologically). If `query?.series` is provided, keep only rows whose `seriesId` is
    in that set. Group surviving rows by `seriesId` into arrays of `{ time: row.date, value: row.value }`, and sort
    each array ASCENDING by `time` (D-10 — opposite of COT's DESC read; sort here regardless of repo order). Return
    `ok(map)`; empty input → `ok({})`. Pure — no I/O, no throw. Re-export `makeGetMacroUseCase` + `ForRunningGetMacro`
    + `MacroSeriesQuery` through journal/index.ts + core/index.ts. Write the RED tests FIRST (fake read port: group +
    ascending-sort, empty→{}, default-90d window, days override, series filter) and watch them fail before implementing.
  </action>
  <verify>
    <automated>bun run test -- packages/core/src/journal/application/getMacro.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test -- packages/core/src/journal/application/getMacro.test.ts` passes: group-by-series ascending, empty→{}, 90-day default window excludes older rows, `days` override widens, `series` filter narrows.
    - The ascending-order assertion proves each series array is sorted by `time` ascending (D-10).
    - `rg -n "makeGetMacroUseCase|ForRunningGetMacro" packages/core/src/index.ts` shows the barrel exports.
    - `bun run typecheck` clean; getMacro imports only `./ports.ts` + `@morai/shared` (no contracts/adapters/framework).
  </acceptance_criteria>
  <done>getMacro groups stored rows into the ascending series map with 90-day-default windowing + days/series filters; empty→{}.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| use-case → repo/adapter ports | Core orchestration; all I/O is behind injected function-type ports (no direct I/O) |
| query params → getMacro | `days`/`series` arrive already Zod-validated at the route (14-06); the use-case trusts the parsed numbers/list |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-09 | Denial of Service | silent data holes from a partial fetch failure | medium | mitigate | Fail-loud finish (D-07): any failed series makes the job Result err so pg-boss/status surface it; successes still persist and next run self-heals — no silent gap (Phase 11 chain-observedAt lesson) |
| T-14-10 | Information Disclosure | StorageError propagation | low | mitigate | Use-case propagates the typed StorageError unchanged; the route (14-06) maps it to a flat `{error:"internal"}` body — DB internals never leave core |
| T-14-11 | Tampering | hexagon purity (no framework leakage into core) | low | mitigate | Both use-cases import only `./ports.ts` + `@morai/shared`; ESLint boundary rules fail the build otherwise (architecture-boundaries §2) |
| T-14-SC | Tampering | npm/bun installs | low | accept | Zero new dependencies — no legitimacy gate |
</threat_model>

<verification>
- `bun run test -- packages/core/src/journal/application/fetchMacroSeries.test.ts` + `getMacro.test.ts` green.
- `bun run typecheck` + `bun run lint` clean (hexagon purity enforced).
</verification>

<success_criteria>
- fetchMacroSeries: per-series independence (allSettled), best-effort persist, fail-loud finish naming failed series (D-07).
- getMacro: ascending series map, 90-day default window + days/series filters, empty→{} (D-10/D-11).
- Both use-cases + driver-port types exported from @morai/core; hexagon stays pure.
</success_criteria>

<output>
Create `.planning/phases/14-fred-expansion/14-04-SUMMARY.md` when done.
</output>
