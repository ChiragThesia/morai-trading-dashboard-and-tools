---
phase: 14-fred-expansion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/adapters/src/postgres/schema.ts
  - packages/adapters/src/postgres/migrations/0013_macro_observations.sql
  - packages/adapters/src/postgres/migrations/meta/_journal.json
  - packages/adapters/src/postgres/migrations/meta/0013_snapshot.json
autonomous: true
requirements: [MAC-01]

must_haves:
  truths:
    - "The macro_observations table exists in the live schema with a composite (date, series_id) primary key and RLS enabled"
    - "The generated migration is a clean CREATE TABLE (not a drop/recreate of any existing table) and leaves rate_observations untouched"
    - "bun run migrate applies 0013 and a second run is a no-op (idempotent)"
  artifacts:
    - path: "packages/adapters/src/postgres/migrations/0013_macro_observations.sql"
      provides: "macro_observations table DDL with composite PK (date, series_id) + ENABLE ROW LEVEL SECURITY"
      contains: "macro_observations"
      min_lines: 8
    - path: "packages/adapters/src/postgres/schema.ts"
      provides: "macroObservations pgTable (date, series_id, value, source) composite time-leading PK"
      contains: "macroObservations"
  key_links:
    - from: "packages/adapters/src/postgres/migrations/0013_macro_observations.sql"
      to: "packages/adapters/src/postgres/schema.ts"
      via: "drizzle-kit generate emits SQL from the macroObservations pgTable definition"
      pattern: "macroObservations"
    - from: "packages/adapters/src/postgres/migrations/meta/_journal.json"
      to: "packages/adapters/src/postgres/migrations/0013_macro_observations.sql"
      via: "ledger entry for the 0013 migration"
      pattern: "0013_macro_observations"
---

<objective>
Add the `macro_observations` table (14-CONTEXT D-01 — a NEW table, NOT a widening of `rate_observations`),
generate migration `0013_macro_observations.sql`, and apply it to the live schema with a [BLOCKING] push so
downstream repo/testcontainer work in 14-03 runs against a proven table.

Purpose: the composite time-leading PK `(date, series_id)` is the MAC-01 idempotency key (D-05 upsert target).
This is the schema-relevant [BLOCKING] gate the phase cannot pass verification without — build/typecheck pass
without it (types come from config, not the live DB), so the push must be explicit.
Output: `macroObservations` pgTable + generated 0013 migration + live-applied table.

Implements MAC-01 (storage), per D-01/D-14 and DATA-01 (time-leading composite PK).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-fred-expansion/14-CONTEXT.md
@.planning/phases/14-fred-expansion/14-PATTERNS.md
@.claude/rules/architecture-boundaries.md

# Analogs to mirror:
@packages/adapters/src/postgres/schema.ts
@packages/adapters/src/postgres/migrations/0012_cot_observations.sql
</context>

## Artifacts this phase produces (this plan)

| Symbol / file | Layer |
|---|---|
| `macroObservations` pgTable (`macro_observations`) — `packages/adapters/src/postgres/schema.ts` | adapters |
| `0013_macro_observations.sql` migration + `meta/0013_snapshot.json` + `_journal.json` ledger entry | adapters |

Depends on nothing (schema is independent of the 14-01 contract/ports). See 14-01 for the full phase manifest.

<tasks>

<task type="execute">
  <name>Task 1: macro_observations table + migration 0013 + [BLOCKING] live schema push</name>
  <files>packages/adapters/src/postgres/schema.ts, packages/adapters/src/postgres/migrations/0013_macro_observations.sql, packages/adapters/src/postgres/migrations/meta/_journal.json, packages/adapters/src/postgres/migrations/meta/0013_snapshot.json</files>
  <read_first>
    - packages/adapters/src/postgres/schema.ts — the drizzle-orm/pg-core imports (`pgTable`, `date`, `text`, `numeric`, `primaryKey`), `rateObservations` (single-column PK + `.enableRLS()`, line ~202), and a composite time-leading PK example such as `calendarSnapshots`/`legObservations` (`primaryKey({ columns: [table.time, table.calendarId] })`, line ~107/142).
    - packages/adapters/src/postgres/migrations/0012_cot_observations.sql — the generated-SQL style (CREATE TABLE + `--> statement-breakpoint` + `ENABLE ROW LEVEL SECURITY`).
    - packages/adapters/src/postgres/migrations/meta/_journal.json — ledger shape; the latest entry is `0012_cot_observations`.
    - .planning/phases/14-fred-expansion/14-CONTEXT.md D-01 (columns), D-14 (raw value), and the Claude's-Discretion `source` column note; 14-PATTERNS.md "migration" + "schema.ts" sections.
    - .planning/phases/14-fred-expansion/14-RESEARCH.md Pitfall 7 (`bun run migrate` validates the FULL worker env — needs SIDECAR_URL + all workerConfigSchema fields in local `.env` before it will run).
  </read_first>
  <action>
    Add a `macroObservations` pgTable named `macro_observations` to `schema.ts`, placed next to `rateObservations`.
    Columns: `date` = `date("date").notNull()`; `seriesId` = `text("series_id").notNull()`; `value` =
    `numeric("value").notNull()` (RAW as reported — no /100, D-14); `source` = `text("source").notNull()`
    (`fred`|`cboe` provenance, matches the codebase's source-tagging convention). Use the table-callback form to
    declare a COMPOSITE time-leading primary key `primaryKey({ columns: [table.date, table.seriesId] })` (DATA-01 —
    time-leading; this is the D-05 idempotency target). Call `.enableRLS()` to match every other table. Do NOT add
    `id`/uuid — this is a composite-PK observation table, not the uuid+unique shape `cot_observations` uses; do NOT
    touch `rateObservations` (D-02). Then run `bunx drizzle-kit generate` so it emits `0013_macro_observations.sql`,
    `meta/0013_snapshot.json`, and updates `meta/_journal.json`. HAND-REVIEW the emitted SQL: confirm it is a clean
    `CREATE TABLE "macro_observations"` with the composite `PRIMARY KEY ("date","series_id")` and `ALTER TABLE ...
    ENABLE ROW LEVEL SECURITY`, and that it contains NO `DROP TABLE`/`ALTER TABLE "rate_observations"` (this is a new
    table only). If drizzle-kit emits a random-word filename, rename it to `0013_macro_observations.sql` and update
    the `_journal.json` tag to match (established precedent: 0003_broker_tokens, 0004_calendar_events). [BLOCKING]
    schema push: ensure the local `.env` has every `workerConfigSchema` field populated (dummy values fine for
    unrelated fields — Pitfall 7), then run `bun run migrate` against the live database so the table is applied and
    the SQL proven valid BEFORE 14-03's repo/testcontainer work. Re-run once to confirm idempotency (0 changes).
    Do not hand-write the SQL — generate it from the schema so the Drizzle ledger stays consistent.
  </action>
  <verify>
    <automated>bun run migrate && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `bun run migrate` applies cleanly and exits 0; a second run reports no pending migrations (idempotent).
    - `rg -n "macro_observations" packages/adapters/src/postgres/migrations/0013_macro_observations.sql` matches the CREATE TABLE.
    - The emitted SQL declares a composite primary key over `date` and `series_id` (time-leading) and enables row level security.
    - `rg -n "DROP TABLE|rate_observations" packages/adapters/src/postgres/migrations/0013_macro_observations.sql` returns NO match (new table only; rate_observations untouched — D-02).
    - `rg -n "0013_macro_observations" packages/adapters/src/postgres/migrations/meta/_journal.json` shows the ledger entry.
  </acceptance_criteria>
  <done>macro_observations table live with composite (date, series_id) PK + RLS; 0013 migration generated, hand-reviewed, and applied idempotently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema → DB | DDL applied to Postgres; PK/constraint correctness and non-destructiveness are the surface this plan owns |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-04 | Tampering | macro_observations duplicate (series, date) rows | high | mitigate | Composite PK `(date, series_id)` enforces MAC-01 idempotency at the DB; the repo upserts on this key (14-03) |
| T-14-05 | Denial of Service | destructive migration on the live rate_observations table | high | mitigate | Hand-review generated SQL; acceptance criterion negative-checks for DROP TABLE / rate_observations ALTER before the live push (Pitfall 2 discipline, though D-01 avoids any PK change on an existing table) |
| T-14-SC | Tampering | npm/bun installs | low | accept | Phase 14 adds ZERO new dependencies (drizzle-orm/drizzle-kit already present) — no package-legitimacy gate |
</threat_model>

<verification>
- `bun run migrate` applies 0013 to the live schema; re-run applies 0 (idempotent).
- `bun run typecheck` clean (schema compiles).
- Generated SQL hand-reviewed: clean CREATE TABLE, composite PK, RLS, no destructive statements.
</verification>

<success_criteria>
- macro_observations table live with composite (date, series_id) PK + RLS enabled.
- 0013 migration generated from schema, hand-reviewed, applied idempotently; rate_observations untouched.
</success_criteria>

<output>
Create `.planning/phases/14-fred-expansion/14-02-SUMMARY.md` when done.
</output>
