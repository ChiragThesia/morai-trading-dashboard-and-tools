---
phase: 14-fred-expansion
plan: 05
type: execute
wave: 3
depends_on: [14-03, 14-04]
files_modified:
  - apps/worker/src/handlers/fetch-rates.ts
  - apps/worker/src/handlers/fetch-rates.test.ts
  - apps/worker/src/schedule.ts
  - apps/worker/src/schedule.test.ts
  - apps/worker/src/main.ts
autonomous: true
requirements: [MAC-01]

user_setup:
  - service: fred
    why: "The 7 FRED macro series (MAC-01) require a production API key; without it the macro fetch fails loud (D-09). Carried-forward blocker: FRED_API_KEY is unset in prod (STATE.md)."
    env_vars:
      - name: FRED_API_KEY
        source: "https://fredaccount.stlouisfed.org/apikeys — set on the Railway WORKER service and in local .env (D-13). Provided in-session by the user; deliberately NOT stored in any repo file."

must_haves:
  truths:
    - "The fetch-rates handler calls BOTH the existing fetchRate use-case (DGS3MO→rate_observations, unchanged) AND the new fetchMacroSeries use-case in the same run; a macro failure throws so pg-boss marks the job failed"
    - "fetch-rates is scheduled TWICE daily (09:00 ET and 18:30 ET, Mon-Fri, America/New_York) per D-06"
    - "The worker composition root wires makeFredSeriesAdapter + makeCboeVvixAdapter + makePostgresMacroObservationsRepo + makeFetchMacroSeries into fetchRatesHandler"
  artifacts:
    - path: "apps/worker/src/handlers/fetch-rates.ts"
      provides: "extended makeFetchRatesHandler calling fetchMacroSeriesUseCase additively (D-02: existing rate path unchanged)"
      contains: "fetchMacroSeriesUseCase"
    - path: "apps/worker/src/schedule.ts"
      provides: "two fetch-rates cron registrations (09:00 + 18:30 ET, D-06)"
      contains: "30 18"
  key_links:
    - from: "apps/worker/src/main.ts"
      to: "apps/worker/src/handlers/fetch-rates.ts"
      via: "fetchMacroSeriesUseCase injected into makeFetchRatesHandler deps"
      pattern: "fetchMacroSeriesUseCase"
    - from: "apps/worker/src/main.ts"
      to: "packages/adapters/src/http/fred.ts"
      via: "makeFredSeriesAdapter wired with config.FRED_API_KEY"
      pattern: "makeFredSeriesAdapter"
---

<objective>
Wire the macro ingestion into the worker: extend the `fetch-rates` handler to call `fetchMacroSeries` additively
(the existing DGS3MO→`rate_observations` path stays byte-for-byte, D-02), add the second daily cron run (D-06),
and compose the FRED-series adapter + CBOE VVIX adapter + macro repo + `fetchMacroSeries` use-case in
`apps/worker/src/main.ts`.

Purpose: this is where MAC-01 actually runs on a schedule. One `fetch-rates` job now performs two independent
responsibilities; the twice-daily cadence catches same-day VIXCLS/treasury prints (evening) and SOFR's T+1 lag
(morning). The prod `FRED_API_KEY` is an operator prerequisite (D-13) surfaced in user_setup — the executor must
NOT invent or commit a key.
Output: macro fetch running inside fetch-rates on the two-run daily cron.

Implements MAC-01 (scheduled ingestion), per D-02/D-06/D-07/D-08/D-09/D-13.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-fred-expansion/14-CONTEXT.md
@.planning/phases/14-fred-expansion/14-RESEARCH.md
@.planning/phases/14-fred-expansion/14-PATTERNS.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/tdd.md

# Files this plan extends + analogs:
@apps/worker/src/handlers/fetch-rates.ts
@apps/worker/src/handlers/fetch-cot.ts
@apps/worker/src/schedule.ts
</context>

## Artifacts this phase produces (this plan)

| Symbol / file | Layer |
|---|---|
| extended `makeFetchRatesHandler` (macro call) — `apps/worker/src/handlers/fetch-rates.ts` | worker |
| second `fetch-rates` cron `30 18 * * 1-5` — `apps/worker/src/schedule.ts` | worker |
| worker composition wiring (FRED-series adapter, VVIX adapter, macro repo, fetchMacroSeries) — `apps/worker/src/main.ts` | worker |

Consumes: `makeFredSeriesAdapter`, `makeCboeVvixAdapter`, `makePostgresMacroObservationsRepo` (14-03); `makeFetchMacroSeries` (14-04); `FRED_API_KEY` config (existing). See 14-01 for the full phase manifest.

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: extend fetch-rates handler to run the macro fetch</name>
  <files>apps/worker/src/handlers/fetch-rates.ts, apps/worker/src/handlers/fetch-rates.test.ts</files>
  <read_first>
    - apps/worker/src/handlers/fetch-rates.ts — the existing thin handler: array-guard (pg-boss v12 undefined element, T-02-18) → `isNyseHoliday(now)` gate → `fetchRateUseCase()` → throw on err. The `FetchRatesHandlerDeps` type.
    - apps/worker/src/handlers/fetch-cot.ts — the sibling thin handler for a second use-case call pattern.
    - apps/worker/src/handlers/fetch-rates.test.ts — existing handler test structure (fake use-case, holiday gate, throw-on-err).
    - .planning/phases/14-fred-expansion/14-CONTEXT.md D-02 (existing rate path unchanged), D-07 (macro failure must surface — throw so pg-boss marks failed).
  </read_first>
  <behavior>
    - On a normal weekday the handler calls fetchRateUseCase() AND fetchMacroSeriesUseCase(); both run.
    - The existing rate path is unchanged: a fetchRate err still throws (regression).
    - When fetchMacroSeriesUseCase returns err, the handler throws (so pg-boss marks the job failed → /api/status lastErr, D-07).
    - On an NYSE holiday the handler skips BOTH use-cases and returns (holiday gate preserved for the Mon-Fri cadence).
    - The array-guard for an undefined pg-boss job element is preserved.
  </behavior>
  <action>
    Add `fetchMacroSeriesUseCase: () => Promise<{ ok: boolean; error?: { message: string } }>` to
    `FetchRatesHandlerDeps` (mirror the existing `fetchRateUseCase` local type). In the handler body, AFTER the
    existing holiday gate + `fetchRateUseCase()` call (leave that call and its throw-on-err untouched, D-02), add a
    call to `fetchMacroSeriesUseCase()`; if its Result is not ok, throw an `Error` with
    `result.error?.message ?? "fetchMacroSeries use-case failed"` so pg-boss marks the job failed (D-07). Keep the
    handler thin (architecture-boundaries §3 — zero business logic; orchestration lives in the use-case). Keep the
    same NYSE-holiday gate covering both calls (D-06 cadence is still Mon-Fri). Write the RED tests FIRST (extend
    fetch-rates.test.ts: both-called, macro-err→throw, holiday→neither-called, rate-err→throw regression) and watch
    them fail before implementing.
  </action>
  <verify>
    <automated>bun run test -- --project apps/worker fetch-rates</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test -- --project apps/worker fetch-rates` passes: both-use-cases-called, macro-err→throw, holiday→skip-both, rate-err→throw regression.
    - `rg -n "fetchMacroSeriesUseCase" apps/worker/src/handlers/fetch-rates.ts` matches in both the deps type and the call site.
    - The existing `fetchRateUseCase` call + its throw-on-err are unchanged (D-02 — verified by the rate-err regression test).
    - `bun run typecheck` clean.
  </acceptance_criteria>
  <done>fetch-rates handler runs the macro fetch alongside the unchanged rate fetch and fails loud on macro error.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: schedule fetch-rates twice daily (D-06)</name>
  <files>apps/worker/src/schedule.ts, apps/worker/src/schedule.test.ts</files>
  <read_first>
    - apps/worker/src/schedule.ts — the fetch-rates schedule call (currently a single `schedule("fetch-rates", "0 9 * * 1-5", null, { tz: "America/New_York" })`, ~line 96), the createQueue phase, and the count comments ("6 jobs scheduled").
    - apps/worker/src/schedule.test.ts — how a job's cron is asserted (the fetch-rates entries at lines ~65/78, the sync-fills cron assertion at ~136 as the assertion style).
    - .planning/phases/14-fred-expansion/14-CONTEXT.md D-06 (two runs: 09:00 + 18:30 ET, Mon-Fri).
  </read_first>
  <behavior>
    - registerAllJobs schedules fetch-rates with cron "0 9 * * 1-5" AND cron "30 18 * * 1-5", both tz America/New_York.
    - The fetch-rates queue is still created exactly once (a single queue, two cron schedules).
    - No other job's schedule changes.
  </behavior>
  <action>
    In `schedule.ts`, keep the fetch-rates `createQueue("fetch-rates")` call as-is (one queue). Keep the existing
    `schedule("fetch-rates", "0 9 * * 1-5", null, { tz: "America/New_York" })` and ADD a second
    `schedule("fetch-rates", "30 18 * * 1-5", null, { tz: "America/New_York" })` immediately after it (D-06 — 18:30
    ET evening run; both idempotent, safe on every boot). Update the file's header/count comments to reflect the
    added scheduled cron (the scheduled-cron count increases by one; queue count unchanged at 10). Do NOT add a new
    queue or handler. Update `schedule.test.ts` FIRST (RED) to assert BOTH fetch-rates crons are registered
    (`0 9 * * 1-5` and `30 18 * * 1-5`, tz America/New_York) using the existing cron-assertion style, and watch it
    fail before adding the second schedule call.
  </action>
  <verify>
    <automated>bun run test -- --project apps/worker schedule</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test -- --project apps/worker schedule` passes and asserts both fetch-rates crons (`0 9 * * 1-5` + `30 18 * * 1-5`, tz America/New_York).
    - `rg -n "30 18 \* \* 1-5" apps/worker/src/schedule.ts` matches the second fetch-rates cron.
    - Exactly one `createQueue("fetch-rates")` remains (single queue, two schedules).
    - `bun run typecheck` clean.
  </acceptance_criteria>
  <done>fetch-rates runs twice daily (09:00 + 18:30 ET) per D-06; a RED-first schedule test locks both crons.</done>
</task>

<task type="execute">
  <name>Task 3: worker composition wiring (FRED-series + VVIX adapters + macro repo + use-case)</name>
  <files>apps/worker/src/main.ts</files>
  <read_first>
    - apps/worker/src/main.ts — the existing FRED/rate wiring (`makeFredRateAdapter` ~line 117, `makeFetchRateUseCase` ~line 143, `makeFetchRatesHandler` ~line 205), the COT wiring block (`makeCftcCotAdapter`/`makePostgresCotObservationsRepo`/`makeFetchCot` ~lines 368-376) as the composition pattern to mirror, and the config usage (`config.FRED_API_KEY`, `config.CBOE_USER_AGENT`/`userAgent` for cboe adapter — reuse whatever the existing `makeCboeChainAdapter` uses for userAgent).
    - apps/worker/src/config.ts — `FRED_API_KEY` is `z.string().optional()`; the CBOE user-agent config field used by the existing cboe adapter.
    - packages/adapters/src/index.ts — barrel names `makeFredSeriesAdapter`, `makeCboeVvixAdapter`, `makePostgresMacroObservationsRepo` (added in 14-03).
    - packages/core/src/index.ts — `makeFetchMacroSeries` (added in 14-04).
  </read_first>
  <action>
    In the worker composition root (this is pure wiring — architecture-boundaries §6; TDD-exempt), near the existing
    fred/rate wiring: construct `const fetchFredSeries = makeFredSeriesAdapter({ fetch: globalThis.fetch, apiKey:
    config.FRED_API_KEY })`; `const fetchVvixQuote = makeCboeVvixAdapter({ fetch: globalThis.fetch, userAgent:
    <the same userAgent the existing makeCboeChainAdapter is constructed with> })`; `const macroObsRepo =
    makePostgresMacroObservationsRepo(db)`; `const fetchMacroSeriesUseCase = makeFetchMacroSeries({ fetchFredSeries,
    fetchVvixQuote, persistMacroObservation: macroObsRepo.insertMacroObservation })` (rely on the use-case's default
    7-FRED-id list — do not re-list them here). Then pass `fetchMacroSeriesUseCase` into the existing
    `makeFetchRatesHandler({ fetchRateUseCase, fetchMacroSeriesUseCase, now: () => new Date() })` call. Add the
    needed imports from `@morai/adapters` and `@morai/core`. Do NOT change the rate/BSM wiring (`makeFredRateAdapter`,
    `makeFetchRateUseCase`, `computeBsmGreeks`) — D-02. Update the boot `console.warn` count line only if it states a
    scheduled-job count that changed.
  </action>
  <verify>
    <automated>bun run typecheck && bun run test -- --project apps/worker</automated>
  </verify>
  <acceptance_criteria>
    - `bun run typecheck` clean; `bun run test -- --project apps/worker` green (whole worker suite).
    - `rg -n "makeFredSeriesAdapter|makeCboeVvixAdapter|makePostgresMacroObservationsRepo|makeFetchMacroSeries" apps/worker/src/main.ts` shows all four wired.
    - `rg -n "fetchMacroSeriesUseCase" apps/worker/src/main.ts` shows it passed into makeFetchRatesHandler.
    - The existing `makeFredRateAdapter` + `makeFetchRateUseCase` wiring is unchanged (D-02).
  </acceptance_criteria>
  <done>Worker composes the macro adapters + repo + use-case and injects it into the fetch-rates handler; rate/BSM wiring untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker env → adapters | FRED_API_KEY read once at the composition root (Zod-parsed config), flows inward typed |
| cron → handler | pg-boss triggers the job; a failed macro fetch must be observable, not silent |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-12 | Information Disclosure | FRED_API_KEY handling in worker | high | mitigate | Key read once from Zod-parsed config at main.ts; passed to the adapter which never logs it (T-14-06); never committed (D-13 — operator sets it on Railway + local .env) |
| T-14-09 | Denial of Service | silent partial-fetch hole | medium | mitigate | Handler throws on macro Result err (D-07) → pg-boss marks the job failed → /api/status lastErr; next scheduled run self-heals gaps (D-05) |
| T-14-13 | Tampering | regressing the BSM rate path while extending the job | high | mitigate | fetchRate call + its throw-on-err left byte-for-byte (D-02); rate-err regression test in Task 1 proves it; whole worker suite must stay green |
| T-14-SC | Tampering | npm/bun installs | low | accept | Zero new dependencies — no legitimacy gate |
</threat_model>

<verification>
- `bun run test -- --project apps/worker` green (fetch-rates handler + schedule + whole worker suite).
- `bun run typecheck` + `bun run lint` clean.
- OPERATOR (D-13, tracked, not blocking build/test): set FRED_API_KEY on the Railway worker + local .env before the live-fetch UAT in /gsd-verify-work.
</verification>

<success_criteria>
- fetch-rates handler runs the macro fetch additively (rate/BSM path unchanged, D-02) and fails loud on macro error.
- fetch-rates scheduled twice daily (09:00 + 18:30 ET, D-06); schedule test locks both crons.
- Worker composition wires the macro adapters + repo + use-case; FRED_API_KEY surfaced as an operator step (D-13).
</success_criteria>

<output>
Create `.planning/phases/14-fred-expansion/14-05-SUMMARY.md` when done.
</output>
