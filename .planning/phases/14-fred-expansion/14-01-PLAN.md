---
phase: 14-fred-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture/jobs.md
  - docs/architecture/data-model.md
  - packages/contracts/src/macro.ts
  - packages/contracts/src/macro.test.ts
  - packages/contracts/src/index.ts
  - packages/core/src/journal/application/ports.ts
  - packages/core/src/journal/index.ts
  - packages/core/src/index.ts
autonomous: true
requirements: [MAC-01, MAC-02]

must_haves:
  truths:
    - "The macro Zod contract parses a valid {seriesId: [{time,value}]} payload and rejects a malformed one"
    - "macroQuery rejects days > 1825 and an unknown series id; accepts an omitted-params request"
    - "Core exposes ForFetchingFredSeries, ForFetchingVvixQuote, ForPersistingMacroObservation, ForReadingMacroObservations as function-type ports with a MacroObservationRow domain type"
    - "docs/architecture/jobs.md records fetch-rates as a two-run daily cron (09:00 + 18:30 ET) and no longer states the stale 0 7 cron"
  artifacts:
    - path: "packages/contracts/src/macro.ts"
      provides: "macroSeriesPoint + macroResponse (z.record of arrays) + macroQuery params + MACRO_SERIES_IDS (D-10/D-11)"
      contains: "macroResponse"
      min_lines: 30
    - path: "packages/core/src/journal/application/ports.ts"
      provides: "MacroObservationRow + 4 macro ports (fetch FRED, fetch VVIX, persist, read)"
      contains: "ForFetchingFredSeries"
    - path: "docs/architecture/jobs.md"
      provides: "corrected fetch-rates cron + macro-series expansion note (D-06, docs-before-code)"
      contains: "18:30"
  key_links:
    - from: "packages/contracts/src/index.ts"
      to: "packages/contracts/src/macro.ts"
      via: "barrel re-export of macroSeriesPoint/macroResponse/macroQuery/MACRO_SERIES_IDS"
      pattern: "from \"./macro.ts\""
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/journal/application/ports.ts"
      via: "barrel re-export of the 4 macro ports + MacroObservationRow"
      pattern: "ForFetchingFredSeries"
---

<objective>
Lay the Phase 14 macro foundation and satisfy the docs-before-code rule (workflow.md) for the
new table + job change: update `docs/architecture/jobs.md` and `docs/architecture/data-model.md`
FIRST, then create the shared macro Zod contract (`packages/contracts/src/macro.ts`) and the core
driven ports + domain type (`MacroObservationRow`, `ForFetchingFredSeries`, `ForFetchingVvixQuote`,
`ForPersistingMacroObservation`, `ForReadingMacroObservations`).

Purpose: every downstream wave (adapters, repo, use-cases, job, route, MCP, FE) compiles against
these interfaces. Interface-first so parallel waves never invent divergent shapes; docs-first so the
architecture record leads the code (new `macro_observations` table + expanded `fetch-rates` cadence).
Output: corrected architecture docs + macro contract schemas + core macro ports.

Implements MAC-02 (shared contract) and the MAC-01 port surface, per 14-CONTEXT D-01/D-06/D-10/D-11/D-14.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-fred-expansion/14-CONTEXT.md
@.planning/phases/14-fred-expansion/14-RESEARCH.md
@.planning/phases/14-fred-expansion/14-PATTERNS.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/typescript.md
@.claude/rules/docs.md

# Direct analogs to mirror:
@packages/contracts/src/cot.ts
@packages/contracts/src/status.ts
@packages/core/src/journal/application/ports.ts
</context>

## Artifacts this phase produces (canonical manifest)

The full Phase 14 output across all seven plans. Downstream plans build against these names.

| Symbol / file | Plan | Layer |
|---|---|---|
| `macroSeriesPoint`, `macroResponse`, `macroQuery`, `MACRO_SERIES_IDS`, `macroSeriesId` — `packages/contracts/src/macro.ts` | 14-01 | contracts |
| `MacroObservationRow` domain type — core ports | 14-01 | core |
| `ForFetchingFredSeries`, `ForFetchingVvixQuote`, `ForPersistingMacroObservation`, `ForReadingMacroObservations` ports | 14-01 | core |
| `macroObservations` pgTable + `0013_macro_observations.sql` migration | 14-02 | adapters |
| `makeFredSeriesAdapter` (+ extracted shared FRED fetch helper) — `packages/adapters/src/http/fred.ts` | 14-03 | adapters |
| `makeCboeVvixAdapter` — `packages/adapters/src/http/cboe-vvix.ts` | 14-03 | adapters |
| `makePostgresMacroObservationsRepo` — `packages/adapters/src/postgres/repos/macro-observations.ts` | 14-03 | adapters |
| `makeMemoryMacroObservationsRepo` — `packages/adapters/src/memory/macro-observations.ts` (twin) | 14-03 | adapters |
| `runMacroObservationsContractTests` — `packages/adapters/src/__contract__/macro-observations.contract.ts` | 14-03 | adapters (test) |
| `makeFetchMacroSeries` use-case + `ForRunningFetchMacroSeries` — `packages/core/src/journal/application/fetchMacroSeries.ts` | 14-04 | core |
| `makeGetMacroUseCase` + `ForRunningGetMacro` + `MacroSeriesQuery` — `packages/core/src/journal/application/getMacro.ts` | 14-04 | core |
| extended `makeFetchRatesHandler` (macro call) — `apps/worker/src/handlers/fetch-rates.ts` + 2× `fetch-rates` cron | 14-05 | worker |
| `GET /api/analytics/macro` route | 14-06 | server |
| `get_macro` MCP tool (`registerGetMacroTool`) | 14-06 | server |
| `useMacro` hook + `MacroCard` component + Overview mount | 14-07 | web |

## Multi-Source Coverage Audit

Every source item is COVERED by a plan. No item is omitted or simplified.

| Source | Item | Covered by |
|---|---|---|
| GOAL | fetch-rates extended to 7 FRED series + VVIX via CBOE into macro_observations | 14-02, 14-03, 14-04, 14-05 |
| GOAL | GET /api/analytics/macro + MCP get_macro over shared contract | 14-01, 14-04, 14-06 |
| GOAL | prod FRED_API_KEY set (operator) | 14-05 (user_setup, D-13) |
| REQ | MAC-01 (macro_observations rows for 8 series, idempotent) | 14-02, 14-03, 14-04, 14-05 |
| REQ | MAC-02 (route + MCP series, MCP-02) | 14-01, 14-04, 14-06 |
| REQ | MCP-02 (route + MCP same change, shared Zod) | 14-06 |
| RESEARCH | Parameterized no-fallback FRED adapter (extract shared helper) | 14-03 |
| RESEARCH | CBOE VVIX endpoint (_VVIX.json) spot-resolution + UTC timestamp | 14-03 |
| RESEARCH | Promise.allSettled per-series independence (refresh-tokens precedent) | 14-04 |
| RESEARCH | z.record(z.string(), z.array(...)) two-arg contract (status.ts precedent) | 14-01 |
| RESEARCH | Composite time-leading PK (DATA-01) | 14-02 |
| RESEARCH | Pitfall 1 (no /100 for index series), Pitfall 3 (stale jobs.md cron), Pitfall 7 (migrate env) | 14-01, 14-02, 14-03 |
| CONTEXT | D-01 new macro_observations table (NOT widen rate_observations) | 14-02 |
| CONTEXT | D-02 rate_observations / readRate / BSM path untouched | 14-03, 14-04 |
| CONTEXT | D-03 VVIX stored series_id='VVIX' | 14-03 |
| CONTEXT | D-04 5y backfill, chunk ≤2,000 | 14-03, 14-04 |
| CONTEXT | D-05 self-healing incremental fetch (max(date)+1), idempotent upsert | 14-03, 14-04 |
| CONTEXT | D-06 two daily cron runs (09:00 + 18:30 ET, Mon-Fri) | 14-01 (docs), 14-05 |
| CONTEXT | D-07 best-effort + fail-loud finish | 14-04 |
| CONTEXT | D-08 no in-handler retry (pg-boss retryLimit) | 14-05 |
| CONTEXT | D-09 macro fetch hard-requires FRED_API_KEY (fail loud) | 14-03, 14-04 |
| CONTEXT | D-10 shared contract {seriesId:[{time,value}]}, time ascending | 14-01, 14-04 |
| CONTEXT | D-11 default 90d window, optional days (max 1825) + series CSV filter | 14-01, 14-04, 14-06 |
| CONTEXT | D-12 MacroCard wired in-phase (replace Overview stub) | 14-07 |
| CONTEXT | D-13 FRED_API_KEY operator step surfaced (not done by executor) | 14-05 |
| CONTEXT | D-14 store RAW values (no /100) | 14-03 |
| CONTEXT | D-15 VVIX via _VVIX.json, date from UTC timestamp | 14-03 |
| CONTEXT | Docs-first jobs.md cron fix + macro schema docs | 14-01 |

Excluded (not gaps): historical CSV bulk backfill (RESEARCH tertiary, optional future); BSM rate-proxy change (RESEARCH Open Q3, explicitly out of scope); widen-rate_observations (RESEARCH recommendation superseded by D-01).

<tasks>

<task type="execute">
  <name>Task 1: Docs-before-code — fetch-rates cadence + macro_observations schema</name>
  <files>docs/architecture/jobs.md, docs/architecture/data-model.md</files>
  <read_first>
    - docs/architecture/jobs.md — find the fetch-rates entry (currently documents the stale `0 7 * * 1-5` cron; actual registered cron is `0 9 * * 1-5` per apps/worker/src/schedule.ts).
    - docs/architecture/data-model.md — find the observation-tables section (rate_observations, cot_observations) to mirror the documentation style for a new table.
    - .planning/phases/14-fred-expansion/14-CONTEXT.md §Decisions (D-01, D-06, D-14) and §Post-Research Addendum (Docs-first item).
    - .planning/phases/14-fred-expansion/14-RESEARCH.md Pitfall 3 (confirmed stale cron drift).
  </read_first>
  <action>
    Update `docs/architecture/jobs.md` fetch-rates entry: (1) correct the documented cron from the stale
    `0 7 * * 1-5` to reflect the actual two-run daily cadence `0 9 * * 1-5` and `30 18 * * 1-5` (09:00 ET
    and 18:30 ET, Mon-Fri, `America/New_York`) per D-06 — the evening run catches same-day VIXCLS/treasury
    publications, the morning run catches SOFR's T+1 lag; (2) note fetch-rates now performs TWO independent
    responsibilities in one run — the existing DGS3MO→rate_observations fetch (unchanged, BSM risk-free rate)
    PLUS a new expanded macro fetch (7 FRED series + VVIX) into the new `macro_observations` table; (3) state
    the macro fetch is best-effort per series with a fail-loud finish (D-07) and hard-requires FRED_API_KEY
    (D-09). Update `docs/architecture/data-model.md`: add a `macro_observations` section describing columns
    `series_id text NOT NULL`, `date date NOT NULL`, `value numeric NOT NULL` (RAW as reported — no /100, D-14),
    `source text` (`fred`|`cboe`), composite time-leading primary key `(date, series_id)` (DATA-01), RLS enabled;
    call out that this is a NEW table (D-01) and that `rate_observations` stays single-series for the BSM path
    (D-02 — unchanged). Follow Hemingway style (docs.md) and update `docs/TOPIC-MAP.md` only if a new doc file is
    created (none expected — these are edits to existing sections). Do not touch any code in this task.
  </action>
  <verify>
    <automated>grep -n "18:30\|30 18" docs/architecture/jobs.md && grep -n "macro_observations" docs/architecture/data-model.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/architecture/jobs.md` fetch-rates entry names both cron runs (09:00 and 18:30 ET) and no longer presents `0 7 * * 1-5` as the live cron.
    - `docs/architecture/data-model.md` documents `macro_observations` with a composite `(date, series_id)` PK, `value` stored RAW (no /100), and `source` column.
    - Both docs state `rate_observations` / the BSM path is unchanged (D-02).
    - `bun run lint` is unaffected (docs-only change; no code touched).
  </acceptance_criteria>
  <done>jobs.md + data-model.md reflect the two-run cadence and the new macro_observations table BEFORE any code lands.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: macro Zod contract (macroSeriesPoint + macroResponse + macroQuery)</name>
  <files>packages/contracts/src/macro.ts, packages/contracts/src/macro.test.ts, packages/contracts/src/index.ts</files>
  <read_first>
    - packages/contracts/src/cot.ts — header-comment + export style + MCP-02 single-schema note to mirror.
    - packages/contracts/src/status.ts — the two-argument `z.record(z.string(), ...)` form (Zod v4; one-arg is a TS2554 error).
    - packages/contracts/src/index.ts — barrel export style (value + type exports per module; see the cot block).
    - .planning/phases/14-fred-expansion/14-CONTEXT.md D-10 (response shape) + D-11 (query params) + Claude's Discretion (`time` is YYYY-MM-DD).
  </read_first>
  <behavior>
    - macroResponse parses a valid map `{ DFF: [{ time: "2026-04-02", value: 3.83 }], VVIX: [{ time: "2026-04-02", value: 89.0 }] }` and the empty map `{}`.
    - macroSeriesPoint rejects a `time` that is not a YYYY-MM-DD date string and a `value` that is not a number.
    - macroQuery parses `{}` (all params omitted) successfully; parses `{ days: "90" }` coercing to number 90; rejects `days` > 1825; rejects a `series` CSV containing an id not in MACRO_SERIES_IDS.
    - macroQuery parses a valid `series` CSV (e.g. "DFF,VVIX") into a value the route can use to filter.
  </behavior>
  <action>
    Create `macroSeriesPoint` as `z.object({ time: z.string().date(), value: z.number() })` (D-10: `time` is the
    YYYY-MM-DD date string, matching FRED granularity + COT `asOf` precedent). Export `macroResponse =
    z.record(z.string(), z.array(macroSeriesPoint))` using the TWO-argument `z.record` form (status.ts precedent).
    Export a canonical `MACRO_SERIES_IDS` as-const tuple of the eight ids: `DFF`, `DGS1MO`, `DGS3MO`, `SOFR`,
    `T10Y2Y`, `T10Y3M`, `VIXCLS`, `VVIX`; derive `macroSeriesId = z.enum(MACRO_SERIES_IDS)`. Export `macroQuery`
    (D-11) as a `z.object` with `days` = `z.coerce.number().int().positive().max(1825).optional()` and `series`
    optional — model `series` as a string that, when present, is split on commas and refined so every token is a
    member of MACRO_SERIES_IDS (reject on unknown id); expose the parsed result as a string array the route/use-case
    can consume. Export inferred types `MacroSeriesPoint`, `MacroResponse`, `MacroQuery`. Document at the top that
    this is the ONE schema shared by `GET /api/analytics/macro` and the `get_macro` MCP tool (MCP-02). Add the
    barrel block to `packages/contracts/src/index.ts` mirroring the cot block (value exports + type exports).
    Write the round-trip + rejection tests FIRST and watch them fail (RED) before macro.ts exists.
  </action>
  <verify>
    <automated>bun run test -- packages/contracts/src/macro.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test -- packages/contracts/src/macro.test.ts` passes with valid-map, empty-map, bad-point rejection, days>1825 rejection, and unknown-series rejection cases.
    - `bun run typecheck` is clean (types flow from `z.infer`; no `any`/`as`/`!`).
    - `rg -n "from \"./macro.ts\"" packages/contracts/src/index.ts` matches (barrel wired).
    - `rg -n "MACRO_SERIES_IDS" packages/contracts/src/macro.ts` shows all eight series ids including `VVIX` and `VIXCLS`.
  </acceptance_criteria>
  <done>macroSeriesPoint/macroResponse/macroQuery/MACRO_SERIES_IDS exported from @morai/contracts, round-trip + param-validation tested.</done>
</task>

<task type="execute">
  <name>Task 3: core macro ports + MacroObservationRow domain type</name>
  <files>packages/core/src/journal/application/ports.ts, packages/core/src/journal/index.ts, packages/core/src/index.ts</files>
  <read_first>
    - packages/core/src/journal/application/ports.ts — the `RateObservation` + `ForFetchingRate`/`ForPersistingRate`/`ForReadingRate` block AND the Phase 13 `CotReport`/`CotObservationRow` + COT ports block; mirror that trio/section style. Reuse existing `FetchError` and `StorageError`.
    - packages/core/src/journal/index.ts and packages/core/src/index.ts — how port TYPES + domain types are re-exported outward (see the CotObservationRow / ForReadingCotObservations exports).
    - .planning/phases/14-fred-expansion/14-CONTEXT.md D-02 (do NOT touch ForFetchingRate/ForReadingRate/RateObservation), D-03 (VVIX seriesId), D-14 (raw value).
  </read_first>
  <action>
    Append a "Phase 14: FRED macro expansion" section to `ports.ts`. Add domain type `MacroObservationRow`
    (readonly fields: `seriesId` string, `date` string YYYY-MM-DD, `value` number stored RAW per D-14, `source`
    `"fred" | "cboe"`). Define four function-type ports (`ForVerbingNoun`, architecture-boundaries §5), all reusing
    the existing `FetchError`/`StorageError`: `ForFetchingFredSeries = (seriesId: string) => Promise<Result<
    MacroObservationRow, FetchError>>` (no fabricated fallback — a missing/errored series returns err, D-09);
    `ForFetchingVvixQuote = () => Promise<Result<MacroObservationRow, FetchError>>` (returns seriesId `VVIX`,
    source `cboe`, D-03); `ForPersistingMacroObservation = (row: MacroObservationRow) => Promise<Result<void,
    StorageError>>` (idempotent upsert at the adapter, D-05); `ForReadingMacroObservations = () => Promise<Result<
    ReadonlyArray<MacroObservationRow>, StorageError>>` (bulk, unfiltered — grouping + windowing happens in
    getMacro, 14-04). Types only — no implementations. Do NOT modify `ForFetchingRate`, `ForPersistingRate`,
    `ForReadingRate`, or `RateObservation` (D-02 — the BSM path is frozen). Re-export `MacroObservationRow` and all
    four ports through `journal/index.ts` and `core/index.ts` next to the existing Cot/Rate exports.
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `bun run typecheck` is clean.
    - `rg -n "ForFetchingFredSeries|ForFetchingVvixQuote|ForPersistingMacroObservation|ForReadingMacroObservations" packages/core/src/index.ts` shows all four exported from the core barrel.
    - `rg -n "MacroObservationRow" packages/core/src/index.ts` shows the domain type exported.
    - `rg -n "ForFetchingRate|ForReadingRate|RateObservation" packages/core/src/journal/application/ports.ts` still matches the ORIGINAL rate ports (D-02: untouched — no signature change).
  </acceptance_criteria>
  <done>Four macro ports + MacroObservationRow exported from @morai/core; the rate/BSM port trio is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contract → consumers | The macro Zod schema is the only shape crossing into routes/MCP/FE; a wrong shape must fail typecheck, not runtime |
| user query → macroQuery | `days`/`series` are user-controlled; validated here before any use-case sees them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Tampering | macroQuery `series` CSV | medium | mitigate | z.enum(MACRO_SERIES_IDS) refinement rejects any unknown series id at the boundary before it reaches storage filtering |
| T-14-02 | Tampering | macroQuery `days` | low | mitigate | `z.coerce.number().int().positive().max(1825)` caps the window (D-11) — no unbounded scan request |
| T-14-03 | Information Disclosure | shared contract stability (MCP-02) | low | mitigate | ONE macroResponse schema for route + MCP; a one-sided field change fails `bun run typecheck` |
| T-14-SC | Tampering | npm/bun installs | low | accept | Phase 14 adds ZERO new dependencies (zod already present) — no package-legitimacy gate required |
</threat_model>

<verification>
- `bun run test -- packages/contracts/src/macro.test.ts` green (contract round-trip + param validation).
- `bun run typecheck` clean across contracts + core (ports + contract compile).
- `bun run lint` clean (no `any`/`as`/`!`).
- Docs greps in Task 1 pass (jobs.md cadence + data-model.md macro_observations).
</verification>

<success_criteria>
- docs/architecture/jobs.md + data-model.md updated FIRST (two-run cadence + macro_observations table).
- macroSeriesPoint/macroResponse/macroQuery/MACRO_SERIES_IDS exported from @morai/contracts, tested.
- MacroObservationRow + 4 macro ports exported from @morai/core; rate/BSM ports untouched (D-02).
</success_criteria>

<output>
Create `.planning/phases/14-fred-expansion/14-01-SUMMARY.md` when done.
</output>
