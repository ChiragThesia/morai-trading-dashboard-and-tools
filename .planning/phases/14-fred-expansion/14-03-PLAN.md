---
phase: 14-fred-expansion
plan: 03
type: tdd
wave: 2
depends_on: [14-01, 14-02]
files_modified:
  - packages/adapters/src/http/fred.ts
  - packages/adapters/src/http/fred.test.ts
  - packages/adapters/src/http/cboe-vvix.ts
  - packages/adapters/src/http/cboe-vvix.test.ts
  - packages/adapters/src/postgres/repos/macro-observations.ts
  - packages/adapters/src/postgres/repos/macro-observations.contract.test.ts
  - packages/adapters/src/memory/macro-observations.ts
  - packages/adapters/src/memory/macro-observations.contract.test.ts
  - packages/adapters/src/__contract__/macro-observations.contract.ts
  - packages/adapters/src/index.ts
autonomous: true
requirements: [MAC-01]

must_haves:
  truths:
    - "makeFredSeriesAdapter fetches a series by id, filters '.' rows, returns the raw value with NO /100 division, and returns err (no fabricated fallback) on missing key / network / all-'.' (D-09/D-14)"
    - "makeCboeVvixAdapter parses _VVIX.json, resolves current_price ?? close ?? prev_day_close, derives the date from the UTC timestamp, and returns seriesId VVIX / source cboe"
    - "The macro-observations repo upserts on (date, series_id) so a second same-key write revises the value (onConflictDoUpdate) and reads back all rows; the same contract passes against the in-memory twin"
    - "The existing makeFredRateAdapter (DGS3MO, /100, silent fallback) is unchanged (D-02)"
  artifacts:
    - path: "packages/adapters/src/http/fred.ts"
      provides: "extracted shared FRED fetch/parse/'.'-filter helper + makeFredSeriesAdapter (no-fallback, raw value)"
      contains: "makeFredSeriesAdapter"
    - path: "packages/adapters/src/http/cboe-vvix.ts"
      provides: "makeCboeVvixAdapter implementing ForFetchingVvixQuote"
      contains: "makeCboeVvixAdapter"
    - path: "packages/adapters/src/postgres/repos/macro-observations.ts"
      provides: "makePostgresMacroObservationsRepo (upsert onConflictDoUpdate + read all)"
      contains: "makePostgresMacroObservationsRepo"
    - path: "packages/adapters/src/__contract__/macro-observations.contract.ts"
      provides: "runMacroObservationsContractTests shared suite (idempotent upsert + multi-series)"
      contains: "runMacroObservationsContractTests"
  key_links:
    - from: "packages/adapters/src/index.ts"
      to: "packages/adapters/src/http/fred.ts"
      via: "barrel export of makeFredSeriesAdapter"
      pattern: "makeFredSeriesAdapter"
    - from: "packages/adapters/src/postgres/repos/macro-observations.contract.test.ts"
      to: "packages/adapters/src/__contract__/macro-observations.contract.ts"
      via: "runMacroObservationsContractTests against the Postgres repo (testcontainers)"
      pattern: "runMacroObservationsContractTests"
---

<objective>
Build the macro data adapters: extend `fred.ts` with a parameterized no-fallback `makeFredSeriesAdapter`
(extracting the shared fetch/parse/`.`-filter helper), add a new `makeCboeVvixAdapter` for the CBOE VVIX
quote, and add the `macro_observations` Postgres repo + in-memory twin under one shared contract test suite.

Purpose: the adapters are the external-HTTP + storage boundary for MAC-01 — Zod-parsed before core, raw values
(no /100, D-14), no fabricated fallback (D-09), idempotent upsert on `(date, series_id)` (D-05). All four adapter
artifacts live in one plan so the single `packages/adapters/src/index.ts` barrel is edited exactly once.
Output: FRED-series + VVIX HTTP adapters + Postgres/memory macro-observations repos + contract suite.

Implements MAC-01 (fetch + persist + idempotency), per D-05/D-09/D-14/D-15.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-fred-expansion/14-CONTEXT.md
@.planning/phases/14-fred-expansion/14-RESEARCH.md
@.planning/phases/14-fred-expansion/14-PATTERNS.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/tdd.md
@.claude/rules/typescript.md

# Direct analogs to mirror:
@packages/adapters/src/http/fred.ts
@packages/adapters/src/http/cboe.ts
@packages/adapters/src/postgres/repos/cot-observations.ts
@packages/adapters/src/memory/cot-observations.ts
@packages/adapters/src/__contract__/cot-observations.contract.ts
</context>

## Artifacts this phase produces (this plan)

| Symbol / file | Layer |
|---|---|
| shared FRED fetch helper + `makeFredSeriesAdapter` — `packages/adapters/src/http/fred.ts` | adapters |
| `makeCboeVvixAdapter` — `packages/adapters/src/http/cboe-vvix.ts` | adapters |
| `makePostgresMacroObservationsRepo` — `packages/adapters/src/postgres/repos/macro-observations.ts` | adapters |
| `makeMemoryMacroObservationsRepo` — `packages/adapters/src/memory/macro-observations.ts` | adapters |
| `runMacroObservationsContractTests` — `packages/adapters/src/__contract__/macro-observations.contract.ts` | adapters (test) |
| barrel exports in `packages/adapters/src/index.ts` | adapters |

Consumes: `ForFetchingFredSeries`, `ForFetchingVvixQuote`, `ForPersistingMacroObservation`, `ForReadingMacroObservations`, `MacroObservationRow` (from 14-01); live `macro_observations` table (from 14-02). See 14-01 for the full phase manifest.

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: FRED series adapter (parameterized, no-fallback, raw value)</name>
  <files>packages/adapters/src/http/fred.ts, packages/adapters/src/http/fred.test.ts</files>
  <read_first>
    - packages/adapters/src/http/fred.ts — the existing `makeFredRateAdapter`: URL param build (`series_id`/`api_key`/`file_type=json`/`sort_order=desc`/`limit=5`), `FredResponseSchema`/`FredObservationSchema` (lines 15-22), the `.`-sentinel filter + take-first-non-`.` logic, the never-log-apiKey discipline (T-02-11/T-02-12), and the `/100` transform (DGS3MO-only).
    - packages/adapters/src/http/fred.test.ts — existing msw test structure for the DGS3MO adapter (mock FRED at the network layer).
    - .planning/phases/14-fred-expansion/14-CONTEXT.md D-09 (hard-require key), D-14 (no /100); 14-PATTERNS.md "fred.ts (extend)" deviations; 14-RESEARCH.md Pitfall 1 (index series must not be /100) + Pitfall 4/5 (`.`-filter already handles business-day gaps + SOFR T+1).
  </read_first>
  <behavior>
    - makeFredSeriesAdapter("VIXCLS") on a mocked FRED response `[{date, value:"18.9"}]` returns ok with value 18.9 (RAW — NOT 0.189; NO /100) and seriesId "VIXCLS", source "fred".
    - It filters `"."` rows and takes the most-recent non-`.` observation (response is sort_order=desc), matching the existing DGS3MO logic.
    - With apiKey undefined/empty it returns err (fetch-error) — NOT a fabricated fallback (D-09), the opposite of makeFredRateAdapter.
    - On non-2xx, network throw, Zod parse failure, or all-`.` rows it returns err (fetch-error), never ok.
    - The apiKey value never appears in any thrown/warned message (assert the warn text is static).
    - Regression: the existing makeFredRateAdapter still returns ok(fallback) with the /100 decimal transform (its test stays green).
  </behavior>
  <action>
    Extract the shared low-level FRED mechanics from `makeFredRateAdapter` into one internal helper (build URL for a
    given `seriesId`, fetch, `response.ok` guard, `FredResponseSchema.safeParse`, filter `value !== "."`, take the
    first non-`.` observation, `parseFloat`), reusing `FredObservationSchema`/`FredResponseSchema` unchanged. Keep
    `makeFredRateAdapter` behavior byte-for-byte identical (DGS3MO, silent 4.5% fallback, `/100` decimal) — it now
    calls the helper but retains its fallback + division branch (D-02). Add `makeFredSeriesAdapter(deps: { fetch;
    apiKey: string | undefined })` returning `ForFetchingFredSeries`: on missing/empty key OR any failure return
    `err({ kind: "fetch-error", message })` (D-09 — no fallback); on success return `ok({ seriesId, date:
    first.date, value: parseFloat(first.value), source: "fred" })` with the RAW value (NO `/100`, D-14). Never log
    `deps.apiKey` (reuse the existing static-warn discipline). Export `makeFredSeriesAdapter` from the module.
    Write the RED tests FIRST (msw-mocked FRED) covering raw-value, no-fallback-on-missing-key, error paths, and the
    DGS3MO regression, and watch them fail before implementing.
  </action>
  <verify>
    <automated>bun run test -- packages/adapters/src/http/fred.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test -- packages/adapters/src/http/fred.test.ts` passes: raw-value (18.9 not 0.189), missing-key→err, network/parse/all-`.`→err, and the DGS3MO fallback+/100 regression case.
    - `rg -n "makeFredSeriesAdapter" packages/adapters/src/http/fred.ts` matches.
    - The new adapter's value path performs no division by 100 (verified by the raw-value test asserting 18.9).
    - `bun run typecheck` clean (returns MacroObservationRow via ForFetchingFredSeries; no `any`/`as`/`!`).
  </acceptance_criteria>
  <done>makeFredSeriesAdapter returns raw per-series values with no fallback; makeFredRateAdapter unchanged (D-02).</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: CBOE VVIX quote adapter</name>
  <files>packages/adapters/src/http/cboe-vvix.ts, packages/adapters/src/http/cboe-vvix.test.ts</files>
  <read_first>
    - packages/adapters/src/http/cboe.ts — the spot-resolution chain `current_price ?? close ?? prev_day_close` (lines 186-198), the UTC-timestamp parse `new Date(payload.timestamp.replace(" ", "T") + "Z")` with NaN guard (lines 200-207), the Zod `safeParse`-before-core discipline, and the `{ fetch, userAgent }` deps shape.
    - .planning/phases/14-fred-expansion/14-CONTEXT.md D-15 (endpoint `https://cdn.cboe.com/api/global/delayed_quotes/quotes/_VVIX.json`, same `{timestamp, data:{current_price, close, prev_day_close}}` shape, date from UTC timestamp), D-03 (seriesId VVIX), D-14 (raw value).
    - 14-RESEARCH.md Pitfall 6 (do NOT parse `last_trade_time`; derive the date from the top-level UTC `timestamp`).
  </read_first>
  <behavior>
    - makeCboeVvixAdapter on a mocked _VVIX.json `{ timestamp: "2026-07-02 01:00:55", data: { current_price: 89.0, close: 88.1, prev_day_close: 87.5 } }` returns ok with seriesId "VVIX", value 89.0 (RAW), source "cboe", and date derived from the UTC timestamp (YYYY-MM-DD of the UTC instant).
    - Spot resolution falls through current_price → close → prev_day_close when earlier fields are null.
    - Returns err (fetch-error) when spot is null/0, on non-2xx, on network throw, and on Zod parse failure.
    - The derived `date` uses the UTC timestamp (not `last_trade_time`), matching the CBOE-UTC regression gate.
  </behavior>
  <action>
    Create `makeCboeVvixAdapter(deps: { fetch; userAgent })` returning `ForFetchingVvixQuote`. Define a local Zod
    schema (adapter-local, not in contracts) for the `{ timestamp: string, data: { current_price, close,
    prev_day_close } }` shape (numbers nullable), mirroring cboe.ts. Point at
    `https://cdn.cboe.com/api/global/delayed_quotes/quotes/_VVIX.json` (D-15). On fetch: guard `response.ok`,
    `safeParse` before use, resolve `spot = current_price ?? close ?? prev_day_close`, and return
    `err({ kind: "fetch-error", ... })` when spot is null or 0. Parse the top-level UTC timestamp with
    `new Date(payload.timestamp.replace(" ", "T") + "Z")` and NaN-guard it (CBOE-UTC gate); derive `date` as the
    UTC `YYYY-MM-DD` slice of that instant (Pitfall 6 — do NOT read `last_trade_time`). Return `ok({ seriesId:
    "VVIX", date, value: spot, source: "cboe" })` with the RAW value (no /100, D-14). Write the RED tests FIRST
    (msw-mocked _VVIX.json) covering fallthrough resolution, error paths, and the UTC-date derivation, and watch
    them fail before implementing.
  </action>
  <verify>
    <automated>bun run test -- packages/adapters/src/http/cboe-vvix.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test -- packages/adapters/src/http/cboe-vvix.test.ts` passes: raw-value, spot fallthrough, null/0-spot→err, non-2xx/network/parse→err, and UTC-date derivation.
    - `rg -n "_VVIX.json" packages/adapters/src/http/cboe-vvix.ts` matches the D-15 endpoint.
    - The adapter returns `seriesId: "VVIX"` and `source: "cboe"` (asserted in the test).
    - `bun run typecheck` clean (implements ForFetchingVvixQuote; no `any`/`as`/`!`).
  </acceptance_criteria>
  <done>makeCboeVvixAdapter returns the raw VVIX index level with a UTC-derived date, reusing the CBOE spot/timestamp conventions.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: macro_observations Postgres repo + in-memory twin + shared contract suite</name>
  <files>packages/adapters/src/postgres/repos/macro-observations.ts, packages/adapters/src/postgres/repos/macro-observations.contract.test.ts, packages/adapters/src/memory/macro-observations.ts, packages/adapters/src/memory/macro-observations.contract.test.ts, packages/adapters/src/__contract__/macro-observations.contract.ts, packages/adapters/src/index.ts</files>
  <read_first>
    - packages/adapters/src/postgres/repos/cot-observations.ts — the repo shape (insert via Drizzle parameterized values, read via `db.select().from(...).orderBy(...)`, try/catch → StorageError). NOTE the deviation below: macro upserts with onConflictDoUpdate, not onConflictDoNothing.
    - packages/adapters/src/postgres/repos/rate-observations.ts — the existing `onConflictDoUpdate` pattern (last-write-wins) to mirror for the composite key.
    - packages/adapters/src/memory/cot-observations.ts — the in-memory twin (Map keyed by the unique key, replace-by-key semantics) — for macro the same key must REPLACE (upsert), not skip.
    - packages/adapters/src/__contract__/cot-observations.contract.ts — the shared `run...ContractTests(makeRepo)` suite factory; and packages/adapters/src/postgres/repos/cot-observations.contract.test.ts + packages/adapters/src/memory/cot-observations.contract.test.ts — how the suite is invoked against each twin (testcontainers for Postgres, plain for memory).
    - packages/adapters/src/index.ts — barrel export style (see the cot repo + memory twin export block, lines ~90-94).
    - .planning/phases/14-fred-expansion/14-CONTEXT.md D-05 (idempotent upsert; FRED may revise preliminary values → onConflictDoUpdate) + D-04 (chunk ≤2,000 rows on bulk backfill — but the port persists one row at a time, so chunking lives in the use-case/backfill; the repo insert is single-row).
  </read_first>
  <behavior>
    - insertMacroObservation writes a row; reading back returns it with the same seriesId/date/value/source.
    - A second insert for the SAME (date, seriesId) with a DIFFERENT value REVISES the stored value (onConflictDoUpdate / replace-by-key) — MAC-01 idempotency where re-runs never duplicate but may update.
    - readMacroObservations returns ALL rows across multiple series (no filtering; grouping happens in getMacro).
    - The identical `runMacroObservationsContractTests` suite passes against BOTH the Postgres repo (testcontainers) and the in-memory twin.
  </behavior>
  <action>
    Create the shared suite `runMacroObservationsContractTests(makeRepo)` in `__contract__/macro-observations.contract.ts`
    (mirror the cot contract-suite factory) asserting: round-trip persist→read; upsert-revises on duplicate
    `(date, series_id)` (write value A then value B for the same key → read returns B, and exactly one row for that
    key); multi-series read returns all rows. Implement `makePostgresMacroObservationsRepo(db)` in
    `postgres/repos/macro-observations.ts` with `insertMacroObservation` (ForPersistingMacroObservation) using
    Drizzle `.insert(macroObservations).values({...}).onConflictDoUpdate({ target: [macroObservations.date,
    macroObservations.seriesId], set: { value: ..., source: ... } })` (D-05 last-write-wins; parameterized, no string
    interpolation) and `readMacroObservations` (ForReadingMacroObservations) selecting all rows, mapping numeric
    `value` back to number, wrapped in try/catch → `err({ kind: "storage-error", message })`. Implement
    `makeMemoryMacroObservationsRepo()` in `memory/macro-observations.ts` with a Map keyed by `${seriesId}|${date}`
    that REPLACES on duplicate key (upsert semantics matching onConflictDoUpdate). Add the two `.contract.test.ts`
    runners (Postgres via testcontainers, memory plain) invoking `runMacroObservationsContractTests`. Export
    `makePostgresMacroObservationsRepo` (+ its repo type) and `makeMemoryMacroObservationsRepo` (+ type) from
    `packages/adapters/src/index.ts`, mirroring the cot repo/twin export block. Write the contract suite + runners
    FIRST (RED) and watch them fail before the repos exist.
  </action>
  <verify>
    <automated>bun run test -- --project packages/adapters macro-observations</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test -- --project packages/adapters macro-observations` passes for both the Postgres (testcontainers) and memory contract runners.
    - The upsert case proves a duplicate `(date, series_id)` write REVISES the value and leaves exactly one row (idempotent, MAC-01).
    - `rg -n "onConflictDoUpdate" packages/adapters/src/postgres/repos/macro-observations.ts` matches (last-write-wins, D-05).
    - `rg -n "makePostgresMacroObservationsRepo|makeMemoryMacroObservationsRepo" packages/adapters/src/index.ts` shows both barrel exports.
    - `bun run typecheck` clean.
  </acceptance_criteria>
  <done>macro_observations repo + twin pass one shared contract suite; duplicate (date,series_id) upserts (revise, never duplicate).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FRED/CBOE HTTP → core | Untrusted upstream JSON crosses here; must be Zod-parsed before any value reaches core |
| repo → DB | Parameterized SQL only; composite-key upsert governs idempotency |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-06 | Information Disclosure | FRED api_key in logs/errors | high | mitigate | Reuse fred.ts static-warn discipline (T-02-11/12) — apiKey never interpolated into any thrown/warned message; test asserts static warn text |
| T-14-07 | Tampering | malformed FRED/CBOE payload reaching core | medium | mitigate | Zod `safeParse` at both adapter boundaries before any value crosses into core (fred.ts/cboe.ts precedent) |
| T-14-08 | Tampering | SQL injection via series/value | medium | mitigate | Drizzle parameterized `.values()` — no string interpolation (cot-observations repo precedent) |
| T-14-04 | Tampering | duplicate (series, date) rows | high | mitigate | onConflictDoUpdate on composite PK — re-run revises, never duplicates (MAC-01) |
| T-14-SC | Tampering | npm/bun installs | low | accept | Zero new dependencies (zod, drizzle, msw, testcontainers already present) — no legitimacy gate |
</threat_model>

<verification>
- `bun run test -- packages/adapters/src/http/fred.test.ts` + `cboe-vvix.test.ts` green (msw).
- `bun run test -- --project packages/adapters macro-observations` green (testcontainers + memory contract parity).
- `bun run typecheck` + `bun run lint` clean.
</verification>

<success_criteria>
- makeFredSeriesAdapter (raw, no-fallback) + makeCboeVvixAdapter live; makeFredRateAdapter unchanged (D-02).
- macro_observations Postgres repo + memory twin pass one shared contract suite with idempotent upsert.
- All new adapters exported from @morai/adapters barrel.
</success_criteria>

<output>
Create `.planning/phases/14-fred-expansion/14-03-SUMMARY.md` when done.
</output>
