---
phase: 17.1
slug: overview-payoff-tos-fidelity-graph-interactivity
status: draft
shadcn_initialized: true
preset: base-nova
created: 2026-07-03
---

# Phase 17.1 — UI Design Contract

> Visual and interaction contract for OVW-03..06 (positions-box dates, TOS-fidelity payoff
> graph, date picker, per-calendar series selection). Extends the shipped Phase 17 Overview
> TOS-dock — this is a surgical restyle + wiring fix on `PayoffChart.tsx` and `Overview.tsx`,
> not a new screen. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (already initialized — detected `apps/web/components.json`) |
| Preset | `base-nova`, baseColor `neutral`, cssVariables true |
| Component library | Radix primitives via shadcn (`badge`, `dialog`, `tooltip`, `input`, `button`, `slider`, `tabs`, `toggle`, `toggle-group` installed) |
| Icon library | lucide |
| Font | `Space Grotesk` (display — headings, values, section labels) + `JetBrains Mono` (mono — body, all data, the app's default `body` font) |
| Chart library | visx (`LinePath`/`AreaClosed`/`LinearGradient`/`localPoint`) — locked, no new charting dep |

No shadcn gate action needed — `components.json` already present with a locked preset from
Phase 9. This phase adds **zero new npm dependencies** (v1.2 lock, restated in 17.1-CONTEXT.md
Claude's Discretion) and **zero new shadcn components** — the date picker is a native
`<input type="date">` styled to the existing dark token set, not a new registry block.

---

## Spacing Scale

The existing codebase does not run a clean 4/8/16 Tailwind scale — `Overview.tsx` and
`PayoffChart.tsx` already use a dense financial-terminal micro-scale (`gap-1`, `gap-1.5`,
`gap-2`, `px-2`, `py-1`, `px-[7px]`, `py-0.5`) established in Phase 9's UI-SPEC and unchanged
since. This phase is a surgical extension of that surface, not a new design — **match the
existing micro-scale exactly**, do not introduce a fresh 8pt grid for the new elements.

| Token | Value | Usage (this phase's new elements) |
|-------|-------|-------|
| xs | 4px (`gap-1`) | Icon-to-label gaps (‹/› arrow to date input) |
| sm | 6-8px (`px-2 py-1`, `gap-2`) | Date-picker row internal spacing, positions-box two-line cell gap |
| toolbar-pad | 7px / 2px (`px-[7px] py-0.5`) | Step-arrow buttons and the "Today" reset button — reuses the exact `Reset`/`Fit Y` button padding already in `Analyzer.tsx`/`Overview.tsx` |
| md | 16px | New date-picker row's vertical gap from the header row above it |

Exceptions: the project's established fractional/odd px values (`px-[7px]`, `py-0.5`,
`gap-1.5`) are the norm here, not an exception — do not round them to a clean 8pt grid.

---

## Typography

Existing dense-terminal type scale (Phase 9 UI-SPEC, unchanged) — this phase reuses it
verbatim for all new text, it does not introduce new sizes/weights.

| Role | Size | Weight | Line Height | Font |
|------|------|--------|-------------|------|
| Body / data (base) | 12px | 400 | 1.45 | JetBrains Mono (`body` default) |
| Table cell / row data | 11px | 400 | default | JetBrains Mono, `tabular-nums` |
| Positions-box expiry line 1 (`Aug 8 → Sep 5`) | 11px | 400 | default | JetBrains Mono, `text-muted-foreground` |
| Positions-box expiry line 2 (`32d/59d · 27d wide`) | 9px | 400 | default | JetBrains Mono, `text-dim` |
| Micro-label (`Date:`, axis ticks, badges) | 9-10px | 400-500 | default | JetBrains Mono |
| Section label (uppercase tracked) | 10px | 600 | default | Space Grotesk, `tracking-[0.09em] uppercase` |
| Emphasis value (chart tooltip P&L) | 13-15px | 700 | default | Space Grotesk |

No new weights: the app uses exactly two — 400 (regular, JetBrains Mono default) and
600/700 (semibold/bold, Space Grotesk headings and emphasis values). Do not introduce a
third weight for the date picker or reformatted DTE cell.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#0a0e14` (`--color-bg`) | Page background |
| Secondary (30%) | `#0f1521` / `#161d2b` (`--color-panel`, `--color-raise`) | Panel surfaces, table row hover, date-input background |
| Accent (10%) | `#a78bfa` (`--color-violet`) | Reserved for: focus rings, primary badges/buttons elsewhere on the app — **NOT** the payoff-hero curve colors in this phase (see override below) |
| Destructive | `#ef5350` (`--color-down`) | Negative P&L only — no new destructive action in this phase (view-only, no delete/confirm flows) |

**Locked TOS-fidelity override for `PayoffChart.tsx` curves only (OVW-04, D-03 — explicit
user instruction, pre-approved brand divergence for this one chart):**

| Element | Value | Replaces |
|---------|-------|----------|
| "Today"/date-projected curve stroke | `#e91e8c` (magenta) — new token, add as `--color-tos-magenta` in `index.css` `@theme` | `#a78bfa` (violet) `VIOLET` constant |
| "@exp" curve stroke | `#22d3ee` (`--color-cyan`, already an existing token — reuse, don't add a new one) | `#7b8696` (`GRAY_MUTED`) `strokeDasharray "5 4"` styling stays, only the color changes |
| Today-curve breakeven (BE) lines/labels | Same magenta as its curve, at existing 0.55 opacity | was `VIOLET` — no color change needed if the curve token itself is renamed, otherwise re-point to the new magenta token |
| @exp breakeven (BE) lines/labels | Same cyan as its curve, at existing 0.55 opacity | was `GRAY_MUTED` |
| Legend swatches (Overview.tsx header row) | Magenta dot for "T+0" / "Date", cyan dot for "@ exp" | was `bg-violet` / `bg-muted-foreground` |
| Gridlines, zero line, GEX wall lines (put/call wall, γ flip), spot marker, profit-zone fill, glow filter | **Unchanged** — keep existing `GRID_LINE`/`ZERO_LINE`/`CORAL`/`TEAL`/`AMBER`/`BLUE` tokens | n/a — TOS reference is about the two P&L curves + axis, not Morai's GEX overlay, which stays as the app's own value-add layer on top |

⚠ **Unresolved reference gap**: the two TOS screenshots named in
`mockups/tos-reference/README.md` (`tos-risk-profile-combined-graph.png`,
`tos-analyze-date-picker.png`) were never re-saved to disk — only the README exists. The
magenta/cyan hex values above are a best-effort match to Thinkorswim's known Risk Profile
palette (magenta "today" line, cyan "@exp" line on a black background), consistent with the
locked CONTEXT.md decision (D-03: magenta + cyan). **Before or during execution, the user
must re-drop the two screenshots into `mockups/tos-reference/`; the executor should do a
final pixel-check against them and adjust the two hex values only if they visibly mismatch**
— the curve roles (magenta=today/date, cyan=@exp) and the "override, don't blend with brand
palette" rule are locked regardless.

Accent reserved for (unchanged elsewhere on Overview): live-cell flash background, checkbox
`accent-blue`, focus rings. The magenta/cyan override is scoped **only** to the payoff-hero
chart curves + their BE lines + legend swatches — nothing else on the Overview screen changes
palette.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | None — this phase is view-only (no create/submit/save action). The closest equivalent is the date-picker's reset affordance. |
| Date-picker label | `Date:` (exact TOS convention, per D-01 mockup reference) |
| Date-picker reset | `Today` — button, resets the projected date back to the current date (`daysForward: 0`) |
| Step-arrow controls | `‹` / `›` glyphs only, `aria-label="Previous day"` / `aria-label="Next day"` (no visible text label) |
| Per-calendar chart toggle | Reuses the existing positions-table checkbox; update its `aria-label` from `Include {label} in total` to `Include {label} in risk profile & total` — the checkbox is now unified (D-02a: one checkbox drives both the chart curves and the P&L/greeks total) |
| Positions-box expiry cell (calendar row) | Line 1: `{frontExpiry, e.g. "Aug 8"} → {backExpiry, e.g. "Sep 5"}`. Line 2: `{frontDte}d/{backDte}d · {backDte − frontDte}d wide` (e.g. `32d/59d · 27d wide`) |
| Positions-box expiry cell (single-leg row) | Line 1: `{expiry, e.g. "Aug 8"}`. Line 2: `{dte}d` — no "wide" line (no calendar width concept for a single leg) |
| Column header (was "DTE") | `Expiry / DTE` |
| Empty state (no positions) | Unchanged from shipped Phase 17: `No open positions. Register a calendar via the API or paste a TOS order in the Analyzer.` |
| Non-convergent IV badge | Unchanged: badge `IV n/a`, tooltip `IV n/a — did not converge. @exp shown; excluded from T+0.` |
| Date-range clamp | No error copy needed — enforced silently via the native `<input type="date">` `min`/`max` attributes (today → earliest front expiry in the book, D-01a). The browser's own picker UI disables out-of-range dates; no custom validation message. |
| Destructive confirmation | None — no destructive action in this phase. |
| View-only note (existing, unchanged) | `view-only · Analyzer →` stays in the payoff-hero header row |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|--------------|
| shadcn official | None new this phase — reuses already-installed `badge`, `tooltip`, existing `Panel`/`SectionLabel`/`Stat` molecules from `src/components/system/index.tsx` | not required |
| Third-party | none | not applicable — no third-party registry declared |

The date picker is a **native HTML `<input type="date">`**, not a shadcn/registry
component — set `style={{ colorScheme: "dark" }}` on the input so Chrome/Safari/Firefox all
render the native calendar-picker popup in dark mode automatically (no CSS filter hacks, no
new dependency). Step-arrow buttons reuse the exact Tailwind classes already on the
`Reset`/`Fit Y` buttons in `Analyzer.tsx`/`Overview.tsx` (`rounded-[3px] border border-line2
bg-transparent px-[7px] py-0.5 font-mono text-[9px] text-dim`).

---

## Phase-Specific Component Contracts

Supplementary detail beyond the 6 standard dimensions, because this phase is chart-heavy and
the planner/executor need an unambiguous rendering contract.

### Date picker + step arrows (OVW-05)

- **Placement**: a new row in the payoff-hero `Panel`, between the existing header row
  (`SectionLabel` + GEX/mark freshness badges) and the existing curve-legend row (T+0/@exp/γ
  flip/walls swatches). Left-aligned: `Date: [‹] [date input] [›] [Today]`.
- **Behavior**: picking a date (or stepping ‹/›) computes `daysForward` = whole days between
  today and the picked date, and feeds it into the existing `ScenarioParams.daysForward` for
  the "today"/date curve only (`scenario.payoffCurve`) — **the `@exp` curve
  (`scenario.expirationCurve`) is NOT re-derived from the picked date; it stays fixed** at
  each position's front expiry (locked in D-01, matches TOS's model where the expiration
  profile never moves).
- **Range clamp (D-01a)**: `min` = today, `max` = the earliest front expiry across included
  calendars (a calendar projected past its own front expiry is degenerate). When there are no
  included calendars, `max` = today (picker effectively disabled, matches empty-state).
- **Step increment**: 1 day per ‹/› click (Claude's Discretion, confirmed in CONTEXT.md).
- **Default**: today (`daysForward: 0`), matching the shipped Phase 17 baseline — no visual
  change when the picker is untouched.
- **Reuse note**: this ports `Analyzer.tsx`'s `daysForward` slider pattern (already wired
  end-to-end through `repriceScenario`) — the Overview version replaces the slider control
  with the TOS-style picker + arrows, not a new pricing path.

### TOS-fidelity payoff chart (OVW-04)

- **Curve colors**: see Color section override above (magenta today/date, cyan @exp).
- **Y-axis auto-scale fix**: `computeYDomain` currently derives the y-domain from
  `baseExpirationCurve` alone (`PayoffChart.tsx:146`), which is the literal cause of "@exp
  dwarfing a near-flat T+0" named in OVW-04's success criteria. The y-domain must be computed
  from **both** curves' combined min/max (today/date-projected curve AND @exp curve) so a
  near-flat T+0 line is never squashed to a hairline against a tall @exp tent.
- **X-axis ticks**: replace the current hardcoded `[7000, 7200, 7400, 7600, 7800]` strike
  label array (`PayoffChart.tsx:385`) with **evenly-spaced round-number ticks** computed from
  the live `X_MIN`/`X_MAX` domain (e.g. 5 ticks at a round step — 100 or 200 points depending
  on spot level) — this is the "mixed round/key-level x-ticks" problem named in OVW-04. GEX
  wall/flip vertical reference lines (put wall, call wall, γ flip) stay as separate overlay
  lines with their own labels — they are not part of the axis tick row itself.
- **Everything else unchanged**: the 9-layer z-order, gridlines, profit-zone fill, GEX wall
  lines, spot marker, crosshair/tooltip — none of that is in scope for the TOS restyle; only
  the two curve colors, the y-domain calc, and the x-tick generation change.

### Per-calendar series selection (OVW-06)

- **No new visual component.** The existing positions-table row checkbox
  (`PositionsTable`'s `excluded` Set, `Overview.tsx:295`) is the single control.
- **The wiring gap being fixed**: today `calendarPositions` for `repriceScenario` are built
  with `included: true` hard-coded (`Overview.tsx:144`), so unchecking a row currently affects
  only the P&L total/greeks, never the chart. This phase routes the same `excluded` Set into
  the chart's `calendarPositions` inclusion flag — unchecking a row must remove that
  calendar's contribution from **both** curves (unified behavior, D-02a working assumption,
  pending the TOS-behavior research note in CONTEXT.md D-02).
- **Visual feedback**: unchanged — the row keeps its existing `opacity-40` dim when excluded
  (`Overview.tsx:364`). No new highlight/dim treatment needed on the chart curves themselves;
  they simply stop including that calendar's contribution once its checkbox is unchecked.

### Positions-box expiry/DTE reformat (OVW-03)

- **Column**: rename header `DTE` → `Expiry / DTE`, keep `text-right` alignment.
- **Cell layout**: two stacked lines inside the same `<td>` (flex-col), replacing the current
  single-line `{frontDte}d → {backDte}d` string built in `buildRows()`
  (`Overview.tsx:239-256`).
  - Calendar row: `Aug 8 → Sep 5` (11px, `text-muted-foreground`) then `32d/59d · 27d wide`
    (9px, `text-dim`) — short-month date format matches the existing `gexAsOf`/`markAsOf`
    convention already in `Overview.tsx` (`toLocaleString("en-US", { month: "short", day:
    "numeric" })`).
  - Single-leg row: `Aug 8` then `32d` — no "wide" second metric.
- **Days-between math**: `backDte − frontDte`, computed from the same `CalendarGroup.dteFront`
  / `dteBack` fields already returned by `pairPositionsIntoCalendars` — no new data source.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
