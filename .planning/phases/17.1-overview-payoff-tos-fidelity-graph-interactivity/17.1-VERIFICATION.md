---
phase: 17.1-overview-payoff-tos-fidelity-graph-interactivity
verified: 2026-07-04T21:35:00Z
status: passed
status_note: "human_needed → passed 2026-07-04. The sole human item (OVW-04 TOS pixel-match) was RETIRED by user design decision: keep the TOS graph logic, style with the MORAI palette, not TOS neon. TOS color override removed (commit 1142d54); Overview hero now uses the MORAI default PayoffChart palette matching the already-tested Analyzer. Resolved in 17.1-UAT.md (passed 1/1). The graph-logic half of OVW-04 was already mechanically verified below."
score: 4/4 must-haves verified
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - test: "Re-drop `tos-risk-profile-combined-graph.png` and `tos-analyze-date-picker.png` into `mockups/tos-reference/` (currently only the README exists — confirmed empty at verification time), open the Overview in a browser, and pixel-compare the today/date (magenta `#e91e8c`) + @exp (cyan `#22d3ee`) curve colors, tick spacing, and axis scaling against the real TOS Risk Profile chart."
    expected: "The Overview payoff-hero curves, tick density, and axis auto-scale visually read as a TOS Risk Profile chart. If the magenta/cyan hex mismatch the real TOS palette, adjust only those two `--color-tos-magenta`/`--color-cyan` hex values (curve roles and the override-not-blend rule are locked regardless of the final hex)."
    why_human: "OVW-04's success criterion is a visual-fidelity claim ('visually emulates the TOS Risk Profile combined chart') that cannot be judged by grep/unit tests. The two reference screenshots the UI-SPEC calls for were never re-dropped into mockups/tos-reference/ (verified: directory contains only README.md), so the magenta/cyan hex values are explicitly marked [ASSUMED] in the UI-SPEC, PLAN 02/04, and both their SUMMARYs. This is a pre-planned deferral (plan 04's own <human-check> block, config human_verify_mode: end-of-phase), not a gap introduced by this verification."
---

# Phase 17.1: Overview Payoff — TOS-Fidelity Graph + Interactivity Verification Report

**Phase Goal:** Users get a Thinkorswim-grade payoff experience on the Overview: the combined
risk-profile graph visually matches TOS's Analyze → Risk Profile chart, a date picker projects
the curve to a chosen future date, individual real-book calendars can be toggled on/off in the
graph, and the positions box shows each calendar's leg expiration dates + DTEs + days-between.
View-only — NO simulated/example trades.

**Verified:** 2026-07-04T21:35:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | (OVW-03) Each positions-box row shows both leg expiration dates, both DTEs, and the days between (calendar width) | ✓ VERIFIED | `formatExpiryCell` (`apps/web/src/screens/Overview.tsx:270-288`) builds `"Aug 8 → Sep 5"` / `"32d/59d · 27d wide"` for calendars from `CalendarGroup.dteFront/dteBack`; single-leg rows get `"Aug 8"` / `"32d"`. Render + unit tests pass live (`formatExpiryCell (OVW-03)` + `positions-box expiry/DTE cell + header (OVW-03)` describe blocks, 6 tests, run directly by this verifier). |
| 2 | (OVW-04) The combined payoff graph visually emulates the TOS Risk Profile chart (smooth curves, TOS-style ticks/scaling); the wonky mixed-tick / dwarfed-T+0 rendering is resolved | ⚠ Mechanically VERIFIED / visually HUMAN-NEEDED | `computeYDomain(todayCurve, expCurve)` now scans BOTH curves (`PayoffChart.tsx:164-189`); `buildXTicks` derives round-number ticks from `X_MIN`/`X_MAX` (`PayoffChart.tsx:196-205`), replacing the old hardcoded array. Both wired into the component body (lines 358, 279). Confirmed by live test run (`computeYDomain — combined-curve y-axis`, `buildXTicks — derived round-number x-axis ticks`, both green). The exact TOS magenta/cyan hex match is [ASSUMED] pending a human pixel-check — see Human Verification below. |
| 3 | (OVW-05) A date picker projects the payoff curve to a chosen future date via the existing `daysForward` path of `repriceScenario` | ✓ VERIFIED | `resolveDaysForward`/`computeProjectionBounds` (`apps/web/src/lib/date-projection.ts`) feed `daysForward` into `Overview.tsx`'s `scenario` useMemo (`ScenarioParams.daysForward`, line 879), replacing the old `daysForward: 0` literal. Native `<input type="date">` + `‹`/`›`/`Today` controls render at `Overview.tsx:996-1031`. `expirationCurve` invariance across `daysForward` locked by a new characterization test in `scenario-engine.test.ts:206-222`. All confirmed passing in a live `vitest run`. |
| 4 | (OVW-06) The user can pick/choose which real-book calendars are drawn on the graph; no simulated calendars ever appear | ✓ VERIFIED | Single lifted `excludedCalendars` `useState<ReadonlySet<string>>` (`Overview.tsx:816`, confirmed exactly 1 occurrence in the file) feeds both `buildCalendarPosition`'s `included` param (line 832) and the controlled `PositionsTable` (`excluded`/`onToggleExcluded` props, no internal `useState`). `positionSetSignature` encodes `included` (line 888). CR-01 non-convergence exclusion (`includedForT0 = pos.included && !isIvExcludedFromT0`, `scenario-engine.ts:249`) proven to compose correctly by a dedicated regression test. No `mockPosition`/`simulated`/`example` data source found in `Overview.tsx` (grep, 0 matches) — view-only confirmed. |

**Score:** 4/4 truths mechanically verified (1 of the 4 carries an unresolvable visual-fidelity component routed to human verification, per the "always needs human: visual appearance" rule).

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `apps/web/src/lib/date-projection.ts` | Pure, timezone-safe date helpers | ✓ VERIFIED | Exports `toDateInputValue`, `parseLocalDateInput`, `daysBetween`, `resolveDaysForward`, `computeProjectionBounds` exactly per Plan 01's contract. No `any`/`as`/`!`; local-timezone `Date` construction only (no `new Date(string)`). |
| `apps/web/src/lib/date-projection.test.ts` | Unit + fast-check tests | ✓ VERIFIED | 13 tests including the UTC-drift round-trip fast-check property; all pass in a live run. |
| `apps/web/src/components/charts/PayoffChart.tsx` | Two-curve y-domain, derived x-ticks, color props, WR-03 fix | ✓ VERIFIED | All four sub-changes present, wired, and covered by passing tests (see Truth #2 evidence + WR-03 spot-check below). |
| `apps/web/src/index.css` | `--color-tos-magenta` token | ✓ VERIFIED | `index.css:26` — `--color-tos-magenta: #e91e8c;` inside the `@theme` block; Tailwind v4 CSS-first config auto-generates `bg-tos-magenta`/`text-tos-magenta` utilities from this declaration (confirmed by inspecting the existing `--color-cyan`/`bg-cyan` sibling pattern already in use). |
| `apps/web/src/screens/Overview.tsx` | Date picker, lifted inclusion state, TOS colors, reformatted expiry cell | ✓ VERIFIED | All four sub-features present and wired (see Truths #2-#4 evidence). |
| `apps/web/src/screens/Overview.test.tsx` | Behavioral coverage for OVW-03/04/05/06 | ✓ VERIFIED | 4 requirement-tagged describe blocks (`OVW-06`, `OVW-05`, `OVW-04`, `OVW-03`) with substantive assertions on rendered DOM / spied component props, not placeholder tests. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `resolveDaysForward`/`computeProjectionBounds` (Plan 01) | Overview date-picker `onChange`/native `min`/`max` | Direct import + call (`Overview.tsx:17, 852-870`) | ✓ WIRED | `daysForward` derived every render from `dateInputValue` via `resolveDaysForward`; `bounds` derived via `computeProjectionBounds` over `calendarPositions.filter(p => p.included)`. |
| `daysForward` | `ScenarioParams.daysForward` | `scenario` useMemo (`Overview.tsx:876-885`) | ✓ WIRED | Replaces the old `daysForward: 0` literal; dep array includes `daysForward`. |
| `todayCurveColor`/`expirationCurveColor` props (Plan 02) | Overview `<PayoffChart>` instance | Direct props (`Overview.tsx:1070-1071`) | ✓ WIRED | `var(--color-tos-magenta)` / `var(--color-cyan)` passed only at the Overview call site; the Analyzer's `<PayoffChart>` call passes neither (confirmed by grep — Analyzer.tsx has no `todayCurveColor`/`expirationCurveColor` occurrences), preserving violet/gray there. |
| `excludedCalendars` (lifted state) | `buildCalendarPosition`'s `included` param + `PositionsTable`'s `excluded`/`onToggleExcluded` props | Direct props/args (`Overview.tsx:816-834`, `1121-1122`) | ✓ WIRED | Single source of truth; `PositionsTable`'s former internal `useState` deleted (confirmed: exactly 1 `useState<ReadonlySet<string>>` in the file). |
| `positionSetSignature` | `PayoffChart`'s y-domain lock (`useEffect`) | `positionSetSignature` prop (`Overview.tsx:887-889` → `PayoffChart.tsx:259-264`) | ✓ WIRED | Signature appends `:${p.included}` per position; PayoffChart's lock-on-signature `useEffect` recomputes the y-domain whenever this string changes. |
| `formatExpiryCell`/`CalendarGroup.dteFront/dteBack` | Positions-box `<td>` render | `buildRows` → `Row.expiry` (`Overview.tsx:302-320` → `483-487`) | ✓ WIRED | No stale `Row.dte` reader remains (grep for the removed field name: 0 matches). |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| `PayoffChart` (`Overview.tsx` instance) | `scenario.payoffCurve`/`scenario.expirationCurve` | `repriceScenario(calendarPositions, params)` over live `usePositions()`/`useLiveStream()` greeks | Yes — real broker positions, live greeks, real IV bisection | ✓ FLOWING |
| Positions-box expiry cell | `r.expiry` (`ExpiryCell`) | `parseOccSymbol` on real `BrokerPositionResponse.occSymbol` + `CalendarGroup.dteFront/dteBack` computed from real expiry dates | Yes | ✓ FLOWING |
| Date-picker `bounds` | `computeProjectionBounds` | `calendarPositions.filter(p => p.included).map(p => p.frontDte)` — real, live-computed DTEs | Yes | ✓ FLOWING |

No hardcoded-empty props or static returns found at any of these call sites — this phase introduces no new data source, only client-side derivations over already-live data (consistent with the "view-only, no simulated trades" constraint).

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full phase test suite (5 files touched by this phase) actually passes, run by the verifier directly (not trusting SUMMARY claims) | `bunx vitest run src/lib/date-projection.test.ts src/components/charts/PayoffChart.test.tsx src/screens/Overview.test.tsx src/screens/Analyzer.test.tsx src/lib/scenario-engine.test.ts` | `Test Files 5 passed (5)` / `Tests 87 passed (87)` | ✓ PASS |
| WR-03 antipattern regression: does the fitY→onFitYConsumed path avoid React's "Cannot update a component while rendering" warning | `PayoffChart.test.tsx:279-303` (named test, part of the run above) | `warnedAboutRenderPhaseUpdate === false` | ✓ PASS |
| D-01 characterization: `expirationCurve` invariant across `daysForward` | `scenario-engine.test.ts:206-222` (part of the run above) | Point-for-point identical for `daysForward: 0` vs `20` | ✓ PASS |
| `apps/web` typecheck introduces no new errors in the 5 phase-touched files | `bunx tsc --noEmit` (run directly by this verifier) | 0 errors in `date-projection.ts`, `PayoffChart.tsx`, `Overview.tsx`; all remaining errors confined to pre-existing, documented `Analyzer.test.tsx`/`JournalContainer.test.tsx` debt | ✓ PASS |
| No debt markers (`TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER`) in the 4 core phase files | `grep -n -E "TBD\|FIXME\|XXX\|TODO\|HACK\|PLACEHOLDER"` | 0 matches | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| OVW-03 | 17.1-05 | Positions-box shows both expiries + DTEs + calendar width | ✓ SATISFIED | `formatExpiryCell` + render tests, verified live. |
| OVW-04 | 17.1-02, 17.1-04 | Combined graph visually emulates TOS Risk Profile chart | ✓ SATISFIED (mechanically) / pixel-check deferred | Y-domain/x-tick fix + color props verified; exact hex match to TOS reference needs human pixel-check (screenshots absent — see Human Verification). |
| OVW-05 | 17.1-01, 17.1-04 | Date picker projects via `daysForward` | ✓ SATISFIED | Full wiring + clamp + @exp-invariance test, verified live. |
| OVW-06 | 17.1-03 | Per-calendar chart toggle, no simulated calendars | ✓ SATISFIED | Single lifted source of truth, CR-01 regression proven, no simulated-data injection found. |

No orphaned requirements: `.planning/REQUIREMENTS.md`'s Phase 17.1 row maps exactly OVW-03/04/05/06, all four of which appear in a plan's `requirements:` frontmatter (Plan 01: OVW-05; Plan 02: OVW-04; Plan 03: OVW-06; Plan 04: OVW-05, OVW-04; Plan 05: OVW-03).

### Anti-Patterns Found

None. No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` markers, no empty implementations, no hardcoded-empty data flowing to render, no `any`/`as`/`!` in the phase's new/modified source (comments referencing `new Date(string)` are documentation, not code). `bun run lint` clean for the phase's files (only pre-existing, unrelated `boundaries` config warnings elsewhere in the repo).

### Human Verification Required

#### 1. TOS Risk Profile visual pixel-check (OVW-04)

**Test:** Re-drop the two TOS reference screenshots (`tos-risk-profile-combined-graph.png`,
`tos-analyze-date-picker.png`) into `mockups/tos-reference/` (currently empty except for a
README — confirmed at verification time), open the Overview screen in a browser, and
pixel-compare the today/date curve (magenta, `#e91e8c`) and @exp curve (cyan, `#22d3ee`),
tick spacing, and axis auto-scaling against the real Thinkorswim Risk Profile chart.

**Expected:** The Overview payoff-hero visually reads as a TOS Risk Profile chart. If the
magenta/cyan hex values visibly mismatch the real TOS palette, adjust only those two
`--color-tos-magenta`/`--color-cyan` CSS custom-property values in `apps/web/src/index.css`
— the curve-role assignment (magenta=today/date, cyan=@exp) and the "override, don't blend
with brand palette" rule are locked regardless of the final hex.

**Why human:** This is an inherently visual-fidelity claim that no grep/unit-test check can
resolve. The UI-SPEC itself flags this as an "Unresolved reference gap" and explicitly defers
the final pixel-check to end-of-phase human-verify (`human_verify_mode: end-of-phase`); Plans
02 and 04's own SUMMARYs both independently confirm the reference screenshots were never
re-dropped to disk. This is a pre-planned deferral, not a defect this verification introduced.

### Gaps Summary

No gaps. All four requirement IDs (OVW-03/04/05/06) have substantive, wired, tested
implementations verified directly against the codebase (not just SUMMARY claims) — including
a live re-run of the full test suite for all 5 touched files (87/87 passing) and a live
typecheck confirming zero new errors. The only open item is a pre-planned, explicitly-deferred
visual pixel-check against TOS reference screenshots that were never supplied to the repo —
this routes the phase to `human_needed` rather than `passed`, per the verification framework's
rule that any human-verification item (including "always needs human: visual appearance")
takes precedence over an otherwise-clean truths/artifacts/links scorecard.

---

*Verified: 2026-07-04T21:35:00Z*
*Verifier: Claude (gsd-verifier)*
