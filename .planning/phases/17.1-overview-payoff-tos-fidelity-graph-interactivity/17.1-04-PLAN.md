---
phase: 17.1-overview-payoff-tos-fidelity-graph-interactivity
plan: 04
type: execute
wave: 2
depends_on: [17.1-01, 17.1-02, 17.1-03]
files_modified:
  - apps/web/src/screens/Overview.tsx
  - apps/web/src/screens/Overview.test.tsx
  - apps/web/src/lib/scenario-engine.test.ts
autonomous: true
requirements: [OVW-05, OVW-04]
must_haves:
  truths:
    - "A native date picker with ‹ › day-step arrows and a Today reset projects the today/date payoff curve to the chosen date via ScenarioParams.daysForward."
    - "The @exp curve does NOT move when the picked date changes — it stays fixed at each position's front expiry (D-01)."
    - "The date input is range-clamped to today → earliest front expiry across included calendars; an out-of-range value is clamped in code, not just by native attributes."
    - "The Overview payoff-hero curves render TOS magenta (today/date) + cyan (@exp), and the legend swatches match; the Analyzer's chart is unchanged."
    - "TOS curve colors/tick/axis conventions (D-03a) are applied per the UI-SPEC's locked magenta/cyan + auto-scaled axis; the values are [ASSUMED] (reference screenshots absent) and verified by the end-of-phase human pixel-check (Task 3 <human-check>), not treated as pixel-verified."
  artifacts:
    - apps/web/src/screens/Overview.tsx
  key_links:
    - "resolveDaysForward / computeProjectionBounds (Plan 01) drive the picker's daysForward + min/max."
    - "daysForward feeds the scenario useMemo's ScenarioParams (replacing daysForward:0 at Overview.tsx:756)."
    - "todayCurveColor/expirationCurveColor props (Plan 02) inject var(--color-tos-magenta)/var(--color-cyan) on the Overview PayoffChart only."
---

<objective>
Fix OVW-05 and apply OVW-04's color override on the Overview hero: add a TOS-style date picker
(native `<input type="date">` + `‹`/`›` day-step arrows + `Today` reset) that projects the
today/date payoff curve to a chosen future date via the scenario engine's existing `daysForward`
path, keeping the @exp curve fixed (D-01); and inject the pre-approved TOS magenta/cyan curve
colors + legend swatches on the Overview payoff chart only.

Purpose: this is the headline interactivity port from the Analyzer's `daysForward` slider,
trimmed to a view-only picker. The pure day-math and clamp already exist and are tested (Plan 01);
the color props already exist with brand defaults (Plan 02). This plan is the Overview-side
wiring + presentation. `bookPLAtExpiry` already ignores `daysForward`, so "@exp stays fixed"
needs zero engine change — only a characterization test to lock it.
Output: the date-picker control row, the daysForward wiring, and the TOS color application.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17.1-overview-payoff-tos-fidelity-graph-interactivity/17.1-CONTEXT.md
@.planning/phases/17.1-overview-payoff-tos-fidelity-graph-interactivity/17.1-RESEARCH.md
@.planning/phases/17.1-overview-payoff-tos-fidelity-graph-interactivity/17.1-UI-SPEC.md
@.claude/rules/tdd.md
@.claude/rules/typescript.md
@apps/web/src/screens/Overview.tsx
@apps/web/src/screens/Overview.test.tsx
@apps/web/src/lib/date-projection.ts
@apps/web/src/lib/scenario-engine.ts
@apps/web/src/lib/scenario-engine.test.ts
@apps/web/src/screens/Analyzer.tsx
</context>

## Artifacts this phase produces

- `apps/web/src/screens/Overview.tsx`:
  - New state for the projected date (the `<input type="date">` value string) + a derived `daysForward` via `resolveDaysForward` (Plan 01).
  - New date-picker row rendered between the payoff-hero header row (line ~868) and the legend row (line ~869): `Date: [‹] [<input type="date">] [›] [Today]`, native input with `min`/`max` from `computeProjectionBounds`, `style={{ colorScheme: "dark" }}`; `‹`/`›` `aria-label="Previous day"`/`"Next day"`; `Today` reset button; step-arrow + reset buttons reuse the existing `rounded-[3px] border border-line2 bg-transparent px-[7px] py-0.5 font-mono text-[9px] text-dim` class string (verbatim, no new spacing).
  - `daysForward` fed into the `scenario` useMemo's `ScenarioParams` (replaces `daysForward: 0` at line 756).
  - `<PayoffChart>` instance gains `todayCurveColor="var(--color-tos-magenta)"` and `expirationCurveColor="var(--color-cyan)"`.
  - Legend swatches: `bg-violet` (line 871) → `bg-tos-magenta`; `bg-muted-foreground` (line 875) → `bg-cyan`.
- `apps/web/src/lib/scenario-engine.test.ts`: one new characterization assertion (expirationCurve invariant across daysForward).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Date-picker control + daysForward wiring (RED→GREEN)</name>
  <files>apps/web/src/screens/Overview.tsx, apps/web/src/screens/Overview.test.tsx</files>
  <read_first>
    - apps/web/src/screens/Overview.tsx (payoff-hero header 838-868; legend row 869-886; scenario useMemo 753-762 incl. daysForward:0 at 756; calendarPositions 744-747; excludedCalendars from Plan 03)
    - apps/web/src/lib/date-projection.ts (resolveDaysForward, computeProjectionBounds, toDateInputValue — Plan 01)
    - apps/web/src/screens/Analyzer.tsx (Days-fwd slider pattern 326-344; Reset button classes 298-303 — the class string to reuse)
    - .planning/phases/17.1-overview-payoff-tos-fidelity-graph-interactivity/17.1-UI-SPEC.md (Date picker component contract: placement, behavior, range clamp, step increment, default; Copywriting Contract: Date:/Today/‹›)
  </read_first>
  <behavior>
    - Default (untouched): daysForward 0, today curve identical to the shipped baseline.
    - Picking a date N days ahead (within range) re-projects the today/date curve: the `PayoffChart` `todayCurve` prop changes toward the +N-day scenario; the `expirationCurve` prop does NOT change.
    - Clicking `›` steps the date +1 day (daysForward +1, clamped at max); `‹` steps −1 (clamped at 0); `Today` resets to daysForward 0.
    - The input `min` is today and `max` is the earliest front expiry across included calendars (`computeProjectionBounds`); with no included calendars, `max === min` (picker effectively disabled).
  </behavior>
  <action>
    RED first: add Overview tests that set two included calendars, change the date input value
    (fireEvent) to today+N, and assert the `todayCurve` prop handed to `PayoffChart` changes while
    `expirationCurve` stays equal; plus a `Today`/arrow test. Then implement: hold the input value
    as component state (string, default `toDateInputValue(today)`); derive
    `bounds = computeProjectionBounds(calendarPositions.filter(p => p.included).map(p => p.frontDte), today)`
    and `daysForward = resolveDaysForward(value, today, bounds.maxDaysForward)`; render the picker row
    between the header and legend rows with `min={bounds.minIso}`, `max={bounds.maxIso}`,
    `value`, `onChange` → set value; `‹`/`›` buttons compute the neighbor ISO date and set value
    (clamped by `resolveDaysForward` on read); `Today` sets value to `toDateInputValue(today)`.
    Feed `daysForward` into the `scenario` useMemo `ScenarioParams` (replace the `0` literal at line
    756) and add `daysForward` to its dep array. Reuse the exact Reset/Fit-Y button class string for
    the arrows + Today. Use a single stable `today` reference (e.g. a `useMemo(() => new Date(), [])`)
    so tests are deterministic. No `any`/`as`/`!`; the input value is a browser-native string routed
    only through the tested Plan-01 helpers.
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/screens/Overview.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Tests green: picking a future date moves `todayCurve` but not `expirationCurve`; `‹`/`›`/`Today` behave per behavior list.
    - Accessible names present: `Date:` label, `Previous day`, `Next day`, `Today`.
    - The native input carries `min`/`max`/`style={{ colorScheme: "dark" }}`.
    - `bun run typecheck` clean.
  </acceptance_criteria>
  <done>The date picker projects the today curve via daysForward, is range-clamped, and defaults to today.</done>
</task>

<task type="tdd">
  <name>Task 2: Characterization — @exp curve invariant across daysForward (D-01)</name>
  <files>apps/web/src/lib/scenario-engine.test.ts</files>
  <read_first>
    - apps/web/src/lib/scenario-engine.ts (bookPLAtExpiry 300-315 — structurally no daysForward param; expirationCurve build 444-447)
    - apps/web/src/lib/scenario-engine.test.ts (existing repriceScenario describe blocks 117/174; fixture position 27-52)
    - .planning/phases/17.1-overview-payoff-tos-fidelity-graph-interactivity/17.1-RESEARCH.md (Code Examples: bookPLAtExpiry ignores daysForward; anti-pattern: do NOT thread daysForward into @exp)
  </read_first>
  <behavior>
    - `repriceScenario(positions, { ...params, daysForward: 0 }).expirationCurve` is point-for-point equal to `repriceScenario(positions, { ...params, daysForward: 20 }).expirationCurve`.
  </behavior>
  <action>
    Add one characterization test to `scenario-engine.test.ts` asserting the `expirationCurve` is
    identical across two different `daysForward` values in otherwise-identical params (locks D-01's
    "@exp stays fixed" guarantee so a future edit that accidentally threads `daysForward` into the
    expiration pricing is caught). Do NOT modify `scenario-engine.ts` — this is a guard over existing
    behavior. This is a numeric-engine assertion; compare arrays elementwise with `toBeCloseTo`.
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/lib/scenario-engine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - New test green; expirationCurve equal across daysForward 0 vs 20.
    - `scenario-engine.ts` unchanged (`git diff --stat` shows only the test file for this task).
  </acceptance_criteria>
  <done>The @exp-stays-fixed guarantee is locked by a regression test.</done>
</task>

<task type="auto">
  <name>Task 3: Apply TOS magenta/cyan curve colors + legend swatches (Overview hero only)</name>
  <files>apps/web/src/screens/Overview.tsx, apps/web/src/screens/Overview.test.tsx</files>
  <read_first>
    - apps/web/src/screens/Overview.tsx (PayoffChart instance 887-903; legend row swatches bg-violet 871 + bg-muted-foreground 875)
    - apps/web/src/components/charts/PayoffChart.tsx (todayCurveColor/expirationCurveColor props from Plan 02)
    - apps/web/src/index.css (--color-tos-magenta from Plan 02; --color-cyan existing)
    - .planning/phases/17.1-overview-payoff-tos-fidelity-graph-interactivity/17.1-UI-SPEC.md (Color override table: legend swatches magenta/cyan; Unresolved reference gap note)
  </read_first>
  <action>
    Pass `todayCurveColor="var(--color-tos-magenta)"` and `expirationCurveColor="var(--color-cyan)"`
    to the Overview's `<PayoffChart>` instance (the Analyzer instance passes neither, so it keeps
    violet/gray — D-03 hero-only scope). Re-point the legend swatch dots: `bg-violet` → `bg-tos-magenta`
    for the `T+0`/`Date` swatch, `bg-muted-foreground` → `bg-cyan` for the `@ exp` swatch. Leave the
    `γ flip`/`walls` swatches unchanged. This is a styling-only change (TDD-exempt per tdd.md) but keep
    a render smoke assertion on the today-curve stroke. The magenta/cyan hex are [ASSUMED] (RESEARCH
    A1 — TOS screenshots absent): add a `ponytail`/TODO note at the color-prop site pointing to the
    human pixel-check, and do NOT treat the values as pixel-verified.
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/screens/Overview.test.tsx apps/web/src/screens/Analyzer.test.tsx</automated>
    <human-check>
      Re-drop the two TOS reference screenshots into `mockups/tos-reference/`
      (`tos-risk-profile-combined-graph.png`, `tos-analyze-date-picker.png`), open the Overview in the
      browser, and pixel-compare the today/date (magenta) + @exp (cyan) curves, tick spacing, and
      axis scaling against the TOS Risk Profile chart. Adjust ONLY the two hex values if they visibly
      mismatch (curve roles magenta=today/date, cyan=@exp, and the override-not-blend rule are locked
      regardless). Deferred to end-of-phase human-verify per config `human_verify_mode: end-of-phase`
      (RESEARCH Pitfall 5 / Open Question 1).
    </human-check>
  </verify>
  <acceptance_criteria>
    - Render assertion: the Overview payoff today-curve stroke is `var(--color-tos-magenta)` and the @exp stroke is `var(--color-cyan)`.
    - Render assertion: the Analyzer payoff chart still renders VIOLET/GRAY (unchanged — no props passed).
    - Legend swatch classes are `bg-tos-magenta` / `bg-cyan`.
    - `bun run typecheck` + `bun run lint` clean.
  </acceptance_criteria>
  <done>The Overview hero curves + legend render TOS magenta/cyan; the Analyzer chart is untouched; a human pixel-check is queued for end-of-phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| date-input value → client scenario | The `<input type="date">` value is a client-only projection parameter routed through the tested Plan-01 helpers; it never reaches the server. Range is enforced by native `min`/`max` AND clamped in code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17.1-04 | Tampering | native date input range | low | mitigate | Native `min`/`max` can be bypassed via devtools; `resolveDaysForward` (Plan 01) clamps `daysForward` into `[0, maxDaysForward]` in code, so an out-of-range value cannot produce a degenerate projection past the front expiry. Defense-in-depth beyond the native attributes. |
| — Supply chain | Tampering | npm/pip/cargo installs | n/a | n/a | Zero new dependencies — native `<input type="date">`, no new shadcn/registry block (v1.2 lock). Not applicable. |
</threat_model>

<verification>
- `bunx vitest run apps/web/src/screens/Overview.test.tsx apps/web/src/screens/Analyzer.test.tsx apps/web/src/lib/scenario-engine.test.ts` green.
- `bun run typecheck` + `bun run lint` clean.
- @exp curve invariant across daysForward (Task 2); Overview curves magenta/cyan, Analyzer unchanged (Task 3).
- Human pixel-check queued for end-of-phase verification (D-03a).
</verification>

<success_criteria>
The Overview has a working TOS-style date picker that projects the today curve to a chosen date
(range-clamped, @exp fixed), and the payoff hero renders the pre-approved TOS magenta/cyan colors
+ legend without disturbing the Analyzer's identical chart.
</success_criteria>

<output>
Create `.planning/phases/17.1-overview-payoff-tos-fidelity-graph-interactivity/17.1-04-SUMMARY.md` when done.
</output>
