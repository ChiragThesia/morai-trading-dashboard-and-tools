# Phase 17.1: Overview Payoff — TOS-Fidelity Graph + Interactivity - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-03
**Phase:** 17.1-overview-payoff-tos-fidelity-graph-interactivity
**Areas discussed:** Date-projection control, Calendar series selection, TOS graph fidelity, Positions-box date format

---

## Date-projection control (OVW-05)

| Option | Description | Selected |
|--------|-------------|----------|
| Calendar date picker (TOS-style) | Real date picker; reproject T+0 → T+date via daysForward, @exp fixed | |
| Days-forward slider (reuse Analyzer) | Reuse the Analyzer's +Nd slider; least new code | |
| Picker + step arrows | Calendar picker plus ‹ › day-step arrows (most TOS-faithful) | ✓ |

**User's choice:** Picker + step arrows.
**Notes:** Picking a future date reprojects the "today" curve to that date; @exp curve stays fixed (TOS model).

---

## Calendar series selection (OVW-06)

| Option | Description | Selected |
|--------|-------------|----------|
| Unify — one checkbox = total + graph | Existing "Include in total" checkbox also drives the graph | (assumed pending research) |
| Separate graph-only visibility | Independent per-calendar chart show/hide, decoupled from P&L total | |

**User's choice:** "Follow what TOS does — the chart changes based on deselecting the calendars; check how TOS does it, do that."
**Notes:** Requires research into TOS's exact Risk Profile include coupling. Today the Overview row checkboxes drive the total/greeks but NOT the graph (decoupled) — that's the gap. Working assumption: unified, pending TOS-behavior research.

---

## TOS graph fidelity (OVW-04)

| Option | Description | Selected |
|--------|-------------|----------|
| TOS behavior + scaling, Morai colors | Match TOS structure/scaling, keep Morai palette | |
| Match TOS exactly | Magenta today/date + cyan @exp, TOS ticks + scaling | ✓ |
| Keep current style, fix scaling/ticks only | Minimal change to violet/gray | |

**User's choice:** Match TOS exactly.
**Notes:** Overrides Morai's brand palette for the payoff hero — explicit user instruction (user > design system). Expect a UI-checker brand-divergence flag; pre-approved. Researcher to capture exact TOS colors/ticks/scaling from screenshots.

---

## Positions-box date format (OVW-03)

| Option | Description | Selected |
|--------|-------------|----------|
| Replace DTE cell (dates + width, DTEs inline) | `Aug 8 → Sep 5` / `32d/59d · 27d wide` | ✓ |
| Add a separate EXP column | Keep DTE column, add EXP column | |
| Stacked two-line cell | Dates on top, DTEs + width below | |

**User's choice:** Replace DTE cell (dates + width, DTEs inline).
**Notes:** Short-month dates (Aug 8), front→back expiries, days-between = backDte − frontDte shown as "· 27d wide".

## Claude's Discretion

- Zero-dep date-picker implementation (native `<input type="date">` or small custom popover), step increment (1 day), date-range clamp, exact TOS hex colors, tick density, axis-scaling algorithm.

## Deferred Ideas

- Simulated/example calendars, "Add Simulated Trades", candidate overlays — OUT (Analyzer → Picker, Phases 18–19). Overview stays view-only.
- Phase 17 open review warnings WR-02 / WR-04 / WR-05 — tracked in 17-REVIEW.md, not this phase.
- Reviewed todos not folded: `03-code-review-followups.md`, `over-engineering-cleanup.md` (both stale Phase-3/v1.0, unrelated).
