---
phase: 17.1-overview-payoff-tos-fidelity-graph-interactivity
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/charts/PayoffChart.tsx
  - apps/web/src/components/charts/PayoffChart.test.tsx
  - apps/web/src/index.css
autonomous: true
requirements: [OVW-04]
must_haves:
  truths:
    - "The payoff y-axis is scaled from BOTH the today/date curve and the @exp curve, so a near-flat T+0 line is never squashed against a tall @exp tent."
    - "X-axis ticks are evenly-spaced round numbers derived from the domain, not a hardcoded literal array."
    - "PayoffChart accepts per-curve stroke-color props; its defaults keep the Analyzer's brand (violet/gray) unchanged."
    - "PayoffChart no longer calls setState or the parent onFitYConsumed callback from inside useMemo (WR-03), while the fitY reset and lock-on-signature behavior still work."
  artifacts:
    - apps/web/src/components/charts/PayoffChart.tsx
    - apps/web/src/index.css
  key_links:
    - "computeYDomain(todayCurve, expCurve) two-arg signature is re-exported and consumed by the component body + existing tests."
    - "todayCurveColor / expirationCurveColor props are the seam Plan 04 uses to inject TOS magenta/cyan from the Overview only."
    - "--color-tos-magenta token in index.css @theme backs both the SVG stroke (var()) and the Tailwind bg-tos-magenta legend swatch."
---

<objective>
Restyle `PayoffChart.tsx` toward TOS Risk Profile fidelity (OVW-04, D-03): fix the y-axis
auto-scale to combine both curves, replace the hardcoded x-tick array with a derived
round-number generator, make the two P&L curve colors prop-configurable (so the TOS override
can be scoped to the Overview hero without touching the Analyzer's identical chart), and clear
the in-scope WR-03 setState-in-`useMemo` warning while this file is open.

Purpose: PayoffChart is shared by BOTH `Overview.tsx` and `Analyzer.tsx` — a blanket color
constant swap would leak the pre-approved brand divergence into the Analyzer. The color must be
a prop with brand defaults. The y-domain and x-tick math are the "wonky rendering" named in
OVW-04's success criterion.
Output: chart internals (two-curve y-domain, `buildXTicks`, color props, WR-03 fix) +
the `--color-tos-magenta` design token. Actual magenta/cyan application from the Overview is
Plan 04.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17.1-overview-payoff-tos-fidelity-graph-interactivity/17.1-CONTEXT.md
@.planning/phases/17.1-overview-payoff-tos-fidelity-graph-interactivity/17.1-RESEARCH.md
@.planning/phases/17.1-overview-payoff-tos-fidelity-graph-interactivity/17.1-UI-SPEC.md
@.planning/phases/17-overview-v2-redesign-iv-calibration-fix/17-REVIEW.md
@.claude/rules/tdd.md
@.claude/rules/typescript.md
@apps/web/src/components/charts/PayoffChart.tsx
@apps/web/src/components/charts/PayoffChart.test.tsx
@apps/web/src/screens/Analyzer.tsx
@apps/web/src/index.css
</context>

## Artifacts this phase produces

- `apps/web/src/index.css`: new `@theme` token `--color-tos-magenta: #e91e8c` (added beside the existing `--color-violet`/`--color-cyan`; `--color-cyan: #22d3ee` already exists — reuse it, do not re-add).
- `apps/web/src/components/charts/PayoffChart.tsx`:
  - Changed `computeYDomain` signature: `computeYDomain(todayCurve: ReadonlyArray<PayoffPoint>, expCurve: ReadonlyArray<PayoffPoint>): { lo; hi }` (combines both curves; still floors at 0 / caps at 0 as today).
  - New exported pure `buildXTicks(min: number, max: number, targetCount = 5): ReadonlyArray<number>` — round-number ticks from the domain.
  - New optional props `todayCurveColor?: string` (default `VIOLET` `#a78bfa`) and `expirationCurveColor?: string` (default `GRAY_MUTED` `#7b8696`) on `PayoffChartProps`.
  - Re-export list updated to include `buildXTicks`.
  - WR-03: y-domain lock + fitY handled without setState/parent-callback inside `useMemo`.

<tasks>

<task type="tdd">
  <name>Task 1: Two-curve computeYDomain + buildXTicks (RED→GREEN)</name>
  <files>apps/web/src/components/charts/PayoffChart.tsx, apps/web/src/components/charts/PayoffChart.test.tsx</files>
  <read_first>
    - apps/web/src/components/charts/PayoffChart.tsx (computeYDomain at line 146; hardcoded x-tick array at line 385; X_MIN/X_MAX at lines 99-100; re-export at line 816)
    - apps/web/src/components/charts/PayoffChart.test.tsx (existing computeYDomain call sites — must be updated to the two-arg signature, Pitfall 4)
    - .planning/phases/17.1-overview-payoff-tos-fidelity-graph-interactivity/17.1-RESEARCH.md (Pattern 3 computeYDomain, Pattern 4 buildXTicks)
    - .planning/phases/17.1-overview-payoff-tos-fidelity-graph-interactivity/17.1-UI-SPEC.md ("TOS-fidelity payoff chart" component contract: Y-axis auto-scale, X-axis ticks)
  </read_first>
  <behavior>
    - `computeYDomain(todayCurve, expCurve)` returns a domain covering the combined min/max of BOTH curves: given a near-flat today curve (pl ≈ 0 everywhere) and a tall @exp tent, `lo <= min(both)` and `hi >= max(both)`; the today curve occupies a non-trivial fraction of the range (not squashed to a hairline).
    - `computeYDomain` with two empty curves returns the `{ lo: -500, hi: 500 }` fallback.
    - `buildXTicks(6900, 7900)` returns evenly-spaced round numbers spanning the domain (step snaps to 200 → the five ticks 7000..7800), ascending, each a multiple of the chosen round step.
    - `buildXTicks(min, max)` ticks all lie within `[min, max]` and are strictly increasing for any positive range.
  </behavior>
  <action>
    RED first: update the existing `computeYDomain` test cases in `PayoffChart.test.tsx` to the
    two-argument signature and add `buildXTicks` cases; run and confirm they fail for the right
    reason. Then change `computeYDomain` to accept `(todayCurve, expCurve)` and fold both curves'
    points into the min/max scan (spread both, or iterate both) — keep the existing 0.18 padding
    and the `if (lo > 0) lo = 0` / `if (hi < 0) hi = 0` clamps. Add pure `buildXTicks(min, max,
    targetCount = 5)` using a round-step snap (candidate steps ascending, pick the smallest `>=`
    the raw step), first tick at `Math.ceil(min / step) * step`. Update the component body to call
    `computeYDomain(todayCurve, baseExpirationCurve)` (it currently passes only the exp curve) and
    to render x-tick labels from `buildXTicks(X_MIN, X_MAX)` in place of the hardcoded five-value
    strike-label array. Add `buildXTicks` to the bottom `export { ... }` re-export beside
    `computeYDomain`. `grep -rn computeYDomain apps/web/src` first and update every call site in
    the same commit (Pitfall 4). No `any`/`as`/`!`.
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/components/charts/PayoffChart.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `bunx vitest run apps/web/src/components/charts/PayoffChart.test.tsx` passes with the two-curve `computeYDomain` and `buildXTicks` cases.
    - Behavior assertion: a near-flat today curve + tall @exp tent yields a domain where the today curve's rendered vertical span is a non-trivial fraction of the axis (encoded as a numeric assertion, e.g. today-range / total-range above a threshold).
    - `bun run typecheck` clean — no stale single-arg `computeYDomain` call site remains (Pitfall 4).
  </acceptance_criteria>
  <done>Y-domain combines both curves; x-ticks are derived round numbers; both re-exported and typecheck-clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-03 — remove setState/parent-callback from useMemo, preserve fitY + lock</name>
  <files>apps/web/src/components/charts/PayoffChart.tsx, apps/web/src/components/charts/PayoffChart.test.tsx</files>
  <read_first>
    - apps/web/src/components/charts/PayoffChart.tsx (y-domain lock useMemo lines 210-227; fitY / onFitYConsumed props; gridLines memo lines 298-305)
    - apps/web/src/screens/Analyzer.tsx (fitY wiring at lines 506-507, 773, 813-814 — the ACTIVE fitY consumer that must keep working)
    - .planning/phases/17-overview-v2-redesign-iv-calibration-fix/17-REVIEW.md (WR-03, lines 111-119 — the exact fix guidance)
    - apps/web/src/components/charts/PayoffChart.test.tsx (existing render tests that must stay green)
  </read_first>
  <behavior>
    - When `positionSetSignature` changes, the y-domain recomputes from the current curves (lock-on-signature) — the axis does NOT rescale on every render (e.g. when the date picker later moves the today curve without changing the signature).
    - When `fitY` is true, the y-domain refits AND `onFitYConsumed()` is called exactly once — but never from inside a `useMemo` render body (no "Cannot update a component while rendering" path).
    - The Analyzer's Fit-Y button (`fitY={fitY && !fitYConsumed}`) still refits the axis (regression: existing Analyzer render tests stay green).
  </behavior>
  <action>
    Replace the two side-effecting `useMemo` blocks (WR-03: one calls `setYDomain`/`setYDomainSig`
    during render, one calls `onFitYConsumed()` during render). Derive the locked y-domain without
    a render-phase setState — either compute it in a `useMemo` keyed on `positionSetSignature` that
    RETURNS the domain value (referencing the current curves at lock time), or use `useState` +
    `useEffect` keyed on the signature; move the `onFitYConsumed()` call into a `useEffect` keyed on
    `fitY`. Preserve the "y-axis stays put while the today curve moves" contract (this is why the
    lock exists — the date picker in Plan 04 will move the today curve and the axis must not jump).
    Follow the 17-REVIEW WR-03 fix note verbatim. Do NOT change the fitY/onFitYConsumed prop
    contract (Analyzer depends on it). WR-02/WR-04/WR-05 are OUT OF SCOPE — do not touch
    scenario-engine.ts or its greek strips.
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/components/charts/PayoffChart.test.tsx apps/web/src/screens/Analyzer.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: no `setYDomain` / `setYDomainSig` / `onFitYConsumed` call appears inside a `useMemo` body (moved to effect or render-derived).
    - PayoffChart.test.tsx and Analyzer.test.tsx both green (fitY regression clear).
    - `bun run typecheck` clean.
  </acceptance_criteria>
  <done>WR-03 cleared: no setState/parent-callback inside useMemo; fitY reset and signature-lock behavior preserved; Analyzer unaffected.</done>
</task>

<task type="auto">
  <name>Task 3: Curve-color props (brand defaults) + --color-tos-magenta token</name>
  <files>apps/web/src/components/charts/PayoffChart.tsx, apps/web/src/index.css, apps/web/src/components/charts/PayoffChart.test.tsx</files>
  <read_first>
    - apps/web/src/components/charts/PayoffChart.tsx (color constants lines 102-112; T+0 curve stroke line 602-615; T+0 BE lines 576-600; @exp curve stroke lines 446-460; @exp BE lines 549-574)
    - apps/web/src/index.css (@theme block lines 6-27 — --color-violet line 21, --color-cyan line 25 already present)
    - apps/web/src/screens/Analyzer.tsx (renders PayoffChart WITHOUT the new color props → must keep violet/gray)
    - .planning/phases/17.1-overview-payoff-tos-fidelity-graph-interactivity/17.1-UI-SPEC.md (Color override table: which strokes/BE lines change)
  </read_first>
  <action>
    Add `--color-tos-magenta: #e91e8c;` to the `@theme` block in `index.css` (beside the existing
    tokens; the `--color-cyan` token already exists — reuse it). Add two optional props to
    `PayoffChartProps`: `todayCurveColor?: string` (default the existing `VIOLET`) and
    `expirationCurveColor?: string` (default the existing `GRAY_MUTED`). Thread `todayCurveColor`
    into the T+0 curve `stroke` (line ~610) and the T+0 BE line/label strokes (layer 8); thread
    `expirationCurveColor` into the @exp curve `stroke` (line ~454, keep the `strokeDasharray "5 4"`
    and opacity unchanged) and the @exp BE line/label strokes (layer 7). Leave gridlines, zero
    line, GEX walls, spot marker, profit-zone fill, glow filter, and the highlighted single-position
    curves UNCHANGED. Because the defaults equal today's constants, `Analyzer.tsx` (which passes
    neither prop) renders identically — this is the scoping mechanism for the pre-approved brand
    divergence (D-03: hero only). The actual magenta/cyan values are injected from the Overview in
    Plan 04. [ASSUMED] hex per RESEARCH A1 — a human pixel-check is retained in Plan 04.
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/components/charts/PayoffChart.test.tsx apps/web/src/screens/Analyzer.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Render assertion (new test): with no color props, the T+0 curve stroke is `#a78bfa` (VIOLET) and the @exp curve stroke is `#7b8696` (GRAY_MUTED) — Analyzer default preserved.
    - Render assertion (new test): passing `todayCurveColor="#e91e8c"` / `expirationCurveColor="#22d3ee"` sets those strokes on the respective LinePaths.
    - Source assertion: `index.css` contains `--color-tos-magenta`.
    - `bun run typecheck` + `bun run lint` clean.
  </acceptance_criteria>
  <done>Curve colors are prop-driven with brand defaults; the magenta token exists; Analyzer's payoff chart is visually unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API data → SVG render | `PayoffChart` renders numbers already sourced from `usePositions()`/`useGex()` (Zod-validated at the contract boundary) into SVG text/paths. No new user-supplied string is rendered; no new input crosses a boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17.1-02 | Information Disclosure / Injection | `PayoffChart` SVG text nodes | low | accept | Values are pre-validated numeric curve/GEX data, not free-form user strings; SVG numeric coordinates carry no injection surface. No new boundary created by this restyle. |
| — Supply chain | Tampering | npm/pip/cargo installs | n/a | n/a | Zero new dependencies (visx already installed; v1.2 lock). Not applicable. |
</threat_model>

<verification>
- `bunx vitest run apps/web/src/components/charts/PayoffChart.test.tsx apps/web/src/screens/Analyzer.test.tsx` green.
- `bun run typecheck` + `bun run lint` clean (catches any missed `computeYDomain` call site — Pitfall 4).
- Source check: no setState/parent-callback inside any `useMemo` (WR-03); `--color-tos-magenta` present in `index.css`.
</verification>

<success_criteria>
The payoff chart's y-axis combines both curves, x-ticks are derived round numbers, curve colors
are prop-scoped with brand defaults (Analyzer untouched), and WR-03 is cleared — all with the
Analyzer and Overview chart tests green. The TOS magenta/cyan values are ready for Plan 04 to
inject on the Overview hero only.
</success_criteria>

<output>
Create `.planning/phases/17.1-overview-payoff-tos-fidelity-graph-interactivity/17.1-02-SUMMARY.md` when done.
</output>
