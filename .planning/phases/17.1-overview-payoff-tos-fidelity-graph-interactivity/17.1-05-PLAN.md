---
phase: 17.1-overview-payoff-tos-fidelity-graph-interactivity
plan: 05
type: execute
wave: 3
depends_on: [17.1-04]
files_modified:
  - apps/web/src/screens/Overview.tsx
  - apps/web/src/screens/Overview.test.tsx
autonomous: true
requirements: [OVW-03]
must_haves:
  truths:
    - "Each calendar positions-box row shows both leg expiration dates (Aug 8 → Sep 5) with both DTEs and the calendar width beneath (32d/59d · 27d wide)."
    - "Single-leg rows show the one expiry date and its DTE, with no calendar-width line."
    - "The positions column header reads Expiry / DTE."
  artifacts:
    - apps/web/src/screens/Overview.tsx
  key_links:
    - "formatExpiryCell derives line1 from parseOccSymbol(front/back occSymbol) and line2 from CalendarGroup.dteFront/dteBack (days-between = dteBack − dteFront)."
    - "buildRows Row type carries the structured expiry cell in place of the single dte string."
---

<objective>
Fix OVW-03: replace the compact single-line `Nd → Nd` DTE cell in the docked positions table
with a two-line expiries + calendar-width cell — `Aug 8 → Sep 5` over `32d/59d · 27d wide` for
calendars, `Aug 8` over `32d` for single legs — and rename the column header to `Expiry / DTE`
(D-04).

Purpose: the user needs each calendar's actual leg expiration dates and its width (days between
the two expiries), not just relative DTEs. The data already exists on `CalendarGroup`
(`dteFront`/`dteBack`) plus a `parseOccSymbol` call for the two expiry dates that `buildRows`
does not yet make for calendar rows.
Output: a tested pure `formatExpiryCell` helper + the reformatted table cell/header.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17.1-overview-payoff-tos-fidelity-graph-interactivity/17.1-CONTEXT.md
@.planning/phases/17.1-overview-payoff-tos-fidelity-graph-interactivity/17.1-RESEARCH.md
@.planning/phases/17.1-overview-payoff-tos-fidelity-graph-interactivity/17.1-UI-SPEC.md
@.claude/rules/tdd.md
@.claude/rules/typescript.md
@apps/web/src/screens/Overview.tsx
@apps/web/src/screens/Overview.test.tsx
@apps/web/src/lib/pair-calendars.ts
</context>

## Artifacts this phase produces

- `apps/web/src/screens/Overview.tsx`:
  - New pure helper `formatExpiryCell(...)` returning `{ line1: string; line2: string }` — calendar: `line1 = "{frontExpiry} → {backExpiry}"` (short month/day via `toLocaleString("en-US", { month: "short", day: "numeric" })`), `line2 = "{dteFront}d/{dteBack}d · {dteBack − dteFront}d wide"`; single: `line1 = "{expiry}"`, `line2 = "{dte}d"`; `"—"` fallback when `parseOccSymbol` fails.
  - Changed `Row` type: the single `dte: string` field replaced by the structured expiry cell (line1/line2), populated in `buildRows`.
  - Changed `PositionsTable` DTE `<td>`: two stacked lines (flex-col; line1 11px `text-muted-foreground`, line2 9px `text-dim`).
  - Changed `COLS` header entry `DTE` → `Expiry / DTE` (keep `text-right`).
- No new files. (`BookSummary` uses only `Row.legs`, so it is unaffected by the `Row` shape change.)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure formatExpiryCell helper (RED→GREEN)</name>
  <files>apps/web/src/screens/Overview.tsx, apps/web/src/screens/Overview.test.tsx</files>
  <read_first>
    - apps/web/src/screens/Overview.tsx (buildRows 239-256; the singles DTE pattern 247-254; Row type 232-237; gexAsOf short-month convention 818-821)
    - apps/web/src/lib/pair-calendars.ts (CalendarGroup fields: front/back BrokerPositionResponse, dteFront/dteBack, key/strike/optionType — lines 16-30)
    - apps/web/src/screens/Overview.test.tsx (CAL_FRONT/CAL_BACK fixtures 106-124; POS single fixture 96-104)
    - .planning/phases/17.1-overview-payoff-tos-fidelity-graph-interactivity/17.1-UI-SPEC.md (Positions-box expiry cell copy, calendar + single-leg rows)
  </read_first>
  <behavior>
    - `formatExpiryCell` for a calendar with front expiry Aug 8 / back Sep 5, dteFront 32, dteBack 59 → `line1: "Aug 8 → Sep 5"`, `line2: "32d/59d · 27d wide"` (27 = 59 − 32).
    - For a single leg with expiry Aug 8, dte 32 → `line1: "Aug 8"`, `line2: "32d"` (no "wide").
    - When `parseOccSymbol` fails on an occSymbol, `line1` falls back to `"—"` without throwing.
  </behavior>
  <action>
    RED first: add unit tests for the calendar shape, the single-leg shape, and the parse-failure
    fallback. Then implement `formatExpiryCell` as a pure function: for a calendar, call
    `parseOccSymbol(front.occSymbol)` / `parseOccSymbol(back.occSymbol)` and format each expiry with
    `toLocaleString("en-US", { month: "short", day: "numeric" })` (matches the existing `gexAsOf`
    convention); build `line2` from `dteFront`/`dteBack` and days-between `dteBack − dteFront`. For a
    single leg, format the one expiry + `{dte}d`. Guard the `parseOccSymbol` `Result` (`.ok`) and
    return `"—"` on failure — no `!`, no `as`. Keep it exported (or module-local) but independently
    testable without a full render.
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/screens/Overview.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Tests green for calendar (`Aug 8 → Sep 5` / `32d/59d · 27d wide`), single (`Aug 8` / `32d`), and parse-failure (`—`).
    - Days-between is `dteBack − dteFront` (not a re-parse) — asserted by the 27 case.
    - `bun run typecheck` clean.
  </acceptance_criteria>
  <done>formatExpiryCell renders the two-line expiry/DTE/width content and is unit-tested for both row shapes + fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Reformat the positions-box DTE cell + column header</name>
  <files>apps/web/src/screens/Overview.tsx, apps/web/src/screens/Overview.test.tsx</files>
  <read_first>
    - apps/web/src/screens/Overview.tsx (Row type 232-237; buildRows 239-256; COLS 258; DTE td 413-414; PositionsTable render; BookSummary buildRows use 524-525)
    - .planning/phases/17.1-overview-payoff-tos-fidelity-graph-interactivity/17.1-UI-SPEC.md (Positions-box expiry/DTE reformat component contract: header rename, two-line cell, typography 11px/9px)
  </read_first>
  <action>
    Replace `Row.dte: string` with the structured expiry cell (line1/line2 from `formatExpiryCell`),
    populate it in `buildRows` for both calendar and single-leg rows. Render the DTE `<td>` as a
    `flex-col`: line1 at `text-[11px] text-muted-foreground`, line2 at `text-[9px] text-dim`
    (inherited Phase-9 sizes — introduce no new size). Rename the `COLS` `DTE` entry to
    `Expiry / DTE`, keeping the existing right-alignment. `BookSummary` reads only `Row.legs`, so it
    keeps compiling — verify no other `Row.dte` reader remains. No new spacing tokens; reuse existing
    cell padding.
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/screens/Overview.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Render test: a calendar row shows `Aug 8 → Sep 5` and `27d wide`; a single-leg row shows its expiry + `{dte}d`; the column header shows `Expiry / DTE`.
    - Source assertion: no remaining reference to the old `Row.dte` string field (`grep` for the removed field name returns no reader).
    - `bun run typecheck` + `bun run lint` clean; existing Overview tests (row rendering, live overlay) stay green.
  </acceptance_criteria>
  <done>The positions box shows both expiries, both DTEs, and the calendar width; the header reads Expiry / DTE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API data → cell text | The expiry cell formats `occSymbol`/`dte` values already fetched from `usePositions()` (Zod-validated at the contract boundary). No new input crosses a boundary; formatting is client-side over validated data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17.1-05 | Information Disclosure | `formatExpiryCell` rendered text | low | accept | Renders parsed OCC-symbol expiry dates + integer DTEs already present in the validated broker payload; `parseOccSymbol` failure returns `"—"` (no throw, no leaked raw symbol). No new boundary. |
| — Supply chain | Tampering | npm/pip/cargo installs | n/a | n/a | Zero new dependencies. Not applicable. |
</threat_model>

<verification>
- `bunx vitest run apps/web/src/screens/Overview.test.tsx` green (formatExpiryCell unit + cell/header render).
- `bun run typecheck` + `bun run lint` clean.
- No stale `Row.dte` reader; `BookSummary` unaffected.
</verification>

<success_criteria>
The positions box shows each calendar's two leg expiration dates, both DTEs, and the calendar
width (days between), and single legs show their one expiry + DTE — under an `Expiry / DTE`
column header, matching the UI-SPEC copy.
</success_criteria>

<output>
Create `.planning/phases/17.1-overview-payoff-tos-fidelity-graph-interactivity/17.1-05-SUMMARY.md` when done.
</output>
