---
status: testing
phase: 17.1-overview-payoff-tos-fidelity-graph-interactivity
source: [17.1-VERIFICATION.md]
started: 2026-07-04T02:33:08Z
updated: 2026-07-04T02:33:08Z
---

## Current Test

number: 1
name: OVW-04 — TOS Risk Profile visual pixel-check (curve colors, tick spacing, axis scaling)
expected: |
  The Overview payoff-hero curves, tick density, and axis auto-scale visually read as a
  TOS Risk Profile chart. If the magenta/cyan hex mismatch the real TOS palette, adjust only
  those two `--color-tos-magenta` / `--color-cyan` hex values (curve roles and the
  override-not-blend rule are locked regardless of the final hex).
awaiting: user response

## Tests

### 1. OVW-04 — TOS Risk Profile visual pixel-check

expected: |
  Re-drop `tos-risk-profile-combined-graph.png` and `tos-analyze-date-picker.png` into
  `mockups/tos-reference/` (currently only the README exists), open the Overview in a browser,
  and pixel-compare the today/date (magenta `#e91e8c`) + @exp (cyan `#22d3ee`) curve colors,
  tick spacing, and axis scaling against the real TOS Risk Profile chart. The hero should read
  as a TOS Risk Profile combined chart. If the magenta/cyan hex mismatch the real TOS palette,
  adjust only those two hex values — curve roles and the override-not-blend rule stay locked.
why_human: |
  OVW-04's success criterion is a visual-fidelity claim that grep/unit tests cannot judge. The
  two reference screenshots the UI-SPEC calls for were never re-dropped into
  `mockups/tos-reference/` (verified: only README.md present), so the magenta/cyan hex values are
  explicitly `[ASSUMED]` in UI-SPEC, PLANs 02/04, and their SUMMARYs. Pre-planned deferral
  (plan 04 `human_verify_mode: end-of-phase`), not a gap introduced by verification.
result: [pending]

## Summary

total: 1
passed: 0
issues: 0
pending: 1
skipped: 0
blocked: 0

## Gaps
