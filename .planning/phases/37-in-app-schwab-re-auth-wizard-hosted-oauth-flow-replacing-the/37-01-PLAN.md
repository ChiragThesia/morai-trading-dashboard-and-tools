---
phase: 37-in-app-schwab-re-auth-wizard-hosted-oauth-flow-replacing-the
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture/stack-decisions.md
  - packages/adapters/src/postgres/schema.ts
  - packages/adapters/src/postgres/migrations/0024_reauth_nonces.sql
  - packages/adapters/src/postgres/migrations/meta/_journal.json
autonomous: true
requirements: [REAUTH-02, REAUTH-07]
must_haves:
  truths:
    - "A new architecture decision entry documents the wizard-as-primary / CLI-as-fallback re-auth path, the Drizzle-tracked-but-Python-written nonce table, and the SIDECAR_ADMIN_TOKEN / SCHWAB_WEB_CALLBACK_URL secrets — written BEFORE any implementation plan runs (Docs Before Code, workflow.md)."
    - "A reauth_nonces table exists (state PK, app_id, created_at) as Drizzle-tracked DDL, mirroring broker_tokens' split ownership (declared in schema.ts, written only by the Python sidecar)."
  artifacts:
    - docs/architecture/stack-decisions.md
    - packages/adapters/src/postgres/schema.ts
    - packages/adapters/src/postgres/migrations/0024_reauth_nonces.sql
  key_links:
    - "schema.ts reauthNonces entry ↔ migration 0024 SQL ↔ meta/_journal.json idx 24 (all three must agree or drizzle drift-checks fail)."
---

<objective>
Land the docs-first architecture decision and the database foundation (the CSRF nonce table)
for the in-app Schwab re-auth wizard, before any code plan runs.

Purpose: workflow.md mandates "Docs Before Code" — this phase introduces a new bounded
context (reauth), a new Postgres table, and a new shared secret, so the decision record is
updated first. The nonce table is the storage for the single-use CSRF `state` (REAUTH-02);
declaring it here unblocks the sidecar exchange plan (37-04).
Output: a new decision entry in stack-decisions.md; the `reauthNonces` Drizzle table + the
`0024_reauth_nonces.sql` migration + its journal entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-in-app-schwab-re-auth-wizard-hosted-oauth-flow-replacing-the/37-CONTEXT.md
@.planning/phases/37-in-app-schwab-re-auth-wizard-hosted-oauth-flow-replacing-the/37-RESEARCH.md
@.planning/phases/37-in-app-schwab-re-auth-wizard-hosted-oauth-flow-replacing-the/37-PATTERNS.md

# The nonce table follows the brokerTokens precedent (Drizzle-declared, Python-written).
@packages/adapters/src/postgres/schema.ts
@packages/adapters/src/postgres/migrations/0022_rule_overrides.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Docs-first architecture decision entry</name>
  <files>docs/architecture/stack-decisions.md</files>
  <action>
    Add ONE new decision entry to docs/architecture/stack-decisions.md documenting the Phase 37
    re-auth architecture (REAUTH-07). Grep the file for its current highest `### D` heading number
    at execution time and use the next free number (RESEARCH observed D22 at stack-decisions.md;
    later phases may have added more — verify, do not assume D23 is free). The entry MUST record:
    (1) the in-app hosted wizard becomes the PRIMARY Schwab re-auth path; the local CLI dance
    (seed_token.py, 127.0.0.1:8182 callback) remains the documented fallback, unchanged;
    (2) a new `reauth_nonces` table is Drizzle-tracked in schema.ts but written only by the Python
    sidecar via psycopg2 — the exact split-ownership precedent already set by broker_tokens;
    (3) `SIDECAR_ADMIN_TOKEN` (shared secret gating the two new sidecar admin endpoints, since the
    sidecar has no auth today) and `SCHWAB_WEB_CALLBACK_URL` (the registered https://morai.wtf
    callback) are new Railway env vars on BOTH the server and sidecar services;
    (4) the re-auth admin surface is HTTP-only behind the existing Supabase JWT — MCP is explicitly
    scoped OUT (no MCP tool mints or exchanges auth URLs). Match the file's existing decision-entry
    prose style and Hemingway tone (docs.md). Do not restate code; reference file paths + behavior.
  </action>
  <verify>
    <automated>test -f docs/architecture/stack-decisions.md &amp;&amp; grep -Eiq 'reauth_nonces' docs/architecture/stack-decisions.md &amp;&amp; grep -Eiq 'SIDECAR_ADMIN_TOKEN' docs/architecture/stack-decisions.md &amp;&amp; grep -Eiq 'SCHWAB_WEB_CALLBACK_URL' docs/architecture/stack-decisions.md &amp;&amp; echo OK</automated>
  </verify>
  <done>stack-decisions.md has a new numbered decision entry naming reauth_nonces, SIDECAR_ADMIN_TOKEN, SCHWAB_WEB_CALLBACK_URL, wizard-as-primary/CLI-as-fallback, and MCP-out.</done>
</task>

<task type="auto">
  <name>Task 2: reauth_nonces table + migration 0024</name>
  <files>packages/adapters/src/postgres/schema.ts, packages/adapters/src/postgres/migrations/0024_reauth_nonces.sql, packages/adapters/src/postgres/migrations/meta/_journal.json</files>
  <action>
    Declare a `reauthNonces` pgTable in packages/adapters/src/postgres/schema.ts using the exact
    shape and conventions of the existing single-row/small tables (e.g. ruleOverrides): columns
    `state: text("state").primaryKey()`, `appId: text("app_id").notNull()`,
    `createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow()`, chained
    with `.enableRLS()` (every table in schema.ts calls bare `.enableRLS()` with no explicit
    policy — follow that convention, do not invent a policy). Add the migration file
    `0024_reauth_nonces.sql` (next free index — confirmed via meta/_journal.json's last entry idx
    23 "0023_gex_implied_carry"): a `CREATE TABLE "reauth_nonces"` with the same three columns +
    `--> statement-breakpoint` + `ALTER TABLE "reauth_nonces" ENABLE ROW LEVEL SECURITY;`, matching
    the byte-shape of prior migration files. Append the corresponding idx-24 entry to
    meta/_journal.json (version "7", a fresh `when` epoch-ms, tag "0024_reauth_nonces",
    breakpoints true) so drizzle's journal stays consistent. Prefer running the project's drizzle
    generate command if one exists to author the SQL + journal deterministically; otherwise author
    the three artifacts by hand so all three agree. This table is written ONLY by the Python
    sidecar (37-04) — no TS repo or in-memory twin is created here (HTTP/Python-owned, not a
    TS-driven port).
  </action>
  <verify>
    <automated>test -f packages/adapters/src/postgres/migrations/0024_reauth_nonces.sql &amp;&amp; grep -q 'reauth_nonces' packages/adapters/src/postgres/migrations/0024_reauth_nonces.sql &amp;&amp; grep -q 'reauthNonces' packages/adapters/src/postgres/schema.ts &amp;&amp; grep -q '0024_reauth_nonces' packages/adapters/src/postgres/migrations/meta/_journal.json &amp;&amp; cd packages/adapters &amp;&amp; bunx tsc --noEmit -p tsconfig.json 2>/dev/null; echo "typecheck exit: $?"</automated>
  </verify>
  <done>schema.ts has reauthNonces (state PK/app_id/created_at, .enableRLS()); 0024_reauth_nonces.sql creates the table + enables RLS; _journal.json has the idx-24 entry; adapters typecheck passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sidecar → Postgres | The CSRF nonce (untrusted-origin `state` value) is persisted and later validated here; the table is the single source of truth for replay-kill. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-01 | Spoofing/Tampering | reauth_nonces table (schema/migration) | high | mitigate | Table exists with `state` as PRIMARY KEY (uniqueness) + `created_at` for the 10-minute TTL predicate; single-use consumption logic lands in 37-04's atomic `DELETE ... RETURNING`. This plan provides the storage the control depends on. |
| T-37-SC | Tampering | package installs | low | accept | No npm/pip/cargo installs in this phase (RESEARCH Package Legitimacy Audit: zero new dependencies). No slopcheck checkpoint required. |
</threat_model>

<verification>
- `docs/architecture/stack-decisions.md` has the new decision entry (naming reauth_nonces,
  the two new secrets, wizard-primary/CLI-fallback, MCP-out).
- `schema.ts` compiles and declares `reauthNonces`; migration 0024 + journal idx-24 agree.
- No implementation code touched — this plan is docs + DDL only.
</verification>

<success_criteria>
Docs-first decision recorded; reauth_nonces table + migration 0024 + journal entry exist and
the adapters package typechecks. 37-04 can now persist/consume nonces against a declared table.
</success_criteria>

<output>
Create `.planning/phases/37-in-app-schwab-re-auth-wizard-hosted-oauth-flow-replacing-the/37-01-SUMMARY.md` when done.
</output>
