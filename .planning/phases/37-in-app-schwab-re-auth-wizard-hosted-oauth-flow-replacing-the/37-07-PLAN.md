---
phase: 37-in-app-schwab-re-auth-wizard-hosted-oauth-flow-replacing-the
plan: 07
type: execute
wave: 3
depends_on: ["37-04", "37-05", "37-06"]
files_modified:
  - docs/operations/schwab-reauth-runbook.md
autonomous: true
requirements: [REAUTH-07]
user_setup:
  - service: railway
    why: "The two admin endpoints mint Schwab tokens over Railway's private network; the shared secret must exist on BOTH services before deploy, and the sidecar needs the registered web callback URL."
    env_vars:
      - name: SIDECAR_ADMIN_TOKEN
        source: "Generate one strong random secret (>=16 chars); set the SAME value on the Railway server service AND the sidecar service."
      - name: SCHWAB_WEB_CALLBACK_URL
        source: "Set to https://morai.wtf (the registered callback) on the sidecar service; the server does not need it. Defaulted in sidecar config, so setting it is optional-but-explicit."
    dashboard_config:
      - task: "Confirm https://morai.wtf is registered as a callback URL on BOTH the trader AND market Schwab Developer Portal apps (RESEARCH Open Question 1 / Pitfall 4 — test trader first, then market)."
        location: "Schwab Developer Portal → each app → Callback URLs"
must_haves:
  truths:
    - "The runbook documents the in-app wizard as the primary re-auth path and keeps the CLI (seed_token.py) as the documented fallback."
    - "The full TS suite + sidecar pytest + typecheck + lint are green across every layer this phase touched."
    - "The next real re-auth (~2026-07-20) is performed through the wizard as the human UAT gate."
  artifacts:
    - docs/operations/schwab-reauth-runbook.md
  key_links:
    - "SIDECAR_ADMIN_TOKEN on server ↔ SIDECAR_ADMIN_TOKEN on sidecar (must be identical) — the header the adapter sends must match the header the sidecar guard checks."
---

<objective>
Close the phase: document the UI re-auth path in the runbook, run the full cross-layer test/
typecheck/lint gate, capture the Railway deploy prerequisites, and set the human UAT — performing
the next real re-auth through the wizard.

Purpose: the wizard's true acceptance is a live 7-day re-auth done from the browser instead of the
CLI. Everything is unit/contract-tested by 37-01..06; this plan proves the pieces integrate
(suites + typecheck + lint), records the operator runbook, and pins the one human gate this phase
cannot self-verify (an actual Schwab login round trip through morai.wtf).
Output: runbook UI-path section; a green full-suite gate; the deploy/env prerequisites; the human
UAT instruction.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/37-in-app-schwab-re-auth-wizard-hosted-oauth-flow-replacing-the/37-CONTEXT.md
@.planning/phases/37-in-app-schwab-re-auth-wizard-hosted-oauth-flow-replacing-the/37-RESEARCH.md

# The runbook to extend (UI path primary, CLI fallback stays):
@docs/operations/schwab-reauth-runbook.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Runbook — add the UI (wizard) path</name>
  <files>docs/operations/schwab-reauth-runbook.md</files>
  <action>
    Add a section to docs/operations/schwab-reauth-runbook.md documenting the in-app wizard as the
    PRIMARY re-auth path (REAUTH-07): from any authenticated screen, click Reconnect on the expiry
    banner, run Trader then Market, log into Schwab + authorize when redirected, and land back on
    morai.wtf where the app exchanges silently and the banner clears. Note the operator no longer
    needs a terminal or the CLI for a routine re-auth. Keep the existing CLI steps (seed_token.py
    login/authurl/exchange) as the explicitly-labeled FALLBACK (unchanged), for when the wizard is
    unavailable (e.g. the app itself is down) — including `railway redeploy --service sidecar` as
    the manual re-init fallback. Add a short troubleshooting note: a per-app "reconnect failed —
    Schwab didn't confirm a fresh token" means retry only that app (mirrors the CLI's "do NOT
    restart the sidecar; re-run for the failed app"). Hemingway style (docs.md); reference file
    paths + behavior, no code dumps. If TOPIC-MAP or a rule's "Where to Look" references this
    runbook, keep them consistent (docs.md maintenance rule).
  </action>
  <verify>
    <automated>grep -Eiq 'reconnect|wizard|banner' docs/operations/schwab-reauth-runbook.md &amp;&amp; grep -Eiq 'fallback' docs/operations/schwab-reauth-runbook.md &amp;&amp; echo OK</automated>
  </verify>
  <done>Runbook has a UI/wizard primary-path section and retains the CLI steps clearly marked as the fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite integration gate + deploy prerequisites + human UAT</name>
  <files>docs/operations/schwab-reauth-runbook.md</files>
  <action>
    Run the phase gate and confirm all layers integrate: `bun run test` (full TS workspace —
    contracts, core, adapters, server, web) AND `cd apps/sidecar &amp;&amp; python -m pytest` (sidecar),
    then `bun run typecheck &amp;&amp; bun run lint`. All must be green; fix any cross-layer wiring gap
    surfaced (e.g. a barrel export missed, a config field not threaded) before completing. Record the
    Railway deploy prerequisites in the runbook's deploy note (from this plan's user_setup):
    SIDECAR_ADMIN_TOKEN set to the SAME value on both the server and sidecar services, and
    SCHWAB_WEB_CALLBACK_URL=https://morai.wtf on the sidecar — both set BEFORE deploy (a mismatch
    makes every exchange 401; the wrong callback string makes Schwab reject the exchange with a
    redirect_uri mismatch, RESEARCH Pitfall 4). Do NOT commit any secret value — only the variable
    names + where they go. The live human UAT is captured as this plan's human-check below (the next
    real re-auth, ~2026-07-20, driven through the wizard) — deploy must land before that window so
    this re-auth cycle is the last CLI one.
  </action>
  <verify>
    <automated>bun run test 2>&amp;1 | tail -15 &amp;&amp; cd apps/sidecar &amp;&amp; python -m pytest -q 2>&amp;1 | tail -15</automated>
    <automated>bun run typecheck 2>&amp;1 | tail -8 &amp;&amp; bun run lint 2>&amp;1 | tail -8</automated>
    <human-check>
      Live UAT (next real re-auth window, ~2026-07-20): with SIDECAR_ADMIN_TOKEN set identically on
      both Railway services and https://morai.wtf registered on both Schwab apps, wait for the expiry
      banner, click Reconnect, complete Trader then Market through the Schwab login, and confirm on
      landing back at morai.wtf that (1) the URL has no ?code/&state (stripped), (2) both steps show
      "connected", (3) the banner clears within ~30s (next status poll), and (4) live data resumes —
      all WITHOUT touching the CLI or restarting the sidecar. If one app fails, confirm Retry
      re-auths only that app and the other stays connected.
    </human-check>
  </verify>
  <done>Full TS suite + sidecar pytest + typecheck + lint all green; deploy prerequisites recorded (no secrets committed); the live re-auth UAT instruction is captured for the ~2026-07-20 window.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Railway env → both services | The shared secret's correctness across two services is a deploy-time trust dependency; a mismatch silently 401s every exchange. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-03 | Elevation of Privilege | Railway SIDECAR_ADMIN_TOKEN setup | high | mitigate | Runbook + user_setup require the identical secret on both services before deploy; verified live by the human UAT (a successful exchange proves the header matches). |
| T-37-04 | Elevation of Privilege | live JWT-gated surface | high | mitigate | Human UAT confirms the wizard works only for an authenticated operator session on morai.wtf. |
| T-37-SC | Tampering | package installs | low | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: zero new deps) — no slopcheck checkpoint required. |
</threat_model>

<verification>
- `bun run test` + `python -m pytest` (sidecar) + `bun run typecheck` + `bun run lint` all green.
- Runbook documents the wizard primary path + CLI fallback + deploy prerequisites.
- Human UAT captured for the ~2026-07-20 real re-auth.
</verification>

<success_criteria>
Every layer integrates green; the operator runbook and deploy prerequisites are recorded; the
live re-auth UAT is pinned. After the ~2026-07-20 wizard re-auth passes, Phase 37 is complete and
this becomes the last CLI re-auth cycle.
</success_criteria>

<output>
Create `.planning/phases/37-in-app-schwab-re-auth-wizard-hosted-oauth-flow-replacing-the/37-07-SUMMARY.md` when done.
</output>
