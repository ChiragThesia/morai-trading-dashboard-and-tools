---
phase: 04-schwab-auth-brokerage
plan: 05
type: tdd
wave: 5
depends_on: ["04-04"]
files_modified:
  - packages/core/src/brokerage/application/ports.ts
  - packages/core/src/brokerage/application/getPositions.ts
  - packages/core/src/brokerage/application/getPositions.test.ts
  - packages/core/src/brokerage/application/getTransactions.ts
  - packages/core/src/brokerage/application/getTransactions.test.ts
  - packages/core/src/brokerage/index.ts
  - packages/core/src/index.ts
  - packages/adapters/src/schwab/trader/account-hash.ts
  - packages/adapters/src/schwab/trader/positions-adapter.ts
  - packages/adapters/src/schwab/trader/transactions-adapter.ts
  - packages/adapters/src/schwab/trader/orders-adapter.ts
  - packages/adapters/src/schwab/trader/trader-adapter.test.ts
  - packages/adapters/src/test/fixtures/schwab-positions.fixture.json
  - packages/adapters/src/test/fixtures/schwab-transactions.fixture.json
  - packages/adapters/src/memory/schwab-trader.ts
  - packages/adapters/src/index.ts
  - packages/contracts/src/brokerage.ts
  - packages/contracts/src/index.ts
  - apps/server/src/adapters/http/brokerage.routes.ts
  - apps/server/src/adapters/mcp/tools.ts
  - apps/server/src/adapters/mcp/server.ts
  - apps/server/src/main.ts
autonomous: true
requirements: [BRK-02]
must_haves:
  truths:
    - "The Schwab trader adapter returns positions, orders, and transactions behind their ports, Zod-parsed before reaching core (BRK-02, success criterion 5)"
    - "A failed parse surfaces a typed Result.err, not a thrown exception (BRK-02, success criterion 5)"
    - "getPositions / getTransactions ship both an HTTP route and an MCP tool sharing one contracts Zod schema (MCP-02)"
    - "Trader-app AUTH_EXPIRED pauses positions/orders/transactions while the market app keeps working (D-09)"
  artifacts:
    - path: "packages/adapters/src/schwab/trader/positions-adapter.ts"
      provides: "makeSchwabPositionsAdapter implementing ForFetchingPositions"
      contains: "ForFetchingPositions"
    - path: "packages/contracts/src/brokerage.ts"
      provides: "positionsResponse + transactionsResponse + ordersResponse Zod schemas"
      contains: "positionsResponse"
    - path: "apps/server/src/adapters/http/brokerage.routes.ts"
      provides: "GET /api/positions, /api/transactions, /api/orders"
      contains: "/positions"
  key_links:
    - from: "brokerage.routes.ts + mcp tools"
      to: "packages/contracts/src/brokerage.ts"
      via: "shared Zod schema parsed on both surfaces (MCP-02)"
      pattern: "positionsResponse"
    - from: "trader adapters"
      to: "account-hash.ts"
      via: "resolve hashValue from /accounts/accountNumbers before data calls"
      pattern: "hashValue"
---

<objective>
Read-only Schwab trader data (BRK-02): a trader adapter that resolves the account hash and fetches positions, orders, and transactions behind new ports, Zod-parsing every response before it reaches core. Then the `getPositions` / `getTransactions` / `getOrders` use-cases, their shared `contracts` schemas, and BOTH the HTTP routes and MCP tools (MCP-02). Trader-app AUTH_EXPIRED pauses these reads while the market app (04-04) keeps working (D-09).

Purpose: BRK-02 — positions/orders/transactions become queryable by API and Claude Code, the foundation Phase 5's fill-rebuild consumes. No order placement (read-only this phase). TDD because each adapter + use-case has a fixture-defined response/contract.
Output: trader ports + adapters + memory twin + fixtures, three use-cases, contracts, HTTP routes, MCP tools, composition-root wiring.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/04-schwab-auth-brokerage/04-RESEARCH.md
@.planning/phases/04-schwab-auth-brokerage/04-PATTERNS.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/typescript.md
@.claude/rules/tdd.md
@packages/adapters/src/http/cboe.ts
@packages/adapters/src/schwab/market/chain-adapter.ts
@packages/adapters/src/memory/chain.ts
@packages/core/src/brokerage/application/ports.ts
@packages/contracts/src/journal.ts
@apps/server/src/adapters/http/journal.routes.ts
@apps/server/src/adapters/mcp/tools.ts
@apps/server/src/adapters/mcp/server.ts
@apps/server/src/main.ts
</context>

<feature>
  <name>Schwab trader adapter (positions/orders/transactions) + use-cases + HTTP + MCP</name>
  <files>packages/core/src/brokerage/application/ports.ts (+ getPositions/getTransactions/getOrders use-cases), packages/adapters/src/schwab/trader/* , packages/adapters/src/memory/schwab-trader.ts, packages/contracts/src/brokerage.ts, apps/server/src/adapters/http/brokerage.routes.ts, apps/server/src/adapters/mcp/tools.ts + server.ts + main.ts</files>
  <behavior>
    Trader adapters (trader-adapter.test.ts, msw — mirror cboe.test.ts lifecycle):
    - Positions: GET /trader/v1/accounts/{hash}/?fields=positions with fixture → ok(BrokerPosition[]) where each has occSymbol (21-char OCC via parseSchwabSymbol), putCall, longQty, shortQty, averagePrice, marketValue, underlyingSymbol.
    - Transactions: GET /trader/v1/accounts/{hash}/transactions?startDate&endDate&types=TRADE → ok(BrokerTransaction[]) with activityId, tradeDate, netAmount, orderId, and legs[] (occSymbol, qty, price, positionEffect "OPENING"|"CLOSING"|"UNKNOWN").
    - Orders: GET /trader/v1/accounts/{hash}/orders → ok(BrokerOrder[]) (read-only metadata: orderId, status, occSymbol legs, qty, side).
    - account-hash: GET /trader/v1/accounts/accountNumbers → resolve hashValue; adapters use the HASH not the raw number (RESEARCH Pitfall 5). Unknown/empty → typed err.
    - getAccessToken err (trader AUTH_EXPIRED) → adapter returns err({kind:"auth-expired", appId:"trader"}) WITHOUT calling fetch (D-09).
    - Malformed JSON / failed Zod parse → Result.err, NEVER a thrown exception (BRK-02 success criterion 5, D-12). Add an explicit test asserting no throw on a garbage body.

    Use-cases (in-memory twin) + MCP-02 surfaces:
    - getPositions() → ok(positions) or err(auth-expired) passthrough; getTransactions(from,to) and getOrders() analogous.
    - GET /api/positions returns positionsResponse-parsed JSON; MCP get_positions returns the SAME schema's JSON. Trader AUTH_EXPIRED → route responds 200 with a typed empty/paused payload (or a documented 503) AND market-side flows are unaffected (D-09) — pick one shape, encode it in the contract, test both surfaces.
  </behavior>
  <implementation>
    Ports (brokerage/application/ports.ts — append): `BrokerPosition`, `BrokerTransaction`, `BrokerOrder` domain types (readonly, OccSymbol legs — RESEARCH §New Ports); `ForFetchingPositions = (accountHash) => Promise<Result<ReadonlyArray<BrokerPosition>, FetchError | AuthExpiredError>>`, `ForFetchingTransactions = (accountHash, from, to) => ...`, `ForFetchingOrders = (accountHash) => ...`, plus a `ForResolvingAccountHash = () => Promise<Result<string, FetchError | AuthExpiredError>>`. Re-import FetchError from journal ports. No Drizzle/Hono/process.env in core.

    Adapters (schwab/trader/*): factories `makeSchwabPositionsAdapter`/`...Transactions`/`...Orders`/`makeAccountHashResolver` each taking `{ fetch, getAccessToken, userAgent }` (PATTERNS positions/transactions section, mirror chain-adapter). Each: check getAccessToken first (auth-expired short-circuit), Bearer header, `.passthrough()` + `.optional()` Zod schemas (MEDIUM-confidence shapes), safeParse → err on failure, map vendor fields → domain types via parseSchwabSymbol (reuse from 04-04). Never log tokens. memory/schwab-trader.ts: in-memory twins for all three ports + a `seed(...)` helper (mirror memory/chain.ts) — required by the hexagon's in-memory-twin rule.

    Fixtures: schwab-positions.fixture.json + schwab-transactions.fixture.json hand-built from RESEARCH §Positions/Transactions shapes (one option position; one TRADE transaction with an OPENING + CLOSING transferItem). Reuse one for orders if shape allows, else add an orders fixture.

    Use-cases (core/brokerage): `makeGetPositionsUseCase({ resolveAccountHash, fetchPositions })`, `makeGetTransactionsUseCase`, `makeGetOrdersUseCase` — resolve hash once then call the fetcher; absorb errors into typed Result (getStatus pattern). Export from brokerage index + root index.

    Contracts (packages/contracts/src/brokerage.ts): `positionsResponse`, `transactionsResponse`, `ordersResponse` Zod schemas (ONE source for HTTP + MCP — MCP-02). Export from contracts index. HTTP routes (apps/server/src/adapters/http/brokerage.routes.ts): thin — call use-case → parse through contract → c.json (mirror journal.routes.ts); mount in main.ts under /api. MCP tools (tools.ts): registerGetPositionsTool/registerGetTransactionsTool/registerGetOrdersTool mirroring registerGetJournalTool (safeParse args, parse output through the SAME contract); register them in server.ts makeServerAndTransport and pass the new use-cases through makeMcpRouter + main.ts wiring. Build the trader adapters in main.ts with a getAccessToken closure that reads the trader app's token from broker_tokens (refresh-on-stale optional this phase — on-demand only per deferred JOB-02).

    TDD order: ports (RED assignability) → adapters per-port (RED→GREEN with fixtures, incl. the no-throw test) → use-cases (RED→GREEN) → contracts + routes + MCP tools (RED route/tool tests → GREEN) → wire main.ts/server.ts. Commit at greens: `test(04-05): ...` / `feat(04-05): ...`.
  </implementation>
</feature>

<verification>
  <automated>cd /Users/chiragpersonalmac/Desktop/morai-trading-dashboard-and-tools && bun vitest run packages/adapters/src/schwab/trader/ packages/core/src/brokerage/application/getPositions.test.ts packages/core/src/brokerage/application/getTransactions.test.ts && bun run test && bun run typecheck && bun run lint</automated>
  <human-check>Live positions/transactions pull against the authed trader app is operator-gated (needs a funded account); after wiring, hit GET /api/positions and the MCP get_positions tool and confirm matching payloads.</human-check>
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → Schwab trader endpoints | Bearer token leaves; account numbers + positions/transactions return |
| Schwab response → core | untrusted JSON crosses into the hexagon |
| core → HTTP/MCP clients | positions/transactions exposed to API + Claude Code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-18 | Tampering | Schwab positions/transactions/orders responses | mitigate | Zod safeParse at the adapter boundary; failed parse → Result.err, never throw (BRK-02 sc5, D-12) — explicit no-throw test |
| T-04-19 | Information Disclosure | trader adapter logs | mitigate | Bearer token + account numbers never logged; only {kind,message} on error |
| T-04-20 | Information Disclosure | account hash vs raw account number | mitigate | Resolve + use hashValue from /accounts/accountNumbers (Pitfall 5); raw number not used in data-call URLs |
| T-04-21 | Denial of Service | trader app AUTH_EXPIRED | mitigate | Auth-expired short-circuits before the network call; positions paused while market app keeps working (D-09); status surfaces it (04-06) |
| T-04-22 | Elevation of Privilege | accidental order placement | accept | Read-only this phase — no write/trade endpoints implemented; only GET routes exist |
</threat_model>

<success_criteria>
- Trader adapter returns positions, orders, and transactions behind their ports, Zod-parsed; failed parse → Result.err not a throw (BRK-02).
- getPositions/getTransactions ship HTTP route + MCP tool sharing one contracts schema (MCP-02).
- Trader AUTH_EXPIRED pauses trader reads; market flows unaffected (D-09).
- `bun run test` + typecheck + lint clean; account hash (not number) used; no token logged.
</success_criteria>

<output>
Create `.planning/phases/04-schwab-auth-brokerage/04-05-SUMMARY.md` with RED/GREEN notes + commit list.
</output>
