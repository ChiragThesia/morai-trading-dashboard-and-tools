---
phase: 04-schwab-auth-brokerage
plan: 04
type: tdd
wave: 4
depends_on: ["04-02"]
files_modified:
  - packages/core/src/journal/application/ports.ts
  - packages/adapters/src/schwab/market/chain-adapter.ts
  - packages/adapters/src/schwab/market/chain-adapter.test.ts
  - packages/adapters/src/schwab/market/chain-adapter.contract.test.ts
  - packages/adapters/src/schwab/market/schwab-symbol.ts
  - packages/adapters/src/schwab/market/schwab-symbol.test.ts
  - packages/adapters/src/test/fixtures/schwab-chain.fixture.json
  - packages/adapters/src/index.ts
  - packages/core/src/brokerage/application/selectChainSource.ts
  - packages/core/src/brokerage/application/selectChainSource.test.ts
  - packages/core/src/brokerage/index.ts
  - packages/core/src/index.ts
autonomous: true
requirements: [BRK-01]
must_haves:
  truths:
    - "The Schwab market adapter fetches a chain via the same ForFetchingChain port as CBOE and returns RawChain from the nested callExpDateMap/putExpDateMap response (BRK-01, success criterion 3)"
    - "Observations from the Schwab adapter are tagged source='schwab_chain'; the port type permits 'cboe' | 'schwab_chain' (BRK-01, success criterion 3)"
    - "When the market app is AUTH_EXPIRED the source selector returns the CBOE fetcher so new snapshots never go stale (D-07/D-08)"
    - "A failed Zod parse of the Schwab chain returns Result.err, never a thrown exception (D-12)"
  artifacts:
    - path: "packages/adapters/src/schwab/market/chain-adapter.ts"
      provides: "makeSchwabChainAdapter implementing ForFetchingChain"
      contains: "makeSchwabChainAdapter"
    - path: "packages/core/src/journal/application/ports.ts"
      provides: "widened ObservationRow.source + SnapshotRow.source unions"
      contains: "schwab_chain"
    - path: "packages/core/src/brokerage/application/selectChainSource.ts"
      provides: "Schwab-primary / CBOE-fallback chain fetcher selector"
      contains: "selectChainSource"
  key_links:
    - from: "chain-adapter.ts"
      to: "formatOccSymbol (@morai/shared)"
      via: "Schwab padded symbol → OCC conversion"
      pattern: "formatOccSymbol"
    - from: "selectChainSource"
      to: "readTokenFreshness + cboe/schwab fetchChain"
      via: "freshness check picks the implementation"
      pattern: "AUTH_EXPIRED"
---

<objective>
Bring Schwab market data into the hexagon behind the existing `ForFetchingChain` port (D-11): a Schwab chain adapter that authenticates with the stored access token, flattens the nested `callExpDateMap`/`putExpDateMap` response into the same `RawChain`/`RawQuote` types the rest of the system consumes, and tags observations `source='schwab_chain'`. Widen `ObservationRow.source` / `SnapshotRow.source` to the union, then add the Schwab-primary / CBOE-fallback source selector (D-07/D-08) so the journal never goes stale when the market app is AUTH_EXPIRED.

Purpose: BRK-01 — Schwab becomes the primary chain feed while CBOE stays a hot fallback. TDD because the flattener, symbol conversion, error paths, and the selector all have crisp I/O contracts (msw fixture + in-memory twins).
Output: Schwab chain adapter + symbol converter + msw fixture + contract test, widened source ports, source selector.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/04-schwab-auth-brokerage/04-RESEARCH.md
@.planning/phases/04-schwab-auth-brokerage/04-PATTERNS.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/typescript.md
@.claude/rules/tdd.md
@packages/adapters/src/http/cboe.ts
@packages/adapters/src/http/cboe.test.ts
@packages/adapters/src/http/cboe.contract.test.ts
@packages/adapters/src/__contract__/chain.contract.ts
@packages/core/src/journal/application/ports.ts
@packages/core/src/brokerage/application/ports.ts
@packages/adapters/src/postgres/repos/leg-observations.ts
</context>

<feature>
  <name>Schwab market chain adapter + source widening + CBOE-fallback selector</name>
  <files>packages/adapters/src/schwab/market/chain-adapter.ts, schwab-symbol.ts, the test + contract + fixture files, packages/core/src/journal/application/ports.ts (widen), packages/core/src/brokerage/application/selectChainSource.ts</files>
  <behavior>
    Schwab symbol conversion (schwab-symbol.test.ts, pure):
    - parseSchwabSymbol("SPX   250620P07100000") → { root:"SPX", expiry: Date(2025-06-20), type:"P", strike:7100 } then formatOccSymbol → a 21-char OCC symbol (matches the cboe osiToOcc output length).
    - A malformed symbol → Result.err (never throws).

    Schwab chain adapter (chain-adapter.test.ts, msw — mirror cboe.test.ts lifecycle, onUnhandledRequest:"error"):
    - On 200 with schwab-chain.fixture.json → ok(RawChain) where root matches, spot = top-level underlyingPrice, and every quote.occSymbol is 21 chars; quotes are flattened from BOTH callExpDateMap and putExpDateMap.
    - Field mapping: bidPrice/askPrice → bid/ask, markPrice → mark (or (bid+ask)/2 fallback), totalVolume → volume, strikePrice already in points (no ÷1000), vendor delta/gamma/theta/vega/volatility carried through.
    - getAccessToken returns err (AUTH_EXPIRED) → adapter returns err({kind:"fetch-error", message:"AUTH_EXPIRED"}) WITHOUT calling fetch.
    - HTTP 401 → err fetch-error; HTTP 400 invalid_grant → err fetch-error; malformed JSON body → err (parse) — never throws (D-12).
    - Authorization: Bearer <token> header present on the outbound request; symbol passed through as a parameter ($SPX vs SPX is caller-supplied — RESEARCH A3/Open Question 2, not hardcoded).

    Source selector (selectChainSource.test.ts, in-memory twins):
    - readTokenFreshness → market status "fresh" → selector returns the Schwab fetchChain.
    - market status "AUTH_EXPIRED" → selector returns the CBOE fetchChain (D-08 fallback).
    - readTokenFreshness err or "none yet" → selector returns CBOE (safe default — journal never stalls).
  </behavior>
  <implementation>
    Widen ports FIRST (cross-cutting — PATTERNS Key Constraint 1): in journal/application/ports.ts change `ObservationRow.source: "cboe"` → `"cboe" | "schwab_chain"` and `SnapshotRow.source: "cboe"` → `"cboe" | "schwab_chain"`. The DB enums already include `schwab_chain` (schema.ts observationSourceEnum/snapshotSourceEnum) so no migration is needed — this is a TYPE-only change. Run typecheck to find every consumer the widening touches (snapshotCalendars hardcodes source:"cboe" — leave it "cboe" there, that path is the CBOE snapshot; the widening just permits the other value). Add a focused test asserting an ObservationRow with source:"schwab_chain" is assignable.

    schwab-symbol.ts: `parseSchwabSymbol(sym)` splits Schwab's root-padded-to-6 format (root, YYMMDD, C/P, 8-digit strike×1000), then reuse `formatOccSymbol` from @morai/shared (same approach as cboe osiToOcc — PATTERNS symbol conversion). Pure, Result-returning.

    chain-adapter.ts: factory `makeSchwabChainAdapter({ fetch, getAccessToken, userAgent, symbol })` returning `{ fetchChain: ForFetchingChain }` (PATTERNS chain-adapter section, mirror cboe.ts). Define `SchwabChainResponseSchema` with `.passthrough()` and all fields `.optional()` (MEDIUM-confidence shape — PATTERNS Zod note): underlyingPrice, callExpDateMap, putExpDateMap as nested `z.record(z.record(z.array(optionContractSchema)))`. safeParse → err on failure. Flatten both maps → RawQuote[] via parseSchwabSymbol + field mapping above; spot from underlyingPrice; observedAt from a response timestamp or `new Date()`-injected clock. Check getAccessToken first; on err return fetch-error AUTH_EXPIRED before any network call. Never log tokens.

    schwab-chain.fixture.json: a small hand-crafted fixture from the RESEARCH §Option Chain response shape — at least one call expiry/strike and one put expiry/strike, a top-level underlyingPrice, padded Schwab symbols. Place in packages/adapters/src/test/fixtures/ (same dir as cboe-spx.fixture.json).

    chain-adapter.contract.test.ts: copy cboe.contract.test.ts, swap to makeSchwabChainAdapter (getAccessToken stub returns ok("test-token")), msw onUnhandledRequest:"warn", run `runChainContractTests` against the Schwab fixture — proving the Schwab adapter satisfies the SAME ForFetchingChain contract as CBOE.

    selectChainSource.ts (core/brokerage): `selectChainSource({ readTokenFreshness, schwabFetchChain, cboeFetchChain }) => Promise<ForFetchingChain>` — read freshness, return schwab when market is "fresh"/"stale", else CBOE (D-07/D-08). Pure-ish application logic (no I/O beyond the injected ports); export from brokerage index + root index. Export makeSchwabChainAdapter from adapters index.

    TDD order: widen ports (RED assignability test) → schwab-symbol (RED→GREEN) → chain-adapter (RED→GREEN) → contract test → selectChainSource (RED→GREEN). Commit at greens: `test(04-04): ...` / `feat(04-04): ...`.
  </implementation>
</feature>

<verification>
  <automated>cd /Users/chiragpersonalmac/Desktop/morai-trading-dashboard-and-tools && bun vitest run packages/adapters/src/schwab/market/ packages/core/src/brokerage/application/selectChainSource.test.ts && bun run typecheck && bun run lint</automated>
  <human-check>Live BRK-01 (real SPX chain via the authed market app during RTH, leg_observations tagged source='schwab_chain') is a manual-only verification per 04-VALIDATION.md — run a live chain pull after 04-06 wires the worker, during market hours, and confirm `SELECT count(*) FROM leg_observations WHERE source='schwab_chain'` is > 0.</human-check>
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → Schwab market data endpoint | Bearer access token leaves the process; chain JSON returns |
| Schwab response → core | untrusted nested JSON crosses into the hexagon |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | Tampering | Schwab chain response | mitigate | SchwabChainResponseSchema.safeParse before any data reaches core; malformed → Result.err, never throw (D-12) |
| T-04-15 | Information Disclosure | adapter logs | mitigate | Bearer token never logged; only {kind,message} returned on error (cboe.ts pattern) |
| T-04-16 | Denial of Service | market app AUTH_EXPIRED / Schwab outage | mitigate | selectChainSource falls back to CBOE so the journal keeps writing (D-08); AUTH_EXPIRED short-circuits before the network call |
| T-04-17 | Tampering | Schwab 120 req/min rate limit (429) | accept | 30-min snapshot cadence with few calendars stays well under the limit (RESEARCH Pitfall 9); revisit if bulk fetch is added |
</threat_model>

<success_criteria>
- Schwab chain adapter returns RawChain from the fixture and satisfies the ForFetchingChain contract test (BRK-01).
- ObservationRow.source / SnapshotRow.source accept 'schwab_chain'; Schwab observations tagged accordingly (BRK-01).
- selectChainSource returns Schwab when fresh, CBOE when market is AUTH_EXPIRED / none-yet (D-07/D-08).
- Malformed Schwab JSON → Result.err, never throws; no token logged; typecheck + lint clean.
</success_criteria>

<output>
Create `.planning/phases/04-schwab-auth-brokerage/04-04-SUMMARY.md` with RED/GREEN notes + commit list.
</output>
