---
phase: 04-schwab-auth-brokerage
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - apps/auth/package.json
  - apps/auth/tsconfig.json
  - apps/auth/vitest.config.ts
  - apps/auth/src/config.ts
  - apps/auth/src/main.ts
  - apps/auth/src/setup.ts
  - apps/auth/src/setup.test.ts
  - apps/auth/src/refresh.ts
  - apps/auth/src/status.ts
  - apps/auth/src/doctor.ts
  - apps/auth/src/doctor.test.ts
autonomous: false
requirements: [AUTH-03]
must_haves:
  truths:
    - "`auth setup` opens the browser, captures the code on the loopback HTTPS listener, exchanges it, and writes an encrypted broker_tokens row (AUTH-03, success criterion 1)"
    - "`auth status` reads broker_tokens and reports per-app freshness WITHOUT calling Schwab (AUTH-03, success criterion 1)"
    - "`auth doctor` reports three distinct conditions: missing env var, callback-URL mismatch, live refresh-grant failure (AUTH-03, success criterion 2)"
    - "`auth refresh` performs an on-demand refresh via the refreshToken use-case (AUTH-03)"
  artifacts:
    - path: "apps/auth/src/main.ts"
      provides: "CLI dispatch over process.argv for setup|refresh|status|doctor"
      contains: "case \"doctor\""
    - path: "apps/auth/src/doctor.ts"
      provides: "pure doctor diagnostics (env completeness, callback exact-match, live refresh)"
      contains: "callback"
    - path: "apps/auth/src/setup.ts"
      provides: "validateAndExchange (pure CSRF/state + code-exchange ordering) + loopback HTTPS OAuth dance writing one encrypted token row"
      contains: "validateAndExchange"
    - path: "apps/auth/src/setup.test.ts"
      provides: "TDD unit test for validateAndExchange (state-mismatch / happy / exchange-failure)"
      contains: "validateAndExchange"
  key_links:
    - from: "apps/auth/src/setup.ts"
      to: "broker-tokens repo writeTokens (pgcrypto)"
      via: "exchangeCode → encrypt → persist"
      pattern: "writeTokens"
    - from: "apps/auth/src/doctor.ts"
      to: "SCHWAB_*_CALLBACK_URL env"
      via: "exact-string comparison against registered URL"
      pattern: "callbackUrl"
---

<objective>
Ship the `auth` CLI — a new `apps/auth` app exposing `setup | refresh | status | doctor` (D-06). `setup` runs the loopback HTTPS OAuth dance (D-04) and writes the first encrypted `broker_tokens` row end-to-end; `status` reads freshness without touching Schwab; `doctor` diagnoses the three failure conditions; `refresh` drives the on-demand refresh use-case. This makes the AUTH-01/02 plumbing operable by a human.

Purpose: this is the user-facing capability of the auth slice — after this plan a real operator can authenticate both Schwab apps and inspect token health from one CLI. The doctor's diagnostic logic is pure and unit-tested; the live browser dance is verified at a human checkpoint (cannot be automated — registered callback + real Schwab login).
Output: apps/auth scaffold + four subcommands + doctor unit tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-schwab-auth-brokerage/04-RESEARCH.md
@.planning/phases/04-schwab-auth-brokerage/04-PATTERNS.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/typescript.md
@apps/server/src/config.ts
@apps/server/src/main.ts
@apps/worker/src/config.ts
@packages/adapters/src/postgres/repos/broker-tokens.ts
@packages/adapters/src/schwab/auth/oauth-client.ts
@packages/core/src/brokerage/application/refreshToken.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold apps/auth + config + CLI dispatch + doctor (TDD doctor)</name>
  <read_first>apps/server/src/config.ts (bootConfig loud-fail pattern), apps/server/src/main.ts (composition-root wiring of db + repos), apps/worker/src/config.ts, .planning/phases/04-schwab-auth-brokerage/04-PATTERNS.md (apps/auth section), .planning/phases/04-schwab-auth-brokerage/04-RESEARCH.md (CLI Design, Callback URL Exact Match, Pitfall 1)</read_first>
  <files>apps/auth/package.json, apps/auth/tsconfig.json, apps/auth/vitest.config.ts, apps/auth/src/config.ts, apps/auth/src/main.ts, apps/auth/src/doctor.ts, apps/auth/src/doctor.test.ts, apps/auth/src/status.ts</files>
  <action>Scaffold `apps/auth` as a Bun workspace app mirroring apps/server: package.json (name `@morai/auth`, depends on `@morai/adapters` + `@morai/core`), tsconfig.json + vitest.config.ts copied from apps/server with paths adjusted. `src/config.ts`: an `authConfigSchema` (or reuse the shared shape) requiring `DATABASE_URL`, `TOKEN_ENCRYPTION_KEY`, and the six `SCHWAB_*` fields, with `parseAuthConfig(env)` + `bootAuthConfig()` exactly mirroring the server's parseConfig/bootConfig loud-fail (prints field names only — never values). `src/main.ts`: read `process.argv` ONCE here (composition root), `bootAuthConfig()`, build `db = makeDb(...)` + `makePostgresBrokerTokensRepo(db, config.TOKEN_ENCRYPTION_KEY)`, then dispatch on subcommand to `runSetup/runRefresh/runStatus/runDoctor` (PATTERNS CLI dispatch). Default/unknown subcommand → `console.error` + `process.exit(1)`.

  DOCTOR is the unit-tested core (write doctor.test.ts FIRST, RED, then implement — doctor.ts holds PURE diagnostic functions taking explicit inputs, no process.env, no real network in the unit path): expose `checkEnvCompleteness(env)` → lists any missing/empty SCHWAB_* / TOKEN_ENCRYPTION_KEY keys; `checkCallbackExactMatch(envCallback, registeredCallback)` → ok only on character-for-character equality (D-06; RESEARCH Pitfall 1 — trailing slash differs); `checkLiveRefresh(refreshFn)` → calls an injected refresh function and classifies ok vs auth-expired vs network. `runDoctor` composes these against config + the injected oauth client's refreshTokens and prints a three-line report. STATUS (`src/status.ts`): `runStatus` calls `brokerTokensRepo.readTokenFreshness()` and prints per-app status; it MUST NOT construct or call the oauth client (success criterion: reads DB only, no Schwab call). No tokens or keys in any console output anywhere.</action>
  <verify>
    <automated>cd /Users/chiragpersonalmac/Desktop/morai-trading-dashboard-and-tools && bun vitest run apps/auth/src/doctor.test.ts && bun run typecheck && bun run lint apps/auth</automated>
  </verify>
  <acceptance_criteria>apps/auth installs in the workspace; doctor.test.ts green covering env-missing, callback-mismatch, and live-refresh-failure classification; main dispatches all four subcommands; status reads DB only; typecheck + lint clean.</acceptance_criteria>
  <done>CLI skeleton + config + doctor (tested) + status land; no secret ever logged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: auth setup (pure validateAndExchange + loopback HTTPS dance) + auth refresh</name>
  <read_first>.planning/phases/04-schwab-auth-brokerage/04-RESEARCH.md (Loopback OAuth Capture Pattern, HTTPS Requirement, Pitfall 3/4, Open Question 1), .planning/phases/04-schwab-auth-brokerage/04-PATTERNS.md (loopback listener note), packages/adapters/src/schwab/auth/oauth-client.ts, packages/core/src/brokerage/application/refreshToken.ts, apps/auth/src/doctor.test.ts (test style for the new setup.test.ts)</read_first>
  <files>apps/auth/src/setup.ts, apps/auth/src/setup.test.ts, apps/auth/src/refresh.ts</files>
  <behavior>
    validateAndExchange(result, expectedState, client) — pure, no I/O, no browser, no DB (security-critical decision logic):
    - result.state !== expectedState → returns Result.err({ kind: "state-mismatch" }) (CSRF defense) AND client.exchangeCode is NEVER called (assert the spy/fake records zero calls).
    - state matches, client.exchangeCode(result.code) returns ok(tokens) → returns ok(tokens); exchangeCode called exactly once with result.code.
    - state matches, client.exchangeCode returns err(oauth-error) → returns err (exchange-failure) and does NOT fabricate tokens.
    - Ordering invariant: the state check happens BEFORE the exchange (the state-mismatch case proves no exchange attempt).
  </behavior>
  <action>Extract the security-critical decision logic into a PURE function in setup.ts: `validateAndExchange(result, expectedState, client): Promise<Result<SchwabTokens, AuthError>>` taking the parsed loopback-callback result ({ code, state }), the expected state string, and the oauth client port (only its `exchangeCode` is used) — NO browser launch, NO loopback listen, NO DB. It (a) rejects on `result.state !== expectedState` returning a typed `Result.err` (state-mismatch / CSRF) WITHOUT calling exchangeCode, (b) on match calls `client.exchangeCode(result.code)` (the exchange happens BEFORE any persistence), (c) returns `Result<SchwabTokens, AuthError>`. TDD this first (RED→GREEN): write apps/auth/src/setup.test.ts covering the three behaviors above using an IN-MEMORY fake oauth client (a small object whose exchangeCode is a spy/counter — no live Schwab, no msw needed), run RED, then implement validateAndExchange to GREEN.

  The imperative `runSetup(config, db, repo, appId)` shell stays thin and wraps the pure core: build `makeSchwabOAuthClient({ appKey, appSecret, callbackUrl, fetch })` for the chosen app ("trader"|"market" from argv); generate `state = crypto.randomUUID()` (CSRF — RESEARCH Don't-Hand-Roll); derive the listener port from `new URL(config.SCHWAB_<APP>_CALLBACK_URL).port` (NO hardcoded port — Open Question 1); use the installed `oauth-callback` `getAuthCode({ authorizationUrl: client.buildAuthUrl(state), launch: (url) => open(url), port, hostname: "127.0.0.1", timeout: 120_000 })` over an HTTPS loopback (Schwab requires HTTPS on 127.0.0.1 — fall back to `Bun.serve({ tls:{ cert, key } })` with an in-memory self-signed cert if oauth-callback cannot self-sign). Pass the captured result + state + client into `validateAndExchange`; on ok build a SchwabTokenRow (issuedAt=now, refreshIssuedAt=now, expiresAt=now+expiresIn) and `repo.writeTokens(appId, row)` (pgcrypto). The 30s code TTL (Pitfall 4) is honored because validateAndExchange exchanges immediately and persistence only happens after a successful exchange. Print success with appId + timestamps only — never the tokens. `runRefresh(config, repo, appId)` wires `makeRefreshTokenUseCase({ readTokens: repo.readTokens, writeTokens: repo.writeTokens, refreshTokens: client.refreshTokens })` and runs it, printing ok/auth-expired. The browser-launch + loopback-listen + persist shell stays manual (Task 3); only the pure decision logic is unit-tested here.</action>
  <verify>
    <automated>cd /Users/chiragpersonalmac/Desktop/morai-trading-dashboard-and-tools && bun vitest run apps/auth/src/setup.test.ts && bun run typecheck && bun run lint apps/auth</automated>
  </verify>
  <acceptance_criteria>setup.test.ts green covering state-mismatch (no exchange), happy path (exchange called once → tokens), and exchange-failure → err; validateAndExchange is pure (no browser/loopback/DB) and exchanges before any persistence; runSetup shell + refresh compile and lint clean; no token/key logged.</acceptance_criteria>
  <done>validateAndExchange extracted + TDD-tested; setup/refresh shells implemented; setup.test.ts + typecheck + lint green (live dance verified at the next checkpoint).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live verify — auth setup + status + doctor against real Schwab</name>
  <files>apps/auth/src/main.ts (run targets: setup/status/doctor/refresh), broker_tokens (live)</files>
  <action>Human-verify checkpoint: drive the live `auth` CLI against the real Schwab apps (setup both apps, then status/doctor/refresh) following the how-to-verify steps, and confirm two encrypted token rows land in broker_tokens with no secret in any CLI output.</action>
  <verify><human-check>Follow the how-to-verify steps: setup writes two encrypted rows via the live OAuth dance; status reads DB-only; doctor shows all three diagnostics; refresh rotates a token; no secret in output.</human-check></verify>
  <done>Both apps authenticate; status/doctor/refresh behave; encrypted rows present; no token/key logged.</done>
  <what-built>The `auth` CLI: `setup` (loopback HTTPS OAuth dance → encrypted broker_tokens row), `status` (per-app freshness read from DB, no Schwab call), `doctor` (env / callback-match / live-refresh diagnostics), `refresh` (on-demand refresh).</what-built>
  <how-to-verify>
    Requires the live Schwab apps (D-05) + TOKEN_ENCRYPTION_KEY + broker_tokens table from 04-01. Manual because it needs a real Schwab login + the registered callback.
    1. `bun run apps/auth/src/main.ts doctor` — confirm it reports env completeness and callback-URL match cleanly (and try temporarily unsetting one SCHWAB_* var to see it flag the missing one + a mismatched callback to see the third diagnostic).
    2. `bun run apps/auth/src/main.ts setup trader` — browser opens to Schwab; log in; confirm the loopback catches the redirect and the CLI prints success. Repeat `setup market`.
    3. Confirm encrypted rows exist: `psql "$DATABASE_URL" -c "SELECT app_id, length(access_token) FROM broker_tokens"` shows two rows with non-zero bytea length; `SELECT access_token FROM broker_tokens LIMIT 1` is NOT human-readable plaintext.
    4. `bun run apps/auth/src/main.ts status` — prints `trader: fresh`, `market: fresh` (or appropriate) WITHOUT any Schwab network call.
    5. `bun run apps/auth/src/main.ts refresh trader` — succeeds and rotates the row (updated_at advances).
  </how-to-verify>
  <acceptance_criteria>setup writes two encrypted token rows via the live OAuth dance; status reports per-app freshness from the DB without hitting Schwab; doctor surfaces all three diagnostic conditions; refresh rotates a token; no token/key appears in any CLI output.</acceptance_criteria>
  <resume-signal>Reply "approved" after both apps authenticate and status/doctor/refresh behave, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → loopback listener | Schwab redirects the authorization code to 127.0.0.1 over HTTPS |
| CLI → Schwab token endpoint | code/refresh exchanged for tokens |
| CLI → Postgres | tokens encrypted + persisted |
| operator TTY → CLI | argv selects app + subcommand |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Spoofing / CSRF | OAuth state param | mitigate | state = crypto.randomUUID() sent in authorizationUrl and verified on callback (result.state === state) before exchange |
| T-04-10 | Tampering | loopback callback interception on localhost | mitigate | Listener bound to 127.0.0.1 only on the exact registered port; HTTPS (self-signed) per Schwab requirement; server shuts down immediately after one capture |
| T-04-11 | Information Disclosure | CLI stdout (setup/status/doctor) | mitigate | Only appId + timestamps + status printed; tokens, refresh tokens, app secrets, encryption key NEVER echoed |
| T-04-12 | Repudiation / config error | callback-URL mismatch | mitigate | doctor performs character-for-character compare of env callback vs registered value (D-06, Pitfall 1) and fails loudly |
| T-04-13 | Elevation / replay | 30-second auth code TTL | mitigate | exchangeCode called immediately after capture, before any other I/O (Pitfall 4) |
</threat_model>

<verification>
- `bun vitest run apps/auth/src/doctor.test.ts apps/auth/src/setup.test.ts` green (doctor diagnostics + pure validateAndExchange CSRF/ordering logic).
- `bun run typecheck && bun run lint` clean.
- Human checkpoint: live setup/status/doctor/refresh against real Schwab (AUTH-03 success criteria 1 + 2).
</verification>

<success_criteria>
- `auth setup` writes encrypted tokens for both apps via the loopback dance (AUTH-03).
- `auth status` reports per-app freshness from the DB, no Schwab call (AUTH-03).
- `auth doctor` reports env-missing, callback-mismatch, and live-refresh-failure as three distinct conditions (AUTH-03).
- No secret in any CLI output; typecheck + lint clean.
</success_criteria>

<output>
Create `.planning/phases/04-schwab-auth-brokerage/04-03-SUMMARY.md` when done.
</output>
