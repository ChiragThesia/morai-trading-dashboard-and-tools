---
phase: 04-schwab-auth-brokerage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - bun.lock
  - packages/core/src/brokerage/application/ports.ts
  - packages/core/src/brokerage/domain/token-freshness.ts
  - packages/core/src/brokerage/index.ts
  - packages/core/src/index.ts
  - packages/adapters/src/postgres/schema.ts
  - packages/adapters/src/postgres/migrations/0003_broker_tokens.sql
  - packages/adapters/src/postgres/migrations/meta/_journal.json
  - packages/adapters/src/postgres/migrations/meta/0003_snapshot.json
  - packages/adapters/src/memory/broker-tokens.ts
  - packages/adapters/src/index.ts
  - apps/server/src/config.ts
  - apps/worker/src/config.ts
  - docs/architecture/data-model.md
autonomous: false
requirements: [AUTH-02]
user_setup:
  - service: schwab
    why: "Schwab two-app OAuth credentials + token encryption key (already-registered apps per D-05)"
    env_vars:
      - name: TOKEN_ENCRYPTION_KEY
        source: "Generate a 32+ char random secret; add to Railway secrets + local .env"
      - name: SCHWAB_TRADER_APP_KEY
        source: "Schwab Developer Portal → Trader app → App Key"
      - name: SCHWAB_TRADER_APP_SECRET
        source: "Schwab Developer Portal → Trader app → Secret"
      - name: SCHWAB_TRADER_CALLBACK_URL
        source: "Schwab Developer Portal → Trader app → registered callback URL (exact string)"
      - name: SCHWAB_MARKET_APP_KEY
        source: "Schwab Developer Portal → Market Data app → App Key"
      - name: SCHWAB_MARKET_APP_SECRET
        source: "Schwab Developer Portal → Market Data app → Secret"
      - name: SCHWAB_MARKET_CALLBACK_URL
        source: "Schwab Developer Portal → Market Data app → registered callback URL (exact string)"

must_haves:
  truths:
    - "broker_tokens table exists in the live DB with bytea access_token/refresh_token columns and pgcrypto enabled (success criterion 1 precondition)"
    - "The brokerage bounded context exposes token ports and a pure token-freshness domain that classifies fresh/stale/AUTH_EXPIRED/none_yet per app"
    - "An in-memory broker-tokens twin exists so downstream ports can be tested without Postgres"
  artifacts:
    - path: "packages/core/src/brokerage/application/ports.ts"
      provides: "SchwabTokenRow, AppId, AppTokenStatus, TokenFreshnessMap, AuthExpiredError, ForReadingTokens, ForWritingTokens, ForReadingTokenFreshness"
      contains: "ForReadingTokenFreshness"
    - path: "packages/core/src/brokerage/domain/token-freshness.ts"
      provides: "isTokenExpired, isTokenStale, toAppTokenStatus pure functions"
      contains: "export function toAppTokenStatus"
    - path: "packages/adapters/src/postgres/schema.ts"
      provides: "brokerTokens Drizzle table with bytea encrypted columns"
      contains: "broker_tokens"
    - path: "packages/adapters/src/postgres/migrations/0003_broker_tokens.sql"
      provides: "CREATE EXTENSION pgcrypto + CREATE TABLE broker_tokens migration"
      contains: "pgcrypto"
    - path: "packages/adapters/src/memory/broker-tokens.ts"
      provides: "makeMemoryBrokerTokensRepo in-memory twin with seed()"
      contains: "makeMemoryBrokerTokensRepo"
  key_links:
    - from: "apps/worker/src/migrate.ts (bun run migrate)"
      to: "0003_broker_tokens.sql"
      via: "drizzle migrator picks up meta/_journal.json idx 3"
      pattern: "0003_broker_tokens"
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/brokerage/index.ts"
      via: "barrel re-export of brokerage ports + domain"
      pattern: "brokerage"
---

<objective>
Lay the brokerage foundation: install the two verified OAuth packages, define the `brokerage` bounded context (token ports + pure token-freshness domain), add the `broker_tokens` Drizzle table with pgcrypto-encrypted `bytea` columns, write and PUSH the migration to the live DB, ship the in-memory token twin, and extend both app configs with the Schwab + encryption env vars.

Purpose: every later slice (OAuth client, repo, CLI, adapters, status degradation) consumes the ports and the live table created here. Defining contracts first eliminates the executor scavenger-hunt and guarantees the table exists before any repo queries it (RESEARCH Pitfall 10).
Output: brokerage ports + domain, broker_tokens schema + applied migration, in-memory twin, config env vars, data-model doc update.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-schwab-auth-brokerage/04-CONTEXT.md
@.planning/phases/04-schwab-auth-brokerage/04-RESEARCH.md
@.planning/phases/04-schwab-auth-brokerage/04-PATTERNS.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/typescript.md
@.claude/rules/workflow.md
@packages/core/src/journal/application/ports.ts
@packages/adapters/src/postgres/schema.ts
@packages/adapters/src/memory/chain.ts
@packages/adapters/src/postgres/migrations/meta/_journal.json
@apps/server/src/config.ts
@apps/worker/src/config.ts
@docs/architecture/data-model.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install verified OAuth packages + add Schwab/encryption config env vars</name>
  <read_first>apps/server/src/config.ts, apps/worker/src/config.ts, package.json, .planning/phases/04-schwab-auth-brokerage/04-RESEARCH.md (Package Legitimacy Audit + Project Constraints)</read_first>
  <files>package.json, apps/server/src/config.ts, apps/worker/src/config.ts</files>
  <action>Both packages passed the legitimacy gate at planning time with verdict OK (oauth-callback: kriasoft, published 2026-01-25, 4.2k weekly dl, no postinstall; open: sindresorhus, 115M weekly dl, no postinstall). Run `bun add oauth-callback open` at the repo root. Then extend BOTH `configSchema` (apps/server/src/config.ts) and `workerConfigSchema` (apps/worker/src/config.ts) with the seven Phase-4 env fields, mirroring the existing `z.string()` style: `TOKEN_ENCRYPTION_KEY: z.string().min(32, ...)`, `SCHWAB_TRADER_APP_KEY: z.string().min(1)`, `SCHWAB_TRADER_APP_SECRET: z.string().min(1)`, `SCHWAB_TRADER_CALLBACK_URL: z.string().url()`, and the three `SCHWAB_MARKET_*` equivalents (per D-01/D-02/D-03/D-05). Keep them REQUIRED (no defaults) so boot fails loud if missing (DATA-04 pattern). Do NOT read process.env anywhere except the existing bootConfig/bootWorkerConfig wrappers. Never log any config value — the existing bootConfig only prints field names; preserve that.</action>
  <verify>
    <automated>cd /Users/chiragpersonalmac/Desktop/morai-trading-dashboard-and-tools && node -e "const p=require('./package.json');if(!p.dependencies['oauth-callback']||!p.dependencies['open'])process.exit(1)" && grep -q "TOKEN_ENCRYPTION_KEY" apps/server/src/config.ts && grep -q "SCHWAB_MARKET_CALLBACK_URL" apps/worker/src/config.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>oauth-callback and open are in package.json dependencies; both config schemas parse the 7 new fields; `bun run typecheck` is clean.</acceptance_criteria>
  <done>Packages installed; both configs hold all 7 Schwab/encryption fields as required; typecheck passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define brokerage ports + pure token-freshness domain + core barrel</name>
  <read_first>packages/core/src/journal/application/ports.ts (StorageError, FetchError, OccSymbol usage, ForVerbingNoun convention), packages/core/src/journal/domain/rth-window.ts (pure-domain style), packages/core/src/index.ts (barrel pattern), .planning/phases/04-schwab-auth-brokerage/04-PATTERNS.md (ports.ts + token-freshness.ts sections)</read_first>
  <files>packages/core/src/brokerage/application/ports.ts, packages/core/src/brokerage/domain/token-freshness.ts, packages/core/src/brokerage/domain/token-freshness.test.ts, packages/core/src/brokerage/index.ts, packages/core/src/index.ts</files>
  <behavior>
    - toAppTokenStatus(null, now) → { status: "none_yet", expiresAt: null, refreshIssuedAt: null }
    - toAppTokenStatus(row with refreshIssuedAt 8 days ago, now) → status "AUTH_EXPIRED"
    - toAppTokenStatus(row with refreshIssuedAt 1 day ago and expiresAt 5 min in the past, now) → status "stale"
    - toAppTokenStatus(row with refreshIssuedAt 1 day ago and expiresAt 20 min in the future, now) → status "fresh"
    - isTokenStale uses a 60-second clock-skew buffer: expiresAt exactly 30s in the future → still "stale" (true)
    - isTokenExpired: exactly 7 days minus 1 minute old → false; exactly 7 days plus 1 minute → true
  </behavior>
  <action>Create the `brokerage` bounded context in core. In `application/ports.ts` define the domain types and ports per D-01/D-09 (RESEARCH §AUTH_EXPIRED Wiring + PATTERNS ports.ts section): `AppId = "trader" | "market"`; `SchwabTokenRow = { readonly appId: AppId; readonly accessToken: string; readonly refreshToken: string; readonly issuedAt: Date; readonly refreshIssuedAt: Date; readonly expiresAt: Date }`; `AppTokenStatus = { readonly status: "fresh" | "stale" | "AUTH_EXPIRED" | "none_yet"; readonly expiresAt: Date | null; readonly refreshIssuedAt: Date | null }`; `TokenFreshnessMap = { readonly trader: AppTokenStatus; readonly market: AppTokenStatus }`; `AuthExpiredError = { readonly kind: "auth-expired"; readonly appId: AppId }`. Re-import `StorageError` from the journal ports (cross-context import of an application-level error is allowed; do NOT import another context's `domain/`). Define ports `ForReadingTokens`, `ForWritingTokens`, `ForReadingTokenFreshness` exactly as in PATTERNS (return `Result<…, StorageError>`, freshness returns `Result<TokenFreshnessMap | "none yet", StorageError>`). In `domain/token-freshness.ts` write ONLY pure functions (imports limited to `@morai/shared` + the port types): `isTokenExpired(refreshIssuedAt, now)` (7-day hard cutoff), `isTokenStale(expiresAt, now)` (60s buffer), `toAppTokenStatus(row, now)`. No `Date.now()`, no I/O, no `any`/`as`/`!`. Create `brokerage/index.ts` re-exporting ports + domain, and extend `packages/core/src/index.ts` with `export type { … } from "./brokerage/index.ts"` and `export { isTokenExpired, isTokenStale, toAppTokenStatus } from "./brokerage/index.ts"`. Follow TDD: write token-freshness.test.ts first (the behaviors above), run it RED, implement to GREEN.</action>
  <verify>
    <automated>cd /Users/chiragpersonalmac/Desktop/morai-trading-dashboard-and-tools && bun vitest run packages/core/src/brokerage/domain/token-freshness.test.ts && bun run typecheck && bun run lint packages/core/src/brokerage</automated>
  </verify>
  <acceptance_criteria>token-freshness.test.ts passes covering all six behaviors; core barrel exports brokerage ports + domain; typecheck + lint clean; core imports only @morai/shared.</acceptance_criteria>
  <done>Brokerage ports + pure freshness domain exist and are exported; tests green; hexagon boundary intact.</done>
</task>

<task type="auto">
  <name>Task 3: broker_tokens Drizzle table + in-memory twin + data-model doc</name>
  <read_first>packages/adapters/src/postgres/schema.ts, packages/adapters/src/memory/chain.ts (twin shape), packages/adapters/src/index.ts (barrel), docs/architecture/data-model.md, .planning/phases/04-schwab-auth-brokerage/04-PATTERNS.md (schema.ts + memory broker-tokens sections)</read_first>
  <files>packages/adapters/src/postgres/schema.ts, packages/adapters/src/memory/broker-tokens.ts, packages/adapters/src/index.ts, docs/architecture/data-model.md</files>
  <action>Docs-before-code (workflow rule): first add a `broker_tokens` section to docs/architecture/data-model.md describing the table, the per-app `app_id` PK discriminator, the `bytea` encrypted columns, and the D-03 constraint that the encryption key never lives in the DB. Then add the `brokerTokens` table to schema.ts (per D-01/D-02 + PATTERNS schema section): `appId text PK`, `accessToken`/`refreshToken` as `bytea NOT NULL` (use Drizzle `customType<{ data: string; driverData: Buffer }>` from `drizzle-orm/pg-core`, dataType `"bytea"` — RESEARCH open question A6; the round-trip is verified in plan 04-02's testcontainers test), `issuedAt`/`refreshIssuedAt`/`expiresAt`/`updatedAt` as `timestamp(..., { withTimezone: true }).notNull()`. Call `.enableRLS()` like the sibling tables. Create the in-memory twin `makeMemoryBrokerTokensRepo` mirroring memory/chain.ts: a `Map<AppId, SchwabTokenRow>` backing `readTokens`, `writeTokens`, `readTokenFreshness` (compose `toAppTokenStatus` for both apps using an injected/`new Date()` now — accept a `now` param defaulting to Date for testability), plus a `seed(appId, row)` helper. Export `makeMemoryBrokerTokensRepo` + its type from packages/adapters/src/index.ts. Do NOT write the Postgres repo here — that is plan 04-02 (it needs the pgcrypto round-trip test).</action>
  <verify>
    <automated>cd /Users/chiragpersonalmac/Desktop/morai-trading-dashboard-and-tools && grep -q 'broker_tokens' packages/adapters/src/postgres/schema.ts && grep -q 'makeMemoryBrokerTokensRepo' packages/adapters/src/index.ts && grep -q 'broker_tokens' docs/architecture/data-model.md && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>brokerTokens table declared with bytea columns; in-memory twin exported; data-model.md documents the table; typecheck clean.</acceptance_criteria>
  <done>Schema + twin + doc landed; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 4: Generate broker_tokens migration with pgcrypto prologue</name>
  <read_first>packages/adapters/src/postgres/migrations/0002_watery_molecule_man.sql, packages/adapters/src/postgres/migrations/meta/_journal.json, drizzle.config.ts</read_first>
  <files>packages/adapters/src/postgres/migrations/0003_broker_tokens.sql, packages/adapters/src/postgres/migrations/meta/_journal.json, packages/adapters/src/postgres/migrations/meta/0003_snapshot.json</files>
  <action>Run `bunx drizzle-kit generate` (drizzle.config.ts already points schema + out at the right paths) to emit `0003_broker_tokens.sql` plus the meta journal/snapshot entries. Then HAND-PREPEND `CREATE EXTENSION IF NOT EXISTS pgcrypto;` as the FIRST statement of the generated SQL (drizzle-kit will not add it — RESEARCH Pitfall 10 + PATTERNS migration section). Confirm the generated table uses `bytea` for access_token/refresh_token and `timestamptz` for the time columns; if drizzle emitted a different type for the bytea customType, fix the SQL by hand to `bytea NOT NULL`. Verify meta/_journal.json gained an idx-3 entry tagged `0003_broker_tokens` so the boot migrator (apps/worker/src/migrate.ts) will apply it. Do NOT run the migration in this task — the live push is the next [BLOCKING] task.</action>
  <verify>
    <automated>cd /Users/chiragpersonalmac/Desktop/morai-trading-dashboard-and-tools && grep -qi 'CREATE EXTENSION IF NOT EXISTS pgcrypto' packages/adapters/src/postgres/migrations/0003_broker_tokens.sql && grep -q 'bytea' packages/adapters/src/postgres/migrations/0003_broker_tokens.sql && grep -q '0003_broker_tokens' packages/adapters/src/postgres/migrations/meta/_journal.json</automated>
  </verify>
  <acceptance_criteria>0003_broker_tokens.sql exists, first statement is the pgcrypto CREATE EXTENSION, columns are bytea/timestamptz, journal references the new tag.</acceptance_criteria>
  <done>Migration file + meta entries generated with the pgcrypto prologue; not yet applied.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: [BLOCKING] Push migration to the live DB + provision TOKEN_ENCRYPTION_KEY</name>
  <files>packages/adapters/src/postgres/migrations/0003_broker_tokens.sql (applied via apps/worker/src/migrate.ts), .env / Railway secrets</files>
  <action>[BLOCKING] Human-action checkpoint: provision the secrets and apply the migration to the live DB. Set TOKEN_ENCRYPTION_KEY + the six SCHWAB_* vars in .env and Railway, run `bun run migrate` to apply 0003_broker_tokens, verify pgcrypto + broker_tokens exist, and confirm a second migrate is a no-op (steps in how-to-verify). Mandatory because typecheck/build pass WITHOUT the live table (types come from Drizzle), so any later live verification would otherwise false-pass.</action>
  <what-built>The 0003_broker_tokens migration (CREATE EXTENSION pgcrypto + broker_tokens table) and the env-var config requiring TOKEN_ENCRYPTION_KEY + the six SCHWAB_* vars.</what-built>
  <verify><human-check>Run the how-to-verify steps: pgcrypto present, broker_tokens with bytea columns + app_id PK, second `bun run migrate` is a clean no-op.</human-check></verify>
  <done>broker_tokens + pgcrypto live; second migrate is a no-op; all 7 env vars set locally and in Railway.</done>
  <how-to-verify>
    This task is [BLOCKING] per the schema-push requirement: build/typecheck pass WITHOUT the live table because types come from Drizzle, not the DB — so verification that depends on the live table would falsely pass. Run the migration against the live DB before any plan that queries broker_tokens (04-02 onward).

    1. Set local `.env` (and Railway secrets) with `TOKEN_ENCRYPTION_KEY` (32+ random chars) and the six `SCHWAB_TRADER_*` / `SCHWAB_MARKET_*` values from the already-registered apps (D-05). Without these, `bun run migrate` (and later boots) fail loud at config parse.
    2. Run `bun run migrate` (executes apps/worker/src/migrate.ts → runMigrations over the direct DATABASE_URL).
    3. Confirm the table + extension exist:
       `psql "$DATABASE_URL" -c "SELECT 1 FROM pg_extension WHERE extname='pgcrypto'"` returns one row, and
       `psql "$DATABASE_URL" -c "\d broker_tokens"` shows access_token/refresh_token as `bytea` and app_id as the PK.
    4. Re-run `bun run migrate` once more — it must be a no-op (idempotent, no errors).
  </how-to-verify>
  <acceptance_criteria>pgcrypto enabled in the live DB; broker_tokens exists with bytea columns + app_id PK; a second `bun run migrate` is a clean no-op; TOKEN_ENCRYPTION_KEY + Schwab vars set locally and in Railway.</acceptance_criteria>
  <resume-signal>Reply "migrated" once broker_tokens + pgcrypto are live and the env vars are set, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env/secret → app config | TOKEN_ENCRYPTION_KEY + Schwab secrets cross from Railway/.env into typed config |
| app → Postgres | encrypted token columns persisted; key passed at query time (later plans) |
| npm registry → build | oauth-callback + open pulled into the dependency tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | config schema / bootConfig | mitigate | New secrets are required `z.string()` fields; bootConfig prints only field names on failure (existing pattern preserved) — never values (D-03) |
| T-04-02 | Tampering | broker_tokens at rest | mitigate | access_token/refresh_token typed `bytea`, written via pgp_sym_encrypt in 04-02; key never stored in DB (D-03) — table design enforces encrypted-only columns |
| T-04-SC | Tampering | npm installs (oauth-callback, open) | mitigate | Legitimacy gate run at planning time → both verdict OK (no postinstall, established maintainers); recorded here, no blocking checkpoint needed |
| T-04-03 | Denial of Service | migration push | accept | Idempotent migrator (re-run = no-op); single-user system, manual push under operator control |
</threat_model>

<verification>
- `bun run typecheck && bun run lint` clean across the workspace.
- `bun vitest run packages/core/src/brokerage/domain/token-freshness.test.ts` green.
- Live DB: pgcrypto extension present; broker_tokens table present with bytea columns; second migrate is a no-op.
</verification>

<success_criteria>
- oauth-callback + open installed (both legitimacy-OK).
- brokerage ports + pure freshness domain exported from @morai/core; in-memory twin exported from @morai/adapters.
- broker_tokens migration applied to the live DB with pgcrypto enabled (AUTH-02 precondition; success criterion 1 of the phase).
- Both app configs require the 7 Schwab/encryption env vars.
</success_criteria>

<output>
Create `.planning/phases/04-schwab-auth-brokerage/04-01-SUMMARY.md` when done.
</output>
