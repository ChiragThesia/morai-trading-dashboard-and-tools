---
phase: 18-analyzer-picker-ui-redesign
plan: 04
type: execute
wave: 3
depends_on: ["18-01", "18-02", "18-03"]
files_modified:
  - apps/web/src/screens/Analyzer.tsx
  - apps/web/src/screens/Analyzer.test.tsx
  - apps/web/src/components/picker/CandidateCard.tsx
  - apps/web/src/components/picker/CandidateCard.test.tsx
  - apps/web/src/components/picker/ScenarioStrip.tsx
autonomous: true
requirements: [ANLZ-01, ANLZ-02]

must_haves:
  truths:
    - "Opening /analyzer shows the ranked candidate-cards rail — each card renders its score + 4 breakdown bars read FROM candidate.breakdown filtered by criterion name (slope/fwdEdge/gexFit/eventAdjustment), never a hard-coded index; the beVsEm 5th entry is NEVER a 5th card bar"
    - "The guard-case card (fwdIv null) renders n/a for the fwd-edge bar caption with a zero-width bar and no throw/NaN"
    - "Clicking a card selects it; the payoff center repricing that candidate via candidateToAnalyzerPosition → repriceScenario → PayoffChart (todayCurveColor blue, expirationCurveColor violet)"
    - "Clicking ⊕ loads a single compare candidate as a dashed-amber compareCurve overlay; the ±1σ EM band renders at spot±expectedMove; the scenario strip shows T+0/@exp P&L at the 5 key levels"
  artifacts:
    - apps/web/src/screens/Analyzer.tsx
    - apps/web/src/components/picker/CandidateCard.tsx
    - apps/web/src/components/picker/ScenarioStrip.tsx
  key_links:
    - "Analyzer keeps its exact export name/signature so App.tsx route wiring needs zero changes"
    - "picker consumes pickerSnapshotFixture from @morai/contracts (18-01) and the candidateToAnalyzerPosition adapter (18-03); no usePositions/useGex live hooks (fixture-only, D-02b)"
---

<objective>
Rewrite `Analyzer.tsx` into the ranked-cards calendar PICKER (D-04) — the left rail of ranked
`CandidateCard`s with data-driven breakdown bars (ANLZ-01, D-01/D-05) and the center payoff panel that
reprices the selected candidate, overlays a ⊕-compare candidate, and draws the ±1σ EM band + scenario
strip (ANLZ-02, D-02/D-02a). This is `mockups/playground-v4.html` variant B made real against the
frozen fixture, using the one payoff engine and the additive PayoffChart props from 18-02. The right
column (why-panel/term-structure/plan) and the orphan deletions are 18-05.

Purpose: deliver the core scan → select → compare loop end-to-end against typed fixtures.
Output: rewritten `Analyzer.tsx` (screen skeleton, 3-col grid, selection/compare state, left rail,
center payoff), `CandidateCard.tsx`, `ScenarioStrip.tsx`, and rewritten `Analyzer.test.tsx` for these surfaces.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-analyzer-picker-ui-redesign/18-CONTEXT.md
@.planning/phases/18-analyzer-picker-ui-redesign/18-RESEARCH.md
@.planning/phases/18-analyzer-picker-ui-redesign/18-PATTERNS.md
@.planning/phases/18-analyzer-picker-ui-redesign/18-UI-SPEC.md
@mockups/playground-v4.html
</context>

<artifacts_this_phase_produces>
This plan: `CandidateCard` component, `ScenarioStrip` component, and the rewritten `Analyzer` screen
(same export name). Newly-created — exclude from drift/convergence checks. Consumes 18-01
(`pickerSnapshotFixture`), 18-02 (`compareCurve`/`compareCurveColor`/`expectedMoveBand`), 18-03
(`candidateToAnalyzerPosition`). The right-column components (`WhyPanel`/`TermStructureChart`/
`EntryExitPlan`) and deletions land in 18-05.
</artifacts_this_phase_produces>

<prohibitions>
- Breakdown bars MUST be rendered by filtering `candidate.breakdown` on `criterion` name — NEVER by
  hard-coded array index or four hard-coded fields (D-05). The 5th `beVsEm` entry is NEVER a 5th card bar.
- The picker screen MUST NOT call `usePositions`/`useGex`/`useLiveStream` or import
  `pairPositionsIntoCalendars`/`CalendarGroup` — it is 100% fixture-driven (D-02b; RESEARCH Open Question 1).
- Keep the exact `export function Analyzer(): React.ReactElement` name/signature — do NOT rename to Picker,
  do NOT touch App.tsx.
- No TOS-neon override, no new npm deps, no new shadcn components (UI-SPEC). No `any`/`as`/`!`.
- Do NOT write a second payoff path — reprice via `repriceScenario` only.
</prohibitions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CandidateCard component — data-driven breakdown bars + guard case (RED→GREEN)</name>
  <files>apps/web/src/components/picker/CandidateCard.tsx, apps/web/src/components/picker/CandidateCard.test.tsx</files>
  <read_first>
    - mockups/playground-v4.html — the `.cand-card` anatomy (header name + score, sub-line DTE/debit/θ/vega + event tags, the 4 breakdown-bar rows, ⊕ button, selected/hover states)
    - .planning/phases/18-analyzer-picker-ui-redesign/18-UI-SPEC.md — "Ranked candidate cards" contract + Color/Typography/Spacing tables + the guard-case render table (fwd-edge bar → n/a caption, zero-width bar)
    - apps/web/src/components/system/index.tsx — Panel/SectionLabel/Stat/MetricChip/PanelHeading molecules (reuse; do not invent layout primitives)
    - packages/contracts/src/picker.ts — PickerCandidate/breakdownEntry shape (18-01)
    - .planning/phases/18-analyzer-picker-ui-redesign/18-PATTERNS.md — Analyzer.tsx screen section (Overview.tsx as the sibling-screen analog)
  </read_first>
  <behavior>
    - RED: given a candidate, CandidateCard renders exactly 4 breakdown bars whose labels are slope/fwd edge/GEX fit/event adj and whose fill widths derive from the matching `candidate.breakdown` entries selected BY criterion name (test passes a candidate whose breakdown array is in a shuffled order and asserts each bar still maps to the right criterion — proving no index dependence).
    - RED: the beVsEm entry is present in the data but produces NO 5th bar (assert only 4 bar rows render).
    - RED: guard-case candidate (fwdIv null / fwdIvGuard "inverted") → the fwd-edge bar caption reads `n/a`, the bar width is 0, and nothing renders NaN or throws.
    - RED: clicking the card fires onSelect(candidate); clicking the ⊕ button fires onCompareToggle(candidate) and does NOT fire onSelect.
    - GREEN: implement to satisfy.
  </behavior>
  <action>
    Create `apps/web/src/components/picker/CandidateCard.tsx` exporting `CandidateCard` taking a
    `PickerCandidate`, `selected: boolean`, `compared: boolean`, `onSelect`, `onCompareToggle` props.
    Render the header (name 14px/700 + score number 14px/700 violet, right-aligned), the sub-line
    (9px dim: `DTE {front}/{back} · debit ${n} · θ +{n}/d · vega +{n}` + inline event tags per the
    `{Event}◂f`/`◂b`/`clean` convention), and FOUR breakdown-bar rows produced by looking up
    `candidate.breakdown` entries by criterion name in the fixed display order slope→fwdEdge→gexFit→
    eventAdjustment (fills: violet/blue/up-teal/amber per the UI-SPEC Color table; width = contribution
    normalized 0–100%). For the fwdEdge bar when `candidate.fwdIv === null`, render width 0 + caption `n/a`.
    Add the ⊕ Compare button (`button.mini` neutral styling, amber tint when `compared`), selected border/tint
    per UI-SPEC. Whole-card click = onSelect; ⊕ click = onCompareToggle (stop propagation). Author the RED
    tests in CandidateCard.test.tsx (shuffled-breakdown-order, 4-bars-only, guard-case, click delegation).
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/components/picker/CandidateCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Tests GREEN including the shuffled-order data-driven mapping, exactly-4-bars, guard-case n/a/zero-width/no-NaN, and click/⊕ delegation.
    - Grep confirms no hard-coded `breakdown[0]`/`breakdown[1]` index access in CandidateCard.tsx (lookups are by criterion name).
    - No `any`/`as`/`!`; `bun run typecheck` clean.
  </acceptance_criteria>
  <done>CandidateCard renders data-driven 4-bar cards, guard-safe, with correct select/compare delegation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Analyzer screen skeleton + ranked rail + scoring-methodology panel (RED→GREEN)</name>
  <files>apps/web/src/screens/Analyzer.tsx, apps/web/src/screens/Analyzer.test.tsx</files>
  <read_first>
    - apps/web/src/screens/Overview.tsx — sibling screen using Panel molecules + grid layout + PayoffChart (structural analog); confirm its PayoffChart call site props
    - apps/web/src/screens/Analyzer.tsx — the current file (to be REPLACED; keep only the `export function Analyzer()` name/signature)
    - apps/web/src/screens/Analyzer.test.tsx — the current tests (to be REWRITTEN wholesale — all assert retired behavior)
    - .planning/phases/18-analyzer-picker-ui-redesign/18-UI-SPEC.md — Layout (3-col grid 300px/1fr/330px), Copywriting Contract (locked section headings, scoring-methodology 3-item list, empty state)
    - packages/contracts/src/__fixtures__/picker-candidates.fixture.ts (via @morai/contracts) — the ranked candidate source
  </read_first>
  <behavior>
    - RED: Analyzer renders a "Suggested calendars" panel containing one CandidateCard per fixture candidate, ordered by descending score (ranked rail).
    - RED: the first (top-ranked) candidate is selected by default (selected card has the selected styling / is the payoff subject).
    - RED: clicking a different card updates the selected candidate (assert the selected state moves).
    - RED: the "Scoring methodology — verified & refuted" collapsible panel renders the locked 3-item list verbatim; the empty-state copy renders when the candidate list is empty.
    - GREEN: implement the skeleton + rail + state.
  </behavior>
  <action>
    Rewrite `Analyzer.tsx` keeping the exact `export function Analyzer(): React.ReactElement` signature.
    Import `{ pickerSnapshotFixture }` from "@morai/contracts". Build the 3-column grid (300px / 1fr / 330px,
    stacking below 1280px in DOM order) using the `system/index.tsx` molecules. Hold `selectedId` and
    `compareId` in `useState` (single compare slot). Left column: "Suggested calendars" Panel mapping the
    fixture candidates (already score-sorted, or sort defensively by score desc) to `CandidateCard`, wiring
    onSelect→setSelectedId and onCompareToggle→toggle compareId. Default `selectedId` to the top candidate.
    Center column: render the "Risk profile" Panel shell (payoff wiring lands in Task 3) with its dynamic
    subtitle, and below it the "Scoring methodology — verified & refuted" collapsible Panel with the locked
    3-item list (fixed reference copy, not fixture-driven). Render the empty-state copy when candidates is
    empty. Right column: leave a placeholder region for 18-05 (three stacked Panel shells with the locked
    headings "Why this calendar" / "Term structure + your legs" / "Entry / exit plan"). Rewrite
    `Analyzer.test.tsx` from scratch (delete all retired-behavior blocks) covering the RED behaviors, using
    Overview.test.tsx conventions (@testing-library/react render + Panel/CandidateCard queries).
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/screens/Analyzer.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Tests GREEN: ranked rail (one card per candidate, score-desc), default top selection, click-to-select, methodology list verbatim, empty state.
    - `export function Analyzer(): React.ReactElement` name/signature unchanged; `git diff apps/web/src/App.tsx` empty.
    - Grep confirms Analyzer.tsx imports NO usePositions/useGex/useLiveStream/pairPositionsIntoCalendars/CalendarGroup.
    - No `any`/`as`/`!`; `bun run typecheck` clean.
  </acceptance_criteria>
  <done>The picker screen skeleton + ranked rail + methodology/empty-state render and select correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Payoff center — reprice selected + ⊕-compare overlay + EM band + scenario strip (RED→GREEN)</name>
  <files>apps/web/src/screens/Analyzer.tsx, apps/web/src/screens/Analyzer.test.tsx, apps/web/src/components/picker/ScenarioStrip.tsx</files>
  <read_first>
    - apps/web/src/lib/candidate-to-position.ts (18-03 adapter) + apps/web/src/lib/scenario-engine.ts (repriceScenario, ScenarioParams, buildScenarioStrip level logic)
    - apps/web/src/components/charts/PayoffChart.tsx (18-02 props: todayCurveColor/expirationCurveColor already exist; compareCurve/compareCurveColor/expectedMoveBand new)
    - .planning/phases/18-analyzer-picker-ui-redesign/18-UI-SPEC.md — "Payoff center" contract (engine reuse, curve colors, EM band, scenario strip 5 levels, single compare slot)
    - mockups/playground-v4.html — the payoff center + scenario strip (5 levels: put wall, γ flip, spot, call wall, candidate strike) + `cmpExp` single compare curve
  </read_first>
  <behavior>
    - RED: selecting a candidate feeds `candidateToAnalyzerPosition(selected)` → `repriceScenario([pos], params)` and passes payoffCurve as todayCurve + expirationCurve to PayoffChart with todayCurveColor #5b9cf6 and expirationCurveColor #a78bfa (assert the props/colors on the rendered chart).
    - RED: loading a ⊕-compare candidate passes a non-null `compareCurve` (the compare candidate's expiration P&L) with compareCurveColor amber; clearing it passes `compareCurve={null}`.
    - RED: `expectedMoveBand` is passed as `{ spot: fixtureSpot, em: selected.expectedMove }`.
    - RED: ScenarioStrip renders T+0/@exp P&L for exactly the 5 key levels (put wall, γ flip, spot, call wall, candidate strike), deduped, computed via the same engine (no second pricing path).
    - GREEN: wire it all.
  </behavior>
  <action>
    Create `apps/web/src/components/picker/ScenarioStrip.tsx` exporting `ScenarioStrip` that, given the
    adapted position + the 5 key spot levels + the GEX context (from the fixture snapshot), renders a grid
    of T+0/@exp P&L values by re-running the existing engine at those 5 spots (reuse `buildScenarioStrip`'s
    dedup/cap logic conceptually or call it directly if the single-candidate signature fits — no new pricing
    math). In `Analyzer.tsx`, in the "Risk profile" Panel, useMemo the selected candidate's
    `repriceScenario` result and (when compareId set) the compare candidate's result; render `PayoffChart`
    with todayCurve=payoffCurve, expirationCurve=expirationCurve, rollCurve={null}, todayCurveColor="#5b9cf6",
    expirationCurveColor="#a78bfa", compareCurve={compareExpCurve ?? null}, compareCurveColor="#f0b429",
    expectedMoveBand={{ spot: fixtureSpot, em: selected.expectedMove }}, and the existing required props
    (gex from the fixture GEX context, spot, toggles, fitY/onFitYConsumed, positionSetSignature,
    baseExpirationCurve). Append the compare-title suffix `vs {compareName} (dashed)` in amber when compared.
    Render `ScenarioStrip` below the chart. Extend Analyzer.test.tsx with the RED assertions.
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/screens/Analyzer.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Tests GREEN: selected reprice + curve colors, compare overlay on/off (compareCurve null when cleared), EM band spot±em, scenario-strip 5 levels via the engine.
    - Grep confirms Analyzer.tsx calls `repriceScenario` and `candidateToAnalyzerPosition` (single payoff path) and passes `rollCurve={null}`.
    - `bun run test` + `bun run typecheck` + `bun run lint` GREEN at wave merge; `git diff apps/web/src/screens/Overview.tsx` shows no behavioral change.
  </acceptance_criteria>
  <done>The payoff center reprices the selected candidate, overlays a single dashed-amber compare, draws the EM band, and renders the 5-level scenario strip — all via the one engine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pickerSnapshotFixture (@morai/contracts) → picker screen render | Already-parsed typed fixture crosses into the UI; guard-case must render safely |
| candidate → view-only AnalyzerPosition → repriceScenario | Hypothetical position for curve drawing only; never persisted/order-routed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-07 | Tampering / DoS | guard-case card render (fwdIv null) | medium | mitigate | fwd-edge bar renders `n/a` + zero width; component test asserts no throw/NaN for the guard candidate |
| T-18-08 | Spoofing | candidate-derived positions | low | mitigate | live:false via the 18-03 adapter; picker is view-only, no persistence/order path (D-02b) |
| T-18-09 | Tampering (integrity) | additive PayoffChart props vs Overview | low | mitigate | picker passes the new props; Overview call site untouched; Overview.test.tsx stays GREEN |

V5 boundary is enforced upstream at 18-01's `.parse()`; this screen consumes the already-validated
fixture. V2/V3/V4/V6 N/A (the `/analyzer` route keeps its existing session gate; no new endpoint). No
package installs, no DB-schema file.
</threat_model>

<verification>
- `bunx vitest run apps/web/src/components/picker/CandidateCard.test.tsx` GREEN.
- `bunx vitest run apps/web/src/screens/Analyzer.test.tsx` GREEN (rail, selection, payoff, compare, EM, strip).
- `bun run test && bun run typecheck && bun run lint` clean; App.tsx and Overview.tsx unchanged.
- Manual (phase gate): `/analyzer` reads like `mockups/playground-v4.html` variant B left+center columns.
</verification>

<success_criteria>
The picker's scan → select → compare loop works end-to-end against the fixture: ranked cards with
data-driven breakdown bars (ANLZ-01), guard-safe rendering, and a payoff center that reprices the
selected candidate, overlays a single ⊕-compare curve, and shows the EM band + scenario strip (ANLZ-02).
</success_criteria>

<output>
Create `.planning/phases/18-analyzer-picker-ui-redesign/18-04-SUMMARY.md` when done.
</output>
