---
phase: 18-analyzer-picker-ui-redesign
plan: 03
type: tdd
wave: 2
depends_on: ["18-01"]
files_modified:
  - apps/web/src/lib/candidate-to-position.ts
  - apps/web/src/lib/candidate-to-position.test.ts
autonomous: true
requirements: [ANLZ-02]

must_haves:
  truths:
    - "candidateToAnalyzerPosition maps a PickerCandidate's two legs to exactly ONE AnalyzerPosition with live:false, included:true, front/back DTE + IV from the legs, and a synthesized occSymbol carrying the strike"
    - "A candidate-derived position's worst-case P&L on the expirationCurve does not exceed its debit within pricing tolerance (debit = max-loss invariant, D-01b) — proven by an example test AND a fast-check property test (numRuns ≥ 100)"
  artifacts:
    - apps/web/src/lib/candidate-to-position.ts
    - apps/web/src/lib/candidate-to-position.test.ts
  key_links:
    - "adapter imports PickerCandidate/pickerSnapshotFixture from @morai/contracts (18-01) and AnalyzerPosition from ./scenario-engine.ts — never the private calendarToAnalyzerPosition nor CalendarGroup"
    - "the debit-invariant test feeds the adapter output straight into the existing, already-tested repriceScenario (no second payoff path)"
---

<objective>
Author the NEW `candidateToAnalyzerPosition` adapter (D-02/D-02b, ANLZ-02) — the genuinely untested
numerical surface this phase introduces (RESEARCH Pitfall 1). It converts one `PickerCandidate` into
one throwaway, view-only `AnalyzerPosition` (`live: false`) that the existing `repriceScenario`/
`PayoffChart` stack draws. It follows the PATTERN of `Analyzer.tsx`'s private `calendarToAnalyzerPosition`
but imports/reuses NONE of it (that function is unexported and shaped for broker `CalendarGroup` legs a
hypothetical candidate does not have).

Purpose: give the picker screen (18-04) a tested, boundary-clean way to render a candidate's payoff via
the one payoff engine, and PROVE the debit=max-loss claim the entry/exit plan card (18-05) depends on.
Output: `candidate-to-position.ts` (adapter + `occSymbolForStrike` helper) + its test file (example +
fast-check property).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-analyzer-picker-ui-redesign/18-CONTEXT.md
@.planning/phases/18-analyzer-picker-ui-redesign/18-RESEARCH.md
@.planning/phases/18-analyzer-picker-ui-redesign/18-PATTERNS.md
@apps/web/src/lib/scenario-engine.ts
</context>

<artifacts_this_phase_produces>
This plan: `candidateToAnalyzerPosition` (adapter) + `occSymbolForStrike` (helper). Newly-created —
exclude from drift/convergence checks. Depends on 18-01's `PickerCandidate`/`pickerSnapshotFixture`.
Consumed by 18-04's picker screen. Reuses the KEEP symbols `AnalyzerPosition`/`repriceScenario` verbatim.
</artifacts_this_phase_produces>

<prohibitions>
- Do NOT import or export/reuse `Analyzer.tsx`'s private `calendarToAnalyzerPosition`, `CalendarGroup`,
  `pairPositionsIntoCalendars`, or `BrokerPositionResponse` (Pitfall 2). The picker never touches broker data.
- Do NOT write a second BSM/pricing path. Feed the adapter output into the existing `repriceScenario`.
- `live` MUST be `false` on every candidate-derived position (D-02b). No `any`/`as`/`!`.
</prohibitions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: candidateToAnalyzerPosition adapter + occSymbolForStrike helper (RED→GREEN)</name>
  <files>apps/web/src/lib/candidate-to-position.ts, apps/web/src/lib/candidate-to-position.test.ts</files>
  <read_first>
    - apps/web/src/lib/scenario-engine.ts — the AnalyzerPosition type (fields: id, name, live, occSymbol, putCall, frontDte, backDte, frontIv, backIv, qty, included)
    - apps/web/src/screens/Analyzer.tsx — the private `calendarToAnalyzerPosition` (the field-mapping PATTERN to imitate, NOT import)
    - .planning/phases/18-analyzer-picker-ui-redesign/18-RESEARCH.md — "Architecture Patterns / Pattern 3" (the concrete `occSymbolForStrike` + adapter code) and Pitfall 2
    - .planning/phases/18-analyzer-picker-ui-redesign/18-PATTERNS.md — candidate-to-position.ts section
  </read_first>
  <behavior>
    - RED: `candidateToAnalyzerPosition(fixtureCandidate)` returns an AnalyzerPosition with `live === false`, `included === true`, `qty` from the candidate (default 1), `frontDte`/`backDte` from frontLeg.dte/backLeg.dte, `frontIv`/`backIv` from frontLeg.iv/backLeg.iv, `putCall` from the leg, and an `occSymbol` that round-trips the back-leg strike (the existing strike-extraction the engine uses recovers the candidate's strike).
    - RED: the guard-case fixture candidate (fwdIv null) still adapts to a valid position without throwing (adapter ignores fwdIv — it only consumes legs).
    - GREEN: minimum adapter + helper to satisfy.
  </behavior>
  <action>
    Create `apps/web/src/lib/candidate-to-position.ts`. Import `type { AnalyzerPosition } from
    "./scenario-engine.ts"` and `type { PickerCandidate } from "@morai/contracts"`. Add a private
    `occSymbolForStrike(strike, putCall)` that synthesizes a well-formed OCC-shaped symbol carrying the
    strike (per RESEARCH Pattern 3) — never a real broker symbol (D-02b). Export
    `candidateToAnalyzerPosition(candidate: PickerCandidate): AnalyzerPosition` mapping: id←candidate.id,
    name←candidate.name, live←false, occSymbol←occSymbolForStrike(backLeg.strike, backLeg.putCall),
    putCall←backLeg.putCall, frontDte←frontLeg.dte, backDte←backLeg.dte, frontIv←frontLeg.iv,
    backIv←backLeg.iv, qty←1, included←true. Author the test importing `{ pickerSnapshotFixture }` from
    "@morai/contracts", selecting a normal candidate and the guard-case candidate, asserting the RED
    behaviors (verify occSymbol round-trips the strike via the same extraction the engine uses — read
    scenario-engine.ts for the strike-parse helper).
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/lib/candidate-to-position.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Adapter test GREEN: field mapping (incl. live:false, included:true, DTE/IV from legs), strike round-trip, guard-case adapts without throw.
    - Grep confirms candidate-to-position.ts does NOT import CalendarGroup / pairPositionsIntoCalendars / BrokerPositionResponse.
    - No `any`/`as`/`!`; `bun run typecheck` clean.
  </acceptance_criteria>
  <done>The adapter maps a candidate to one view-only AnalyzerPosition; boundary-clean, guard-safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Debit = max-loss invariant — example + fast-check property test (RED→GREEN)</name>
  <files>apps/web/src/lib/candidate-to-position.test.ts, apps/web/src/lib/candidate-to-position.ts</files>
  <read_first>
    - apps/web/src/lib/scenario-engine.ts — repriceScenario signature + ScenarioParams (spot, daysForward, ivShift, rate, divYield) + the expirationCurve field of ScenarioResult
    - apps/web/src/lib/scenario-engine.test.ts — the existing 1000-run fast-check heatmap property test (copy its fc.assert(fc.property(...)) generator/assertion style)
    - .planning/phases/18-analyzer-picker-ui-redesign/18-RESEARCH.md — "Code Examples / Debit = max-loss invariant" (the exact assertion shape) + Pitfall 1
    - .planning/research/calendar-selection-criteria.md — criterion 8 (debit = max loss valid for SPX European IF closed at/before front expiry)
  </read_first>
  <behavior>
    - RED (example): for a fixture candidate, `Math.min(...repriceScenario([candidateToAnalyzerPosition(c)], BASE_PARAMS).expirationCurve.map(p => p.pl))` is `>= -(c.debit) - TOLERANCE` — worst case at/before front expiry does not exceed the debit.
    - RED (property, numRuns ≥ 100): for arbitrary in-range candidate legs (strike near spot, front DTE 21–36, back DTE ≥ front+21, plausible IVs), the same invariant holds — the adapter never produces a position whose expiration worst case is worse than its stated debit beyond tolerance.
    - GREEN: adjust the adapter/helper only if the invariant legitimately fails (it should hold if the mapping is correct); do NOT weaken the assertion to force green.
  </behavior>
  <action>
    Extend `candidate-to-position.test.ts` with the debit=max-loss example test (use `pickerSnapshotFixture`
    candidates and a BASE_PARAMS with spot = fixture spot, daysForward 0, ivShift 0, a plausible rate, and
    the engine's default divYield). Add a `fast-check` property test (import `fc from "fast-check"`) with
    numRuns ≥ 100 that builds an arbitrary candidate (or the minimal leg set the adapter needs) within the
    calendar constraints and asserts the expiration-curve worst case ≥ −debit − TOLERANCE. Model the
    generator + `fc.assert(fc.property(...))` shape on scenario-engine.test.ts's existing property test.
    Pick TOLERANCE consistent with the engine's pricing granularity (document the chosen value in a comment).
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/lib/candidate-to-position.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both the example and the fast-check property (numRuns ≥ 100) assertions are GREEN.
    - The property test imports `fast-check` and mirrors the engine test's fc.assert(fc.property(...)) style.
    - `bun run test` (full suite) + `bun run typecheck` + `bun run lint` GREEN at wave merge.
  </acceptance_criteria>
  <done>The debit=max-loss invariant the plan card assumes is proven by example + property test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PickerCandidate (fixture, 18-01) → AnalyzerPosition (view-only) → repriceScenario | Hypothetical candidate data becomes a throwaway position for curve drawing; must never be mistaken for a real broker position |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-05 | Spoofing / Repudiation | candidate-derived AnalyzerPosition | low | mitigate | `live: false` on every adapted position (D-02b); no code path in this phase persists or order-routes candidate-derived positions — asserted by the adapter unit test |
| T-18-06 | Tampering (numerical integrity) | debit=max-loss claim feeding the plan card | medium | mitigate | Example + fast-check property test proves expiration worst case ≤ debit within tolerance, fed through the already-kernel-parity-tested repriceScenario (no second pricing path) |

V5 N/A in this plan (the schema/parse boundary is 18-01; here the input is the already-parsed typed
fixture). V2/V3/V4/V6 N/A. No package installs, no DB-schema file.
</threat_model>

<verification>
- `bunx vitest run apps/web/src/lib/candidate-to-position.test.ts` GREEN (mapping + guard + debit invariant example + property).
- `bun run test && bun run typecheck && bun run lint` clean at wave merge.
- Grep: adapter imports no broker/CalendarGroup symbols; `live: false` literal present.
</verification>

<success_criteria>
A tested, boundary-clean `candidateToAnalyzerPosition` adapter exists; the debit=max-loss invariant the
entry/exit plan card depends on is proven; the picker screen (18-04) can feed candidates into the one
payoff engine with no broker-data coupling and no second payoff code path.
</success_criteria>

<output>
Create `.planning/phases/18-analyzer-picker-ui-redesign/18-03-SUMMARY.md` when done.
</output>
