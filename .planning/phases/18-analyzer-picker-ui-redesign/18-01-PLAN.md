---
phase: 18-analyzer-picker-ui-redesign
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/picker.ts
  - packages/contracts/src/picker.test.ts
  - packages/contracts/src/__fixtures__/picker-candidates.fixture.ts
  - packages/contracts/src/index.ts
autonomous: true
requirements: [ANLZ-01, ANLZ-02, ANLZ-03]

must_haves:
  truths:
    - "pickerCandidate Zod schema parses a valid oracle payload and REJECTS a malformed breakdown entry via .parse() (never a swallowed .safeParse)"
    - "The frozen pickerSnapshotFixture (6-8 real candidates + 1 guard-case) parses clean against pickerSnapshotResponse"
    - "The guard-case candidate carries fwdIv=null + fwdIvGuard='inverted' and is schema-legal (never NaN)"
    - "breakdownEntry.criterion is a CLOSED enum {slope, fwdEdge, gexFit, eventAdjustment, beVsEm} — structurally excludes every REFUTED criterion (no IV-rank gate, no -1..-3% IV-diff band, no debit-%-of-back band)"
  artifacts:
    - packages/contracts/src/picker.ts
    - packages/contracts/src/picker.test.ts
    - packages/contracts/src/__fixtures__/picker-candidates.fixture.ts
    - packages/contracts/src/index.ts
  key_links:
    - "index.ts re-exports pickerCandidate/pickerSnapshotResponse + pickerSnapshotFixture so apps/web imports them from @morai/contracts with no cross-boundary reach into __fixtures__"
    - "fixture is typed to PickerSnapshotResponse so the Phase-19 /api/picker/candidates swap is import-only, zero layout/shape change"
---

<objective>
Author the `PickerCandidate` Zod contract (D-01) in `packages/contracts` and freeze
`mockups/playground-v4.html`'s real candidate data into a typed fixture (D-03) — the display-complete,
Phase-19-ready wire shape both the fixture (this phase) and the future `/api/picker/candidates`
response satisfy. This is the contract-first foundation the adapter (18-03) and the picker screen
(18-04/18-05) build on.

Purpose: lock the schema and the fixture BEFORE the real `scoreCalendarCandidates` engine lands so
UX risk is decoupled from scoring-correctness risk. The UI ships pure-render against typed fixtures.
Output: `picker.ts` (schema + inferred types), `picker.test.ts` (oracle-payload + reject + guard
parse tests), `__fixtures__/picker-candidates.fixture.ts` (frozen `pickerSnapshotFixture`), and the
`index.ts` re-export block.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-analyzer-picker-ui-redesign/18-CONTEXT.md
@.planning/phases/18-analyzer-picker-ui-redesign/18-RESEARCH.md
@.planning/phases/18-analyzer-picker-ui-redesign/18-PATTERNS.md
@.planning/phases/18-analyzer-picker-ui-redesign/18-UI-SPEC.md
@.planning/research/calendar-selection-criteria.md
</context>

<artifacts_this_phase_produces>
Contract layer (this plan): `pickerCandidateLeg`/`PickerCandidateLeg`, `breakdownEntry`/`BreakdownEntry`
(criterion enum slope|fwdEdge|gexFit|eventAdjustment|beVsEm), `exitPlan`/`ExitPlan`,
`pickerCandidate`/`PickerCandidate` (with nullable `fwdIv` + `fwdIvGuard` enum ok|inverted),
`termStructurePoint`/`TermStructurePoint`, `pickerGexContext`/`PickerGexContext`,
`pickerEvent`/`PickerEvent`, `pickerSnapshotResponse`/`PickerSnapshotResponse`, and the frozen
`pickerSnapshotFixture` const.
Downstream (other plans): `candidateToAnalyzerPosition`/`occSymbolForStrike` (18-03); PayoffChart props
`compareCurve`/`compareCurveColor`/`expectedMoveBand` (18-02); `CandidateCard` (18-04);
`WhyPanel`/`TermStructureChart`/`EntryExitPlan` + rewritten `Analyzer` screen (18-04/18-05).
These are newly-created symbols — exclude them from any drift/convergence check against prior phases.
</artifacts_this_phase_produces>

<prohibitions>
- The `breakdownEntry.criterion` enum MUST be exactly {slope, fwdEdge, gexFit, eventAdjustment, beVsEm}.
  It MUST NOT contain any criterion from the REFUTED list in `calendar-selection-criteria.md`
  (IV-rank / IV-percentile entry gates, the "back−front IV differential −1% to −3% ideal band", the
  "fair debit = 25–40% of back-month premium" band). The closed enum is the structural guarantee.
- `fwdIv` MUST be a nullable number paired with the `fwdIvGuard` enum tag — never encoded as NaN,
  never a Result<T,E>-shaped Zod union (that is a core/adapter idiom, not this repo's wire format).
- No `any`, no `as`, no `!` (typescript.md). Contracts layer imports zod + @morai/shared only.
</prohibitions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the pickerCandidate Zod contract + barrel re-export (RED→GREEN)</name>
  <files>packages/contracts/src/picker.ts, packages/contracts/src/picker.test.ts, packages/contracts/src/index.ts</files>
  <read_first>
    - packages/contracts/src/gex.ts (nested-object composition + z.infer-per-concern + nullable doc-comment idiom — the exact analog)
    - packages/contracts/src/gex.test.ts (oracle-payload .parse().not.toThrow() test pattern, lines 1-40)
    - packages/contracts/src/index.ts (existing per-contract re-export block formatting)
    - .planning/phases/18-analyzer-picker-ui-redesign/18-RESEARCH.md — "Architecture Patterns / Pattern 1" (concrete field list, accepted starting point) + "Standard Stack / Alternatives Considered" (nullable+enum-tag over Result union)
    - .planning/phases/18-analyzer-picker-ui-redesign/18-PATTERNS.md — picker.ts + picker.test.ts sections
  </read_first>
  <behavior>
    - RED: `picker.test.ts` asserts `pickerCandidate.parse(oraclePayload)` does NOT throw for a valid literal candidate (fails first — schema absent).
    - RED: a second `it` asserts `pickerCandidate.parse(malformedPayload)` DOES throw when a breakdown entry has an out-of-enum `criterion` or a missing numeric field (proves the V5 boundary rejects, not silently drops).
    - RED: a third `it` asserts a guard-case oracle payload (`fwdIv: null`, `fwdIvGuard: "inverted"`) parses clean — the nullable path is schema-legal.
    - RED: an `it` asserts `breakdownEntry.criterion` accepts each of {slope, fwdEdge, gexFit, eventAdjustment, beVsEm} and rejects any other string — the closed-set guarantee that structurally excludes refuted criteria.
    - GREEN: implement the schemas so all four pass.
  </behavior>
  <action>
    Create `packages/contracts/src/picker.ts` importing only `z` from "zod". Define, each as a named
    `z.object` + `z.infer` type mirroring gex.ts:
    - `pickerCandidateLeg`: strike (number), putCall (enum "C"/"P"), dte (int), iv (number).
    - `breakdownEntry`: criterion (enum ["slope","fwdEdge","gexFit","eventAdjustment","beVsEm"] — CLOSED),
      weight (number), rawValue (number), contribution (number).
    - `exitPlan`: profitTargetPct (number), stopPct (number), manageShortDte (int), closeByExpiry (string ISO date).
    - `pickerCandidate`: id (string), name (string), score (number.min(0).max(100)),
      breakdown (array(breakdownEntry)), debit (number), theta (number), vega (number), delta (number),
      fwdIv (number().nullable()), fwdIvGuard (enum ["ok","inverted"]), slope (number), fwdEdge (number),
      expectedMove (number), frontEvents (array(string)), backEvents (array(string)),
      frontLeg (pickerCandidateLeg), backLeg (pickerCandidateLeg), exitPlan (exitPlan).
    - `termStructurePoint`: dte (int), iv (number).
    - `pickerGexContext`: flip (number.nullable()), callWall (number.nullable()), putWall (number.nullable()),
      netGammaAtSpot (number), absGammaStrike (number.nullable()).
    - `pickerEvent`: date (string ISO), name (string).
    - `pickerSnapshotResponse`: spot (number), termStructure (array(termStructurePoint)),
      gex (pickerGexContext), events (array(pickerEvent)), candidates (array(pickerCandidate)).
    Add a module header comment (mirror gex.ts) stating this is the ONE schema both the Phase-18
    fixture and the Phase-19 GET /api/picker/candidates response + get_picker_candidates MCP tool must
    satisfy (MCP-02 pattern), per D-01. Doc-comment `fwdIv`/`fwdIvGuard` per D-01a: fwdIvGuard==="inverted"
    implies fwdIv===null (inverted term structure → negative forward radicand), mirroring gex.ts's
    nullable-field doc comments. Do NOT add `.refine()` cross-field validation (gex.ts has none; RESEARCH
    Assumption A4 — keep the repo's existing lightness; the fixture's own test asserts the invariant).
    Re-export every schema + inferred type from `index.ts` in a new "Picker contracts (Phase 18 — D-01;
    MCP-02: ONE schema source for the Phase-19 /api/picker/candidates + get_picker_candidates)" block
    matching the existing block formatting.
    Author `picker.test.ts` per the RED behaviors above using inline oracle literals (mirror gex.test.ts's
    `oraclePayload` const), importing `{ describe, it, expect } from "vitest"` and the schemas from "./picker.ts".
  </action>
  <verify>
    <automated>bunx vitest run packages/contracts/src/picker.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bunx vitest run packages/contracts/src/picker.test.ts` is GREEN with ≥4 assertions: valid parse, malformed reject, guard-case parse, closed-criterion-enum.
    - `pickerCandidate`, `pickerSnapshotResponse`, and their `z.infer` types are exported from `packages/contracts/src/index.ts` (grep confirms the new export block).
    - `bun run typecheck` clean; no `any`/`as`/`!` in picker.ts (grep the file).
    - No `.safeParse` in picker.test.ts (grep confirms `.parse` only for the boundary assertions).
  </acceptance_criteria>
  <done>Schema authored, re-exported, and the four RED tests pass GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze playground-v4 candidates into the typed fixture + parse test (RED→GREEN)</name>
  <files>packages/contracts/src/__fixtures__/picker-candidates.fixture.ts, packages/contracts/src/picker.test.ts</files>
  <read_first>
    - mockups/playground-v4.html — the header comment (real data anchors) + `buildCandidates()` OUTPUT values, `EVENTS`, `TERM`, `GEX` constants, `beWidth()` (for the beVsEm 5th entry). Port OUTPUT, NOT logic (D-03).
    - .planning/phases/18-analyzer-picker-ui-redesign/18-RESEARCH.md — Summary finding #3 + Assumption A3 (the 5th breakdown entry is beVsEm synthesized from beWidth/expectedMove, NOT the mockup's literal K===7500 strike-proximity term) + Common Pitfalls Pitfall 3 (guard case nulls ONLY fwdIv — expectedMove/theta/vega/score stay normal)
    - .planning/phases/18-analyzer-picker-ui-redesign/18-UI-SPEC.md — "Null-FwdIV / guard-case render branch" table
    - packages/contracts/src/picker.ts (the schema this fixture must satisfy)
    - .planning/phases/18-analyzer-picker-ui-redesign/18-PATTERNS.md — __fixtures__ section
  </read_first>
  <behavior>
    - RED: in `picker.test.ts`, `pickerSnapshotResponse.parse(pickerSnapshotFixture)` does NOT throw (fails first — fixture absent).
    - RED: assert the fixture contains exactly ONE guard-case candidate (`fwdIv === null && fwdIvGuard === "inverted"`) and that every OTHER candidate has `fwdIv !== null && fwdIvGuard === "ok"`.
    - RED: assert the guard-case candidate's `expectedMove`, `theta`, `vega`, `score` are all finite non-null numbers (Pitfall 3 — never null those out).
    - RED: assert every candidate's `breakdown` array contains all five criteria names once (the 4 card bars + the beVsEm entry).
  </behavior>
  <action>
    Create `packages/contracts/src/__fixtures__/picker-candidates.fixture.ts`. Import only
    `type { PickerSnapshotResponse } from "../picker.ts"`. Export
    `export const pickerSnapshotFixture: PickerSnapshotResponse = { ... }` as pure frozen data — NO
    functions, NO scoring logic (D-03). Capture the mockup's real anchors: spot 7498.85, GEX flip 7473,
    callWall 7525, putWall 7400, netGammaAtSpot +26.2B (in the mockup's units), absGammaStrike from the
    mockup; the FOMC 7/29 · CPI 7/14 + 8/12 · NFP 7/3 + 8/7 event set into `events`; the ATM-IV term
    structure points into `termStructure` (dte/iv pairs across the 0–82 DTE range the UI-SPEC axis names).
    Freeze the top 6–8 candidates from `buildCandidates()` OUTPUT — capture the exact rendered per-candidate
    values (id, name like "7500P Aug 8 / Sep 12", score, debit, theta, vega, delta, fwdIv, slope, fwdEdge,
    expectedMove, frontEvents/backEvents, frontLeg/backLeg strike·putCall·dte·iv). Set each candidate's
    `exitPlan` to the D-01b defaults: profitTargetPct 0.25, stopPct 0.175, manageShortDte 21, closeByExpiry
    = the pre-computed (front expiry) ISO date. Build each `breakdown[]` from the four scored criteria
    (slope/fwdEdge/gexFit/eventAdjustment — weights 40/25/15/10 per the mockup's score formula, contribution
    = each term's 0–100 normalized share) PLUS a fifth `beVsEm` entry whose rawValue/contribution are
    SYNTHESIZED from the candidate's breakeven-width-vs-expectedMove ratio (the mockup's `beWidth(c)/(2*em)`
    table calc, weight 10) — do NOT port the mockup's literal K===7500 strike-proximity term (RESEARCH A3).
    Add exactly ONE guard-case candidate (D-03a; the specific inverted-structure numbers are the planner/
    executor's to pick per D-06, provided `fwdIv` resolves to the null/guard branch): an inverted-structure
    calendar where the forward radicand is negative → `fwdIv: null`, `fwdIvGuard: "inverted"`, its
    `fwdEdge`-named breakdown entry contribution 0;
    every other field (expectedMove, theta, vega, score, slope, gexFit, beVsEm) a normal finite value.
    All other candidates: `fwdIvGuard: "ok"`. Extend `picker.test.ts` importing `{ pickerSnapshotFixture }`
    from "./__fixtures__/picker-candidates.fixture.ts" with the RED assertions above.
  </action>
  <verify>
    <automated>bunx vitest run packages/contracts/src/picker.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bunx vitest run packages/contracts/src/picker.test.ts` GREEN including the fixture-parse, single-guard-case, guard-fields-finite, and all-five-criteria assertions.
    - Fixture has between 6 and 8 non-guard candidates plus exactly 1 guard-case candidate (test asserts the counts).
    - `pickerSnapshotFixture` re-exported from `index.ts` so `apps/web` imports it from `@morai/contracts` (add to the Task-1 export block).
    - Grep confirms the fixture file contains no function declarations / no scoring arithmetic identifiers (`buildCandidates`, `fwdIV(`, `putGreeks`) — data only.
    - `bun run typecheck` clean.
  </acceptance_criteria>
  <done>Frozen fixture parses against the schema, the guard-case is present and safe, and all fixture assertions pass GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture data (this phase) / future /api/picker/candidates response (Phase 19) → picker render | Untrusted-shaped display data crosses into the UI; the Zod schema is the sole validation gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Tampering | pickerSnapshotResponse `.parse()` (V5 Input Validation) | medium | mitigate | Parse the fixture with `.parse()` (never a swallowed `.safeParse`) in `picker.test.ts`; a malformed/incomplete fixture fails the suite, never reaches render as NaN |
| T-18-02 | Tampering / DoS | `fwdIv` nullable field | low | mitigate | Nullable value + `fwdIvGuard` enum tag; guard-case candidate proves the null path is schema-legal (never NaN) — consumed safely by the render branch in 18-04/18-05 |
| T-18-03 | Spoofing | REFUTED-criteria encoding | low | mitigate | `breakdownEntry.criterion` is a CLOSED enum structurally excluding IV-rank/IV-diff-band/debit-%-of-back; a test asserts the enum rejects out-of-set strings |

V2/V3/V4/V6 are N/A this phase (no new auth/session/endpoint/crypto — the `/analyzer` route keeps its
existing session gate; the real API is Phase 19). No package installs (zero new deps, v1.2 lock) → no
supply-chain checkpoint. No DB-schema file in scope (`picker.ts` is a Zod API contract, not a migration).
</threat_model>

<verification>
- `bunx vitest run packages/contracts/src/picker.test.ts` GREEN (all schema + fixture assertions).
- `bun run typecheck && bun run lint` clean at wave merge.
- Grep: picker.ts has no `any`/`as`/`!`; picker.test.ts uses `.parse` (not swallowed `.safeParse`);
  fixture is data-only.
</verification>

<success_criteria>
The `PickerCandidate`/`pickerSnapshotResponse` contract exists and is re-exported; the frozen
`pickerSnapshotFixture` (6-8 candidates + 1 guard-case) parses clean against it; the guard-case renders
schema-legal null (never NaN); the criterion enum is closed against refuted criteria. Phase 19 can swap
the fixture import for a live API response with zero shape change.
</success_criteria>

<output>
Create `.planning/phases/18-analyzer-picker-ui-redesign/18-01-SUMMARY.md` when done.
</output>
