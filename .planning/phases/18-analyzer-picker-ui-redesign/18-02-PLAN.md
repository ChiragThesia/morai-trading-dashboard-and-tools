---
phase: 18-analyzer-picker-ui-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/charts/PayoffChart.tsx
  - apps/web/src/components/charts/PayoffChart.test.tsx
autonomous: true
requirements: [ANLZ-02]

must_haves:
  truths:
    - "PayoffChart renders compareCurve as a single dashed amber line when supplied, and renders nothing extra when compareCurve is null/absent"
    - "PayoffChart renders an expectedMoveBand as two ticks at spot±em joined at the zero-P&L y-position, BELOW (never occluding) the T+0/@exp curve layers"
    - "Overview.tsx's existing PayoffChart call site is behaviorally unchanged — the two new prop pairs are optional with defaults, no required-prop break under exactOptionalPropertyTypes"
  artifacts:
    - apps/web/src/components/charts/PayoffChart.tsx
    - apps/web/src/components/charts/PayoffChart.test.tsx
  key_links:
    - "compareCurve is a SEPARATE conditional layer next to the retired rollCurve layer (picker passes rollCurve={null}) — not a repurposing of rollCurve"
    - "expectedMoveBand drawn between the existing zero-line layer and the T+0 curve layer (z-order per UI-SPEC)"
---

<objective>
Extend `PayoffChart` with the two additive optional prop pairs the picker's payoff center needs
(D-02a, ANLZ-02): `compareCurve`/`compareCurveColor` (the ⊕-compare overlay) and `expectedMoveBand`
(the ±1σ EM band). Follow the component's own established additive-prop idiom (3 prior rounds) so
`Overview.tsx`'s existing call site is untouched. No second payoff code path — this is presentation
only; the curve data comes from `repriceScenario` output wired by the screen (18-04).

Purpose: give the picker screen the chart primitives to overlay a compare candidate and mark the
expected-move band without inventing a parallel chart or breaking the Overview hero.
Output: extended `PayoffChartProps` + two new conditional SVG layers, covered by new component tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-analyzer-picker-ui-redesign/18-CONTEXT.md
@.planning/phases/18-analyzer-picker-ui-redesign/18-RESEARCH.md
@.planning/phases/18-analyzer-picker-ui-redesign/18-PATTERNS.md
@.planning/phases/18-analyzer-picker-ui-redesign/18-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
This plan: PayoffChart props `compareCurve` (ReadonlyArray<PayoffPoint> | null), `compareCurveColor`
(string, default AMBER), `expectedMoveBand` ({ spot: number; em: number } | null). These are
newly-created props — exclude from drift/convergence checks against prior phases.
Related phase artifacts (other plans): `pickerCandidate`/`pickerSnapshotResponse`/`pickerSnapshotFixture`
(18-01), `candidateToAnalyzerPosition` (18-03), `CandidateCard`/`WhyPanel`/`TermStructureChart`/
`EntryExitPlan` + rewritten `Analyzer` (18-04/18-05).
</artifacts_this_phase_produces>

<prohibitions>
- Do NOT repurpose `rollCurve` or the `highlighted*Curve` props for the compare overlay (rollCurve is
  solid/no-dash/semantically "roll"; highlighted* are non-amber and drive Overview's row-highlight).
  Add a distinct `compareCurve` prop + its own conditional layer.
- Do NOT make either new prop required. Each MUST be `?`-optional in the interface AND have a
  destructured default in the function signature (Pitfall 4 / exactOptionalPropertyTypes).
- Do NOT touch `Overview.tsx` — its call site must remain byte-for-byte behaviorally identical.
- No `any`/`as`/`!`. Reuse the existing module constants `AMBER`/`BLUE` (do not redeclare).
</prohibitions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add compareCurve/compareCurveColor prop pair + dashed-amber layer (RED→GREEN)</name>
  <files>apps/web/src/components/charts/PayoffChart.tsx, apps/web/src/components/charts/PayoffChart.test.tsx</files>
  <read_first>
    - apps/web/src/components/charts/PayoffChart.tsx — PayoffChartProps interface, the destructuring defaults in the function signature, the module color constants (AMBER/BLUE), and the existing rollCurve conditional layer (the `!== null && .length > 0 &&` guard idiom)
    - apps/web/src/components/charts/PayoffChart.test.tsx — existing @testing-library/react render + SVG-assertion style
    - apps/web/src/screens/Overview.tsx — the PayoffChart call site (proof it omits the new props)
    - .planning/phases/18-analyzer-picker-ui-redesign/18-PATTERNS.md — PayoffChart.tsx section (interface-extension + destructuring-default + z-order patterns)
    - .planning/phases/18-analyzer-picker-ui-redesign/18-UI-SPEC.md — Color section (⊕-compare curve row: amber, dashed, 1.6px stroke, no glow)
  </read_first>
  <behavior>
    - RED: rendering PayoffChart with a non-empty `compareCurve` produces exactly one additional dashed-amber path (assert stroke color = the AMBER constant value, a dasharray attribute present, single line — no T+0 twin).
    - RED: rendering with `compareCurve={null}` (and when the prop is omitted entirely) produces NO extra path — the SVG path count matches the pre-change baseline (Overview regression guard).
    - GREEN: minimum layer + prop to satisfy both.
  </behavior>
  <action>
    Extend `PayoffChartProps` with `compareCurve?: ReadonlyArray<PayoffPoint> | null` and
    `compareCurveColor?: string`, each with a doc comment naming ANLZ-02 and the default. Add
    `compareCurve = null, compareCurveColor = AMBER` to the destructured function-signature defaults
    list (same idiom as the existing `todayCurveColor = VIOLET` / `expirationCurveColor = GRAY_MUTED`).
    Render a NEW conditional block — its own layer, alongside (not inside) the existing rollCurve block —
    guarded by `compareCurve !== null && compareCurve.length > 0`, drawing a single `LinePath` with
    `stroke={compareCurveColor}`, a dashed `strokeDasharray`, ~1.6px stroke width, and no glow filter.
    Draw it in the compare-overlay z-band (over the base curves, matching the mockup's `cmpExp` single
    compare curve). Add the two RED test cases to `PayoffChart.test.tsx`.
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/components/charts/PayoffChart.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - New tests GREEN: dashed-amber single line when supplied; zero extra path when null/absent.
    - `PayoffChartProps` declares both new props as `?`-optional; the function signature has destructured defaults `compareCurve = null` and `compareCurveColor = AMBER`.
    - `bunx vitest run apps/web/src/screens/Overview.test.tsx` still GREEN (call-site regression).
    - `bun run typecheck` clean (no exactOptionalPropertyTypes break in Overview.tsx); no `any`/`as`/`!` added.
  </acceptance_criteria>
  <done>compareCurve renders as a distinct dashed-amber layer, absent-safe, Overview unaffected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add expectedMoveBand prop + tick/connector layer at the zero line (RED→GREEN)</name>
  <files>apps/web/src/components/charts/PayoffChart.tsx, apps/web/src/components/charts/PayoffChart.test.tsx</files>
  <read_first>
    - apps/web/src/components/charts/PayoffChart.tsx — the existing "Zero line" layer and the xScale/zeroY helpers used to map spot→x and 0→y; the T+0 curve layer that follows it (for z-order placement)
    - .planning/phases/18-analyzer-picker-ui-redesign/18-UI-SPEC.md — Color section (±1σ EM band row: blue tick marks + horizontal connector at the zero line, label `±1σ EM`) and the Payoff-center EM-band contract
  </read_first>
  <behavior>
    - RED: with `expectedMoveBand={{ spot, em }}`, two vertical tick marks render at the x-positions of `spot − em` and `spot + em`, joined by a horizontal connector at the zero-P&L y (assert element positions map to spot±em via xScale and to zeroY).
    - RED: the EM-band group appears in SVG source order BEFORE the T+0 and @exp curve layers (z-order — never occludes a curve).
    - RED: with `expectedMoveBand={null}`/omitted, no band elements render.
    - GREEN: minimum layer to satisfy.
  </behavior>
  <action>
    Extend `PayoffChartProps` with `expectedMoveBand?: { spot: number; em: number } | null` (doc comment,
    ANLZ-02). Add `expectedMoveBand = null` to the destructured defaults. Render a new conditional group
    guarded by `expectedMoveBand !== null`, drawing two short vertical ticks at `xScale(spot - em)` and
    `xScale(spot + em)` plus a thin horizontal connector between them at the zero-P&L y (reuse the existing
    zeroY the "Zero line" layer already computes), colored with the existing BLUE constant, with a 9px
    `±1σ EM` label. Place this block BETWEEN the existing zero-line layer and the first curve layer so it
    sits under the curves (UI-SPEC z-order). Add the RED tests to `PayoffChart.test.tsx` asserting the
    spot±em x-mapping, the zero-line y, and the source-order-before-curves invariant.
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/components/charts/PayoffChart.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - EM-band tests GREEN: ticks at spot±em at the zero-line y; group ordered before curve layers; absent-safe when null.
    - `expectedMoveBand` is `?`-optional with a `= null` destructured default; BLUE constant reused (not redeclared).
    - `bun run test` (full suite) + `bun run typecheck` + `bun run lint` GREEN at wave merge; `git diff apps/web/src/screens/Overview.tsx` shows no behavioral change.
  </acceptance_criteria>
  <done>expectedMoveBand renders under the curves at spot±em on the zero line, absent-safe, Overview untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| screen props → PayoffChart SVG render | Curve/band data (from repriceScenario output in 18-04) crosses into presentational SVG; no external/untrusted input, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-04 | Tampering (integrity) | additive PayoffChartProps breaking Overview | medium | mitigate | New props optional with destructured defaults; Overview.test.tsx regression must stay GREEN; typecheck under exactOptionalPropertyTypes gates a missing default |

V5 is N/A here (no schema/parse boundary in this plan — typed props only). V2/V3/V4/V6 N/A (no
auth/session/endpoint/crypto). No package installs, no DB-schema file in scope.
</threat_model>

<verification>
- `bunx vitest run apps/web/src/components/charts/PayoffChart.test.tsx` GREEN (compare + EM-band cases).
- `bunx vitest run apps/web/src/screens/Overview.test.tsx` GREEN (call-site regression).
- `bun run typecheck && bun run lint` clean; Overview.tsx behaviorally unchanged.
</verification>

<success_criteria>
PayoffChart accepts a dashed-amber compare overlay and an expected-move band as optional additive
props, both absent-safe, with the Overview hero's existing call site behaviorally identical. The picker
screen (18-04) can now render the ⊕-compare curve and the ±1σ EM band with no second payoff code path.
</success_criteria>

<output>
Create `.planning/phases/18-analyzer-picker-ui-redesign/18-02-SUMMARY.md` when done.
</output>
