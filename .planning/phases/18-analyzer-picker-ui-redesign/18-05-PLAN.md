---
phase: 18-analyzer-picker-ui-redesign
plan: 05
type: execute
wave: 4
depends_on: ["18-04"]
files_modified:
  - apps/web/src/components/picker/WhyPanel.tsx
  - apps/web/src/components/picker/WhyPanel.test.tsx
  - apps/web/src/components/picker/TermStructureChart.tsx
  - apps/web/src/components/picker/TermStructureChart.test.tsx
  - apps/web/src/components/picker/EntryExitPlan.tsx
  - apps/web/src/components/picker/EntryExitPlan.test.tsx
  - apps/web/src/screens/Analyzer.tsx
  - apps/web/src/screens/Analyzer.test.tsx
  - apps/web/src/lib/scenario-engine.ts
  - apps/web/src/lib/scenario-engine.test.ts
autonomous: true
requirements: [ANLZ-03]

must_haves:
  truths:
    - "The why-panel shows the Fwd IV / Slope / Net θ / θ:vega stat grid and TWO candidate-conditional sentences — forward-edge branches on fwdEdge>0 vs the guard sentence when fwdIv is null; event-premium branches on frontEvents.length>0"
    - "The term-structure mini-chart draws the fixture's term-structure line + amber event markers + front/back leg dots + a forward-IV bracket between the leg x-positions — and OMITS the bracket (showing a guard tag) when fwdIv is null"
    - "The entry/exit plan card renders the 5 locked rows and computes profit target = debit×0.25 and stop = debit×0.175 from candidate.exitPlan, formatting manageShortDte (21) and closeByExpiry"
    - "Every old-Analyzer orphan (RollSimulator, AdHocPicker, AttributionWaterfall, GreekStrips, PnlHeatmap, LevelBar, tos-parser, rollScenario) is deleted after grep-confirming no remaining caller; Overview.tsx and the KEEP engine symbols are untouched"
  artifacts:
    - apps/web/src/components/picker/WhyPanel.tsx
    - apps/web/src/components/picker/TermStructureChart.tsx
    - apps/web/src/components/picker/EntryExitPlan.tsx
  key_links:
    - "right-column panels wire into Analyzer.tsx under the locked headings from 18-04's placeholder shells"
    - "deletions are delete-if-orphaned only (D-04a) — repriceScenario/AnalyzerPosition/PayoffChart/pairPositionsIntoCalendars stay for Overview"
---

<objective>
Complete the picker's right column (ANLZ-03, D-01b): the `WhyPanel` (stat grid + candidate-conditional
narrative with the guard branch), the `TermStructureChart` (term line + event markers + leg dots +
forward-IV bracket, guard-omitted), and the `EntryExitPlan` card (5 locked rows, +25%/−17.5% defaults).
Then retire the old position-analyzer machinery (D-04/D-04a) — delete the now-orphaned components/lib
files after grep-confirming callers, leaving the KEEP engine symbols and `Overview.tsx` untouched.

Purpose: finish the why/reasoning surfaces and remove the dead position-analyzer surface, so the phase
ships the full picker with the smallest surface area and no duplication.
Output: three right-column components + tests, their wiring into `Analyzer.tsx`, and the orphan deletions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-analyzer-picker-ui-redesign/18-CONTEXT.md
@.planning/phases/18-analyzer-picker-ui-redesign/18-RESEARCH.md
@.planning/phases/18-analyzer-picker-ui-redesign/18-PATTERNS.md
@.planning/phases/18-analyzer-picker-ui-redesign/18-UI-SPEC.md
@mockups/playground-v4.html
</context>

<artifacts_this_phase_produces>
This plan: `WhyPanel`, `TermStructureChart`, `EntryExitPlan` components. Newly-created — exclude from
drift/convergence checks. Consumes 18-01 (`pickerCandidate`/`pickerSnapshotFixture` term-structure + GEX
+ events context) and wires into 18-04's `Analyzer` shells. Deletions retire the old-Analyzer orphans.
</artifacts_this_phase_produces>

<prohibitions>
- The guard-case candidate MUST NOT render a fabricated forward-IV bracket or a numeric Fwd IV value —
  Fwd IV stat shows `—`, the forward-edge sentence is the dedicated guard sentence, the term structure
  omits the bracket and shows a `guard` tag. Never NaN, never a blank number (UI-SPEC guard table).
- Net θ is always rendered as an up/positive value (constraint θ>0 enforced by fixture) — never negative.
- Deletions are delete-if-orphaned ONLY (D-04a): grep-confirm zero non-test callers before deleting each
  symbol/file. Do NOT delete or alter `repriceScenario`, `AnalyzerPosition`, `bookPL`, `buildScenarioStrip`,
  `PayoffChart`, `pairPositionsIntoCalendars`, `CalendarGroup`. Do NOT touch `Overview.tsx`.
- No `any`/`as`/`!`; no new npm deps; inline SVG for the term-structure chart (no new charting dep).
</prohibitions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WhyPanel + EntryExitPlan — stat grid, conditional narrative, plan arithmetic (RED→GREEN)</name>
  <files>apps/web/src/components/picker/WhyPanel.tsx, apps/web/src/components/picker/WhyPanel.test.tsx, apps/web/src/components/picker/EntryExitPlan.tsx, apps/web/src/components/picker/EntryExitPlan.test.tsx</files>
  <read_first>
    - mockups/playground-v4.html — the why-panel stat grid + the forward-edge/event narrative branches (`edgeTxt`/`evtTxt`) + `renderPlan()` (target=debit×0.25, stop=debit×0.175, manage date, hard-close row)
    - .planning/phases/18-analyzer-picker-ui-redesign/18-UI-SPEC.md — "Why-panel" + "Entry/exit plan card" contracts, the Copywriting Contract (locked guard sentence, plan row labels, footnote), and the guard-case render table
    - apps/web/src/components/system/index.tsx — Stat molecule (2×2 grid) reuse
    - packages/contracts/src/picker.ts — PickerCandidate fields (fwdIv, fwdEdge, slope, theta, vega, frontEvents, exitPlan, debit)
  </read_first>
  <behavior>
    - RED (WhyPanel): stat grid renders Fwd IV / Slope / Net θ / θ:vega; for a normal candidate Fwd IV shows `{n}%`, for the guard candidate (fwdIv null) it shows `—` with the front-IV sub-caption unchanged.
    - RED: the forward-edge sentence uses the front-rich wording when fwdEdge>0, the forward-tailwind wording when fwdEdge≤0, and the dedicated locked guard sentence when fwdIv is null.
    - RED: the event-premium sentence warns (naming the events) when frontEvents.length>0, else states the structural/non-event wording; Net θ always renders positive.
    - RED (EntryExitPlan): renders the 5 locked rows; target value = debit×0.25 and stop value = debit×0.175 (assert the computed dollar amounts); manageShortDte (21) and closeByExpiry formatted; the footnote renders verbatim.
    - GREEN: implement both.
  </behavior>
  <action>
    Create `WhyPanel.tsx` exporting `WhyPanel(candidate, gexContext)`: a Stat 2×2 grid (Fwd IV — nullable
    → `—`; Slope violet if positive else down/red; Net θ always up-teal; θ:vega ratio) plus two prose
    sentences (forward-edge branch on `fwdEdge>0` vs guard sentence when `fwdIv===null`; event-premium branch
    on `frontEvents.length>0`) and a closing GEX-fit sentence referencing the fixture's static GEX context
    (flip/walls/netGamma — NOT live). Use the exact locked copy from the Copywriting Contract for the guard
    sentence. Create `EntryExitPlan.tsx` exporting `EntryExitPlan(candidate)`: the 5 locked rows
    (`Debit = max loss` / `Profit target (+25%)` / `Stop (−17.5%)` / `Manage short (21 DTE)` / `Hard close by`)
    with values from `candidate.exitPlan` and `candidate.debit` (target = debit×profitTargetPct,
    stop = debit×stopPct), plus the verbatim footnote. Author both test files per the RED behaviors.
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/components/picker/WhyPanel.test.tsx apps/web/src/components/picker/EntryExitPlan.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Tests GREEN: stat grid (incl. guard `—`), forward-edge 3-way branch, event branch, Net θ positive, plan arithmetic debit×0.25 / debit×0.175, formatted dates, verbatim footnote.
    - Grep confirms the locked guard sentence + plan row labels + footnote strings appear in the components.
    - No `any`/`as`/`!`; `bun run typecheck` clean.
  </acceptance_criteria>
  <done>WhyPanel and EntryExitPlan render the reasoning + plan surfaces with correct branching and arithmetic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TermStructureChart (guard-aware) + wire the right column into Analyzer (RED→GREEN)</name>
  <files>apps/web/src/components/picker/TermStructureChart.tsx, apps/web/src/components/picker/TermStructureChart.test.tsx, apps/web/src/screens/Analyzer.tsx, apps/web/src/screens/Analyzer.test.tsx</files>
  <read_first>
    - mockups/playground-v4.html — the term-structure mini-chart (~310×150, term line, amber event markers, front coral / back teal leg dots, blue dashed forward-IV bracket) and its axis range
    - .planning/phases/18-analyzer-picker-ui-redesign/18-UI-SPEC.md — "Term structure mini-chart" contract (axis DTE 0–82, IV 8%–15.5%, fixture-fixed) + guard-case bracket-omission + `guard` tag
    - apps/web/src/components/charts/PayoffChart.tsx — the existing hand-rolled inline-SVG precedent (svg/path helpers, scale mapping)
    - apps/web/src/screens/Analyzer.tsx — the right-column placeholder shells from 18-04 (locked headings) to wire into
  </read_first>
  <behavior>
    - RED: given the fixture term structure + a candidate, TermStructureChart renders the term-structure polyline, one amber vertical marker per event in the fixture events list, and two leg dots at the front/back DTE x-positions.
    - RED (normal candidate): a forward-IV bracket (blue dashed horizontal) renders between the two leg x-positions.
    - RED (guard candidate, fwdIv null): NO bracket renders; a `guard` amber tag renders near the leg dots instead; no throw/NaN.
    - RED (Analyzer wiring): selecting a candidate renders WhyPanel + TermStructureChart + EntryExitPlan for that candidate under the three locked right-column headings; selecting the guard candidate shows the guard sentence and the omitted bracket.
    - GREEN: implement + wire.
  </behavior>
  <action>
    Create `TermStructureChart.tsx` exporting `TermStructureChart(termStructure, events, candidate)` drawing
    an inline SVG (~310×150, axis DTE 0–82 / IV 8%–15.5%, fixture-fixed — not auto-scaled): the term-structure
    polyline, amber dashed vertical event markers, front (coral/down) + back (teal/up) leg dots at their DTE
    x-positions, and — only when `candidate.fwdIv !== null` — a blue dashed horizontal forward-IV bracket
    between the two leg x-positions; when null, omit the bracket and render a small amber `guard` `.tag`.
    Reuse PayoffChart.tsx's hand-rolled SVG helper style. In `Analyzer.tsx`, replace the three right-column
    placeholder shells (18-04) with `WhyPanel`, `TermStructureChart`, `EntryExitPlan` for the selected
    candidate (passing the fixture snapshot's termStructure/events/gex context). Extend Analyzer.test.tsx
    with the right-column + guard-candidate integration assertions.
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/components/picker/TermStructureChart.test.tsx apps/web/src/screens/Analyzer.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Tests GREEN: term line + event markers + leg dots; bracket present for normal / omitted + guard tag for guard case; Analyzer renders all three right-column panels under the locked headings for the selected candidate.
    - No `any`/`as`/`!`; `bun run typecheck` clean; `git diff apps/web/src/App.tsx` empty.
  </acceptance_criteria>
  <done>The term-structure chart renders guard-aware and the full right column is wired into the picker.</done>
</task>

<task type="auto">
  <name>Task 3: Retire old-Analyzer orphans (delete-if-orphaned) + full-suite green gate</name>
  <files>apps/web/src/lib/scenario-engine.ts, apps/web/src/lib/scenario-engine.test.ts</files>
  <read_first>
    - .planning/phases/18-analyzer-picker-ui-redesign/18-RESEARCH.md — "Old-Analyzer Retirement — Caller-Verified Keep/Delete List" (the exact keep/delete table)
    - .planning/phases/18-analyzer-picker-ui-redesign/18-PATTERNS.md — Deletions table (verified current locations)
    - apps/web/src/lib/scenario-engine.ts — locate `rollScenario` + its describe block in scenario-engine.test.ts
  </read_first>
  <action>
    For each retirement candidate — `RollSimulator.tsx`, `AdHocPicker.tsx`, `AttributionWaterfall.tsx`,
    `GreekStrips.tsx`, `PnlHeatmap.tsx`, `LevelBar.tsx`, `tos-parser.ts` (+ each one's `*.test.tsx`/`*.test.ts`),
    and the `rollScenario` function (+ its describe block in scenario-engine.test.ts) — run `rg -l` for
    non-test importers FIRST. Delete a file/symbol ONLY when the sole remaining importer is the (now
    rewritten) picker or nothing (D-04a). Leave `bookGreekStrips`/`heatmapCells` fields on
    `repriceScenario`'s output untouched (D-02: no engine surgery — cheaper to leave the fields than to
    touch the tested shape). Do NOT delete any KEEP symbol. Run the full suite + typecheck + lint and
    confirm `Overview.tsx` has no behavioral diff.
  </action>
  <verify>
    <automated>bun run test && bun run typecheck && bun run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -l "RollSimulator|AdHocPicker|AttributionWaterfall|GreekStrips|PnlHeatmap|LevelBar|parseTosOrder|rollScenario" apps/web/src` returns ONLY historical/no matches (no live import of a deleted symbol) — verify no dangling imports.
    - `bun run test` full suite GREEN; `bun run typecheck` + `bun run lint` clean.
    - `git diff apps/web/src/screens/Overview.tsx` shows no behavioral change; the KEEP engine symbols (repriceScenario/AnalyzerPosition/bookPL/buildScenarioStrip/pairPositionsIntoCalendars) still export.
  </acceptance_criteria>
  <done>The dead position-analyzer surface is gone, the suite is green, and Overview + the KEEP engine are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pickerSnapshotFixture (term structure / events / GEX + candidate) → why-panel + term-structure render | Already-parsed typed fixture crosses into the reasoning UI; guard-case must render safely |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-10 | Tampering / DoS | guard-case why-panel + term-structure (fwdIv null) | medium | mitigate | Fwd IV stat `—`, dedicated guard sentence, omitted bracket + `guard` tag; component tests assert no throw/NaN/fabricated bracket |
| T-18-11 | Tampering (integrity) | orphan deletions touching shared engine | medium | mitigate | delete-if-orphaned only (grep-verified callers), KEEP symbols untouched, full suite + Overview diff gate proves no regression |

V5 boundary is enforced upstream at 18-01's `.parse()`. V2/V3/V4/V6 N/A. No package installs, no
DB-schema file.
</threat_model>

<verification>
- `bunx vitest run apps/web/src/components/picker/WhyPanel.test.tsx apps/web/src/components/picker/EntryExitPlan.test.tsx apps/web/src/components/picker/TermStructureChart.test.tsx` GREEN.
- `bunx vitest run apps/web/src/screens/Analyzer.test.tsx` GREEN (right-column + guard integration).
- `bun run test && bun run typecheck && bun run lint` GREEN; Overview.tsx behaviorally unchanged; no dangling imports of deleted symbols.
- Manual (phase gate): `/analyzer` right column reads like `mockups/playground-v4.html` variant B.
</verification>

<success_criteria>
The picker's why-panel, term-structure mini-chart, and entry/exit plan card render per candidate (ANLZ-03)
with a safe guard branch, and the old position-analyzer machinery is retired with the full suite green and
Overview + the KEEP engine intact.
</success_criteria>

<output>
Create `.planning/phases/18-analyzer-picker-ui-redesign/18-05-SUMMARY.md` when done.
</output>
