---
phase: 32-rule-settings-modal-v2-explain-what-you-touch-per-knob-help
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/rule-explainers.ts
  - packages/contracts/src/rule-explainers.test.ts
  - packages/contracts/src/rule-preview.ts
  - packages/contracts/src/rule-preview.test.ts
  - packages/contracts/src/rule-settings.ts
  - packages/contracts/src/index.ts
autonomous: true
requirements: [B6, B4, B5, B7]
must_haves:
  truths:
    - "Every knob path in the REAL ruleConfig schema has exactly one explainer registry entry (no gaps, no extras)"
    - "The explainer completeness test derives its path list from the imported ruleConfig Zod schema, never a hand-copied literal array"
    - "previewRuleOverridesRequest is the same ruleOverrides-shaped partial the PUT accepts (same bounds + refinements, strict)"
    - "previewRuleOverridesResponse carries per-candidate oldScore + newScore inline (client diffs), gate before/after, sizing before/after, universe honest-note, exits current/staged, and snapshot asOf staleness"
  artifacts:
    - packages/contracts/src/rule-explainers.ts
    - packages/contracts/src/rule-preview.ts
  key_links:
    - "rule-explainers keys === dotted paths walked from ruleConfig.shape (exported by this plan)"
    - "previewRuleOverridesRequest === ruleOverrides (reused, not a looser preview-only schema)"
---

<objective>
Ship the two contract-package foundations Phase 32 builds on: the typed per-knob explainer
registry (B6 — single source of truth for what each knob does), and the preview request/response
Zod contract (B4/B5/B7 — the shared wire schema the HTTP route and its MCP twin both parse
through so they can never drift).

Purpose: The registry is the ONE place per-knob help copy lives (Hemingway, trader-facing),
keyed by the contract's own knob paths — no scattered inline strings. The preview contract fixes
the wire shape once so picker/exit preview deltas travel identically over HTTP and MCP.

Output: `rule-explainers.ts` (+ completeness test), `rule-preview.ts` (+ parse tests), a
one-line `ruleConfig` schema export from `rule-settings.ts`, and barrel exports.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-rule-settings-modal-v2-explain-what-you-touch-per-knob-help/32-CONTEXT.md
@.planning/phases/32-rule-settings-modal-v2-explain-what-you-touch-per-knob-help/32-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Explainer registry keyed by real knob paths + schema-derived completeness test</name>
  <files>packages/contracts/src/rule-explainers.ts, packages/contracts/src/rule-explainers.test.ts, packages/contracts/src/rule-settings.ts</files>
  <read_first>
    - packages/contracts/src/rule-settings.ts — the full knob whitelist; note `ruleConfig`
      (line ~266) is a module-local const and must be EXPORTED by this task so the test can walk
      `ruleConfig.shape`. Also note `pickerConfig`/`exitsConfig`/`regimeConfig` sub-shapes and the
      nested `weights`/`vixLadder`/`sizingContracts`/`take`/`stop` objects.
    - packages/core/src/picker/domain/rules.ts — RULE_SET_METADATA rationale prose (reuse where
      good for the score-weight explainers) + debitFitFraction band semantics.
    - packages/core/src/picker/domain/candidate-selection.ts — delta band + DTE-window meaning
      (deltaBandMin/Max, frontDteMin/Max, backDteMinGap/Max) for the "affects Picker universe" copy.
    - packages/core/src/picker/domain/entry-gate.ts (VIX ladder), sizing.ts (tiers), brakes.ts
      (maxOpenCalendars) — gate/sizing knob meaning.
    - packages/core/src/exits/domain/exit-rules.ts — EXIT_RULE_METADATA + TAKE/STOP rung arm/disarm
      semantics for the exits explainers.
    - packages/core/src/analytics/domain/regime.ts — the four warn/crisis band rationales.
    - 32-CONTEXT.md "Specific Ideas" — the two locked copy-tone examples (deltaBandMax, takePlus15Arm).
  </read_first>
  <behavior>
    - Export `ruleConfig` (the existing Zod schema) from rule-settings.ts (one-line `export`).
    - `RULE_EXPLAINERS` is a typed record keyed by the DOTTED knob path INCLUDING the group prefix
      (e.g. "picker.weights.slope", "picker.deltaBandMax", "exits.take.plus15Arm", "regime.vvixWarn").
    - Each entry: { summary (one plain sentence: what it gates/scores), unit, direction (direction-of-effect
      sentence), affects ∈ "Picker candidates" | "Exit verdicts" | "Regime board" }.
    - Completeness test: recursively walk `ruleConfig.shape` (ZodObject → recurse keys; ZodNumber →
      leaf path) to ENUMERATE every dotted leaf path from the real schema, then assert
      RULE_EXPLAINERS has an entry for each AND no extra keys. RED first: the test fails until every
      knob has copy. This is the Pitfall-4 guard — the path list is DERIVED from the imported schema,
      never a hand-written array.
    - `affects` value is correct per group: picker.* → "Picker candidates", exits.* → "Exit verdicts",
      regime.* → "Regime board".
  </behavior>
  <action>
    Export the `ruleConfig` Zod schema from `packages/contracts/src/rule-settings.ts` (change
    `const ruleConfig` to `export const ruleConfig`). Create `RULE_EXPLAINERS` in a new
    `rule-explainers.ts`: a `Readonly<Record<string, RuleExplainer>>` where `RuleExplainer` =
    `{ summary; unit; direction; affects }`. Write trader-facing Hemingway copy for all ~20 knob
    paths, reusing RULE_SET_METADATA / EXIT_RULE_METADATA rationale prose where it reads well; match
    the two locked tone examples in 32-CONTEXT.md exactly. Write the completeness test to walk
    `ruleConfig.shape` recursively into dotted paths and assert bijection with `RULE_EXPLAINERS`
    keys. Do NOT hand-list the paths in the test.
  </action>
  <verify>
    <automated>bun run test -- packages/contracts/src/rule-explainers.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: `rule-settings.ts` exports `ruleConfig`; `rule-explainers.ts` exports `RULE_EXPLAINERS` + `RuleExplainer` type.
    - behavior: completeness test enumerates paths from `ruleConfig.shape` (no `const ...PATHS = [...]` literal in the test) and passes with a 1:1 registry↔schema mapping.
    - behavior: adding a knob to `ruleConfig` without a registry entry fails the test (prove by a scratch edit reverted, or by reasoning documented in SUMMARY).
    - test-command: `bun run test -- packages/contracts/src/rule-explainers.test.ts` green.
  </acceptance_criteria>
  <done>Registry has an entry for every real knob path, verified by a schema-derived completeness test; ruleConfig schema exported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preview request/response Zod contract + barrel exports</name>
  <files>packages/contracts/src/rule-preview.ts, packages/contracts/src/rule-preview.test.ts, packages/contracts/src/index.ts</files>
  <read_first>
    - packages/contracts/src/rule-settings.ts — reuse `ruleOverrides` verbatim for the request
      (same bounds + weight-sum-100 + hysteresis refinements); note `getRuleSettingsResponse` shape.
    - packages/contracts/src/index.ts — the existing barrel export block for rule-settings (lines
      ~179/186) + analyze exports; mirror that grouping for the two new files.
    - 32-RESEARCH.md Open Question 2 (resolved) — response returns the full re-scored candidate list
      with old score inline; client computes diffs.
    - packages/core/src/picker/application/ports.ts (PickerCandidateDomain / PickerGate / PickerSizing
      shapes) + the existing `pickerCandidate` contract schema — the response's picker candidate shape
      reuses that plus an `oldScore`.
    - packages/core/src/exits/domain/types.ts (ExitVerdictKind, ExitMetric) — the exits branch shape.
  </read_first>
  <behavior>
    - `previewRuleOverridesRequest` === `ruleOverrides` (reuse the exact schema — the same partial,
      strict, per-group-nullable body the PUT accepts). Never a looser preview-only schema.
    - `previewRuleOverridesResponse` (`.strict()` at every level):
        - `asOf: string | null` — the stored snapshot staleness marker.
        - `picker: null | { candidates: Array<pickerCandidate-with-oldScore>, gate: {before,after}|null,
          sizing: {before,after}|null, universeNote: string | null }` — universeNote is the honest
          "affects next compute cycle" text present only when band/DTE knobs are staged.
        - `exits: null | Array<{ calendarId, current: {verdict,rung,ruleId}, staged: {verdict,rung,ruleId,metric} }>`.
      (regime preview is client-side — NOT part of this server response.)
    - Round-trip parse tests: a representative response parses; a body with an unknown key fails
      `.strict()`; an empty-groups request (`{}`) parses.
  </behavior>
  <action>
    Create `rule-preview.ts` exporting `previewRuleOverridesRequest` (= `ruleOverrides`) and
    `previewRuleOverridesResponse` per the behavior shape, plus their inferred types. Reuse the
    existing `pickerCandidate` schema extended with `oldScore: z.number()` for the picker candidate
    entries; reuse the exit verdict enum/metric shapes. Barrel-export both schemas + types (and the
    `rule-explainers` registry + type from Task 1) through `packages/contracts/src/index.ts`, grouped
    beside the existing rule-settings exports. Write parse tests proving strict rejection of unknown
    keys and acceptance of the empty-groups request.
  </action>
  <verify>
    <automated>bun run test -- packages/contracts/src/rule-preview.test.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - source: `previewRuleOverridesRequest` is literally `ruleOverrides` (identity reuse, not a copy).
    - source: `previewRuleOverridesResponse` picker candidate entries carry both `oldScore` and `score` (newScore); response is `.strict()`.
    - behavior: unknown-key body rejected; `{}` request accepted; representative response round-trips.
    - test-command: `bun run test -- packages/contracts/src/rule-preview.test.ts` green + `bun run typecheck` clean.
  </acceptance_criteria>
  <done>Preview request reuses ruleOverrides; response fixes the picker/exits delta wire shape with oldScore inline; both barrel-exported.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API (contract layer) | staged override values cross into the preview request; validated here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-05 | Tampering/Input Validation | previewRuleOverridesRequest | high | mitigate | Reuse `ruleOverrides` verbatim — same `.strict()` + weight-sum-100 + hysteresis-pair refinements; never a looser preview schema |
| T-32-06 | Information Disclosure | previewRuleOverridesResponse | low | mitigate | Response schema carries only computed deltas + asOf; no storage/DB internals modeled |
| T-32-SC | Tampering | npm installs | low | accept | Zero new dependencies (contracts-only); no install task |
</threat_model>

<verification>
- `bun run test -- packages/contracts/src/rule-explainers.test.ts packages/contracts/src/rule-preview.test.ts` green
- `bun run typecheck` clean; `bun run lint` clean
- Completeness test provably fails if a knob lacks an explainer (schema-derived path list)
</verification>

<success_criteria>
- Registry covers 100% of ruleConfig knob paths, enforced by a schema-derived test (no hand list)
- Preview request === ruleOverrides; response carries oldScore inline + gate/sizing before-after + universe note + exits current/staged + asOf
- All four new/edited contract exports reachable from `@morai/contracts`
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 01)
- `packages/contracts/src/rule-explainers.ts` — typed per-knob explainer registry (B6 single source of truth)
- `packages/contracts/src/rule-preview.ts` — preview request/response Zod contract (B4/B5/B7 shared wire schema)
- `ruleConfig` Zod schema now exported from `rule-settings.ts` (enables schema-derived completeness test)
- barrel exports through `packages/contracts/src/index.ts`
</artifacts>

<output>
Create `.planning/phases/32-rule-settings-modal-v2-explain-what-you-touch-per-knob-help/32-01-SUMMARY.md` when done
</output>
