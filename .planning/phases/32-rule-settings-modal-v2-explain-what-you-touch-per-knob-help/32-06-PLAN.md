---
phase: 32-rule-settings-modal-v2-explain-what-you-touch-per-knob-help
plan: 06
type: execute
wave: 3
depends_on: [01, 05]
files_modified:
  - apps/web/src/hooks/useRuleSettingsPreview.ts
  - apps/web/src/hooks/useRuleSettingsPreview.test.ts
  - apps/web/src/screens/RuleSettingsModal.tsx
  - apps/web/src/screens/RuleSettingsModal.test.tsx
autonomous: true
requirements: [B1, B2, B3, B5, B7]
must_haves:
  truths:
    - "An explicit Preview button (no keystroke spam) sends the staged group(s) and renders the returned deltas: picker top-movers (old→new score), gate before→after chip, sizing before→after, universe honest-note; exits current→staged per calendar"
    - "Regime preview is computed client-side by importing bandVixTermStructure/bandVvix/bandVix9dRatio/bandHyOas from @morai/core and re-banding the on-screen indicator values with the staged thresholds — no duplicated band logic, no server round-trip"
    - "Preview staleness (snapshot asOf) and loading/error states are surfaced honestly"
    - "Empty staged preview shows zero score movement (byte-parity surfaces to the user as 'no change')"
  artifacts:
    - apps/web/src/hooks/useRuleSettingsPreview.ts
  key_links:
    - "useRuleSettingsPreview mirrors useAnalyzeCalendar's mutation shape; parses previewRuleOverridesResponse"
    - "regime re-band imports the ACTUAL core band functions (Phase 17-01 precedent, legal per eslint boundaries) — parity by construction"
---

<objective>
Ship the staged-change impact preview (B1/B2/B3/B5/B7) — the half of the user's complaint that
matters most ("it doesn't tell me how things will change if I move things"). An explicit Preview
button dry-runs the staged overrides: picker + exit deltas come from `POST /api/settings/rules/preview`
(Plan 04) via a new mutation hook; regime deltas are computed client-side by importing the core band
functions directly (legal — Phase 17-01 precedent) and re-banding the on-screen indicator values.

Purpose: This completes modal v2. Preview happens BEFORE save, on explicit click, and shows honest
deltas (or an honest "affects next cycle" note for universe knobs).

Output: `useRuleSettingsPreview.ts` hook + the Preview button + delta rendering + regime client-side
re-band + loading/error/staleness states in the modal.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-rule-settings-modal-v2-explain-what-you-touch-per-knob-help/32-CONTEXT.md
@.planning/phases/32-rule-settings-modal-v2-explain-what-you-touch-per-knob-help/32-RESEARCH.md
@packages/contracts/src/rule-preview.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useRuleSettingsPreview mutation hook + client-side regime re-band helper</name>
  <files>apps/web/src/hooks/useRuleSettingsPreview.ts, apps/web/src/hooks/useRuleSettingsPreview.test.ts</files>
  <read_first>
    - apps/web/src/hooks/useAnalyzeCalendar.ts — the non-optimistic useMutation + apiFetch + Zod-parse
      shape to mirror exactly (POST, parse the response contract, throw only on genuine HTTP failure).
    - apps/web/src/hooks/useRuleSettings.ts — the existing settings query/mutation conventions +
      how apiFetch and the contract parse are used; the test harness (apiFetch mock, per 30-06 decision
      apps/web has NO msw — mirror useRuleSettings.test.ts's apiFetch-mock harness).
    - apps/web/src/hooks/useRegimeBoard.ts — GET /api/analytics/regime → regimeResponse array of
      { id, label, value, band, asOf } (the on-screen indicator values the regime re-band consumes).
    - packages/contracts/src/rule-preview.ts — previewRuleOverridesRequest/Response.
    - packages/core/src/analytics/domain/regime.ts — bandVixTermStructure(ratio,{warn,crisis}),
      bandVvix(level,...), bandVix9dRatio(ratio,...), bandHyOas(percent,...). Map each regime indicator
      id to its band function + its staged {warn,crisis} field pair (regime.vixTermStructureWarn/Crisis,
      vvixWarn/Crisis, vix9dRatioWarn/Crisis, hyOasWarn/Crisis).
    - eslint.config.js — confirm the `apps → core` allow rule (regime import is legal; the CLAUDE.md
      "web imports contracts only" line is stale per RESEARCH Finding 3 / Pitfall 2).
  </read_first>
  <behavior>
    - `useRuleSettingsPreview()` returns a mutation posting the staged ruleOverrides-shaped body to
      `/api/settings/rules/preview`, parsing `previewRuleOverridesResponse`; throws only on a real
      HTTP/network failure (mirrors useAnalyzeCalendar).
    - A pure `previewRegimeBands(stagedRegime, indicators)` helper: for each on-screen regime indicator,
      call the matching core band function with the staged {warn,crisis} thresholds → returns
      { id, before: indicator.band, after: rebandedBand }. Imports the ACTUAL core functions (no copy).
    - Parity test: for each of the 4 indicators, `previewRegimeBands` with a given staged threshold
      returns exactly `band<X>(value, {warn,crisis})` from @morai/core (no divergence).
  </behavior>
  <action>
    Create `useRuleSettingsPreview.ts` with the mutation hook (mirror useAnalyzeCalendar) plus the pure
    `previewRegimeBands` helper importing the four band functions from `@morai/core`. Write tests using
    the apiFetch-mock harness: the mutation posts + parses the response; the regime helper matches the
    core band functions for staged thresholds across all four indicators (parity), and re-bands to the
    stored band when staged === defaults.
  </action>
  <verify>
    <automated>bun run test -- apps/web/src/hooks/useRuleSettingsPreview.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: hook uses apiFetch + `previewRuleOverridesResponse.parse`; regime helper imports the four functions from `@morai/core` (no local band if-ladder).
    - behavior: mutation posts/parses correctly; `previewRegimeBands` output === core band functions for staged thresholds (parity) across all 4 indicators.
    - test-command: `bun run test -- apps/web/src/hooks/useRuleSettingsPreview.test.ts` green.
  </acceptance_criteria>
  <done>Preview hook posts staged overrides + parses deltas; regime re-band reuses the core band functions client-side (parity).</done>
</task>

<task type="auto">
  <name>Task 2: Preview button + delta rendering + loading/error/staleness in the modal</name>
  <files>apps/web/src/screens/RuleSettingsModal.tsx, apps/web/src/screens/RuleSettingsModal.test.tsx</files>
  <read_first>
    - apps/web/src/screens/RuleSettingsModal.tsx — the GroupPanel draft state (per-group `draft` map)
      the Preview button reads to build the staged group body; keep Save/Reset/captions (Plan 05) intact.
    - apps/web/src/hooks/useRuleSettingsPreview.ts (Task 1) + apps/web/src/hooks/useRegimeBoard.ts.
    - packages/contracts/src/rule-preview.ts — the response branches (picker candidates with
      oldScore/newScore, gate before/after, sizing before/after, universeNote; exits current/staged; asOf).
    - 32-CONTEXT.md "Claude's Discretion" — delta presentation (top-movers table, count chips, gate
      before→after chip) + loading/error/staleness note are the planner/executor's call; keep it compact
      (the modal is long).
  </read_first>
  <behavior>
    - Each group panel gets a "Preview" button (explicit click only — no keystroke spam). On click:
        - picker/exits groups → call the mutation with the staged group; render the returned deltas:
          picker = top movers by |newScore − oldScore| (old→new), a gate before→after chip when gate
          state changes, sizing before→after, and the universeNote verbatim when present; exits =
          per-calendar current→staged verdict/rung.
        - regime group → call `previewRegimeBands` with the staged thresholds + `useRegimeBoard` values;
          render each indicator's band before→after.
    - Empty/unchanged staged group → the preview surfaces "no change" (byte-parity: newScore === oldScore).
    - Loading spinner while the mutation is pending; an error line on HTTP failure; a staleness note
      showing the snapshot `asOf` for the picker deltas.
    - Save flow (Plan 05 / v1) unchanged — preview never persists and never auto-saves.
  </behavior>
  <action>
    Add a Preview button per group + a delta panel rendering the picker/exits/regime results per the
    behavior. Wire picker/exits to `useRuleSettingsPreview`, regime to `previewRegimeBands` +
    `useRegimeBoard`. Surface loading/error/asOf-staleness. Add component tests: staging a weight →
    Preview → a movers list renders with old→new; staging a universe knob → the honest note renders (no
    fake movers); staging a regime threshold → band before→after renders; an empty staged group → "no
    change"; the Save button still works unchanged.
  </action>
  <verify>
    <automated>bun run test -- apps/web/src/screens/RuleSettingsModal.test.tsx && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - source: Preview is an explicit button (no onChange-triggered preview); picker/exits use the hook, regime uses `previewRegimeBands`.
    - behavior: weight stage → movers old→new; universe stage → honest note only; regime stage → band before→after; empty stage → "no change"; asOf staleness shown; Save unchanged.
    - test-command: `bun run test -- apps/web/src/screens/RuleSettingsModal.test.tsx` green + `bun run typecheck` clean.
  </acceptance_criteria>
  <done>Explicit Preview shows honest picker/exit/regime deltas before save, with loading/error/staleness; Save unregressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| modal→preview API | staged draft values are posted to the authenticated preview endpoint |
| core band fns→client | pure core functions imported into the browser bundle (Phase 17-01 precedent) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-12 | Information Disclosure (silent-wrong) | regime client re-band | high | mitigate | Import the ACTUAL core band functions (no hand-copy) so client and prod band identically — the CONTEXT-mandated "hand-copies rot" mitigation; parity test enforces it |
| T-32-13 | Tampering | preview render | medium | mitigate | Preview is read-only display; never writes; Save is a separate explicit action (preview never auto-saves) |
| T-32-SC | Tampering | npm installs | low | accept | Zero new dependencies (@morai/core already a web dependency); no install task |
</threat_model>

<verification>
- `bun run test -- apps/web/src/hooks/useRuleSettingsPreview.test.ts apps/web/src/screens/RuleSettingsModal.test.tsx` green
- `bun run typecheck` + `bun run lint` clean (eslint boundary allows the @morai/core regime import)
- Manual UAT: on morai.wtf stage a weight swap → Preview → sensible movers; stage a universe knob → honest note; stage a regime band → band shifts
</verification>

<success_criteria>
- Explicit Preview shows picker top-movers (old→new) + gate/sizing before-after + universe honest-note
- Exit current→staged per open calendar
- Regime band before→after computed client-side via the real core functions (parity)
- Staleness/loading/error surfaced; empty stage = "no change"; Save unregressed
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 06)
- `apps/web/src/hooks/useRuleSettingsPreview.ts` — preview mutation hook + client-side regime re-band helper (B3)
- `RuleSettingsModal.tsx` Preview button + picker/exit/regime delta rendering + loading/error/staleness (B1/B2/B5/B7)
</artifacts>

<output>
Create `.planning/phases/32-rule-settings-modal-v2-explain-what-you-touch-per-knob-help/32-06-SUMMARY.md` when done
</output>
