---
phase: 32-rule-settings-modal-v2-explain-what-you-touch-per-knob-help
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/exits/application/previewExitRuleOverrides.ts
  - packages/core/src/exits/application/previewExitRuleOverrides.test.ts
  - packages/core/src/exits/application/ports.ts
  - packages/core/src/exits/index.ts
autonomous: true
requirements: [B2, B7]
must_haves:
  truths:
    - "Every open position gets a current-vs-staged verdict pair by running the pure evaluateExit twice (current effective config, staged config)"
    - "Empty staged exit overrides reproduce the current verdict for every open position EXACTLY (byte-parity property)"
    - "The exit preview use-case deps type structurally excludes persistExitVerdict (read-only, never persists)"
    - "The live exit re-eval is built (B2 — proportionate per RESEARCH Pattern 3), not the static-note fallback"
  artifacts:
    - packages/core/src/exits/application/previewExitRuleOverrides.ts
  key_links:
    - "evaluateExit(position, context, previousVerdict, resolveExitRuleConfig(stagedOverrides)) — the same pure evaluator computeExitAdvice uses, config as 4th arg"
    - "context built from the SAME reads computeExitAdvice uses, rollChain.candidates = [] (rung changes don't drive ROLL)"
---

<objective>
Build the exit preview use-case (B2) — the live current-vs-staged re-evaluation of every open
calendar's TAKE/STOP verdict against staged rung overrides. RESEARCH Pattern 3 / Pitfall 3 proved
this is PROPORTIONATE and cheap (only 2 overridable groups, pure evaluateExit, ports already
composed server-side, open-calendar count is single-digit) — so we build the live preview, not
CONTEXT's static-note hedge.

Purpose: This is the compute core of the exits branch of `POST /api/settings/rules/preview`. It is
a read-only fork of `computeExitAdvice` minus persist and minus chain-for-roll: it loops open
positions, evaluates each with the current config and the staged config, and returns the diff.

Output: `previewExitRuleOverrides.ts` (+ tests incl. a byte-parity fast-check property).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-rule-settings-modal-v2-explain-what-you-touch-per-knob-help/32-CONTEXT.md
@.planning/phases/32-rule-settings-modal-v2-explain-what-you-touch-per-knob-help/32-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Exit preview use-case — evaluateExit twice per open position, structural no-persist</name>
  <files>packages/core/src/exits/application/previewExitRuleOverrides.ts, packages/core/src/exits/application/previewExitRuleOverrides.test.ts, packages/core/src/exits/application/ports.ts</files>
  <read_first>
    - packages/core/src/exits/application/computeExitAdvice.ts — the read order + loop body to fork:
      readHeldPositions → readLatestSnapshotPerOpenCalendar → readLatestVerdictsPerCalendar →
      readEconomicEvents → per-position build MarketContext → evaluateExit. Copy the CONTEXT
      construction (netMark/pnlOpen/spot/frontIv/backIv/dteFront/dteBack/snapshotTime/cohortNow/
      marketSession/tier1Events/rollChain). Also copy `isExitRuleOverrides` (narrows the stored
      `exits` group) — REUSE it, do not re-author. NOTE: the preview differs in exactly three ways:
      (1) config comes from staged overrides, (2) NO persistExitVerdict, (3) it also computes the
      CURRENT verdict for the diff.
    - packages/core/src/exits/domain/evaluate-exit.ts evaluateExit signature (line 232):
      (position, context, previousVerdict, config = resolveExitRuleConfig()).
    - packages/core/src/exits/domain/rule-config.ts — resolveExitRuleConfig(overrides?) + ExitRuleOverrides.
    - packages/core/src/exits/domain/types.ts — MarketContext, ExitVerdict (verdict/rung/ruleId/metric/
      indicative/escalate), PreviousVerdict.
    - packages/core/src/exits/application/ports.ts — ForReadingHeldPositions,
      ForReadingLatestSnapshotPerOpenCalendar, ForReadingLatestVerdictsPerCalendar,
      ForReadingEconomicEvents, StorageError. (Do NOT include ForPersistingExitVerdict.)
    - packages/core/src/settings/application/ports.ts — ForReadingRuleOverrides (for the CURRENT
      effective exit config when the request omits the exits group).
    - 32-RESEARCH.md Pattern 3 + Pitfall 3 (why live, not the static note) + Pitfall 5 (structural
      no-persist, not just unused-at-runtime).
  </read_first>
  <behavior>
    - `ExitPreviewDeps` = { readHeldPositions, readLatestSnapshotPerOpenCalendar,
      readLatestVerdictsPerCalendar, readEconomicEvents, readRuleOverrides, now }. NO
      persistExitVerdict, NO readChainForRoll (structural exclusion — Pitfall 5).
    - Input: the staged exits override group (or absent). currentExitConfig =
      resolveExitRuleConfig(isExitRuleOverrides(storedExits) ? storedExits : undefined);
      stagedExitConfig = resolveExitRuleConfig(staged ?? storedExits). An ABSENT staged group makes
      staged === current.
    - Per open position (same read joins as computeExitAdvice): build MarketContext with
      rollChain.candidates = [] (a TAKE/STOP rung change never affects a ROLL suggestion, and
      omitting the chain read keeps the preview bounded + read-only). previousVerdict = the stored
      latest verdict for that calendar. Compute current = evaluateExit(pos, ctx, prev, currentExitConfig)
      and staged = evaluateExit(pos, ctx, prev, stagedExitConfig). Return
      { calendarId, current: {verdict,rung,ruleId}, staged: {verdict,rung,ruleId,metric} }.
    - Skip a position with no snapshot this cohort (same safe-skip as computeExitAdvice).
    - Never persists; returns Result<ExitPreviewResult, StorageError>.
  </behavior>
  <action>
    Create `previewExitRuleOverrides.ts` with `makePreviewExitRuleOverridesUseCase(deps)` returning
    a `Result<ExitPreviewResult, StorageError>` driver port. Fork computeExitAdvice's read order +
    context construction, reusing `isExitRuleOverrides`, `resolveExitRuleConfig`, and `evaluateExit`
    verbatim; drop the persist and chain-for-roll dependencies entirely (structural). Compute both
    the current and staged verdict per position and emit the diff. Add `ExitPreviewResult` +
    `ExitPreviewDeps` + `ForPreviewingExitRuleOverrides` to exits/application/ports.ts. Write example
    tests (a rung change flips a verdict; an AH/stale position stays indicative) plus a fast-check
    byte-parity property: an ABSENT/empty staged exits group ⇒ staged verdict === current verdict for
    every position.
  </action>
  <verify>
    <automated>bun run test -- packages/core/src/exits/application/previewExitRuleOverrides.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: `previewExitRuleOverrides.ts` deps type has NO `persistExitVerdict` and NO `readChainForRoll` (grep the file).
    - source: it imports `evaluateExit` + `resolveExitRuleConfig` + `isExitRuleOverrides` — no re-authored rung/evaluator logic.
    - behavior: byte-parity property — empty staged exits ⇒ staged === current for every open position.
    - behavior: a staged rung change (e.g. plus10Arm) flips the previewed verdict where the metric crosses the new arm; an AH/stale snapshot stays `indicative`.
    - test-command: `bun run test -- packages/core/src/exits/application/previewExitRuleOverrides.test.ts` green.
  </acceptance_criteria>
  <done>Exit preview re-evaluates open-position verdicts current-vs-staged via the pure evaluateExit, byte-parity on empty, structurally never persists.</done>
</task>

<task type="auto">
  <name>Task 2: Barrel-export the exit preview use-case + types through the context barrel</name>
  <files>packages/core/src/exits/index.ts</files>
  <read_first>
    - packages/core/src/exits/index.ts — how makeComputeExitAdviceUseCase / makeGetExitAdviceUseCase
      + their deps types are re-exported; mirror that grouping.
    - packages/core/src/index.ts (top barrel) — NOTE: the top-level re-export is OWNED by Plan 04
      (avoids a Wave-1 barrel edit conflict with Plan 02). Do NOT edit the top barrel here.
  </read_first>
  <action>
    Re-export `makePreviewExitRuleOverridesUseCase`, `ForPreviewingExitRuleOverrides`,
    `ExitPreviewResult`, and `ExitPreviewDeps` from `packages/core/src/exits/index.ts`, grouped
    beside the existing exit use-case exports. Leave the top-level `packages/core/src/index.ts`
    untouched (Plan 04 owns it).
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - source: `exits/index.ts` re-exports the preview use-case + its three types.
    - behavior: the top-level `packages/core/src/index.ts` is NOT modified by this plan.
    - test-command: `bun run typecheck` clean.
  </acceptance_criteria>
  <done>Exit preview use-case reachable via the exits context barrel; top barrel deferred to Plan 04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staged rungs → verdict re-eval | staged config drives a live re-evaluation of open-position verdicts; must never persist |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-02 | Tampering | previewExitRuleOverrides deps | high | mitigate | Deps type structurally excludes persistExitVerdict (Pitfall 5) — a copy-pasted persist call from computeExitAdvice would fail to typecheck |
| T-32-08 | Repudiation/Correctness | AH/stale gating | medium | mitigate | evaluateExit's own session/staleness gate is reused unchanged — previewed verdicts on AH/gap marks stay `indicative`, never actionable |
| T-32-SC | Tampering | npm installs | low | accept | Zero new dependencies; no install task |
</threat_model>

<verification>
- `bun run test -- packages/core/src/exits/application/previewExitRuleOverrides.test.ts` green (incl. fast-check byte-parity)
- `bun run typecheck` clean; `bun run lint` clean
- grep confirms no persistExitVerdict / readChainForRoll in the deps type
</verification>

<success_criteria>
- Live current-vs-staged re-eval built (B2), not the static-note fallback
- Byte-parity: empty staged ⇒ staged === current
- Structurally read-only (no persist, no chain read)
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 03)
- `packages/core/src/exits/application/previewExitRuleOverrides.ts` — exit dry-run preview use-case (B2)
- `ExitPreviewResult` / `ExitPreviewDeps` / `ForPreviewingExitRuleOverrides` types + exits-barrel export
</artifacts>

<output>
Create `.planning/phases/32-rule-settings-modal-v2-explain-what-you-touch-per-knob-help/32-03-SUMMARY.md` when done
</output>
