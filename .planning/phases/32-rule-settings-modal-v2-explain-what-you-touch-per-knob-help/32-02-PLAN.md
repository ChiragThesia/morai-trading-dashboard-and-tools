---
phase: 32-rule-settings-modal-v2-explain-what-you-touch-per-knob-help
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/picker/application/previewPickerRuleOverrides.ts
  - packages/core/src/picker/application/previewPickerRuleOverrides.test.ts
  - packages/core/src/picker/application/computePickerSnapshot.ts
  - packages/core/src/picker/application/ports.ts
  - packages/core/src/picker/index.ts
autonomous: true
requirements: [B1, B7]
must_haves:
  truths:
    - "Score-only knobs (9 weights + debitIdealMin/Max) re-score the stored candidates with zero I/O beyond the snapshot read"
    - "Empty staged picker overrides reproduce every stored candidate's score EXACTLY (byte-parity property)"
    - "Gate/sizing knobs (vixLadder, maxOpenCalendars, sizingContracts) re-resolve gate state + sizing tier from stored gate scalars (+ one open-calendars count read for maxOpen)"
    - "Universe knobs (deltaBandMin/Max, frontDteMin/Max, backDteMinGap/Max) return the honest 'affects next compute cycle' note — never a fabricated candidate diff"
    - "The use-case deps type structurally excludes any persist port and any chain read"
  artifacts:
    - packages/core/src/picker/application/previewPickerRuleOverrides.ts
  key_links:
    - "debitFit re-score calls debitFitFraction(candidate.debit, stagedBand) — never a re-derived formula copy"
    - "gate re-resolve reuses exported toEntryGateState/toPickerGate + resolveEntryGate/resolveSizingTier — never a hand-rolled tier ladder"
---

<objective>
Build the picker preview use-case (B1) — the server-side dry-run that re-scores the LATEST stored
snapshot cohort against staged picker overrides and reports the deltas, honestly, per knob group:
score-only knobs re-weight stored contributions; gate/sizing knobs re-resolve from stored scalars;
universe-membership knobs get an honest note (never a faked diff — Pitfall 1).

Purpose: This is the compute core of the picker branch of `POST /api/settings/rules/preview`.
It follows the Phase-30 `analyzeAdHocCalendar` precedent exactly: bounded reads, never recompute
the chain, never persist (T-19-17), structural exclusion of persist/chain ports.

Output: `previewPickerRuleOverrides.ts` (+ tests incl. a byte-parity fast-check property) and the
`toEntryGateState`/`toPickerGate` exports the gate re-resolve reuses.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-rule-settings-modal-v2-explain-what-you-touch-per-knob-help/32-CONTEXT.md
@.planning/phases/32-rule-settings-modal-v2-explain-what-you-touch-per-knob-help/32-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Export gate/sizing helpers + build score-only + gate/sizing + universe-note branches</name>
  <files>packages/core/src/picker/application/previewPickerRuleOverrides.ts, packages/core/src/picker/application/previewPickerRuleOverrides.test.ts, packages/core/src/picker/application/computePickerSnapshot.ts, packages/core/src/picker/application/ports.ts</files>
  <read_first>
    - packages/core/src/picker/application/analyzeAdHocCalendar.ts — the bounded-read, never-persist,
      structural-exclusion precedent to mirror (deps carry no persist/chain port; overrides resolved
      fresh per call). Copy its shape, not its scoring path.
    - packages/core/src/picker/application/computePickerSnapshot.ts — `toEntryGateState` (line ~243)
      and `toPickerGate` (line ~258) are module-private and MUST be exported (30-04 export precedent
      for isPickerRuleOverrides/applyGatePenalty). Also read the maxOpen/cooldown brake wiring
      (~540-589: readOpenCalendars → maxOpenTripped(openCount, config.maxOpenCalendars) → toPickerGate)
      and `isPickerRuleOverrides`.
    - packages/core/src/picker/domain/scoring.ts scoreOne (~231-241) — the rawScore formula
      `Σ weight*fraction`, `score = clamp(round(rawScore))`, and that breakdown[].contribution is
      fraction*100. The re-score reuses stored contributions; only debitFit's contribution is recomputed.
    - packages/core/src/picker/domain/rules.ts debitFitFraction (~217) — reuse it for the staged debit band.
    - packages/core/src/picker/domain/rule-config.ts resolvePickerRuleConfig — turns staged overrides
      into weights/debitBand/vixLadder/sizingContracts/maxOpenCalendars.
    - packages/core/src/picker/domain/entry-gate.ts resolveEntryGate + resolveVixLadder + EntryGateState;
      sizing.ts resolveSizingTier + SizingTierOverride; brakes.ts maxOpenTripped.
    - packages/core/src/picker/application/ports.ts — ForReadingPickerSnapshot, PickerSnapshot (gate
      scalars: gate.vix/vix3m/ratio/asOf/state/penaltyMultiplier/brakes), ForGettingOpenCalendars.
    - packages/core/src/settings/application/ports.ts — ForReadingRuleOverrides (cross-context read).
    - 32-RESEARCH.md Patterns 1 & 2 + Pitfall 1 (universe knobs are NOT re-scorable from stored data).
    - NOTE (Phase 31 overlap): re-read computePickerSnapshot.ts / ports.ts / index.ts at execution
      time — Phase 31 may have touched the picker barrel; do not assume planning-time contents.
  </read_first>
  <behavior>
    - `PickerPreviewDeps` = { readPickerSnapshot, readRuleOverrides, readOpenCalendars } + rate/dividend
      only if genuinely needed (score-only path needs none). NO persist port, NO gex/chain read
      (structural exclusion — enforced by the type, mirrors analyzeAdHocCalendar's T-19-17).
    - Input: the staged picker override group (or absent). Effective picker config for the re-score =
      resolvePickerRuleConfig(staged ?? storedPickerOverrides) so an ABSENT/empty staged group
      reproduces the stored effective config.
    - Score branch: for each stored candidate, newScore = clamp(round( Σ over 9 criteria of
      stagedWeight[criterion] * contribution/100 )), where 8 contributions are the STORED
      breakdown values and debitFit's contribution is recomputed as
      debitFitFraction(candidate.debit, config.debitBand)*100. Return each candidate with oldScore
      (= stored score) and newScore.
    - Gate branch: reconstruct EntryGateState from stored gate scalars (reuse exported
      toEntryGateState against the stored PickerGate), recompute maxOpen brake via
      maxOpenTripped(openCount from readOpenCalendars, config.maxOpenCalendars), REUSE the stored
      cooldown brake verbatim (cooldown is not an editable knob), call resolveEntryGate with the
      staged vixLadder, project via toPickerGate → gate {before (stored), after (re-resolved)}.
      Sizing branch: resolveSizingTier(gate.vix, {ladder, contracts}) → sizing {before, after}.
    - Universe branch: when any of deltaBandMin/Max, frontDteMin/Max, backDteMinGap/Max is staged,
      set universeNote to the honest "affects the next compute cycle — no live candidate re-selection
      without a chain re-read" text; NEVER fabricate an entered/left-universe candidate diff (Pitfall 1).
    - Cold start: no stored snapshot → a clean documented degradation (mirror analyzeAdHocCalendar's
      no-snapshot result), never a throw.
  </behavior>
  <action>
    Export `toEntryGateState` and `toPickerGate` from computePickerSnapshot.ts (add `export`).
    Create `previewPickerRuleOverrides.ts` with `makePreviewPickerRuleOverridesUseCase(deps)`
    returning a `Result<PickerPreviewResult, StorageError>` driver port. Implement the score/gate/
    sizing/universe branches per the behavior. Add the `PickerPreviewResult` + `PickerPreviewDeps` +
    `ForPreviewingPickerRuleOverrides` port types to picker/application/ports.ts. Reuse
    debitFitFraction / resolvePickerRuleConfig / resolveEntryGate / resolveSizingTier / maxOpenTripped
    verbatim — no second scoring or tier formula. Write example tests for each branch plus a
    fast-check byte-parity property: for arbitrary stored candidates, an ABSENT/empty staged picker
    group yields newScore === stored score for every candidate and gate/sizing after === before.
  </action>
  <verify>
    <automated>bun run test -- packages/core/src/picker/application/previewPickerRuleOverrides.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: computePickerSnapshot.ts exports `toEntryGateState` + `toPickerGate`; `previewPickerRuleOverrides.ts` deps type contains NO persist/gex/chain port.
    - source: debitFit re-score calls `debitFitFraction` (grep the file: it imports it) — no inline band formula.
    - behavior: byte-parity property — empty staged overrides ⇒ every candidate newScore === oldScore, gate/sizing after === before.
    - behavior: staging a universe knob (e.g. deltaBandMax) sets `universeNote` and returns NO fabricated candidate in/out diff.
    - behavior: staging maxOpenCalendars flips gate.after.brakes.maxOpen using the fresh open-count read, cooldown reused from stored.
    - test-command: `bun run test -- packages/core/src/picker/application/previewPickerRuleOverrides.test.ts` green.
  </acceptance_criteria>
  <done>Picker preview re-scores stored candidates (byte-parity on empty), re-resolves gate/sizing from stored scalars, and returns an honest universe note — never persisting, never reading the chain.</done>
</task>

<task type="auto">
  <name>Task 2: Barrel-export the picker preview use-case + types through the context barrel</name>
  <files>packages/core/src/picker/index.ts</files>
  <read_first>
    - packages/core/src/picker/index.ts — how makeAnalyzeAdHocCalendarUseCase + its deps/result types
      are re-exported (mirror that grouping). Read at execution time (Phase 31 may have touched it).
    - packages/core/src/index.ts (top barrel) — NOTE: the top-level re-export is OWNED by Plan 04
      (which wires the server), to avoid a Wave-1 barrel edit conflict with Plan 03. Do NOT edit the
      top barrel here.
  </read_first>
  <action>
    Re-export `makePreviewPickerRuleOverridesUseCase`, `ForPreviewingPickerRuleOverrides`,
    `PickerPreviewResult`, and `PickerPreviewDeps` from `packages/core/src/picker/index.ts`, grouped
    beside the existing analyze use-case exports. Leave the top-level `packages/core/src/index.ts`
    untouched (Plan 04 owns it).
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - source: `picker/index.ts` re-exports the preview use-case + its three types.
    - behavior: the top-level `packages/core/src/index.ts` is NOT modified by this plan.
    - test-command: `bun run typecheck` clean.
  </acceptance_criteria>
  <done>Picker preview use-case reachable via the picker context barrel; top barrel deferred to Plan 04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staged overrides → compute | staged config drives a re-score of stored data; must never persist or read the chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-01 | Tampering | previewPickerRuleOverrides deps | high | mitigate | Deps type structurally excludes any persist port and any chain read (mirrors analyzeAdHocCalendar T-19-17); a stray persist import would fail typecheck |
| T-32-07 | Information Disclosure (silent-wrong) | universe-knob branch | high | mitigate | Universe-membership knobs return an honest note, never a fabricated in/out diff (Pitfall 1) — a wrong preview is worse than none |
| T-32-SC | Tampering | npm installs | low | accept | Zero new dependencies; no install task |
</threat_model>

<verification>
- `bun run test -- packages/core/src/picker/application/previewPickerRuleOverrides.test.ts` green (incl. fast-check byte-parity)
- `bun run typecheck` clean; `bun run lint` clean
- grep confirms `debitFitFraction` import present and no persist/chain port in the deps type
</verification>

<success_criteria>
- Score-only re-weight matches scoreOne's formula (byte-parity on empty staged)
- Gate/sizing re-resolve reuses exported helpers + resolve functions (no hand-rolled ladder)
- Universe knobs → honest note, never faked numbers
- No persist, no chain read (structural)
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 02)
- `packages/core/src/picker/application/previewPickerRuleOverrides.ts` — picker dry-run preview use-case (B1)
- `toEntryGateState` / `toPickerGate` exported from computePickerSnapshot.ts (gate re-resolve reuse)
- `PickerPreviewResult` / `PickerPreviewDeps` / `ForPreviewingPickerRuleOverrides` types + picker-barrel export
</artifacts>

<output>
Create `.planning/phases/32-rule-settings-modal-v2-explain-what-you-touch-per-knob-help/32-02-SUMMARY.md` when done
</output>
