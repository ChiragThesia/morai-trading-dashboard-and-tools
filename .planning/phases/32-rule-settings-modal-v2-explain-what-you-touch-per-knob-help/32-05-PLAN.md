---
phase: 32-rule-settings-modal-v2-explain-what-you-touch-per-knob-help
plan: 05
type: execute
wave: 2
depends_on: [01]
files_modified:
  - apps/web/src/screens/RuleSettingsModal.tsx
  - apps/web/src/screens/RuleSettingsModal.test.tsx
autonomous: true
requirements: [B6, B9]
must_haves:
  truths:
    - "Every rendered knob row shows its explainer summary (inline caption) and its affected-surface tag, sourced from RULE_EXPLAINERS keyed by the row's dotted path"
    - "The longer direction-of-effect + unit copy is reachable via an info-icon popover built on the existing Tooltip primitive (its first consumer)"
    - "All Phase-29 v1 modal behaviors (per-group Save, Reset to defaults, group validation error) remain unregressed"
  artifacts:
    - apps/web/src/screens/RuleSettingsModal.tsx
  key_links:
    - "caption/popover key = [group, ...row.path].join('.') — matches the registry's group-prefixed knob paths"
    - "Tooltip primitive at apps/web/src/components/ui/tooltip.tsx is the ONLY popover mechanism (no hand-rolled popover)"
---

<objective>
Give the settings modal its explain-what-you-touch surface (B6): every knob row renders a plain
one-sentence summary and an affected-surface tag (Picker candidates / Exit verdicts / Regime board),
with the longer direction-of-effect + unit copy behind an info-icon popover. All copy comes from the
`RULE_EXPLAINERS` registry (Plan 01) — no inline strings. The Tooltip primitive
(`apps/web/src/components/ui/tooltip.tsx`, B9 — already vendored, currently unused) becomes its first
consumer. No preview yet (Plan 06) and no v1 behavior regressions.

Purpose: This is the first half of the modal v2 — the static help layer the user asked for ("it
doesn't tell me what things do"). It ships independently of the preview flow.

Output: extended `RuleSettingsModal.tsx` rendering captions/popovers/affects tags from the registry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-rule-settings-modal-v2-explain-what-you-touch-per-knob-help/32-CONTEXT.md
@.planning/phases/32-rule-settings-modal-v2-explain-what-you-touch-per-knob-help/32-RESEARCH.md
@packages/contracts/src/rule-explainers.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render per-knob caption + affected-surface tag from the registry</name>
  <files>apps/web/src/screens/RuleSettingsModal.tsx, apps/web/src/screens/RuleSettingsModal.test.tsx</files>
  <read_first>
    - apps/web/src/screens/RuleSettingsModal.tsx — the existing GroupPanel row render (flattenNumeric →
      row.path → label + number input + "default N"). The registry key for a row is
      `[group, ...row.path].join(".")`. Keep flatten/lookup/unflatten and the v1 Save/Reset unchanged.
    - apps/web/src/screens/RuleSettingsModal.test.tsx — the existing v1 assertions (save/reset/validation)
      to keep green.
    - packages/contracts/src/rule-explainers.ts — RULE_EXPLAINERS shape { summary, unit, direction, affects }.
    - Note: group prefix mapping — GROUPS uses "picker"/"exits"/"regime" which are exactly the registry
      path prefixes.
  </read_first>
  <behavior>
    - Each rendered knob row shows the registry `summary` as an inline caption (small, dim) under/beside
      the label, and the `affects` value as a compact tag chip.
    - Lookup is by `[group, ...row.path].join(".")`; a missing entry is impossible (Plan 01 completeness
      test guarantees coverage) but the render must not crash if undefined (fail-soft to no caption).
    - v1 behaviors unchanged: per-group Save, Reset to defaults, single group error line still render/work.
  </behavior>
  <action>
    In GroupPanel, look up `RULE_EXPLAINERS[[group, ...row.path].join(".")]` per row and render the
    `summary` caption + `affects` tag alongside the existing label/input. Keep the flatten/unflatten/Save/
    Reset machinery byte-unchanged. Add component tests asserting a representative row (e.g. picker
    weights.slope, exits.take.plus15Arm, regime.vvixWarn) renders its registry summary text + affects tag,
    and that the existing v1 save/reset/validation tests still pass.
  </action>
  <verify>
    <automated>bun run test -- apps/web/src/screens/RuleSettingsModal.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - source: caption/affects lookup uses `[group, ...row.path].join('.')` against RULE_EXPLAINERS (no inline copy).
    - behavior: representative rows render the exact registry summary + affects tag; v1 save/reset/validation assertions still green.
    - test-command: `bun run test -- apps/web/src/screens/RuleSettingsModal.test.tsx` green.
  </acceptance_criteria>
  <done>Every knob row shows its registry summary + affected-surface tag; v1 behavior unregressed.</done>
</task>

<task type="auto">
  <name>Task 2: Info-icon popover (Tooltip primitive) for direction + unit, first consumer</name>
  <files>apps/web/src/screens/RuleSettingsModal.tsx, apps/web/src/screens/RuleSettingsModal.test.tsx</files>
  <read_first>
    - apps/web/src/components/ui/tooltip.tsx — TooltipProvider/Tooltip/TooltipTrigger/TooltipContent
      (@base-ui/react). This is its first consumer; wrap once at the modal (or group) level with a
      Provider, then per-row Trigger+Content.
    - apps/web/src/screens/RuleSettingsModal.tsx — where the per-row caption from Task 1 renders; add the
      info-icon trigger beside it. lucide `Info` icon (already used elsewhere via lucide-react).
    - packages/contracts/src/rule-explainers.ts — `direction` + `unit` fields go in the popover content.
  </read_first>
  <behavior>
    - Each knob row has an info-icon trigger; on hover/focus a popover shows the registry `direction`
      sentence + `unit`. Keyboard-focusable (accessibility basics — trigger is a real button).
    - A single TooltipProvider wraps the group/modal; no hand-rolled popover, no new dependency.
  </behavior>
  <action>
    Add an `Info` info-icon TooltipTrigger per knob row rendering the registry `direction` + `unit` in
    TooltipContent, wrapped by one TooltipProvider. Add a component test asserting the popover content
    for a representative row surfaces the registry `direction` text (query by the trigger's accessible
    name / open interaction).
  </action>
  <verify>
    <automated>bun run test -- apps/web/src/screens/RuleSettingsModal.test.tsx && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - source: popover uses the Tooltip primitive from `components/ui/tooltip.tsx` (no custom popover); zero new dependency.
    - behavior: a representative row's info-icon exposes the registry `direction` + `unit`; trigger is keyboard-focusable.
    - test-command: `bun run test -- apps/web/src/screens/RuleSettingsModal.test.tsx` green + `bun run typecheck` clean.
  </acceptance_criteria>
  <done>Direction/unit copy reachable via the Tooltip primitive's first consumer; accessible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry copy → DOM | app-authored static strings render into the modal (no user-supplied content) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-11 | Information Disclosure (XSS) | caption/popover render | low | accept | Copy is app-authored static registry text rendered as React children (auto-escaped); no user input path into the caption/popover |
| T-32-SC | Tampering | npm installs | low | accept | Zero new dependencies (Tooltip already vendored); no install task |
</threat_model>

<verification>
- `bun run test -- apps/web/src/screens/RuleSettingsModal.test.tsx` green (new caption/popover assertions + v1 assertions)
- `bun run typecheck` + `bun run lint` clean
- Manual: modal copy reads right to a trader (editorial UAT, deferred to phase gate)
</verification>

<success_criteria>
- Every knob row: registry summary caption + affects tag + info-popover with direction/unit
- Tooltip primitive is the sole popover mechanism (first consumer), zero new deps
- v1 Save/Reset/validation unregressed
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 05)
- `RuleSettingsModal.tsx` per-knob captions + affected-surface tags + info-popovers, all from RULE_EXPLAINERS (B6)
- Tooltip primitive's first consumer (B9)
</artifacts>

<output>
Create `.planning/phases/32-rule-settings-modal-v2-explain-what-you-touch-per-knob-help/32-05-SUMMARY.md` when done
</output>
