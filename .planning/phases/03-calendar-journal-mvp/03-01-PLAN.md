---
phase: 03-calendar-journal-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/journal/application/ports.ts
  - packages/core/src/journal/domain/dte.ts
  - packages/core/src/journal/domain/dte.test.ts
  - packages/core/src/journal/index.ts
autonomous: true
requirements: [CAL-01, CAL-02, CAL-04]
must_haves:
  truths:
    - "The Calendar domain type carries every field the snapshot and targeted-fetch need (strike, optionType, front/back expiry, qty, openNetDebit, status, closedAt)"
    - "All new calendar/snapshot/journal port types are exported from @morai/core and compile under strict TS"
    - "calendarDte returns integer calendar-day DTE, clamping expired dates to 0, with standalone unit coverage"
  artifacts:
    - path: "packages/core/src/journal/application/ports.ts"
      provides: "Extended Calendar type + 9 new port/domain types for Phase 3"
      contains: "ForResolvingLegSnapshot"
    - path: "packages/core/src/journal/index.ts"
      provides: "Public re-exports of all new Phase 3 types"
      contains: "ForPersistingSnapshot"
  key_links:
    - from: "packages/core/src/journal/index.ts"
      to: "packages/core/src/journal/application/ports.ts"
      via: "type re-export"
      pattern: "ForResolvingLegSnapshot|SnapshotRow|ForRegisteringCalendar"
---

<objective>
Extend the `Calendar` domain type and add every new driven-port type Phase 3 needs, so that all downstream use-cases (register, list, close, snapshot, journal, targeted-fetch, live-greeks) compile against a stable contract. This is the unblocking critical path: the current `Calendar` type is `{id, underlying, openedAt}` (a Phase 1 status-check placeholder) and no new use-case can compile until it carries the calendar's real fields.

Purpose: Interface-first foundation. Define the contracts the rest of the phase implements against, eliminating the scavenger-hunt anti-pattern.
Output: Extended `ports.ts`, an exported `calendarDte` helper in `dte.ts`, and `index.ts` re-exports — types only, no runtime behavior beyond `calendarDte`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-calendar-journal-mvp/03-SPEC.md
@.planning/phases/03-calendar-journal-mvp/03-CONTEXT.md
@.planning/phases/03-calendar-journal-mvp/03-PATTERNS.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/typescript.md
</context>

<artifacts_this_phase_produces>
This plan creates these symbols (Wave 1 foundation; downstream plans consume them):
- Extended `Calendar` type fields: `strike` (×1000 int), `optionType` ("C"|"P"), `frontExpiry` (YYYY-MM-DD), `backExpiry` (YYYY-MM-DD), `qty`, `openNetDebit`, `status` ("open"|"closed"), `closedAt` (Date|null), `notes` (string|null)
- New port types: `ForRegisteringCalendar`, `ForListingCalendars`, `ForClosingCalendar`, `ForGettingCalendarById`, `ForGettingOpenCalendarLegs`, `ForResolvingLegSnapshot`, `ForPersistingSnapshot`, `ForReadingJournal`, `ForReadingLatestLegObs`
- New domain types: `LegSnapshot`, `SnapshotRow`, `CalendarNotFound` / `CalendarAlreadyClosed` error variants
- Exported `calendarDte(now, expiry): number` helper in `domain/dte.ts`
</artifacts_this_phase_produces>

<tasks>

<task type="execute">
  <name>Task 1: Extend Calendar type + add Phase 3 port types in ports.ts</name>
  <read_first>
    - packages/core/src/journal/application/ports.ts (the file being modified — current `Calendar` at lines 16-20, existing `ForGettingOpenCalendars` at lines 86-88 show the `ForVerbingNoun` convention to mirror)
    - .planning/phases/03-calendar-journal-mvp/03-PATTERNS.md (section "ports.ts (extend)" — the exact target type definitions to copy)
    - .planning/phases/03-calendar-journal-mvp/03-CONTEXT.md (D-01 optionType, D-05 net-greek shape, D-06 NaN convention)
    - .claude/rules/typescript.md (no any/as/!; readonly fields; Result<T,E>)
  </read_first>
  <action>
    Replace the minimal `Calendar` type (id/underlying/openedAt) with the richer type carrying readonly fields: `id: string`, `underlying: string`, `strike: number` (×1000 int, single strike shared by both legs per D-02 same-strike-only — document the convention in a comment matching `ContractRow.strike` at line 73), `optionType: "C" | "P"`, `frontExpiry: string` (YYYY-MM-DD), `backExpiry: string` (YYYY-MM-DD), `qty: number`, `openNetDebit: number`, `status: "open" | "closed"`, `openedAt: Date`, `closedAt: Date | null`, `notes: string | null`.

    Add these new types following the exact `ForVerbingNoun` function-type convention already in this file:
    - `ForRegisteringCalendar`: takes `{ underlying, strike, optionType, frontExpiry, backExpiry, qty, openNetDebit, openedAt, notes? }` (all readonly), returns `Promise<Result<Calendar, StorageError>>`.
    - `ForListingCalendars`: takes `(filter?: "open" | "closed")`, returns `Promise<Result<ReadonlyArray<Calendar>, StorageError>>`.
    - `ForGettingCalendarById`: takes `(id: string)`, returns `Promise<Result<Calendar | null, StorageError>>` (null when the id is unknown). Backs get_live_greeks leg resolution and any single-calendar read.
    - `ForClosingCalendar`: takes `(id: string, closeNetCredit: number)`, returns `Promise<Result<Calendar, StorageError | CalendarNotFound | CalendarAlreadyClosed>>`. Define `CalendarNotFound = { readonly kind: "not-found" }` and `CalendarAlreadyClosed = { readonly kind: "already-closed" }` as exported domain error types alongside `StorageError`.
    - `ForGettingOpenCalendarLegs`: `() => Promise<Result<ReadonlyArray<OccSymbol>, StorageError>>` — D-04 targeted-fetch port returning OCC symbols of every open calendar's two legs.
    - `LegSnapshot` type: readonly `occSymbol: OccSymbol`, `mark: number`, `underlyingPrice: number`, `ivRaw: number | null`, `bsmIv: string | null`, `bsmDelta: string | null`, `bsmGamma: string | null`, `bsmTheta: string | null`, `bsmVega: string | null` (bsm fields are Drizzle-numeric strings or the literal `'NaN'` per D-06).
    - `ForResolvingLegSnapshot`: takes `{ underlying: string, strike: number, optionType: "C" | "P", expiry: string }` (readonly), returns `Promise<Result<LegSnapshot | null, StorageError>>` (null when no matching contract / no observation for the slot).
    - `SnapshotRow` type: the full 18-column journal row — readonly `time: Date`, `calendarId: string`, then `spot`, `netMark`, `frontMark`, `backMark`, `frontIv`, `backIv`, `frontIvRaw`, `backIvRaw`, `netDelta`, `netGamma`, `netTheta`, `netVega`, `termSlope`, `pnlOpen` ALL typed `string` (Drizzle numeric maps to string; `'NaN'` is a valid value), `dteFront: number`, `dteBack: number` (integer calendar days), `source: "cboe"`.
    - `ForPersistingSnapshot`: `(row: SnapshotRow) => Promise<Result<void, StorageError>>`.
    - `ForReadingJournal`: `(calendarId: string) => Promise<Result<ReadonlyArray<SnapshotRow> | null, StorageError>>` — returns `null` when the calendar id is unknown (drives 404), empty array when known but zero snapshots.
    - `ForReadingLatestLegObs`: `(occSymbol: OccSymbol) => Promise<Result<LegSnapshot | null, StorageError>>` — backs `get_live_greeks`.

    Use `OccSymbol` from the existing `@morai/shared` import at line 1. Do NOT remove or alter any existing port type (fetchChain/computeBsm/rate/jobRuns ports stay intact). No `any`, no `as`, no `!`.
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/journal/application/ports.ts` defines `Calendar` with a `strike` field, an `optionType` field, and a `status` field (grep: `rg -n "optionType:|frontExpiry:|status:" packages/core/src/journal/application/ports.ts` matches the Calendar type)
    - `ports.ts` exports `ForResolvingLegSnapshot`, `ForPersistingSnapshot`, `ForReadingJournal`, `SnapshotRow`, `LegSnapshot`, `ForRegisteringCalendar`, `ForListingCalendars`, `ForClosingCalendar`, `ForGettingCalendarById`, `ForGettingOpenCalendarLegs`, `ForReadingLatestLegObs`, `CalendarNotFound`, `CalendarAlreadyClosed` (grep each symbol present)
    - `bun run typecheck` exits 0
  </acceptance_criteria>
  <done>The extended Calendar type and all 11 new port types plus 2 error variants exist and compile.</done>
</task>

<task type="tdd">
  <name>Task 2: Export tested calendarDte helper + re-export Phase 3 types from journal index</name>
  <read_first>
    - packages/core/src/journal/domain/dte.ts (the file being modified — `computeT` at line 89, `isThirdFriday` at line 74; the private `calendarDte` shape lives in fetchChain.ts lines 38-50, reproduce it here as an exported pure helper)
    - packages/core/src/journal/application/fetchChain.ts (lines 38-50 — the existing private `calendarDte` to mirror exactly: UTC-floored calendar-day difference, clamped at 0)
    - packages/core/src/journal/index.ts (the file being modified — existing re-export block, add the new types)
    - .planning/phases/03-calendar-journal-mvp/03-RESEARCH.md ("Open Questions" item 1 — recommendation: export calendarDte from dte.ts)
  </read_first>
  <behavior>
    - calendarDte(now, expiry) where expiry is in the past → 0 (zero-clamp; never negative DTE).
    - calendarDte(now, sameCalendarDayAsNow) → 0 (same-day expiry).
    - calendarDte(now, now+30 calendar days) → 30 (typical integer DTE).
    - DST boundary: a now/expiry pair spanning a US DST transition still returns the integer calendar-day count (uses UTC-floored Date.UTC math, no timezone drift).
  </behavior>
  <action>
    Write `packages/core/src/journal/domain/dte.test.ts` FIRST (RED) covering the four behaviors above (expired→0, same-day→0, 30-day→30, DST-spanning pair→correct integer). Run it failing. Then add an exported `calendarDte(now: Date, expiry: Date): number` to `packages/core/src/journal/domain/dte.ts` that computes the UTC-floored calendar-day count from `now` to `expiry`, clamped at 0 (`Math.max(0, Math.floor((expiryMs - nowMs) / 86_400_000))` using `Date.UTC(...)` for both). This is the integer DTE the snapshot's `dteFront`/`dteBack` columns need — calendar days, NOT the year-fraction `computeT` returns. Keep the function pure (no imports beyond what dte.ts already uses). Run the test GREEN.

    In `packages/core/src/journal/index.ts`, add to the existing `export type { ... } from "./application/ports.ts"` block ALL new types from Task 1: `Calendar` is already exported (now richer), plus `ForRegisteringCalendar`, `ForListingCalendars`, `ForClosingCalendar`, `ForGettingCalendarById`, `ForGettingOpenCalendarLegs`, `ForResolvingLegSnapshot`, `ForPersistingSnapshot`, `ForReadingJournal`, `ForReadingLatestLegObs`, `LegSnapshot`, `SnapshotRow`, `CalendarNotFound`, `CalendarAlreadyClosed`. Add `export { calendarDte } from "./domain/dte.ts";` alongside the existing `computeT`/`isThirdFriday` export. Do not remove existing exports.
  </action>
  <verify>
    <automated>bun run test --run dte.test && bun run typecheck && rg -n "calendarDte" packages/core/src/journal/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/journal/domain/dte.ts` exports `calendarDte` (grep: `rg -n "export function calendarDte" packages/core/src/journal/domain/dte.ts`)
    - `dte.test.ts` asserts calendarDte: expired date → 0, same-day → 0, 30-day → 30; `bun run test --run dte.test` exits 0
    - `packages/core/src/journal/index.ts` re-exports `calendarDte`, `ForResolvingLegSnapshot`, `ForPersistingSnapshot`, `ForReadingJournal`, `SnapshotRow`, `ForRegisteringCalendar`, `ForListingCalendars`, `ForClosingCalendar`, `ForGettingCalendarById`, `ForGettingOpenCalendarLegs`, `ForReadingLatestLegObs`
    - `bun run typecheck` exits 0 and `bun run lint` exits 0
  </acceptance_criteria>
  <done>`calendarDte` is exported from core with standalone unit coverage (zero-clamp + typical DTE), and all Phase 3 types are reachable via `@morai/core`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan is type-definitions + one pure helper. No runtime input crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `Calendar.optionType` / `strike` typing | mitigate | Types are `"C" \| "P"` literal union and `number`; downstream Zod (plan 03/04) enforces values at the boundary — this plan ensures the type cannot widen to `string`. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH: "no new packages"). No supply-chain surface. |
</threat_model>

<verification>
- `bun run typecheck` passes (the whole workspace compiles against the new types).
- `bun run lint` passes (no boundary violation — `ports.ts` imports only `@morai/shared`).
- No existing port type was removed (Phase 1/2 use-cases still compile).
</verification>

<success_criteria>
- The `Calendar` domain type carries strike, optionType, front/back expiry, qty, openNetDebit, status, closedAt, notes.
- All 10 new port types + 2 error variants exist and are exported from `@morai/core`.
- `calendarDte` is an exported pure helper returning integer calendar days.
- Workspace typecheck + lint green.
</success_criteria>

<output>
Create `.planning/phases/03-calendar-journal-mvp/03-01-SUMMARY.md` when done.
</output>
