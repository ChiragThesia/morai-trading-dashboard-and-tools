---
phase: 03-calendar-journal-mvp
plan: 04
type: tdd
wave: 3
depends_on: ["03-01", "03-03"]
files_modified:
  - packages/core/src/journal/domain/nyse-holidays.ts
  - packages/core/src/journal/domain/nyse-holidays.test.ts
  - packages/core/src/journal/index.ts
  - apps/worker/src/handlers/fetch-cboe-chain.ts
  - apps/worker/src/handlers/fetch-cboe-chain.test.ts
  - apps/worker/src/handlers/fetch-rates.ts
autonomous: true
requirements: [CAL-05]
must_haves:
  truths:
    - "isNyseHoliday returns true for the 2026-2027 NYSE full-closure dates and false for normal RTH days and early-close half days (2026-07-03)"
    - "fetch-cboe-chain no-ops with a 'skipping — outside RTH or NYSE holiday' warn and writes nothing on a holiday or weekend"
  artifacts:
    - path: "packages/core/src/journal/domain/nyse-holidays.ts"
      provides: "NYSE 2026-2027 full-closure list + isNyseHoliday(now) pure gate"
      contains: "isNyseHoliday"
    - path: "apps/worker/src/handlers/fetch-cboe-chain.ts"
      provides: "RTH + holiday gate before fetching"
      contains: "isNyseHoliday"
  key_links:
    - from: "apps/worker/src/handlers/fetch-cboe-chain.ts"
      to: "packages/core/src/journal/domain/nyse-holidays.ts"
      via: "import { isNyseHoliday } from @morai/core"
      pattern: "isNyseHoliday"
---

<objective>
Add NYSE full-closure holiday awareness (CAL-05) as pure data in the core domain alongside `isWithinRth()`, and wire the combined RTH+holiday gate into the existing fetch handlers. The snapshot handler (plan 05) reuses the same `isNyseHoliday` gate. The holiday list gates BOTH the new snapshot job and the existing Phase 2 fetch job — per SPEC: "snapshot AND existing fetch jobs."

Purpose: CAL-05. The RTH half already exists (`isWithinRth`); only the holiday list + gate wiring is new. Parallel with plan 03 (zero file overlap).
Output: `nyse-holidays.ts` domain module + `isNyseHoliday` export, the gate added to `fetch-cboe-chain` (and conservatively `fetch-rates`), and unit tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-calendar-journal-mvp/03-SPEC.md
@.planning/phases/03-calendar-journal-mvp/03-CONTEXT.md
@.planning/phases/03-calendar-journal-mvp/03-RESEARCH.md
@.planning/phases/03-calendar-journal-mvp/03-PATTERNS.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/tdd.md
@packages/core/src/journal/domain/rth-window.ts
@apps/worker/src/handlers/fetch-cboe-chain.ts
</context>

<artifacts_this_phase_produces>
This plan creates:
- `NYSE_HOLIDAYS` set (9 full closures in 2026, 9 in 2027) + `isNyseHoliday(now: Date): boolean` in `packages/core/src/journal/domain/nyse-holidays.ts`
- `isNyseHoliday` re-export from `@morai/core`
- The combined `!isWithinRth(now) || isNyseHoliday(now)` gate in `fetch-cboe-chain` and `fetch-rates` handlers
</artifacts_this_phase_produces>

<tasks>

<task type="tdd">
  <name>Task 1: NYSE holiday domain module + isNyseHoliday gate (RED→GREEN)</name>
  <read_first>
    - packages/core/src/journal/domain/rth-window.ts (the exact Intl.DateTimeFormat + formatToParts + America/New_York pattern to mirror — use year/month/day parts instead of hour/minute/weekday)
    - packages/core/src/journal/index.ts (the file being modified — add the isNyseHoliday export alongside isWithinRth)
    - .planning/phases/03-calendar-journal-mvp/03-RESEARCH.md (Focus Area 3 — the AUTHORITATIVE 2026-2027 closure list at lines 358-379; note July 4 2026 is a Saturday so the 2026 list has 9 full closures and Friday 2026-07-03 is an EARLY CLOSE, NOT a full closure)
    - .planning/phases/03-calendar-journal-mvp/03-PATTERNS.md (section "nyse-holidays.ts (new)" — note its warning to verify the 2026 list against RESEARCH lines 358-379; the RESEARCH list is canonical, NOT the PATTERNS draft which includes 2026-07-04)
    - .claude/rules/architecture-boundaries.md (core imports only shared; this file needs NO imports — pure data)
  </read_first>
  <behavior>
    - isNyseHoliday(2026-01-01 14:00Z) → true (New Year's Day).
    - isNyseHoliday(2026-11-26 ...) → true (Thanksgiving).
    - isNyseHoliday(2026-07-03 ...) → false (early close, treated as normal day per SPEC).
    - isNyseHoliday(a normal weekday RTH instant) → false.
    - isNyseHoliday(a Saturday) → false (weekend handling belongs to isWithinRth, not the holiday gate).
    - 2027 closures (e.g. 2027-12-24 observed Christmas) → true.
  </behavior>
  <action>
    Write `packages/core/src/journal/domain/nyse-holidays.ts` with NO imports (pure data). Define `const NYSE_HOLIDAYS = new Set<string>([...])` containing EXACTLY the dates from RESEARCH.md lines 358-379 (the authoritative ICE-sourced list): 2026 = 01-01, 01-19, 02-16, 04-03, 05-25, 06-19, 09-07, 11-26, 12-25 (NINE dates — do NOT include 2026-07-04, it is a Saturday); 2027 = 01-01, 01-18, 02-15, 03-26, 05-31, 06-18, 09-06, 11-25, 12-24 (nine dates). Export `isNyseHoliday(now: Date): boolean` using `Intl.DateTimeFormat("en-US", { timeZone: "America/New_York", year: "numeric", month: "2-digit", day: "2-digit" })` + `formatToParts`, assembling `${year}-${month}-${day}` and returning `NYSE_HOLIDAYS.has(key)`. Return `false` if any part is missing (defensive, matches rth-window). Add `export { isNyseHoliday } from "./domain/nyse-holidays.ts";` to `packages/core/src/journal/index.ts`.

    Write `nyse-holidays.test.ts` FIRST (RED), covering every behavior above (assert the full closure dates true, 2026-07-03 false, a normal RTH instant false, a Saturday false, at least two 2027 dates). Run it failing, then implement to GREEN. Keep the comment in the source noting July 4 2026 is a Saturday and 07-03 is an early close (v1 normal day).
  </action>
  <verify>
    <automated>bun run test --run nyse-holidays.test && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/journal/domain/nyse-holidays.ts` exports `isNyseHoliday` and contains a `NYSE_HOLIDAYS` set with 18 dates total (9 in 2026, 9 in 2027); it does NOT contain `2026-07-04` (grep: `rg -n '2026-07-04' packages/core/src/journal/domain/nyse-holidays.ts` returns nothing)
    - `nyse-holidays.test.ts` asserts `isNyseHoliday` true for 2026-01-01 and 2026-11-26, false for 2026-07-03 and a normal RTH day
    - `isNyseHoliday` re-exported from `packages/core/src/journal/index.ts` (grep)
    - `bun run test --run nyse-holidays.test` exits 0
  </acceptance_criteria>
  <done>The NYSE holiday gate exists as pure core domain data with full unit coverage and is exported from @morai/core.</done>
</task>

<task type="tdd">
  <name>Task 2: Wire RTH + holiday gate into fetch handlers (RED→GREEN)</name>
  <read_first>
    - apps/worker/src/handlers/fetch-cboe-chain.ts (the file being modified — current RTH-only gate at lines 42-46; the isWithinRth import at line 2)
    - apps/worker/src/handlers/fetch-cboe-chain.test.ts (the file being modified — existing handler test harness; add the holiday no-op case)
    - apps/worker/src/handlers/fetch-rates.ts (the file being modified — currently no RTH gate; add the holiday-only conservative gate per RESEARCH A2)
    - .planning/phases/03-calendar-journal-mvp/03-PATTERNS.md (section "RTH + Holiday Gate" shared pattern — the exact combined condition + warn message)
    - .planning/phases/03-calendar-journal-mvp/03-RESEARCH.md (Focus Area 3 gate-target table + Assumptions A1/A2)
  </read_first>
  <behavior>
    - fetch-cboe-chain handler: on a holiday instant (inject now=2026-01-01T14:00:00Z), the use-case is NOT called and a warn is logged; on a normal RTH instant the use-case IS called.
    - fetch-rates handler: on a holiday the use-case is NOT called (rates don't update on holidays); on a normal weekday it IS called.
  </behavior>
  <action>
    In `fetch-cboe-chain.ts`, change the import at line 2 to `import { isWithinRth, isNyseHoliday } from "@morai/core";` and replace the RTH-only block (lines 42-46) with: compute `const now = deps.now();` then `if (!isWithinRth(now) || isNyseHoliday(now)) { console.warn("fetch-cboe-chain: skipping — outside RTH or NYSE holiday"); return; }`. Pass `now` to `deps.fetchChainUseCase` only if its signature takes it (it does not — leave the call unchanged). Keep the array-guard and the success-chain `boss.send` intact.

    In `fetch-rates.ts`, add a `now: () => Date` dep to `FetchRatesHandlerDeps` (the composition root will pass `() => new Date()`), import `isNyseHoliday` from `@morai/core`, and add a holiday-only guard after the array-guard: `if (isNyseHoliday(deps.now())) { console.warn("fetch-rates: skipping — NYSE holiday"); return; }`. (Do NOT add an RTH gate to fetch-rates — it runs at 09:00 ET before open; RESEARCH A2.) Note: the worker composition root (`apps/worker/src/main.ts`) must pass the new `now` dep to `makeFetchRatesHandler` — this wiring is performed in plan 05 Task 4 where main.ts is already being edited (flag in SUMMARY for the executor wiring main.ts).

    Extend `fetch-cboe-chain.test.ts` (RED first): add a test injecting `now` = a known NYSE holiday (2026-01-01T14:00:00Z) asserting the fake `fetchChainUseCase` is never invoked and the handler returns; keep existing RTH/array-guard tests green. Run RED, then implement to GREEN.
  </action>
  <verify>
    <automated>bun run test --run fetch-cboe-chain.test && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/worker/src/handlers/fetch-cboe-chain.ts` imports `isNyseHoliday` and gates on `!isWithinRth(now) || isNyseHoliday(now)` with the warn message `outside RTH or NYSE holiday` (grep both)
    - `apps/worker/src/handlers/fetch-rates.ts` imports `isNyseHoliday` and guards on a holiday (grep)
    - `fetch-cboe-chain.test.ts` has a holiday no-op test asserting the use-case is not called on 2026-01-01
    - `bun run test --run fetch-cboe-chain.test` exits 0
  </acceptance_criteria>
  <done>The Phase 2 fetch handlers now no-op on NYSE holidays; the snapshot handler (plan 05) reuses the same gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pg-boss scheduler → handler | A job fires on a cron/chain trigger; the handler decides (via the gate) whether to do work. The clock (`now`) is injected, not read from a network input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Tampering | wrong-day execution writing stale/holiday data | mitigate | `isNyseHoliday` is pure, DST-correct via Intl America/New_York (same mechanism as `isWithinRth`); the handler gate prevents any write on a holiday (Task 2 test asserts the use-case is never invoked). |
| T-03-09 | Denial of Service | unbounded holiday-list growth | accept | List is a fixed 18-entry static Set for 2026-2027; no runtime input expands it. Re-research before 2028 (RESEARCH valid-until 2026-12-31). |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. |
</threat_model>

<verification>
- `bun run test` green including `nyse-holidays.test` and the extended `fetch-cboe-chain.test`.
- `bun run typecheck` + `bun run lint` green (nyse-holidays.ts imports nothing; handlers import from `@morai/core`).
- The 2026 holiday set has exactly 9 dates and excludes 2026-07-04 (Saturday).
</verification>

<success_criteria>
- `isNyseHoliday` exists as pure core data with full unit coverage, exported from `@morai/core`.
- fetch-cboe-chain and fetch-rates no-op on NYSE holidays (snapshot reuses the gate in plan 05).
- Full suite green.
</success_criteria>

<output>
Create `.planning/phases/03-calendar-journal-mvp/03-04-SUMMARY.md` when done.
</output>
