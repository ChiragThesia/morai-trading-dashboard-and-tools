---
phase: 03-calendar-journal-mvp
plan: 03
type: tdd
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - packages/contracts/src/calendar.ts
  - packages/contracts/src/calendar.test.ts
  - packages/contracts/src/index.ts
  - packages/core/src/journal/application/registerCalendar.ts
  - packages/core/src/journal/application/registerCalendar.test.ts
  - packages/core/src/journal/application/listCalendars.ts
  - packages/core/src/journal/application/closeCalendar.ts
  - packages/core/src/journal/application/closeCalendar.test.ts
  - packages/core/src/journal/index.ts
  - packages/adapters/src/postgres/repos/calendars.ts
  - packages/adapters/src/memory/calendars.ts
  - packages/adapters/src/__contract__/calendars.contract.ts
  - packages/adapters/src/postgres/repos/calendars.contract.test.ts
  - apps/server/src/adapters/http/calendar.routes.ts
  - apps/server/src/adapters/http/calendar.routes.test.ts
  - apps/server/src/main.ts
autonomous: true
requirements: [CAL-01, CAL-04]
must_haves:
  truths:
    - "POST /api/calendars registers an open calendar and returns 201 + the created calendar with a UUID"
    - "POST /api/calendars with backExpiry <= frontExpiry returns 400"
    - "GET /api/calendars lists calendars ordered openedAt desc, with optional ?status filter"
    - "POST /api/calendars/:id/close sets status=closed; closing unknown returns 404, already-closed returns 409"
    - "The same calendar Zod schema from @morai/contracts is used by the HTTP route (and re-used by the MCP list_calendars tool in plan 07)"
  artifacts:
    - path: "packages/contracts/src/calendar.ts"
      provides: "registerCalendarRequest, calendarResponse, listCalendarsResponse, closeCalendarRequest schemas"
      exports: ["registerCalendarRequest", "calendarResponse", "listCalendarsResponse", "closeCalendarRequest"]
    - path: "packages/core/src/journal/application/registerCalendar.ts"
      provides: "makeRegisterCalendarUseCase with backExpiry>frontExpiry domain validation"
      contains: "makeRegisterCalendarUseCase"
    - path: "apps/server/src/adapters/http/calendar.routes.ts"
      provides: "POST /calendars, GET /calendars, POST /calendars/:id/close"
      contains: "calendarRoutes"
    - path: "packages/adapters/src/memory/calendars.ts"
      provides: "in-memory twin with register/list/close/getOpenCalendarLegs"
      contains: "registerCalendar"
  key_links:
    - from: "apps/server/src/adapters/http/calendar.routes.ts"
      to: "@morai/contracts"
      via: "zValidator(registerCalendarRequest) + calendarResponse.parse"
      pattern: "registerCalendarRequest|calendarResponse"
    - from: "apps/server/src/main.ts"
      to: "calendarRoutes"
      via: "app.route('/api', calendarRoutes(...))"
      pattern: "calendarRoutes"
    - from: "packages/adapters/src/postgres/repos/calendars.ts"
      to: "calendars table"
      via: "Drizzle insert/select/update"
      pattern: "db\\.(insert|select|update)\\(calendars\\)"
---

<objective>
Deliver the calendar register / list / close vertical slice end-to-end: shared Zod contracts → core use-cases (with the `backExpiry > frontExpiry` domain rule and not-found/already-closed close semantics) → Postgres repo write methods → in-memory twin → HTTP routes → wired into the server composition root. After this plan a user can `POST /api/calendars`, `GET /api/calendars`, and `POST /api/calendars/:id/close` against real Postgres. This is the first user-visible capability of the MVP anchor (register).

Purpose: CAL-01 + CAL-04. Vertical slice (MVP mode): one new capability the user can exercise, not a horizontal layer.
Output: Calendar CRUD contracts, use-cases, both repo implementations + shared contract test, HTTP routes, server wiring. (MCP `list_calendars` tool is wired in plan 07, which reuses these same contracts and the `ForListingCalendars` use-case — MCP-02 satisfied at phase scope.)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-calendar-journal-mvp/03-SPEC.md
@.planning/phases/03-calendar-journal-mvp/03-CONTEXT.md
@.planning/phases/03-calendar-journal-mvp/03-PATTERNS.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/tdd.md
@.claude/rules/typescript.md
@packages/contracts/src/status.ts
@packages/adapters/src/postgres/repos/calendars.ts
@packages/adapters/src/memory/calendars.ts
@apps/server/src/adapters/http/status.routes.ts
@apps/server/src/main.ts
</context>

<artifacts_this_phase_produces>
This plan creates:
- Contracts: `registerCalendarRequest`, `calendarResponse`, `listCalendarsResponse`, `closeCalendarRequest` (+ inferred types) in `packages/contracts/src/calendar.ts`
- Use-cases: `makeRegisterCalendarUseCase` (+ `ForRunningRegisterCalendar`, `ValidationError`), `makeListCalendarsUseCase`, `makeCloseCalendarUseCase` in `packages/core/src/journal/application/`
- Repo methods on `makePostgresCalendarsRepo`: `registerCalendar`, `listCalendars`, `closeCalendar`, `getCalendarById` (implements `ForGettingCalendarById`), `getOpenCalendarLegs` (implements `ForGettingOpenCalendarLegs`, D-04)
- In-memory twin methods on `makeMemoryCalendarsRepo`: same four methods; extended `seedOpenCalendar` taking the full Calendar shape
- Shared contract runner `calendars.contract.ts` extended for the new methods
- HTTP route factory `calendarRoutes(...)` mounting POST/GET/close
- Server composition-root wiring of the three use-cases + routes
</artifacts_this_phase_produces>

<tasks>

<task type="tdd">
  <name>Task 1: Calendar contracts + register/list/close use-cases (RED→GREEN)</name>
  <read_first>
    - packages/contracts/src/status.ts (the analog — Zod object + z.infer export + MCP-02 single-source comment)
    - packages/contracts/src/index.ts (the file being modified — add the new exports)
    - packages/core/src/journal/application/fetchChain.ts (use-case factory analog: makeXxx(deps) returning a driver port; err() shape lines 207-210)
    - packages/core/src/journal/application/ports.ts (the new Calendar/ForRegisteringCalendar/ForListingCalendars/ForClosingCalendar/CalendarNotFound/CalendarAlreadyClosed from plan 01)
    - .planning/phases/03-calendar-journal-mvp/03-PATTERNS.md (sections "calendar.ts (new)", "registerCalendar.ts (new use-case)")
    - .claude/rules/tdd.md (RED→GREEN→REFACTOR; in-memory port + use-case test same PR)
  </read_first>
  <behavior>
    - registerCalendarRequest rejects: missing optionType, optionType not in {C,P}, non-positive strike, non-positive qty, malformed expiry (not YYYY-MM-DD). Accepts a well-formed body.
    - makeRegisterCalendarUseCase: backExpiry <= frontExpiry → err ValidationError {kind:"validation-error"}; valid input → calls persistCalendar and returns its Result; openedAt defaults to deps.now() when omitted.
    - makeCloseCalendarUseCase: unknown id → err {kind:"not-found"}; already-closed → err {kind:"already-closed"}; open calendar → ok with status "closed".
    - makeListCalendarsUseCase: returns all calendars when no filter; only matching status when filtered.
  </behavior>
  <action>
    Write `packages/contracts/src/calendar.ts` mirroring `status.ts`: `registerCalendarRequest` = `z.object({ underlying: z.string().min(1).max(16), strike: z.number().int().positive(), optionType: z.enum(["C","P"]), frontExpiry: z.string().regex(/^\d{4}-\d{2}-\d{2}$/), backExpiry: z.string().regex(/^\d{4}-\d{2}-\d{2}$/), qty: z.number().int().positive(), openNetDebit: z.number(), openedAt: z.string().datetime().optional(), notes: z.string().optional() })`. `calendarResponse` = the full calendar shape (id uuid, ..., status enum, openedAt datetime, closedAt datetime nullable, notes nullable). `listCalendarsResponse` = `z.object({ calendars: z.array(calendarResponse) })`. `closeCalendarRequest` = `z.object({ closeNetCredit: z.number() })`. Export each schema + its `z.infer` type. Add all to `packages/contracts/src/index.ts`.

    Write the three core use-cases in `packages/core/src/journal/application/`:
    - `registerCalendar.ts`: `RegisterCalendarDeps = { persistCalendar: ForRegisteringCalendar; now: () => Date }`; export `ValidationError = { readonly kind: "validation-error"; readonly message: string }`; `ForRunningRegisterCalendar` driver port; `makeRegisterCalendarUseCase(deps)` validates `input.backExpiry <= input.frontExpiry` (lexicographic compare of ISO date strings is correct here) → `err({kind:"validation-error", message:"backExpiry must be after frontExpiry"})`, else `deps.persistCalendar({...input, openedAt: input.openedAt ?? deps.now()})`.
    - `listCalendars.ts`: `makeListCalendarsUseCase({ listCalendars: ForListingCalendars })` returning a driver that forwards the optional filter.
    - `closeCalendar.ts`: `makeCloseCalendarUseCase({ closeCalendar: ForClosingCalendar })` forwarding `(id, closeNetCredit)`; the not-found/already-closed branches originate in the repo.
    Re-export the factories + `ForRunningRegisterCalendar` + `ValidationError` from `packages/core/src/journal/index.ts`.

    Write tests FIRST (RED), run them failing, then implement (GREEN):
    - `packages/contracts/src/calendar.test.ts`: `.safeParse` rejects each invalid case above; accepts a valid body.
    - `packages/core/src/journal/application/registerCalendar.test.ts`: uses an in-memory fake `persistCalendar` (plain function) — asserts the backExpiry rule, the openedAt default, and pass-through on success.
    - `packages/core/src/journal/application/closeCalendar.test.ts`: fake `closeCalendar` returning each variant — asserts not-found/already-closed/ok mapping.
    No `any`/`as`/`!`; parse don't cast; Result everywhere in core.
  </action>
  <verify>
    <automated>bun run test --run calendar.test && bun run test --run registerCalendar.test && bun run test --run closeCalendar.test</automated>
  </verify>
  <acceptance_criteria>
    - `packages/contracts/src/calendar.ts` exports `registerCalendarRequest`, `calendarResponse`, `listCalendarsResponse`, `closeCalendarRequest` (grep each)
    - `registerCalendar.test.ts` asserts `backExpiry <= frontExpiry` yields `{kind:"validation-error"}` and that omitted `openedAt` defaults to `deps.now()`
    - `closeCalendar.test.ts` asserts unknown→not-found, already-closed→already-closed, open→ok(status:"closed")
    - `bun run test --run calendar.test`, `registerCalendar.test`, `closeCalendar.test` all exit 0
  </acceptance_criteria>
  <done>Contracts + three use-cases exist with passing RED-first tests; types exported from core + contracts.</done>
</task>

<task type="tdd">
  <name>Task 2: Postgres + in-memory calendar write methods + shared contract test (RED→GREEN)</name>
  <read_first>
    - packages/adapters/src/postgres/repos/calendars.ts (the file being modified — current getOpenCalendars/pingDb/seedOpenCalendar; the try/catch→ok/err pattern at lines 37-61; the strike=0 sentinel seed at lines 76-92 to replace)
    - packages/adapters/src/memory/calendars.ts (the file being modified — the Map-wrapped-in-async-ok pattern at lines 29-57)
    - packages/adapters/src/__contract__/calendars.contract.ts (the shared contract runner to extend — run against BOTH adapters)
    - packages/adapters/src/__contract__/leg-observations.contract.ts (analog for a richer contract runner with testcontainers Postgres)
    - packages/adapters/src/postgres/repos/leg-observations.ts (line 1 — the `formatOccSymbol` import; onConflict / mapping patterns; the repo `return {...}` surface at line 231)
    - packages/shared/src/occ-symbol.ts (formatOccSymbol signature — strike in POINTS, so divide calendars.strike by 1000)
    - .planning/phases/03-calendar-journal-mvp/03-PATTERNS.md (sections "calendars.ts (extend)", "memory/calendars.ts (extend)")
    - .claude/rules/tdd.md (Postgres repos → testcontainers against real Postgres; new port → in-memory twin same PR)
  </read_first>
  <behavior>
    - registerCalendar (both adapters): inserts and returns the created Calendar with a generated UUID, status "open", closedAt null; openNetDebit round-trips as a number.
    - listCalendars (both): returns rows ordered openedAt desc; ?status filter narrows; mapped to the full Calendar type.
    - closeCalendar (both): open id → status "closed", closedAt set, closeNetCredit persisted; unknown id → err not-found; already-closed id → err already-closed.
    - getCalendarById (both): returns the full Calendar for a known id; null for an unknown id.
    - getOpenCalendarLegs (both): returns the OCC symbols of every open calendar's front AND back leg, built via formatOccSymbol(strike/1000), no duplicates.
    - Contract suite passes identically against Postgres (testcontainers) and in-memory.
  </behavior>
  <action>
    Extend `makePostgresCalendarsRepo` with `registerCalendar: ForRegisteringCalendar` (Drizzle `insert(calendars).values({...input, optionType, openNetDebit: String(input.openNetDebit), status:"open", notes: input.notes ?? null}).returning()` → map row to Calendar; guard `row === undefined` → storage-error), `listCalendars: ForListingCalendars` (`select().from(calendars)` + optional `where(eq(calendars.status, filter))` + `orderBy(desc(calendars.openedAt))`), `closeCalendar: ForClosingCalendar` (first `select` the row by id; undefined → err not-found; status==="closed" → err already-closed; else `update(calendars).set({status:"closed", closedAt: new Date(), closeNetCredit: String(closeNetCredit)}).where(eq(calendars.id, id)).returning()` → map), `getCalendarById: ForGettingCalendarById` (`select().from(calendars).where(eq(calendars.id, id)).limit(1)` → map the row to Calendar or `ok(null)` when absent), and `getOpenCalendarLegs: ForGettingOpenCalendarLegs` (select open rows' id/underlying/strike/optionType/frontExpiry/backExpiry, then for each build two OCC symbols via `formatOccSymbol({ root, expiry, type: optionType, strike: strike/1000 })` for front and back expiry; collect into a deduped array). Add a `mapRow` helper turning a DB row into the Calendar type (parseFloat on `open_net_debit`). Replace the old `strike=0` sentinel `seedOpenCalendar` so it accepts the full Calendar shape (strike, optionType, expiries, qty, openNetDebit). Add all five methods (`registerCalendar`, `listCalendars`, `closeCalendar`, `getCalendarById`, `getOpenCalendarLegs`) to the repo's `return {...}` and to `PostgresCalendarsRepo` type. Export `makePostgresCalendarsRepo` unchanged via the adapters index (already exported).

    Mirror the SAME five methods in `makeMemoryCalendarsRepo`: backing `Map<string, Calendar>`; registerCalendar generates a UUID (`crypto.randomUUID()`), stores and returns; listCalendars filters/sorts the map; closeCalendar mutates status with the same not-found/already-closed guards; getCalendarById returns `ok(store.get(id) ?? null)`; getOpenCalendarLegs builds OCC symbols the same way via formatOccSymbol. Update `MemoryCalendarsRepo` type + `seedOpenCalendar` to the full Calendar shape.

    Extend `packages/adapters/src/__contract__/calendars.contract.ts` with a shared suite covering all four behaviors above; wire it into `packages/adapters/src/postgres/repos/calendars.contract.test.ts` (testcontainers Postgres — mirror `leg-observations.contract.test.ts` setup) AND run the same suite against the in-memory twin. Write the contract suite RED first (it will fail until methods exist), then implement to GREEN.

    Hexagon law: no business logic in the repos beyond mapping; Drizzle stays in adapters; the OCC-symbol construction uses `@morai/shared` `formatOccSymbol`.
  </action>
  <verify>
    <automated>bun run test --run calendars.contract && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `packages/adapters/src/postgres/repos/calendars.ts` `return {...}` includes `registerCalendar, listCalendars, closeCalendar, getCalendarById, getOpenCalendarLegs` (grep)
    - `packages/adapters/src/memory/calendars.ts` exposes the same five methods incl. `getCalendarById` (grep)
    - The shared contract suite asserts: register returns a UUID + status open; close on unknown → not-found; close on already-closed → already-closed; getCalendarById hit/miss (null on unknown); getOpenCalendarLegs returns front+back OCC for each open calendar
    - `bun run test --run calendars.contract` exits 0 (passes against both Postgres and in-memory) and `bun run typecheck` exits 0
  </acceptance_criteria>
  <done>Both adapters implement the five new methods (incl. getCalendarById) and pass one shared contract suite; the strike=0 sentinel seed is gone.</done>
</task>

<task type="tdd">
  <name>Task 3: Calendar HTTP routes + server wiring (RED→GREEN)</name>
  <read_first>
    - apps/server/src/adapters/http/status.routes.ts (the exact route-factory analog: Hono router, call use-case, result.ok guard, contract.parse, c.json)
    - apps/server/src/adapters/http/status.routes.test.ts (the route test harness analog — app.request against a Hono instance with an injected fake use-case)
    - apps/server/src/main.ts (the file being modified — composition root; how statusRoutes is mounted at app.route("/api", ...))
    - packages/contracts/src/calendar.ts (the schemas from Task 1)
    - apps/server/package.json (line 7 — @hono/zod-validator is available for zValidator)
    - .planning/phases/03-calendar-journal-mvp/03-PATTERNS.md (section "calendar.routes.ts (new)" — the exact route bodies + status-code mapping)
  </read_first>
  <behavior>
    - POST /api/calendars valid body → 201 + calendarResponse JSON; validation-error from use-case → 400; storage-error → 500.
    - POST /api/calendars malformed body (zValidator) → 400.
    - GET /api/calendars → 200 + {calendars:[...]} ; ?status=open|closed narrows; invalid status ignored (treated as no filter).
    - POST /api/calendars/:id/close → 200 + calendarResponse; not-found → 404; already-closed → 409.
  </behavior>
  <action>
    Write `apps/server/src/adapters/http/calendar.routes.ts` exporting `calendarRoutes(registerCalendar: ForRunningRegisterCalendar, listCalendars: ForListingCalendars, closeCalendar: ForClosingCalendar)` returning a Hono router with the three routes per PATTERNS.md: `router.post("/calendars", zValidator("json", registerCalendarRequest), ...)` mapping `result.error.kind === "validation-error"` → 400 else 500, success → `c.json(calendarResponse.parse(result.value), 201)`; `router.get("/calendars", ...)` reading `c.req.query("status")`, coercing to `"open"|"closed"|undefined`, returning `listCalendarsResponse.parse({calendars: result.value})`; `router.post("/calendars/:id/close", zValidator("json", closeCalendarRequest), ...)` mapping `not-found`→404, `already-closed`→409, else success → `calendarResponse.parse(result.value)`. Thin-adapter rule: zero business logic, Zod-parse in, parse contract out.

    Wire into `apps/server/src/main.ts`: build the calendars repo (already built as `calendarsRepo`), construct `makeRegisterCalendarUseCase({ persistCalendar: calendarsRepo.registerCalendar, now: () => new Date() })`, `makeListCalendarsUseCase({ listCalendars: calendarsRepo.listCalendars })`, `makeCloseCalendarUseCase({ closeCalendar: calendarsRepo.closeCalendar })`, then `app.route("/api", calendarRoutes(register, list, close))`. Keep existing status route + MCP mount intact.

    Write `apps/server/src/adapters/http/calendar.routes.test.ts` FIRST (RED): mount `calendarRoutes` with fake use-cases (plain async functions returning Result variants), drive via `app.request("/calendars", {method:"POST", body})`; assert 201/400/404/409/200 and JSON shapes. Then implement to GREEN.
  </action>
  <verify>
    <automated>bun run test --run calendar.routes.test && bun run typecheck && bun run lint</automated>
  </verify>
  <acceptance_criteria>
    - `apps/server/src/adapters/http/calendar.routes.ts` exports `calendarRoutes` and imports `registerCalendarRequest`, `calendarResponse`, `listCalendarsResponse`, `closeCalendarRequest` from `@morai/contracts` (grep)
    - `calendar.routes.test.ts` asserts: valid POST→201, invalid expiry order→400, malformed body→400, close unknown→404, close already-closed→409, GET list→200
    - `apps/server/src/main.ts` contains `calendarRoutes(` mounted under `/api` (grep)
    - `bun run test --run calendar.routes.test`, `bun run typecheck`, `bun run lint` all exit 0
  </acceptance_criteria>
  <done>The three calendar routes are wired into the server and pass route-level tests; register/list/close work end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /api/calendars | Untrusted JSON body (underlying, strike, optionType, expiries, qty, debit) crosses into the system. |
| client → POST /api/calendars/:id/close | Untrusted path param `:id` + JSON `closeNetCredit`. |
| client → GET /api/calendars?status= | Untrusted query string. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | OCC-symbol / SQL injection via calendar registration | mitigate | `registerCalendarRequest` Zod-validates `optionType: z.enum(["C","P"])`, `strike: z.number().int().positive()`, expiries via regex; Drizzle parameterized `insert().values()` never interpolates user input (Task 1 + Task 2). |
| T-03-05 | Tampering | malformed `:id` on close route | mitigate | Drizzle parameterized `where(eq(calendars.id, id))`; a non-UUID id simply matches no row → not-found 404, never an injection vector. |
| T-03-06 | Information Disclosure | error bodies leaking internals | mitigate | Routes map errors to flat `{error:"..."}` strings (400/404/409/500); no stack traces or DB messages returned (Task 3). |
| T-03-07 | Elevation of Privilege | closing another user's calendar | accept | Single-user system in v1 (RESEARCH Security Domain V4); no ownership model exists. Revisit at multi-user (deferred). |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase; `@hono/zod-validator` already present (apps/server/package.json). |
</threat_model>

<verification>
- `bun run test` (full workspace) green including the new contract + route + use-case tests.
- `bun run typecheck` + `bun run lint` green (hexagon boundaries respected: core imports only shared; Drizzle confined to adapters; routes thin).
- Manual sanity (optional): `curl -X POST localhost:PORT/api/calendars` with a valid body returns 201 + UUID; back<=front returns 400.
</verification>

<success_criteria>
- POST/GET/close calendar routes live and wired into the server, backed by real Postgres.
- One shared contract suite passes against Postgres + in-memory twins.
- Shared Zod contracts in place for reuse by the MCP `list_calendars` tool (plan 07).
- Full suite green.
</success_criteria>

<output>
Create `.planning/phases/03-calendar-journal-mvp/03-03-SUMMARY.md` when done.
</output>
