---
phase: 03-calendar-journal-mvp
plan: 07
type: tdd
wave: 6
depends_on: ["03-03", "03-06"]
files_modified:
  - apps/server/src/adapters/mcp/tools.ts
  - apps/server/src/adapters/mcp/server.ts
  - apps/server/src/adapters/mcp/mcp.test.ts
  - apps/server/src/main.ts
autonomous: false
requirements: [MCP-01, CAL-04, CAL-03]
must_haves:
  truths:
    - "All six MCP tools (get_status, list_calendars, get_journal, get_live_greeks, get_term_structure, get_skew) are registered and reachable"
    - "list_calendars and get_journal return the SAME data as their HTTP routes via the SAME @morai/contracts schema (MCP-02)"
    - "get_term_structure and get_skew return a typed empty {observations:[]} result, never an error"
    - "trigger_job is NOT registered (deferred to Phase 5, D-08)"
    - "trigger_job is absent from the MCP tool registry (D-08 holds; Phase 5)"
  artifacts:
    - path: "apps/server/src/adapters/mcp/tools.ts"
      provides: "registerListCalendarsTool, registerGetJournalTool, registerGetLiveGreeksTool, registerGetTermStructureTool, registerGetSkewTool"
      contains: "registerGetJournalTool"
    - path: "apps/server/src/adapters/mcp/server.ts"
      provides: "makeMcpRouter registering all six tools"
      contains: "registerGetJournalTool"
  key_links:
    - from: "apps/server/src/adapters/mcp/tools.ts"
      to: "@morai/contracts"
      via: "journalResponse / listCalendarsResponse parse"
      pattern: "journalResponse|listCalendarsResponse"
    - from: "apps/server/src/main.ts"
      to: "makeMcpRouter"
      via: "use-cases injected into makeMcpRouter"
      pattern: "makeMcpRouter\\("
---

<objective>
Register the five remaining MCP tools alongside the existing `get_status`, so all six MCP-01 tools are reachable from Claude Code, each sharing one Zod contract with its HTTP route (MCP-02). `list_calendars`, `get_journal`, and `get_live_greeks` wrap the use-cases built in plans 03/06; `get_term_structure` and `get_skew` return a typed-empty `{observations:[]}` payload (their analytics compute lands in Phase 6) — never an error. `trigger_job` stays deferred to Phase 5 (D-08). This is the "read via MCP" half of the MVP anchor and the final phase deliverable.

Purpose: MCP-01 (six tools) + MCP-02 (route+tool share one contract). Completes register→snapshot→read via BOTH HTTP and MCP.
Output: five `registerXxxTool` functions, an expanded `makeMcpRouter` signature, server composition wiring, and an expanded `mcp.test.ts` exercising all six tools.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-calendar-journal-mvp/03-SPEC.md
@.planning/phases/03-calendar-journal-mvp/03-CONTEXT.md
@.planning/phases/03-calendar-journal-mvp/03-PATTERNS.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/tdd.md
@.claude/rules/typescript.md
@apps/server/src/adapters/mcp/tools.ts
@apps/server/src/adapters/mcp/server.ts
@apps/server/src/main.ts
</context>

<artifacts_this_phase_produces>
This plan creates the six MCP tool names (5 new) and their registration functions:
- `registerListCalendarsTool(server, listCalendars)` → tool `list_calendars`
- `registerGetJournalTool(server, getJournal)` → tool `get_journal`
- `registerGetLiveGreeksTool(server, getLiveGreeks)` → tool `get_live_greeks`
- `registerGetTermStructureTool(server)` → tool `get_term_structure` (typed-empty stub, no use-case)
- `registerGetSkewTool(server)` → tool `get_skew` (typed-empty stub, no use-case)
- Expanded `makeMcpRouter(config, getStatus, listCalendars, getJournal, getLiveGreeks)` signature
NOT created: `trigger_job` (deferred Phase 5, D-08).
</artifacts_this_phase_produces>

<tasks>

<task type="tdd">
  <name>Task 1: Register the five new MCP tools (RED→GREEN)</name>
  <read_first>
    - apps/server/src/adapters/mcp/tools.ts (the file being modified — the exact registerStatusTool pattern: server.registerTool(name, {title, description, inputSchema}, async handler); `type:"text" as const`; result.ok guard; contract.parse)
    - apps/server/src/adapters/mcp/mcp.test.ts (the file being modified — the existing McpServer-direct test harness for get_status; expand it for six tools)
    - packages/contracts/src/journal.ts, calendar.ts, live-greeks.ts, analytics.ts (the shared schemas from plans 03/06 — journalResponse, listCalendarsResponse, liveGreeksResponse, termStructureResponse, skewResponse)
    - packages/core/src/journal/index.ts (the use-case driver port types: ForListingCalendars, ForReadingJournal, ForReadingLatestLegObs-backed getLiveGreeks)
    - .planning/phases/03-calendar-journal-mvp/03-PATTERNS.md (section "tools.ts (extend)" — exact tool bodies incl. parameter re-parse for get_journal/get_live_greeks, and the typed-empty stub shape)
    - .planning/phases/03-calendar-journal-mvp/03-RESEARCH.md (Focus Area 2 — Pitfall 6 exactOptionalPropertyTypes: use z.string().uuid() and re-parse args at the boundary; typed-empty tools have NO result.ok path)
  </read_first>
  <behavior>
    - list_calendars (no params): returns the same {calendars:[...]} payload as GET /api/calendars, parsed through listCalendarsResponse.
    - get_journal (calendarId param): re-parses args via z.object({calendarId: z.string().uuid()}); returns journalResponse-shaped {snapshots:[...]} matching the HTTP route; unknown id → a typed empty/`not found` text payload (mirror SPEC: never throws).
    - get_live_greeks (calendarId param): returns liveGreeksResponse-shaped payload from Phase 2 leg data.
    - get_term_structure / get_skew (no params): always return {observations:[]} (typed-empty), never an error, no use-case call.
    - trigger_job is absent.
  </behavior>
  <action>
    In `apps/server/src/adapters/mcp/tools.ts`, add five functions mirroring `registerStatusTool`:
    - `registerListCalendarsTool(server, listCalendars: ForListingCalendars)`: `inputSchema: {}`; call `listCalendars(undefined)`; result.ok guard; `listCalendarsResponse.parse({ calendars: result.value })` → JSON text content.
    - `registerGetJournalTool(server, getJournal: ForReadingJournal)`: `inputSchema: { calendarId: z.string().uuid() }`; in the handler re-parse `const { calendarId } = z.object({ calendarId: z.string().uuid() }).parse(args);` (boundary rule, Pitfall 6); call `getJournal(calendarId)`; on `!result.ok` return a text `internal error`; on `result.value === null` return text `{"error":"not found"}` (never throw); else `journalResponse.parse({ snapshots: result.value })` → JSON text.
    - `registerGetLiveGreeksTool(server, getLiveGreeks)`: same parameter re-parse; parse through `liveGreeksResponse`.
    - `registerGetTermStructureTool(server)`: `inputSchema: {}`; handler returns `{ content: [{ type:"text" as const, text: JSON.stringify({ observations: [] }) }] }` — NO use-case, NO result.ok branch (SPEC §7 "never an error").
    - `registerGetSkewTool(server)`: identical shape for `get_skew`.
    Keep `type:"text" as const` (exactOptionalPropertyTypes). No any/as/!.

    Expand `apps/server/src/adapters/mcp/mcp.test.ts` (RED first): drive a fresh `McpServer` with fakes (list returns ok([cal]); getJournal returns ok([snapshot]) and ok(null); getLiveGreeks returns ok(...)); register all six tools; invoke each via the server's tool-call path; assert each returns a schema-valid payload, that get_term_structure/get_skew return `{observations:[]}` with no error, and that get_journal on an unknown id returns the not-found text rather than throwing. Run RED, implement GREEN.
  </action>
  <verify>
    <automated>bun run test --run mcp.test && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `tools.ts` exports `registerListCalendarsTool`, `registerGetJournalTool`, `registerGetLiveGreeksTool`, `registerGetTermStructureTool`, `registerGetSkewTool` (grep each)
    - `get_journal` / `get_live_greeks` handlers re-parse args via `z.string().uuid()`; term-structure/skew handlers have no `result.ok` branch and emit `{observations:[]}` (grep `observations: []`)
    - `tools.ts` does NOT register a `trigger_job` tool (grep: `rg -n 'trigger_job' apps/server/src/adapters/mcp/tools.ts` returns nothing)
    - `mcp.test.ts` invokes all six tools and asserts schema-valid payloads + typed-empty for term-structure/skew
    - `bun run test --run mcp.test` exits 0
  </acceptance_criteria>
  <done>All five new MCP tools register against the shared contracts and pass direct-McpServer tests; trigger_job stays absent.</done>
</task>

<task type="execute">
  <name>Task 2: Expand makeMcpRouter signature + wire all six tools in the server root</name>
  <read_first>
    - apps/server/src/adapters/mcp/server.ts (the file being modified — current makeMcpRouter(config, getStatus) at line 22; makeServerAndTransport at line 33 calling registerStatusTool; bearer gate at line 29 stays)
    - apps/server/src/main.ts (the file being modified — the use-case consts built in plans 03/06: listCalendars, getJournal, getLiveGreeks; the existing makeMcpRouter(config, getStatus) call at line 44)
    - .planning/phases/03-calendar-journal-mvp/03-PATTERNS.md (section "server.ts (extend)" — the expanded signature + makeServerAndTransport registering all six)
    - .claude/rules/architecture-boundaries.md (adapter wiring only; no business logic)
  </read_first>
  <action>
    Expand `makeMcpRouter` in `server.ts` to `makeMcpRouter(config: Config, getStatus: ForGettingStatus, listCalendars: ForListingCalendars, getJournal: ForReadingJournal, getLiveGreeks: <getLiveGreeks driver type>)`. Import the five new register functions + the use-case port types from `@morai/core`. In `makeServerAndTransport`, after `registerStatusTool(server, getStatus);` add `registerListCalendarsTool(server, listCalendars); registerGetJournalTool(server, getJournal); registerGetLiveGreeksTool(server, getLiveGreeks); registerGetTermStructureTool(server); registerGetSkewTool(server);`. Keep the bearer gate and the stateless per-request server/transport pattern unchanged.

    In `apps/server/src/main.ts`, pass the existing `listCalendars`, `getJournal`, `getLiveGreeks` use-case consts (built in plans 03/06) into the `makeMcpRouter(config, getStatus, listCalendars, getJournal, getLiveGreeks)` call. Pure composition — no logic. Keep the route mounts intact.
  </action>
  <verify>
    <automated>bun run typecheck && bun run lint && bun run test</automated>
  </verify>
  <acceptance_criteria>
    - `apps/server/src/adapters/mcp/server.ts` `makeServerAndTransport` calls all six `registerXxxTool` functions (grep each of the five new ones present)
    - `apps/server/src/main.ts` calls `makeMcpRouter(config, getStatus, listCalendars, getJournal, getLiveGreeks)` (grep the expanded call)
    - `bun run typecheck`, `bun run lint`, `bun run test` (full suite) all exit 0
  </acceptance_criteria>
  <done>The MCP router registers all six tools per request; the server boots with the full tool set wired from one contract source.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify all six MCP tools reachable from Claude Code (live transport)</name>
  <action>
    Human-required live verification (no automation substitute for the Claude-Code MCP client round-trip): from Claude Code, invoke each of the six MCP tools against the running bearer-protected /mcp endpoint and confirm list_calendars + get_journal match their HTTP-route outputs, term-structure/skew return typed-empty, and trigger_job is absent. Automated tests already cover the McpServer-direct and HTTP paths; only the live transport hand-off is manual.
  </action>
  <verify>
    <human-check>From Claude Code, all six MCP tools round-trip; list_calendars and get_journal outputs match the HTTP routes; get_term_structure and get_skew return {observations:[]}.</human-check>
  </verify>
  <done>All six MCP tools are reachable from Claude Code; HTTP and MCP outputs match for list_calendars + get_journal; trigger_job is not listed.</done>
  <what-built>
    All six MCP tools (get_status, list_calendars, get_journal, get_live_greeks, get_term_structure, get_skew) are registered on the streamable-HTTP MCP endpoint and share their Zod contracts with the HTTP routes. Automated tests cover the McpServer-direct path and the HTTP routes; the live Claude-Code-client round-trip over the bearer-protected transport is the one thing automated tests cannot fully exercise (per 03-VALIDATION.md Manual-Only Verifications).
  </what-built>
  <how-to-verify>
    1. Start the server locally (or use the deployed Railway MCP endpoint) and ensure the MCP bearer token is configured in Claude Code's MCP client config.
    2. From Claude Code, register a calendar via `POST /api/calendars` (or confirm one exists).
    3. Invoke `list_calendars` from Claude Code — confirm it returns the same list as `GET /api/calendars`.
    4. Invoke `get_journal` with that calendar's id — confirm it returns the same ordered series as `GET /api/journal/:calendarId` (run both and diff).
    5. Invoke `get_term_structure` and `get_skew` — confirm each returns `{observations: []}` and NOT an error.
    6. Confirm `get_live_greeks` returns leg greeks (or a typed-empty result if no Phase 2 data is present yet), never an error.
    7. Confirm `trigger_job` is NOT listed among available tools (deferred to Phase 5).
  </how-to-verify>
  <acceptance_criteria>
    - All six tools appear in Claude Code's tool list; `trigger_job` does not.
    - `list_calendars` and `get_journal` outputs match their HTTP-route counterparts byte-for-byte (same shared contract).
    - `get_term_structure` and `get_skew` return `{observations:[]}` with no error.
  </acceptance_criteria>
  <resume-signal>Type "verified" once all six tools round-trip from Claude Code and HTTP/MCP outputs match, or describe the discrepancy.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code MCP client → /mcp | Untrusted JSON-RPC tool-call args (e.g. calendarId) cross the bearer-protected transport. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-18 | Spoofing | unauthenticated MCP tool calls | mitigate | Bearer gate on `/mcp/*` (Phase 1, retained in server.ts line 29); no new auth surface this phase. |
| T-03-19 | Tampering | malicious calendarId in get_journal/get_live_greeks | mitigate | Tool handler re-parses args via `z.string().uuid()` at the boundary (Pitfall 6, Task 1); use-case uses parameterized Drizzle queries downstream. |
| T-03-20 | Information Disclosure | typed-empty / error tools leaking internal errors | mitigate | term-structure/skew never call a use-case and return a constant `{observations:[]}`; get_journal unknown id returns a flat `not found` text, not a DB error (Task 1, SPEC §7). |
| T-03-21 | Elevation of Privilege | trigger_job firing jobs manually | mitigate | trigger_job is NOT registered (D-08 ban, Phase 5); acceptance criteria grep-asserts its absence (Task 1). |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. |
</threat_model>

<verification>
- `bun run test` (full workspace) green incl. the six-tool mcp.test.
- `bun run typecheck` + `bun run lint` green (tools thin; one contract source per tool; trigger_job absent).
- Human checkpoint (Task 3): all six tools round-trip from Claude Code; HTTP and MCP outputs match for list_calendars + get_journal.
</verification>

<success_criteria>
- All six MCP-01 tools registered and reachable; trigger_job absent (D-08).
- list_calendars + get_journal share one contract with their HTTP routes (MCP-02); outputs match.
- term-structure + skew return typed-empty, never errors.
- Full suite green + live MCP verification passed.
</success_criteria>

<output>
Create `.planning/phases/03-calendar-journal-mvp/03-07-SUMMARY.md` when done.
</output>
