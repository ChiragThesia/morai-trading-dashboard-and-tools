---
phase: 03-calendar-journal-mvp
plan: 06
type: tdd
wave: 5
depends_on: ["03-01", "03-03", "03-05"]
files_modified:
  - packages/contracts/src/journal.ts
  - packages/contracts/src/journal.test.ts
  - packages/contracts/src/live-greeks.ts
  - packages/contracts/src/analytics.ts
  - packages/contracts/src/index.ts
  - packages/core/src/journal/application/getJournal.ts
  - packages/core/src/journal/application/getLiveGreeks.ts
  - packages/core/src/journal/index.ts
  - packages/adapters/src/postgres/repos/leg-observations.ts
  - packages/adapters/src/memory/leg-observations.ts
  - apps/server/src/adapters/http/journal.routes.ts
  - apps/server/src/adapters/http/journal.routes.test.ts
  - apps/server/src/main.ts
autonomous: true
requirements: [CAL-03, MCP-01]
must_haves:
  truths:
    - "GET /api/journal/:calendarId returns an ordered JSON array of full 18-field snapshot objects for a registered calendar"
    - "An unknown calendarId returns 404; a known calendar with zero snapshots returns 200 + empty array"
    - "The journal Zod schema in @morai/contracts is the single source shared by the HTTP route and (in plan 07) the MCP get_journal tool"
    - "get_live_greeks resolves the latest bsm-greek legs for a calendar's contracts from Phase 2 data"
  artifacts:
    - path: "packages/contracts/src/journal.ts"
      provides: "snapshotResponse + journalResponse Zod schemas (18 fields)"
      exports: ["snapshotResponse", "journalResponse"]
    - path: "packages/core/src/journal/application/getJournal.ts"
      provides: "makeGetJournalUseCase forwarding ForReadingJournal"
      contains: "makeGetJournalUseCase"
    - path: "apps/server/src/adapters/http/journal.routes.ts"
      provides: "GET /journal/:calendarId with 404/empty/200 handling"
      contains: "journalRoutes"
    - path: "packages/contracts/src/analytics.ts"
      provides: "typed-empty termStructureResponse + skewResponse for plan 07 stub tools"
      exports: ["termStructureResponse", "skewResponse"]
  key_links:
    - from: "apps/server/src/adapters/http/journal.routes.ts"
      to: "@morai/contracts"
      via: "journalResponse.parse({snapshots})"
      pattern: "journalResponse"
    - from: "apps/server/src/main.ts"
      to: "journalRoutes"
      via: "app.route('/api', journalRoutes(getJournal))"
      pattern: "journalRoutes"
---

<objective>
Deliver the journal read surface (CAL-03) — `GET /api/journal/:calendarId` returns the ordered snapshot series from the shared `journalResponse` Zod contract — completing the register→snapshot→read MVP anchor over HTTP. Also add the `get_live_greeks` use-case (latest bsm-greek legs for a calendar, from Phase 2 data) and the typed-empty `analytics` contracts, so plan 07 can register all six MCP tools from one contract source. The journal schema and the live-greeks/analytics schemas all land here; plan 07 wires the MCP tools onto them (MCP-02: one schema source).

Purpose: CAL-03 (journal read) + the contract/use-case half of MCP-01 (the MCP tools themselves register in plan 07). The "read" step of the MVP anchor.
Output: journal/live-greeks/analytics contracts + tests, getJournal/getLiveGreeks use-cases, the live-leg-obs read repo method on both adapters, the journal HTTP route, server wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-calendar-journal-mvp/03-SPEC.md
@.planning/phases/03-calendar-journal-mvp/03-CONTEXT.md
@.planning/phases/03-calendar-journal-mvp/03-PATTERNS.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/tdd.md
@.claude/rules/typescript.md
@packages/contracts/src/status.ts
@apps/server/src/adapters/http/status.routes.ts
@apps/server/src/main.ts
</context>

<artifacts_this_phase_produces>
This plan creates:
- Contracts: `snapshotResponse`, `journalResponse` in `journal.ts`; `liveGreeksResponse` in `live-greeks.ts`; `termStructureResponse`, `skewResponse` (typed-empty) in `analytics.ts` (+ inferred types, all exported from contracts index)
- Use-cases: `makeGetJournalUseCase` (forwards `ForReadingJournal`), `makeGetLiveGreeksUseCase` (uses `ForReadingLatestLegObs`)
- Repo method `getLatestLegObs` implementing `ForReadingLatestLegObs` on Postgres + memory leg-observations
- HTTP route `journalRoutes(getJournal)` → `GET /api/journal/:calendarId`
- Server wiring of getJournal/getLiveGreeks use-cases + journal route
</artifacts_this_phase_produces>

<tasks>

<task type="tdd">
  <name>Task 1: journal + live-greeks + analytics contracts and getJournal/getLiveGreeks use-cases (RED→GREEN)</name>
  <read_first>
    - packages/contracts/src/status.ts (the Zod-object + z.infer + MCP-02 single-source analog)
    - packages/contracts/src/index.ts (the file being modified — add exports)
    - packages/core/src/journal/application/ports.ts (plan 01: ForReadingJournal, SnapshotRow, ForReadingLatestLegObs, LegSnapshot)
    - packages/core/src/journal/application/getStatus.ts (a thin core use-case analog — makeGetStatusUseCase shape)
    - .planning/phases/03-calendar-journal-mvp/03-PATTERNS.md (sections "journal.ts (new)", "analytics.ts (new)" — exact schema field lists; snapshotResponse is the 18-field string/number shape)
    - .claude/rules/tdd.md (RED→GREEN; contract validation test)
  </read_first>
  <behavior>
    - snapshotResponse validates a full snapshot row: time datetime, calendarId uuid, the numeric fields as strings (incl. 'NaN' allowed), dteFront/dteBack ints, source enum.
    - journalResponse = { snapshots: array(snapshotResponse) }.
    - termStructureResponse / skewResponse = { observations: array(unknown) } and accept { observations: [] }.
    - makeGetJournalUseCase forwards calendarId to ForReadingJournal and returns its Result (null passthrough for unknown).
    - makeGetLiveGreeksUseCase resolves latest legs via ForReadingLatestLegObs (shape returned validates against liveGreeksResponse).
  </behavior>
  <action>
    Write `packages/contracts/src/journal.ts` mirroring status.ts: `snapshotResponse = z.object({ time: z.string().datetime(), calendarId: z.string().uuid(), spot: z.string(), netMark: z.string(), frontMark: z.string(), backMark: z.string(), frontIv: z.string(), backIv: z.string(), frontIvRaw: z.string(), backIvRaw: z.string(), netDelta: z.string(), netGamma: z.string(), netTheta: z.string(), netVega: z.string(), termSlope: z.string(), dteFront: z.number().int(), dteBack: z.number().int(), pnlOpen: z.string(), source: z.enum(["cboe","schwab_chain","computed_only"]) })`; `journalResponse = z.object({ snapshots: z.array(snapshotResponse) })`; export both + inferred types. Write `packages/contracts/src/live-greeks.ts` with `liveGreeksResponse` (a small object: `calendarId` uuid + a `legs` array of `{ occSymbol, bsmIv, bsmDelta, bsmGamma, bsmTheta, bsmVega }` strings — shape it to whatever getLiveGreeks returns; keep it minimal but typed). Write `packages/contracts/src/analytics.ts` with `termStructureResponse`/`skewResponse` = `z.object({ observations: z.array(z.unknown()) })` (typed-empty per SPEC §7). Add all to `packages/contracts/src/index.ts`.

    Write the two core use-cases: `getJournal.ts` — `makeGetJournalUseCase({ readJournal: ForReadingJournal })` returning a driver `(calendarId) => deps.readJournal(calendarId)` (export `ForRunningGetJournal` = `ForReadingJournal`-shaped, or re-use ForReadingJournal directly). `getLiveGreeks.ts` — `makeGetLiveGreeksUseCase({ resolveLatest: ForReadingLatestLegObs, ... })` that, given a calendarId, reads the calendar's two legs' latest bsm greeks (it may need `ForListingCalendars`/`ForGettingOpenCalendars` to find the calendar's legs; keep deps explicit and injected). Export both factories from `packages/core/src/journal/index.ts`.

    Write `packages/contracts/src/journal.test.ts` FIRST (RED): assert snapshotResponse accepts a full row including `frontIv:"NaN"`; rejects a row missing `pnlOpen`; journalResponse accepts `{snapshots:[]}`; termStructureResponse accepts `{observations:[]}`. Add minimal use-case tests (in-memory fakes) for getJournal (null passthrough) and getLiveGreeks. Run RED, implement GREEN.
  </action>
  <verify>
    <automated>bun run test --run journal.test && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `packages/contracts/src/journal.ts` exports `snapshotResponse` (18 fields) + `journalResponse`; `analytics.ts` exports `termStructureResponse` + `skewResponse` (grep each)
    - `journal.test.ts` asserts snapshotResponse accepts `frontIv:"NaN"` and rejects a row missing `pnlOpen`; journalResponse accepts empty snapshots
    - `makeGetJournalUseCase` + `makeGetLiveGreeksUseCase` re-exported from journal index (grep)
    - `bun run test --run journal.test` exits 0
  </acceptance_criteria>
  <done>All read-side contracts (journal, live-greeks, typed-empty analytics) and the getJournal/getLiveGreeks use-cases exist with passing RED-first tests.</done>
</task>

<task type="tdd">
  <name>Task 2: getLatestLegObs repo method (Postgres + memory) (RED→GREEN)</name>
  <read_first>
    - packages/adapters/src/postgres/repos/leg-observations.ts (the file being modified — the repo return surface at line 231; the latest-by-time query shape; formatOccSymbol import; map patterns)
    - packages/adapters/src/memory/leg-observations.ts (the file being modified — mirror the resolution method added in plan 05 Task 2)
    - packages/core/src/journal/application/ports.ts (ForReadingLatestLegObs, LegSnapshot from plan 01)
    - .planning/phases/03-calendar-journal-mvp/03-PATTERNS.md (section "leg-observations.ts (extend)")
    - .claude/rules/tdd.md (Postgres repos → testcontainers; memory twin same PR)
  </read_first>
  <behavior>
    - getLatestLegObs(occSymbol): returns the latest leg_observations row (ORDER BY time DESC LIMIT 1) as a LegSnapshot; returns null when no row exists for that symbol.
    - bsm_* and ivRaw map exactly as in resolveLegSnapshot (numeric strings or null; mark/underlyingPrice parsed to number).
  </behavior>
  <action>
    Add `getLatestLegObs: ForReadingLatestLegObs` to `makePostgresLegObservationsRepo` — `select().from(legObservations).where(eq(contract, occSymbol)).orderBy(desc(time)).limit(1)` → map the single row to LegSnapshot (reuse the same mapping helper as plan 05's resolveLegSnapshot; if plan 05 added a private `mapLegSnapshot`, share it). None → ok(null). Add it to the repo `return {...}` and the `PostgresLegObservationsRepo` type. Mirror in `makeMemoryLegObservationsRepo` (memory twin). Extend the leg-observations contract test (or the calendar-snapshots contract suite) to cover the hit/miss of getLatestLegObs. Write the assertion RED first, then implement to GREEN.
  </action>
  <verify>
    <automated>bun run test --run leg-observations.contract && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `packages/adapters/src/postgres/repos/leg-observations.ts` `return {...}` includes `getLatestLegObs` (grep)
    - The contract test asserts getLatestLegObs returns the latest row and null on a miss
    - `bun run test --run leg-observations.contract` exits 0 and `bun run typecheck` exits 0
  </acceptance_criteria>
  <done>Both leg-observations adapters expose getLatestLegObs backing get_live_greeks, with contract coverage.</done>
</task>

<task type="tdd">
  <name>Task 3: journal HTTP route + server wiring (RED→GREEN)</name>
  <read_first>
    - apps/server/src/adapters/http/status.routes.ts (the route-factory analog)
    - apps/server/src/adapters/http/status.routes.test.ts (the route test harness analog)
    - apps/server/src/main.ts (the file being modified — composition root; calendar route mount from plan 03; how use-cases are built + repos available)
    - packages/contracts/src/journal.ts (journalResponse from Task 1)
    - .planning/phases/03-calendar-journal-mvp/03-PATTERNS.md (section "journal.routes.ts (new)" — exact body: 500 on err, 404 when value null, else journalResponse.parse({snapshots: value}))
  </read_first>
  <behavior>
    - GET /api/journal/:calendarId with snapshots → 200 + {snapshots:[...ordered...]}.
    - Unknown calendarId (use-case returns ok(null)) → 404.
    - Known calendar, zero snapshots (ok([])) → 200 + {snapshots:[]}.
    - storage-error → 500.
  </behavior>
  <action>
    Write `apps/server/src/adapters/http/journal.routes.ts` exporting `journalRoutes(getJournal: ForReadingJournal)` returning a Hono router with `router.get("/journal/:calendarId", ...)`: read `c.req.param("calendarId")`, call `getJournal(calendarId)`; `!result.ok` → `c.json({error:"internal"},500)`; `result.value === null` → `c.json({error:"not found"},404)`; else `c.json(journalResponse.parse({ snapshots: result.value }))`. Thin adapter, contract-parsed out.

    Wire into `apps/server/src/main.ts`: build `calendarSnapshotsRepo = makePostgresCalendarSnapshotsRepo(db)`, `getJournal = makeGetJournalUseCase({ readJournal: calendarSnapshotsRepo.readJournal })`, `getLiveGreeks = makeGetLiveGreeksUseCase({...})`, then `app.route("/api", journalRoutes(getJournal))`. Keep existing status + calendar routes + MCP mount. (The getJournal + getLiveGreeks + listCalendars use-cases built here are also passed to the MCP router in plan 07 — leave them in scope as named consts.)

    Write `apps/server/src/adapters/http/journal.routes.test.ts` FIRST (RED): mount with fake getJournal returning ok([...]) / ok(null) / ok([]) / err; assert 200+ordered, 404, 200+empty, 500. Run RED, implement GREEN.
  </action>
  <verify>
    <automated>bun run test --run journal.routes.test && bun run typecheck && bun run lint</automated>
  </verify>
  <acceptance_criteria>
    - `apps/server/src/adapters/http/journal.routes.ts` exports `journalRoutes` and parses `journalResponse` (grep)
    - `journal.routes.test.ts` asserts: ordered 200, unknown→404, empty→200+[], err→500
    - `apps/server/src/main.ts` mounts `journalRoutes(` under `/api` (grep)
    - `bun run test --run journal.routes.test`, `bun run typecheck`, `bun run lint` all exit 0
  </acceptance_criteria>
  <done>GET /api/journal/:calendarId returns the ordered series with correct 404/empty handling; register→snapshot→read works over HTTP.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /api/journal/:calendarId | Untrusted path param `:calendarId` crosses into a DB query. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-14 | Information Disclosure | calendarId enumeration / cross-calendar leak | mitigate | Drizzle parameterized `where(eq(calendarId, ...))`; unknown id → 404 (not 403 — single user, no ownership model in v1, RESEARCH V4); no other calendar's rows are reachable from one id (Task 3). |
| T-03-15 | Tampering | SQL injection via :calendarId | mitigate | Parameterized query only; a non-UUID id matches no calendar → 404; never interpolated (Task 2/3). |
| T-03-16 | Information Disclosure | error body leaking DB internals | mitigate | Route returns flat `{error:"internal"}` / `{error:"not found"}`; no stack/DB message returned (Task 3). |
| T-03-17 | Elevation of Privilege | reading another user's journal | accept | Single-user v1; no per-calendar authz (RESEARCH V4); revisit at multi-user (deferred). |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. |
</threat_model>

<verification>
- `bun run test` (full workspace) green incl. journal contract, getLatestLegObs contract, journal route tests.
- `bun run typecheck` + `bun run lint` green (one shared journalResponse schema; route thin; Drizzle in adapters).
- Manual sanity (optional): register a calendar, let a snapshot write (or seed), `curl /api/journal/:id` returns the ordered array; unknown id → 404.
</verification>

<success_criteria>
- GET /api/journal/:calendarId returns the ordered 18-field snapshot series; 404 unknown; 200+[] empty.
- journal/live-greeks/typed-empty analytics contracts in place for the MCP tools (plan 07).
- get_live_greeks use-case + getLatestLegObs repo method live.
- Full suite green.
</success_criteria>

<output>
Create `.planning/phases/03-calendar-journal-mvp/03-06-SUMMARY.md` when done.
</output>
