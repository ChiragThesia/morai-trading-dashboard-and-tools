---
phase: 39-market-regime-rail-all-rows-as-gauges-teaching-tooltips-rate
plan: 04
type: execute
wave: 3
depends_on: ["39-02", "39-03"]
files_modified: []
autonomous: false
requirements: [GAUGE-01, GAUGE-02, GAUGE-03, GAUGE-04, GAUGE-05]
must_haves:
  truths:
    - "Full web + workspace suites, workspace typecheck, apps/web tsc (no NEW errors vs baseline 8), and lint are all green"
    - "Cross-cutting laws hold: neutral tracks never verdict-colored, RATE_BANDS never a gate input, tooltip copy verbatim, docs bands present"
    - "The change is deployed (git push → Vercel) and the desktop regime rail is verified on morai.wtf"
  artifacts:
    - path: .planning/phases/39-market-regime-rail-all-rows-as-gauges-teaching-tooltips-rate/39-04-SUMMARY.md
      provides: "integration-gate results, deploy identity, and UAT dispositions"
      contains: "GAUGE"
  key_links:
    - from: apps/web/src/components/RegimeBoard.tsx
      to: morai.wtf desktop Overview rail
      via: "git push to main triggers the Vercel web deploy; visual UAT confirms the rendered rail"
      pattern: "regime"
---

<objective>
Prove the whole phase holds together and ship it (GAUGE-01..05). Run the full test suites, both
typechecks, and lint; assert the cross-cutting laws with greps the per-plan tests can't span
(neutral-never-verdict, gate-blind RATE_BANDS, verbatim tooltip copy, docs-before-code bands
present); then deploy via git push (Vercel) and human-verify the desktop regime rail on morai.wtf.

Purpose: the integration + deploy + acceptance gate for the phase. Output: `39-04-SUMMARY.md`
with gate results, deploy identity, and UAT dispositions. No production-code files change here —
this plan runs gates and ships.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-market-regime-rail-all-rows-as-gauges-teaching-tooltips-rate/39-CONTEXT.md
@.planning/phases/39-market-regime-rail-all-rows-as-gauges-teaching-tooltips-rate/39-UI-SPEC.md
@.planning/phases/39-market-regime-rail-all-rows-as-gauges-teaching-tooltips-rate/39-VALIDATION.md

# The surfaces the greps assert against:
@apps/web/src/components/RegimeBoard.tsx
@apps/web/src/components/CotCard.tsx
@docs/architecture/regime-board.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration gate — suites + typechecks + lint + cross-cutting law greps, then deploy</name>
  <files>(no source changes — runs gates and pushes)</files>
  <action>
    Run the full phase gate and record each result in the SUMMARY:
      1. `bun run test` — the whole Vitest workspace (contracts/server/web) green, including the
         new BulletGauge, RegimeBoard, and CotCard suites.
      2. `bun run typecheck` — workspace typecheck (contracts/core/adapters/server) clean.
      3. `cd apps/web && bunx tsc --noEmit` — apps/web has 8 PRE-EXISTING standing tsc errors
         (itemized in 38-07-SUMMARY). The gate passes if the error count is ≤ 8 AND no NEW error
         references a Phase-39 file (BulletGauge.tsx, RegimeBoard.tsx, CotCard.tsx). Record the
         count; do NOT fix or add to the standing errors.
      4. `bun run lint` — clean (no `any`/`as`/`!`, no floating promises introduced).
    Then assert the cross-cutting laws the per-plan tests cannot span, and record each in the SUMMARY:
      - Neutral-never-verdict is covered by the RegimeBoard + CotCard unit assertions (39-02/39-03);
        re-confirm via the full suite run above.
      - GATE-BLIND: `grep -rEn 'RATE_BANDS|RATE_GAUGE_SCALE|COT_GAUGE_SCALE' apps/web/src apps/server/src packages` returns matches ONLY inside `apps/web/src/components/RegimeBoard.tsx` and
        `apps/web/src/components/CotCard.tsx` — never in any gate/regime resolution, hook, server,
        or core file. Any match outside those two display components fails the gate.
      - DOCS-BEFORE-CODE: `grep -Eiq 't10y2y' docs/architecture/regime-board.md && grep -Eiq 't10y3m' docs/architecture/regime-board.md && grep -Eiq '\[ASSUMED\]' docs/architecture/regime-board.md` passes.
      - TOOLTIP VERBATIM: spot-check that a distinctive locked substring for at least one regime,
        one rate, and one COT row is present in the component source (positive grep) — the row-level
        verbatim assertions live in the 39-02/39-03 suites.
    With every gate green, DEPLOY: make the pathspec-scoped commit(s) for the phase's source +
    docs, then `git push` to `main` (Vercel auto-deploys `apps/web`). Record the pushed commit sha
    and the Vercel deploy identity (dashboard/`vercel` CLI) in the SUMMARY — deploy proof is the
    deployed build id + timestamp, not just the sha. Do NOT touch `.planning/STATE.md` or
    `.planning/ROADMAP.md`.
  </action>
  <verify>
    <automated>bun run test && bun run typecheck && bun run lint && cd apps/web && bunx tsc --noEmit; echo "gate-exit=$?"</automated>
  </verify>
  <done>Full suite + workspace typecheck + lint green; apps/web tsc shows no NEW errors beyond the 8-error baseline; the gate greps (gate-blind, docs-present, verbatim copy) pass; the phase is committed (pathspec-scoped) and pushed, with the Vercel deploy identity recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Desktop rail UAT — all rows as gauges + teaching tooltips on morai.wtf</name>
  <action>
    Pause for the human visual verification. Present the deployed morai.wtf Overview (desktop
    width, left Market Regime rail) and walk the operator through the how-to-verify items below
    (rates neutral vs banded, COT direction tint, four-part tooltips, no verdict color on any
    neutral track, one visual density across all three blocks). Do not mark the phase complete
    until the operator responds with the resume signal.
  </action>
  <what-built>
    The Market Regime desktop rail now renders every row as a bullet gauge with a teaching ⓘ
    tooltip: the 4 regime indicators (banded, unchanged look), the 6 rates rows (Fed Funds/SOFR/
    1M/3M as neutral gray-marker position tracks; 10Y−2Y/10Y−3M as banded inversion tracks), and
    the 5 COT rows (neutral, marker green when net long / red when net short). Every ⓘ now teaches
    WHAT / WHY / BANDS / SOURCE. Deployed to morai.wtf via Vercel.
  </what-built>
  <how-to-verify>
    On a DESKTOP browser at https://morai.wtf (Overview tab, left Market Regime rail):
    1. Rates block: Fed Funds / SOFR / 1M / 3M each show a gauge with a flat GRAY marker on a plain
       track — NO amber/red/green band coloring (position only). 10Y−2Y and 10Y−3M show a banded
       track (amber/red segments) with the marker colored by where today's spread sits.
    2. COT block: each class row shows a gauge whose marker is GREEN when net long / RED when net
       short — never amber; the ▲/▼ week-over-week arrow is still present to the right.
    3. Hover the ⓘ on one regime row, one rate row, and one COT row: the tooltip reads four parts —
       a plain-English WHAT, a WHY tied to SPX calendar trading, a BANDS line (thresholds for
       banded rows, "position only" for neutral rows), and a quiet SOURCE line at the bottom.
    4. Sanity: no neutral (Fed Funds/SOFR/1M/3M or any COT) row shows a warning/crisis color; all
       three blocks (regime / rates / COT) read at one visual density, not three.
    Report "approved" if all four hold, or describe what's off (which row, what you saw).
  </how-to-verify>
  <resume-signal>Type "approved" or describe the issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client display constants → gate/server | RATE_BANDS/*_GAUGE_SCALE must be confined to the two display components |
| local green suites → deployed rail | unit-green must be confirmed on the real deployed desktop rail (jsdom can't render the tint a human reads) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-04 | Tampering (gate leak) | RATE_BANDS/*_GAUGE_SCALE | high | mitigate | Whole-tree grep confirms the display constants appear ONLY in RegimeBoard.tsx + CotCard.tsx — never in a gate/server/core/hook path |
| T-39-07 | Spoofing (green suite hides real regression) | jsdom vs deployed rail | medium | mitigate | The blocking human-verify UAT confirms the rendered tint/typography on morai.wtf — the acceptance bar the unit suite can't reach (green-suite lesson) |
| T-39-08 | Tampering (standing tsc drift) | apps/web tsc | medium | mitigate | Gate records the apps/web error count and fails on any NEW error referencing a Phase-39 file; the 8-error baseline is neither fixed nor grown |
| T-39-SC | Tampering | npm installs | low | accept | Zero new dependencies across the phase |
</threat_model>

<verification>
- `bun run test` + `bun run typecheck` + `bun run lint` green; `cd apps/web && bunx tsc --noEmit`
  shows no NEW error vs the 8-error baseline.
- Gate greps: RATE_BANDS/*_GAUGE_SCALE confined to the two display components; t10y2y/t10y3m +
  [ASSUMED] present in regime-board.md; a locked tooltip substring present per block.
- Deployed to morai.wtf; the desktop rail human-verify UAT returns "approved".
</verification>

<success_criteria>
Every gate is green, the client display bands never leave the two components, the phase is
deployed, and a human confirms the desktop regime rail renders all rows as gauges with teaching
tooltips and no verdict color on any neutral track.
</success_criteria>

<output>
Create `.planning/phases/39-market-regime-rail-all-rows-as-gauges-teaching-tooltips-rate/39-04-SUMMARY.md` when done.

Execution notes for the executor: pathspec-scoped commits only; do NOT touch `.planning/STATE.md`
or `.planning/ROADMAP.md`. DESKTOP-rail only — UAT is on desktop morai.wtf (mobile is out of
scope per the phase boundary).
</output>
