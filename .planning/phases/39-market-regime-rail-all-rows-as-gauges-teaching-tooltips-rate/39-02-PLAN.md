---
phase: 39-market-regime-rail-all-rows-as-gauges-teaching-tooltips-rate
plan: 02
type: tdd
wave: 2
depends_on: ["39-01"]
files_modified:
  - apps/web/src/components/RegimeBoard.tsx
  - apps/web/src/components/RegimeBoard.test.tsx
autonomous: true
requirements: [GAUGE-02, GAUGE-04, GAUGE-05]
must_haves:
  truths:
    - "Fed Funds/SOFR/1M/3M render NEUTRAL gauges — bg-dim marker on a plain bg-line2 track, NO band segments, NO verdict color, ever"
    - "10Y−2Y/10Y−3M render BANDED gauges from client-side RATE_BANDS constants (calm >0 / warning ≤0 / crisis ≤−0.50) — display-only, never a gate input"
    - "Every rate row + the 4 regime rows show a 4-part WHAT/WHY/BANDS/SOURCE tooltip with copy verbatim from the UI-SPEC"
    - "The regime rows keep server source/rationale as the SOURCE line; the picker gate/regime-gate take zero new inputs"
  artifacts:
    - path: apps/web/src/components/RegimeBoard.tsx
      provides: "RATE_BANDS + RATE_GAUGE_SCALE constants; RateGaugeRow (neutral 4 + banded 2); 4-part TooltipContent + static copy map"
      contains: "RATE_BANDS"
  key_links:
    - from: apps/web/src/components/RegimeBoard.tsx
      to: apps/web/src/components/system/BulletGauge.tsx
      via: "RateGaugeRow renders <BulletGauge variant=\"neutral\"|\"banded\"> for the track"
      pattern: "BulletGauge"
    - from: apps/web/src/components/RegimeBoard.tsx
      to: docs/architecture/regime-board.md
      via: "RATE_BANDS thresholds cite the t10y2y/t10y3m evidence-table rows added in 39-01"
      pattern: "RATE_BANDS"
---

<objective>
Turn the rates block into gauges (GAUGE-02) and rewrite every RegimeBoard-owned ⓘ tooltip to
teach (GAUGE-04). The four money-rate rows (Fed Funds, SOFR, 1M, 3M) become NEUTRAL
position-only tracks — a marker on a fixed 0–8% range with NO band segments and NO verdict
color, upholding the evidence law. The two yield-curve spreads (10Y−2Y, 10Y−3M) become BANDED
gauges from client-side `RATE_BANDS` constants documented in 39-01 — display-only, gate stays
blind (GAUGE-05 encoding). Then the four existing regime rows and the six rate rows all get the
four-part WHAT/WHY/BANDS/SOURCE tooltip, copy rendered VERBATIM from the UI-SPEC's locked payload.

Purpose: the rates block reads as one gauge rhythm with the regime rows, and a glance at any ⓘ
teaches what the dial means. Output: `RegimeBoard` gains `RATE_BANDS`/`RATE_GAUGE_SCALE`, a
`RateGaugeRow`, a static tooltip-copy map, and a 4-part `TooltipContent`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-market-regime-rail-all-rows-as-gauges-teaching-tooltips-rate/39-CONTEXT.md
@.planning/phases/39-market-regime-rail-all-rows-as-gauges-teaching-tooltips-rate/39-UI-SPEC.md

# The panel — RATES array (~322-329), fmtRate (~331-335), RateRow to replace (~337-348), the Row
# tooltip block to upgrade (~177-204), GAUGE_SCALE/MARKER_CLASSES precedent, BulletGauge (from 39-01):
@apps/web/src/components/RegimeBoard.tsx
# The suite to extend (rate-chip-{id} value assertions at ~359-373 stay green; add gauge + tooltip tests):
@apps/web/src/components/RegimeBoard.test.tsx
# The docs bands 39-01 wrote — RATE_BANDS thresholds must match these documented values:
@docs/architecture/regime-board.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RATE_BANDS + RATE_GAUGE_SCALE + RateGaugeRow (neutral 4, banded 2) — RED→GREEN</name>
  <files>apps/web/src/components/RegimeBoard.tsx, apps/web/src/components/RegimeBoard.test.tsx</files>
  <behavior>
    - Fed Funds/SOFR/1M/3M (`DFF`/`SOFR`/`DGS1MO`/`DGS3MO`): `rate-gauge-{id}` role=meter, min 0 / max 8, marker `bg-dim`, and NO band-segment children (`:scope > div` length 1). Marker is bg-dim regardless of value — never bg-txt/bg-amber/bg-down.
    - 10Y−2Y (`T10Y2Y`): banded, axis −1.5..2.5; a value ≤ 0 puts the marker in the warning segment, a value ≤ −0.50 in the crisis segment; band segments present. 10Y−3M (`T10Y3M`): banded, axis −2.0..3.0, same band thresholds.
    - aria-valuetext for a neutral row states the value + "position" (never a band word like calm/warning/crisis); for a banded row states value + band.
    - The printed value line (`rate-chip-{id}` textContent) is unchanged from today (fmtRate, e.g. "4.33%").
  </behavior>
  <action>
    Add two named-constant maps next to `GAUGE_SCALE`, each with an inline comment citing its
    rationale (mirror the existing GAUGE_SCALE comment discipline — "visual axis, NOT semantic
    thresholds" for the scale; "display-only client band, gate BLIND" for RATE_BANDS):
    `RATE_GAUGE_SCALE` (per-id `{min,max}` from the UI-SPEC Gauge-scale table: DFF/SOFR/DGS1MO/
    DGS3MO 0..8; T10Y2Y −1.5..2.5; T10Y3M −2.0..3.0) and `RATE_BANDS` (per banded id
    `{warn: 0.0, crisis: -0.50}` for T10Y2Y and T10Y3M, matching the evidence-table rows 39-01
    documented). Mark each rate id neutral vs banded via the RATES entries (extend the existing
    `RATES` array with a `variant: "neutral" | "banded"` field, or a lookup — no `as`).
    Build `RateGaugeRow` to REPLACE `RateRow`: keep the existing label + value line (`rate-chip-{id}`
    testid, label at 10px, value at 11px `font-mono tabular-nums`), then render `<BulletGauge>`
    below it. For a neutral id: `variant="neutral"`, min/max from RATE_GAUGE_SCALE,
    `markerColorClass="bg-dim"` ALWAYS, `ariaValueText={`${value} — position`}`, testId
    `rate-gauge-{id}`, markerTestId `rate-gauge-marker-{id}`. For a banded id: `variant="banded"`,
    bandWarn/bandCrisis from RATE_BANDS, `markerColorClass` from `MARKER_CLASSES` keyed on the band
    the value falls in (reuse the existing calm/warning/crisis → bg-txt/bg-amber/bg-down mapping by
    computing the band from RATE_BANDS: value > warn → calm, value ≤ crisis → crisis, else warning —
    a plain client-side classifier for the display band, matching the documented thresholds), and
    `ariaValueText={`${value} — ${band}`}`. Parse the numeric value from `fmtRate`'s underlying
    macro point (reuse the existing `data[id]` latest-point access already in `fmtRate`; factor a
    tiny `latestValue(data, id): number | null` helper so both the printed string and the gauge
    read one source — no double parse, no `!`). A row with no macro point renders the value dash
    and omits the gauge (never a marker at a fabricated 0 — catch #26 honesty). Update the rates
    grid wrapper (`regime-rates-row`) so each cell is a `RateGaugeRow` (2-line label/value + track);
    keep `py-1.5`/spacing per the UI-SPEC so all three blocks read as one rhythm.
    RED FIRST: extend RegimeBoard.test.tsx with the behavior block — neutral markers are bg-dim and
    the neutral track has no band-segment children; the two curve rows are banded with correct
    segment membership at boundary values (e.g. a −0.60 T10Y2Y → crisis, a −0.20 → warning, a
    +0.50 → calm); a NEUTRAL row's marker is NEVER bg-amber/bg-down/bg-txt for any value (the
    evidence-law assertion); rate-chip value strings unchanged. Confirm RED, then implement to GREEN.
    The existing `rate-chip-{id}` value assertions must still pass. Do NOT feed RATE_BANDS into any
    gate/regime resolution — they are local display constants only.
  </action>
  <verify>
    <automated>bun run test -- --run apps/web/src/components/RegimeBoard.test.tsx</automated>
  </verify>
  <done>The 4 money-rate rows render neutral bg-dim gauges with no band segments and no possible verdict color; 10Y−2Y/10Y−3M render banded gauges from RATE_BANDS at the documented thresholds; printed rate values are unchanged; RATE_BANDS never reaches a gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Four-part teaching tooltips — regime rows (upgrade) + rate rows (new) — RED→GREEN</name>
  <files>apps/web/src/components/RegimeBoard.tsx, apps/web/src/components/RegimeBoard.test.tsx</files>
  <behavior>
    - The `TooltipContent` inner stack renders FOUR lines in order: WHAT (`text-txt`), WHY (`text-txt`), BANDS (`text-muted-foreground`), SOURCE (`text-dim`).
    - For the 4 regime rows: WHAT/WHY/BANDS come from a static copy map keyed by indicator id (verbatim UI-SPEC copy); SOURCE renders the server `indicator.source` then `indicator.rationale` (unchanged) — the existing "tooltip renders source + rationale verbatim" test still passes.
    - For the 6 rate rows: a new `rate-why-{id}` ⓘ trigger opens a 4-part tooltip; WHAT/WHY/BANDS/SOURCE all from the static rate copy map (verbatim UI-SPEC copy — rate rows have no server source).
    - Neutral rate rows' BANDS line states the range/"position only" wording; banded rate rows' BANDS line states the calm/warning/crisis thresholds — exactly as the UI-SPEC copy reads.
  </behavior>
  <action>
    Change the shared `TooltipContent` inner `<div>` from two `<span>`s to four, per the UI-SPEC
    "Tooltip layout" block: WHAT and WHY at `text-txt`, BANDS at `text-muted-foreground`, SOURCE at
    `text-dim` (keep `max-w-xs flex-col gap-1 font-mono text-xs leading-[1.45]`). Author a single
    static copy map (e.g. `TOOLTIP_COPY: Record<string, { what: string; why: string; bands: string }>`)
    keyed by every regime id AND every rate id, with the WHAT/WHY/BANDS strings copied VERBATIM from
    the UI-SPEC "Tooltip Copy (LOCKED)" section — do not paraphrase, reflow, or invent financial
    claims. For rate rows add a fourth `source` string per id (the UI-SPEC's static SOURCE line,
    e.g. "FRED series DFF, daily."); regime rows instead render the server `indicator.source` +
    `indicator.rationale` as the SOURCE line (unchanged). Move the ⓘ `Badge` glyph to `text-[10px]`
    (from `text-[9px]`) per the Typography table. Wire the rate rows' new ⓘ trigger inside
    `RateGaugeRow` (same `TooltipProvider`/`Tooltip`/`TooltipTrigger`/`TooltipContent` idiom as the
    regime `Row`), `data-testid="rate-why-{id}"`, `aria-label="{label} explanation"`. Factor the
    4-part TooltipContent into one small local component both callers use so the copy path is
    single-sourced. IMPORTANT (comment-text discipline): the verify greps POSITIVELY for copy
    presence; keep the strings only in the copy map, not duplicated in prose comments.
    RED FIRST: extend RegimeBoard.test.tsx — hovering `regime-why-hy-oas` shows its WHAT sentence
    AND still shows the server source + rationale (SOURCE line preserved); hovering `rate-why-T10Y2Y`
    shows its WHAT + the banded BANDS wording; hovering `rate-why-DFF` shows its WHAT + the neutral
    "position only" BANDS wording. Assert one distinctive verbatim substring per checked row so a
    paraphrase fails. Confirm RED, implement to GREEN.
  </action>
  <verify>
    <automated>bun run test -- --run apps/web/src/components/RegimeBoard.test.tsx</automated>
  </verify>
  <done>Regime and rate ⓘ tooltips render the 4-part WHAT/WHY/BANDS/SOURCE structure with verbatim UI-SPEC copy; regime rows keep server source/rationale as SOURCE; the existing provenance test still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client-side RATE_BANDS → picker/regime gate | display constants must never cross into gate/verdict resolution |
| neutral row → verdict color | a colorless (no-evidence) row must never render a verdict marker |
| locked copy → rendered tooltip | financial explanations are authored, not improvised at execute time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-04 | Tampering (gate leak) | RATE_BANDS | high | mitigate | RATE_BANDS/RATE_GAUGE_SCALE are local display constants in RegimeBoard.tsx only — never passed to usePicker/useRegimeBoard/gate resolution; the picker gate stays blind (grep-asserted in 39-04) |
| T-39-02 | Spoofing (false verdict) | neutral rate gauges | high | mitigate | Neutral markers are `bg-dim` for ALL values with no band segments; a test asserts a neutral marker is never bg-amber/bg-down/bg-txt — "no verdict" can never read as calm/warning/crisis |
| T-39-05 | Tampering (fabricated explanation) | tooltip copy | medium | mitigate | Copy is a single static map rendered verbatim from the UI-SPEC LOCKED payload; tests assert distinctive verbatim substrings so a paraphrase fails |
| T-39-06 | Spoofing (marker at fabricated 0) | missing macro point | medium | mitigate | A row with no macro point renders the value dash and omits the gauge — never a marker at a guessed 0 (catch #26) |
| T-39-SC | Tampering | npm installs | low | accept | Zero new dependencies — reuses Tooltip/Badge already in RegimeBoard |
</threat_model>

<verification>
- `bun run test -- --run apps/web/src/components/RegimeBoard.test.tsx` green (existing rate-chip value + regime provenance tests still pass alongside the new gauge + 4-part-tooltip tests).
- Neutral markers assert `bg-dim` and no band-segment children; banded curve rows assert correct segment membership at boundary values.
- Tooltip copy asserted by verbatim substring (regime + rate rows).
- `RATE_BANDS` appears only in RegimeBoard.tsx display code — the gate-blind grep runs in 39-04's gate.
</verification>

<success_criteria>
The rates block reads as bullet gauges — four neutral position-only tracks and two evidence-banded
curve tracks — and every RegimeBoard ⓘ teaches with four-part verbatim copy, with the picker gate
still blind to the client-side rate bands.
</success_criteria>

<output>
Create `.planning/phases/39-market-regime-rail-all-rows-as-gauges-teaching-tooltips-rate/39-02-SUMMARY.md` when done.

Execution notes for the executor: pathspec-scoped commits only (this plan's `files_modified`);
do NOT touch `.planning/STATE.md` or `.planning/ROADMAP.md`. DESKTOP-rail only — do not touch
`MobileMarketSection` or any mobile component. Runs in Wave 2 in parallel with 39-03 (COT), which
owns a disjoint file set (`CotCard.tsx`) — do not edit CotCard here.
</output>
