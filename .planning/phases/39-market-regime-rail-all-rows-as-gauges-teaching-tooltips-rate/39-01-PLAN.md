---
phase: 39-market-regime-rail-all-rows-as-gauges-teaching-tooltips-rate
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - docs/architecture/regime-board.md
  - apps/web/src/components/system/BulletGauge.tsx
  - apps/web/src/components/system/BulletGauge.test.tsx
  - apps/web/src/components/system/index.tsx
  - apps/web/src/components/RegimeBoard.tsx
  - apps/web/src/components/RegimeBoard.test.tsx
autonomous: true
requirements: [GAUGE-01, GAUGE-05]
must_haves:
  truths:
    - "docs/architecture/regime-board.md documents t10y2y/t10y3m inversion bands BEFORE any component encodes them (docs-before-code)"
    - "A shared BulletGauge renders a role=meter track with banded and neutral variants, marker clamped to the axis"
    - "The 4 regime rows render through BulletGauge with the identical DOM/testids/aria as today — existing RegimeBoard gauge tests pass UNMODIFIED"
  artifacts:
    - path: apps/web/src/components/system/BulletGauge.tsx
      provides: "presentational bullet-gauge track; banded|neutral variants; clampedAxisPct math; role=meter a11y"
      contains: "role=\"meter\""
    - path: apps/web/src/components/system/BulletGauge.test.tsx
      provides: "unit contract for BulletGauge (banded segments, neutral no-segments, marker clamp, aria, testids)"
      contains: "BulletGauge"
    - path: docs/architecture/regime-board.md
      provides: "t10y2y/t10y3m evidence-table rows with cited rationale + [ASSUMED] crisis tier"
      contains: "t10y2y"
  key_links:
    - from: apps/web/src/components/RegimeBoard.tsx
      to: apps/web/src/components/system/BulletGauge.tsx
      via: "Row renders <BulletGauge variant=\"banded\"> for the track, keeping its own label/value line"
      pattern: "BulletGauge"
---

<objective>
Lay the two foundations Phase 39 builds on: (1) the docs-before-code evidence entry for the
yield-curve inversion bands (GAUGE-05), and (2) a shared `BulletGauge` presentational track
extracted from `RegimeBoard`'s existing gauge markup, with the four regime rows refactored onto
it at ZERO visual change (GAUGE-01). Both later waves (rates in 39-02, COT in 39-03) consume
`BulletGauge`; the banded rate rows in 39-02 consume the evidence doc written here.

Purpose: one gauge track implementation with three callers, and a documented evidence trail for
the only new bands this phase ships. Output: `docs/architecture/regime-board.md` extended;
`system/BulletGauge.tsx` + its unit test; `RegimeBoard` rewired with its suite green UNMODIFIED
(the regression guard that proves "zero visual change").
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-market-regime-rail-all-rows-as-gauges-teaching-tooltips-rate/39-CONTEXT.md
@.planning/phases/39-market-regime-rail-all-rows-as-gauges-teaching-tooltips-rate/39-UI-SPEC.md

# The gauge markup + math to extract (Row lines ~114-126, ~217-243), GAUGE_SCALE, MARKER_CLASSES:
@apps/web/src/components/RegimeBoard.tsx
# The regression-guard suite — the gauge/aria/marker/segment assertions must stay green unmodified:
@apps/web/src/components/RegimeBoard.test.tsx
# The extraction target directory + barrel (ChipRail/Button precedent):
@apps/web/src/components/system/index.tsx
# The evidence-table doc to extend (Admitted indicators table + [ASSUMED] disclosure pattern):
@docs/architecture/regime-board.md
# Threshold citation source (READ-ONLY reference — never edited): "Inverted curves ... have
# historically preceded recessions by 6-12 months":
@knowledge-base/grouped-data/macro_rates.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Docs-before-code — yield-curve inversion bands in the evidence table (GAUGE-05)</name>
  <files>docs/architecture/regime-board.md</files>
  <action>
    Per the docs-before-code rule (workflow.md) and CONTEXT's evidence discipline, add two rows
    to the "Admitted indicators" table in `docs/architecture/regime-board.md` for `t10y2y` and
    `t10y3m`, mirroring the table's existing six-column format (id / Formula-inputs / Bands /
    Threshold rationale / Source / Verified). Use the PROPOSED rows verbatim from the UI-SPEC's
    "Yield-curve evidence-table addition" section: bands `> 0.0` calm / `≤ 0.0` warning /
    `≤ -0.50` crisis, with the −0.50 crisis tier marked `[ASSUMED]` (structural interpolation,
    not independently backtested — same disclosure class as the existing `vix9d-vix`/`hy-oas`
    rows). Cite the zero-crossing (inversion) recession-precursor rationale to
    `knowledge-base/grouped-data/macro_rates.md`, and note for `t10y3m` that the 3-month leg is
    the spread the NY Fed's recession-probability model tracks. Add a one-sentence note under the
    table (or extend "Known limitations") stating these two are a CLIENT-VISUAL-ONLY display band:
    rates come from `useMacro`/`macro_observations`, there is no server-side regime indicator for
    these ids, and the picker gate stays BLIND to them (no new gate inputs) — the same "display
    only this phase" posture the doc already uses. Do NOT edit `knowledge-base/` (read-only). If
    `docs/TOPIC-MAP.md` already lists `regime-board.md`, no TOPIC-MAP change is needed (extending
    an existing doc's table, not adding a file) — verify and leave it untouched if so.
  </action>
  <verify>
    <automated>grep -Eiq 't10y2y' docs/architecture/regime-board.md && grep -Eiq 't10y3m' docs/architecture/regime-board.md && grep -Eiq '\[ASSUMED\]' docs/architecture/regime-board.md && grep -Eiq 'macro_rates' docs/architecture/regime-board.md && echo DOCS_OK</automated>
  </verify>
  <done>The evidence table has t10y2y + t10y3m rows with cited rationale and the [ASSUMED] −0.50 crisis-tier disclosure, plus the client-visual-only / gate-blind note. No component encodes a band yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract system/BulletGauge.tsx (banded + neutral variants) — RED→GREEN</name>
  <files>apps/web/src/components/system/BulletGauge.tsx, apps/web/src/components/system/BulletGauge.test.tsx, apps/web/src/components/system/index.tsx</files>
  <behavior>
    - Renders the outer `<div role="meter">` track with the IDENTICAL className string RegimeBoard.tsx:219 uses today — `relative h-1.5 w-full overflow-hidden rounded-full bg-line2` (CHECKER WARNING 1: the leading `relative` is load-bearing — segments/marker are `absolute inset-y-0` children that lose their positioning context without it, and the kept-unmodified regression suite does NOT assert this className, so dropping it passes tests but breaks the real page; copy the string verbatim from source, do not retype) — carrying `data-testid={testId}`, `aria-valuemin={min}`, `aria-valuemax={max}`, `aria-valuenow` = value CLAMPED to [min,max], `aria-valuetext={ariaValueText}` (passed in, unclamped truth), `aria-label={ariaLabel}`.
    - variant "banded": renders exactly two segment divs then the marker div, in that DOM order — [0] warn segment (`bg-amber/30`, left=clampedAxisPct(bandWarn), width=crisisPct−warnPct), [1] crisis segment (`bg-down/30`, left=crisisPct, width=100−crisisPct), [2] marker (`markerColorClass`, left=clampedAxisPct(value)); band positions clamp like the marker (no negative CSS width — the CR-01 regression).
    - variant "neutral": renders NO band segment divs — only the marker div (`markerColorClass`, left=clampedAxisPct(value)). `querySelectorAll(":scope > div")` returns exactly one child.
    - marker carries `data-testid={markerTestId}`.
    - marker `left` is always a clamped percentage in [0,100] for any value/min/max (fast-check).
  </behavior>
  <action>
    Create `apps/web/src/components/system/BulletGauge.tsx` — a presentational-only component. Move
    the `axisPct` and `clampedAxisPct` helpers out of `RegimeBoard.tsx` into this file (they become
    BulletGauge-internal; RegimeBoard will import the component, not the helpers). Props:
    `min: number`, `max: number`, `value: number`, `variant: "banded" | "neutral"`,
    `bandWarn?: number`, `bandCrisis?: number` (required when variant is "banded" — narrow via an
    explicit branch, never `!`), `markerColorClass: string` (caller-computed Tailwind `bg-*`),
    `ariaLabel: string`, `ariaValueText: string`, `testId: string`, `markerTestId: string`.
    Reproduce the EXACT existing gauge markup so the refactored regime rows stay byte-identical:
    the meter `<div>` is the track; banded renders the warn div, crisis div, then marker div as its
    three direct children (same classNames + inline `left`/`width` as `RegimeBoard.tsx` today);
    neutral renders only the marker div. aria-valuenow uses the clamped value (WR-01 meter
    contract); aria-valuetext is passed in verbatim. CRITICAL testid contract: the meter root uses
    `testId` and the marker uses the separate `markerTestId` prop (NOT derived) — this lets
    RegimeBoard pass `regime-gauge-{id}` + `regime-gauge-marker-{id}` and keep its existing tests
    unmodified. No `any`/`as`/`!`; keep `variant` exhaustive.
    Write `BulletGauge.test.tsx` FIRST (RED) asserting the behavior block above: a banded render
    exposes `[warn, crisis, marker]` children with the documented left/width and the marker color;
    a neutral render exposes ONE child (marker only, no band segments); aria-valuenow clamps while
    aria-valuetext carries the raw string; a fast-check property proves the marker `left` stays in
    [0,100] and banded segment left/width never go negative for arbitrary value/min/max (mirror
    RegimeBoard.test.tsx's existing clamp property). Run it, confirm it fails for the RIGHT reason
    (missing component), then implement to green. Export `BulletGauge` (+ its props type) from
    `apps/web/src/components/system/index.tsx` alongside `ChipRail`/`Button`.
  </action>
  <verify>
    <automated>bun run test -- --run apps/web/src/components/system/BulletGauge.test.tsx</automated>
  </verify>
  <done>BulletGauge renders a role=meter track with banded (warn+crisis+marker) and neutral (marker-only) variants, clamps marker + band segments to [0,100], carries separate testId/markerTestId, and is exported from the system barrel; its unit + fast-check tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Refactor RegimeBoard Row onto BulletGauge — zero visual change (regression-guarded)</name>
  <files>apps/web/src/components/RegimeBoard.tsx, apps/web/src/components/RegimeBoard.test.tsx</files>
  <behavior>
    - Every existing RegimeBoard.test.tsx assertion still passes with the test file UNMODIFIED: `regime-gauge-{id}` role=meter, aria-valuenow=String(value), aria-valuetext=`{value} — {band}`, aria-label=`{label} gauge`; `:scope > div` order [warn, crisis, marker]; `regime-gauge-marker-{id}` color = MARKER_CLASSES[band]; band segment left positions from bandWarn/bandCrisis; the fast-check clamp property.
    - The live-display path (Phase 38 liveValue/liveBand) is preserved — the marker/aria still reflect the live value + client-recomputed band when present.
  </behavior>
  <action>
    Replace the inline `<div role="meter">…</div>` block inside `Row` (the warn/crisis/marker
    markup) with `<BulletGauge variant="banded" min={scale.min} max={scale.max} value={value}
    bandWarn={indicator.bandWarn} bandCrisis={indicator.bandCrisis}
    markerColorClass={MARKER_CLASSES[displayBand]} ariaLabel={`${indicator.label} gauge`}
    ariaValueText={`${value.toFixed(2)} — ${displayBand}`} testId={`regime-gauge-${indicator.id}`}
    markerTestId={`regime-gauge-marker-${indicator.id}`} />`. Keep `Row`'s own label + value line,
    the `GAUGE_SCALE`/`scale` fallback, the clamped-value computation for aria, and the entire
    live-display overlay (`liveValue`/`liveBand`, `displayBand`, `value`) exactly as today — only
    the track sub-tree moves into BulletGauge. Delete the now-unused local `axisPct`/`clampedAxisPct`
    from `RegimeBoard.tsx` (they live in BulletGauge now); remove only imports/locals this change
    orphans. Do NOT modify `RegimeBoard.test.tsx` — its green run IS the zero-visual-change proof
    (project TDD note: a pure refactor keeps the existing suite green rather than adding a RED
    test). If any assertion fails, the extraction diverged from the original markup — fix
    BulletGauge/Row to match, never edit the test to fit. Leave `RateRow`/`RATES` and the rates
    grid untouched (39-02 owns them); leave the freshness footer, GateChip, and the three data
    sources untouched.
  </action>
  <verify>
    <automated>bun run test -- --run apps/web/src/components/RegimeBoard.test.tsx</automated>
  </verify>
  <done>RegimeBoard's four regime rows render through BulletGauge (banded) with identical DOM, testids, aria, marker colors, and band-segment positions; the existing RegimeBoard suite passes UNMODIFIED; axisPct/clampedAxisPct no longer live in RegimeBoard.tsx.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documented evidence → encoded band | a band may only ship after its rationale is in the evidence doc (evidence law) |
| shared presentational component → existing regime rows | the extraction must not change rendered DOM/behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-01 | Repudiation (undocumented band) | yield-curve bands | high | mitigate | Bands are documented in regime-board.md's evidence table WITH cited rationale + [ASSUMED] disclosure BEFORE Task 2/39-02 encode them (docs-before-code); no undocumented verdict ships |
| T-39-02 | Tampering (silent visual regression) | BulletGauge extraction | high | mitigate | RegimeBoard.test.tsx (meter role, aria, marker color, band-segment positions, clamp property) stays UNMODIFIED and green — a divergent extraction fails the suite, not the test |
| T-39-03 | Information disclosure | knowledge-base edit | low | mitigate | knowledge-base/ is read-only reference — only cited, never edited (data discipline) |
| T-39-SC | Tampering | npm installs | low | accept | Zero new dependencies — extraction reuses existing tokens + Testing Library |
</threat_model>

<verification>
- `bun run test -- --run apps/web/src/components/system/BulletGauge.test.tsx apps/web/src/components/RegimeBoard.test.tsx` green.
- `git diff --quiet apps/web/src/components/RegimeBoard.test.tsx` (test file UNMODIFIED — the zero-visual-change guard).
- `grep -Eiq 't10y2y' docs/architecture/regime-board.md` and `t10y3m` + `[ASSUMED]` present.
- No `any`/`as`/`!` introduced (lint clean, deferred to 39-04 full gate).
</verification>

<success_criteria>
The evidence doc carries the two yield-curve rows; a shared BulletGauge (banded + neutral) is
exported from the system barrel; the four regime rows render through it with the existing
RegimeBoard suite passing UNMODIFIED — the constructive proof of zero visual change.
</success_criteria>

<output>
Create `.planning/phases/39-market-regime-rail-all-rows-as-gauges-teaching-tooltips-rate/39-01-SUMMARY.md` when done.

Execution notes for the executor: pathspec-scoped commits only (the files in this plan's
`files_modified`); do NOT touch `.planning/STATE.md` or `.planning/ROADMAP.md`. This phase is
DESKTOP-rail only — do not touch mobile components (`MobileMarketSection` is a separate
stat-grid, out of scope per the phase boundary).
</output>
