---
phase: 13-cot-adapter
plan: 06
type: execute
wave: 5
depends_on: [13-04]
files_modified:
  - apps/server/src/adapters/http/analytics.routes.ts
  - apps/server/src/adapters/http/analytics.routes.test.ts
  - apps/server/src/adapters/mcp/server.ts
  - apps/server/src/adapters/mcp/mcp.test.ts
  - apps/server/src/main.ts
autonomous: true
requirements: [COT-02]

must_haves:
  truths:
    - "GET /api/analytics/cot returns a JSON array validated by the shared cotResponse Zod contract (the net-per-class series), backed by the makeGetCotUseCase"
    - "MCP get_cot returns the SAME cotResponse payload from the SAME use-case (MCP-02 — route and MCP ship together over one contract)"
    - "Both surfaces return a contract-valid EMPTY array (not an error) when no observations exist"
  artifacts:
    - path: "apps/server/src/adapters/http/analytics.routes.ts"
      provides: "GET /api/analytics/cot route over cotResponse"
      contains: "/cot"
    - path: "apps/server/src/adapters/mcp/server.ts"
      provides: "get_cot MCP tool over cotResponse"
      contains: "get_cot"
  key_links:
    - from: "apps/server/src/adapters/http/analytics.routes.ts"
      to: "packages/core makeGetCotUseCase"
      via: "route handler calls the injected ForRunningGetCot and serializes cotResponse"
      pattern: "get_cot|getCot"
---

<objective>
Expose the COT series on both read surfaces in one change (MCP-02): `GET /api/analytics/cot`
(Hono route over the shared `cotResponse` Zod contract) and the `get_cot` MCP tool — both backed by
the single `makeGetCotUseCase` (13-04). Mirror the skew / term-structure / gex route+MCP trio exactly.
Read-only, public CFTC data (no auth gating beyond the existing analytics group convention).
Implements COT-02 + MCP-02, per D-10.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-cot-adapter/13-CONTEXT.md
@.planning/phases/13-cot-adapter/13-RESEARCH.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/typescript.md

# Analogs to mirror (the skew/term-structure/gex trio):
@apps/server/src/adapters/http/analytics.routes.ts
@apps/server/src/adapters/mcp/server.ts
@packages/contracts/src/cot.ts
</context>

<tasks>

<task type="execute">
  <name>Task 1: GET /api/analytics/cot route</name>
  <files>apps/server/src/adapters/http/analytics.routes.ts, apps/server/src/adapters/http/analytics.routes.test.ts, apps/server/src/main.ts</files>
  <read_first>
    - apps/server/src/adapters/http/analytics.routes.ts — the skew/term-structure/gex route shape: inject the use-case, call it, map Result → JSON, validate against the shared contract. THE template.
    - apps/server/src/adapters/http/analytics.routes.test.ts — the route test pattern (fake use-case returning the contract series, incl. the empty-array case).
    - packages/contracts/src/cot.ts — cotResponse (the response schema).
    - apps/server/src/main.ts — how analytics routes receive their use-case deps.
  </read_first>
  <behavior>
    - GET /api/analytics/cot → 200 with a JSON array satisfying cotResponse; an empty store → 200 with [] (NOT an error). A use-case err → the analytics group's standard error mapping (mirror skew/gex).
  </behavior>
  <action>
    Add a `/cot` GET to `analytics.routes.ts` calling the injected `getCot` (ForRunningGetCot), mapping the
    Result to JSON exactly like the gex/skew handlers. Wire the `getCot` use-case into the analytics routes
    in `main.ts` (construct `makeGetCotUseCase({ readCotObservations: makePostgresCotObservationsRepo(db).list })`
    — composition root only). Add a route test mirroring the existing ones: fake use-case returns a 2-entry
    series → assert 200 + shape; returns [] → assert 200 + []. Keep the route thin (parse→use-case→map).
  </action>
  <verify>
    <automated>bun run test -- apps/server/src/adapters/http/analytics.routes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "/cot" apps/server/src/adapters/http/analytics.routes.ts` matches the new route.
    - Route test passes incl. the empty-array case.
    - `bun run typecheck && bun run lint` clean.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: get_cot MCP tool (MCP-02, same change)</name>
  <files>apps/server/src/adapters/mcp/server.ts, apps/server/src/adapters/mcp/mcp.test.ts</files>
  <read_first>
    - apps/server/src/adapters/mcp/server.ts — how get_gex / get_skew / get_term_structure tools register + call their use-case + return the shared contract.
    - apps/server/src/adapters/mcp/mcp.test.ts — the MCP tool test pattern incl. the "contract-valid EMPTY array (not an error) on no data" case.
  </read_first>
  <behavior>
    - get_cot returns the SAME cotResponse payload from the SAME makeGetCotUseCase as the route; no data → contract-valid empty array (not an error).
  </behavior>
  <action>
    Register a `get_cot` tool in the MCP server backed by the same `getCot` use-case instance the route uses
    (single source — MCP-02). Mirror the get_gex registration. Add an mcp.test.ts case: real use-case fake →
    get_cot returns the contract series; no data → returns []. Update the MCP tool-count comment if present.
  </action>
  <verify>
    <automated>bun run test -- apps/server/src/adapters/mcp/mcp.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "get_cot" apps/server/src/adapters/mcp/server.ts` matches the tool registration.
    - MCP test passes incl. the no-data empty-array case.
    - The route and the MCP tool reference the SAME use-case (grep both call getCot).
    - `bun run typecheck && bun run lint` clean.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /api/analytics/cot | Public read of stored CFTC data; no request body, no mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06-IDOR | Info Disclosure | /api/analytics/cot | accept | Data is public CFTC positioning; no tenant/PII — same exposure class as the existing gex/skew analytics routes |
| T-13-06-INJ | Injection | route input | mitigate | Route takes no user-controlled query into SQL; output validated against the cotResponse Zod contract before send |
| T-13-06-DOS | DoS | unbounded read | mitigate | Use-case applies a sane row limit (mirror gex/skew); single small weekly series |
</threat_model>

<verification>
- `bun run test -- apps/server/src/adapters/http/analytics.routes.test.ts apps/server/src/adapters/mcp/mcp.test.ts` green.
- `bun run typecheck && bun run lint` clean.
- Route + MCP both return the shared cotResponse from one use-case (MCP-02 satisfied).
</verification>

<success_criteria>
- GET /api/analytics/cot and MCP get_cot both return the net-per-class COT series over the shared contract (COT-02, MCP-02).
- Empty store → contract-valid [] on both surfaces (no error).
</success_criteria>

<output>
Create `.planning/phases/13-cot-adapter/13-06-SUMMARY.md` when done.
</output>
