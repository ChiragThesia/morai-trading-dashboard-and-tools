---
phase: 13-cot-adapter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/cot.ts
  - packages/contracts/src/cot.test.ts
  - packages/contracts/src/index.ts
  - packages/core/src/journal/application/ports.ts
  - packages/core/src/journal/index.ts
  - packages/core/src/index.ts
  - packages/adapters/src/postgres/schema.ts
  - packages/adapters/src/postgres/migrations/0012_cot_observations.sql
  - packages/adapters/src/postgres/migrations/meta/_journal.json
autonomous: true
requirements: [COT-01, COT-02]

must_haves:
  truths:
    - "The cot Zod contract parses a valid net-positioning entry and rejects a malformed one"
    - "The cot_observations table exists in the live schema with a UNIQUE(contract_code, as_of) constraint"
    - "Core exposes ForFetchingCotReport, ForPersistingCotObservation, ForReadingCotObservations as function-type ports with CotReport / CotObservationRow domain types"
  artifacts:
    - path: "packages/contracts/src/cot.ts"
      provides: "cotSeriesEntry + cotResponse Zod schemas (TFF net-per-class shape, D-05)"
      contains: "cotSeriesEntry"
      min_lines: 30
    - path: "packages/adapters/src/postgres/migrations/0012_cot_observations.sql"
      provides: "cot_observations table DDL with UNIQUE(contract_code, as_of)"
      contains: "cot_observations"
      min_lines: 10
    - path: "packages/core/src/journal/application/ports.ts"
      provides: "ForFetchingCotReport / ForPersistingCotObservation / ForReadingCotObservations ports + CotReport/CotObservationRow"
      contains: "ForFetchingCotReport"
  key_links:
    - from: "packages/contracts/src/index.ts"
      to: "packages/contracts/src/cot.ts"
      via: "barrel re-export of cotSeriesEntry/cotResponse"
      pattern: "from \"./cot.ts\""
    - from: "packages/adapters/src/postgres/migrations/0012_cot_observations.sql"
      to: "packages/adapters/src/postgres/schema.ts"
      via: "drizzle-kit generate emits SQL from the cotObservations pgTable definition"
      pattern: "cotObservations"
---

<objective>
Lay the COT data-shape foundation: the shared Zod contract (`packages/contracts/src/cot.ts`),
the core driven ports + domain types (`ForFetchingCotReport`, `ForPersistingCotObservation`,
`ForReadingCotObservations`, `CotReport`, `CotObservationRow`), and the `cot_observations`
table (migration `0012`) applied to the live schema.

Purpose: every downstream plan (adapter, repo, use-cases, job, route, MCP) compiles against
these interfaces. Interface-first so the parallel waves never invent divergent shapes.
Output: contract schemas + core ports + a live `cot_observations` table.

Implements COT-01 (table + idempotency constraint), COT-02 (contract), per D-01/D-04/D-05/D-09.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-cot-adapter/13-CONTEXT.md
@.planning/phases/13-cot-adapter/13-RESEARCH.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/typescript.md

# Analogs to mirror:
@packages/contracts/src/gex.ts
@packages/core/src/journal/application/ports.ts
@packages/adapters/src/postgres/schema.ts
</context>

## Artifacts this phase produces (canonical manifest)

The full Phase 13 output across all six plans. Downstream plans build against these names.

| Symbol / file | Plan | Layer |
|---|---|---|
| `cotSeriesEntry`, `cotResponse` — `packages/contracts/src/cot.ts` | 13-01 | contracts |
| `CotReport`, `CotObservationRow`, `CotSeriesEntry` domain types — core ports | 13-01 / 13-04 | core |
| `ForFetchingCotReport`, `ForPersistingCotObservation`, `ForReadingCotObservations` ports | 13-01 | core |
| `cotObservations` pgTable + `0012_cot_observations.sql` migration | 13-01 | adapters |
| `makeCftcCotAdapter` — `packages/adapters/src/http/cftc.ts` | 13-02 | adapters |
| `makeMemoryCotReportAdapter` — `packages/adapters/src/memory/cot.ts` (port twin) | 13-02 | adapters |
| `makePostgresCotObservationsRepo` — `packages/adapters/src/postgres/repos/cot-observations.ts` | 13-03 | adapters |
| `makeMemoryCotObservationsRepo` — `packages/adapters/src/memory/cot-observations.ts` (repo twin) | 13-03 | adapters |
| `cotNetSeries` net-derivation — `packages/core/src/journal/application/cotNet.ts` | 13-04 | core |
| `makeFetchCot` use-case — `packages/core/src/journal/application/fetchCot.ts` | 13-04 | core |
| `makeGetCotUseCase` + `ForRunningGetCot` — `packages/core/src/journal/application/getCot.ts` | 13-04 | core |
| `makeFetchCotHandler` — `apps/worker/src/handlers/fetch-cot.ts` + `fetch-cot` queue/cron | 13-05 | worker |
| `GET /api/analytics/cot` route | 13-06 | server |
| `get_cot` MCP tool (`registerGetCotTool`) | 13-06 | server |

## Multi-Source Coverage Audit

Every source item is COVERED by a plan. No item is omitted or simplified.

| Source | Item | Covered by |
|---|---|---|
| GOAL | Weekly fetch-cot → cot_observations (as_of vs published_at) | 13-01, 13-03, 13-05 |
| GOAL | GET /api/analytics/cot + MCP get_cot over shared contract | 13-01, 13-04, 13-06 |
| REQ | COT-01 (job, table, as_of/published_at, idempotent) | 13-01, 13-03, 13-04, 13-05 |
| REQ | COT-02 (route + MCP series) | 13-01, 13-04, 13-06 |
| REQ | MCP-02 (route + MCP same change, shared Zod) | 13-06 |
| RESEARCH | Dataset gpe5-46if.json, code 13874A, field names, z.coerce.number | 13-02 |
| RESEARCH | Schema 0012 + UNIQUE(contract_code, as_of) | 13-01 |
| RESEARCH | Hexagonal wiring (port/adapter/repo/job/contract/route/MCP) | 13-01..13-06 |
| RESEARCH | Landmines (strings→numbers, 13874A, date field, no fabricated fallback, TFF-not-legacy, idempotency, no token) | 13-02, 13-01, 13-03 |
| CONTEXT | D-01 CFTC Socrata TS adapter mirroring fred.ts | 13-02 |
| CONTEXT | D-02 fetch-cot TS worker job (no auth) | 13-05 |
| CONTEXT | D-03 TFF report for E-mini S&P 500 | 13-02 |
| CONTEXT | D-04 store long+short per class; net derived at API | 13-01, 13-04 |
| CONTEXT | D-05 TFF-class net naming (netLeveraged primary signal) | 13-01, 13-04 |
| CONTEXT | D-06 first run current week only | 13-02, 13-05 |
| CONTEXT | D-07 cron Friday 17:00 ET; published_at = fetch ts | 13-05 |
| CONTEXT | D-08 as_of from report_date_as_yyyy_mm_dd (no date-math) | 13-02 |
| CONTEXT | D-09 idempotency key = as_of week (upsert / unique) | 13-01, 13-03 |
| CONTEXT | D-10 get_cot + route same change, shared contract | 13-06 |

Excluded (not gaps): historical backfill, other instruments, Overview COT UI panel — all CONTEXT Deferred Ideas / scoped to a later UI pass.

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: cot Zod contract (cotSeriesEntry + cotResponse)</name>
  <files>packages/contracts/src/cot.ts, packages/contracts/src/cot.test.ts, packages/contracts/src/index.ts</files>
  <read_first>
    - packages/contracts/src/gex.ts — the contract+doc style to mirror (single schema source, MCP-02 note).
    - packages/contracts/src/index.ts — barrel export style (value + type exports per module).
    - .planning/phases/13-cot-adapter/13-RESEARCH.md §3 "Contract" — field list + D-05 reconciliation.
  </read_first>
  <behavior>
    - A fully-populated entry (asOf YYYY-MM-DD, publishedAt ISO datetime, contractCode, openInterest int, and per-TFF-class long/short/net ints) parses successfully through cotSeriesEntry.
    - cotResponse parses an array of such entries (including the empty array).
    - An entry with a non-integer openInterest, or asOf that is not a date string, fails parsing (safeParse.success === false).
    - For any entry, netLeveraged equals levMoneyLong − levMoneyShort by construction of the fixture used in the round-trip example (the derivation invariant itself is property-tested in 13-04).
  </behavior>
  <action>
    Create `cotSeriesEntry` as a `z.object` exposing: `asOf` (`z.string().date()`), `publishedAt`
    (`z.string().datetime()`), `contractCode` (`z.string()`), `openInterest` (`z.number().int()`),
    and for each TFF trader class — Dealer, Asset Manager, Leveraged Funds, Other Reportable,
    Non-Reportable (D-04) — the raw `*Long` / `*Short` ints PLUS the derived `net*` int:
    `dealerLong`/`dealerShort`/`netDealer`, `assetMgrLong`/`assetMgrShort`/`netAssetManager`,
    `levMoneyLong`/`levMoneyShort`/`netLeveraged`, `otherReptLong`/`otherReptShort`/`netOther`,
    `nonreptLong`/`nonreptShort`/`netNonreportable` (all `z.number().int()`). `netLeveraged` is the
    headline "big guys" signal (D-05) — keep both legacy-reconciled net fields AND raw legs so the
    Overview chart and the net invariant both have what they need. Export `cotResponse =
    z.array(cotSeriesEntry)` and the inferred `CotSeriesEntry` / `CotResponse` types. Document at the
    top that this is the ONE schema shared by `GET /api/analytics/cot` and `get_cot` (MCP-02) and that
    the TFF class names supersede COT-02's illustrative legacy `net_noncommercial`/`net_commercial`
    (D-05). Add the barrel re-export block to `packages/contracts/src/index.ts` mirroring the gex block.
    Write the example/round-trip test FIRST and watch it fail (RED) before the schema exists.
  </action>
  <verify>
    <automated>bun run test -- packages/contracts/src/cot.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test -- packages/contracts/src/cot.test.ts` passes with at least one valid round-trip case and one rejection case.
    - `bun run typecheck` is clean (no `any`/`as`/`!`; types flow from `z.infer`).
    - `rg -n "from \"./cot.ts\"" packages/contracts/src/index.ts` matches (barrel wired).
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: core COT ports + domain types</name>
  <files>packages/core/src/journal/application/ports.ts, packages/core/src/journal/index.ts, packages/core/src/index.ts</files>
  <read_first>
    - packages/core/src/journal/application/ports.ts — the existing `RateObservation` + `ForFetchingRate`/`ForPersistingRate`/`ForReadingRate` block is the exact analog to mirror (D-04 fetch/persist/read trio).
    - packages/core/src/journal/index.ts and packages/core/src/index.ts — how port TYPES are re-exported outward.
    - .planning/phases/13-cot-adapter/13-RESEARCH.md §3 "Port (core)".
  </read_first>
  <action>
    Append a COT section to `ports.ts`. Add domain type `CotReport` (readonly fields: `contractCode`
    string, `asOf` string YYYY-MM-DD, `openInterest` number, and the ten raw class legs
    `dealerLong`/`dealerShort`/`assetMgrLong`/`assetMgrShort`/`levMoneyLong`/`levMoneyShort`/
    `otherReptLong`/`otherReptShort`/`nonreptLong`/`nonreptShort` — all `number`). Add
    `CotObservationRow = CotReport & { readonly publishedAt: Date }` (the persisted row; `published_at`
    is the fetch clock per D-07, stamped by the use-case, not by the adapter). Define three
    function-type ports (`ForVerbingNoun`, architecture-boundaries §5): `ForFetchingCotReport =
    (contractCode: string) => Promise<Result<CotReport, FetchError>>`; `ForPersistingCotObservation =
    (row: CotObservationRow) => Promise<Result<void, StorageError>>`; `ForReadingCotObservations =
    (limit?: number) => Promise<Result<ReadonlyArray<CotObservationRow>, StorageError>>`. No
    implementations here — types only. Re-export `CotReport`, `CotObservationRow`,
    `ForFetchingCotReport`, `ForPersistingCotObservation`, `ForReadingCotObservations` through
    `journal/index.ts` and `core/index.ts` next to the existing `ForFetchingRate` exports. Reuse the
    existing `FetchError` and `StorageError` types — do not define new error kinds.
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `bun run typecheck` is clean.
    - `rg -n "ForFetchingCotReport|ForPersistingCotObservation|ForReadingCotObservations" packages/core/src/index.ts` shows all three exported from the core barrel.
    - `ForFetchingCotReport` returns `Result<CotReport, FetchError>` (no fabricated fallback contract — error is a real variant; landmine 4).
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: cot_observations table + migration 0012 + [BLOCKING] schema push</name>
  <files>packages/adapters/src/postgres/schema.ts, packages/adapters/src/postgres/migrations/0012_cot_observations.sql, packages/adapters/src/postgres/migrations/meta/_journal.json</files>
  <read_first>
    - packages/adapters/src/postgres/schema.ts — `rateObservations`, `termStructureObservations` pgTable style + `.enableRLS()`; column helpers (`date`, `timestamp`, `integer`, `text`, `uuid`, `numeric`).
    - packages/adapters/src/postgres/migrations/0007_analytics_observations.sql + 0008_gex_snapshot.sql — generated-SQL style (CREATE TABLE + ENABLE ROW LEVEL SECURITY + statement-breakpoint).
    - .planning/phases/13-cot-adapter/13-RESEARCH.md §2 "Schema".
    - drizzle.config.ts (root) — `drizzle-kit generate` writes into packages/adapters/src/postgres/migrations.
  </read_first>
  <action>
    Add a `cotObservations` pgTable named `cot_observations` to `schema.ts` (place it next to
    `rateObservations`). Columns: `id` uuid PK default `gen_random_uuid()`; `contractCode` text
    notNull (stores `13874A`); `asOf` date notNull (from `report_date_as_yyyy_mm_dd`, the Tuesday);
    `publishedAt` timestamptz notNull (fetch clock, Friday); `openInterest` integer notNull; the ten
    raw class legs `dealerLong`/`dealerShort`/`assetMgrLong`/`assetMgrShort`/`levMoneyLong`/
    `levMoneyShort`/`otherReptLong`/`otherReptShort`/`nonreptLong`/`nonreptShort` all integer notNull;
    `createdAt` timestamptz notNull default `now()`. Add a table-level UNIQUE constraint on
    `(contractCode, asOf)` — this is the COT-01 idempotency key (D-09). Call `.enableRLS()` to match
    every other table. NET is NOT stored (D-04 — derived in 13-04). Then run `bunx drizzle-kit generate`
    so it emits `0012_cot_observations.sql` and updates `meta/_journal.json` + the meta snapshot; review
    the emitted SQL to confirm it contains the CREATE TABLE, the UNIQUE constraint, and ENABLE ROW LEVEL
    SECURITY. [BLOCKING] schema push: run `bun run migrate` against the live dev database (DATABASE_URL
    set) so the table is applied to the real schema and the SQL is proven valid BEFORE any repo
    testcontainers work in 13-03. Do not hand-write the SQL — generate it from the schema so the Drizzle
    ledger stays consistent.
  </action>
  <verify>
    <automated>bun run migrate && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `bun run migrate` applies cleanly (idempotent on re-run) and exits 0.
    - `rg -n "cot_observations" packages/adapters/src/postgres/migrations/0012_cot_observations.sql` matches the CREATE TABLE.
    - The emitted SQL contains a UNIQUE constraint over `contract_code` and `as_of` (idempotency key).
    - `rg -n "0012_cot_observations" packages/adapters/src/postgres/migrations/meta/_journal.json` shows the ledger entry.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema → DB | DDL applied to Postgres; constraint correctness is the only surface this plan owns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | cot_observations duplicate-week rows | mitigate | UNIQUE(contract_code, as_of) constraint enforces COT-01 idempotency at the DB (D-09) |
| T-13-SC | Tampering | npm/bun installs | accept | Phase 13 adds ZERO new dependencies (zod, drizzle, drizzle-kit already present) — no package-legitimacy gate required |
</threat_model>

<verification>
- `bun run test -- packages/contracts/src/cot.test.ts` green (contract round-trip).
- `bun run typecheck` clean across contracts + core (ports compile).
- `bun run migrate` applies 0012 to the live schema; re-run applies 0 (idempotent).
- `bun run lint` clean (no `any`/`as`/`!`).
</verification>

<success_criteria>
- cotSeriesEntry/cotResponse exported from `@morai/contracts` and round-trip tested.
- ForFetchingCotReport/ForPersistingCotObservation/ForReadingCotObservations + CotReport/CotObservationRow exported from `@morai/core`.
- cot_observations table live with UNIQUE(contract_code, as_of).
</success_criteria>

<output>
Create `.planning/phases/13-cot-adapter/13-01-SUMMARY.md` when done.
</output>
