---
phase: 13-cot-adapter
plan: 02
type: tdd
wave: 2
depends_on: [13-01]
files_modified:
  - packages/adapters/src/http/cftc.ts
  - packages/adapters/src/http/cftc.test.ts
  - packages/adapters/src/http/cftc.contract.test.ts
  - packages/adapters/src/memory/cot.ts
  - packages/adapters/src/memory/cot.contract.test.ts
  - packages/adapters/src/__contract__/cot.contract.ts
  - packages/adapters/src/index.ts
autonomous: true
requirements: [COT-01]

must_haves:
  truths:
    - "The CFTC adapter fetches the TFF E-mini row (contract 13874A) and parses Socrata string-numbers into numeric CotReport fields"
    - "as_of is taken from report_date_as_yyyy_mm_dd (the report's own date), never computed by date-math"
    - "A non-2xx, network, empty, or malformed Socrata response yields a Result error (no throw, no fabricated fallback row)"
    - "The in-memory ForFetchingCotReport twin satisfies the same shared contract as the real adapter (architecture-boundaries §8)"
  artifacts:
    - path: "packages/adapters/src/http/cftc.ts"
      provides: "makeCftcCotAdapter — ForFetchingCotReport over CFTC Socrata gpe5-46if.json"
      exports: ["makeCftcCotAdapter"]
      min_lines: 50
    - path: "packages/adapters/src/memory/cot.ts"
      provides: "makeMemoryCotReportAdapter — in-memory ForFetchingCotReport twin with seed()"
      exports: ["makeMemoryCotReportAdapter"]
    - path: "packages/adapters/src/__contract__/cot.contract.ts"
      provides: "shared ForFetchingCotReport contract run against both the msw-seeded real adapter and the memory twin"
  key_links:
    - from: "packages/adapters/src/http/cftc.ts"
      to: "packages/core (ForFetchingCotReport, CotReport)"
      via: "imports the port + domain type, returns Result<CotReport, FetchError>"
      pattern: "ForFetchingCotReport"
    - from: "packages/adapters/src/http/cftc.test.ts"
      to: "CFTC Socrata endpoint"
      via: "msw intercepts gpe5-46if.json and asserts the $where contract-code filter"
      pattern: "gpe5-46if"
---

<objective>
Build the CFTC Socrata HTTP adapter (`makeCftcCotAdapter`) implementing `ForFetchingCotReport`,
plus its in-memory twin (`makeMemoryCotReportAdapter`), validated with msw against a captured
TFF fixture. Mirror the FRED adapter exactly (D-01), but with NO fabricated fallback (landmine 4).

Purpose: the fetch side of the COT pipeline — turn the live Socrata TFF row for E-mini S&P 500
into a typed `CotReport`. Output: the adapter + twin + shared contract test.

Implements COT-01 (fetch half) per D-01/D-03/D-06/D-08; landmines 1-4.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-cot-adapter/13-CONTEXT.md
@.planning/phases/13-cot-adapter/13-RESEARCH.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/typescript.md
@.claude/rules/tdd.md

# Analogs to mirror (read before writing):
@packages/adapters/src/http/fred.ts
@packages/adapters/src/http/fred.test.ts
@packages/adapters/src/http/fred.contract.test.ts
@packages/adapters/src/memory/rate.ts
</context>

## Wave 0 — fixture (install first)

Capture a real CFTC Socrata JSON row for `13874A` (one TFF week) as
`packages/adapters/src/http/__fixtures__/cot-tff-emini.json` — numbers as STRINGS, real field
names (`report_date_as_yyyy_mm_dd`, `open_interest_all`, `lev_money_positions_long_all`, …). This
is the msw response body for both the adapter test and the msw-seeded contract test. Source the
shape from 13-RESEARCH.md §1; a hand-built fixture with the exact field names + string values is
acceptable if a live pull is unavailable.

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: makeCftcCotAdapter (Socrata TFF) + msw tests</name>
  <files>packages/adapters/src/http/cftc.ts, packages/adapters/src/http/cftc.test.ts, packages/adapters/src/http/__fixtures__/cot-tff-emini.json, packages/adapters/src/index.ts</files>
  <read_first>
    - packages/adapters/src/http/fred.ts — URL build via `new URL` + `searchParams`, Zod-parse-before-use, static `console.warn` + Result on error, `noUncheckedIndexedAccess` first-element guard.
    - packages/adapters/src/http/fred.test.ts — msw setupServer per-test handler override pattern (200, non-2xx, network error, malformed body).
    - .planning/phases/13-cot-adapter/13-RESEARCH.md §1 (field table + latest-week query) and §5 (landmines 1-4).
  </read_first>
  <behavior>
    - Given a 200 Socrata array whose numbers are STRINGS, the adapter returns ok(CotReport) with every numeric field coerced to a number (z.coerce.number), and openInterest taken from open_interest_all.
    - The request targets gpe5-46if.json with `$where=cftc_contract_market_code='13874A'`, `$order=report_date_as_yyyy_mm_dd DESC`, `$limit=1` (assert the outgoing URL via the msw handler).
    - asOf equals the date part of report_date_as_yyyy_mm_dd (e.g. "2026-06-23"), NOT a value derived by subtracting days.
    - A non-2xx response returns err({kind:"fetch-error", ...}) — no throw.
    - A network rejection returns err — no throw.
    - An empty array (no rows) or a body failing the Zod schema returns err — NO fabricated/fallback row (landmine 4).
  </behavior>
  <action>
    Create `makeCftcCotAdapter(deps: { readonly fetch: typeof globalThis.fetch })` returning
    `ForFetchingCotReport`. Inside the returned function, build the Socrata URL for
    `https://publicreporting.cftc.gov/resource/gpe5-46if.json` setting `$where` to
    `cftc_contract_market_code='<contractCode>'` (use the passed contractCode arg; prefer the exact
    code over a name LIKE — landmine 2), `$order` to `report_date_as_yyyy_mm_dd DESC`, and `$limit` to
    `1` (D-06 current week only). Do NOT send any app token header (anonymous throttle is far under a
    weekly job; landmine 7 — keep tokens out of code/logs). Define a `CftcRowSchema` z.object whose
    numeric fields use `z.coerce.number()` (landmine 1 — Socrata returns strings) and
    `report_date_as_yyyy_mm_dd` as `z.string()`; parse the response as `z.array(CftcRowSchema)`. On
    non-2xx → static `console.warn` + return `err` FetchError; on `fetch` throw → static warn + err; on
    Zod failure or empty array → static warn + err. Map the first row to `CotReport`: `asOf` =
    `report_date_as_yyyy_mm_dd` sliced to the 10-char date; `openInterest` = open_interest_all; the ten
    legs from the `*_positions_long_all` / `*_positions_short_all` fields per the RESEARCH field table.
    Export `makeCftcCotAdapter` (+ its type) from `packages/adapters/src/index.ts` under an "HTTP
    adapters" comment next to `makeFredRateAdapter`. Write the failing msw tests FIRST (RED), then the
    adapter (GREEN). Commit only at green.
  </action>
  <verify>
    <automated>bun run test -- packages/adapters/src/http/cftc.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test -- packages/adapters/src/http/cftc.test.ts` passes with cases for: string→number coercion, the 13874A `$where`, as_of from the report date field, non-2xx→err, network→err, empty/malformed→err.
    - The adapter never throws on any branch (every path returns a Result).
    - `bun run typecheck` + `bun run lint` clean (no `any`/`as`/`!`).
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: in-memory ForFetchingCotReport twin + shared contract</name>
  <files>packages/adapters/src/memory/cot.ts, packages/adapters/src/__contract__/cot.contract.ts, packages/adapters/src/memory/cot.contract.test.ts, packages/adapters/src/http/cftc.contract.test.ts, packages/adapters/src/index.ts</files>
  <read_first>
    - packages/adapters/src/memory/rate.ts — twin shape with `seed()`.
    - packages/adapters/src/__contract__/rate.contract.ts — shared-contract function exported and run by BOTH `http/fred.contract.test.ts` (msw-seeded) and `memory/rate.contract.test.ts`.
    - packages/adapters/src/memory/rate.contract.test.ts + packages/adapters/src/http/fred.contract.test.ts — the two call sites.
  </read_first>
  <behavior>
    - The shared contract `runCotReportContractTests(makeSubject)` asserts: a seeded/known TFF week returns ok(CotReport) with the expected contractCode, asOf, openInterest and legs.
    - The memory twin, when unseeded, returns err(fetch-error) (no fabricated row — matches the real adapter's no-fallback rule); when seeded via `seed(report)`, returns ok(report).
    - The same contract passes against the real adapter with msw returning the captured fixture (proving twin ≡ real for the contract surface).
  </behavior>
  <action>
    Create `makeMemoryCotReportAdapter()` returning `{ fetchReport: ForFetchingCotReport; seed:
    (r: CotReport) => void }`, mirroring `makeMemoryRateAdapter` but with NO default fallback — unseeded
    `fetchReport` returns `err` FetchError (landmine 4 parity with the real adapter). Extract the shared
    `runCotReportContractTests(makeSubject)` into `packages/adapters/src/__contract__/cot.contract.ts`
    (mirror `rate.contract.ts`): seed a known CotReport, call the subject, assert the parsed fields.
    Add `memory/cot.contract.test.ts` running the contract against the memory twin (no Docker), and
    `http/cftc.contract.test.ts` running the SAME contract against `makeCftcCotAdapter` with an msw
    handler serving the captured fixture. Export `makeMemoryCotReportAdapter` from
    `packages/adapters/src/index.ts`. Write the contract + twin tests FIRST (RED), implement the twin
    (GREEN).
  </action>
  <verify>
    <automated>bun run test -- packages/adapters/src/memory/cot.contract.test.ts packages/adapters/src/http/cftc.contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both contract test files pass — the memory twin and the msw-seeded real adapter satisfy the identical `ForFetchingCotReport` contract.
    - Unseeded memory twin returns a Result error (not a fabricated report).
    - `rg -n "makeMemoryCotReportAdapter|makeCftcCotAdapter" packages/adapters/src/index.ts` shows both exported.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Socrata (publicreporting.cftc.gov) → adapter | untrusted external HTTP JSON crosses into the app here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02 | Tampering | malformed / poisoned Socrata JSON | mitigate | Zod-parse with `z.coerce.number()` BEFORE use; malformed/empty → Result error, never used raw (cftc.ts) |
| T-13-03 | Denial of Service | unbounded Socrata response | mitigate | `$limit=1` on the query — single-row fetch (D-06) |
| T-13-04 | Information Disclosure | app token leakage | accept | No app token is sent or stored (anonymous access); nothing to leak (landmine 7) |
</threat_model>

<verification>
- `bun run test -- packages/adapters/src/http/cftc.test.ts` green (msw edge cases).
- Both contract test files green (twin ≡ real adapter).
- `bun run typecheck` + `bun run lint` clean.
</verification>

<success_criteria>
- makeCftcCotAdapter implements ForFetchingCotReport, parses Socrata strings→numbers, targets 13874A, takes as_of from the report date, errors (no fallback) on failure.
- makeMemoryCotReportAdapter twin passes the shared contract.
</success_criteria>

<output>
Create `.planning/phases/13-cot-adapter/13-02-SUMMARY.md` when done.
</output>
