---
phase: 13-cot-adapter
plan: 04
type: tdd
wave: 4
depends_on: [13-02, 13-03]
files_modified:
  - packages/core/src/journal/application/cotNet.ts
  - packages/core/src/journal/application/cotNet.test.ts
  - packages/core/src/journal/application/fetchCot.ts
  - packages/core/src/journal/application/fetchCot.test.ts
  - packages/core/src/journal/application/getCot.ts
  - packages/core/src/journal/application/getCot.test.ts
  - packages/core/src/journal/index.ts
  - packages/core/src/index.ts
autonomous: true
requirements: [COT-01, COT-02]

must_haves:
  truths:
    - "cotNet derives net = long − short for every TFF class (property: net + short == long for all integer inputs)"
    - "makeFetchCot fetches a CotReport, stamps published_at with the injected clock, and persists exactly one CotObservationRow; a second run for the same as_of week persists 0 new rows (idempotent, via the repo's unique constraint)"
    - "makeGetCotUseCase reads stored observations and returns a cotResponse-shaped series with net-per-class derived (netLeveraged is the headline series, D-05)"
  artifacts:
    - path: "packages/core/src/journal/application/cotNet.ts"
      provides: "cotNet — pure net-per-class derivation (D-04)"
      contains: "cotNet"
    - path: "packages/core/src/journal/application/fetchCot.ts"
      provides: "makeFetchCot use-case — orchestrates fetch → stamp publishedAt → persist (COT-01)"
      contains: "makeFetchCot"
    - path: "packages/core/src/journal/application/getCot.ts"
      provides: "makeGetCotUseCase + ForRunningGetCot — read + net-derive → cot series (COT-02)"
      contains: "makeGetCotUseCase"
  key_links:
    - from: "packages/core/src/journal/application/fetchCot.ts"
      to: "ForFetchingCotReport + ForPersistingCotObservation"
      via: "makeFetchCot(deps) injects both ports"
      pattern: "ForFetchingCotReport"
    - from: "packages/core/src/journal/application/getCot.ts"
      to: "packages/contracts (cotResponse shape)"
      via: "returns the net-per-class series the contract describes"
      pattern: "cotNet"
---

<objective>
Build the core use-cases that tie the COT pipeline together: `cotNet` (pure net-per-class
derivation, D-04), `makeFetchCot` (fetch a report → stamp `published_at` from an injected clock →
persist; COT-01), and `makeGetCotUseCase` (read observations → derive net → return the series;
COT-02). Pure hexagon — depends only on the ports from 13-01 and on `@morai/shared`. NET lives here
(D-04: not stored). Implements COT-01 (fetch orchestration + idempotency) and COT-02 (read series).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-cot-adapter/13-CONTEXT.md
@.planning/phases/13-cot-adapter/13-RESEARCH.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/typescript.md
@.claude/rules/tdd.md

# Analogs to mirror:
@packages/core/src/journal/application/fetchRate.ts
@packages/contracts/src/cot.ts
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: cotNet pure net-per-class derivation (fast-check)</name>
  <files>packages/core/src/journal/application/cotNet.ts, packages/core/src/journal/application/cotNet.test.ts</files>
  <read_first>
    - packages/core/src/journal/application/ports.ts — CotReport / CotObservationRow leg field names (from 13-01).
    - packages/contracts/src/cot.ts — the net field names the API expects (netDealer/netAssetManager/netLeveraged/netOther/netNonreportable).
    - An existing fast-check property test in the repo for the style.
  </read_first>
  <behavior>
    - cotNet(report) returns { netDealer, netAssetManager, netLeveraged, netOther, netNonreportable } where each net = classLong − classShort.
    - Property (fast-check): for arbitrary non-negative integer legs, net + short === long for every class; nets are integers.
  </behavior>
  <action>
    Write the property test FIRST (RED). Implement `cotNet(report: Pick<CotReport, legs...>)` as a pure
    function returning the five `net*` integers (long − short per class). No I/O, no ports. This is the
    single source of the net derivation (D-04) used by getCot. Keep it in core (importable by the
    use-case). GREEN, REFACTOR.
  </action>
  <verify>
    <automated>bun run test -- packages/core/src/journal/application/cotNet.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Property test passes (net + short == long; integer nets).
    - `bun run typecheck && bun run lint` clean.
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: makeFetchCot use-case (fetch → stamp publishedAt → persist; idempotent)</name>
  <files>packages/core/src/journal/application/fetchCot.ts, packages/core/src/journal/application/fetchCot.test.ts, packages/core/src/journal/index.ts, packages/core/src/index.ts</files>
  <read_first>
    - packages/core/src/journal/application/fetchRate.ts — makeXxx(deps) factory shape, Result handling, clock injection.
    - 13-02 memory twin (makeMemoryCotReportAdapter) + 13-03 memory repo (makeMemoryCotObservationsRepo) — the test doubles to inject.
  </read_first>
  <behavior>
    - makeFetchCot({ fetchCotReport, persistCotObservation, now }) returns a driver. On invoke: fetches a CotReport for the configured contract code; if err → propagate err (no persist). On ok → build CotObservationRow = report + { publishedAt: now() } and persist; return ok.
    - Idempotency: invoking twice with the same report week persists the second as a no-op (the repo's ON CONFLICT / unique constraint absorbs it) — the use-case does not itself need to dedup, but the test asserts 2 invocations → repo holds 1 row for that as_of (using the memory repo that mirrors the unique key).
    - published_at comes from the injected `now` clock (D-07), NOT from the adapter; as_of comes from the report (D-08). They differ in the test.
  </behavior>
  <action>
    Write the test FIRST (RED) using the memory adapter + memory repo from 13-02/13-03 and a fixed
    injected `now`. Implement `makeFetchCot(deps: { fetchCotReport: ForFetchingCotReport;
    persistCotObservation: ForPersistingCotObservation; now: () => Date; contractCode?: string })`
    returning `() => Promise<Result<void, FetchError | StorageError>>`. Default contractCode "13874A".
    Re-export `makeFetchCot` through journal/index.ts + core/index.ts. GREEN, REFACTOR.
  </action>
  <verify>
    <automated>bun run test -- packages/core/src/journal/application/fetchCot.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test proves: err short-circuits persist; ok persists one row; double-invoke → memory repo holds 1 row for the as_of; published_at = injected now, as_of = report date.
    - `rg -n "makeFetchCot" packages/core/src/index.ts` shows it exported.
    - `bun run typecheck && bun run lint` clean.
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: makeGetCotUseCase (read → net-derive → series; COT-02)</name>
  <files>packages/core/src/journal/application/getCot.ts, packages/core/src/journal/application/getCot.test.ts, packages/core/src/journal/index.ts, packages/core/src/index.ts</files>
  <read_first>
    - packages/contracts/src/cot.ts — cotSeriesEntry shape the use-case must return.
    - cotNet.ts (Task 1) — the net derivation to apply per row.
    - 13-03 memory repo — seed rows for the test.
  </read_first>
  <behavior>
    - makeGetCotUseCase({ readCotObservations }) returns ForRunningGetCot: () => Promise<Result<CotSeriesEntry[], StorageError>>.
    - Reads stored observations (most-recent-first, optional limit), maps each CotObservationRow → a cotSeriesEntry: asOf (YYYY-MM-DD), publishedAt (ISO), openInterest, the raw legs, AND the five net* from cotNet. An empty store → ok([]).
  </behavior>
  <action>
    Write the test FIRST (RED) seeding the memory repo with 2 weeks. Implement `makeGetCotUseCase` +
    the `ForRunningGetCot` port type; map rows → cotSeriesEntry using `cotNet`. Format `asOf` as
    YYYY-MM-DD and `publishedAt` as ISO so the row satisfies the contract (validate against
    `cotSeriesEntry.safeParse` in the test). Re-export through journal/index.ts + core/index.ts. GREEN.
  </action>
  <verify>
    <automated>bun run test -- packages/core/src/journal/application/getCot.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test: 2 seeded weeks → 2 entries, each passing `cotSeriesEntry.safeParse`; net = long − short; empty store → [].
    - `rg -n "makeGetCotUseCase|ForRunningGetCot" packages/core/src/index.ts` shows them exported.
    - `bun run typecheck && bun run lint` clean.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `bun run test -- packages/core/src/journal/application/cotNet.test.ts packages/core/src/journal/application/fetchCot.test.ts packages/core/src/journal/application/getCot.test.ts` all green.
- `bun run typecheck && bun run lint` clean (core imports only @morai/shared + ports; no adapters).
- Idempotency truth proven at the use-case level via the memory repo (double-invoke → 1 row).
</verification>

<success_criteria>
- cotNet, makeFetchCot, makeGetCotUseCase + ForRunningGetCot exported from @morai/core.
- Net derived in core (D-04); published_at from clock (D-07); as_of from report (D-08).
</success_criteria>

<output>
Create `.planning/phases/13-cot-adapter/13-04-SUMMARY.md` when done.
</output>
