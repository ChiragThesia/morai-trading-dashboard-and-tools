---
phase: 13-cot-adapter
plan: 05
type: execute
wave: 5
depends_on: [13-04]
files_modified:
  - apps/worker/src/handlers/fetch-cot.ts
  - apps/worker/src/handlers/fetch-cot.test.ts
  - apps/worker/src/schedule.ts
  - apps/worker/src/main.ts
autonomous: true
requirements: [COT-01]

must_haves:
  truths:
    - "A fetch-cot pg-boss queue is created, a weekly cron '0 17 * * 5' (America/New_York) is scheduled, and the handler is registered — mirroring fetch-rates"
    - "makeFetchCotHandler invokes the makeFetchCot use-case and throws on a Result error so pg-boss retries; a successful run is a no-op-safe re-run (idempotent via the repo unique key, D-09)"
    - "main.ts wires the CFTC adapter + postgres cot repo + a real clock into makeFetchCot (composition root only — no business logic)"
  artifacts:
    - path: "apps/worker/src/handlers/fetch-cot.ts"
      provides: "makeFetchCotHandler — pg-boss adapter around makeFetchCot"
      contains: "makeFetchCotHandler"
  key_links:
    - from: "apps/worker/src/schedule.ts"
      to: "fetch-cot queue + cron + handler"
      via: "createQueue('fetch-cot') + schedule('fetch-cot', '0 17 * * 5', ...) + work('fetch-cot', handler)"
      pattern: "fetch-cot"
---

<objective>
Wire the weekly `fetch-cot` worker job: a thin pg-boss handler around the `makeFetchCot` use-case
(13-04), a `fetch-cot` queue, a Friday-17:00-ET cron, and composition-root wiring in `main.ts`
(CFTC adapter + postgres repo + clock). Mirror `fetch-rates` exactly. No RTH gate (weekly). No auth.
Implements COT-01 (the job that lands one row per week), per D-02/D-06/D-07/D-09.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-cot-adapter/13-CONTEXT.md
@.planning/phases/13-cot-adapter/13-RESEARCH.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/tdd.md

# Analogs to mirror:
@apps/worker/src/handlers/fetch-rates.ts
@apps/worker/src/schedule.ts
@apps/worker/src/main.ts
</context>

<tasks>

<task type="execute">
  <name>Task 1: fetch-cot handler (thin pg-boss adapter around makeFetchCot)</name>
  <files>apps/worker/src/handlers/fetch-cot.ts, apps/worker/src/handlers/fetch-cot.test.ts</files>
  <read_first>
    - apps/worker/src/handlers/fetch-rates.ts — the handler shape: deps factory, call use-case, throw on err (so pg-boss retries), static logging. THE template (note: fetch-rates has NO RTH gate — copy that, NOT the chain handlers' RTH self-check).
    - packages/core makeFetchCot signature (from 13-04).
  </read_first>
  <behavior>
    - makeFetchCotHandler({ fetchCot }) returns a pg-boss handler that awaits the use-case; on err → throw (pg-boss retry); on ok → return. Idempotent: a duplicate run for the same week persists 0 new rows (repo unique key).
  </behavior>
  <action>
    Implement `makeFetchCotHandler(deps: { fetchCot: ForRunningFetchCot })` mirroring `makeFetchRatesHandler`.
    Test (msw/double level, no real network): inject a `fetchCot` stub that returns err → assert the handler
    throws; returns ok → assert it resolves. Keep the handler logic-free beyond the throw-on-err.
  </action>
  <verify>
    <automated>bun run test -- apps/worker/src/handlers/fetch-cot.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Handler throws on use-case err, resolves on ok.
    - `bun run typecheck && bun run lint` clean.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: register fetch-cot queue + cron + handler; wire deps in main.ts</name>
  <files>apps/worker/src/schedule.ts, apps/worker/src/main.ts</files>
  <read_first>
    - apps/worker/src/schedule.ts — the createQueue / schedule(cron, tz) / work() registration order + the existing fetch-rates cron line.
    - apps/worker/src/main.ts — composition root: how fetch-rates' adapter + repo + handler are constructed and passed to registerAllJobs.
  </read_first>
  <action>
    In `schedule.ts`: `createQueue("fetch-cot")`; `schedule("fetch-cot", "0 17 * * 5", null, { tz:
    "America/New_York" })` (Friday 17:00 ET, D-07); `work("fetch-cot", POLLING_INTERVAL, handlers.fetchCot)`.
    Add `fetchCot` to the handlers type + the registration. In `main.ts`: construct `makeCftcCotAdapter()`,
    the `makePostgresCotObservationsRepo(db)`, build `makeFetchCot({ fetchCotReport, persistCotObservation,
    now: () => new Date(), contractCode: "13874A" })`, wrap in `makeFetchCotHandler`, and pass it into
    `registerAllJobs`. Composition root only — no logic. Update the cron-count comment in schedule.ts.
  </action>
  <verify>
    <automated>bun run typecheck && bun run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "fetch-cot" apps/worker/src/schedule.ts` shows queue + cron `0 17 * * 5` + work registration.
    - `rg -n "makeFetchCot|makeFetchCotHandler" apps/worker/src/main.ts` shows the wiring.
    - `bun run typecheck && bun run lint` clean.
    - No RTH gate in the handler (weekly job; grep confirms no isWithinRth in fetch-cot.ts).
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `bun run test -- apps/worker/src/handlers/fetch-cot.test.ts` green.
- `bun run typecheck && bun run lint` clean.
- schedule.ts registers the fetch-cot queue + Friday-17:00-ET cron + handler.
</verification>

<success_criteria>
- Weekly fetch-cot job lands one cot_observations row per report week; re-run is idempotent (COT-01).
- Cron is Friday 17:00 America/New_York; published_at from the run clock (D-07).
</success_criteria>

<output>
Create `.planning/phases/13-cot-adapter/13-05-SUMMARY.md` when done.
</output>
