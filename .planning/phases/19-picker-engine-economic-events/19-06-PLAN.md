---
phase: 19-picker-engine-economic-events
plan: 06
type: tdd
wave: 4
depends_on: ["19-01", "19-03", "19-04", "19-05"]
files_modified:
  - packages/core/src/picker/application/computePickerSnapshot.ts
  - packages/core/src/picker/application/computePickerSnapshot.test.ts
  - packages/core/src/picker/application/getPicker.ts
  - packages/core/src/picker/application/getPicker.test.ts
autonomous: true
requirements: [PICK-01, PICK-02]
must_haves:
  truths:
    - "computePickerSnapshot reads chain + GEX context + economic events, selects+scores candidates, takes the top-N (6-8) ranked, and persists ONE picker_snapshot row stamped with the cohort's observedAt"
    - "The persisted snapshot sets source (schwab|cboe) from the chain cohort (D-15) and gexContextStatus/eventsContextStatus (D-17): 'missing' when the context is absent, 'stale' when older than the freshness window, 'ok' otherwise"
    - "When gexContextStatus/eventsContextStatus is not 'ok', the corresponding scoring term (gexFit / eventAdjustment) contributes 0 — a degraded context never produces a falsely-clean score"
    - "An empty chain cohort writes NO row and returns ok(undefined) (no crash, no NaN row); getPicker is a thin forwarder returning the latest row or null"
  artifacts:
    - packages/core/src/picker/application/computePickerSnapshot.ts
    - packages/core/src/picker/application/getPicker.ts
  key_links:
    - "This is the orchestration seam: it composes the 19-03 domain (selectCandidates + scoreCalendarCandidates) with the 19-01 ports, using in-memory twins (19-04/19-05) for tests"
    - "top-N cap and stable tie-break (by id) are set here (D-03); observedAt = the chain cohort's data instant, never now() (mirrors computeGexSnapshot)"
  prohibitions:
    - "MUST NOT stamp observedAt from now() — it derives from the chain cohort's data time (idempotency / honesty precedent)"
    - "MUST NOT silently zero a degraded context without tagging its status field — status tag and term-zeroing move together (D-17)"
    - "MUST NOT recompute on read — getPicker forwards ForReadingPickerSnapshot only"
---

<objective>
Compose the picker engine's application seam: `computePickerSnapshot` (read chain + GEX + events →
select → score → tag degraded contexts per D-17 → take top-N per D-03 → persist one JSONB row per
cohort instant per D-05/D-06) and `getPicker` (thin latest-row forwarder). Mirrors the analytics
`computeGexSnapshot` / `getGex` precompute-then-read pattern exactly.

Purpose: The wiring brain that turns domain math + ports into a persisted, honestly-staleness-tagged snapshot.
Output: `computePickerSnapshot.ts`, `getPicker.ts` under `packages/core/src/picker/application/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-picker-engine-economic-events/19-CONTEXT.md
@.planning/phases/19-picker-engine-economic-events/19-PATTERNS.md
@packages/core/src/picker/application/ports.ts
@packages/core/src/picker/domain/scoring.ts
@packages/core/src/picker/domain/candidate-selection.ts
@packages/core/src/analytics/application/computeGexSnapshot.ts
@packages/core/src/analytics/application/getGex.ts
@packages/adapters/src/memory/economic-events.ts
@packages/adapters/src/memory/picker-snapshot.ts
@.claude/rules/tdd.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/typescript.md
</context>

## Artifacts this phase produces (this plan)

- `makeComputePickerSnapshotUseCase(deps)` → `ForRunningComputePicker` (deps: readChainForPicker,
  readGexContext, readEconomicEvents, persistPickerSnapshot, rate, dividendYield, now).
- `makeGetPickerUseCase(deps)` → `ForRunningGetPicker` (deps: readPickerSnapshot).
- Exported freshness-window constants for gex/events staleness tagging.

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: computePickerSnapshot use-case (compose, tag degraded contexts, persist top-N)</name>
  <files>packages/core/src/picker/application/computePickerSnapshot.ts, packages/core/src/picker/application/computePickerSnapshot.test.ts</files>
  <read_first>
    - packages/core/src/analytics/application/computeGexSnapshot.ts (the read→guard-empty→compute→persist structure; observedAt from data not now(); ok(undefined) on empty cohort)
    - packages/core/src/picker/domain/candidate-selection.ts + scoring.ts (selectCandidates + scoreCalendarCandidates signatures composed here)
    - packages/core/src/picker/application/ports.ts (all ports + PickerSnapshot/PickerSnapshotRow the row assembles)
    - packages/contracts/src/picker.ts (the exact PickerSnapshot field set to assemble: asOf, spot, source, gexContextStatus, eventsContextStatus, termStructure, gex, events, candidates)
    - .planning/phases/19-picker-engine-economic-events/19-CONTEXT.md (D-03 top-N 6-8; D-15 source; D-17 status tagging + term-zeroing; D-18 zero-candidate case)
    - .planning/phases/19-picker-engine-economic-events/19-PATTERNS.md ("computePickerSnapshot.ts" section)
  </read_first>
  <behavior>
    - Test (in-memory twins): a seeded chain + GEX + events → persists exactly one PickerSnapshotRow whose observedAt equals the chain cohort's data time, source from the cohort, candidates sorted score-desc capped at the top-N, gexContextStatus/eventsContextStatus "ok".
    - Test: GEX context null → gexContextStatus "missing" AND every candidate's gexFit breakdown contributes 0.
    - Test: economic_events empty → eventsContextStatus "missing" AND eventAdjustment contributes 0 (no event penalty fabricated).
    - Test: GEX computedAt older than the freshness window → gexContextStatus "stale" (term still zeroed per D-17).
    - Test: empty chain cohort → no row persisted, ok(undefined).
    - Test: chain present but zero candidates survive net-θ>0 → a row IS persisted with candidates:[] (so the UI can show the D-18 "zero passed filter" message with a real asOf), not ok(undefined).
    - Test: stable tie-break — equal scores order deterministically by id.
  </behavior>
  <action>
    Implement `makeComputePickerSnapshotUseCase(deps)` returning `ForRunningComputePicker`. Steps:
    read chain (err→propagate; empty→ok(undefined)); resolve spot/asOf/source from the cohort; read GEX
    context and economic events, deriving gexContextStatus/eventsContextStatus (missing when null/empty,
    stale when computedAt/latest-event older than the exported freshness window, else ok); call
    selectCandidates(chain, events, {rate, dividendYield}) then scoreCalendarCandidates(raw, gexContextOrZeroed,
    {...}) — when a context is not "ok", pass it so the corresponding term contributes 0 (never silently
    dropped); sort score-desc with id tie-break; take the top-N (D-03, exported cap 6-8); assemble the
    PickerSnapshot (termStructure/gex/events/candidates + source + both statuses); persist a PickerSnapshotRow
    {observedAt: cohort time, snapshot}. Return ok(undefined). observedAt is NEVER now() (now injected for
    freshness bounds only). Write RED tests first against the memory twins, run to failure, then GREEN.
  </action>
  <verify>
    <automated>bun run test --project @morai/core -- picker/application/computePickerSnapshot.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test --project @morai/core -- picker/application/computePickerSnapshot.test.ts` exits 0 with the missing-GEX-zeroing, missing-events-zeroing, stale-tagging, empty-cohort, and zero-candidate-row cases all green.
    - `rg -n 'gexContextStatus|eventsContextStatus' packages/core/src/picker/application/computePickerSnapshot.ts` shows both statuses set on the persisted snapshot.
    - `rg -n 'deps.now\(\)' packages/core/src/picker/application/computePickerSnapshot.ts` shows now() used only for freshness bounds, never assigned to observedAt.
  </acceptance_criteria>
  <done>The use-case persists one honestly-tagged, top-N snapshot per cohort, zeroing degraded-context terms without ever hiding them.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: getPicker use-case (thin latest-row forwarder)</name>
  <files>packages/core/src/picker/application/getPicker.ts, packages/core/src/picker/application/getPicker.test.ts</files>
  <read_first>
    - packages/core/src/analytics/application/getGex.ts (verbatim thin-forwarder template — copy shape, rename types)
    - packages/core/src/picker/application/ports.ts (ForReadingPickerSnapshot, ForRunningGetPicker, PickerSnapshotRow)
  </read_first>
  <behavior>
    - getPicker returns ok(row) when a snapshot exists, ok(null) when none, and propagates err unchanged.
    - No business logic / no recompute — a pure forward of ForReadingPickerSnapshot.
  </behavior>
  <action>
    Implement `makeGetPickerUseCase({ readPickerSnapshot })` returning `ForRunningGetPicker` = `() =>
    readPickerSnapshot()`. Copy getGex.ts's shape and doc-comment ("NEVER recomputed on read"). Write a
    RED test (ok(row) / ok(null) / err passthrough via a fake port), run to failure, then GREEN.
  </action>
  <verify>
    <automated>bun run test --project @morai/core -- picker/application/getPicker.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test --project @morai/core -- picker/application/getPicker.test.ts` exits 0 (row / null / err passthrough green).
    - `rg -n 'select|compute|score' packages/core/src/picker/application/getPicker.ts` returns no match (pure forwarder).
  </acceptance_criteria>
  <done>getPicker forwards the latest row with zero recompute.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| degraded context → score | Missing/stale GEX or events could silently produce a falsely-clean score |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-13 | Tampering (false-clean) | degraded gex/events context | high | mitigate | Status tag + term-zeroing move together (D-17); tests assert the term contributes 0 exactly when the status is not "ok" |
| T-19-14 | Tampering (data integrity) | observedAt stamped from now() | medium | mitigate | observedAt derives from the chain cohort's data time (asserted); now() only bounds freshness |
| T-19-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new dependencies (v1.2 lock); no install task; no legitimacy checkpoint applies |
</threat_model>

<verification>
- `bun run test --project @morai/core -- picker/application` green (both use-cases).
- `bun run typecheck` green.
</verification>

<success_criteria>
The compute + get use-cases exist, honestly tag degraded contexts, persist top-N per cohort, and read without recompute.
</success_criteria>

<output>
Create `.planning/phases/19-picker-engine-economic-events/19-06-SUMMARY.md` when done.
</output>
