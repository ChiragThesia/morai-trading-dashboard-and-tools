---
phase: 19-picker-engine-economic-events
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/picker.ts
  - packages/contracts/src/__fixtures__/picker-candidates.fixture.ts
  - packages/contracts/src/picker.test.ts
  - packages/core/src/picker/application/ports.ts
autonomous: true
requirements: [PICK-01, PICK-02]
must_haves:
  truths:
    - "pickerSnapshotResponse.parse accepts a payload carrying source/gexContextStatus/eventsContextStatus and rejects one missing any of them"
    - "The Phase-18 fixture still satisfies pickerSnapshotResponse after the additive change (bun run typecheck green in apps/web)"
    - "packages/core/src/picker/application/ports.ts compiles and exports the full picker driven-port + row-type set, importing ONLY @morai/shared"
  artifacts:
    - packages/contracts/src/picker.ts
    - packages/contracts/src/__fixtures__/picker-candidates.fixture.ts
    - packages/core/src/picker/application/ports.ts
  key_links:
    - "source (D-15) + gexContextStatus/eventsContextStatus (D-17) are the ONLY additive fields; every existing field stays byte-identical (import-only swap promise, PICK-02)"
    - "PickerSnapshot domain type in core mirrors pickerSnapshotResponse field-for-field — adapters (not core) parse through the contract at the boundary"
  prohibitions:
    - "MUST NOT rename or remove any existing pickerSnapshotResponse/pickerCandidate field (one-sided rename fails bun run typecheck; breaks the fixture→live import-only swap)"
    - "MUST NOT import @morai/contracts from packages/core (hexagon law §2 — core imports @morai/shared only; ports.ts re-declares StorageError/FetchError locally)"
    - "MUST NOT add a REFUTED criterion to the breakdownEntry enum (it stays closed: slope|fwdEdge|gexFit|eventAdjustment|beVsEm)"
---

<objective>
Establish the shared shapes every downstream picker plan fills: (a) the additive contract fields
`source` (D-15) and `gexContextStatus`/`eventsContextStatus` (D-17) on `pickerSnapshotResponse`, with
the Phase-18 fixture updated to satisfy them, and (b) the complete core picker driven-port set +
row/domain types in a new `packages/core/src/picker/application/ports.ts`.

Purpose: Interface-first. Later plans (domain, adapters, use-cases, surfaces) import these types and
never re-open this file, so waves parallelize cleanly.
Output: Additive contract + fixture; core picker ports module.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-picker-engine-economic-events/19-CONTEXT.md
@.planning/phases/19-picker-engine-economic-events/19-PATTERNS.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/typescript.md
@.claude/rules/tdd.md
</context>

## Artifacts this phase produces (this plan)

- Contract fields added: `pickerSnapshotResponse.source` (`z.enum(["schwab","cboe"])`),
  `pickerSnapshotResponse.gexContextStatus` and `.eventsContextStatus` (`z.enum(["ok","stale","missing"])`).
- New core module `packages/core/src/picker/application/ports.ts` exporting: types `StorageError`,
  `FetchError`, `EconomicEvent`, `ChainQuoteForPicker`, `GexContextForPicker`, `PickerCandidateDomain`,
  `PickerSnapshot`, `PickerSnapshotRow`; ports `ForReadingChainForPicker`, `ForReadingGexContext`,
  `ForFetchingEconomicEvents`, `ForReadingEconomicEvents`, `ForPersistingEconomicEvents`,
  `ForPersistingPickerSnapshot`, `ForReadingPickerSnapshot`, `ForRunningComputePicker`,
  `ForRunningGetPicker`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add source + context-status fields to pickerSnapshotResponse (additive)</name>
  <files>packages/contracts/src/picker.ts, packages/contracts/src/picker.test.ts</files>
  <read_first>
    - packages/contracts/src/picker.ts (the frozen contract; edit region is `pickerSnapshotResponse`, lines ~156-164)
    - .planning/phases/19-picker-engine-economic-events/19-PATTERNS.md ("packages/contracts/src/picker.ts" section — Pattern 4 shows the exact additive diff and field ordering)
    - .planning/phases/19-picker-engine-economic-events/19-RESEARCH.md (Pattern 4)
  </read_first>
  <behavior>
    - pickerSnapshotResponse.parse SUCCEEDS on a payload with source:"schwab", gexContextStatus:"ok", eventsContextStatus:"ok" plus all existing fields.
    - pickerSnapshotResponse.parse FAILS when source is omitted, and FAILS when either status field is omitted.
    - source only accepts "schwab"|"cboe"; the two status fields only accept "ok"|"stale"|"missing".
  </behavior>
  <action>
    Insert three additive fields into `pickerSnapshotResponse` after `spot` and before `termStructure`,
    matching the ordering in 19-PATTERNS.md: `source: z.enum(["schwab","cboe"])` (D-15),
    `gexContextStatus: z.enum(["ok","stale","missing"])` and `eventsContextStatus: z.enum(["ok","stale","missing"])`
    (D-17). Leave every existing field and every other exported schema (pickerCandidate, breakdownEntry
    closed enum, exitPlan, pickerGexContext, pickerEvent, termStructurePoint) unchanged. Write the RED
    test in picker.test.ts first (asserting parse-success with the fields present and parse-failure when
    each is omitted), run it to confirm it fails for the assertion reason, then add the fields to reach GREEN.
  </action>
  <verify>
    <automated>bun run test --project @morai/contracts -- picker.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'source: z.enum\(\["schwab", ?"cboe"\]\)' packages/contracts/src/picker.ts` matches.
    - `rg -c 'z.enum\(\["ok", ?"stale", ?"missing"\]\)' packages/contracts/src/picker.ts` returns 2.
    - `bun run test --project @morai/contracts -- picker.test.ts` exits 0 with the new parse-success and parse-failure cases green.
  </acceptance_criteria>
  <done>pickerSnapshotResponse carries source + both context-status enums; the closed breakdownEntry enum is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Update picker-candidates fixture to satisfy the new required fields</name>
  <files>packages/contracts/src/__fixtures__/picker-candidates.fixture.ts</files>
  <read_first>
    - packages/contracts/src/__fixtures__/picker-candidates.fixture.ts (the Phase-18 fixture; add the three new snapshot-level fields)
    - packages/contracts/src/picker.ts (the schema the fixture must satisfy after Task 1)
  </read_first>
  <action>
    Add `source: "schwab"`, `gexContextStatus: "ok"`, `eventsContextStatus: "ok"` to the top-level
    `pickerSnapshotFixture` object (snapshot-level, next to `asOf`/`spot`). Do NOT alter any candidate,
    the guard-case row, events, or termStructure. This keeps `Analyzer.tsx`'s existing synchronous fixture
    import type-valid until Plan 09 swaps it for live data. The fixture stays a value-only module (no logic).
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'source: "schwab"' packages/contracts/src/__fixtures__/picker-candidates.fixture.ts` matches.
    - `bun run typecheck` exits 0 (fixture satisfies the post-Task-1 `PickerSnapshotResponse` type; apps/web's fixture import still compiles).
  </acceptance_criteria>
  <done>The frozen fixture satisfies the additive contract with zero candidate/event/termStructure edits.</done>
</task>

<task type="auto">
  <name>Task 3: Create the picker core ports module (driven ports + row/domain types)</name>
  <files>packages/core/src/picker/application/ports.ts</files>
  <read_first>
    - packages/core/src/analytics/application/ports.ts (verbatim structural template: header comment, local StorageError, LegObsForGex/GexSnapshotRow row types, ForVerbingNoun port naming)
    - packages/core/src/journal/application/ports.ts (FetchError shape: `{ readonly kind: "fetch-error"; readonly message: string }`)
    - packages/contracts/src/picker.ts (the shape PickerSnapshot must mirror field-for-field — asOf, spot, source, gexContextStatus, eventsContextStatus, termStructure, gex, events, candidates)
    - .planning/phases/19-picker-engine-economic-events/19-PATTERNS.md ("packages/core/src/picker/application/ports.ts" section — the exact port set to declare)
  </read_first>
  <action>
    Create `packages/core/src/picker/application/ports.ts` importing ONLY `@morai/shared` (hexagon law §2).
    Re-declare `StorageError` and `FetchError` locally (analytics/journal precedent — no cross-context import).
    Declare row/domain types: `EconomicEvent` (`{ date: string /* YYYY-MM-DD ET calendar day */; name: "FOMC"|"CPI"|"NFP"; source: "fred"|"seed" }`);
    `ChainQuoteForPicker` (`{ time: Date; strike: number /* ×1000 int, e.g. 7400000 */; expiration: string; contractType: "C"|"P"; underlyingPrice: number; bsmIv: string | null; source: "schwab"|"cboe" }`);
    `GexContextForPicker` (`{ flip: number|null; callWall: number|null; putWall: number|null; netGammaAtSpot: number; absGammaStrike: number|null; computedAt: Date }`);
    `PickerCandidateDomain` (readonly mirror of `pickerCandidate` — id, name, score, breakdown[], debit, theta, vega, delta, fwdIv, fwdIvGuard, slope, fwdEdge, expectedMove, frontEvents[], backEvents[], frontLeg, backLeg, exitPlan);
    `PickerSnapshot` (readonly mirror of `pickerSnapshotResponse` incl. source/gexContextStatus/eventsContextStatus);
    `PickerSnapshotRow` (`{ observedAt: Date; snapshot: PickerSnapshot }`).
    Declare ports (ForVerbingNoun): `ForReadingChainForPicker` (`() => Promise<Result<ReadonlyArray<ChainQuoteForPicker>, StorageError>>`),
    `ForReadingGexContext` (`() => Promise<Result<GexContextForPicker | null, StorageError>>`),
    `ForFetchingEconomicEvents` (`() => Promise<Result<ReadonlyArray<EconomicEvent>, FetchError>>`),
    `ForReadingEconomicEvents` (`() => Promise<Result<ReadonlyArray<EconomicEvent>, StorageError>>`),
    `ForPersistingEconomicEvents` (`(rows: ReadonlyArray<EconomicEvent>) => Promise<Result<void, StorageError>>`),
    `ForPersistingPickerSnapshot` (`(row: PickerSnapshotRow) => Promise<Result<void, StorageError>>`),
    `ForReadingPickerSnapshot` (`() => Promise<Result<PickerSnapshotRow | null, StorageError>>`),
    `ForRunningComputePicker` (`() => Promise<Result<void, StorageError>>`),
    `ForRunningGetPicker` (`() => Promise<Result<PickerSnapshotRow | null, StorageError>>`).
    Prefer `readonly`/`ReadonlyArray` throughout. This file is type-only — no runtime logic, no test (typecheck is the gate).
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c 'export type For' packages/core/src/picker/application/ports.ts` returns 8.
    - `rg -n '@morai/contracts|drizzle|process.env' packages/core/src/picker/application/ports.ts` returns no matches (hexagon-pure).
    - `bun run typecheck` exits 0.
  </acceptance_criteria>
  <done>The picker context has a complete, hexagon-pure port + row-type module ready for downstream import.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan defines types/schemas only — no runtime I/O, no new external surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Tampering | Additive contract fields | low | mitigate | Fields are additive-only; existing fields byte-identical → no shape break for existing parsers (bun run typecheck enforces) |
| T-19-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new dependencies this phase (v1.2 lock); no install task exists, so no package-legitimacy checkpoint applies |
</threat_model>

<verification>
- `bun run test --project @morai/contracts -- picker.test.ts` green.
- `bun run typecheck` green (fixture + core ports + apps/web fixture import all compile).
</verification>

<success_criteria>
Additive contract fields land, fixture satisfies them, and the core picker ports module compiles hexagon-pure.
</success_criteria>

<output>
Create `.planning/phases/19-picker-engine-economic-events/19-01-SUMMARY.md` when done.
</output>
