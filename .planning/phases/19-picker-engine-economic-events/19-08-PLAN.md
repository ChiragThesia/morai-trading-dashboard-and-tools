---
phase: 19-picker-engine-economic-events
plan: 08
type: execute
wave: 5
depends_on: ["19-04", "19-05", "19-06"]
files_modified:
  - apps/worker/src/handlers/compute-picker.ts
  - apps/worker/src/handlers/compute-picker.test.ts
  - apps/worker/src/handlers/fetch-economic-events.ts
  - apps/worker/src/handlers/fetch-economic-events.test.ts
  - apps/worker/src/handlers/compute-gex-snapshot.ts
  - apps/worker/src/handlers/compute-gex-snapshot.test.ts
  - apps/worker/src/schedule.ts
  - apps/worker/src/schedule.test.ts
  - apps/worker/src/main.ts
autonomous: true
requirements: [PICK-01, PICK-03]
must_haves:
  truths:
    - "compute-picker is chain-triggered by compute-gex-snapshot on success (it needs the GEX context, criterion 7) and is the new terminal job in the analytics chain"
    - "fetch-economic-events runs on a weekly cron (Friday, America/New_York), fetches the unified FRED+seed events, and persists them to economic_events"
    - "compute-gex-snapshot's handler now enqueues compute-picker on success with a singletonKey (mirrors compute-analytics → compute-gex-snapshot), gated by the same RTH+holiday check"
    - "The full worker suite (schedule + all handlers) stays green with 12 queues wired"
  artifacts:
    - "compute-picker queue + handler (chain-triggered, no cron)"
    - "fetch-economic-events queue + weekly cron + handler"
    - "compute-gex-snapshot → compute-picker chain-trigger edit"
    - "worker composition wiring for the picker + economic-events adapters/use-cases"
  key_links:
    - "compute-picker reads the chain (readChainForPicker), the GEX context (adapted from the existing gex snapshot read → GexContextForPicker), and economic_events, then persists a picker_snapshot row"
    - "the GEX-context adapter maps GexSnapshotRow → GexContextForPicker at the composition root, keeping the picker core free of an analytics-context import"
  prohibitions:
    - "MUST NOT give compute-picker a cron — it is chain-triggered only (D-04, mirrors compute-gex-snapshot)"
    - "MUST NOT schedule fetch-economic-events twice with the default key (CR-01: distinct keys required or the second overwrites the first)"
    - "MUST NOT store economic-event dates as instants in the persistence path (plain-date discipline carried from 19-04)"
---

<objective>
Wire the picker engine into the worker (PICK-01/PICK-03): a chain-triggered `compute-picker` job (fires
after `compute-gex-snapshot`, D-04), a weekly `fetch-economic-events` cron (Friday ET, D-14), the
`compute-gex-snapshot` handler edit that enqueues `compute-picker` on success, the schedule.ts
registrations, and the composition-root wiring (adapters → use-cases → handlers).

Purpose: The precompute pipeline that produces a fresh picker_snapshot ~13×/day RTH and refreshes the events table weekly.
Output: two new job handlers, one chain-trigger edit, schedule + main.ts wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-picker-engine-economic-events/19-CONTEXT.md
@.planning/phases/19-picker-engine-economic-events/19-PATTERNS.md
@apps/worker/src/schedule.ts
@apps/worker/src/handlers/compute-gex-snapshot.ts
@apps/worker/src/handlers/compute-analytics.ts
@apps/worker/src/handlers/fetch-cot.ts
@packages/core/src/picker/application/computePickerSnapshot.ts
@packages/core/src/picker/application/ports.ts
@.claude/rules/tdd.md
@.claude/rules/architecture-boundaries.md
</context>

## Artifacts this phase produces (this plan)

- `makeComputePickerHandler(deps)` (thin pg-boss adapter: array-guard → RTH+holiday gate → call
  computePicker use-case → throw on err; terminal, no boss.send).
- `makeFetchEconomicEventsHandler(deps)` (fetch unified events → persist to economic_events).
- compute-gex-snapshot handler gains a `boss` dep + `boss.send("compute-picker", {}, {singletonKey})` on success.
- schedule.ts: `compute-picker` queue (no cron) + `fetch-economic-events` queue+cron+work; AllHandlers += computePicker, fetchEconomicEvents (12 queues total).
- Worker composition: picker-chain repo, gex-context adapter (GexSnapshotRow→GexContextForPicker), economic-events repo, picker-snapshot repo, computePicker + fetchEconomicEvents use-cases + handlers.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: compute-picker + fetch-economic-events handlers + chain-trigger edit</name>
  <files>apps/worker/src/handlers/compute-picker.ts, apps/worker/src/handlers/compute-picker.test.ts, apps/worker/src/handlers/fetch-economic-events.ts, apps/worker/src/handlers/fetch-economic-events.test.ts, apps/worker/src/handlers/compute-gex-snapshot.ts, apps/worker/src/handlers/compute-gex-snapshot.test.ts</files>
  <read_first>
    - apps/worker/src/handlers/compute-gex-snapshot.ts (current TERMINAL handler — array-guard + RTH/holiday gate; add a boss dep + boss.send to un-terminalize it)
    - apps/worker/src/handlers/compute-analytics.ts (the boss.send("...", {}, {singletonKey}) fire-and-forget chain-trigger pattern to copy for both the gex→picker edit and the compute-picker terminal shape)
    - apps/worker/src/handlers/fetch-cot.ts (the fetch→persist handler shape for fetch-economic-events)
    - packages/core/src/picker/application/computePickerSnapshot.ts (ForRunningComputePicker the handler calls)
    - packages/core/src/picker/application/ports.ts (ForFetchingEconomicEvents + ForPersistingEconomicEvents for the events handler)
  </read_first>
  <behavior>
    - compute-picker handler: array-guard undefined; RTH+holiday gate no-ops; on gate-pass calls the use-case; err → throw (pg-boss retry); terminal (no boss.send).
    - fetch-economic-events handler: calls the fetch use-case, persists results; err → throw; runs regardless of RTH (weekly, like fetch-cot).
    - compute-gex-snapshot handler: unchanged gate behavior, but on use-case success now does `void boss.send("compute-picker", {}, {singletonKey:"triggered-by-gex"})` (fire-and-forget, a send failure does not fail the gex job).
  </behavior>
  <action>
    Implement `makeComputePickerHandler({computePickerUseCase, now})` mirroring makeComputeGexSnapshotHandler
    (array-guard → RTH+holiday gate → call use-case → throw on err; terminal). Implement
    `makeFetchEconomicEventsHandler({fetchEconomicEvents, persistEconomicEvents, now})` mirroring fetch-cot
    (fetch → persist → throw on err). Edit compute-gex-snapshot.ts: add a `boss: BossForChainHandler` dep and,
    after the successful use-case call, `void deps.boss.send("compute-picker", {}, {singletonKey:"triggered-by-gex"}).catch(...)`
    — updating its doc comment from "terminal" to "chain-triggers compute-picker (D-04)". Update the three
    handler tests: add compute-picker + fetch-economic-events tests (RED first), and extend compute-gex-snapshot.test.ts
    to assert the boss.send-on-success (its existing tests may need a boss fake added). Run RED→GREEN for each.
  </action>
  <verify>
    <automated>bun run test --project @morai/worker -- compute-picker fetch-economic-events compute-gex-snapshot</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test --project @morai/worker -- compute-picker fetch-economic-events compute-gex-snapshot` exits 0.
    - `rg -n 'boss.send\("compute-picker"' apps/worker/src/handlers/compute-gex-snapshot.ts` matches; the compute-gex-snapshot test asserts the send fires on success.
    - `rg -n 'boss.send' apps/worker/src/handlers/compute-picker.ts` returns no match (terminal job).
  </acceptance_criteria>
  <done>The two new handlers and the gex→picker chain-trigger are implemented and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: schedule.ts — register compute-picker (no cron) + fetch-economic-events (weekly)</name>
  <files>apps/worker/src/schedule.ts, apps/worker/src/schedule.test.ts</files>
  <read_first>
    - apps/worker/src/schedule.ts (the 10-queue registerAllJobs: createQueue→schedule→work order; AllHandlers type; fetch-cot weekly cron "0 17 * * 5" {tz:"America/New_York"}; the CR-01 distinct-key note at lines ~103-109)
    - apps/worker/src/schedule.test.ts (the registration-assertion shape to extend)
    - .planning/phases/19-picker-engine-economic-events/19-PATTERNS.md ("schedule.ts" section — compute-picker createQueue+work only; fetch-economic-events createQueue+schedule+work)
  </read_first>
  <behavior>
    - registerAllJobs now creates 12 queues (adds compute-picker, fetch-economic-events), keeps the createQueue→schedule→work ordering (CR-01 FK constraint).
    - compute-picker: createQueue + work, NO schedule (chain-triggered).
    - fetch-economic-events: createQueue + schedule (weekly Friday ET) + work; a distinct queue name (its own default key is fine since it schedules once).
    - AllHandlers gains computePicker + fetchEconomicEvents fields.
  </behavior>
  <action>
    Extend `AllHandlers` with `computePicker: PgBossHandler` and `fetchEconomicEvents: PgBossHandler`. In
    registerAllJobs add `createQueue("compute-picker")` (comment: chain-triggered by compute-gex-snapshot;
    no cron, D-04) and `createQueue("fetch-economic-events")`; add a `schedule("fetch-economic-events",
    "0 17 * * 5", null, {tz:"America/New_York"})` (weekly Friday, D-14 default — matches fetch-cot's slot);
    add `work("compute-picker", POLLING_INTERVAL, handlers.computePicker)` and
    `work("fetch-economic-events", POLLING_INTERVAL, handlers.fetchEconomicEvents)`. Preserve createQueue→
    schedule→work phase ordering. Update the doc header queue count. Extend schedule.test.ts to assert the
    two new queues, the compute-picker no-cron, and the fetch-economic-events weekly schedule (RED first, then GREEN).
  </action>
  <verify>
    <automated>bun run test --project @morai/worker -- schedule.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test --project @morai/worker -- schedule.test.ts` exits 0 with the 12-queue + compute-picker-no-cron + weekly-events-cron assertions green.
    - `rg -n '"compute-picker"' apps/worker/src/schedule.ts` shows createQueue + work but NO schedule call for it.
    - `rg -n '"fetch-economic-events".*0 17 \* \* 5|America/New_York' apps/worker/src/schedule.ts` confirms the weekly ET cron.
  </acceptance_criteria>
  <done>Both jobs are registered with the correct cron/no-cron semantics and the suite is green.</done>
</task>

<task type="auto">
  <name>Task 3: Worker composition — wire picker + economic-events adapters, use-cases, handlers</name>
  <files>apps/worker/src/main.ts</files>
  <read_first>
    - apps/worker/src/main.ts (the composition root: makeFredSeriesAdapter + macroObsRepo wiring ~155-168; gexRepo + computeGexSnapshotUseCase ~205-213; handler construction ~215-257; registerAllJobs call at end)
    - packages/adapters/src/http/economic-events.ts + memory/postgres economic-events repos (constructors)
    - packages/adapters/src/postgres/repos/picker-chain.ts + picker-snapshot.ts (constructors)
    - packages/core/src/picker/application/computePickerSnapshot.ts (deps it needs, incl. rate/dividendYield from config)
    - packages/core/src/analytics/application/getGex.ts / the gex snapshot repo readGexSnapshot (the source to adapt into GexContextForPicker)
  </read_first>
  <action>
    In the worker composition root, construct: `makeEconomicEventsAdapter({fetch: globalThis.fetch, apiKey:
    config.FRED_API_KEY, fomcSeed: FOMC_SEED})`, `makePostgresEconomicEventsRepo(db)`,
    `makePostgresPickerChainRepo(db)`, `makePostgresPickerSnapshotRepo(db)`; a small
    `readGexContextForPicker` adapter that calls the existing gex snapshot read and maps GexSnapshotRow →
    GexContextForPicker (flip/callWall/putWall/netGammaAtSpot/absGammaStrike/computedAt), keeping the picker
    core free of an analytics import; then `makeComputePickerSnapshotUseCase({readChainForPicker,
    readGexContext, readEconomicEvents, persistPickerSnapshot, rate: config.BSM_RATE_FALLBACK,
    dividendYield: config.BSM_DIVIDEND_YIELD, now})`; a `makeFetchEconomicEvents` use-case (or inline
    fetch→persist wiring) for the events handler. Build `computePickerHandler` and `fetchEconomicEventsHandler`,
    add `boss` to the compute-gex-snapshot handler construction (it now chain-triggers), and pass
    `computePicker` + `fetchEconomicEvents` into the AllHandlers object handed to registerAllJobs.
    Composition-only (TDD-exempt).
  </action>
  <verify>
    <automated>bun run typecheck && bun run build --filter @morai/worker</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'makeComputePickerSnapshotUseCase|makeEconomicEventsAdapter|computePicker:|fetchEconomicEvents:' apps/worker/src/main.ts` matches.
    - The compute-gex-snapshot handler construction now passes `boss`.
    - `bun run typecheck` green; the worker builds.
  </acceptance_criteria>
  <done>The worker composes the picker + economic-events pipeline end to end and builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FRED HTTP → fetch-economic-events job | External JSON enters the weekly cron path (mitigated in 19-04's adapter) |
| chain-trigger enqueue | compute-gex-snapshot → compute-picker fire-and-forget send |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-18 | Denial of Service | duplicate compute-picker enqueues | medium | mitigate | singletonKey on the boss.send (mirrors compute-analytics→gex T-08-10) prevents duplicate enqueues |
| T-19-19 | Availability | a failed events fetch stalling the picker | medium | mitigate | fetch-economic-events err → throw for pg-boss retry; compute-picker still runs and tags eventsContextStatus honestly (D-17), never blocked on events |
| T-19-20 | Tampering (config) | double-scheduled events cron overwriting itself | medium | mitigate | Single weekly schedule with a stable key; CR-01 distinct-key discipline honored (no second same-name schedule) |
| T-19-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new dependencies (v1.2 lock); no install task; no legitimacy checkpoint applies |
</threat_model>

<verification>
- `bun run test --project @morai/worker` green (handlers + schedule).
- `bun run typecheck && bun run build --filter @morai/worker` green.
- compute-picker chain-triggered (no cron); fetch-economic-events weekly ET.
</verification>

<success_criteria>
The worker produces picker_snapshot rows on the chain trigger and refreshes economic_events weekly, with a green suite.
</success_criteria>

<output>
Create `.planning/phases/19-picker-engine-economic-events/19-08-SUMMARY.md` when done.
</output>
