---
phase: 19-picker-engine-economic-events
plan: 03
type: tdd
wave: 2
depends_on: ["19-01", "19-02"]
files_modified:
  - packages/core/src/picker/domain/types.ts
  - packages/core/src/picker/domain/candidate-selection.ts
  - packages/core/src/picker/domain/candidate-selection.test.ts
  - packages/core/src/picker/domain/scoring.ts
  - packages/core/src/picker/domain/scoring.test.ts
autonomous: true
requirements: [PICK-01, PICK-03]
must_haves:
  truths:
    - "selectCandidates picks, per front/back expiry pair, the chain strike whose BSM put delta is nearest each target rung (ATM/-0.30/-0.20/-0.10), over the DTE grid front 21-36 / back front+21..80, and drops any calendar with net theta <= 0"
    - "A leg 'spans' an event iff the event ISO date is in (today, legExpiry] — string-interval membership, no Date arithmetic across a timezone"
    - "scoreCalendarCandidates emits a 0-100 score and a breakdown[] whose criterion labels are a subset of {slope,fwdEdge,gexFit,eventAdjustment,beVsEm}, using the named 40/25/15/10/10 weights and the REAL beVsEm ratio (breakeven width / expected move)"
    - "Every candidate carries fwdIv/fwdIvGuard from computeFwdIv; an inverted candidate still gets a finite score (fwdEdge term contributes 0), never NaN"
  artifacts:
    - packages/core/src/picker/domain/types.ts
    - packages/core/src/picker/domain/candidate-selection.ts
    - packages/core/src/picker/domain/scoring.ts
  key_links:
    - "selectCandidates converts LegObsForGex-style ×1000 strikes to points ONCE at this boundary; the domain math never sees the ×1000 form (Pitfall 1)"
    - "dedupe keys on (deltaRung, frontExpiry) not on resolved strike, since delta-targeted strikes move run-to-run (Pitfall 5)"
    - "front-leg event penalty (D-11) uses named per-event tunable weights (default 0.5 FOMC/CPI/NFP); back-leg events are informational only (D-11 criterion 4)"
  prohibitions:
    - "MUST NOT encode any REFUTED criterion — no IV-rank gate, no IV-diff band, no debit-percent-of-back band; the produced breakdown criterion set stays within the closed enum"
    - "MUST NOT ship the mockup's faked beVsEm strike proxy (the K-equals-a-fixed-strike ? 1 : 0.7 term) — compute the real breakeven-width/expected-move ratio via findBreakevens (D-09)"
    - "MUST NOT re-implement BSM — price/greeks come from @morai/quant bsmPrice/bsmGreeks only (Pitfall 2)"
    - "MUST NOT compare event dates via Date-object arithmetic across a timezone — use ISO YYYY-MM-DD string comparison (Pitfall 3)"
---

<objective>
Port and generalize (D-07) the mockup's `buildCandidates()` scoring engine into two hexagon-pure domain
modules: `candidate-selection.ts` (delta-targeted OTM-put strike/expiry universe over the live chain
per D-01/D-02, net-θ>0 filter, event-span flags per D-10, dedupe per Pitfall 5) and `scoring.ts`
(the named 40/25/15/10/10 weighted score per D-08 with the REAL beVsEm ratio per D-09, emitting the
closed-enum breakdown). Pricing flows through `@morai/quant`; the fwdIv guard and breakevens come from
plan 19-02.

Purpose: This IS the picker engine's brain — the mockup made real and more honest (real BE-vs-EM).
Output: `types.ts`, `candidate-selection.ts`, `scoring.ts` under `packages/core/src/picker/domain/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-picker-engine-economic-events/19-CONTEXT.md
@.planning/phases/19-picker-engine-economic-events/19-RESEARCH.md
@.planning/phases/19-picker-engine-economic-events/19-PATTERNS.md
@.planning/research/calendar-selection-criteria.md
@packages/core/src/picker/application/ports.ts
@packages/core/src/picker/domain/fwd-iv.ts
@packages/core/src/picker/domain/breakevens.ts
@.claude/rules/tdd.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/typescript.md
</context>

## Artifacts this phase produces (this plan)

- `packages/core/src/picker/domain/types.ts` — `RawCandidate` (selection output → scoring input) +
  `ScoredCandidate` (scoring output, mirrors PickerCandidateDomain) + scoring tunable constants type.
- `selectCandidates(chain, events, params)` → `ReadonlyArray<RawCandidate>` and the selection helpers
  `nearestStrikeByDelta`, `legSpansEvents`, plus exported DTE-grid + delta-rung named constants.
- `scoreCalendarCandidates(rawCandidates, gexContext, params)` → `ReadonlyArray<ScoredCandidate>` and the
  exported named weight constants (`WEIGHT_SLOPE=40`, `WEIGHT_FWD_EDGE=25`, `WEIGHT_GEX_FIT=15`,
  `WEIGHT_EVENT=10`, `WEIGHT_BE_VS_EM=10`) + per-event penalty constants (`EVENT_PENALTY`).

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: candidate-selection — delta-targeted universe, DTE grid, theta filter, event spans</name>
  <files>packages/core/src/picker/domain/types.ts, packages/core/src/picker/domain/candidate-selection.ts, packages/core/src/picker/domain/candidate-selection.test.ts</files>
  <read_first>
    - mockups/playground-v4.html (buildCandidates lines 246-284: DTE filters `dte(fe)>=21&&dte(fe)<=36` and `dte(be)-dte(fe)>=21&&dte(be)<=80`; legEvents 242-244; dedupe `key=K+"-"+fe`)
    - packages/quant/src/bsm.ts (bsmGreeks returns {delta,gamma,theta(/365.25),vega(/100)}; call bsmGreeks(S,K,T,sigma,r,q,"P") to get per-leg delta/theta for selection and the net-θ filter)
    - packages/core/src/picker/application/ports.ts (ChainQuoteForPicker: strike ×1000, bsmIv string|null, contractType, expiration, underlyingPrice, source; EconomicEvent)
    - packages/core/src/analytics/application/computeGexSnapshot.ts (strike-iteration / cohort-spot averaging shape; ×1000→points conversion discipline)
    - .planning/phases/19-picker-engine-economic-events/19-RESEARCH.md (Pitfall 1 ×1000 unit, Pitfall 5 dedupe re-key, Open Question 2 delta rungs -0.30/-0.20/-0.10 + ATM)
    - .planning/phases/19-picker-engine-economic-events/19-PATTERNS.md ("candidate-selection.ts" section)
  </read_first>
  <behavior>
    - Test: given a small synthetic put chain across two expiries, nearestStrikeByDelta picks the strike whose bsmGreeks put delta is closest to each target rung (-0.50 ATM, -0.30, -0.20, -0.10).
    - Test: only front legs with DTE in [21,36] and back legs with (backDTE-frontDTE)>=21 and backDTE<=80 form pairs.
    - Test: a calendar with net theta (backθ - frontθ)*100 <= 0 is dropped (criterion 6).
    - Test (fast-check): legSpansEvents(expiryISO, todayISO, events) returns exactly the event names whose date d satisfies todayISO < d <= expiryISO (string compare), for any set of ISO dates.
    - Test: dedupe keeps one candidate per (deltaRung, frontExpiry) — never per resolved strike value.
  </behavior>
  <action>
    In `types.ts` declare `RawCandidate` (frontLeg/backLeg legs in POINTS, per-leg IV, front/back DTE,
    net theta/vega/delta in dollars, debit in dollars, slope, spot, deltaRung label "ATM"|"30D"|"20D"|"10D",
    frontEvents/backEvents string arrays, id, name) and `ScoredCandidate` (RawCandidate-derived +
    score:number + breakdown:ReadonlyArray<{criterion,weight,rawValue,contribution}> + fwdIv/fwdIvGuard +
    fwdEdge + expectedMove + exitPlan). In `candidate-selection.ts` implement: (1) convert
    ChainQuoteForPicker.strike ×1000 → points ONCE at entry; parse bsmIv (skip null/non-finite legs);
    (2) resolve cohort spot (average underlyingPrice) and asOf/source from the cohort; (3) `nearestStrikeByDelta`
    scanning puts per expiry for the closest bsmGreeks put-delta to each rung; (4) build front/back expiry
    pairs over the exported DTE-grid constants; (5) price legs + greeks via @morai/quant, compute net
    theta/vega/delta and debit; (6) drop net-θ<=0; (7) `legSpansEvents` via ISO string interval (today, expiry];
    frontEvents = spans(front), backEvents = spans(back) minus frontEvents; (8) slope = (ivB-ivF)/(tb-tf)*365;
    (9) dedupe by (deltaRung, frontExpiry). r and q are params (use-case supplies from config). Write RED
    tests first (examples + the legSpansEvents fast-check property), run to failure, then GREEN.
  </action>
  <verify>
    <automated>bun run test --project @morai/core -- picker/domain/candidate-selection.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test --project @morai/core -- picker/domain/candidate-selection.test.ts` exits 0 with the legSpansEvents fast-check property and the net-θ-filter example both green.
    - `rg -n 'normCdf|putPrice\b' packages/core/src/picker/domain/candidate-selection.ts` returns no matches; `rg -n 'bsmGreeks|bsmPrice' packages/core/src/picker/domain/candidate-selection.ts` matches.
    - `rg -n 'new Date\(' packages/core/src/picker/domain/candidate-selection.ts` returns no match inside the event-span comparison path (ISO string compare only, Pitfall 3).
  </acceptance_criteria>
  <done>The delta-targeted universe, DTE grid, net-θ filter, and event-span flags are implemented and property-tested.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: scoreCalendarCandidates — named-weight score + closed-enum breakdown + real beVsEm</name>
  <files>packages/core/src/picker/domain/scoring.ts, packages/core/src/picker/domain/scoring.test.ts</files>
  <read_first>
    - mockups/playground-v4.html (score formula lines 267-271: 40·clamp(slope/0.6) + 25·clamp((fwdEdge+0.02)/0.04) + 15·gexFit + 10·max(0,1-evtPenalty) + 10·[LAST TERM REPLACED]; gexFit tiers line ~265; evtPenalty ~264; em ~266)
    - packages/core/src/picker/domain/fwd-iv.ts (computeFwdIv — criterion 1; fwdEdge = ivF - fwdIv, 0 when inverted)
    - packages/core/src/picker/domain/breakevens.ts (findBreakevens — the real beVsEm numerator: breakeven width = upper-lower breakeven, or a documented fallback when <2 breakevens)
    - packages/contracts/src/picker.ts (breakdownEntry closed enum + pickerCandidate score/fwdIv/fwdEdge/expectedMove fields the ScoredCandidate must satisfy)
    - .planning/phases/19-picker-engine-economic-events/19-CONTEXT.md (D-08 weights as documented tunables; D-09 real beVsEm; D-11 front-leg penalty weights)
    - .planning/phases/19-picker-engine-economic-events/19-PATTERNS.md ("scoring.ts" section)
  </read_first>
  <behavior>
    - Test: a normal candidate produces breakdown entries for exactly the five closed-enum criteria, each contribution in [0,100], weights 40/25/15/10/10, and score = rounded sum of (weight·contribution/100), clamped 0-100.
    - Test: the produced breakdown criterion set is a subset of the allowed five labels (asserts REFUTED-criteria absence structurally — no label outside the enum).
    - Test: an inverted candidate (fwdIvGuard "inverted") gets fwdEdge term contribution 0 and still a finite score (never NaN).
    - Test: beVsEm rawValue equals breakevenWidth/expectedMove computed from findBreakevens (NOT a fixed strike proxy) for a fixture candidate.
    - Test (fast-check): for arbitrary in-range inputs, score is finite and within [0,100] and every contribution within [0,100].
  </behavior>
  <action>
    Implement `scoreCalendarCandidates(rawCandidates, gexContext, params)` returning `ScoredCandidate[]`.
    Export named weight constants `WEIGHT_SLOPE=40`, `WEIGHT_FWD_EDGE=25`, `WEIGHT_GEX_FIT=15`,
    `WEIGHT_EVENT=10`, `WEIGHT_BE_VS_EM=10` with a doc comment: "not empirically calibrated — tune later
    PICK-04/05" (D-08). Export `EVENT_PENALTY` per-event tunable map (default FOMC/CPI/NFP = 0.5, D-11).
    For each raw candidate: compute fwdIv via computeFwdIv, fwdEdge = ivF - fwdIv (0 on inverted),
    expectedMove = spot·ivF·sqrt(tf/365) (reuse the mockup's em formula), evtPenalty = sum of EVENT_PENALTY
    over frontEvents only (D-11 front-leg-only), gexFit tiers from gexContext (netGamma>0 base + proximity
    to absGammaStrike / putWall), and REAL beVsEm = breakevenWidth(findBreakevens)/expectedMove with a
    documented-tunable threshold normalizer and a documented fallback contribution when <2 breakevens.
    Assemble the closed-enum breakdown[] and the final rounded, clamped score. Set fwdIv/fwdIvGuard on the
    output. Write RED tests first (examples + the subset-of-enum assertion + the fast-check bound property),
    run to failure, then GREEN.
  </action>
  <verify>
    <automated>bun run test --project @morai/core -- picker/domain/scoring.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test --project @morai/core -- picker/domain/scoring.test.ts` exits 0 with the closed-enum-subset assertion, the real-beVsEm assertion, and the fast-check 0-100 bound property all green.
    - `rg -n 'WEIGHT_SLOPE = 40|WEIGHT_FWD_EDGE = 25|WEIGHT_GEX_FIT = 15|WEIGHT_BE_VS_EM = 10' packages/core/src/picker/domain/scoring.ts` matches all four.
    - `rg -n 'findBreakevens' packages/core/src/picker/domain/scoring.ts` matches (real beVsEm); scoring.ts contains no fixed-strike equality proxy for beVsEm.
  </acceptance_criteria>
  <done>The weighted score matches the approved mockup's weights, uses the real BE-vs-EM ratio, and emits only closed-enum criteria — all fast-check covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chain data → scoring | ×1000 strike ints + string bsmIv from the DB cross into pure math; a mis-conversion produces silently-wrong scores |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-03 | Tampering (data integrity) | strike-unit ×1000 conversion boundary | high | mitigate | Convert ×1000→points ONCE at selection entry; property + example tests assert delta/GEX-proximity use point-scale strikes (Pitfall 1) |
| T-19-04 | Tampering (data integrity) | REFUTED-criteria re-encoding | high | mitigate | Breakdown criterion set asserted to be a subset of the closed enum; no refuted metric computed or emitted |
| T-19-05 | Tampering (data integrity) | faked beVsEm proxy shipping under an honest name | medium | mitigate | Real breakeven-width/EM ratio via findBreakevens, asserted against the fixed-strike proxy in a test (D-09) |
| T-19-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new dependencies (v1.2 lock); no install task; no legitimacy checkpoint applies |
</threat_model>

<verification>
- `bun run test --project @morai/core -- picker/domain` green (all four domain files).
- `bun run typecheck` green; no second BSM; no refuted criterion; real beVsEm.
</verification>

<success_criteria>
The picker scoring brain (selection + scoring) is implemented, closed-enum-safe, real-beVsEm, and fast-check covered.
</success_criteria>

<output>
Create `.planning/phases/19-picker-engine-economic-events/19-03-SUMMARY.md` when done.
</output>
