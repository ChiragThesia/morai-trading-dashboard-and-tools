---
phase: 19-picker-engine-economic-events
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/core/src/picker/domain/fwd-iv.ts
  - packages/core/src/picker/domain/fwd-iv.test.ts
  - packages/core/src/picker/domain/breakevens.ts
  - packages/core/src/picker/domain/breakevens.test.ts
autonomous: true
requirements: [PICK-01]
must_haves:
  truths:
    - "computeFwdIv returns {fwdIv:number, guard:'ok'} for a normal term structure and {fwdIv:null, guard:'inverted'} when the forward-variance radicand is < 0 — never NaN"
    - "findBreakevens returns the spot level(s) where a long put-calendar's payoff-at-front-expiry crosses zero, computed by bounded bisection over @morai/quant bsmPrice — never an ad-hoc closed form"
    - "Both functions carry fast-check property tests proving their invariants (guard never NaN; breakeven search terminates within a hard iteration cap)"
  artifacts:
    - packages/core/src/picker/domain/fwd-iv.ts
    - packages/core/src/picker/domain/breakevens.ts
  key_links:
    - "computeFwdIv is criterion 1's forward-variance identity (PICK-01); findBreakevens is the honest metric behind D-09's real BE-vs-EM ratio (replaces the mockup's faked strike proxy)"
  prohibitions:
    - "MUST NOT change the mockup's radicand guard from rad>0 to rad>=0 (rad===0 degenerately implies fwdIv=0, a valid ok result — Pattern 1)"
    - "MUST NOT introduce a second BSM implementation (no normCdf/putPrice/putGreeks under packages/core/src/picker/) — call @morai/quant bsmPrice/bsmGreeks only (Pitfall 2)"
    - "MUST NOT return NaN or throw from either function on inverted structure or a no-breakeven search — return the tagged/guarded result"
---

<objective>
Build the two pure numeric primitives the scoring engine needs: `computeFwdIv` (criterion-1
forward-variance identity with a never-NaN inverted-structure guard) and `findBreakevens` (numeric
bisection over a long put-calendar's payoff-at-front-expiry, the honest metric behind D-09's real
BE-vs-EM ratio). Both are hexagon-pure domain functions with fast-check property coverage.

Purpose: These are the reusable, independently-testable math kernels the scoring plan (19-03) composes.
Output: `fwd-iv.ts`, `breakevens.ts` under `packages/core/src/picker/domain/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-picker-engine-economic-events/19-CONTEXT.md
@.planning/phases/19-picker-engine-economic-events/19-PATTERNS.md
@.planning/phases/19-picker-engine-economic-events/19-RESEARCH.md
@.claude/rules/tdd.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/typescript.md
</context>

## Artifacts this phase produces (this plan)

- `computeFwdIv(tf: number, ivf: number, tb: number, ivb: number)` → `{ fwdIv: number; guard: "ok" } | { fwdIv: null; guard: "inverted" }`.
- `findBreakevens(input)` → `ReadonlyArray<number>` (spot levels; empty array = no breakeven within search bounds) plus its bisection constants (`BISECT_LO`, `BISECT_HI`, `BISECT_STEPS`, `MAX_ITER`).

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: computeFwdIv — forward-variance identity with never-NaN guard</name>
  <files>packages/core/src/picker/domain/fwd-iv.ts, packages/core/src/picker/domain/fwd-iv.test.ts</files>
  <read_first>
    - mockups/playground-v4.html (fwdIV, lines 238-241 — the verbatim math to port; radicand `(tb*ivb*ivb - tf*ivf*ivf)/(tb-tf)`, guard `rad>0?sqrt:null`)
    - packages/core/src/journal/domain/iv-inversion.ts (the Result/guard-typing convention to mirror; literal-tagged-union shape)
    - packages/contracts/src/picker.ts (pickerCandidate.fwdIv / fwdIvGuard fields, lines ~85-87 — the tagged-union the return type must feed)
    - .planning/phases/19-picker-engine-economic-events/19-PATTERNS.md ("fwd-iv.ts" section)
  </read_first>
  <behavior>
    - Test: normal structure (tf=21, ivf=0.1249, tb=43, ivb=0.1402) → guard "ok", fwdIv finite ≈ sqrt of a positive radicand.
    - Test: inverted structure (tf=21, ivf=0.155, tb=45, ivb=0.105) → radicand < 0 → {fwdIv:null, guard:"inverted"}.
    - fast-check property: for all finite tf<tb and positive ivf,ivb, the result is EITHER {guard:"ok", fwdIv: a finite number} OR {guard:"inverted", fwdIv:null} — never NaN, never a throw.
    - Edge: rad===0 (degenerate) → guard "ok", fwdIv 0.
  </behavior>
  <action>
    Port the mockup's `fwdIV` as `computeFwdIv(tf, ivf, tb, ivb)` returning the literal-tagged union
    `{ fwdIv: number; guard: "ok" } | { fwdIv: null; guard: "inverted" }`. T is in DTE (days), matching
    the mockup. Keep the `rad > 0` guard exactly (do not weaken to `>= 0`). Hexagon-pure: import only
    `@morai/shared` if needed (this function needs no imports). Write RED tests first (example + fast-check),
    run to confirm failure, then implement to GREEN.
  </action>
  <verify>
    <automated>bun run test --project @morai/core -- picker/domain/fwd-iv.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test --project @morai/core -- picker/domain/fwd-iv.test.ts` exits 0 with a fast-check property named for the never-NaN guard.
    - `rg -n 'normCdf|putPrice|putGreeks' packages/core/src/picker/domain/fwd-iv.ts` returns no matches (no re-implemented BSM).
    - Test file imports `fast-check`.
  </acceptance_criteria>
  <done>computeFwdIv tags inverted structure and never yields NaN, proven by example + property tests.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: findBreakevens — bisection over the calendar payoff-at-front-expiry</name>
  <files>packages/core/src/picker/domain/breakevens.ts, packages/core/src/picker/domain/breakevens.test.ts</files>
  <read_first>
    - packages/core/src/journal/domain/iv-inversion.ts (the numeric-solve-with-guard precedent: BISECT_LO/BISECT_HI/BISECT_STEPS, hard iteration cap, Result-never-NaN discipline)
    - packages/quant/src/bsm.ts (bsmPrice signature `bsmPrice(S,K,T,sigma,r,q,"P")` — the ONLY pricing engine to call)
    - mockups/playground-v4.html (candPnl, ~lines 290-296 — the payoff-at-front-expiry function shape: back-leg put value minus front-leg put value, times 100, minus debit)
    - .planning/phases/19-picker-engine-economic-events/19-RESEARCH.md (Open Question 1 — recommends numeric bisection (a) over closed-form; a calendar can have two breakevens, one, or none)
  </read_first>
  <behavior>
    - Test: a normal at-the-money-ish long put calendar (front K≈spot, back further out in time) → findBreakevens returns two spot levels straddling the max-profit region (lower and upper), both finite and within the search bounds.
    - Test: a candidate whose payoff never crosses zero within bounds → returns [] (empty), never NaN, never a throw.
    - fast-check property: the returned array length is 0, 1, or 2, every element is finite and within [BISECT_LO*spot, BISECT_HI*spot], and the search always terminates within MAX_ITER (no unbounded loop).
  </behavior>
  <action>
    Implement `findBreakevens(input)` where input carries the candidate's front/back strike (points),
    front/back DTE, per-leg IV, spot, rate r, dividend yield q, and debit (dollars). Define the
    payoff-at-front-expiry function P(S) using `@morai/quant` `bsmPrice` for both legs (front leg priced
    at T=0 intrinsic when front DTE reaches expiry per candPnl; back leg priced at its remaining T), times
    100 contract multiplier, minus debit. Scan a bounded spot grid (mirror iv-inversion's BISECT_STEPS
    subdivision of [BISECT_LO, BISECT_HI] as multipliers of spot), detect sign changes of P across adjacent
    grid points, and bisect each bracket to a tolerance for the zero-crossing spot; collect crossings
    (0, 1, or 2). Return `ReadonlyArray<number>` of breakeven spots; return `[]` when no sign change is
    found (documented guard — no breakeven within bounds). Hard iteration cap (MAX_ITER) per bracket.
    NO closed-form approximation, NO second BSM. Write RED tests first (example + fast-check), then GREEN.
  </action>
  <verify>
    <automated>bun run test --project @morai/core -- picker/domain/breakevens.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test --project @morai/core -- picker/domain/breakevens.test.ts` exits 0 with a fast-check property asserting length ∈ {0,1,2}, finiteness, and bounded termination.
    - `rg -n 'bsmPrice' packages/core/src/picker/domain/breakevens.ts` matches (uses @morai/quant); `rg -n 'normCdf|Math.exp\(-x\*x' packages/core/src/picker/domain/breakevens.ts` returns no matches.
    - A no-breakeven fixture case returns `[]` (asserted in the example test).
  </acceptance_criteria>
  <done>findBreakevens numerically resolves calendar breakevens with a bounded, guarded search and honest empty-result for none-found.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure domain math; no I/O, no external input, no new surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-02 | Tampering (data integrity) | fwd-iv / breakevens numeric output | medium | mitigate | Never-NaN guards + fast-check invariants prevent a silent NaN/Infinity poisoning a downstream score (the "false-clean" failure class the phase guards against) |
| T-19-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new dependencies (v1.2 lock); no install task; no legitimacy checkpoint applies |
</threat_model>

<verification>
- `bun run test --project @morai/core -- picker/domain` green (both files).
- `bun run typecheck` green; no second BSM implementation introduced.
</verification>

<success_criteria>
Two guarded, fast-check-covered numeric primitives exist and never emit NaN.
</success_criteria>

<output>
Create `.planning/phases/19-picker-engine-economic-events/19-02-SUMMARY.md` when done.
</output>
