---
phase: 19-picker-engine-economic-events
plan: 05
type: execute
wave: 3
depends_on: ["19-01", "19-04"]
files_modified:
  - packages/adapters/src/postgres/schema.ts
  - packages/adapters/src/postgres/repos/picker-snapshot.ts
  - packages/adapters/src/postgres/repos/picker-snapshot.contract.test.ts
  - packages/adapters/src/memory/picker-snapshot.ts
  - packages/adapters/src/memory/picker-snapshot.contract.test.ts
  - packages/adapters/src/__contract__/picker-snapshot.contract.ts
  - packages/adapters/src/postgres/repos/picker-chain.ts
  - packages/adapters/src/postgres/repos/picker-chain.contract.test.ts
  - packages/adapters/src/memory/picker-chain.ts
autonomous: false
requirements: [PICK-02]
must_haves:
  truths:
    - "The picker_snapshot Postgres repo INSERTS one JSONB blob per observedAt (append-history, no upsert) and readLatest returns the most-recent row by observedAt (null when empty)"
    - "The stored JSONB round-trips through pickerSnapshotResponse Zod validation on write and read — a blob that violates the contract is rejected at the adapter boundary, never silently stored"
    - "readChainForPicker returns the latest leg_observations put cohort with per-strike bsmIv + the cohort source (schwab|cboe) for D-15"
    - "bun run migrate applies migrations 0014 (economic_events) + 0015 (picker_snapshot) against the live schema"
  artifacts:
    - packages/adapters/src/postgres/repos/picker-snapshot.ts
    - packages/adapters/src/memory/picker-snapshot.ts
    - packages/adapters/src/postgres/repos/picker-chain.ts
    - "picker_snapshot table (schema.ts + drizzle-generated migration 0015)"
    - packages/adapters/src/__contract__/picker-snapshot.contract.ts
  key_links:
    - "observed_at is a timestamptz (a real instant, D-06) — distinct from economic_events.event_date's plain-date semantics; the JSONB snapshot column holds the whole pickerSnapshotResponse (D-05)"
    - "append-history gives PICK-04's future slope backtest free training data (D-06); the route reads only the latest row"
    - "source on the chain read flows from leg_observations.source (observation_source enum) so the compute layer can set pickerSnapshotResponse.source (D-15)"
  prohibitions:
    - "MUST NOT upsert/replace picker_snapshot rows — append only (D-06); no onConflictDoUpdate on observed_at"
    - "MUST NOT store the snapshot as loosely-typed JSON — validate through pickerSnapshotResponse at the adapter boundary before insert and after read"
    - "MUST NOT recompute anything on read — readLatest is a pure ORDER BY observed_at DESC LIMIT 1 select"
---

<objective>
Build the picker read/write persistence layer: the append-history `picker_snapshot` table (whole
`pickerSnapshotResponse` as one Zod-validated JSONB blob per observedAt, D-05/D-06) with its Postgres
repo + memory twin + shared contract suite; the `readChainForPicker` leg_observations read (per-strike
IV + source for D-15) with its twin; and the [BLOCKING] `bun run migrate` apply of migrations 0014+0015.

Purpose: The precompute-then-read substrate the compute-picker job writes and the route/MCP read.
Output: picker_snapshot table + repo + twin, chain read + twin, shared suite, live migration apply.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-picker-engine-economic-events/19-CONTEXT.md
@.planning/phases/19-picker-engine-economic-events/19-PATTERNS.md
@.planning/phases/19-picker-engine-economic-events/19-RESEARCH.md
@packages/core/src/picker/application/ports.ts
@packages/adapters/src/postgres/repos/macro-observations.ts
@packages/adapters/src/postgres/schema.ts
@.claude/rules/tdd.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/typescript.md
</context>

## Artifacts this phase produces (this plan)

- `pickerSnapshots` table in schema.ts (`observed_at timestamptz` PK/unique, `snapshot jsonb`, RLS) +
  drizzle-generated migration `0015_picker_snapshot.sql`.
- `makePostgresPickerSnapshotRepo(db)` (`ForPersistingPickerSnapshot` insert-append + `ForReadingPickerSnapshot`
  readLatest) + `makeMemoryPickerSnapshotRepo()` twin + `runPickerSnapshotContractTests` shared suite.
- `makePostgresPickerChainRepo(db)` (`ForReadingChainForPicker`) + `makeMemoryPickerChainRepo()` twin.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: picker_snapshot table + append-history repo + memory twin + shared suite</name>
  <files>packages/adapters/src/postgres/schema.ts, packages/adapters/src/postgres/repos/picker-snapshot.ts, packages/adapters/src/postgres/repos/picker-snapshot.contract.test.ts, packages/adapters/src/memory/picker-snapshot.ts, packages/adapters/src/memory/picker-snapshot.contract.test.ts, packages/adapters/src/__contract__/picker-snapshot.contract.ts</files>
  <read_first>
    - packages/adapters/src/postgres/schema.ts (gexSnapshots table ~line 361 — the jsonb blob + timestamptz precedent; add pickerSnapshots after it)
    - packages/adapters/src/postgres/repos/macro-observations.ts (try/catch → StorageError; typed read shape)
    - packages/adapters/src/memory/macro-observations.ts (twin shape — adapt to append array + readLatest, NOT Map upsert)
    - packages/adapters/src/__contract__/cot-observations.contract.ts (shared suite shape; DESC-ordered read; truncate in beforeEach)
    - packages/contracts/src/picker.ts (pickerSnapshotResponse — the Zod schema the adapter validates the blob through at the boundary)
    - packages/core/src/picker/application/ports.ts (PickerSnapshotRow, ForPersistingPickerSnapshot, ForReadingPickerSnapshot)
    - .planning/phases/19-picker-engine-economic-events/19-PATTERNS.md ("picker_snapshot" migration + repo sections)
  </read_first>
  <behavior>
    - Contract suite: insert a PickerSnapshotRow → readLatest returns it; insert a second row with a later observedAt → readLatest returns the second (append-history, both rows retained).
    - readLatest on an empty store returns ok(null).
    - The Postgres repo validates the snapshot through pickerSnapshotResponse before insert and after read; a blob failing the contract → err, never a silently stored/returned bad row.
  </behavior>
  <action>
    Add `pickerSnapshots` to schema.ts: `observedAt: timestamp("observed_at", { withTimezone: true })`
    (unique/PK — the append idempotency guard against an exact-duplicate instant),
    `snapshot: jsonb("snapshot").$type<PickerSnapshotResponse-shaped>().notNull()`, `.enableRLS()`.
    Generate `0015_picker_snapshot.sql` via `bunx drizzle-kit generate` (+ meta snapshot + _journal entry);
    VERIFY the DDL is jsonb + timestamptz + RLS. Implement `makePostgresPickerSnapshotRepo(db)`:
    insertPickerSnapshot = `db.insert(pickerSnapshots).values({observedAt: row.observedAt, snapshot:
    pickerSnapshotResponse.parse(row.snapshot)})` (append, NO onConflictDoUpdate); readPickerSnapshot =
    `db.select().from(pickerSnapshots).orderBy(desc(pickerSnapshots.observedAt)).limit(1)` → map to
    PickerSnapshotRow with `pickerSnapshotResponse.parse(rows[0].snapshot)`, returning ok(null) when empty.
    Adapters MAY import @morai/contracts (boundary parse). Implement `makeMemoryPickerSnapshotRepo()` as an
    append array + max-observedAt readLatest. Write the shared `runPickerSnapshotContractTests(makeRepo)`
    suite and wire both tests. RED first, then GREEN.
  </action>
  <verify>
    <automated>bun run test --project @morai/adapters -- picker-snapshot.contract</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test --project @morai/adapters -- picker-snapshot.contract` exits 0 for BOTH memory + postgres, including the append-history (two rows, latest wins) and empty→null cases.
    - `rg -n 'onConflictDoUpdate' packages/adapters/src/postgres/repos/picker-snapshot.ts` returns no match (append only).
    - `rg -n 'pickerSnapshotResponse.parse' packages/adapters/src/postgres/repos/picker-snapshot.ts` matches (boundary validation).
  </acceptance_criteria>
  <done>picker_snapshot appends Zod-validated JSONB blobs and readLatest returns the newest, proven across memory + Postgres.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: readChainForPicker — latest leg_observations put cohort with IV + source</name>
  <files>packages/adapters/src/postgres/repos/picker-chain.ts, packages/adapters/src/postgres/repos/picker-chain.contract.test.ts, packages/adapters/src/memory/picker-chain.ts</files>
  <read_first>
    - packages/adapters/src/postgres/schema.ts (legObservations table ~line 113 — time/contract PK, bsm_iv, source observation_source enum, underlying_price; the ×1000 strike lives in the contracts join / OCC symbol — mirror how the GEX read resolves strike)
    - packages/core/src/analytics/application/computeGexSnapshot.ts + the gex repo readLegObsForGex (the "latest cohort" resolution + JOIN pattern to mirror for the picker chain read)
    - packages/core/src/picker/application/ports.ts (ChainQuoteForPicker, ForReadingChainForPicker)
    - packages/adapters/src/postgres/repos/macro-observations.contract.test.ts (testcontainers wrapper shape)
  </read_first>
  <behavior>
    - readChainForPicker returns the most-recent leg_observations cohort, puts only, each row carrying strike (×1000 int), expiration, bsmIv (string|null), underlyingPrice, contractType, time, and source.
    - Empty table → ok([]).
    - The cohort's source reflects leg_observations.source (schwab|cboe).
  </behavior>
  <action>
    Implement `makePostgresPickerChainRepo(db)` returning `ForReadingChainForPicker`: resolve the latest
    cohort time (max time), select that cohort's put rows joined to contract metadata for strike/expiration,
    projecting ChainQuoteForPicker incl. `source` from leg_observations.source. Mirror the existing GEX
    `readLegObsForGex` cohort-resolution query (do NOT invent a new resolution scheme). try/catch →
    StorageError. Implement `makeMemoryPickerChainRepo(seed)` twin returning a supplied ChainQuoteForPicker[].
    Write a testcontainers contract test asserting the cohort + source projection (RED first, then GREEN).
    The memory twin needs no separate contract suite file — a direct example test in the postgres contract
    test file plus the seed twin is sufficient here (read-only port, no write path to cross-check).
  </action>
  <verify>
    <automated>bun run test --project @morai/adapters -- picker-chain.contract</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test --project @morai/adapters -- picker-chain.contract` exits 0 (latest-cohort + source projection green; empty→[]).
    - `rg -n 'source' packages/adapters/src/postgres/repos/picker-chain.ts` shows the source column projected into ChainQuoteForPicker.
    - `bun run typecheck` green.
  </acceptance_criteria>
  <done>The picker can read the latest put chain cohort with per-strike IV and provider source.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 [BLOCKING]: Apply migrations 0014 + 0015 to the live schema (bun run migrate)</name>
  <files>packages/adapters/src/postgres/migrations/0014_economic_events.sql, packages/adapters/src/postgres/migrations/0015_picker_snapshot.sql</files>
  <read_first>
    - apps/worker/src/migrate.ts (the `bun run migrate` entrypoint — bootWorkerConfig() then runMigrations(DATABASE_URL))
    - packages/adapters/src/postgres/migrate.ts (runMigrations — idempotent drizzle migrator; safe to re-run)
    - Project memory "migrate gotcha, recurs Phase 14": `bun run migrate` validates the FULL worker env (needs SIDECAR_URL etc.)
  </read_first>
  <what-built>
    Migrations 0014_economic_events.sql (plan 19-04) and 0015_picker_snapshot.sql (Task 1) are generated
    and testcontainer-verified. This [BLOCKING] step applies them to the live schema, since build +
    typecheck pass WITHOUT the live tables (Drizzle types come from schema.ts, not the live DB) — a
    false-positive verification state until applied.
  </what-built>
  <action>
    Ensure the worker env is fully populated (DATABASE_URL/DIRECT connection + SIDECAR_URL etc. — bun run
    migrate validates the full worker config). Run `bun run migrate`; confirm the console shows
    "migrate: all migrations applied" with 0 errors. Confirm both new tables exist in the live schema and
    that economic_events.event_date is type `date` (never timestamp) and picker_snapshot.snapshot is type
    `jsonb`. If `bun run migrate` cannot run unattended in this environment, this task is human-gated
    (plan autonomous:false) — surface the blocker rather than marking applied.
  </action>
  <how-to-verify>
    1. Ensure the worker env is fully populated (DATABASE_URL/DIRECT connection + SIDECAR_URL etc.).
    2. Run `bun run migrate` and confirm "migrate: all migrations applied" with 0 errors.
    3. Confirm both new tables exist (e.g. `\d economic_events` and `\d picker_snapshot`, or a `SELECT`
       returning 0 rows cleanly).
    4. Confirm economic_events.event_date is type `date` and picker_snapshot.snapshot is type `jsonb`.
  </how-to-verify>
  <verify>
    <automated>bun run migrate</automated>
  </verify>
  <acceptance_criteria>
    - `bun run migrate` completes with "migrate: all migrations applied" and 0 errors.
    - Both economic_events and picker_snapshot tables exist in the live schema; event_date is `date`, snapshot is `jsonb`.
  </acceptance_criteria>
  <done>Migrations 0014 + 0015 are applied to the live schema; both tables exist with the correct column types.</done>
  <resume-signal>Type "applied" once both tables exist in the live schema, or describe the migrate error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSONB blob read/write | The whole pickerSnapshotResponse crosses the DB boundary as one blob |
| leg_observations → picker chain read | Existing chain data crosses into the picker |
| migration apply | DDL against the live database |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-10 | Tampering (data integrity) | untyped JSONB blob | high | mitigate | pickerSnapshotResponse.parse at the adapter boundary on write AND read — a contract-violating blob is rejected, never silently stored/served |
| T-19-11 | Information Disclosure | storage-error internals leaking | low | mitigate | Errors mapped to flat StorageError; route layer (plan 19-07) surfaces only {error:...} (no DB internals) |
| T-19-12 | Denial of Service | append-history unbounded growth (~13 rows/day RTH) | low | accept | Growth is bounded and slow; pruning policy deferred (D-06 keeps history for PICK-04); documented, not implemented this phase |
| T-19-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new dependencies (v1.2 lock); no install task; no legitimacy checkpoint applies |
</threat_model>

<verification>
- `bun run test --project @morai/adapters -- picker-snapshot picker-chain` green.
- `bun run migrate` applies 0014 + 0015 to the live schema (blocking checkpoint).
- `bun run typecheck` green.
</verification>

<success_criteria>
Append-history picker_snapshot + the chain read exist and are contract-tested, and the live schema has both new tables.
</success_criteria>

<output>
Create `.planning/phases/19-picker-engine-economic-events/19-05-SUMMARY.md` when done.
</output>
