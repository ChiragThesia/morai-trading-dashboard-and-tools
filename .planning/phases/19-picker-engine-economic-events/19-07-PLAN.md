---
phase: 19-picker-engine-economic-events
plan: 07
type: execute
wave: 5
depends_on: ["19-01", "19-05", "19-06"]
files_modified:
  - apps/server/src/adapters/http/picker.routes.ts
  - apps/server/src/adapters/http/picker.routes.test.ts
  - apps/server/src/adapters/mcp/tools.ts
  - apps/server/src/adapters/mcp/tools.test.ts
  - apps/server/src/main.ts
autonomous: true
requirements: [PICK-02]
must_haves:
  truths:
    - "GET /api/picker/candidates returns 200 + pickerSnapshotResponse-parsed body when a snapshot exists, 404 {error:'no-snapshot'} on cold start, and 500 {error:'internal'} on storage error"
    - "The get_picker_candidates MCP tool returns the SAME pickerSnapshotResponse payload (MCP-02 one schema), {error:'no-snapshot'} when none, never throws"
    - "Both surfaces read the latest picker_snapshot row via getPicker and NEVER recompute scores"
    - "The picker route sits behind the same Bearer-token auth group as /api/analytics/gex"
  artifacts:
    - apps/server/src/adapters/http/picker.routes.ts
    - "get_picker_candidates MCP tool in apps/server/src/adapters/mcp/tools.ts"
  key_links:
    - "picker.routes.ts + get_picker_candidates parse through the SAME pickerSnapshotResponse — a one-sided change fails bun run typecheck (MCP-02)"
    - "since the row stores the whole response as a JSONB blob, mapping is a direct pickerSnapshotResponse.parse(row.snapshot), not a field-by-field reassembly"
  prohibitions:
    - "MUST NOT recompute / call the scoring engine in the route or MCP tool — read the latest row only"
    - "MUST NOT expose storage-error internals — map to flat {error:'internal'} (no DB message leak)"
    - "MUST NOT add a second inline picker schema or a trimmed-text MCP variant — one schema for both surfaces"
---

<objective>
Expose the scored candidates over HTTP + MCP (PICK-02): a `GET /api/picker/candidates` Hono route and a
`get_picker_candidates` MCP tool, both thin readers of the latest `picker_snapshot` via getPicker, both
parsing through the single `pickerSnapshotResponse`. Mirrors `gex.routes.ts` + `registerGetGexTool` exactly.

Purpose: The read surfaces the Analyzer UI (and Claude Code) query for live picker data.
Output: picker route + MCP tool + composition-root wiring in the server.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-picker-engine-economic-events/19-CONTEXT.md
@.planning/phases/19-picker-engine-economic-events/19-PATTERNS.md
@apps/server/src/adapters/http/gex.routes.ts
@apps/server/src/adapters/mcp/tools.ts
@packages/core/src/picker/application/getPicker.ts
@packages/core/src/picker/application/ports.ts
@packages/contracts/src/picker.ts
@.claude/rules/tdd.md
@.claude/rules/architecture-boundaries.md
</context>

## Artifacts this phase produces (this plan)

- `pickerRoutes(getPicker: ForRunningGetPicker)` Hono router mounting `GET /picker/candidates`
  (effective `GET /api/picker/candidates`).
- `registerGetPickerCandidatesTool(server, getPicker)` MCP tool "get_picker_candidates".
- Server composition wiring: `makePostgresPickerSnapshotRepo` + `makeGetPickerUseCase` + route mount + MCP registration.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: picker.routes.ts — GET /picker/candidates (thin reader)</name>
  <files>apps/server/src/adapters/http/picker.routes.ts, apps/server/src/adapters/http/picker.routes.test.ts</files>
  <read_first>
    - apps/server/src/adapters/http/gex.routes.ts (verbatim route template: use-case → err→500 {error:internal} → null→404 {error:no-snapshot} → parse-through contract → 200)
    - apps/server/src/adapters/http/gex.routes.test.ts (Hono test-client route-test shape to mirror; if absent, mirror an existing *.routes.test.ts under apps/server)
    - packages/contracts/src/picker.ts (pickerSnapshotResponse — the parse-through schema)
    - packages/core/src/picker/application/ports.ts (ForRunningGetPicker, PickerSnapshotRow)
  </read_first>
  <behavior>
    - GET /picker/candidates with a stored row → 200 + a body that pickerSnapshotResponse.parse accepts.
    - Cold start (getPicker returns ok(null)) → 404 {error:"no-snapshot"}.
    - Storage error (getPicker returns err) → 500 {error:"internal"} (no DB message).
  </behavior>
  <action>
    Implement `pickerRoutes(getPicker)` returning a Hono router with `router.get("/picker/candidates", ...)`:
    call getPicker → err→`c.json({error:"internal"},500)` → null→`c.json({error:"no-snapshot"},404)` →
    else `c.json(pickerSnapshotResponse.parse(row.snapshot))` (direct blob parse, since D-05 stores the
    whole response). Zero business logic. Write RED route tests first (Hono test client, the three cases),
    run to failure, then GREEN.
  </action>
  <verify>
    <automated>bun run test --project @morai/server -- picker.routes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test --project @morai/server -- picker.routes.test.ts` exits 0 with the 200 / 404 / 500 cases green.
    - `rg -n 'pickerSnapshotResponse.parse\(row.snapshot\)' apps/server/src/adapters/http/picker.routes.ts` matches (direct blob parse).
    - `rg -n 'scoreCalendarCandidates|selectCandidates|compute' apps/server/src/adapters/http/picker.routes.ts` returns no match (no recompute).
  </acceptance_criteria>
  <done>The HTTP route serves the latest snapshot with the correct 200/404/500 semantics and no recompute.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: get_picker_candidates MCP tool (same schema)</name>
  <files>apps/server/src/adapters/mcp/tools.ts, apps/server/src/adapters/mcp/tools.test.ts</files>
  <read_first>
    - apps/server/src/adapters/mcp/tools.ts (registerGetGexTool lines ~480-547 — verbatim template: no input schema; err→"internal error" text; null→{error:no-snapshot} JSON text; else parse-through + JSON.stringify)
    - packages/contracts/src/picker.ts (pickerSnapshotResponse — same schema as the route, MCP-02)
    - packages/core/src/picker/application/ports.ts (ForRunningGetPicker)
  </read_first>
  <behavior>
    - get_picker_candidates with a stored row → content text is JSON that pickerSnapshotResponse.parse accepts.
    - Cold start → content text JSON {error:"no-snapshot"} (never throws, never a 404-equivalent crash).
    - Storage error → content text "internal error".
  </behavior>
  <action>
    Add `registerGetPickerCandidatesTool(server, getPicker)` mirroring `registerGetGexTool`: tool id
    "get_picker_candidates", empty inputSchema, call getPicker → err→"internal error" content → null→
    JSON {error:"no-snapshot"} content → else `pickerSnapshotResponse.parse(row.snapshot)` then
    `JSON.stringify(payload)` content. Same schema as the HTTP route (MCP-02), no trimmed-summary variant.
    Write RED test first (fake getPicker, the three cases), run to failure, then GREEN.
  </action>
  <verify>
    <automated>bun run test --project @morai/server -- tools.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test --project @morai/server -- tools.test.ts` exits 0 with the get_picker_candidates row / no-snapshot / error cases green.
    - `rg -n 'get_picker_candidates' apps/server/src/adapters/mcp/tools.ts` matches; the tool parses through `pickerSnapshotResponse` (no second inline schema).
  </acceptance_criteria>
  <done>The MCP tool returns the same contract payload as the route, stable on cold start and error.</done>
</task>

<task type="auto">
  <name>Task 3: Server composition — wire getPicker, mount route, register MCP tool</name>
  <files>apps/server/src/main.ts</files>
  <read_first>
    - apps/server/src/main.ts (gex wiring at ~lines 142-143; apiRouter .route("/analytics", gexRoutes(getGex)) at ~248; MCP registration block near ~300-308)
    - packages/adapters/src/postgres/repos/picker-snapshot.ts (makePostgresPickerSnapshotRepo — the readPickerSnapshot dep)
    - packages/core/src/picker/application/getPicker.ts (makeGetPickerUseCase)
  </read_first>
  <action>
    In the server composition root: construct `makePostgresPickerSnapshotRepo(db)`, build `const getPicker =
    makeGetPickerUseCase({ readPickerSnapshot: pickerSnapshotRepo.readPickerSnapshot })`, mount
    `.route("/", pickerRoutes(getPicker))` inside the authenticated apiRouter (so the effective path is
    GET /api/picker/candidates, behind the same Bearer-token read group as gex), and call
    `registerGetPickerCandidatesTool(mcpServer, getPicker)` in the MCP registration block. Composition-only
    wiring — no business logic (architecture-boundaries §3/§6; TDD-exempt per tdd.md composition-root rule).
  </action>
  <verify>
    <automated>bun run typecheck && bun run build --filter @morai/server</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'pickerRoutes\(getPicker\)|registerGetPickerCandidatesTool' apps/server/src/main.ts` matches both.
    - `bun run typecheck` green; the server builds.
    - The picker route is mounted inside the authenticated group (same group as gexRoutes), not the public/unauthed one.
  </acceptance_criteria>
  <done>The server exposes GET /api/picker/candidates + get_picker_candidates, both auth-gated and reading the latest snapshot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /api/picker/candidates | Untrusted HTTP request for candidate scores |
| MCP client → get_picker_candidates | Claude Code / MCP client request |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-15 | Spoofing / Elevation | unauthenticated read of picker data | high | mitigate | Route mounted inside the existing Bearer-token authReadGroup (same gate as /api/analytics/gex); no new auth logic |
| T-19-16 | Information Disclosure | storage-error internals in response | medium | mitigate | Flat {error:"internal"} mapping — DB/storage message never surfaced (gex.routes.ts precedent) |
| T-19-17 | Tampering | route/MCP recomputing scores (drift from stored snapshot) | medium | mitigate | Both surfaces read the latest row only; grep asserts no scoring-engine call in the adapters |
| T-19-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new dependencies (v1.2 lock); no install task; no legitimacy checkpoint applies |
</threat_model>

<verification>
- `bun run test --project @morai/server -- picker.routes tools` green.
- `bun run typecheck && bun run build --filter @morai/server` green.
- Route auth-gated, MCP schema parity with the route (both parse pickerSnapshotResponse).
</verification>

<success_criteria>
Live picker candidates are queryable via an auth-gated HTTP route + MCP tool, both thin readers of the latest snapshot.
</success_criteria>

<output>
Create `.planning/phases/19-picker-engine-economic-events/19-07-SUMMARY.md` when done.
</output>
