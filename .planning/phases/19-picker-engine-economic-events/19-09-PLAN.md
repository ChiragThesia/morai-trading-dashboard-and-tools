---
phase: 19-picker-engine-economic-events
plan: 09
type: execute
wave: 6
depends_on: ["19-01", "19-07"]
files_modified:
  - apps/web/src/hooks/usePicker.ts
  - apps/web/src/hooks/usePicker.test.ts
  - apps/web/src/screens/Analyzer.tsx
  - apps/web/src/screens/Analyzer.test.tsx
  - apps/web/src/components/picker/CandidateCard.tsx
  - apps/web/src/components/picker/CandidateCard.test.tsx
autonomous: true
requirements: [PICK-02]
must_haves:
  truths:
    - "usePicker fetches GET /api/picker/candidates via apiFetch, parses through pickerSnapshotResponse, throws non-retryable UnauthorizedError on 401 — mirroring useCot exactly"
    - "Analyzer renders live picker data with no layout change, and shows: a loading skeleton, a fetch-error card with Retry, a cold-start empty message, and a distinct zero-candidates-passed-filter message (with the live asOf)"
    - "Each CandidateCard shows an 'as of {HH:MM} · {source}' staleness+source tag on its existing sub-line (D-16/D-15), plus amber 'GEX unavailable' / 'events unavailable' tags when the snapshot's context status is not 'ok' (D-17)"
    - "When a context status is not 'ok', the matching breakdown bar (gexFit / eventAdjustment) renders its guard/zero-contribution state"
  artifacts:
    - apps/web/src/hooks/usePicker.ts
    - "Analyzer.tsx fixture→live swap + rail states"
    - "CandidateCard staleness+source + context-status tags"
  key_links:
    - "the swap is import-only for layout: SORTED_CANDIDATES now comes from usePicker().data.candidates instead of pickerSnapshotFixture, and PARAMS.spot from data.spot (PICK-02 'no layout change')"
    - "staleness dot uses relAge/GEX_FRESH_MS from Market.tsx (fresh=up, stale=amber) — no new threshold scheme; state precedence loading→error→cold-start→zero-filtered→populated"
  prohibitions:
    - "MUST NOT redesign card anatomy, the 3-column grid, breakdown bars, why-panel, term-structure, or entry/exit plan — additive tags fit inside the existing sub-line/panel shells (UI-SPEC scope statement)"
    - "MUST NOT use a shadcn Skeleton pulse — loading is text-only 'Loading candidates…' matching the useCot/useMacro card convention (D-19)"
    - "MUST NOT render 'Invalid Date' — if asOf fails to parse, show 'as of —' (em-dash)"
    - "MUST NOT add a new npm dependency or a new shadcn component (v1.2 lock; UI-SPEC Registry Safety)"
---

<objective>
Swap the Analyzer picker rail from the frozen fixture to live data (PICK-02) with no layout change:
a `usePicker` react-query hook (mirrors `useCot`), the `Analyzer.tsx` fixture→live swap with the
D-18/D-19 loading/error/cold-start/zero-filtered states, and the `CandidateCard` per-card staleness+
source tag (D-15/D-16) + context-status tags (D-17). Every new visual is a hand-rolled span/div reusing
existing tokens — zero new deps, zero new shadcn components.

Purpose: The live picker finally renders in the browser, honestly staleness-labeled.
Output: usePicker hook + Analyzer swap + CandidateCard additive tags.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-picker-engine-economic-events/19-CONTEXT.md
@.planning/phases/19-picker-engine-economic-events/19-UI-SPEC.md
@.planning/phases/19-picker-engine-economic-events/19-PATTERNS.md
@apps/web/src/hooks/useCot.ts
@apps/web/src/screens/Analyzer.tsx
@apps/web/src/components/picker/CandidateCard.tsx
@apps/web/src/screens/Market.tsx
@packages/contracts/src/picker.ts
@.claude/rules/tdd.md
</context>

## Artifacts this phase produces (this plan)

- `usePicker()` react-query hook → `GET /api/picker/candidates`, `queryKey:["picker"]`,
  `pickerSnapshotResponse.parse`, 401→UnauthorizedError, retry/staleTime tuned for the ~30-min chain cadence.
- Analyzer rail states: `data-testid` picker-loading / picker-error / picker-empty-cold-start / picker-empty-filtered.
- CandidateCard additive tags: staleness+source (`as of {HH:MM} · {source}`) + `GEX unavailable` / `events unavailable`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: usePicker react-query hook</name>
  <files>apps/web/src/hooks/usePicker.ts, apps/web/src/hooks/usePicker.test.ts</files>
  <read_first>
    - apps/web/src/hooks/useCot.ts (verbatim template: apiFetch, 401→UnauthorizedError non-retryable, cotResponse.parse, retry callback — swap for the picker route + schema)
    - packages/contracts/src/picker.ts (pickerSnapshotResponse — the parse schema)
    - .planning/phases/19-picker-engine-economic-events/19-PATTERNS.md ("usePicker.ts" section — shorter staleTime than COT's hourly, ~30-min chain cadence)
  </read_first>
  <behavior>
    - usePicker calls GET /api/picker/candidates via apiFetch; a 200 body parses through pickerSnapshotResponse.
    - 401 → throws UnauthorizedError (retry callback returns false for it).
    - non-2xx (non-401) → throws Error; retries up to the useCot cap.
  </behavior>
  <action>
    Copy useCot.ts to `usePicker.ts`: `queryKey:["picker"]`, `apiFetch("/api/picker/candidates")`,
    `pickerSnapshotResponse.parse(await res.json())`, 401→UnauthorizedError, the same retry callback. Tune
    `staleTime` shorter than COT's hourly (picker updates ~13×/day RTH — e.g. ~30-min window) and set a
    matching `refetchInterval`; note the tuning in a comment. Write RED tests first (mirror useCot.test.ts:
    success-parse, 401-non-retryable, non-2xx-throws), run to failure, then GREEN.
  </action>
  <verify>
    <automated>bun run test --project @morai/web -- usePicker.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test --project @morai/web -- usePicker.test.ts` exits 0 with success/401/non-2xx cases green.
    - `rg -n 'pickerSnapshotResponse.parse|queryKey: \["picker"\]|/api/picker/candidates' apps/web/src/hooks/usePicker.ts` matches all three.
  </acceptance_criteria>
  <done>usePicker mirrors useCot's contract against the picker route, with a chain-cadence staleTime.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Analyzer.tsx fixture→live swap + rail loading/error/empty states</name>
  <files>apps/web/src/screens/Analyzer.tsx, apps/web/src/screens/Analyzer.test.tsx</files>
  <read_first>
    - apps/web/src/screens/Analyzer.tsx (current fixture consumption: SORTED_CANDIDATES from pickerSnapshotFixture ~line 45; PARAMS.spot from fixture ~line 56; CandidateRail props)
    - .planning/phases/19-picker-engine-economic-events/19-UI-SPEC.md (Rail live-data states + Copywriting Contract — exact copy + data-testids + state precedence)
    - apps/web/src/hooks/usePicker.ts (the hook from Task 1; isPending/isError/data/refetch)
    - apps/web/src/screens/Market.tsx (a useCot/useMacro screen-consumer loading/error idiom to mirror)
  </read_first>
  <behavior>
    - Loading (isPending && data===undefined) → the rail shows text-only "Loading candidates…" (data-testid picker-loading), rest of layout unchanged.
    - Error → "Couldn't load candidates." (text-down) + a Retry mini-button calling refetch() (data-testid picker-error).
    - Cold start (settled, no snapshot — 404/undefined data) → "Picker warming up" heading + body copy (data-testid picker-empty-cold-start).
    - Zero-candidates (data present, candidates.length===0) → "No candidates in this snapshot" heading + "No put calendars meet net-θ>0 over the {asOf} snapshot." (data-testid picker-empty-filtered).
    - Populated → the existing rail, now sourced from data.candidates (sorted score-desc), PARAMS.spot from data.spot.
    - State precedence loading→error→cold-start→zero-filtered→populated; never two at once.
  </behavior>
  <action>
    Replace the synchronous `pickerSnapshotFixture` import with `const { data, isPending, isError, refetch } =
    usePicker();`. Derive SORTED_CANDIDATES from `data?.candidates` and PARAMS.spot from `data?.spot`
    (guarded). Render the five mutually-exclusive rail states INSIDE the existing "Suggested calendars"
    Panel/PanelHeading shell (never a new panel/route), using the exact UI-SPEC copy + data-testids + the
    text-only loading (no shadcn Skeleton). Do NOT change the 3-column grid, card anatomy, payoff center,
    why-panel, term-structure, or entry/exit plan. Pass data.asOf/data.source/data.gexContextStatus/
    data.eventsContextStatus down to CandidateCard (Task 3). Write RED component tests first (mock usePicker
    for each state, assert the data-testids + copy), run to failure, then GREEN.
  </action>
  <verify>
    <automated>bun run test --project @morai/web -- Analyzer.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test --project @morai/web -- Analyzer.test.tsx` exits 0 with all five state cases green.
    - `rg -n 'pickerSnapshotFixture' apps/web/src/screens/Analyzer.tsx` returns no match (fixture import removed).
    - `rg -n 'picker-loading|picker-error|picker-empty-cold-start|picker-empty-filtered' apps/web/src/screens/Analyzer.tsx` matches all four testids; `rg -n 'Skeleton' apps/web/src/screens/Analyzer.tsx` returns no match.
  </acceptance_criteria>
  <done>Analyzer renders live picker data with all four honest live-data states and no layout change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CandidateCard per-card staleness+source tag + context-status tags</name>
  <files>apps/web/src/components/picker/CandidateCard.tsx, apps/web/src/components/picker/CandidateCard.test.tsx</files>
  <read_first>
    - apps/web/src/components/picker/CandidateCard.tsx (the sub-line row `<div className="mt-0.5 font-mono text-[9px] text-dim">` ~line 88 with the frontEvents/backEvents/clean tags — the additive tags append here)
    - .planning/phases/19-picker-engine-economic-events/19-UI-SPEC.md (Phase-Specific Component Contracts: staleness+source tag anatomy, context-status tags, guard-bar linkage, exact copy)
    - apps/web/src/screens/Market.tsx (relAge + GEX_FRESH_MS ~lines 48/61 — reuse for the fresh/stale dot threshold)
  </read_first>
  <behavior>
    - CandidateCard renders one appended sub-line tag `as of {HH:MM} · {source}` with a size-1.5 dot (bg-up when snapshot age < GEX_FRESH_MS, bg-amber when stale).
    - A bad/unparseable asOf renders `as of —` (em-dash), never "Invalid Date".
    - When gexContextStatus !== "ok" an amber `GEX unavailable` tag appears; when eventsContextStatus !== "ok" an amber `events unavailable` tag appears (both may show, same wrapped row).
    - When a context tag shows, the corresponding breakdown bar (gexFit / eventAdjustment) renders its zero/guard state (reusing the existing fwdEdge guard-bar visual).
  </behavior>
  <action>
    Extend CandidateCard's props to accept `asOf`, `source`, `gexContextStatus`, `eventsContextStatus`
    (snapshot-level, passed identically to every card by Analyzer). Append to the existing `text-[9px]
    text-dim` sub-line: a `<span className="ml-1 flex items-center gap-1 rounded-sm bg-raise px-1 py-0.5">`
    with a `size-1.5 rounded-full` dot (bg-up/bg-amber via relAge/GEX_FRESH_MS from Market.tsx) + text
    `as of {HH:MM} · {source}` (guard: em-dash on parse failure); then conditional amber
    `GEX unavailable` / `events unavailable` tags (`rounded-sm bg-raise px-1 py-0.5 text-amber`) when the
    respective status is not "ok". Wire the gexFit/eventAdjustment breakdown bars to their existing
    guard/zero visual when the corresponding context is not "ok" (reuse, do not invent a new zero-state).
    No card-height/anatomy change beyond natural text wrap; no new token/dependency/shadcn component. Write
    RED tests first (fresh/stale dot, em-dash guard, both context tags, guard-bar linkage), run to failure, then GREEN.
  </action>
  <verify>
    <automated>bun run test --project @morai/web -- CandidateCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test --project @morai/web -- CandidateCard.test.tsx` exits 0 with the staleness-dot, em-dash-guard, and context-tag cases green.
    - `rg -n 'as of|GEX unavailable|events unavailable' apps/web/src/components/picker/CandidateCard.tsx` matches all three copy strings.
    - `rg -n 'GEX_FRESH_MS|relAge' apps/web/src/components/picker/CandidateCard.tsx` matches (reused freshness math); no new npm/shadcn import added.
  </acceptance_criteria>
  <done>Every card is self-contained with an honest as-of+source tag and degraded-context flags, reusing existing tokens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → browser | The picker response is rendered; a stale/degraded snapshot must not read as fresh/clean |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-21 | Tampering (false-clean UX) | stale snapshot rendered as fresh | high | mitigate | Per-card as-of+source with a fresh/stale dot (D-16) + amber context-unavailable tags (D-17) surface staleness/degradation on every card |
| T-19-22 | Availability / UX | blank rail on cold start or filtered-empty | medium | mitigate | Distinct honest empty messages (D-18) — the rail never renders blank; loading/error states have explicit affordances (D-19) |
| T-19-23 | Spoofing | unauthenticated fetch | low | mitigate | usePicker uses apiFetch (Bearer header) and treats 401 as a non-retryable UnauthorizedError (useCot precedent) |
| T-19-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new dependencies + zero new shadcn components (v1.2 lock, UI-SPEC Registry Safety); no install task |
</threat_model>

<verification>
- `bun run test --project @morai/web -- usePicker Analyzer CandidateCard` green.
- `bun run typecheck && bun run lint` green.
- Manual UAT (per 19-VALIDATION.md): load Analyzer against a live picker_snapshot row; force cold-start, zero-candidate, and stale/missing-context paths; confirm D-16/17/18/19 states render.
</verification>

<success_criteria>
The Analyzer renders live, honestly-staleness-labeled picker candidates with all live-data states and no layout change.
</success_criteria>

<output>
Create `.planning/phases/19-picker-engine-economic-events/19-09-SUMMARY.md` when done.
</output>
