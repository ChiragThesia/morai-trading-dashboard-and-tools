---
phase: 10-stack-decisions-doc-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture/stack-decisions.md
  - docs/architecture/deployment.md
  - docs/architecture/jobs.md
autonomous: true
requirements: [DOC-01]

must_haves:
  truths:
    - "stack-decisions.md D16 carries a Superseded-by-D22 notice citing the dual-refresher rotating-token race and single streamer-session ownership; the original D16 rationale + token-storage text is kept, not deleted"
    - "stack-decisions.md D17 reads Lifted (v1.1), scoped to account/position legs only (not full chain), noting the ~500-symbol streamer cap and that the sidecar owns the single session; the word 'deferred' no longer frames the decision"
    - "stack-decisions.md contains a new D22 decision for the Python schwab-py sidecar as the third Railway service (apps/sidecar/): FastAPI + schwab-py v1.5.1, the broker_tokens client_from_access_functions token-callback pattern, and Railway private-network (internal-only) isolation"
    - "deployment.md (2 sites) and jobs.md (1 site) no longer cite D16/D17 as current state — each now references D22; the phrase 'A future Python sidecar' is gone"
    - "No code files are created; apps/sidecar/ does not exist; docs/TOPIC-MAP.md is unchanged (no new doc created)"
  artifacts:
    - path: "docs/architecture/stack-decisions.md"
      provides: "D16 superseded notice, D17 lifted entry, new D22 sidecar decision (table row + section), updated D16/D17 table rows"
      contains: "## D22"
    - path: "docs/architecture/deployment.md"
      provides: "two cross-references updated from D17/D16 to D22 (Schwab token persistence + weekly re-auth)"
      contains: "D22"
    - path: "docs/architecture/jobs.md"
      provides: "one cross-reference updated from D16 to D22 (token refresh note)"
      contains: "D22"
  key_links:
    - from: "docs/architecture/deployment.md"
      to: "docs/architecture/stack-decisions.md"
      via: "weekly re-auth + token-persistence cross-references now point to D22, not D16/D17"
      pattern: "stack-decisions\\.md` D22|sidecar \\(D22\\)"
    - from: "docs/architecture/jobs.md"
      to: "docs/architecture/stack-decisions.md"
      via: "token-refresh note cross-reference now points to D22"
      pattern: "stack-decisions\\.md` D22"
---

<objective>
Record the three v1.1 architectural changes in `docs/architecture/stack-decisions.md` BEFORE any
sidecar code is written, satisfying the docs-before-code rule (DOC-01) and giving Phases 11-15 a
stable decision record to cite:

1. Supersede D16 (vendored TS Schwab OAuth client) — mark it Superseded by D22, keep its rationale.
2. Lift D17 (streaming deferred) — flip to Lifted (v1.1), scoped to position legs only.
3. Add D22 — Python schwab-py sidecar as the third Railway service.

Plus three stale cross-references in `deployment.md` (2) and `jobs.md` (1) are reconciled to D22 in
the same atomic change so the decision log and its references never contradict.

Purpose: Adding Python is an architecture change. The workflow rule (`.claude/rules/workflow.md`)
forbids architecture changes before the doc set records them. This phase unblocks all of v1.1.

Output: Edits to three existing docs. NO new files, NO code, NO TOPIC-MAP change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-stack-decisions-doc-update/10-RESEARCH.md
@.planning/phases/10-stack-decisions-doc-update/10-VALIDATION.md

# Style + structure constraints (MANDATORY for this docs phase)
@.claude/rules/docs.md
@.claude/rules/workflow.md
@docs/docs-on-docs/hemingway-style.md

# Verified sidecar facts to cite in D22
@.planning/research/SUMMARY.md
</context>

<artifacts_this_phase_produces>
The single new artifact is the **D22 decision** in `docs/architecture/stack-decisions.md`:

- A new decision-table row `| D22 | Python schwab-py sidecar | … |` appended to the Decision Table.
- A new section `## D22 — Python schwab-py sidecar: third Railway service` placed AFTER the D21
  section (D21 is the last section, ending the file at line ~308).

No file is created. No directory is created. `docs/TOPIC-MAP.md` is intentionally NOT touched —
a dedicated `sidecar.md` architecture doc belongs in Phase 11 when `apps/sidecar/` actually exists.
Success Criterion 4 (TOPIC-MAP) is satisfied vacuously: no new doc ⇒ no TOPIC-MAP edit.
</artifacts_this_phase_produces>

<header_convention_note>
The doc uses level-2 headers for decision sections (`## D20`, `## D21` — verified). Use `## D22`
to match, NOT `### D22`. The 10-VALIDATION.md draft greps `^### D22`/`^### D2[12]`; that draft
assumed level-3. The live doc convention (`##`) wins per the docs rule (match existing style). The
acceptance criteria below are written against the ACTUAL convention (`## D22`). If a reviewer insists
on the validation draft's `###`, raise it — do not silently break the file's heading consistency.
</header_convention_note>

<tasks>

<task type="auto">
  <name>Task 1: Supersede D16, lift D17, add D22, and reconcile 3 cross-references (one atomic doc edit)</name>
  <files>docs/architecture/stack-decisions.md, docs/architecture/deployment.md, docs/architecture/jobs.md</files>

  <read_first>
    - docs/architecture/stack-decisions.md — the target. Read the Decision Table (D16 row line ~25,
      D17 row line ~26), the `## D16` section (lines ~149-169), the `## D17` section (lines ~171-184),
      and the `## D21` section (ends ~line 308, the file tail). The file is 308 lines.
    - docs/architecture/deployment.md — the two stale cross-ref sites: line ~53-54 ("A future Python
      sidecar (schwab-py, D17) reads the same row via custom token access functions.") and line ~57
      ("- **Weekly re-auth is mandatory and designed in** (see `stack-decisions.md` D16):").
    - docs/architecture/jobs.md — the one stale cross-ref site: line ~38 ("weekly interactive re-auth
      is mandatory (`deployment.md` + `stack-decisions.md` D16).").
    - .planning/phases/10-stack-decisions-doc-update/10-RESEARCH.md — quotes the verbatim current D16
      and D17 text and specifies the exact target text for every change; the source of all D22 facts.
    - .planning/research/SUMMARY.md — verified sidecar facts (dual-refresher race, streamer-session
      ownership, schwab-py v1.5.1, client_from_access_functions, 500-symbol cap, FastAPI sidecar).
    - docs/docs-on-docs/hemingway-style.md — prose rules (short sentences, active voice, present
      tense for locked decisions; no "will use", no history narration).

    Line numbers may have shifted by an edit. The executor MUST `rg` for the exact current strings
    (below) rather than trusting line numbers. Edit each by unique string match.
  </read_first>

  <action>
Make all edits in this ONE task so the decision log and its cross-references never disagree at any
commit point. Match the doc's existing supersede/lift convention (verified precedents:
"**Recharts**: superseded. Not used." and "The Supabase Auth deferral is lifted. See D20." and
"This lifts the D18 ... deferral."). Hemingway: short sentences, active voice, present tense.

A. DECISION TABLE — replace the D16 row. Find the row beginning `| D16 | Schwab auth | Own TS OAuth
   client`. Replace its Decision/Swap-cost/Revisit cells so the row reads (keep the `| D16 | Schwab
   auth |` lead cells): Decision cell = `**Superseded by D22.** TS OAuth client retired; the
   schwab-py sidecar owns all Schwab auth.`; Swap-cost cell = `—`; Revisit-trigger cell = `—`.

B. DECISION TABLE — replace the D17 row. Find the row beginning `| D17 | Market data streaming |
   Deferred`. Replace its Decision/Swap-cost/Revisit cells so the row reads (keep `| D17 | Market
   data streaming |`): Decision cell = `**Lifted (v1.1)** — the schwab-py sidecar streams position
   legs + fills (not the full chain); see D22`; Swap-cost cell = `Low (sidecar owns the session)`;
   Revisit-trigger cell = `Full-chain streaming needed (impossible at the ~500-symbol cap)`.

C. DECISION TABLE — add the D22 row immediately AFTER the D21 row (`| D21 | BSM kernel leaf |`).
   New row: `| D22 | Python schwab-py sidecar | `apps/sidecar/` — FastAPI + schwab-py; sole Schwab
   auth + REST proxy + streamer; internal Railway network only | Medium (Python service + Railway
   topology) | TS stack fully covers Schwab streaming natively |`.

D. `## D16` SECTION — add a superseded notice at the TOP of the section body, directly under the
   `## D16 — Schwab auth: own TS OAuth client, tokens in Postgres` header and before the existing
   `**Why**:` paragraph. KEEP the entire existing D16 body (Why, hard-constraint, token-storage) —
   it is still accurate; `broker_tokens` is unchanged. The notice is two short bolded lines plus
   the two reasons. Concretely insert:
     - A line `**Superseded by D22.** The schwab-py sidecar is now the sole Schwab authenticator;
       the vendored TS OAuth client and the TS `refresh-tokens` job are retired.`
     - A line `**Why the reversal:**` followed by two bullets stating the two facts (from
       SUMMARY.md): (1) the dual-refresher rotating-token race — Schwab invalidates the old refresh
       token on each refresh, so two refreshers (the TS `refresh-tokens` job + the sidecar) racing
       cause `invalid_grant` inside one 30-min cycle; one process must own the token lifecycle.
       (2) Streamer-session ownership — the sidecar owns the one allowed Schwab websocket session;
       the token owner and the session owner must be the same process, and schwab-py handles both
       by design.
   Do not narrate the original rationale as history; leave it as-is below the notice.

E. `## D17` SECTION — replace the section so it states the lifted decision, scoped to legs only.
   Change the header from `## D17 — Streaming: deferred` to `## D17 — Streaming: lifted (v1.1)`.
   Replace the body (the "Why deferred" + "When the trigger fires" + "Decide at trigger time"
   prose) with present-tense lifted content that states (facts from SUMMARY.md / 10-RESEARCH.md):
     - The streaming deferral is lifted in v1.1 for account/position data. (One sentence; mirror the
       D18→D20 "deferral is lifted" precedent.)
     - Streaming is scoped to LEVELONE_OPTION for open position legs (typically 2-30 symbols) plus
       ACCT_ACTIVITY for fill events.
     - Full SPX chain streaming stays impossible — the streamer caps at ~500 symbols vs the
       2,000-5,000 SPX contracts. The chain snapshot and GEX stay REST jobs.
     - One streamer session per account; the schwab-py sidecar (D22) owns that single session.
   The word "deferred" must not survive as the framing of the decision (it may appear only if you
   write a single explicit "no longer deferred" clause — prefer "lifted").

F. ADD `## D22` SECTION — append AFTER the `## D21` section (which currently ends the file). Header:
   `## D22 — Python schwab-py sidecar: third Railway service`. The body must cover all of the
   following, in Hemingway present tense (cite SUMMARY.md facts; mark the Railway-isolation
   mechanism `[ASSUMED]` per RESEARCH A2 — its specifics are confirmed at Phase 11 infra setup):
     1. **What**: a third Railway service at `apps/sidecar/`. Python 3.10+. FastAPI + uvicorn +
        sse-starlette. schwab-py v1.5.1.
     2. **Why Python, not TS**: schwab-py's `StreamClient` is an asyncio-native WebSocket client that
        handles Schwab's streamer protocol (login, SUBS commands, reconnect). Hand-rolling this in TS
        is the pain the sidecar avoids. FastAPI's asyncio bridges the StreamClient event loop to an
        SSE endpoint without thread hacks.
     3. **Token pattern**: `client_from_access_functions` with custom `token_read`/`token_write`
        callbacks that read and write the EXISTING `broker_tokens` Postgres row (pgcrypto-encrypted,
        same as the TS side). No schema change. The sidecar is the sole writer; the TS
        `refresh-tokens` job is retired (D16 superseded).
     4. **Streaming scope**: LEVELONE_OPTION (position legs only) + ACCT_ACTIVITY. The ~500-symbol
        cap makes full-chain streaming impossible. GEX and journal snapshots stay REST jobs.
     5. **Isolation**: Railway private network — the sidecar has no public ingress. Only
        `apps/server` reaches it. The sidecar pushes SSE; the TS server fans out to browser clients
        with Supabase JWT verification at the server edge (D20). Mark this `[ASSUMED]` (Railway
        private-networking specifics confirmed at Phase 11 infra setup).
     6. **Why no message broker**: sidecar → one TS server is a one-writer, one-reader path. Direct
        SSE is sufficient. No Redis/Kafka/RabbitMQ.
     7. **Swap cost**: Medium. Retiring the Python service means writing a TS streamer adapter (the
        original D17 option 1). The hexagon ports are unchanged either way.
     8. **Revisit trigger**: TS streaming libraries mature enough to cover Schwab's WebSocket
        protocol without the maintenance burden of hand-rolled reconnect + token injection.

G. CROSS-REF — `docs/architecture/deployment.md` site 1. Find the exact string
   `A future Python sidecar (schwab-py, D17) reads the same row via custom token access functions.`
   Replace with `The schwab-py sidecar (D22) reads the same row via `client_from_access_functions`
   callbacks.`

H. CROSS-REF — `docs/architecture/deployment.md` site 2. Find the exact string
   `- **Weekly re-auth is mandatory and designed in** (see `stack-decisions.md` D16):` and replace
   only the `D16` token with `D22` (the line becomes `... (see `stack-decisions.md` D22):`). Change
   nothing else on that line.

I. CROSS-REF — `docs/architecture/jobs.md` site. Find the exact string
   `weekly interactive re-auth is mandatory (`deployment.md` + `stack-decisions.md` D16).` and
   replace only the `D16` token with `D22`.

CONSTRAINTS (hard):
- Create NO files. Create NO `apps/sidecar/` directory or stub. This is docs-only (Pitfall 3).
- Do NOT edit `docs/TOPIC-MAP.md` — no new doc is created (Pitfall: research recommendation + SC4).
- Do NOT delete the D16 section body (Pitfall 1 — supersede, don't erase).
- Keep `stack-decisions.md` cohesive; do not split the ADR into multiple files.
- After your edits, NO active D16/D17 reference may remain in `deployment.md` or `jobs.md`.
  </action>

  <verify>
    <automated>rtk proxy rg -n "Superseded by D22" docs/architecture/stack-decisions.md && rtk proxy rg -n "Lifted \(v1\.1\)" docs/architecture/stack-decisions.md && rtk proxy rg -n "^## D22" docs/architecture/stack-decisions.md && test -z "$(rtk proxy rg -n 'future Python sidecar' docs/architecture/deployment.md)" && test -z "$(rtk proxy rg -n 'stack-decisions\.md` D16|stack-decisions\.md` D17' docs/architecture/deployment.md docs/architecture/jobs.md)"</automated>
  </verify>

  <acceptance_criteria>
    All checks below are exact greps. Each must pass with the stated result.

    - D16 superseded: `rtk proxy rg -n "Superseded by D22" docs/architecture/stack-decisions.md` → ≥1 match.
    - D16 body preserved: `rtk proxy rg -n "Token storage" docs/architecture/stack-decisions.md` → still ≥1 match (original D16 token-storage text not deleted).
    - D17 lifted (section): `rtk proxy rg -n "^## D17 — Streaming: lifted" docs/architecture/stack-decisions.md` → 1 match.
    - D17 lifted (table): `rtk proxy rg -n "Lifted \(v1\.1\)" docs/architecture/stack-decisions.md` → ≥1 match.
    - D17 no longer "deferred"-framed: `rtk proxy rg -ni "Streaming: deferred" docs/architecture/stack-decisions.md` → 0 matches.
    - 500-symbol cap noted: `rtk proxy rg -n "500-symbol" docs/architecture/stack-decisions.md` → ≥1 match.
    - D22 section exists: `rtk proxy rg -n "^## D22" docs/architecture/stack-decisions.md` → 1 match.
    - D22 covers stack: `rtk proxy rg -n "FastAPI" docs/architecture/stack-decisions.md` and `rtk proxy rg -n "client_from_access_functions" docs/architecture/stack-decisions.md` and `rtk proxy rg -n "schwab-py v1.5.1" docs/architecture/stack-decisions.md` → each ≥1 match.
    - D22 sequential (highest ID): `rtk proxy rg -n "^## D2[0-9]" docs/architecture/stack-decisions.md` → highest is D22; no `## D23` exists.
    - deployment.md "future" phrase gone: `rtk proxy rg -n "future Python sidecar" docs/architecture/deployment.md` → 0 matches.
    - cross-refs reconciled: `rtk proxy rg -n "stack-decisions\.md\` D16|stack-decisions\.md\` D17" docs/architecture/deployment.md docs/architecture/jobs.md` → 0 matches.
    - deployment.md now cites D22: `rtk proxy rg -n "sidecar \(D22\)|stack-decisions\.md\` D22" docs/architecture/deployment.md` → ≥2 matches (token-persistence line + weekly re-auth line).
    - jobs.md now cites D22: `rtk proxy rg -n "stack-decisions\.md\` D22" docs/architecture/jobs.md` → 1 match.
    - no code created: `ls apps/sidecar 2>/dev/null` → no such directory.
    - TOPIC-MAP untouched: `rtk proxy rg -n "sidecar" docs/TOPIC-MAP.md` → 0 matches.
  </acceptance_criteria>

  <done>
    D16 is superseded (notice + table row, body kept); D17 is lifted (header + body + table row,
    legs-only + 500-symbol cap); D22 exists (table row + section covering all 8 points); all three
    stale D16/D17 cross-refs in deployment.md/jobs.md point to D22; no code files; TOPIC-MAP
    unchanged. Every acceptance grep returns its stated result.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the full doc-consistency gate (10-VALIDATION.md) and confirm Hemingway prose reads true</name>
  <files>docs/architecture/stack-decisions.md, docs/architecture/deployment.md, docs/architecture/jobs.md (read-only verification — no edits)</files>

  <read_first>
    - .planning/phases/10-stack-decisions-doc-update/10-VALIDATION.md — the Per-Task Verification Map;
      this task runs every check in it (adapted to the live `## D22` header convention).
    - docs/architecture/stack-decisions.md — read the edited D16, D17, and D22 entries end-to-end to
      confirm the rationale (dual-refresher race, streamer ownership, legs-only) reads plainly and in
      present tense (the one manual, non-grep check from 10-VALIDATION.md).
  </read_first>

  <action>
Run the consistency gate as a single shell pass and confirm every assertion. This is the phase
sign-off — no code, just greps + one prose read.

Run these and confirm the stated result for each:
  1. `rtk proxy rg -n "Superseded by D22" docs/architecture/stack-decisions.md` → matches.
  2. `rtk proxy rg -n "Lifted \(v1\.1\)" docs/architecture/stack-decisions.md` → matches.
  3. `rtk proxy rg -n "^## D22" docs/architecture/stack-decisions.md` → exactly one match.
  4. `rtk proxy rg -n "^## D2[0-9]" docs/architecture/stack-decisions.md` → highest ID is D22, no gap/dupe after D21.
  5. `rtk proxy rg -ni "Streaming: deferred" docs/architecture/stack-decisions.md` → zero matches.
  6. `rtk proxy rg -n "future Python sidecar" docs/architecture/deployment.md` → zero matches.
  7. `rtk proxy rg -n "stack-decisions\.md\` D16|stack-decisions\.md\` D17" docs/architecture/deployment.md docs/architecture/jobs.md` → zero matches.
  8. `ls apps/sidecar 2>/dev/null` → directory does not exist.
  9. `rtk proxy rg -n "sidecar" docs/TOPIC-MAP.md` → zero matches.

Then read the D16/D17/D22 entries and confirm (manual, prose-quality — not grep-checkable): the
supersede reason names the dual-refresher race AND streamer-session ownership; D17 says legs-only and
notes the cap; D22 is present tense (no "will use"); sentences are short and active. If any grep
fails or the prose narrates the decision in future tense, return to Task 1, fix, re-run — do not
sign off on a partial pass.
  </action>

  <verify>
    <automated>rtk proxy rg -n "^## D22" docs/architecture/stack-decisions.md && test -z "$(rtk proxy rg -ni 'Streaming: deferred' docs/architecture/stack-decisions.md)" && test -z "$(rtk proxy rg -n 'stack-decisions\.md` D16|stack-decisions\.md` D17' docs/architecture/deployment.md docs/architecture/jobs.md)" && test ! -d apps/sidecar && test -z "$(rtk proxy rg -n 'sidecar' docs/TOPIC-MAP.md)"</automated>
  </verify>

  <acceptance_criteria>
    - All nine consistency checks above return their stated result (D22 present; D17 lifted not
      deferred; no stale D16/D17 cross-refs; no `apps/sidecar/`; TOPIC-MAP clean).
    - The single manual prose read confirms the rationale reads true in present tense (dual-refresher
      race + streamer ownership for D16; legs-only + 500-cap for D17).
  </acceptance_criteria>

  <done>
    The full 10-VALIDATION.md gate passes (all greps return expected) and the manual prose read
    confirms present-tense, plainly-stated rationale. Phase 10 success criteria 1-4 are all
    satisfied (SC4 vacuously — no new doc, TOPIC-MAP unchanged).
  </done>
</task>

</tasks>

<threat_model>
**N/A — documentation-only change, no attack surface introduced.**

This phase edits three existing markdown files (`stack-decisions.md`, `deployment.md`, `jobs.md`).
No code, no new inputs, no endpoints, no dependencies, no package installs, no trust boundaries
crossed. There is nothing to threat-model. STRIDE does not apply to prose edits in a decision log.
</threat_model>

<verification>
Phase-level checks (the 10-VALIDATION.md gate, all `rtk proxy rg` — runtime < 2s):

| Check | Pass condition |
|-------|----------------|
| D16 superseded | `rg "Superseded by D22" docs/architecture/stack-decisions.md` matches |
| D16 body kept | `rg "Token storage" docs/architecture/stack-decisions.md` still matches |
| D17 lifted | `rg "^## D17 — Streaming: lifted"` matches AND `rg "Lifted \(v1.1\)"` matches |
| D17 not deferred | `rg -ni "Streaming: deferred" docs/architecture/stack-decisions.md` → 0 |
| 500-cap noted | `rg "500-symbol" docs/architecture/stack-decisions.md` matches |
| D22 added | `rg "^## D22" docs/architecture/stack-decisions.md` → exactly 1 |
| D22 sequential | `rg "^## D2[0-9]"` highest is D22; no D23 |
| Cross-refs gone | `rg "stack-decisions.md\` D16|stack-decisions.md\` D17" deployment.md jobs.md` → 0 |
| deployment cites D22 | `rg "sidecar \(D22\)|stack-decisions.md\` D22" deployment.md` → ≥2 |
| jobs cites D22 | `rg "stack-decisions.md\` D22" jobs.md` → 1 |
| No code | `ls apps/sidecar 2>/dev/null` → absent |
| TOPIC-MAP clean | `rg "sidecar" docs/TOPIC-MAP.md` → 0 |

Manual (one read): D16/D17/D22 rationale reads true, present tense, Hemingway.
</verification>

<success_criteria>
Phase 10 is complete when (maps to ROADMAP SC1-4):

1. (SC1) `stack-decisions.md` D16 carries a Superseded-by-D22 notice whose rationale references the
   dual-refresher race and streamer ownership; the original D16 body is intact.
2. (SC2) `stack-decisions.md` D17 reads "Lifted (v1.1)", scoped to account/position legs only (not
   full chain), with the ~500-symbol cap noted; the table row matches the section.
3. (SC3) `stack-decisions.md` has a new D22 decision (table row + `## D22` section) documenting the
   FastAPI + schwab-py stack, the `broker_tokens` `client_from_access_functions` callback pattern,
   and Railway private-network isolation.
4. (SC4) `docs/TOPIC-MAP.md` is unchanged — no new sidecar doc was created, so SC4 is satisfied
   vacuously. (Cross-corollary: all three stale D16/D17 cross-refs in deployment.md/jobs.md now
   cite D22; no code files exist.)

All twelve consistency greps pass; a single atomic commit lands all edits.
</success_criteria>

<output>
Create `.planning/phases/10-stack-decisions-doc-update/10-01-SUMMARY.md` when done.

Suggested single commit (all doc edits, no code):
`docs(10): supersede D16, lift D17, add D22 (schwab-py sidecar)`
</output>
