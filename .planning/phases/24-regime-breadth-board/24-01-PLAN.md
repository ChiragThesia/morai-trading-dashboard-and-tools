---
phase: 24-regime-breadth-board
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture/regime-board.md
  - docs/architecture/data-model.md
  - docs/architecture/jobs.md
  - docs/TOPIC-MAP.md
  - packages/core/src/journal/application/fetchMacroSeries.ts
  - packages/core/src/journal/application/fetchMacroSeries.test.ts
  - packages/contracts/src/macro.ts
  - packages/contracts/src/macro.test.ts
  - packages/adapters/src/memory/fred-series.test.ts
  - packages/adapters/src/__contract__/macro-observations.contract.ts
autonomous: true
requirements: [MACRO-02, MACRO-03]
must_haves:
  truths:
    - "docs/architecture/regime-board.md exists, records all 4 admitted indicators with source + rationale + verification date, and documents the RSP:SPY DROP and VVIX/VIX-ratio DEFER refutations (evidence discipline, MACRO-02)"
    - "BAMLH0A0HYM2 (HY OAS) flows through the twice-daily fetch-rates cron into macro_observations with zero new job or migration (MACRO-02/03)"
    - "HY OAS is stored raw as an absolute level (percent units), never a moving average — no warm-up gap on ship day"
  artifacts:
    - path: docs/architecture/regime-board.md
      provides: "Per-indicator evidence table (mirrors picker-rules.md) + Refuted/Dropped section"
      contains: "BAMLH0A0HYM2"
    - path: packages/core/src/journal/application/fetchMacroSeries.ts
      provides: "BAMLH0A0HYM2 in DEFAULT_FRED_SERIES_IDS"
      contains: "BAMLH0A0HYM2"
    - path: packages/contracts/src/macro.ts
      provides: "BAMLH0A0HYM2 in MACRO_SERIES_IDS enum"
      contains: "BAMLH0A0HYM2"
  key_links:
    - from: packages/core/src/journal/application/fetchMacroSeries.ts
      to: macro_observations
      via: "fetch-rates cron persists the new FRED series each run"
      pattern: "BAMLH0A0HYM2"
---

<objective>
Land the phase's evidence-discipline deliverable and the first new indicator input: create
`docs/architecture/regime-board.md` (the per-indicator source+rationale table that admits the
4 board indicators and documents the 2 refutations), correct the macro doc counts, and add the
HY OAS credit series (`BAMLH0A0HYM2`) to the existing FRED macro pipeline.

Purpose: MACRO-02 requires every admitted indicator to carry a documented citation BEFORE it
ships (mirroring picker-rules.md). Docs-first is also a hard project rule (workflow.md: new
table rows / new series → update docs first). HY OAS is a zero-adapter-code addition (a new
series id on the already-parameterized FRED adapter), identical in shape to Phase 23's VXVCLS.
Output: regime-board.md + doc count fixes + HY OAS ingested via fetch-rates, no new job.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-regime-breadth-board/24-CONTEXT.md
@.planning/phases/24-regime-breadth-board/24-RESEARCH.md
@.planning/phases/23-vix3m-ingestion/23-01-SUMMARY.md
@docs/architecture/picker-rules.md
@docs/architecture/data-model.md
@docs/architecture/jobs.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Docs-first — create regime-board.md, correct macro series counts</name>
  <files>docs/architecture/regime-board.md, docs/architecture/data-model.md, docs/architecture/jobs.md, docs/TOPIC-MAP.md</files>
  <read_first>
    - docs/architecture/picker-rules.md (the evidence-format precedent to mirror: per-row formula, inputs, thresholds, rationale, source, verification date)
    - .planning/phases/24-regime-breadth-board/24-RESEARCH.md (Per-Indicator Adjudication + Refuted/Dropped sections are the authoritative source content — copy the adjudications, do not re-research)
    - docs/architecture/data-model.md (lines around the `macro_observations` section — current text says "Nine series")
    - docs/architecture/jobs.md (the "Expanded macro fetch" section — says "8 FRED series" / "the 9 series")
    - docs/TOPIC-MAP.md (the architecture doc index table)
  </read_first>
  <action>
    Create docs/architecture/regime-board.md mirroring picker-rules.md's evidence-per-row
    discipline. Include: an intro naming the board's purpose and the "admit only with a cited
    source + rationale" rule; a per-indicator table with ONE row per admitted indicator (VIX/VIX3M,
    VVIX absolute, VIX9D/VIX, HY OAS) carrying its formula/inputs, its calm/warning/crisis band
    cuts, its threshold rationale, its source URLs, and its 2026-07-09 live-verification date — all
    transcribed from 24-RESEARCH.md's Per-Indicator Adjudication. Mark the VIX9D/VIX and HY OAS band
    cuts explicitly as ASSUMED/newly-calibrated (display-only this phase, no hard gate — Phase 28
    owns gate wiring). Add a "Refuted / Dropped" section transcribing 24-RESEARCH.md's table:
    RSP:SPY equal-weight breadth DROPPED (no verified server-fetchable source; Stooq bot-walled;
    Schwab sidecar has no equity-quote surface; independent evidence weaker than the TOS prior), the
    VVIX/VIX ratio DEFERRED (different numeric scale than the calibrated absolute-VVIX thresholds —
    would double-count the same two raw series), the hallucinated FRED VIX9DCLS series, and the HYG
    ETF leg superseded by HY OAS — each with its documented revival path. State the board is
    computed-on-read from macro_observations with no new table.
    Then correct the counts: in data-model.md's macro_observations section, change "Nine series" to
    the new total (11) and extend the series list to include VIX9D (source cboe) and BAMLH0A0HYM2
    (source fred). In jobs.md's expanded-macro-fetch section, update the FRED series count/list to
    include BAMLH0A0HYM2 and note VIX9D fetched via CBOE alongside VVIX, and update "the 9 series"
    fail-loud count to 11. Add a docs/TOPIC-MAP.md row linking regime-board.md ("Regime & breadth
    board — admitted indicators, thresholds, provenance").
    Per docs.md: prose in Hemingway style, one concept per file, add the cross-reference. Do NOT
    restate config/threshold constants that live in code — the doc is the WHY + citations; the
    banding constants are authored in code in plan 24-03.
  </action>
  <verify>
    <automated>test -f docs/architecture/regime-board.md && grep -q "BAMLH0A0HYM2" docs/architecture/regime-board.md && grep -q "regime-board.md" docs/TOPIC-MAP.md && echo DOCS_OK</automated>
  </verify>
  <acceptance_criteria>
    regime-board.md exists with all 4 admitted indicators (each with source + rationale + band cuts)
    and a Refuted/Dropped section covering RSP:SPY, VVIX/VIX ratio, VIX9DCLS, and HYG. data-model.md
    and jobs.md counts reflect 11 macro series. TOPIC-MAP.md links the new doc. No code changed in
    this task.
  </acceptance_criteria>
  <done>Acceptance criteria above satisfied; verified green at commit (tests/typecheck/lint as applicable).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HY OAS series — BAMLH0A0HYM2 into the FRED pipeline (RED→GREEN)</name>
  <files>packages/core/src/journal/application/fetchMacroSeries.ts, packages/core/src/journal/application/fetchMacroSeries.test.ts, packages/contracts/src/macro.ts, packages/contracts/src/macro.test.ts, packages/adapters/src/memory/fred-series.test.ts, packages/adapters/src/__contract__/macro-observations.contract.ts</files>
  <read_first>
    - .planning/phases/23-vix3m-ingestion/23-01-SUMMARY.md (VXVCLS was added by this EXACT pattern — clone it for BAMLH0A0HYM2)
    - packages/core/src/journal/application/fetchMacroSeries.ts (DEFAULT_FRED_SERIES_IDS — append the new id at the END, matching Phase 23's array-order-stability decision)
    - packages/contracts/src/macro.ts (MACRO_SERIES_IDS enum — append at the END)
    - packages/adapters/src/memory/fred-series.test.ts (the VXVCLS memory-twin seed case to mirror)
    - packages/adapters/src/__contract__/macro-observations.contract.ts (the VXVCLS contract row to mirror)
  </read_first>
  <behavior>
    - fetchMacroSeries.test.ts: DEFAULT_FRED_SERIES_IDS contains BAMLH0A0HYM2 and its length is now 9 FRED ids; the fetch orchestration builds a fetch task for it (fails RED on the length/contains assertion before the constant is widened).
    - macro.test.ts: MACRO_SERIES_IDS contains BAMLH0A0HYM2 (RED on the "contains all N ids" count assertion before the enum is widened).
    - macro-observations contract + fred-series memory twin: a BAMLH0A0HYM2 row seeds/reads/persists like any other series (text column, no enum gate — expected to pass immediately, confirmation coverage per the Phase-23 note, not a true RED case).
  </behavior>
  <action>
    Following Phase 23's VXVCLS precedent exactly: write the failing test assertions first (widened
    counts + contains BAMLH0A0HYM2), run them RED, then append "BAMLH0A0HYM2" to the END of
    DEFAULT_FRED_SERIES_IDS in fetchMacroSeries.ts and to the END of MACRO_SERIES_IDS in macro.ts.
    Add the memory-twin seed case (fred-series.test.ts) and the contract row
    (macro-observations.contract.ts) mirroring the VXVCLS additions. HY OAS uses the EXISTING
    makeFredSeriesAdapter unmodified — a new series-id string only, zero new adapter code. The value
    is stored RAW (FRED percent units, e.g. 2.67 = 2.67%), no /100 (D-14), and shipped as an
    absolute level — no moving-average computation (Research Pitfall 1: the adapter's limit=5 fetch
    does not backfill, so a 20-day average would be non-functional for ~4 weeks). Do NOT add a
    backfill task. Commit only at green.
  </action>
  <verify>
    <automated>bun run test -- packages/core/src/journal/application/fetchMacroSeries.test.ts packages/contracts/src/macro.test.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    BAMLH0A0HYM2 present at the end of both DEFAULT_FRED_SERIES_IDS and MACRO_SERIES_IDS; the widened
    fetchMacroSeries + macro contract tests pass GREEN (were RED on the count/contains assertions);
    memory-twin + contract parity rows present; typecheck clean; no new npm dependency; no migration.
  </acceptance_criteria>
  <done>Acceptance criteria above satisfied; verified green at commit (tests/typecheck/lint as applicable).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FRED public API → makeFredSeriesAdapter | Untrusted vendor JSON; a new series id reuses the already-hardened, Zod-parsed fetch path — no new boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-01 | Tampering | BAMLH0A0HYM2 FRED payload | low | mitigate | Reuses fetchFredSeries' existing Zod parse + '.'-sentinel filter (fred.ts, unchanged) — malformed row is filtered, never fabricated |
| T-24-02 | Information Disclosure | fetch-rates warn logs | low | accept | Existing static warn text; FRED api key never logged (T-02-11 discipline, unchanged code path) |
| T-24-SC | Tampering | npm/pip/cargo installs | low | accept | No new dependency this phase (CONTEXT.md hard constraint) — no package-legitimacy checkpoint applicable |
</threat_model>

<verification>
- `bun run test` (full workspace) green after Task 2.
- `bun run typecheck` clean (closed MacroSeriesId enum widened without any web change).
- `grep -c 'BAMLH0A0HYM2' packages/core/src/journal/application/fetchMacroSeries.ts` == 1 and same in macro.ts.
- regime-board.md renders the 4 admitted indicators + refutations; TOPIC-MAP links it.
</verification>

<success_criteria>
regime-board.md is the citation-of-record for the board (MACRO-02); HY OAS accretes daily into
macro_observations via the existing fetch-rates cron with as-of-dated rows (MACRO-03), no new job,
no migration, no new dependency.
</success_criteria>

<artifacts_produced>
- NEW `docs/architecture/regime-board.md` — evidence table + Refuted/Dropped section
- EDIT `docs/architecture/data-model.md`, `docs/architecture/jobs.md` — macro series count 9 → 11
- EDIT `docs/TOPIC-MAP.md` — regime-board.md index row
- EDIT `packages/core/src/journal/application/fetchMacroSeries.ts` — BAMLH0A0HYM2 in DEFAULT_FRED_SERIES_IDS
- EDIT `packages/contracts/src/macro.ts` — BAMLH0A0HYM2 in MACRO_SERIES_IDS
- EDIT memory-twin + contract test parity rows
</artifacts_produced>

<output>
Create `.planning/phases/24-regime-breadth-board/24-01-SUMMARY.md` when done.
</output>
