---
phase: 24-regime-breadth-board
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/regime.ts
  - packages/contracts/src/regime.test.ts
  - packages/contracts/src/index.ts
  - packages/core/src/analytics/domain/regime.ts
  - packages/core/src/analytics/domain/regime.test.ts
  - packages/core/src/analytics/index.ts
autonomous: true
requirements: [BOARD-01, BOARD-02]
must_haves:
  truths:
    - "regimeResponse is ONE Zod schema (array of {id,label,value,band,asOf,source,rationale,inputs?}) that the HTTP route AND the MCP tool will both parse (MCP-02, BOARD-01/02)"
    - "band is one of calm|warning|crisis and asOf is a date-only string (no time-of-day) — EOD, never intraday (MACRO-03 at the contract level)"
    - "4 pure banding functions classify each indicator's value with named threshold constants — no inlined magic numbers, monotonic with no gap/overlap at the cuts"
  artifacts:
    - path: packages/contracts/src/regime.ts
      provides: "regimeIndicator + regimeResponse Zod schemas"
      contains: "regimeResponse"
    - path: packages/core/src/analytics/domain/regime.ts
      provides: "bandVixTermStructure, bandVvix, bandVix9dRatio, bandHyOas + RegimeBand type"
      contains: "RegimeBand"
  key_links:
    - from: packages/contracts/src/index.ts
      to: packages/contracts/src/regime.ts
      via: "barrel re-export so the route + MCP tool + web hook import the ONE schema"
      pattern: "regimeResponse"
    - from: packages/core/src/analytics/index.ts
      to: packages/core/src/analytics/domain/regime.ts
      via: "public-surface export of the banding functions"
      pattern: "bandVixTermStructure"
---

<objective>
Interface-first foundation: the `regimeResponse` Zod contract (shared by the future route + MCP
tool) and the pure banding domain (4 calm/warning/crisis classifiers with named threshold
constants). No I/O, no wiring — just the contract and the math the use-case (24-04) builds against.

Purpose: BOARD-01 (value + band + as-of per indicator) and BOARD-02 (payload-carried
source/rationale) are contract shapes; the banding is pure derivation (research Pattern 1 — same
idiom as gex.ts's pickWalls/findFlip). Defining them first means 24-04's executor receives the
types and functions instead of scavenger-hunting them.
Output: regime.ts contract + regime.ts domain, both fully unit-tested.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-regime-breadth-board/24-RESEARCH.md
@.planning/phases/24-regime-breadth-board/24-UI-SPEC.md
@packages/contracts/src/macro.ts
@packages/core/src/analytics/domain/gex.ts
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: regimeResponse contract (RED→GREEN)</name>
  <files>packages/contracts/src/regime.ts, packages/contracts/src/regime.test.ts, packages/contracts/src/index.ts</files>
  <read_first>
    - packages/contracts/src/macro.ts (the sibling contract file's structure + docstring style; macroSeriesPoint uses z.string().date())
    - packages/contracts/src/index.ts (the barrel — add the regime exports beside the macro block, ~line 111)
    - .planning/phases/24-regime-breadth-board/24-RESEARCH.md ("Board payload shape" under Architecture Patterns — the exact field set)
    - .planning/phases/24-regime-breadth-board/24-CONTEXT.md ("Specific Ideas" — { id, label, value, band, asOf, source, rationale, inputs })
  </read_first>
  <behavior>
    - regime.test.ts: a well-formed indicator object parses; band outside calm|warning|crisis is rejected; a non-date asOf (e.g. an ISO timestamp with time-of-day) is rejected; inputs is optional and, when present, is a record of string→number; regimeResponse parses an array and accepts [] (empty board).
  </behavior>
  <action>
    Create packages/contracts/src/regime.ts exporting `regimeIndicator` = z.object({ id: z.string(),
    label: z.string(), value: z.number(), band: z.enum(["calm","warning","crisis"]), asOf:
    z.string().date(), source: z.string(), rationale: z.string(), inputs: z.record(z.string(),
    z.number()).optional() }) and `regimeResponse` = z.array(regimeIndicator), plus the inferred
    types (RegimeIndicator, RegimeResponse, RegimeBand from the enum). asOf uses z.string().date()
    (date-only) so the contract itself forbids an intraday timestamp (MACRO-03). Re-export both
    schemas + types from packages/contracts/src/index.ts beside the macro exports. Write regime.test.ts
    RED first (file absent), then implement to GREEN. This schema is the SINGLE source the route and
    the MCP tool will both parse (MCP-02) — do NOT duplicate a second shape anywhere.
  </action>
  <verify>
    <automated>bun run test -- packages/contracts/src/regime.test.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    regime.ts exports regimeIndicator + regimeResponse + inferred types; band-enum and date-only-asOf
    validation enforced (bad band / timestamp-asOf rejected); [] accepted; barrel re-exports them;
    tests GREEN; typecheck clean.
  </acceptance_criteria>
  <done>Acceptance criteria above satisfied; verified green at commit (tests/typecheck/lint as applicable).</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: pure banding domain — 4 classifiers with named constants (RED→GREEN + fast-check)</name>
  <files>packages/core/src/analytics/domain/regime.ts, packages/core/src/analytics/domain/regime.test.ts, packages/core/src/analytics/index.ts</files>
  <read_first>
    - packages/core/src/analytics/domain/gex.ts (the pure-function idiom to mirror: primitive inputs → typed verdict, zero I/O)
    - packages/core/src/analytics/index.ts (the public-surface barrel — export the banding fns + RegimeBand beside the existing domain-function exports, ~top of file)
    - .planning/phases/24-regime-breadth-board/24-RESEARCH.md (Per-Indicator Adjudication — the exact band cuts per indicator)
  </read_first>
  <behavior>
    - regime.test.ts (example): bandVixTermStructure(0.89)="calm", (0.90)="warning", (0.95)="crisis"; bandVvix(99)="calm",(100)="warning",(115)="crisis"; bandVix9dRatio(0.99)="calm",(1.0)="warning",(1.1)="crisis"; bandHyOas(2.99)="calm",(3.0)="warning",(5.0)="crisis".
    - regime.test.ts (fast-check): for each band fn, the classification is monotonic non-decreasing in the input value (calm ≤ warning ≤ crisis ordering never inverts) and every real number maps to exactly one band — no gap, no overlap at the boundary cuts.
  </behavior>
  <action>
    Create packages/core/src/analytics/domain/regime.ts exporting a `RegimeBand = "calm" | "warning"
    | "crisis"` type and four pure functions — bandVixTermStructure(ratio), bandVvix(level),
    bandVix9dRatio(ratio), bandHyOas(percent) — each returning a RegimeBand via plain if/else over
    NAMED threshold constants (no inlined magic numbers; Open Question 1 wants these one-line tunable).
    Cuts from 24-RESEARCH.md: VIX/VIX3M warn ≥0.90 / crisis ≥0.95; VVIX warn ≥100 / crisis ≥115;
    VIX9D/VIX warn ≥1.0 / crisis ≥1.1; HY OAS (percent units) warn ≥3.0 / crisis ≥5.0. Comment the
    VIX9D/VIX and HY OAS constants as ASSUMED/newly-calibrated display-only bands (no hard gate this
    phase). Do NOT build a rules-engine/DSL and do NOT assemble a composite fragility score — plain
    functions only (research Don't-Hand-Roll + Anti-Patterns). Export the fns + RegimeBand from
    packages/core/src/analytics/index.ts. Write regime.test.ts (example + fast-check) RED first, then
    implement to GREEN.
  </action>
  <verify>
    <automated>bun run test -- packages/core/src/analytics/domain/regime.test.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    Four banding functions classify at the documented calm/warning/crisis cuts using named constants;
    example + fast-check (monotonic, total, no gap/overlap) tests GREEN; exported from analytics barrel;
    no DSL, no composite score, no magic numbers; typecheck + lint clean.
  </acceptance_criteria>
  <done>Acceptance criteria above satisfied; verified green at commit (tests/typecheck/lint as applicable).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure contract + pure domain — no network, no storage, no user input crosses here. The contract is a downstream validation boundary the route/MCP enforce in 24-04 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-06 | Tampering | Banded value silently defaulting on bad input | low | mitigate | Banding fns are total over the reals (fast-check proves no gap/overlap) — no NaN-to-default path introduced; missing-input handling lives in 24-04's use-case, not here |
| T-24-07 | Information Disclosure | rationale/source strings in the contract | low | accept | Static, non-secret provenance copy — public analytics data, same trust class as /analytics/macro |
| T-24-SC | Tampering | npm/pip/cargo installs | low | accept | No new dependency (zod already a workspace dep) — no install/checkpoint applicable |
</threat_model>

<verification>
- `bun run test -- packages/contracts/src/regime.test.ts packages/core/src/analytics/domain/regime.test.ts` green (example + fast-check).
- `bun run typecheck` + `bun run lint` clean.
- regimeResponse + banding fns re-exported from their package barrels (import-resolves in 24-04).
</verification>

<success_criteria>
The board's contract and math exist and are proven independent of any wiring: one shared
regimeResponse schema (MCP-02-ready) and four monotonic, gap-free banding functions with tunable
named thresholds — ready for the 24-04 use-case to consume.
</success_criteria>

<artifacts_produced>
- NEW `packages/contracts/src/regime.ts` (+ test) — regimeIndicator + regimeResponse
- NEW `packages/core/src/analytics/domain/regime.ts` (+ test) — 4 banding fns + RegimeBand
- EDIT `packages/contracts/src/index.ts`, `packages/core/src/analytics/index.ts` — barrel exports
</artifacts_produced>

<output>
Create `.planning/phases/24-regime-breadth-board/24-03-SUMMARY.md` when done.
</output>
