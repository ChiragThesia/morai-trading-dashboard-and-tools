---
phase: 24-regime-breadth-board
plan: 04
type: tdd
wave: 3
depends_on: [24-02, 24-03]
files_modified:
  - packages/core/src/analytics/application/getRegimeBoard.ts
  - packages/core/src/analytics/application/getRegimeBoard.test.ts
  - packages/core/src/analytics/index.ts
  - packages/core/src/index.ts
  - apps/server/src/adapters/http/analytics.routes.ts
  - apps/server/src/adapters/http/analytics.routes.test.ts
  - apps/server/src/adapters/mcp/tools.ts
  - apps/server/src/adapters/mcp/server.ts
  - apps/server/src/main.ts
autonomous: true
requirements: [BOARD-01, BOARD-02, BOARD-03, MACRO-03]
must_haves:
  truths:
    - "GET /api/analytics/regime returns the RegimeIndicator[] board — each present indicator with value, band, asOf, source, rationale, inputs (BOARD-01/02)"
    - "The get_regime MCP tool returns the SAME regimeResponse payload as the HTTP route (BOARD-03, MCP-02)"
    - "asOf is the observation date; for a ratio indicator it is the OLDER of its two input dates — never overstates freshness (MACRO-03)"
    - "A missing input series omits its indicator from the array — never a fabricated value or a placeholder row; empty store → [] (not an error)"
  artifacts:
    - path: packages/core/src/analytics/application/getRegimeBoard.ts
      provides: "makeGetRegimeBoardUseCase(readMacroObservations) → RegimeIndicator[]"
      contains: "makeGetRegimeBoardUseCase"
    - path: apps/server/src/adapters/http/analytics.routes.ts
      provides: "GET /analytics/regime route parsing regimeResponse"
      contains: "/analytics/regime"
    - path: apps/server/src/adapters/mcp/tools.ts
      provides: "registerGetRegimeTool (MCP-02)"
      contains: "get_regime"
  key_links:
    - from: apps/server/src/main.ts
      to: packages/core/src/analytics/application/getRegimeBoard.ts
      via: "makeGetRegimeBoardUseCase wired with the EXISTING macroObservationsRepo.readMacroObservations"
      pattern: "makeGetRegimeBoardUseCase"
    - from: apps/server/src/adapters/http/analytics.routes.ts
      to: packages/contracts/src/regime.ts
      via: "regimeResponse.parse before responding (same schema the MCP tool parses)"
      pattern: "regimeResponse"
---

<objective>
Build the board read surface: the `makeGetRegimeBoardUseCase` use-case (reads macro_observations,
picks the latest row per series, computes the 4 indicators with band + provenance + as-of, omits
any indicator missing an input), then expose it through `GET /api/analytics/regime` AND the
`get_regime` MCP tool over the one shared regimeResponse schema.

Purpose: BOARD-01/02/03 + MACRO-03. The use-case reuses the EXISTING ForReadingMacroObservations
port already wired into getMacro's composition root (research: zero new repo, no new table). New
use-case ⇒ HTTP route + MCP tool in the same PR (architecture rule 9).
Output: a live, contract-parsed regime board on HTTP + MCP.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-regime-breadth-board/24-RESEARCH.md
@packages/core/src/journal/application/getMacro.ts
@apps/server/src/adapters/http/analytics.routes.ts
@apps/server/src/adapters/mcp/tools.ts
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: makeGetRegimeBoardUseCase — compute the board from macro_observations (RED→GREEN)</name>
  <files>packages/core/src/analytics/application/getRegimeBoard.ts, packages/core/src/analytics/application/getRegimeBoard.test.ts, packages/core/src/analytics/index.ts, packages/core/src/index.ts</files>
  <read_first>
    - packages/core/src/journal/application/getMacro.ts (the read-use-case shape to mirror: makeXxx(deps) returning a driver port, reads ForReadingMacroObservations, groups rows, propagates StorageError)
    - packages/core/src/journal/application/ports.ts (~line 738 — ForReadingMacroObservations returns ALL rows unfiltered; MacroObservationRow shape at ~line 702)
    - packages/core/src/analytics/domain/regime.ts (the 4 banding fns from 24-03 — this use-case computes the ratio/level then calls them)
    - packages/core/src/analytics/index.ts + packages/core/src/index.ts (the getGex export chain to mirror for the new use-case, ~line 270-280 in core/index.ts)
  </read_first>
  <behavior>
    - getRegimeBoard.test.ts (in-memory macro rows): with latest VIXCLS + VXVCLS rows present, the array includes a "vix-term-structure" indicator whose value = VIXCLS/VXVCLS, band from bandVixTermStructure, asOf = the OLDER of the two rows' dates, inputs = {VIXCLS, VXVCLS}; a VVIX row yields "vvix" (value = level, asOf = that row's date); VIX9D + VIXCLS yield "vix9d-vix"; BAMLH0A0HYM2 yields "hy-oas".
    - Missing input: with no VXVCLS row, the "vix-term-structure" indicator is ABSENT from the array (not zeroed, not a placeholder). Empty store → ok([]).
    - Each indicator carries the source + rationale strings from a domain metadata table (payload-carried, BOARD-02).
    - StorageError from the repo propagates unchanged.
  </behavior>
  <action>
    Create packages/core/src/analytics/application/getRegimeBoard.ts: makeGetRegimeBoardUseCase(deps:
    { readMacroObservations: ForReadingMacroObservations }) returning ForRunningGetRegimeBoard = () =>
    Promise<Result<ReadonlyArray<RegimeIndicatorOut>, StorageError>>. RegimeIndicatorOut is a
    core-local type (core cannot import @morai/contracts) structurally matching regimeIndicator
    ({id,label,value,band,asOf,source,rationale,inputs}); the route/MCP parse it through
    regimeResponse at the edge. Read all rows, group by seriesId, take the MAX-date row per series as
    the latest. Build up to 4 indicators; for each, if any required input series has no row, OMIT the
    indicator (never fabricate). Compute value (ratio = numerator/denominator for VIX/VIX3M and
    VIX9D/VIX; raw level for VVIX and HY OAS), band via the 24-03 fns, and asOf: for single-input
    indicators the input's date; for ratio indicators the OLDER (min) of the two input dates (never
    overstate freshness — staleness-is-first-class). Author the id/label/source/rationale per
    indicator as a small domain metadata constant (source URLs + rationale summarized from
    24-RESEARCH.md; BOARD-02 payload-carried, not hardcoded in the UI). Export makeGetRegimeBoardUseCase
    + ForRunningGetRegimeBoard + GetRegimeBoardDeps from analytics/index.ts and re-export from
    core/index.ts, mirroring the getGex export chain. Write the test RED first, then implement GREEN.
  </action>
  <verify>
    <automated>bun run test -- packages/core/src/analytics/application/getRegimeBoard.test.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    The use-case computes 4 indicators from latest macro rows with correct value/band/asOf/source/
    rationale/inputs; ratio asOf = older input date; a missing input omits its indicator; empty store
    → ok([]); StorageError propagates; exported through both barrels; tests GREEN; typecheck clean.
  </acceptance_criteria>
  <done>Acceptance criteria above satisfied; verified green at commit (tests/typecheck/lint as applicable).</done>
</task>

<task type="auto">
  <name>Task 2: HTTP route + MCP tool over the shared regimeResponse schema (BOARD-03 / MCP-02)</name>
  <files>apps/server/src/adapters/http/analytics.routes.ts, apps/server/src/adapters/http/analytics.routes.test.ts, apps/server/src/adapters/mcp/tools.ts, apps/server/src/adapters/mcp/server.ts, apps/server/src/main.ts</files>
  <read_first>
    - apps/server/src/adapters/http/analytics.routes.ts (the /analytics/cot route — no user input, output-contract-parsed, empty→[] — is the closest shape for /analytics/regime; the analyticsRoutes(...) factory signature takes each use-case as a param)
    - apps/server/src/adapters/http/analytics.routes.test.ts (existing describe blocks — add a /analytics/regime block, do not create a new file)
    - apps/server/src/adapters/mcp/tools.ts (~line 675 registerGetMacroTool — the exact structural mirror for registerGetRegimeTool)
    - apps/server/src/adapters/mcp/server.ts (~line 78 makeMcpRouter signature + ~line 124 the optional getMacro registration block)
    - apps/server/src/main.ts (~line 172 macroObservationsRepo + getMacro wiring; ~line 289 analyticsRoutes(...) mount; ~line 339 makeMcpRouter(...) call)
  </read_first>
  <action>
    Add `getRegimeBoard: ForRunningGetRegimeBoard` as a new param to the analyticsRoutes(...) factory
    and register `router.get("/analytics/regime", ...)`: no user query input (like /analytics/cot),
    call getRegimeBoard(), map err → flat { error: "internal" } 500 (T-14-14 pattern, never leak DB
    internals), parse the ok value through regimeResponse before c.json (empty → [], never an error).
    Add registerGetRegimeTool(server, getRegimeBoard) to tools.ts as a structural mirror of
    registerGetMacroTool — no input schema (or empty), parse the result through the SAME regimeResponse
    schema, return content; flat "internal error" text on err. Thread an optional
    `getRegimeBoard?: ForRunningGetRegimeBoard` param into makeMcpRouter (server.ts) beside getMacro
    and register the tool when defined. In main.ts, build `const getRegimeBoard =
    makeGetRegimeBoardUseCase({ readMacroObservations: macroObservationsRepo.readMacroObservations })`
    reusing the EXISTING macroObservationsRepo (zero new repo), pass it into the analyticsRoutes(...)
    mount and the makeMcpRouter(...) call. Add a route-level integration test to
    analytics.routes.test.ts asserting GET /analytics/regime returns 200 + a regimeResponse-valid body
    (present indicators for seeded rows, [] for an empty store). This is adapter wiring — zero business
    logic in the route/tool (architecture rule 3).
  </action>
  <verify>
    <automated>bun run test -- apps/server/src/adapters/http/analytics.routes.test.ts && bun run typecheck && bun run lint</automated>
  </verify>
  <acceptance_criteria>
    GET /api/analytics/regime returns a regimeResponse-valid board (or [] on empty), err→flat 500;
    get_regime MCP tool returns the SAME schema; both parse the one regimeResponse (a one-sided field
    change fails typecheck, MCP-02); main.ts wires getRegimeBoard with the existing macroObservationsRepo;
    route test GREEN; typecheck + lint clean.
  </acceptance_criteria>
  <done>Acceptance criteria above satisfied; verified green at commit (tests/typecheck/lint as applicable).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /api/analytics/regime | Read-only analytics endpoint inside the existing Supabase-JWT authReadGroup — same trust boundary as /analytics/macro; no new access-control decision |
| MCP client → get_regime | Bearer-gated /mcp surface (existing MCP_BEARER_TOKEN gate) |
| macro_observations → use-case → response | Stored rows shaped into the board; a DB error must not leak internals |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-08 | Information Disclosure | /analytics/regime + get_regime error responses | medium | mitigate | Map StorageError → flat { error: "internal" } / "internal error" (T-14-14 precedent); never return the DB message |
| T-24-09 | Tampering | Fabricated indicator when an input series is missing | high | mitigate | Use-case OMITS an indicator whose input row is absent (tested) — never substitutes a stale/default number; empty store → [] |
| T-24-10 | Tampering | Overstated as-of freshness on a ratio | medium | mitigate | asOf = OLDER of the two input dates (tested) — the ratio is only as fresh as its stalest leg |
| T-24-SC | Tampering | npm/pip/cargo installs | low | accept | No new dependency — no install/checkpoint applicable |
</threat_model>

<verification>
- `bun run test` (full workspace) green, including the new route + use-case tests.
- `bun run typecheck` + `bun run lint` clean (one-sided regimeResponse change would fail typecheck — MCP-02 guard).
- `grep -c '/analytics/regime' apps/server/src/adapters/http/analytics.routes.ts` >= 1; `grep -c 'get_regime' apps/server/src/adapters/mcp/tools.ts` >= 1.
- Manual/route-test: seeded macro rows → 200 with present indicators; empty store → 200 + [].
</verification>

<success_criteria>
The board is queryable via HTTP and MCP over one schema (BOARD-03/MCP-02), computed on-read from
macro_observations with no new table, honestly as-of-stamped (MACRO-03), and never fabricates a
missing indicator (fails closed).
</success_criteria>

<artifacts_produced>
- NEW `packages/core/src/analytics/application/getRegimeBoard.ts` (+ test) — the board use-case
- EDIT `packages/core/src/analytics/index.ts`, `packages/core/src/index.ts` — use-case exports
- EDIT `apps/server/src/adapters/http/analytics.routes.ts` (+ test) — GET /analytics/regime
- EDIT `apps/server/src/adapters/mcp/tools.ts`, `apps/server/src/adapters/mcp/server.ts` — get_regime tool
- EDIT `apps/server/src/main.ts` — use-case + route + MCP wiring (reuses existing macroObservationsRepo)
</artifacts_produced>

<output>
Create `.planning/phases/24-regime-breadth-board/24-04-SUMMARY.md` when done.
</output>
