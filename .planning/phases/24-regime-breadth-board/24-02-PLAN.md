---
phase: 24-regime-breadth-board
plan: 02
type: tdd
wave: 2
depends_on: [24-01]
files_modified:
  - packages/core/src/journal/application/ports.ts
  - packages/core/src/index.ts
  - packages/adapters/src/http/cboe-vix9d.ts
  - packages/adapters/src/http/cboe-vix9d.test.ts
  - packages/adapters/src/memory/vix9d.ts
  - packages/adapters/src/memory/vix9d.test.ts
  - packages/adapters/src/index.ts
  - packages/core/src/journal/application/fetchMacroSeries.ts
  - packages/core/src/journal/application/fetchMacroSeries.test.ts
  - packages/contracts/src/macro.ts
  - packages/contracts/src/macro.test.ts
  - apps/worker/src/main.ts
autonomous: true
requirements: [MACRO-02, MACRO-03]
must_haves:
  truths:
    - "VIX9D is fetched from the CBOE _VIX9D delayed-quote endpoint each fetch-rates run and persisted to macro_observations with source 'cboe' (MACRO-02/03)"
    - "The VIX9D fetch fails closed — a malformed/non-200 payload returns err and omits the row, never a fabricated value"
    - "macro_observations now carries 11 series ids (VIX9D + BAMLH0A0HYM2 added); no new job, no migration"
  artifacts:
    - path: packages/adapters/src/http/cboe-vix9d.ts
      provides: "makeCboeVix9dAdapter — clone of cboe-vvix.ts, URL + seriesId swapped"
      contains: "_VIX9D"
    - path: packages/adapters/src/memory/vix9d.ts
      provides: "in-memory twin of the VIX9D fetch port (architecture rule 8)"
      contains: "ForFetchingVix9dQuote"
    - path: packages/core/src/journal/application/fetchMacroSeries.ts
      provides: "VIX9D fetch task alongside VVIX"
      contains: "fetchVix9dQuote"
  key_links:
    - from: apps/worker/src/main.ts
      to: packages/adapters/src/http/cboe-vix9d.ts
      via: "makeCboeVix9dAdapter wired as fetchVix9dQuote dep of makeFetchMacroSeries"
      pattern: "makeCboeVix9dAdapter"
    - from: packages/core/src/journal/application/fetchMacroSeries.ts
      to: macro_observations
      via: "VIX9D task persists a cboe row each fetch-rates run"
      pattern: "fetchVix9dQuote"
---

<objective>
Add VIX9D ingestion: a new CBOE `_VIX9D` delayed-quote adapter (cloned from the existing VVIX
adapter), a new `ForFetchingVix9dQuote` driven port + its in-memory twin, a VIX9D fetch task in
the macro orchestration, the VIX9D enum id, and the worker composition-root wiring.

Purpose: FRED does not publish VIX9D (VIX9DCLS 404s — 24-RESEARCH.md). CBOE's `_VIX9D.json` is
byte-identical in shape to the already-shipped `_VVIX.json`, so the smaller diff is to CLONE
cboe-vvix.ts, not parameterize it (research Pattern 2 / Anti-Pattern: CBOE serves only 2 series).
Output: VIX9D accretes daily into macro_observations via the existing fetch-rates cron.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-regime-breadth-board/24-RESEARCH.md
@packages/adapters/src/http/cboe-vvix.ts
@packages/adapters/src/memory/vvix.ts
@packages/core/src/journal/application/fetchMacroSeries.ts
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: CBOE VIX9D adapter + port + in-memory twin (RED→GREEN)</name>
  <files>packages/core/src/journal/application/ports.ts, packages/core/src/index.ts, packages/adapters/src/http/cboe-vix9d.ts, packages/adapters/src/http/cboe-vix9d.test.ts, packages/adapters/src/memory/vix9d.ts, packages/adapters/src/memory/vix9d.test.ts, packages/adapters/src/index.ts</files>
  <read_first>
    - packages/adapters/src/http/cboe-vvix.ts (the adapter to clone — change exactly the URL and the returned seriesId literal)
    - packages/adapters/src/http/cboe-vvix.test.ts (the msw test to clone — 200 / non-200 / malformed-payload / missing-spot paths)
    - packages/adapters/src/memory/vvix.ts (the in-memory twin to clone — architecture rule 8)
    - packages/core/src/journal/application/ports.ts (the ForFetchingVvixQuote port type, ~line 720 — add ForFetchingVix9dQuote beside it)
    - packages/core/src/index.ts (~line 316 re-export block — add ForFetchingVix9dQuote next to ForFetchingVvixQuote)
    - packages/adapters/src/index.ts (~line 96/158 — add makeCboeVix9dAdapter + makeMemoryVix9dAdapter exports)
  </read_first>
  <behavior>
    - cboe-vix9d.test.ts (msw at the network layer): a 200 with a `{timestamp, data:{current_price}}` body returns ok({seriesId:"VIX9D", date: ET-trading-day, value: current_price, source:"cboe"}); a non-200 returns err; a malformed/unparseable payload returns err; a missing/zero spot returns err. RED before the adapter file exists.
    - vix9d.test.ts: unseeded fetchVix9dQuote returns err; after seed(row) returns ok(row) — no fabricated fallback.
  </behavior>
  <action>
    Add `ForFetchingVix9dQuote` to ports.ts as an exact structural twin of ForFetchingVvixQuote
    (() => Promise<Result<MacroObservationRow, FetchError>>, docstring noting seriesId 'VIX9D',
    source 'cboe', no fallback), and re-export it from core/index.ts beside ForFetchingVvixQuote.
    Clone cboe-vvix.ts → cboe-vix9d.ts as makeCboeVix9dAdapter: change ONLY the URL constant to
    https://cdn.cboe.com/api/global/delayed_quotes/quotes/_VIX9D.json and the returned seriesId
    literal to "VIX9D". Keep the identical response schema, the current_price ?? close ??
    prev_day_close spot resolution, the UTC-timestamp → America/New_York trading-day derivation
    (the CBOE-UTC + ET-day conventions are load-bearing regression gates), and the fail-closed
    no-fallback behavior. Do NOT build a parameterized generic CBOE fetcher for 2 call sites
    (research Anti-Pattern). Clone the msw test (cboe-vix9d.test.ts) and the in-memory twin
    (memory/vix9d.ts as makeMemoryVix9dAdapter + its test). Export makeCboeVix9dAdapter and
    makeMemoryVix9dAdapter from packages/adapters/src/index.ts beside their VVIX siblings. Write the
    tests RED first (adapter/twin absent), then implement to GREEN. Zero new npm dependency.
  </action>
  <verify>
    <automated>bun run test -- packages/adapters/src/http/cboe-vix9d.test.ts packages/adapters/src/memory/vix9d.test.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    cboe-vix9d.ts fetches _VIX9D and returns a VIX9D MacroObservationRow (source cboe) on 200,
    err on non-200/malformed/missing-spot; the in-memory twin returns err unseeded and the seeded
    row after seed(); both exported from the adapters barrel; ForFetchingVix9dQuote defined in core
    and re-exported; all msw + twin tests GREEN; typecheck clean.
  </acceptance_criteria>
  <done>Acceptance criteria above satisfied; verified green at commit (tests/typecheck/lint as applicable).</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Wire VIX9D into the macro orchestration + enum + worker root (RED→GREEN)</name>
  <files>packages/core/src/journal/application/fetchMacroSeries.ts, packages/core/src/journal/application/fetchMacroSeries.test.ts, packages/contracts/src/macro.ts, packages/contracts/src/macro.test.ts, apps/worker/src/main.ts</files>
  <read_first>
    - packages/core/src/journal/application/fetchMacroSeries.ts (the VVIX task in the `tasks` array + the deps shape — add a VIX9D task + a fetchVix9dQuote dep exactly beside VVIX)
    - packages/contracts/src/macro.ts (MACRO_SERIES_IDS — append "VIX9D" at the END)
    - apps/worker/src/main.ts (~lines 178-191 — the fetchVvixQuote = makeCboeVvixAdapter(...) block + the makeFetchMacroSeries({...}) deps object)
  </read_first>
  <behavior>
    - fetchMacroSeries.test.ts: makeFetchMacroSeries now requires a fetchVix9dQuote dep; the built task list includes a VIX9D task; a run persists the VIX9D row when its fetch succeeds, and names VIX9D among failures when it fails (best-effort + fail-loud, D-07). RED on the new-dep/task assertions before wiring.
    - macro.test.ts: MACRO_SERIES_IDS contains "VIX9D" (RED on the count/contains assertion before the enum widens).
  </behavior>
  <action>
    Add a `fetchVix9dQuote: ForFetchingVix9dQuote` field to makeFetchMacroSeries's deps and push a
    `{ id: "VIX9D", fetch: () => deps.fetchVix9dQuote() }` task into the tasks array right after the
    VVIX task — it inherits the existing Promise.allSettled best-effort + fail-loud finish (D-07)
    with no new control flow. Append "VIX9D" to the END of MACRO_SERIES_IDS in macro.ts. In
    apps/worker/src/main.ts, build `const fetchVix9dQuote = makeCboeVix9dAdapter({ fetch:
    globalThis.fetch, userAgent: USER_AGENT })` beside the existing fetchVvixQuote, and pass
    fetchVix9dQuote into the makeFetchMacroSeries({...}) deps object. No new cron, no new queue —
    VIX9D piggybacks the existing twice-daily fetch-rates run (research architecture diagram).
    Update the fetchMacroSeries + macro tests RED first, then wire to GREEN. Commit at green only.
  </action>
  <verify>
    <automated>bun run test -- packages/core/src/journal/application/fetchMacroSeries.test.ts packages/contracts/src/macro.test.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    makeFetchMacroSeries requires and invokes fetchVix9dQuote; the VIX9D task persists on success and
    is named on failure; MACRO_SERIES_IDS ends with "VIX9D" (11 ids total); worker root wires
    makeCboeVix9dAdapter; all tests GREEN; typecheck clean; no new job/queue/migration/dependency.
  </acceptance_criteria>
  <done>Acceptance criteria above satisfied; verified green at commit (tests/typecheck/lint as applicable).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CBOE _VIX9D public endpoint → makeCboeVix9dAdapter | Untrusted, undocumented vendor JSON crosses into the worker; parsed at the adapter edge |
| worker cron → macro_observations | Fetched value persisted; a fabricated value would poison every downstream band |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-03 | Tampering | CBOE _VIX9D payload shape (vendor changes JSON) | medium | mitigate | Zod safeParse at the adapter edge (cloned from cboe-vvix.ts); malformed/missing-spot returns err → row omitted, never a fabricated value |
| T-24-04 | Tampering | Timezone mislabel of the observation date | low | mitigate | Reuses the UTC → America/New_York trading-day derivation verbatim (regression gate: CBOE timestamps are UTC) |
| T-24-05 | Information Disclosure | worker fetch warn logs | low | accept | Static warn text pattern inherited from cboe-vvix.ts; no secrets (CBOE endpoint needs no key) |
| T-24-SC | Tampering | npm/pip/cargo installs | low | accept | No new dependency (CONTEXT.md constraint); CBOE endpoint live-verified in 24-RESEARCH.md 2026-07-09 — no install/checkpoint applicable |
</threat_model>

<verification>
- `bun run test` (full workspace) green.
- `bun run typecheck` clean.
- `grep -c '_VIX9D' packages/adapters/src/http/cboe-vix9d.ts` >= 1; `grep -c 'VIX9D' packages/contracts/src/macro.ts` == 1.
- `grep -c 'makeCboeVix9dAdapter' apps/worker/src/main.ts` == 1.
</verification>

<success_criteria>
VIX9D is a first-class member of the macro pipeline (fetched, parsed, persisted daily via
fetch-rates), fails closed on bad payloads, and adds zero jobs, migrations, or dependencies —
mirroring the VVIX precedent exactly.
</success_criteria>

<artifacts_produced>
- NEW `packages/adapters/src/http/cboe-vix9d.ts` (+ msw test) — CBOE _VIX9D adapter
- NEW `packages/adapters/src/memory/vix9d.ts` (+ test) — in-memory twin
- EDIT `packages/core/src/journal/application/ports.ts` + `packages/core/src/index.ts` — ForFetchingVix9dQuote port
- EDIT `packages/core/src/journal/application/fetchMacroSeries.ts` — VIX9D fetch task + dep
- EDIT `packages/contracts/src/macro.ts` — VIX9D enum id
- EDIT `packages/adapters/src/index.ts`, `apps/worker/src/main.ts` — barrel exports + wiring
</artifacts_produced>

<output>
Create `.planning/phases/24-regime-breadth-board/24-02-SUMMARY.md` when done.
</output>
