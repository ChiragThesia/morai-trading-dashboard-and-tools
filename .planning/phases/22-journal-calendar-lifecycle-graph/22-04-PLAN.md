---
phase: 22-journal-calendar-lifecycle-graph
plan: 04
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - apps/web/src/hooks/useLifecycle.ts
  - apps/web/src/components/LifecycleMasthead.tsx
  - apps/web/src/components/EdgeCard.tsx
  - apps/web/src/components/GreeksNowCard.tsx
  - apps/web/src/components/PnlBridgeCard.tsx
  - apps/web/src/components/BeatsCard.tsx
autonomous: true
requirements: [JRNL-01]
must_haves:
  truths:
    - "useLifecycle(calendarId) fetches and parses the enriched series via lifecycleResponse, with enabled: !!calendarId so it never fires on an empty selection (the Phase-20 bug NOT repeated)."
    - "The rail surfaces forward vol as THE EDGE (EdgeCard: forward vol prominent, front/back IV as context — never a blended line), matching D-02 / success criterion 3."
    - "Greeks are shown SIGNED at the latest snapshot (GreeksNowCard), surfacing the long-vega / short-gamma / +theta signature (success criterion 4)."
    - "The P&L bridge is crosshair-reactive: it reads the enriched series' cumulative buckets at the hovered index (default latest) with an 'as of {day}' label, and always shows the residual row (D-05)."
  artifacts:
    - apps/web/src/hooks/useLifecycle.ts
    - apps/web/src/components/LifecycleMasthead.tsx
    - apps/web/src/components/EdgeCard.tsx
    - apps/web/src/components/GreeksNowCard.tsx
    - apps/web/src/components/PnlBridgeCard.tsx
    - apps/web/src/components/BeatsCard.tsx
  key_links:
    - "useLifecycle → GET /api/journal/:calendarId/lifecycle (route from Plan 22-03); parsed through lifecycleResponse (Plan 22-01)."
    - "All five cards consume the enriched snapshot type (LifecycleResponse['snapshots'][number]) and are mounted/props-wired by Journal.tsx (Plan 22-06)."
---

<objective>
Ship the Journal rail: the `useLifecycle` data hook plus the five presentational cards (masthead, edge,
greeks-now, P&L bridge, beats) that read the enriched series. All are pure presentation over the
already-computed payload — the browser does zero forward-vol / attribution math (that lives server-side,
Plans 22-01/02/03).

Purpose: the rail is where D-02 (forward vol as the edge), D-03 (signed greeks signature), and the
crosshair-reactive P&L bridge (D-01 totals) read at a glance. The hook must NOT repeat the Phase-20
missing-`enabled` bug.
Output: `useLifecycle` hook; `LifecycleMasthead`, `EdgeCard`, `GreeksNowCard`, `PnlBridgeCard`, `BeatsCard`
components (all reusing existing `system/*` molecules — no new visual pattern).

## Artifacts this phase produces (introduced by THIS plan)
- `useLifecycle(calendarId)` — NEW react-query hook, `apps/web/src/hooks/useLifecycle.ts`
- `LifecycleMasthead` — NEW component (verdict headline + read + net-P&L stat)
- `EdgeCard` — NEW component (forward vol / front·back IV / term ratio + one-sentence narrative)
- `GreeksNowCard` — NEW component (signed greeks at latest snapshot, kv rows)
- `PnlBridgeCard` — NEW component (waterfall bridge, crosshair-reactive via a `hoveredIndex` prop)
- `BeatsCard` — NEW component (dot + date + description event list)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-journal-calendar-lifecycle-graph/22-UI-SPEC.md
@.planning/phases/22-journal-calendar-lifecycle-graph/22-PATTERNS.md
@.planning/phases/22-journal-calendar-lifecycle-graph/22-CONTEXT.md
@mockups/journal-lifecycle-v3.html
@apps/web/src/hooks/useJournal.ts
@apps/web/src/components/system/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: useLifecycle data hook (with the enabled guard)</name>
  <files>apps/web/src/hooks/useLifecycle.ts</files>
  <read_first>
    - apps/web/src/hooks/useJournal.ts (the poll/parse/UnauthorizedError react-query pattern to copy — note it LACKS `enabled`, which is the bug to NOT replicate)
    - apps/web/src/hooks/useRuleTags.ts (same missing-`enabled` gap, per memory morai-phase20-executed-deployed — context for why the guard matters)
    - apps/web/src/lib/rpc.ts (apiFetch signature)
    - packages/contracts/src/journal.ts (lifecycleResponse — the parse target, from Plan 22-01)
    - .planning/phases/22-journal-calendar-lifecycle-graph/22-PATTERNS.md ("useLifecycle.ts" section — the full pattern with the `enabled: !!calendarId` FIX called out)
  </read_first>
  <action>
    Create `apps/web/src/hooks/useLifecycle.ts` copying `useJournal.ts`'s structure exactly: an `UnauthorizedError`
    class, a `useQuery` with `queryKey: ["lifecycle", calendarId]`, `queryFn` that `apiFetch(\`/api/journal/${calendarId}/lifecycle\`)`,
    throws `UnauthorizedError` on 401, throws on other non-ok, and returns `lifecycleResponse.parse(await res.json())`.
    Set `refetchInterval: 60_000`, `staleTime: 50_000`, and the same `retry` predicate (no retry on UnauthorizedError,
    else `< 3`). CRITICAL: include `enabled: !!calendarId` on the useQuery options — the existing `useJournal`/`useRuleTags`
    omit it (a live bug that fires a request with an empty id); the new hook must have it from the start. Per workflow.md
    "minimal impact," do NOT retroactively edit useJournal/useRuleTags in this plan (leave as a noted follow-up). Import
    `lifecycleResponse` from `@morai/contracts`. No `any`/`as`/`!`; the return type flows from `lifecycleResponse` via
    `z.infer` (no manual cast).
  </action>
  <acceptance_criteria>
    - source: `useLifecycle` calls `useQuery` with `enabled: !!calendarId` present (grep-visible on the options object).
    - source: the query fetches `/api/journal/${calendarId}/lifecycle` and returns `lifecycleResponse.parse(...)` (no manual `as`).
    - source: 401 throws `UnauthorizedError` and the `retry` predicate returns false for it — mirroring useJournal.
    - test: `bun run typecheck` clean for apps/web.
  </acceptance_criteria>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <done>useLifecycle compiles, parses through lifecycleResponse, and carries `enabled: !!calendarId` from the first commit.</done>
</task>

<task type="auto">
  <name>Task 2: LifecycleMasthead + EdgeCard + GreeksNowCard</name>
  <files>apps/web/src/components/LifecycleMasthead.tsx, apps/web/src/components/EdgeCard.tsx, apps/web/src/components/GreeksNowCard.tsx</files>
  <read_first>
    - apps/web/src/components/system/index.tsx (Panel, PanelHeading, SectionLabel, Stat, MetricChip — the exact molecules to reuse; do not invent new visual primitives)
    - apps/web/src/screens/Journal.tsx (current trade-header Panel block + LifecycleSection for the loading/empty idiom; the retired "why it moved" callout EdgeCard's narrative replaces)
    - mockups/journal-lifecycle-v3.html (the `.mast` verdict masthead, the `.sig` edge-card narrative pattern, the greeks-now rows — visual source of truth)
    - .planning/phases/22-journal-calendar-lifecycle-graph/22-UI-SPEC.md (Typography roles, Color contract, Chart Series Color Map for amber=forward-vol, Copywriting Contract for the verdict/read/edge templates)
    - packages/contracts/src/journal.ts (the enriched snapshot type = `LifecycleResponse["snapshots"][number]` — the prop these cards accept)
  </read_first>
  <action>
    Create three presentational components under `apps/web/src/components/`, each taking the enriched series (typed as
    `LifecycleResponse["snapshots"]` from `@morai/contracts`) and any needed calendar metadata as props (compute-free
    beyond trivial derivations — no forward-vol/attribution math). (a) `LifecycleMasthead.tsx`: verdict headline (20px
    font-display 700, one bolded state-word in `--color-violet`) + read subtext (12px, the LOCKED sentence template from
    UI-SPEC Copywriting — favorable-state clause + what-happened clause joined by an em dash) + net-P&L Stat (15px
    tabular-nums, `--color-down` when negative). Values are computed from the series (theta banked = latest cumTheta,
    forward vol = latest snapshot's forwardVol, net P&L = latest pnlOpen); the template is locked, the numbers are not.
    (b) `EdgeCard.tsx`: forward vol as the prominent value (`--color-amber`, D-02 the edge), front IV + back IV + term
    ratio as smaller context rows, plus the one-sentence `.sig` narrative. NEVER render a blended/averaged vol figure.
    Handle `forwardVolGuard === "inverted"` (forwardVol null) with an explicit "inverted term structure" caption, never
    a blank/NaN. (c) `GreeksNowCard.tsx`: signed netDelta / netGamma / netTheta / netVega at the latest non-gap snapshot,
    as `Stat`/kv rows, sign-preserving (+/−), colored per the UI-SPEC Chart Series Color Map (delta violet, gamma
    `--color-down`, theta `--color-up`, vega `--color-blue`). Reuse `Panel`/`PanelHeading`/`SectionLabel`/`Stat` — no new
    molecule. Only `--color-*` hex values from the UI-SPEC Chart Series Color Map (no mockup literal hexes). NO `any`/`as`/`!`.
  </action>
  <acceptance_criteria>
    - source: all three components import the enriched snapshot type from `@morai/contracts` (no local re-declaration, no `as`).
    - source: `EdgeCard` renders `forwardVol` as the dominant figure in `--color-amber` with front/back IV as context; there is no averaged/blended vol value anywhere.
    - source: `EdgeCard` branches on `forwardVolGuard === "inverted"` to a caption instead of rendering null/NaN.
    - source: `GreeksNowCard` shows all four greeks with preserved sign and the UI-SPEC per-greek colors; `LifecycleMasthead` bolds exactly one state-word in `--color-violet` and colors negative net P&L `--color-down`.
    - source: only `--color-*` tokens/hexes from the UI-SPEC Color + Chart Series Color Map tables appear (no `#2dd4bf`/`#f5b041`/`#6b8bc7`/`#4a5266` mockup literals).
    - test: `bun run typecheck` clean for apps/web.
  </acceptance_criteria>
  <verify>
    <automated>bun run typecheck</automated>
    <human-check>At the phase gate (chrome-devtools UAT): masthead reads verdict-first with one violet state-word; EdgeCard shows forward vol as the amber hero with front/back IV as context (no blended line); GreeksNowCard shows signed greeks in the mapped colors.</human-check>
  </verify>
  <done>Masthead, edge, and greeks-now cards compile, consume the enriched contract type, surface forward vol as the edge, show signed greeks, and use only UI-SPEC colors.</done>
</task>

<task type="auto">
  <name>Task 3: PnlBridgeCard (crosshair-reactive) + BeatsCard</name>
  <files>apps/web/src/components/PnlBridgeCard.tsx, apps/web/src/components/BeatsCard.tsx</files>
  <read_first>
    - apps/web/src/components/system/index.tsx (Stat / kv-row idiom for the bridge rows)
    - mockups/journal-lifecycle-v3.html (the P&L bridge waterfall entry→theta→vega→Δ·Γ→residual→net, and the "beats" event list dot+date+label)
    - apps/web/src/screens/Journal.tsx (RuleTagChips list-rendering idiom analog for BeatsCard's list)
    - .planning/phases/22-journal-calendar-lifecycle-graph/22-UI-SPEC.md (Attribution Idiom Decision — the crosshair-scrub sync + "as of {day}" label contract; Color contract for signed segments; Event verticals colors for beat kinds)
    - packages/contracts/src/journal.ts (enriched snapshot type incl. optional `trigger` used to derive event-move beats)
  </read_first>
  <action>
    Create two rail components. (a) `PnlBridgeCard.tsx`: props = `{ snapshots: LifecycleResponse["snapshots"];
    hoveredIndex: number | null }`. It renders a waterfall/ledger bridge of the cumulative buckets AT the resolved index
    — `hoveredIndex ?? (last non-gap index)` — in row order entry → theta (`--color-up`) → vega (`--color-blue`) →
    delta-gamma (`--color-violet`) → residual (`--color-faint`/`--color-dim`) → net P&L (`--color-txt`). The residual row
    is ALWAYS rendered even at ~0 (D-05 — never dropped). Above the bridge show a mono `"as of {day}"` label derived from
    the resolved snapshot's `time` so it is never ambiguous which point the totals reflect. If the resolved snapshot
    `isGap`, show the last non-gap point's totals with the label pointing at that non-gap day (never invent values for a
    gap). Negative bucket values render in `--color-down`. (b) `BeatsCard.tsx`: props = `{ beats: ReadonlyArray<{ date:
    string; kind: "entry" | "event" | "close"; label: string }> }` — pure list render (dot + date + label), dot color per
    UI-SPEC Event verticals (entry `--color-violet`, scheduled/adverse event `--color-amber`/`--color-down`, close
    contextual). BeatsCard does NOT itself filter the series — Journal.tsx (Plan 22-06) builds the `beats` array (entry
    from calendar.openedAt, event-move beats from snapshots where `trigger === "event-move"`, close from
    calendar.closedAt). Reuse `Panel`/`PanelHeading`/`Stat`; only UI-SPEC colors. NO `any`/`as`/`!`.
  </action>
  <acceptance_criteria>
    - source: `PnlBridgeCard` accepts `hoveredIndex: number | null` and resolves to `hoveredIndex ?? lastNonGapIndex`; it renders a residual row unconditionally.
    - source: the bridge renders an `"as of {day}"` mono label bound to the resolved snapshot's `time`.
    - source: a gap-resolved index falls back to the last non-gap point's totals (no fabricated gap values).
    - source: `BeatsCard` is a pure presentational list over a `beats` prop (it does not read/filter `snapshots` itself); dot colors come from the UI-SPEC Event-verticals mapping.
    - test: `bun run typecheck` clean for apps/web.
  </acceptance_criteria>
  <verify>
    <automated>bun run typecheck</automated>
    <human-check>At the phase gate (chrome-devtools UAT): hovering the hero chart updates the bridge totals + "as of {day}" label; the residual row is always present; beats list shows entry / event-move / close markers.</human-check>
  </verify>
  <done>PnlBridgeCard is crosshair-reactive with an always-present residual row and an "as of" label; BeatsCard renders the beats prop as a colored event list; both typecheck.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response → browser render | The web trusts the enriched payload only after `lifecycleResponse.parse` in the hook; components never re-parse or cast. |
| gap/null cumulative → UI | A gap or null cumulative must render honestly (fallback to last non-gap, or a caption), never as a fabricated number. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-09 | Tampering (data integrity) | useLifecycle response handling | medium | mitigate | Response parsed through `lifecycleResponse` (Zod) before use; a malformed/NaN body throws rather than rendering silently. |
| T-22-10 | Information Disclosure | useLifecycle on empty selection | low | mitigate | `enabled: !!calendarId` prevents a request with an empty id (also avoids the Phase-20 401 noise). |
| T-22-11 | Tampering (UI honesty) | PnlBridgeCard / EdgeCard null handling | low | mitigate | Gap/inverted/null values render as explicit fallbacks or captions, never fabricated numbers (D-05). |
| T-22-SC | Tampering | npm/pip/cargo installs | low | accept | No new dependencies introduced this phase (22-RESEARCH Package Legitimacy Audit = "Not applicable"). |
</threat_model>

<verification>
- `bun run typecheck` clean for apps/web after each task.
- `bun run lint` clean at wave merge.
- Phase-gate chrome-devtools UAT confirms the rail visuals (forward-vol edge, signed greeks, crosshair-reactive bridge, beats).
</verification>

<success_criteria>
- useLifecycle fetches/parses the enriched series with the enabled guard.
- The five rail cards surface forward vol as the edge, signed greeks, and a crosshair-reactive, residual-always P&L bridge — all reusing existing system molecules and UI-SPEC colors.
- Contributes to ROADMAP success criteria 2, 3, 4.
</success_criteria>

<output>
Create `.planning/phases/22-journal-calendar-lifecycle-graph/22-04-SUMMARY.md` when done.
</output>
