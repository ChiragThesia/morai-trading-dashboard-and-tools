---
phase: 22-journal-calendar-lifecycle-graph
plan: 05
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - apps/web/src/components/LifecycleChart.tsx
autonomous: true
requirements: [JRNL-01]
must_haves:
  truths:
    - "The chart is a single stacked column of TIME-on-x panels — attribution hero → vol & term structure → 4 signed greek small-multiples → price vs strike — sharing ONE date axis (D-04/D-08, success criterion 1)."
    - "The HERO panel is P&L attribution over time (theta/vega/delta-gamma stacked + the net line), with the residual band ALWAYS rendered, making the gamma-vs-theta collision visible (D-01, success criteria 2 & 5)."
    - "The vol panel plots front IV, back IV, and forward vol as DISTINCT series with forward vol as the amber edge line — never a blended/averaged line (D-02, success criterion 3)."
    - "Greeks render SIGNED, one small-multiple panel each (delta/gamma/theta/vega) with a zero baseline (D-03, success criterion 4); feed gaps render as line breaks, never interpolated (D-05, success criterion 5)."
  artifacts:
    - apps/web/src/components/LifecycleChart.tsx
  key_links:
    - "LifecycleChart consumes the enriched series (LifecycleResponse['snapshots']) and exposes onCrosshairChange(index|null) → wired to the rail's hoveredIndex by Journal.tsx (Plan 22-06)."
    - "Gap-aware rendering keys off each snapshot's isGap (from Plan 22-02 via the contract) — the visx LinePath `defined` accessor + manual areaSeg/flush for fills."
---

<objective>
Rewrite `LifecycleChart.tsx` from the retired 3-tab + scrubber engine into the D-08 stacked-panel SVG:
one shared TIME x-axis, five stacked regions (attribution hero, vol & term structure, four signed greek
small-multiples, price vs strike), honest gap breaks, and one crosshair spanning the whole stack that
reports the hovered index up to the rail.

Purpose: this is the visual heart of JRNL-01 — the realized-history view no forward price-on-x tool provides.
Every locked decision (D-01 attribution hero, D-02 forward-vol edge, D-03 signed small-multiples, D-04 shared
time axis, D-05 honest gaps) is enforced here.
Output: a rewritten `LifecycleChart` component taking the enriched series and exposing an `onCrosshairChange`
callback.

## Artifacts this phase produces (introduced by THIS plan)
- `LifecycleChart` (REWRITE) — `apps/web/src/components/LifecycleChart.tsx`: stacked-panel engine, new prop shape (enriched series + `onCrosshairChange`), viewBox `0 0 840 700`
- A module-level color-constant block mapped 1:1 to the UI-SPEC Chart Series Color Map
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-journal-calendar-lifecycle-graph/22-UI-SPEC.md
@.planning/phases/22-journal-calendar-lifecycle-graph/22-PATTERNS.md
@.planning/phases/22-journal-calendar-lifecycle-graph/22-CONTEXT.md
@mockups/journal-lifecycle-v3.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stacked-panel engine — regions, scales, all series, honest gaps</name>
  <files>apps/web/src/components/LifecycleChart.tsx</files>
  <read_first>
    - apps/web/src/components/charts/PayoffChart.tsx (targeted sections only — the visx import block ~19-30, the module-level hex-constant block via `rg -n "^const.*=.*#" apps/web/src/components/charts/PayoffChart.tsx`, and its scaleLinear/LinePath idiom; do NOT load the whole 1000-line file)
    - mockups/journal-lifecycle-v3.html (the stacked layout, panel order/heights, the `areaSeg`/`flush()` gap-aware fill helper at lines ~198-206 to port to TS, and the series set)
    - .planning/phases/22-journal-calendar-lifecycle-graph/22-UI-SPEC.md (Layout & Chart Geometry: viewBox 0 0 840 700, preserveAspectRatio "xMinYMin meet", region heights; the full Chart Series Color Map; Honest Data States)
    - packages/contracts/src/journal.ts (enriched snapshot type — the prop; fields: time, spot, frontIv, backIv, netDelta/Gamma/Theta/Vega, forwardVol, forwardVolGuard, isGap, cumTheta/Vega/DeltaGamma/Residual, pnlOpen)
    - .planning/phases/22-journal-calendar-lifecycle-graph/22-PATTERNS.md ("LifecycleChart.tsx" section — gap-aware LinePath `defined`, color-constant convention)
  </read_first>
  <action>
    Rewrite `apps/web/src/components/LifecycleChart.tsx`. Prop shape becomes `{ snapshots: LifecycleResponse["snapshots"];
    onCrosshairChange?: (index: number | null) => void }` (Task 2 wires the callback). Use SVG viewBox `0 0 840 700` with
    `preserveAspectRatio="xMinYMin meet"` (NOT "none" — a multi-panel stack must scale uniformly). Lay out the regions
    top→bottom at the UI-SPEC logical heights: title/legend band 20, P&L Attribution hero 210, Vol & Term Structure 120,
    4× greek small-multiples 28 each + 8 gaps (144 total), Price vs Strike 60, x-axis date band 24. Build ONE shared x-scale
    over the snapshot `time` values (all panels share it, D-04) and per-panel y-scales. Render:
    • HERO: stacked areas for cumTheta / cumVega / cumDeltaGamma (colors: theta `--color-up` #26a69a, vega `--color-blue`
      #5b9cf6, delta-gamma `--color-violet` #a78bfa) PLUS an always-rendered faint residual band (cumResidual, `--color-faint`/
      `--color-dim`) that is NEVER omitted even near zero (D-05), and the net-P&L line on top (`--color-txt` #d6dbe4, 2.4px).
      Always render all 4 inline legend entries incl. residual.
    • VOL: forward vol as the thick 2.6px amber (`--color-amber` #f0b429) hero line (D-02 the edge), front IV thin solid
      `--color-txt`, back IV dashed `--color-muted` — three DISTINCT series, NEVER a blended/averaged line. Where
      `forwardVolGuard === "inverted"` (forwardVol null), the forward-vol line breaks (treat like a gap point) rather than
      plotting null.
    • GREEKS: four signed small-multiples (delta violet, gamma `--color-down` #ef5350, theta `--color-up`, vega
      `--color-blue`), each with a solid 1px zero baseline (`--color-line2`) and a signed fill (~16% opacity) — surfacing the
      long-vega/short-gamma/+theta signature and the sign flip (D-03).
    • PRICE: spot line (`--color-muted`) with a dashed violet strike reference (from the calendar strike passed as a prop, or
      omit the reference line if not provided — Journal wires it in 22-06).
    Honest gaps (D-05): for LinePath-eligible series use `@visx/shape`'s `LinePath` with `defined={(d) => !d.isGap}` so the
    path breaks at gaps (new `M` subpath, never interpolated, never dashed across). For the stacked-area fills (no visx
    gap-aware area primitive), port the mockup's manual `areaSeg`/`flush()` segment-building approach to TS — flush the fill
    at each gap boundary rather than bridging it. Add a module-level hex-constant block (mirroring PayoffChart's
    VIOLET/TEAL/… convention) mapped 1:1 to the UI-SPEC Chart Series Color Map — introduce NO hex value outside that table
    (do not copy the mockup's `#2dd4bf`/`#f5b041`/`#6b8bc7`/`#4a5266`). Compute all values in variables above the return (no
    IIFEs in JSX). NO `any`/`as`/`!`.
  </action>
  <acceptance_criteria>
    - source: viewBox is `0 0 840 700` with `preserveAspectRatio="xMinYMin meet"`; the five regions are laid out at the UI-SPEC heights sharing one x-scale over `time`.
    - source: the hero renders cumTheta/cumVega/cumDeltaGamma stacked + a residual band + the net line, and the residual series/legend entry is rendered unconditionally (no `if (residual)` guard hiding it).
    - source: the vol panel renders forwardVol (amber, thick), frontIv, and backIv as three separate LinePaths — there is no averaged/blended vol series anywhere in the file.
    - source: each greek panel has a zero baseline and a signed fill; the four panels use the UI-SPEC per-greek colors.
    - source: every plotted line uses `defined={(d) => !d.isGap}` (or the ported flush() for fills) so gaps break the path; no interpolation across gaps.
    - source: a module-level color-constant block maps 1:1 to the UI-SPEC Chart Series Color Map; no out-of-table hex literal appears.
    - test: `bun run typecheck` clean for apps/web.
  </acceptance_criteria>
  <verify>
    <automated>bun run typecheck</automated>
    <human-check>At the phase gate (chrome-devtools UAT): the stacked column renders all five regions on one time axis; a calendar with sparse early snapshots shows real line breaks (not straight-line interpolation) across every panel; the residual band is visible.</human-check>
  </verify>
  <done>LifecycleChart renders the D-08 stacked panels on one shared time axis, forward vol as the distinct amber edge, signed greek small-multiples, an always-shown residual, and honest gap breaks — and typechecks.</done>
</task>

<task type="auto">
  <name>Task 2: Shared crosshair + tooltip + onCrosshairChange</name>
  <files>apps/web/src/components/LifecycleChart.tsx</files>
  <read_first>
    - apps/web/src/components/charts/PayoffChart.tsx (crosshair mapping lines ~324-342: localPoint → svgRect → `scaleX = SVG_W / svgRect.width` → logicalX; tooltip positioning ~862-868: `crosshair.x / SVG_W * renderedWidth + 14`; import `localPoint` from `@visx/event`)
    - .planning/phases/22-journal-calendar-lifecycle-graph/22-UI-SPEC.md (Layout & Chart Geometry crosshair/tooltip spec: one shared vertical line spanning attribution→price, LOCKED tooltip row order, gap tooltip copy)
    - mockups/journal-lifecycle-v3.html (crosshair + tooltip visual treatment)
  </read_first>
  <action>
    Add to `LifecycleChart.tsx` one shared vertical crosshair line (`--color-line2`, opacity 0.3–0.6 on hover) spanning from
    the top of the Attribution panel to the bottom of the Price panel. Reuse PayoffChart's EXACT mapping technique verbatim
    (do NOT re-derive): `localPoint(svg, event)` → `svgRect = svg.getBoundingClientRect()` → `scaleX = SVG_W / svgRect.width`
    → `logicalX = point.x * scaleX`, then snap logicalX to the nearest snapshot index via the shared x-scale. Render ONE fixed
    HTML tooltip (not SVG) positioned with PayoffChart's formula (`crosshair.x / SVG_W * renderedWidth`, clamped to container
    width), treatment `rgba(8,11,16,.97)` bg / `--color-line2` border / 8px radius / JetBrains Mono 10.5px. LOCKED tooltip row
    order: day label + DTE front/back → net P&L (bold, `--color-txt`) → theta Δ (`--color-up`) → vega Δ (`--color-blue`) →
    delta-gamma Δ (`--color-violet`) → forward vol (`--color-amber`) → SPX spot (`--color-muted`). When the hovered index
    `isGap`: show ONLY `"feed lapsed — no data"` in `--color-dim`, suppressing every other row (never show interpolated/zero
    values for a gap). Call `onCrosshairChange?.(index)` on hover-move and `onCrosshairChange?.(null)` on mouse-leave so the
    rail P&L bridge (Plan 22-06 wiring) can react. Compute tooltip rows in variables above the return; no IIFEs in JSX. NO
    `any`/`as`/`!`.
  </action>
  <acceptance_criteria>
    - source: the crosshair uses the PayoffChart `localPoint` → `scaleX = SVG_W / svgRect.width` → `logicalX` chain (not a hand-rolled mapping) and spans the full attribution→price height.
    - source: the tooltip renders the LOCKED row order (day+DTE → net P&L → theta → vega → delta-gamma → forward vol → spot) with the mapped colors.
    - source: on a gap index the tooltip shows only "feed lapsed — no data" (`--color-dim`) and no other rows.
    - source: `onCrosshairChange?.(index)` fires on move and `onCrosshairChange?.(null)` on leave.
    - test: `bun run typecheck` clean for apps/web.
  </acceptance_criteria>
  <verify>
    <automated>bun run typecheck</automated>
    <human-check>At the phase gate (chrome-devtools UAT): one crosshair tracks across all panels; the tooltip shows the locked row order; hovering a gap index shows only "feed lapsed — no data"; the rail bridge updates in sync.</human-check>
  </verify>
  <done>A single shared crosshair + fixed HTML tooltip (locked row order, gap-aware copy) works using PayoffChart's mapping, and onCrosshairChange reports the hovered index for rail sync; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| enriched series → SVG paths | A gap or inverted/null value must break the path, never render as an interpolated/fabricated segment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-12 | Tampering (visual honesty) | gap rendering across all panels | medium | mitigate | Every line uses `defined={(d) => !d.isGap}` and fills flush at gap boundaries; the tooltip shows "feed lapsed — no data" on a gap index — no interpolation or zero-fill (D-05). |
| T-22-13 | Tampering (edge misrepresentation) | vol panel | medium | mitigate | Forward vol / front IV / back IV are three distinct LinePaths; no averaged/blended series exists (D-02 — the refuted framing must not ship). |
| T-22-SC | Tampering | npm/pip/cargo installs | low | accept | No new dependencies introduced this phase (visx already installed; 22-RESEARCH Package Legitimacy Audit = "Not applicable"). |
</threat_model>

<verification>
- `bun run typecheck` clean for apps/web after each task.
- `bun run lint` clean at wave merge.
- Phase-gate chrome-devtools UAT: stacked panels on one time axis, forward-vol amber edge, signed greeks, honest gap breaks, synced crosshair + locked tooltip.
</verification>

<success_criteria>
- LifecycleChart renders the D-08 stacked column with a shared time axis, forward vol as the distinct edge, signed greek small-multiples, an always-shown residual, honest gap breaks, and one synced crosshair reporting the hovered index.
- Contributes to ROADMAP success criteria 1, 2, 3, 4, 5.
</success_criteria>

<output>
Create `.planning/phases/22-journal-calendar-lifecycle-graph/22-05-SUMMARY.md` when done.
</output>
