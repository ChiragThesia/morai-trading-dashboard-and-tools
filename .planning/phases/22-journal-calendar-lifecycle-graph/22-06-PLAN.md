---
phase: 22-journal-calendar-lifecycle-graph
plan: 06
type: execute
wave: 3
depends_on: ["22-03", "22-04", "22-05"]
files_modified:
  - apps/web/src/screens/Journal.tsx
autonomous: true
requirements: [JRNL-01]
must_haves:
  truths:
    - "Selecting a calendar in the existing left list renders its lifecycle (masthead + stacked chart + reactive rail) from the enriched useLifecycle series (success criterion 1)."
    - "The hero chart's crosshair and the rail P&L bridge are synced via a shared hoveredIndex — hovering the chart swaps the bridge 'as of {day}' totals across all panels (D-01/D-04)."
    - "Honest states ship: 'Building the lifecycle' for too-new calendars, the unchanged PreHistoryStub for pre-Jun-12, an error state with Retry, and an always-visible honest-caveats footer (D-05); the mockup 'SKETCH · representative data' tag is NOT shipped."
    - "The existing RULE-01 Notes card (RuleTagChips + textarea) is relocated to the bottom of the rail, visually unchanged (not redesigned, not removed)."
  artifacts:
    - "apps/web/src/screens/Journal.tsx (center + right columns rewired; left list unchanged)"
  key_links:
    - "useLifecycle (22-04) → LifecycleSection center + rail; LifecycleChart.onCrosshairChange (22-05) → shared hoveredIndex → PnlBridgeCard (22-04)."
    - "GET /api/journal/:calendarId/lifecycle (22-03) is the live source the screen fetches at UAT."
---

<objective>
Integrate the phase: rewire the Journal screen's CENTER and RIGHT columns to the enriched lifecycle series,
sync the chart crosshair to the rail P&L bridge via a shared `hoveredIndex`, ship the honest empty/loading/
error states + caveats footer, and relocate the existing RULE-01 Notes card. The LEFT calendar-picker list
is unchanged — it already is the D-09 picker.

Purpose: this is the wave that makes JRNL-01 a working screen — the masthead, stacked chart, and reactive
rail come together over one selected calendar. It depends on the route (22-03), the hook + cards (22-04), and
the chart (22-05) all existing.
Output: a rewired `apps/web/src/screens/Journal.tsx`.

## Artifacts this phase produces (introduced by THIS plan)
- Rewired `Journal.tsx`: `useJournal`→`useLifecycle`, a lifted `hoveredIndex` state, `LifecycleMasthead` + rewritten
  `LifecycleChart` in the center, `PnlBridgeCard`/`EdgeCard`/`GreeksNowCard`/`BeatsCard` in the rail, relocated Notes card,
  new "too-new" + error states, honest-caveats footer
- A local `beats` derivation (entry / event-move / close) feeding `BeatsCard`
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-journal-calendar-lifecycle-graph/22-UI-SPEC.md
@.planning/phases/22-journal-calendar-lifecycle-graph/22-PATTERNS.md
@.planning/phases/22-journal-calendar-lifecycle-graph/22-CONTEXT.md
@apps/web/src/screens/Journal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Center column — useLifecycle swap, shared crosshair state, masthead + chart + honest states</name>
  <files>apps/web/src/screens/Journal.tsx</files>
  <read_first>
    - apps/web/src/screens/Journal.tsx (the whole file: current LifecycleSection center column lines ~319-430, the main-screen render + useJournal/useRuleTags calls lines ~436-560, PreHistoryStub, the SnapshotResponse import)
    - apps/web/src/hooks/useLifecycle.ts (the new hook — returns useQuery result incl. data/isPending/error/refetch — from Plan 22-04)
    - apps/web/src/components/LifecycleMasthead.tsx + LifecycleChart.tsx (the center components + LifecycleChart's new prop shape `{ snapshots, onCrosshairChange }` — from Plans 22-04/22-05)
    - .planning/phases/22-journal-calendar-lifecycle-graph/22-UI-SPEC.md (Placement Decision center-column table; Honest Data States; Copywriting Contract for the "Building the lifecycle" / Error / footer copy — and the instruction to DROP the SKETCH tag)
  </read_first>
  <action>
    In `apps/web/src/screens/Journal.tsx`: (a) replace `useJournal(selectedTrade?.calendarId ?? "")` with
    `useLifecycle(selectedTrade?.calendarId ?? "")`, destructuring `data`, `isPending`, `isError`/`error`, and `refetch`;
    `const snapshots: LifecycleResponse["snapshots"] = data?.snapshots ?? [];` (replace the `SnapshotResponse` import with
    the enriched type from `@morai/contracts`). (b) Lift a `const [hoveredIndex, setHoveredIndex] = useState<number | null>(null)`
    to the Journal screen level so BOTH the center chart and the rail bridge (Task 2) read it. (c) Rewrite `LifecycleSection`
    (center): REPLACE the trade-header Panel + 3-KPI grid with `<LifecycleMasthead snapshots={snapshots} trade={trade} />`
    (verdict headline + read + net-P&L stat — the KPI grid is retired per UI-SPEC). Keep the chart Panel + `RebuildButton`.
    Render `<LifecycleChart snapshots={snapshots} onCrosshairChange={setHoveredIndex} />` for `kind === "history" &&
    snapshots.length > 1`. Retire the tabs/scrubber entirely (they lived in the old LifecycleChart, now gone). (d) Honest
    states in the center: keep the `isPending` skeleton idiom verbatim (`bg-line opacity-40`, `aria-busy`); keep `PreHistoryStub`
    UNCHANGED for `kind === "entry-exit-only"`; for `kind === "history" && snapshots.length <= 1` render the NEW "too-new"
    copy — heading "Building the lifecycle." / sub "Check back after the next snapshot — captured every 30 minutes during RTH."
    (reuse the PreHistoryStub dashed-box treatment with this new copy variant); add a NEW error branch when `isError` — copy
    "Couldn't load this calendar's lifecycle." + a `Button` (existing `secondary` variant, `size="xs"`) labeled "Retry" wired
    to `refetch`. (e) Add the always-visible honest-caveats footer below the chart card: "Attribution is a 2nd-order
    approximation — the faint residual band is the unexplained part, never hidden." + "Line breaks are real feed gaps
    (spot=0 / NaN), drawn as gaps, never interpolated." DO NOT ship the mockup's "SKETCH · representative data" tag. Compute
    values in variables above the return (no IIFEs in JSX). NO `any`/`as`/`!`.
  </action>
  <acceptance_criteria>
    - source: the screen uses `useLifecycle` (not `useJournal`) and `snapshots` is typed as the enriched `LifecycleResponse["snapshots"]`.
    - source: a single `hoveredIndex` state is lifted to the Journal screen and passed as `onCrosshairChange={setHoveredIndex}` to `LifecycleChart`.
    - source: the center renders `LifecycleMasthead` in place of the old trade-header + 3-KPI grid; the tab/scrubber UI is gone.
    - source: the "too-new" branch renders "Building the lifecycle." copy; PreHistoryStub is unchanged; an error branch renders "Couldn't load this calendar's lifecycle." + a secondary xs "Retry" button wired to `refetch`.
    - source: the honest-caveats footer is present and the "SKETCH · representative data" string appears nowhere.
    - test: `bun run typecheck` clean for apps/web.
  </acceptance_criteria>
  <verify>
    <automated>bun run typecheck</automated>
    <human-check>At the phase gate (chrome-devtools UAT): selecting a history calendar shows the masthead + stacked chart; a too-new calendar shows "Building the lifecycle"; the caveats footer is visible; no SKETCH tag.</human-check>
  </verify>
  <done>The center column renders the masthead + rewritten chart from the enriched series, ships the too-new/error/loading states + honest footer, and lifts the shared hoveredIndex; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Right column — reactive rail cards + beats, relocate Notes, retire old cards</name>
  <files>apps/web/src/screens/Journal.tsx</files>
  <read_first>
    - apps/web/src/screens/Journal.tsx (the right column: SnapshotTable card + "Why it moved" callout lines ~560-590, and the Notes card / RuleTagChips block lines ~590-695 to relocate unchanged)
    - apps/web/src/components/PnlBridgeCard.tsx / EdgeCard.tsx / GreeksNowCard.tsx / BeatsCard.tsx (the rail components + their prop shapes — from Plan 22-04; PnlBridgeCard takes `{ snapshots, hoveredIndex }`, BeatsCard takes a `beats` array)
    - packages/core/src/journal/application/ports.ts (SnapshotRow.trigger enum "scheduled"|"event-move" — the source of event-move beats)
    - .planning/phases/22-journal-calendar-lifecycle-graph/22-UI-SPEC.md (Placement Decision right-column table: order PnlBridge → Edge → Greeks·now → Beats → relocated Notes; the Notes card is "shipped functionality, unchanged")
  </read_first>
  <action>
    In the Journal screen's RIGHT column: (a) RETIRE the snapshot-table card and the static "Why it moved" callout — the
    crosshair tooltip + masthead verdict now do that job (UI-SPEC). Remove the `SnapshotTable` usage; if `SnapshotTable`
    (and any `SnapshotTable`-specific test) becomes orphaned, delete it too (delete-if-orphaned, surgical) and remove its
    now-dead tests rather than leaving them red (tdd.md: fix/remove, never skip). (b) Add the rail cards top-to-bottom:
    `<PnlBridgeCard snapshots={snapshots} hoveredIndex={hoveredIndex} />` (reactive to the Task-1 shared state) →
    `<EdgeCard snapshots={snapshots} />` → `<GreeksNowCard snapshots={snapshots} />` → `<BeatsCard beats={beats} />`.
    (c) Build the `beats` array locally in the screen (BeatsCard stays presentational): entry beat from
    `selectedTrade.openedAt` (kind "entry"), one beat per snapshot where `snapshot.trigger === "event-move"` (kind "event",
    label from the move — e.g. date + the snapshot's context), and a close beat from `selectedTrade.closedAt` when the trade
    is closed (kind "close"). When no event-move snapshots exist, beats are just entry (+ close if closed) — never fabricate.
    (d) RELOCATE the existing Notes card (the RULE-01 `RuleTagChips` ENTER/EXIT/ROLL block + free-text textarea + its
    `useRuleTags`/`saveRuleTags`/`retryRuleTags` wiring) to the BOTTOM of the rail, visually and behaviorally UNCHANGED —
    do not redesign or restyle it; it is out of this phase's scope to modify (covered by 20-UI-SPEC). Keep computing values
    above the return; no IIFEs in JSX. NO `any`/`as`/`!`.
  </action>
  <acceptance_criteria>
    - source: the right column no longer renders `SnapshotTable` or the "Why it moved" callout; if orphaned, `SnapshotTable` and its dedicated tests are removed (not left red).
    - source: the rail renders `PnlBridgeCard` (passed the shared `hoveredIndex`) → `EdgeCard` → `GreeksNowCard` → `BeatsCard` in that order, above the relocated Notes card.
    - source: `beats` is derived locally (entry from openedAt, event beats from `trigger === "event-move"` snapshots, close from closedAt) and passed to `BeatsCard`; BeatsCard receives a `beats` prop (it does not read snapshots directly).
    - source: the RULE-01 Notes card (RuleTagChips + textarea + useRuleTags wiring) is present at the bottom of the rail, unchanged in behavior/styling.
    - test: `bun run typecheck` clean and `bun run test` green for apps/web (no orphaned red tests from the retired card).
  </acceptance_criteria>
  <verify>
    <automated>bun run typecheck && bun run test</automated>
    <human-check>At the phase gate (chrome-devtools UAT): hovering the hero chart updates the P&L bridge "as of {day}" totals across the rail; the rail shows edge / greeks-now / beats; the RULE-01 Notes card still works at the bottom.</human-check>
  </verify>
  <done>The rail renders the four reactive cards + relocated unchanged Notes card, beats are derived honestly, the retired snapshot table/callout are gone with no red tests, and the web suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated client → live lifecycle fetch | The screen fetches the JWT-gated route (22-03); an unauthenticated session gets no P&L (verified end-to-end at UAT). |
| gap/too-new/error series → screen render | The screen must render honest states (gap breaks, "Building the lifecycle", error+Retry), never a blank or fabricated view. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-14 | Information Disclosure | live fetch of /lifecycle | high | mitigate | The screen consumes the JWT-gated route from Plan 22-03 (auth by mount); an unauthenticated fetch returns 401 and the hook surfaces the error state — no P&L rendered. Verified end-to-end at the phase gate. |
| T-22-15 | Tampering (UI honesty) | empty/gap/error state rendering | medium | mitigate | Distinct honest states (PreHistoryStub, "Building the lifecycle", error+Retry, caveats footer, gap breaks) — never a blank/fabricated lifecycle; the SKETCH tag is not shipped. |
| T-22-SC | Tampering | npm/pip/cargo installs | low | accept | No new dependencies introduced this phase (22-RESEARCH Package Legitimacy Audit = "Not applicable"). |
</threat_model>

<verification>
- `bun run typecheck` clean for apps/web after each task.
- `bun run test` green for apps/web (retired-card tests removed, not skipped).
- `bun run lint` clean at wave merge.
- Phase-gate chrome-devtools UAT (the two VALIDATION manual-only rows): crosshair↔bridge sync, and line-break rendering at feed gaps across all panels.
</verification>

<success_criteria>
- The Journal screen renders the full JRNL-01 experience: left picker → center masthead + stacked chart → reactive rail, with honest states and the relocated Notes card.
- The crosshair-to-bridge sync and gap line-breaks are demonstrable at UAT.
- Closes ROADMAP success criteria 1–5 (integration point for the whole phase).
</success_criteria>

<output>
Create `.planning/phases/22-journal-calendar-lifecycle-graph/22-06-SUMMARY.md` when done.
</output>
