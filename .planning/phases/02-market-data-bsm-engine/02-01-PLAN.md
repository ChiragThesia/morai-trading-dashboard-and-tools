---
phase: 02-market-data-bsm-engine
plan: 01
type: execute
wave: 0
depends_on: []
autonomous: false
requirements: [MKT-01]
files_modified:
  - apps/worker/package.json
  - packages/adapters/package.json
  - apps/worker/vitest.config.ts
  - packages/adapters/test/fixtures/cboe-spx.fixture.json
  - packages/adapters/test/fixtures/cboe-spxw.fixture.json
  - packages/adapters/test/fixtures/README.md
user_setup: []
must_haves:
  truths:
    - "pg-boss is installed in apps/worker and importable"
    - "msw is installed in packages/adapters devDependencies and importable in tests"
    - "apps/worker has a vitest.config.ts so the root projects glob runs worker tests"
    - "A recorded CBOE _SPX.json payload exists as a committed fixture for msw tests"
    - "A recorded CBOE _SPXW.json payload exists (or its absence is documented) for msw tests"
  artifacts:
    - path: "apps/worker/vitest.config.ts"
      provides: "Worker test project config so apps/*/vitest.config.ts glob picks up worker tests"
      contains: "name"
    - path: "packages/adapters/test/fixtures/cboe-spx.fixture.json"
      provides: "Recorded CBOE SPX delayed-quotes payload for deterministic msw tests"
    - path: "packages/adapters/test/fixtures/README.md"
      provides: "Provenance note: when/how fixtures were captured + SPXW endpoint existence finding"
  key_links:
    - from: "vitest.config.ts (root)"
      to: "apps/worker/vitest.config.ts"
      via: "test.projects glob apps/*/vitest.config.ts"
      pattern: "apps/\\*/vitest.config.ts"
---

<objective>
Wave 0 tooling + validation foundation for Phase 2. Install the two new packages
(pg-boss for the worker, msw for adapter tests) behind a blocking package-legitimacy
checkpoint, scaffold the worker test project so worker handler tests can run, and record
a real CBOE delayed-quotes payload as a committed fixture for deterministic msw tests.
This also resolves RESEARCH Open Question #1 (does `_SPXW.json` exist as a separate
endpoint, or do SPXW contracts live inside `_SPX.json`?) via a one-time live smoke check.

Purpose: Every Phase 2 test depends on these (pg-boss, msw, worker vitest project, CBOE
fixture). Per VALIDATION.md, all test files are new this phase and these are the Wave 0
gaps that block all downstream TDD.
Output: pg-boss + msw installed, worker vitest config, two committed CBOE fixtures, a
provenance README recording the SPXW-endpoint finding.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-market-data-bsm-engine/02-SPEC.md
@.planning/phases/02-market-data-bsm-engine/02-RESEARCH.md
@.planning/phases/02-market-data-bsm-engine/02-VALIDATION.md
@.claude/rules/workflow.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy verification (pg-boss, msw)</name>
  <files>none (human verification gate — no files modified; precedes the Task 2 install)</files>
  <read_first>
    - .planning/phases/02-market-data-bsm-engine/02-RESEARCH.md (## Package Legitimacy Audit — both packages tagged [ASSUMED] because slopcheck was unavailable at research time)
  </read_first>
  <action>
    STOP and present the package-legitimacy gate to the human before any install. Both
    pg-boss and msw are [ASSUMED] in the RESEARCH Package Legitimacy Audit (slopcheck was
    unavailable). Per the planner package-legitimacy gate, the human must verify each
    package on npmjs.com (source repo, license, version, no suspicious postinstall) and
    type "approved" before Task 2 runs `bun add`. This is a blocking-human checkpoint and
    is never auto-approvable (workflow.auto_advance is ignored). Threat T-02-SC.
  </action>
  <what-built>
    Nothing automated yet — this checkpoint precedes installation. RESEARCH.md flagged
    both new packages as [ASSUMED] for slopcheck (slopcheck was unavailable when research
    ran), so per the planner package-legitimacy gate they require a blocking human
    verification before install. Threat T-02-SC.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/pg-boss — confirm: source repo github.com/timgit/pg-boss,
       MIT license, latest version is 12.x (research cited 12.18.3), long publish history (created 2016).
    2. Open https://www.npmjs.com/package/msw — confirm: source repo github.com/mswjs/msw,
       MIT license, latest 2.x (research cited 2.14.6), maintained (created 2018).
    3. Confirm neither package has a suspicious postinstall script on its npm page.
  </how-to-verify>
  <verify>
    <human-check>Human approves package legitimacy for pg-boss + msw before install</human-check>
  </verify>
  <acceptance_criteria>
    - Human confirms pg-boss resolves to github.com/timgit/pg-boss with a 12.x version
    - Human confirms msw resolves to github.com/mswjs/msw with a 2.x version
    - Human types "approved" (or names a substitute/blocker) before any install runs
  </acceptance_criteria>
  <done>Human approved both packages on npmjs.com; install may proceed.</done>
  <resume-signal>Type "approved" to proceed with install, or describe a concern</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install pg-boss + msw and scaffold worker vitest project</name>
  <files>apps/worker/package.json, packages/adapters/package.json, apps/worker/vitest.config.ts</files>
  <read_first>
    - apps/worker/package.json (current deps — has @morai/* + zod; no pg-boss)
    - packages/adapters/package.json (current devDeps — has testcontainers; no msw)
    - packages/adapters/vitest.config.ts (the per-package vitest config shape to mirror for the worker)
    - packages/core/vitest.config.ts (minimal alias + globals:false shape)
    - vitest.config.ts (root — confirms projects glob includes apps/*/vitest.config.ts)
    - apps/worker/tsconfig.json (project references; pg-boss is a runtime dep of the worker app only)
  </read_first>
  <action>
    Add pg-boss at ^12.18.3 to apps/worker/package.json dependencies (run `bun add pg-boss`
    in apps/worker). Add msw at ^2.14.6 to packages/adapters/package.json devDependencies
    (run `bun add -d msw` in packages/adapters). pg-boss ships its own types — do NOT add
    @types/pg-boss. Then create apps/worker/vitest.config.ts mirroring
    packages/adapters/vitest.config.ts: defineConfig with resolve.alias mapping @morai/core,
    @morai/adapters, and @morai/shared to their src/index.ts via fileURLToPath(new URL(...)),
    and test block with name "apps/worker" and globals:false. Do NOT set globalSetup for the
    worker (handler tests use in-memory adapters, no Docker). Run `bun install` at root to
    relink the workspace. This task is pure tooling/wiring — tdd.md exempts composition-root
    and static-config wiring, so no failing test is required to add config + deps.
  </action>
  <verify>
    <automated>cd apps/worker && bun -e "import('pg-boss').then(()=>console.log('pgboss-ok'))" && cd ../../packages/adapters && bun -e "import('msw/node').then(()=>console.log('msw-ok'))"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/worker/package.json` dependencies includes a "pg-boss" entry at ^12.x
    - `packages/adapters/package.json` devDependencies includes an "msw" entry at ^2.x
    - `bun -e "import('pg-boss')"` from apps/worker prints "pgboss-ok" with exit 0
    - `bun -e "import('msw/node')"` from packages/adapters prints "msw-ok" with exit 0
    - `apps/worker/vitest.config.ts` exists, exports a defineConfig with test.name === "apps/worker"
    - No @types/pg-boss dependency was added
  </acceptance_criteria>
  <done>pg-boss + msw installed and importable; worker vitest project resolves under the root projects glob.</done>
</task>

<task type="auto">
  <name>Task 3: Live CBOE smoke check + record fixtures (resolves SPXW endpoint question)</name>
  <files>packages/adapters/test/fixtures/cboe-spx.fixture.json, packages/adapters/test/fixtures/cboe-spxw.fixture.json, packages/adapters/test/fixtures/README.md</files>
  <read_first>
    - .planning/phases/02-market-data-bsm-engine/02-RESEARCH.md (## CBOE Adapter Details — endpoint URLs; ## Open Questions #1 SPXW endpoint; Pattern 1 response shape)
    - .planning/phases/02-market-data-bsm-engine/02-VALIDATION.md (## Wave 0 Requirements — live smoke + recorded fixture)
  </read_first>
  <action>
    Fetch the live CBOE delayed-quotes endpoints once:
    https://cdn.cboe.com/api/global/delayed_quotes/options/_SPX.json and
    https://cdn.cboe.com/api/global/delayed_quotes/options/_SPXW.json (no auth, public CDN;
    send a User-Agent header). Determine the SPXW finding: (a) if `_SPXW.json` returns a
    valid payload with SPXW-root options, both endpoints exist; (b) if it 404s, SPXW
    contracts must be filtered out of `_SPX.json` by OSI root — record which. Write trimmed
    fixtures to packages/adapters/test/fixtures/cboe-spx.fixture.json and cboe-spxw.fixture.json:
    keep the top-level shape (timestamp, data.current_price, data.close, data.prev_day_close)
    and a representative slice of data.options (~30-60 contracts spanning near-money strikes
    and several expirations, including at least one near-expiry and one >90 DTE contract so
    the persistence filter can be exercised; include contracts both inside and outside the
    ±10% strike band). Strip the payload down — do NOT commit the full ~20k-contract file.
    Write packages/adapters/test/fixtures/README.md recording: capture date, the exact URLs,
    the SPXW-endpoint finding (a or b above), and the observed `timestamp` field format
    (RESEARCH Pitfall 1 — likely ET local time without offset). If the live endpoint is
    unreachable from this environment, construct a synthetic fixture matching RESEARCH
    Pattern 1 schema exactly and note in the README that it is synthetic + flag the SPXW
    question as still-open for the Plan 04 executor to confirm.
  </action>
  <verify>
    <automated>cd packages/adapters && bun -e "const f=require('./test/fixtures/cboe-spx.fixture.json'); if(!f.timestamp||!Array.isArray(f.data.options)||f.data.options.length<20) throw new Error('bad fixture'); console.log('fixture-ok', f.data.options.length)"</automated>
  </verify>
  <acceptance_criteria>
    - packages/adapters/test/fixtures/cboe-spx.fixture.json parses as JSON with a top-level `timestamp` string and `data.options` array of >= 20 entries
    - Each option entry has an `option` field that is an OSI string (e.g. matches /^SPXW?\d{6}[CP]\d{8}$/)
    - At least one option in the SPX fixture has strike outside ±10% of data.current_price (so the filter test has a negative case)
    - packages/adapters/test/fixtures/README.md states the SPXW-endpoint finding (separate _SPXW.json exists, OR SPXW is inside _SPX.json, OR synthetic+unconfirmed) and the observed timestamp format
    - cboe-spx.fixture.json file size is < 100 KB (trimmed, not the full chain)
  </acceptance_criteria>
  <done>Deterministic CBOE fixtures committed; SPXW endpoint question resolved and recorded for Plan 04; fixture exercises both in-filter and out-of-filter contracts.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

New files:
- `apps/worker/vitest.config.ts` — worker test project (defineConfig, test.name "apps/worker", aliases for @morai/core, @morai/adapters, @morai/shared)
- `packages/adapters/test/fixtures/cboe-spx.fixture.json` — trimmed recorded CBOE SPX payload
- `packages/adapters/test/fixtures/cboe-spxw.fixture.json` — trimmed recorded CBOE SPXW payload (or documented-synthetic)
- `packages/adapters/test/fixtures/README.md` — fixture provenance + SPXW finding + timestamp format

New dependencies:
- `pg-boss@^12.18.3` (apps/worker dependencies)
- `msw@^2.14.6` (packages/adapters devDependencies)

No new symbols, ports, schemas, or job names (this is tooling + fixtures only).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → repo | New package installs (pg-boss, msw) introduce third-party code |
| CBOE CDN → repo | Recorded fixture is captured untrusted external data, but committed static |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | pg-boss + msw installs | mitigate | Blocking-human legitimacy checkpoint (Task 1) verifies npmjs.com source repo + version before install; both [ASSUMED] per RESEARCH audit |
| T-02-01 | Tampering | recorded CBOE fixture | accept | Fixture is committed static test data, never executed; trimmed and human-reviewed; no secrets |
| T-02-02 | Info Disclosure | fixture README / capture | accept | CBOE delayed quotes are public no-auth data; no credentials involved |
</threat_model>

<verification>
- `bun install` succeeds from clean state
- `bun -e "import('pg-boss')"` (worker) and `import('msw/node')` (adapters) both succeed
- `bunx vitest run` discovers an "apps/worker" project (even with zero tests it must resolve without error)
- Fixtures parse and satisfy the OSI-shape + filter-coverage assertions
</verification>

<success_criteria>
- pg-boss installed in worker, msw in adapters devDeps, both importable
- apps/worker/vitest.config.ts present and discovered by the root projects glob
- Two CBOE fixtures + provenance README committed; SPXW endpoint question resolved
- Package legitimacy checkpoint approved before install
</success_criteria>

<output>
Create `.planning/phases/02-market-data-bsm-engine/02-01-SUMMARY.md` when done.
Record the SPXW-endpoint finding prominently — Plan 04 depends on it.
</output>
