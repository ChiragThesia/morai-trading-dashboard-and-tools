---
phase: 36-analyzer-journal-mobile-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/charts/MobileChartControls.tsx
  - apps/web/src/screens/overview-mobile/MobileRiskPanel.tsx
  - apps/web/src/screens/analyzer-mobile/useAnalyzerModel.ts
  - apps/web/src/screens/analyzer-mobile/AnalyzerMobile.tsx
  - apps/web/src/screens/Analyzer.tsx
  - apps/web/src/screens/Analyzer.test.tsx
autonomous: true
requirements: [MOBILE-11, MOBILE-13]
must_haves:
  truths:
    - "At <1024px (jsdom default) Analyzer renders the mobile tree root; the desktop analyzer-inner-grid / scoring chips / PayoffControls do not mount"
    - "At >=1024px (matchMedia stub) Analyzer renders today's desktop tree with identical structure — rail Panel, scoring chips, Risk profile Panel, right column all present"
    - "MobileRiskPanel.test.tsx passes with ZERO test edits after the MobileChartControls extraction — the D-05 byte-identity guard"
    - "Every pre-existing Analyzer desktop test passes under the matchMedia stub — the per-screen byte-identity guard (D-16)"
  artifacts:
    - "apps/web/src/components/charts/MobileChartControls.tsx — shared slim chrome row + Projection dialog + toggles dialog (extracted verbatim from MobileRiskPanel)"
    - "apps/web/src/screens/analyzer-mobile/useAnalyzerModel.ts — shared model hook + exported constants/helpers"
    - "apps/web/src/screens/analyzer-mobile/AnalyzerMobile.tsx — mobile tree root (skeleton this plan)"
    - "apps/web/src/screens/Analyzer.tsx — thin useIsDesktop switch + AnalyzerDesktop (renamed in-file)"
  key_links:
    - "Analyzer() -> useIsDesktop() -> AnalyzerDesktop | AnalyzerMobile (D-01: only one tree mounts)"
    - "Both trees -> useAnalyzerModel() (D-02: logic never duplicated)"
    - "MobileRiskPanel -> MobileChartControls (D-05: thin consumer, rendered DOM byte-identical)"
---

<objective>
Land the structural foundation for the dedicated mobile Analyzer tree: extract the shared
mobile chart chrome out of MobileRiskPanel into `MobileChartControls` (D-05), extract all
Analyzer state/derivation into `useAnalyzerModel()` (D-02), turn `Analyzer` into a
`useIsDesktop()` switch (D-01), and migrate the existing Analyzer desktop tests to the
matchMedia stub in the same change that lands the switch (D-16). This is the
byte-identity-guard plan for the Analyzer half of Phase 36 — desktop regression
protection lands WITH the switch, not after.

Purpose: everything in plan 36-02 hangs off this branch + model + shared chrome. Desktop
>=1024px must be provably unchanged, and 35.1's phone-checked Overview chart row must be
provably untouched, before any mobile Analyzer UI is built.
Output: MobileChartControls extraction + switch + model hook + skeleton mobile root,
full suite green.

Closes validation claims (36-VALIDATION.md): J1, J2, J8 (extraction half — the
Analyzer-mounts-same-testids half lands in 36-02), plus the desktop-call-site half of J9.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-analyzer-journal-mobile-redesign-same-dedicated-mobile-tree-/36-CONTEXT.md
@.planning/phases/36-analyzer-journal-mobile-redesign-same-dedicated-mobile-tree-/36-UI-SPEC.md
@.planning/phases/36-analyzer-journal-mobile-redesign-same-dedicated-mobile-tree-/36-VALIDATION.md
@apps/web/src/screens/Analyzer.tsx
@apps/web/src/screens/Analyzer.test.tsx
@apps/web/src/screens/overview-mobile/MobileRiskPanel.tsx
@apps/web/src/screens/overview-mobile/MobileRiskPanel.test.tsx
@apps/web/src/screens/Overview.tsx
@apps/web/src/screens/Overview.test.tsx
@apps/web/src/hooks/useIsDesktop.ts
@apps/web/src/hooks/usePayoffDateControl.ts
</context>

<repo_law>
- TDD red→green per .claude/rules/tdd.md: write the failing test, RUN it, show the
  failure output (fails on the assertion, not an import error), then implement, RUN
  green, show output. Commit only at green — test + implementation land together in a
  single conventional commit so the red→green pairing is visible in the diff.
  Extractions (Tasks 1-2) are the sanctioned refactor exception: green on both sides,
  guarded by UNMODIFIED existing suites.
- Commit with EXPLICIT paths only (the repo has unrelated dirty files). Never `git add -A`
  or `git add .`.
- No `any`, no `as` (except `as const`), no `!`. Match existing file style exactly.
- Copy strings/classes/props VERBATIM from 36-UI-SPEC.md — never paraphrase a design
  decision. Cite the D-ID in code comments where the spec does.
- NEVER read, edit, or `git add` `apps/web/src/screens/Analyzer 2.tsx` — stray file.
</repo_law>

<tasks>

<task type="auto">
  <name>Task 1: Extract MobileChartControls from MobileRiskPanel — shared chrome, not a fork (D-05)</name>
  <files>apps/web/src/components/charts/MobileChartControls.tsx, apps/web/src/screens/overview-mobile/MobileRiskPanel.tsx</files>
  <action>
    Pure refactor — no RED step; the guard is the existing MobileRiskPanel.test.tsx
    passing UNMODIFIED (per .claude/rules/tdd.md refactor rule and UI-SPEC D-05: "that IS
    the guard").

    Create `apps/web/src/components/charts/MobileChartControls.tsx` (desktop sibling:
    PayoffControls.tsx). Props exactly per UI-SPEC §3:
    `{ dateControl: PayoffDateControl; bounds: { readonly minIso: string; readonly maxIso: string }; toggles: PayoffChartToggles; onToggle: (key: keyof PayoffChartToggles) => void }`.

    Move VERBATIM out of MobileRiskPanel.tsx into the new component:
    - The entire control-row block: the outer `flex items-center gap-1 px-4` div with the
      ghost `‹`/`›` steppers (aria-labels `Previous day`/`Next day`), the date-pill
      DialogTrigger (testid `date-pill`, aria-label `Projection date`, projected violet
      ring class), the full Projection DialogContent (Today/quick-jump chips/Expiry,
      `date-readout`, `date-slider`, `date-picker-input`), and the `ml-auto` `⋯` trigger
      (aria-label `More chart options`) with the Chart DialogContent's four full-width
      toggle buttons (@ exp/Fan/Walls/Profit zone with active/aria-pressed from toggles).
    - The module constants QUICK_JUMPS and DIALOG_TITLE_CLASS, and the pill/offset
      derivation (todayLocal/maxLocal/maxDays via parseLocalDateInput/daysBetween,
      isoAtOffset via toDateInputValue, offset/projected/pillDate/pillLabel — catch #22:
      all date math stays LOCAL-constructed, move it untouched).
    - The imports these need (cn, Button, buttonClass, Dialog primitives, PayoffDateControl
      type, PayoffChartToggles type, date-projection helpers).

    Rework MobileRiskPanel.tsx into a thin consumer: replace the moved block with
    `<MobileChartControls dateControl={dateControl} bounds={bounds} toggles={toggles} onToggle={onToggle} />`
    and drop the now-unused imports/constants. Everything else stays: the section wrapper,
    the `mobile-payoff` chart block with its existing PayoffChart props, and the
    Overview-specific `mobile-freshness` caption (D-05: the freshness caption stays in
    MobileRiskPanel). The rendered DOM must be byte-identical — same elements, classes,
    testids, aria-labels, order.

    Do NOT touch PayoffChart.tsx, PayoffControls.tsx, useOverviewModel.ts, or
    OverviewMobile.tsx.
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/screens/overview-mobile/MobileRiskPanel.test.tsx && git diff --quiet -- apps/web/src/screens/overview-mobile/MobileRiskPanel.test.tsx && bunx vitest run apps/web/src/screens/Overview.test.tsx && bun run typecheck && ! rg -q 'QUICK_JUMPS' apps/web/src/screens/overview-mobile/MobileRiskPanel.tsx</automated>
  </verify>
  <done>
    MobileRiskPanel.test.tsx green with ZERO test-file edits (J8 extraction guard);
    Overview.test.tsx green (Overview mobile tree mounts MobileRiskPanel); typecheck
    clean; the moved constants no longer live in MobileRiskPanel.tsx.
    Commit: `refactor(36-01): extract MobileChartControls shared chrome from MobileRiskPanel (D-05)`
  </done>
</task>

<!-- planner-discipline-allow: QUICK_JUMPS -->

<task type="auto">
  <name>Task 2: Extract useAnalyzerModel — behavior-preserving refactor (D-02)</name>
  <files>apps/web/src/screens/analyzer-mobile/useAnalyzerModel.ts, apps/web/src/screens/Analyzer.tsx</files>
  <action>
    Pure refactor — no RED step; the guard is the existing Analyzer.test.tsx passing
    UNMODIFIED (green on both sides).

    Create `apps/web/src/screens/analyzer-mobile/useAnalyzerModel.ts` (the
    useOverviewModel precedent):
    - Move from Analyzer.tsx into this file and EXPORT (both trees consume, never
      re-implemented — D-02): the constants PASTED_NOT_SCORED_NOTE, PASTE_ERROR_COPY,
      CHIP_LABELS, EXPERIMENTAL_SHORT, FALLBACK_SCORE_ITEMS, TODAY_CURVE_COLOR,
      EXPIRATION_CURVE_COLOR, and the helper scoreStatus. DEFAULT_RATE/DEFAULT_DIV and
      PASTED_ID_PREFIX move as module-private (only the hook uses them).
    - Export `useAnalyzerModel()` containing ALL non-trivial state/derivation currently
      inline in `Analyzer()`: the usePicker query (expose snapshot AND the raw
      loading/error slice — the trees need the exact `isPending && data === undefined`
      loading gate, so expose e.g. `isLoading` computed that way plus isError/refetch),
      sortedCandidates, spot, selection state (selectedId/selected/handleSelect),
      combinedIds/handleToggleCombine, pasted-candidates state + pastedSeq + pasteText/
      setPasteText + pasteError + handlePasteAnalyze/handleRemovePasted/
      handleClearAllPasted (the useAnalyzeCalendar mutation moves inside the hook),
      railCandidates, copiedId/handleCopyCandidate, selectedPosition, today/bounds/
      dateControl, toggles/handleToggle, params, bookCandidates/combinedPositions/
      payoffDomain/scenarioResult, bookCount/bookDebit/bookTheta/bookVega,
      positionSetSignature, and `repull` (the useRepullChains result — the trees each
      build their own control JSX from it). Return shape per UI-SPEC §2 (names
      indicative — keep them close to the spec sketch so 36-02 wires slices by name).
      Follow existing typing style: explicit return type interface, readonly fields,
      no any/as/!.
    - Move the relevant imports (usePicker, useRepullChains, useAnalyzeCalendar,
      parseTosOrder, parsedCalendarToPickerCandidate, candidateToAnalyzerPosition,
      buildTosCalendarOrder, repriceScenario, computePayoffDomain,
      computeProjectionBounds, usePayoffDateControl, contracts types).

    Rework Analyzer.tsx:
    - `Analyzer()` body starts with `const m = useAnalyzerModel();` and the JSX consumes
      model fields. JSX output must be IDENTICAL — same elements, classes, testids,
      order. CandidateRail, ScoringMethodologyPanel, and RightColumn component
      definitions stay in Analyzer.tsx untouched (desktop view code); they import
      scoreStatus/CHIP_LABELS/FALLBACK_SCORE_ITEMS/EXPERIMENTAL_SHORT/
      PASTED_NOT_SCORED_NOTE from the model module now. The repullControl JSX and the
      five-state railBody precedence stay in Analyzer() as desktop view code, built off
      the model slices.
    - Keep the existing `export function Analyzer` and `export function CandidateRail`
      names/signatures — Analyzer.test.tsx imports both from "./Analyzer.tsx".
    - Do NOT add the useIsDesktop branch yet — this task is extraction only; Analyzer
      still renders the (desktop) tree unconditionally so the existing suite passes
      without any matchMedia stub.
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/screens/Analyzer.test.tsx && git diff --quiet -- apps/web/src/screens/Analyzer.test.tsx && bun run typecheck</automated>
  </verify>
  <done>
    Analyzer.test.tsx passes with ZERO test-file edits (behavior-preservation proof);
    typecheck clean.
    Commit: `refactor(36-01): extract useAnalyzerModel shared model hook (D-02)`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Analyzer useIsDesktop switch + migrate desktop tests to matchMedia stub (D-01/D-16)</name>
  <files>apps/web/src/screens/Analyzer.tsx, apps/web/src/screens/analyzer-mobile/AnalyzerMobile.tsx, apps/web/src/screens/Analyzer.test.tsx</files>
  <behavior>
    New describe "Analyzer branch — D-01/D-16 (36)" in Analyzer.test.tsx, RED first:
    - Test 1 (J1): default jsdom render (NO matchMedia stub) →
      `getByTestId("analyzer-mobile-root")` present; `queryByTestId("analyzer-inner-grid")`,
      `queryByTestId("scoring-pills")`, and `queryByText("Suggested calendars")` all null
      (the desktop tree does not mount).
    - Test 2 (J2 byte-identity guard): with the desktop matchMedia stub →
      `analyzer-inner-grid` present, `analyzer-mobile-root` absent, and the desktop
      structural content present: `Suggested calendars` heading, `scoring-pills`,
      `copy-tos-order` button, the three right-column headings (`Why this calendar`,
      `Term structure + your legs`, `Entry / exit plan`).
    - Test 3 (desktop half of J9): under the stub, `latestPayoffChartProps()` reports
      the two 35.1 mobile chart props (`showBePills`, `aspectRatio`) undefined — the
      desktop call site passes neither.
  </behavior>
  <action>
    GREEN, all in ONE commit with the test migration (a split commit would leave the
    suite red — forbidden):
    1. Create `apps/web/src/screens/analyzer-mobile/AnalyzerMobile.tsx` — skeleton: calls
       `useAnalyzerModel()` and renders the mobile root per UI-SPEC §4:
       `<div data-testid="analyzer-mobile-root" className="flex flex-col gap-6 pb-10 pt-4">`
       with a body comment noting sections land in plan 36-02. No horizontal padding on
       the root (sections own px-4 later). It must reference the model result so the
       hook wiring is live from day one.
    2. Analyzer.tsx: rename today's component body to `function AnalyzerDesktop()`
       (module-private, same file, JSX untouched), and make the exported `Analyzer` the
       thin switch VERBATIM from UI-SPEC §1:
       `const isDesktop = useIsDesktop(); return isDesktop ? <AnalyzerDesktop /> : <AnalyzerMobile />;`
       Import useIsDesktop from ../hooks/useIsDesktop.ts and AnalyzerMobile from
       ./analyzer-mobile/AnalyzerMobile.tsx. `CandidateRail` stays exported.
    3. Migrate the existing Analyzer.test.tsx desktop tests to the stub (D-16, expected
       churn not regression): add ONE shared helper `stubDesktopMatchMedia()` copying the
       exact Object.defineProperty pattern from Overview.test.tsx (matches ===
       `query === "(min-width: 1024px)"`), install it in `beforeEach` of every
       pre-existing describe block that renders `<Analyzer />` (ranked rail, scoring
       checklist, payoff center, right column, pasted calendars, copy TOS order, payoff
       controls, live-data states, rule-registry checklist, and the 35-05 mobile stack
       order describe — its lg:-gated class assertions still hold on the desktop tree
       pre-D-17), with matching afterEach `Reflect.deleteProperty(window, "matchMedia")`.
       Describes that render `<CandidateRail />` directly need no stub (component-level,
       no switch). Do not weaken or delete any pre-existing assertion — only wrap them
       in the stubbed environment.
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/screens/Analyzer.test.tsx && bun run test && bun run typecheck && bun run lint</automated>
  </verify>
  <done>
    RED output shown for the 3 new tests, then the FULL workspace suite green, typecheck
    + lint clean. Every pre-existing Analyzer desktop assertion passes under the stub.
    Commit: `feat(36-01): Analyzer useIsDesktop switch + desktop-test matchMedia migration (D-01/D-16)`
  </done>
</task>

</tasks>

<!-- planner-discipline-allow: showBePills -->
<!-- planner-discipline-allow: aspectRatio -->

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Presentation-only refactor + branch; paste flow reuses the existing tos-parser path unchanged; no new network surface, zero new dependencies |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-01 | DoS | Analyzer branch mounting BOTH trees (double usePicker/model consumers) | low | mitigate | D-01 single-branch switch (one tree mounts); J1/J2 assert the other tree is absent from the DOM |
| T-36-02 | Tampering | Overview mobile chart chrome silently altered by the D-05 extraction | low | mitigate | MobileRiskPanel.test.tsx must pass with zero edits (Task 1 verify includes `git diff --quiet` on the test file) |
| T-36-SC | Tampering | Supply chain (npm installs) | low | accept | Zero new dependencies this phase (locked constraint) — no install tasks exist |
</threat_model>

<verification>
- `bun run test` full workspace green, `bun run typecheck`, `bun run lint` clean.
- Desktop tripwire: J2 structural guard green under the stub; MobileRiskPanel.test.tsx
  and Overview.test.tsx green unmodified (35.1's phone-checked Overview state preserved).
- Rollback note: all three commits are additive/refactor on 6 files; `git revert` of the
  Task 3 commit alone restores the unconditional desktop Analyzer render (the switch is
  a single-file seam) — no data or migration surface.
</verification>

<success_criteria>
- J1, J2, J8 (extraction half) demonstrably green via the commands above.
- Analyzer.tsx exports the same public symbols as before (Analyzer, CandidateRail).
- useAnalyzerModel is the ONLY place Analyzer state/derivation lives; AnalyzerDesktop
  and AnalyzerMobile both call it.
- MobileChartControls is the single home of the slim chrome row + both dialogs;
  MobileRiskPanel consumes it with byte-identical DOM.
</success_criteria>

<output>
Create `.planning/phases/36-analyzer-journal-mobile-redesign-same-dedicated-mobile-tree-/36-01-SUMMARY.md` when done (include the RED run output for Task 3).
</output>
