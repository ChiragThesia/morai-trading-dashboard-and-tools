---
phase: 36-analyzer-journal-mobile-redesign
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/screens/journal-mobile/useJournalModel.tsx
  - apps/web/src/screens/journal-mobile/JournalMobile.tsx
  - apps/web/src/screens/Journal.tsx
  - apps/web/src/screens/Journal.test.tsx
autonomous: true
requirements: [MOBILE-12, MOBILE-13]
must_haves:
  truths:
    - "At <1024px (jsdom default) Journal renders the mobile tree root; the desktop journal-positions grid does not mount"
    - "At >=1024px (matchMedia stub) Journal renders today's desktop tree with identical structure — Trades panel, TradeRows, RebuildButton in the heading row, always-visible footnotes, full rail"
    - "Every pre-existing Journal desktop test passes under the matchMedia stub — the per-screen byte-identity guard (D-16)"
    - "JournalContainer and the TradeSummary prop contract are untouched (D-03)"
  artifacts:
    - "apps/web/src/screens/journal-mobile/useJournalModel.tsx — shared model hook + exported helpers/components (fmtDate/fmtPnl/fmtSnapTime/tagLabel/RULE_TAG_LABELS, HeadingPill, RuleTagChips, DashedStub/PreHistoryStub/BuildingLifecycleStub)"
    - "apps/web/src/screens/journal-mobile/JournalMobile.tsx — mobile tree root (skeleton this plan)"
    - "apps/web/src/screens/Journal.tsx — thin useIsDesktop switch + JournalDesktop (renamed in-file)"
  key_links:
    - "Journal({trades}) -> useIsDesktop() -> JournalDesktop | JournalMobile (D-03: only one tree mounts, one useLifecycle consumer)"
    - "Both trees -> useJournalModel(trades) (D-04: logic never duplicated)"
    - "Journal.tsx re-exports TradeSummary so JournalContainer's import keeps resolving"
---

<objective>
Land the structural foundation for the dedicated mobile Journal tree: extract all Journal
state/derivation + shared helpers into `useJournalModel(trades)` (D-04), turn `Journal`
into a `useIsDesktop()` switch (D-03), and migrate the existing Journal desktop tests to
the matchMedia stub in the same change that lands the switch (D-16). This is the
byte-identity-guard plan for the Journal half of Phase 36.

Purpose: everything in plan 36-04 hangs off this branch + model. Desktop >=1024px must
be provably unchanged before any mobile Journal UI is built.
Output: switch + model hook + skeleton mobile root, full suite green.

Closes validation claims (36-VALIDATION.md): J3.

Parallelism note: this plan shares ZERO files with 36-01 — same wave, independent
executor. Each screen's test file gets its own local `stubDesktopMatchMedia` helper
(the established per-file pattern from Overview.test.tsx/MarketRail.test.tsx); there is
no shared test-helper module to conflict over.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-analyzer-journal-mobile-redesign-same-dedicated-mobile-tree-/36-CONTEXT.md
@.planning/phases/36-analyzer-journal-mobile-redesign-same-dedicated-mobile-tree-/36-UI-SPEC.md
@.planning/phases/36-analyzer-journal-mobile-redesign-same-dedicated-mobile-tree-/36-VALIDATION.md
@apps/web/src/screens/Journal.tsx
@apps/web/src/screens/Journal.test.tsx
@apps/web/src/screens/JournalContainer.tsx
@apps/web/src/screens/Overview.tsx
@apps/web/src/screens/Overview.test.tsx
@apps/web/src/hooks/useIsDesktop.ts
</context>

<repo_law>
- TDD red→green per .claude/rules/tdd.md: failing test RUN and shown first, then
  implement, then green run shown; test + implementation in one conventional commit.
  Task 1 is the sanctioned refactor exception: green on both sides, guarded by the
  UNMODIFIED existing suite.
- Commit with EXPLICIT paths only. Never `git add -A` or `git add .`.
- No `any`, no `as` (except `as const`), no `!`. Match existing file style exactly.
- Copy strings/classes VERBATIM from 36-UI-SPEC.md — never paraphrase. Cite D-IDs.
</repo_law>

<tasks>

<task type="auto">
  <name>Task 1: Extract useJournalModel + shared helpers — behavior-preserving refactor (D-04)</name>
  <files>apps/web/src/screens/journal-mobile/useJournalModel.tsx, apps/web/src/screens/Journal.tsx</files>
  <action>
    Pure refactor — no RED step; the guard is the existing Journal.test.tsx passing
    UNMODIFIED (green on both sides). Journal.test.tsx module-mocks
    ../hooks/useLifecycle.ts and ../hooks/useRuleTags.ts by resolved module id, so
    moving those hook CALLS into the model file keeps the mocks binding — no test edits.

    Create `apps/web/src/screens/journal-mobile/useJournalModel.tsx` (.tsx because the
    shared view helpers below carry JSX — the one deliberate deviation from the
    useOverviewModel .ts precedent, noted in the file header):
    - Move from Journal.tsx and EXPORT (single source, D-04 — Journal.tsx re-imports;
      never re-implemented): fmtDate, fmtPnl, fmtSnapTime, tagLabel, RULE_TAG_LABELS,
      ENTER_OPTIONS/EXIT_OPTIONS/ROLL_OPTIONS, HeadingPill, RuleTagChips, DashedStub,
      PreHistoryStub, BuildingLifecycleStub, and the `TradeSummary` interface.
    - Export `useJournalModel(trades: ReadonlyArray<TradeSummary>)` containing ALL
      non-trivial state/derivation currently inline in `Journal()`: openTrades/
      closedTrades split, selectedId/setSelectedId (same default-selection expression),
      historyOverride state + the derived historyOpen (auto-open when zero open trades)
      + a setter the trees call on toggle, selectedTrade, hoveredIndex/setHoveredIndex,
      the useLifecycle slice (snapshots, isPending, isError, refetch), the useRuleTags
      slice (ruleEvents, rulesPending, ruleErrors, saveRuleTags, retryRuleTags),
      openEvent/closeEvent/rollEvents, selectedTradeTagLabels, and beats. Explicit
      return-type interface, readonly fields, no any/as/!.

    Rework Journal.tsx:
    - `Journal({trades})` body starts with `const m = useJournalModel(trades);` and the
      JSX consumes model fields. JSX output must be IDENTICAL — same elements, classes,
      testids, order. TradeRow and LifecycleSection component definitions stay in
      Journal.tsx untouched (desktop view code), importing the moved helpers/stubs from
      the model file.
    - Preserve the public contract: `export { Journal }` unchanged and re-export the
      moved type — `export type { TradeSummary } from "./journal-mobile/useJournalModel.tsx";`
      — so JournalContainer's import keeps resolving (D-03: JournalContainer untouched).
    - Do NOT add the useIsDesktop branch yet — extraction only; Journal still renders
      the (desktop) tree unconditionally so the existing suite passes without a stub.
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/screens/Journal.test.tsx && git diff --quiet -- apps/web/src/screens/Journal.test.tsx && bunx vitest run apps/web/src/screens/JournalContainer.test.tsx && bun run typecheck</automated>
  </verify>
  <done>
    Journal.test.tsx passes with ZERO test-file edits (behavior-preservation proof);
    JournalContainer.test.tsx green (TradeSummary contract intact); typecheck clean.
    Commit: `refactor(36-03): extract useJournalModel shared model hook + helpers (D-04)`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Journal useIsDesktop switch + migrate desktop tests to matchMedia stub (D-03/D-16)</name>
  <files>apps/web/src/screens/Journal.tsx, apps/web/src/screens/journal-mobile/JournalMobile.tsx, apps/web/src/screens/Journal.test.tsx</files>
  <behavior>
    New describe "Journal branch — D-03/D-16 (36)" in Journal.test.tsx, RED first
    (use the file's existing renderJournal/mock harness):
    - Test 1 (J3 mobile): default jsdom render with [makeHistoryTrade()] (NO stub) →
      `getByTestId("journal-mobile-root")` present; `queryByTestId("journal-positions")`
      null; the desktop `history` badge chip and the always-visible footnote line
      (`Attribution is a 2nd-order approximation…`) absent.
    - Test 2 (J3 byte-identity guard): with the desktop matchMedia stub →
      `journal-positions` present with its three column testids
      (journal-trades-column / journal-lifecycle-column / journal-rail-column),
      `journal-mobile-root` absent, the `Rebuild journal for …` button visible in the
      heading row, and the Trades panel heading present — the desktop keeps ALL of
      today's affordances (OPEN badge + status text + history chip on rows).
    - Test 3 (empty-state parity): default jsdom render with `[]` → the two verbatim
      empty-state lines render (`No journal history yet.` / `Trades before Jun 12 have
      entry/exit only.`) from the mobile tree.
  </behavior>
  <action>
    GREEN, all in ONE commit with the test migration (a split commit would leave the
    suite red — forbidden):
    1. Create `apps/web/src/screens/journal-mobile/JournalMobile.tsx` — skeleton: takes
       `{trades}`, calls `useJournalModel(trades)`, and renders per UI-SPEC §6: the
       empty state (`trades.length === 0`) as the two existing lines verbatim, centered
       (reuse the desktop empty-state classes); otherwise
       `<div data-testid="journal-mobile-root" className="flex flex-col gap-6 pb-10 pt-4">`
       with a body comment noting sections land in plan 36-04. It must reference the
       model result so the single-lifecycle-consumer invariant holds from day one.
    2. Journal.tsx: rename today's component body to `function JournalDesktop({trades}:
       JournalProps)` (module-private, same file, JSX untouched — its empty-state branch
       included), and make the exported `Journal` the thin switch VERBATIM from UI-SPEC
       §5: `const isDesktop = useIsDesktop(); return isDesktop ? <JournalDesktop trades={trades} /> : <JournalMobile trades={trades} />;`
    3. Migrate the existing Journal.test.tsx desktop tests to the stub (D-16): add ONE
       local helper `stubDesktopMatchMedia()` copying the exact Object.defineProperty
       pattern from Overview.test.tsx (matches === `query === "(min-width: 1024px)"`),
       install it in `beforeEach` of every pre-existing describe block that renders
       `<Journal />` — "Journal screen", "Journal screen — rule-tag control (RULE-01)",
       and "Journal — mobile stack order (35-05…)" (its lg:-gated class assertions still
       hold on the desktop tree pre-D-17) — with matching afterEach
       `Reflect.deleteProperty(window, "matchMedia")`. Do not weaken or delete any
       pre-existing assertion — only wrap them in the stubbed environment.
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/screens/Journal.test.tsx && bun run test && bun run typecheck && bun run lint</automated>
  </verify>
  <done>
    RED output shown for the 3 new tests, then the FULL workspace suite green, typecheck
    + lint clean. Every pre-existing Journal desktop assertion passes under the stub.
    Commit: `feat(36-03): Journal useIsDesktop switch + desktop-test matchMedia migration (D-03/D-16)`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Presentation-only refactor + branch; no new input parsing, no new network surface, zero new dependencies |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-05 | DoS | Journal branch mounting BOTH trees (double useLifecycle/useRuleTags poll consumers) | low | mitigate | D-03 single-branch switch; J3 asserts the other tree is absent from the DOM |
| T-36-06 | Tampering | TradeSummary contract drift breaking JournalContainer | low | mitigate | Type re-export + JournalContainer.test.tsx in Task 1 verify |
| T-36-SC | Tampering | Supply chain (npm installs) | low | accept | Zero new dependencies (locked constraint) — no install tasks exist |
</threat_model>

<verification>
- `bun run test` full workspace green, `bun run typecheck`, `bun run lint` clean.
- Desktop tripwire: the J3 structural guard test green under the stub;
  JournalContainer.tsx zero diff this plan.
- Rollback note: two commits on 4 files; `git revert` of the Task 2 commit alone
  restores the unconditional desktop Journal render (single-file seam) — no data or
  migration surface.
</verification>

<success_criteria>
- J3 demonstrably green via the commands above.
- Journal.tsx exports the same public symbols as before (Journal + TradeSummary type).
- useJournalModel is the ONLY place Journal state/derivation lives; JournalDesktop and
  JournalMobile both call it.
</success_criteria>

<output>
Create `.planning/phases/36-analyzer-journal-mobile-redesign-same-dedicated-mobile-tree-/36-03-SUMMARY.md` when done (include the RED run output for Task 2).
</output>
