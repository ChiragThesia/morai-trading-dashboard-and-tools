---
phase: 36-analyzer-journal-mobile-redesign
plan: 05
type: execute
wave: 3
depends_on: [36-02, 36-04]
files_modified:
  - apps/web/src/screens/Analyzer.tsx
  - apps/web/src/screens/Analyzer.test.tsx
  - apps/web/src/screens/Journal.tsx
  - apps/web/src/screens/Journal.test.tsx
autonomous: true
requirements: [MOBILE-13]
must_haves:
  truths:
    - "The Phase-35 reflow arms are gone from both desktop trees: no order-* / contents / mobile-bleed classes in AnalyzerDesktop, no flex-col/lg:-gated arm on journal-positions (D-17)"
    - "1440px screenshots of Analyzer and Journal are pixel-identical before vs after the cleanup (C7)"
    - "Full workspace suite + typecheck + lint green (J16)"
    - "The chrome-devtools C1-C10 checklist passes at 390×844 with 320px and 1440px spot checks"
  artifacts:
    - "apps/web/src/screens/Analyzer.tsx — desktop tree on plain grid classes"
    - "apps/web/src/screens/Journal.tsx — desktop tree on plain grid classes"
    - "Phase-dir screenshots/ — 1440px before/after captures, both screens"
  key_links:
    - "AnalyzerDesktop analyzer-inner-grid -> plain grid grid-cols-[300px_minmax(0,1fr)_330px] gap-4 (D-17)"
    - "JournalDesktop journal-positions -> plain grid h-full grid-cols-[250px_minmax(0,1fr)_290px] (D-17)"
---

<objective>
Close the phase: remove the now-unreachable Phase-35 reflow arms from both desktop trees
(D-17 — the switches from 36-01/36-03 mean the desktop paths only ever mount at
>=1024px), gate the removal on 1440px before/after screenshot identity (C7), then run
the integration gate — full suite/typecheck/lint (J16) plus the chrome-devtools C1-C10
checklist — and hand C11 (the user phone check, the phase's only acceptance bar) to the
user as the end-of-phase human item.

Purpose: dead branches rot; the CONTEXT locks a dedicated, screenshot-gated final task
for their removal, and the phase cannot close without the layout claims jsdom can't see.
Output: clean desktop trees + a recorded C1-C10 gate + the C11 handoff.

Closes validation claims (36-VALIDATION.md): J16, C1-C10 (agent), C11 (user).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-analyzer-journal-mobile-redesign-same-dedicated-mobile-tree-/36-CONTEXT.md
@.planning/phases/36-analyzer-journal-mobile-redesign-same-dedicated-mobile-tree-/36-UI-SPEC.md
@.planning/phases/36-analyzer-journal-mobile-redesign-same-dedicated-mobile-tree-/36-VALIDATION.md
@apps/web/src/screens/Analyzer.tsx
@apps/web/src/screens/Analyzer.test.tsx
@apps/web/src/screens/Journal.tsx
@apps/web/src/screens/Journal.test.tsx
</context>

<repo_law>
- TDD red→green per .claude/rules/tdd.md: the rewritten structural tests are written
  and RUN red BEFORE the class cleanup lands; test + implementation in one commit.
- Commit with EXPLICIT paths only. Never `git add -A` or `git add .`.
- No `any`, no `as` (except `as const`), no `!`.
- chrome-devtools MCP is the sanctioned UAT driver (standing permission): run server +
  web locally READ-ONLY against prod data for undeployed code; never a local worker.
- NEVER read, edit, or `git add` `apps/web/src/screens/Analyzer 2.tsx` — stray file.
</repo_law>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-17 dead-branch cleanup, screenshot-gated (C7)</name>
  <files>apps/web/src/screens/Analyzer.tsx, apps/web/src/screens/Analyzer.test.tsx, apps/web/src/screens/Journal.tsx, apps/web/src/screens/Journal.test.tsx</files>
  <behavior>
    BEFORE any code change: start the local web app (existing dev command, read-only)
    and capture 1440×900 screenshots of /analyzer and /journal via chrome-devtools into
    `.planning/phases/36-analyzer-journal-mobile-redesign-same-dedicated-mobile-tree-/screenshots/`
    as `analyzer-1440-before.png` / `journal-1440-before.png`.

    Then RED: rewrite the two Phase-35 reflow describes IN PLACE (they run under the
    desktop matchMedia stub since 36-01/36-03):
    - Analyzer.test.tsx "Analyzer — mobile stack order (35-05…)" → rename the describe
      to "Analyzer — desktop grid post-cleanup (36 D-17)" and replace its assertions:
      `analyzer-inner-grid` className contains the plain `grid`,
      `grid-cols-[300px_minmax(0,1fr)_330px]`, and `gap-4` tokens and contains NO
      `lg:`-prefixed variants of them; the scorecard/rail/center/right wrappers carry
      no CSS order classes; the chart wrapper carries no negative-margin bleed classes
      (or the wrapper div is gone — assert PayoffChart's parent chain has no such
      class). Keep the DOM-order test (scorecard → rail → center → right) as-is.
    - Journal.test.tsx "Journal — mobile stack order (35-05…)" → rename to "Journal —
      desktop grid post-cleanup (36 D-17)" and replace: `journal-positions` className
      contains plain `grid`, `h-full`, `grid-cols-[250px_minmax(0,1fr)_290px]`,
      `gap-3`, `overflow-hidden`, `p-3` with NO `lg:` prefixes and no `flex-col`; each
      column carries plain `min-h-0` + `overflow-y-auto`. Keep the column DOM-order
      test as-is.
    RUN both files — the rewritten assertions must FAIL against the pre-cleanup classes.
  </behavior>
  <action>
    GREEN — apply UI-SPEC §7 (D-17) exactly:
    - Analyzer.tsx (AnalyzerDesktop only): drop the `order-*`/`lg:order-none` classes on
      the four wrappers; `analyzer-inner-grid` className becomes
      `grid grid-cols-[300px_minmax(0,1fr)_330px] gap-4`; the chart bleed wrapper
      becomes a plain div or is removed entirely. Update the stale 35-05 comments that
      described the reflow arms.
    - Journal.tsx (JournalDesktop only): `journal-positions` className becomes
      `grid h-full grid-cols-[250px_minmax(0,1fr)_290px] gap-3 overflow-hidden p-3`;
      the three columns keep `flex flex-col gap-3 min-h-0 overflow-y-auto` without
      `lg:` prefixes.
    - Nothing else changes in either file — this is class-token cleanup on branches that
      only mount >=1024px; DO NOT touch the mobile trees, the switches, or the model
      hooks.

    AFTER the suite is green: restart the local web app, capture
    `analyzer-1440-after.png` / `journal-1440-after.png` at the same 1440×900 viewport,
    and compare each before/after pair (pixel-diff via chrome-devtools screenshots —
    the pairs must be visually identical; investigate ANY diff before committing, since
    the cleanup is display-invisible by construction). Record the comparison verdict in
    the SUMMARY.
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/screens/Analyzer.test.tsx && bunx vitest run apps/web/src/screens/Journal.test.tsx && ! rg -q 'order-[0-9]' apps/web/src/screens/Analyzer.tsx && ! rg -q 'lg:grid' apps/web/src/screens/Journal.tsx && bun run typecheck</automated>
  </verify>
  <done>
    RED shown for the rewritten describes, then green; no CSS order utilities remain in
    Analyzer.tsx; journal-positions is a plain grid; 1440px before/after screenshot
    pairs captured and identical (C7). Commit:
    `refactor(36-05): remove Phase-35 reflow dead branches from desktop trees (D-17)`
  </done>
</task>

<!-- planner-discipline-allow: lg:grid -->

<task type="auto">
  <name>Task 2: Integration gate — J16 + chrome-devtools C1-C10</name>
  <files>none — verification only (evidence recorded in SUMMARY)</files>
  <action>
    1. J16: run the full gate — `bun run test`, `bun run typecheck`, `bun run lint` —
       all must be green.
    2. Drive the chrome-devtools C1-C10 checklist from 36-VALIDATION.md against the
       local web app (read-only), recording pass/fail + notes per item in the SUMMARY:
       - 390×844 emulation: C1 (Analyzer cold start: paste+Analyze top, bare prompts,
         ZERO hollow shells), C3 (chart edge-to-edge, ONE slim chrome row, dialogs
         thumb-usable, term-structure fits its open disclosure — log a follow-up if it
         clips, internals are frozen), C4 (lifecycle 60%-width bug GONE: 840px pan
         mount, legible labels, opens at latest, swipe pans, crosshair syncs the P&L
         bridge), C5 (focal P&L, single OPEN affordance, History folds, selected
         violet), C6 (⋯ → Rebuild → confirm stack; Cancel unwinds one layer; copy
         verbatim).
       - C2: `document.body.scrollWidth === window.innerWidth` on BOTH screens at 390
         AND 320, with the lifecycle pan container present.
       - C8 at 320px: paste row, control row, checklist rows, trade cards — no
         wrap-break, no clip.
       - C9 iOS profile: focusing the paste input does not zoom (16px input, D-18).
       - C10: resize across 1024px swaps trees on both screens without crash (state
         reset accepted).
       - C7 re-check: confirm the Task 1 after-screenshots stand as the post-D-17
         1440px baseline.
       Any FAIL: stop, fix within this plan's scope if it is a mobile-tree defect
       (small follow-up commit with its own test where feasible), or record it as a
       phase gap if it is out of scope (chart internals are frozen).
    3. Do NOT deploy in this plan — deployment/UAT sequencing is the orchestrator's
       call after C11.
  </action>
  <verify>
    <automated>bun run test && bun run typecheck && bun run lint</automated>
    <human-check>C11 — user phone check on morai.wtf Analyzer + Journal (after deploy):
    both screens read as designed mobile app screens on the real phone. This is the
    phase's only acceptance bar (CONTEXT §Acceptance); C1-C10 agent evidence is
    supporting material, not a substitute.</human-check>
  </verify>
  <done>
    J16 gate green; C1-C10 each recorded pass (or logged as an explicit gap with
    rationale) in the SUMMARY; C11 handed to the user as the end-of-phase human item.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Class-token cleanup on desktop-only branches + verification; no new input, network, or dependency surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-09 | DoS | D-17 cleanup accidentally altering desktop layout (regression for the primary workflow) | medium | mitigate | 1440px before/after screenshot identity gate (C7) + rewritten structural tests + full suite before commit |
| T-36-SC | Tampering | Supply chain (npm installs) | low | accept | Zero new dependencies (locked constraint) — no install tasks exist |
</threat_model>

<verification>
- `bun run test`, `bun run typecheck`, `bun run lint` green (J16).
- C1-C10 evidence in SUMMARY; before/after 1440px screenshots in the phase screenshots/
  directory (screenshots are evidence artifacts — commit them only if commit_docs
  conventions for this repo include phase evidence; otherwise reference their paths).
- Rollback note: Task 1 is one revertable commit on 4 files; reverting restores the
  (unreachable but harmless) reflow arms — zero user-visible effect at any width.
</verification>

<success_criteria>
- J16 green; C1-C10 recorded; C7 screenshot identity proven.
- No order-*/contents/bleed reflow classes remain in the desktop trees.
- C11 explicitly handed to the user with the deploy prerequisite stated.
</success_criteria>

<output>
Create `.planning/phases/36-analyzer-journal-mobile-redesign-same-dedicated-mobile-tree-/36-05-SUMMARY.md` when done (include the RED output, the screenshot-comparison verdict, and the C1-C10 table).
</output>
