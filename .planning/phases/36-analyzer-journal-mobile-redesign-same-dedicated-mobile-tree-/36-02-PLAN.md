---
phase: 36-analyzer-journal-mobile-redesign
plan: 02
type: execute
wave: 2
depends_on: [36-01]
files_modified:
  - apps/web/src/screens/analyzer-mobile/MobileScorecard.tsx
  - apps/web/src/screens/analyzer-mobile/MobileScorecard.test.tsx
  - apps/web/src/screens/analyzer-mobile/MobileAnalyzerChart.tsx
  - apps/web/src/screens/analyzer-mobile/AnalyzerMobile.tsx
  - apps/web/src/screens/analyzer-mobile/AnalyzerMobile.test.tsx
autonomous: true
requirements: [MOBILE-11]
must_haves:
  truths:
    - "Mobile Analyzer flows paste row -> candidates -> scorecard hero -> chart row -> three closed disclosures, in DOM order (D-06)"
    - "With NO candidate selected, nothing below the candidates section renders — zero hollow shells; the five rail states render as bare prompts with copy verbatim (D-07)"
    - "Top 3 scored candidates show; the rest fold behind a real aria-expanded `All candidates (N)` toggle; pasted cards pin on top (D-07)"
    - "The scorecard hero renders the 32px rounded score, verbatim context line, and the full checklist as rows — every string identical to the desktop chip contract (D-08)"
    - "The mobile chart mounts MobileChartControls driven by Analyzer's own dateControl/toggles, and PayoffChart receives the picker colors/EM band PLUS the 35.1 mobile props (D-09)"
    - "Term/Why/Plan are native closed <details>; contents mount only when open; notScored shows the pasted-note inside each (D-10, catches #23/#24)"
  artifacts:
    - "apps/web/src/screens/analyzer-mobile/MobileScorecard.tsx — scorecard verdict hero"
    - "apps/web/src/screens/analyzer-mobile/MobileAnalyzerChart.tsx — chart block (controls + full-bleed PayoffChart + caption)"
    - "apps/web/src/screens/analyzer-mobile/AnalyzerMobile.tsx — full mobile flow"
    - "apps/web/src/screens/analyzer-mobile/AnalyzerMobile.test.tsx + MobileScorecard.test.tsx — J4-J7, J9, J10 coverage"
  key_links:
    - "AnalyzerMobile -> useAnalyzerModel() slices by name (D-02: no second store)"
    - "AnalyzerMobile chart -> MobileChartControls {dateControl,bounds,toggles,onToggle} (D-05/J8: same testids, Analyzer's own state)"
    - "MobileScorecard rows -> scoreStatus/CHIP_LABELS/FALLBACK_SCORE_ITEMS/EXPERIMENTAL_SHORT from the model module (D-08: never re-implemented)"
---

<objective>
Build the full AnalyzerMobile tree on the 36-01 foundation: paste block first (the
screen's verb), candidates with the top-3 fold and bare-prompt rail states, the
MobileScorecard verdict hero, the full-bleed chart block behind one slim chrome row, and
the three closed detail disclosures. Kills the 390px ground-truth failures: hollow
desktop panels, desktop chart chrome at phone width, no mobile hierarchy.

Purpose: this is the user-facing Analyzer deliverable of the phase (CONTEXT §Deliverable).
Output: complete mobile Analyzer flow, all D-06…D-10/D-18 contracts jsdom-verified.

Closes validation claims (36-VALIDATION.md): J4, J5, J6, J7, J9, J10, and the
Analyzer-side half of J8.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-analyzer-journal-mobile-redesign-same-dedicated-mobile-tree-/36-UI-SPEC.md
@.planning/phases/36-analyzer-journal-mobile-redesign-same-dedicated-mobile-tree-/36-VALIDATION.md
@apps/web/src/screens/analyzer-mobile/useAnalyzerModel.ts
@apps/web/src/screens/analyzer-mobile/AnalyzerMobile.tsx
@apps/web/src/screens/Analyzer.tsx
@apps/web/src/screens/Analyzer.test.tsx
@apps/web/src/components/charts/MobileChartControls.tsx
@apps/web/src/components/picker/CandidateCard.tsx
@apps/web/src/components/system/index.tsx
@apps/web/src/screens/overview-mobile/OverviewMobile.tsx
</context>

<repo_law>
- TDD red→green per .claude/rules/tdd.md: failing test RUN and shown first, then
  implement, then green run shown; test + implementation in one conventional commit.
- Commit with EXPLICIT paths only. Never `git add -A` or `git add .`.
- No `any`, no `as` (except `as const`), no `!`. No IIFEs in JSX.
- Copy/classes/testids VERBATIM from 36-UI-SPEC.md Copywriting Contract + §4 — never
  paraphrase. Cite D-IDs in code comments.
- Do NOT edit apps/web/src/screens/Analyzer.test.tsx in this plan (it was migrated in
  36-01); all new mobile assertions live in the two new analyzer-mobile test files.
- NEVER read, edit, or `git add` `apps/web/src/screens/Analyzer 2.tsx` — stray file.
</repo_law>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MobileScorecard — verdict hero + checklist rows (D-08, J7)</name>
  <files>apps/web/src/screens/analyzer-mobile/MobileScorecard.tsx, apps/web/src/screens/analyzer-mobile/MobileScorecard.test.tsx</files>
  <behavior>
    New suite MobileScorecard.test.tsx, RED first. The component is fully controlled —
    tests render it directly with candidates from pickerSnapshotFixture (@morai/contracts;
    sort by score for TOP, find fwdIv===null for GUARD). Base props: ruleSet from the
    fixture, gateDrops {liquidity:2, netTheta:1}, marketSession "rth", bookCount 1,
    bookDebit/bookTheta/bookVega from TOP.
    - J7a null: `candidate={null}` → component renders nothing (container has no
      `mobile-score`, no `Scorecard` label, no checklist testids).
    - J7b not-scored: candidate with `breakdown: []` → only the `Scorecard` label and the
      `Pasted calendar — not engine-scored.` line; no score, no checklist rows.
    - J7c hero: scored TOP → `mobile-score` text equals `String(Math.round(TOP.score))`
      with the 32px mono bold classes; context line carries
      `risk-profile-selected-name` (TOP.name, text-violet) and the verbatim
      ` · debit $… · θ …/d · vega +…` string; NO `combined-book-summary` at bookCount 1.
    - J7d combined: bookCount 2 + book totals → `combined-book-summary` present with the
      verbatim `+ 1 more → combined debit $… (max loss) · θ …/d · vega +…` string.
    - J7e checklist: one row per active score rule (testids `checklist-{key}` preserved),
      icon/class per scoreStatus of the candidate's contribution, label from CHIP_LABELS,
      `w{weight}` span, right-aligned `{n}%`; θ GATE row (`checklist-theta`) shows
      `{✓|✗} {±n.n}/d` sign-colored; GUARD candidate → fwdEdge row shows `— n/a` in
      text-dim; CALIBRATING row (`checklist-experimental`) when candidate.context is
      non-empty; gate-drops fine print (`checklist-gate-drops`) with the verbatim
      `2 illiquid quotes · 1 negative-θ pair dropped this run`.
    - J7f session: marketSession "after-hours" → a SESSION row with `AH — indicative`
      amber text renders first; absent under "rth".
  </behavior>
  <action>
    Create `MobileScorecard.tsx` per UI-SPEC §4 (D-08). Props:
    `{ candidate, ruleSet, gateDrops, marketSession, bookCount, bookDebit, bookTheta, bookVega }`.
    - `candidate === null` → return null (rail states carry the messaging — no hollow
      shells).
    - `candidate.breakdown.length === 0` → SectionLabel `Scorecard` + the pasted-note
      line only (same gate as desktop: breakdown length, never the pasted id).
    - Scored: SectionLabel `Scorecard` → score span
      `font-mono text-[32px] font-bold tabular-nums leading-none text-txt`
      (testid `mobile-score`, `Math.round(candidate.score)`) → 11px mono context line
      (strings verbatim from the desktop selected-name line incl. the amber
      combined-book span when bookCount > 1, testids preserved) → `flex flex-col gap-1`
      checklist ROWS, each `font-mono text-[11px]` with `flex items-center
      justify-between`: left `{icon} {label} w{n}` (icon/status colored via scoreStatus,
      label text-muted-foreground, weight text-dim), right `{n}%` / `n/a` / θ-gate
      `{±n.n}/d`. Row derivation reuses scoreStatus/CHIP_LABELS/FALLBACK_SCORE_ITEMS/
      EXPERIMENTAL_SHORT imported from useAnalyzerModel.ts — NOT re-implemented; the
      fwdEdge/fwdIv-null guard, θ GATE row, AH SESSION row (first, after-hours only),
      CALIBRATING dim row, and gate-drops fine print mirror ScoringMethodologyPanel's
      logic row-for-row. MetricChip is NOT imported or mounted (D-08).
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/screens/analyzer-mobile/MobileScorecard.test.tsx && bun run typecheck</automated>
  </verify>
  <done>
    RED shown, then all J7 tests green; typecheck clean.
    Commit: `feat(36-02): MobileScorecard verdict hero + checklist rows (D-08)`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AnalyzerMobile flow — paste block, candidates + fold, rail states (D-06/D-07/D-18, J5/J6)</name>
  <files>apps/web/src/screens/analyzer-mobile/AnalyzerMobile.tsx, apps/web/src/screens/analyzer-mobile/AnalyzerMobile.test.tsx</files>
  <behavior>
    New suite AnalyzerMobile.test.tsx, RED first. Harness: copy the Analyzer.test.tsx
    mock pattern (vi.hoisted mocks for usePicker / useRepullChains / useAnalyzeCalendar;
    spy-wrap PayoffChart via importOriginal for Task 3) and render `<Analyzer />` — NO
    matchMedia stub, so jsdom's default renders the mobile tree (D-16). The fixture has
    9 candidates, so the fold engages off the stock mock.
    - J6a loading: usePicker `{data: undefined, isPending: true}` → `picker-loading`
      present with `Loading candidates…`; no element in that branch carries the Panel
      gradient class (assert the state node's closest container lacks it); no
      `mobile-score`, no `<details>` elements anywhere.
    - J6b error: `{isError: true}` → `picker-error` + `Couldn't load candidates.` +
      Retry button wired to refetch.
    - J6c cold-start: `{data: null, isPending: false}` → `picker-empty-cold-start` with
      `Picker warming up` + the pending sub-line.
    - J6d zero-filtered: snapshot with `candidates: []` → `picker-empty-filtered` with
      the `No put calendars meet net-θ>0 over the {asOf} snapshot.` line.
    - J6e no hollow shells: in every one of the four states above, `mobile-score`,
      `analyzer-mobile-caption`, and all three disclosure summaries are absent.
    - J5a fold: default fixture (9 scored) → exactly 3 CandidateCards + the
      `all-candidates-toggle` button reading `▸ All candidates (6)` with
      aria-expanded="false"; clicking flips aria-expanded to "true", glyph to `▾`, and
      reveals the remaining 6; second click folds.
    - J5b no fold: snapshot mock with 3 candidates → `all-candidates-toggle` absent.
    - J5c pasted pins: after a successful paste (fireEvent input + Analyze with the
      unscored-fallback mock), the pasted card renders ABOVE the scored cards and is
      auto-selected; `picker-paste-clear-all` appears.
    - Paste block: `picker-paste-input` has the `text-base` class (D-18 iOS zoom guard)
      and `min-h-11`; parse failure (garbage text + Analyze) → `picker-paste-error`
      with the verbatim error copy; `rail-legend` renders below the cards when
      candidates > 0.
  </behavior>
  <action>
    Flesh out AnalyzerMobile.tsx per UI-SPEC §4 (root already exists from 36-01):
    - Paste block (`px-4`, D-18): `flex items-center gap-2` →
      `<input data-testid="picker-paste-input" type="text" …>` with
      `min-h-11 min-w-0 flex-1 rounded-[3px] border border-line2 bg-transparent px-2 font-mono text-base text-txt`,
      controlled by the model's pasteText/setPasteText, plus
      `Button variant="primary" size="touch" data-testid="picker-paste-analyze"` →
      handlePasteAnalyze. Error line below the row: existing `picker-paste-error`
      classes (`font-mono text-[9px] text-down`).
    - Candidates section (`px-4`, D-07): heading row `flex items-center justify-between
      gap-2` → SectionLabel `Candidates` + `flex gap-1.5` holding [`Clear all` ghost
      Button (testid `picker-paste-clear-all`) when pasted > 0, then the repull control
      JSX built verbatim from the model's repull slice — same testids
      `repull-chains-button`/`repull-status` and strings as the desktop control].
      Body = the five rail states with copy + testids VERBATIM from the Copywriting
      Contract, as bare prompts — no Panel import anywhere in this file, "bare" means
      unboxed: loading → one dim mono line; error → line + Retry Button; cold-start →
      the two-line heading+sub block; settled → `flex flex-col gap-2` of CandidateCard:
      all pastedCandidates first (with `pasted` + onRemove wiring), then
      `sortedCandidates.slice(0, 3)`, then when `sortedCandidates.length > 3` the
      `<button type="button" aria-expanded={…} data-testid="all-candidates-toggle">`
      with the history-toggle classes from UI-SPEC §4 revealing `sortedCandidates.slice(3)`
      via React state (real state, catch #24 — glyph `▸`/`▾` + `All candidates (N)`,
      N = scored beyond top 3); zero-filtered branch verbatim. Every CandidateCard prop
      wires exactly as the desktop CandidateRail does today (selected/combined/copied/
      observedAt/source/context statuses/onSelect/onToggleCombine/onCopy). The
      `rail-legend` line (verbatim string, amber glyph spans) renders below the visible
      cards when candidates > 0.
    - Mount `<MobileScorecard …>` (`px-4`) after the candidates section, fed
      candidate={model.selected} + ruleSet/gateDrops/marketSession from the snapshot
      (same fallbacks as the desktop call site) + the book totals.
    - Keep the D-06 order: paste → candidates → scorecard. Chart block + disclosures
      land in Task 3 — leave a placeholder comment.
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/screens/analyzer-mobile/AnalyzerMobile.test.tsx && bunx vitest run apps/web/src/screens/Analyzer.test.tsx && bun run typecheck && ! rg -q 'import \{[^}]*\bPanel\b|<Panel\b' apps/web/src/screens/analyzer-mobile/AnalyzerMobile.tsx</automated>
  </verify>
  <done>
    RED shown, then J5/J6 green; Analyzer.test.tsx (desktop guard) still green
    unmodified; no Panel usage in the mobile tree file.
    Commit: `feat(36-02): AnalyzerMobile paste block + candidates fold + bare rail states (D-06/D-07/D-18)`
  </done>
</task>

<!-- planner-discipline-allow: Panel -->

<task type="auto" tdd="true">
  <name>Task 3: Chart block + disclosures + DOM order (D-09/D-10, J4/J8/J9/J10)</name>
  <files>apps/web/src/screens/analyzer-mobile/MobileAnalyzerChart.tsx, apps/web/src/screens/analyzer-mobile/AnalyzerMobile.tsx, apps/web/src/screens/analyzer-mobile/AnalyzerMobile.test.tsx</files>
  <behavior>
    Extend AnalyzerMobile.test.tsx, RED first:
    - J9: with the default populated mock, `latestPayoffChartProps()` (PayoffChart
      spy) reports todayCurveColor/expirationCurveColor equal to the picker constants,
      expectedMoveBand `{spot, em: selected.expectedMove}`, AND the three mobile props
      exactly: BE pills off, aspect ratio 1.3, highlighted position null.
    - J8 (Analyzer half): `date-pill`, and after opening the Projection dialog
      `date-slider` + `date-picker-input`, all render and the slider max reflects
      Analyzer's own bounds (front-expiry-derived maxDays > 0); clicking `›` advances
      the pill label (driven by Analyzer's dateControl, not Overview's).
    - Caption: `analyzer-mobile-caption` reads `● {source} · {asOf}`; with
      marketSession "after-hours" mock it appends ` · AH — indicative` and the dot is
      `bg-amber`; with both context statuses "ok" + "rth" the dot is `bg-up`.
    - J10a: exactly three `<details>` render, none with the `open` attribute by
      default; summaries read `Term structure + your legs` / `Why this calendar` /
      `Entry / exit plan`.
    - J10b: setting `open` on each details (fireEvent.click on summary) mounts
      TermStructureChart / WhyPanel / EntryExitPlan content respectively (assert via
      each component's known content, e.g. headings/testids they render).
    - J10c: a selected not-scored candidate (paste flow with unscored fallback) →
      each open details contains the `Pasted calendar — not engine-scored.` note
      instead of the component.
    - J4 DOM order (compareDocumentPosition): paste input precedes the card list,
      precedes `mobile-score`, precedes the `date-pill` row, precedes the first
      `<details>`.
    - Null-candidate guard (D-10): with the zero-filtered snapshot and no pasted cards,
      no chart row, no caption, no `<details>` anywhere.
  </behavior>
  <action>
    Create `MobileAnalyzerChart.tsx` per UI-SPEC §4 (D-09): renders only when
    `selected !== null && scenarioResult !== null`; section `px-0`;
    `<MobileChartControls dateControl bounds toggles onToggle />` row wrapper `px-4`;
    then `mt-2 w-full` PayoffChart with the EXACT prop block from UI-SPEC §4 (today/fan/
    expiration/roll curves, snapshot gex walls with null fallbacks, domain/spot/toggles,
    fitY false + noop, positionSetSignature, baseExpirationCurve, the two picker curve
    color constants imported from useAnalyzerModel.ts, expectedMoveBand gated on
    `selected.expectedMove > 0`, and the three 35.1 mobile props verbatim from the spec
    block); then the caption (`px-4 mt-1.5`, testid `analyzer-mobile-caption`,
    `font-mono text-[9px] text-dim`): worst-of dot span (`bg-up` iff gexContextStatus
    AND eventsContextStatus === "ok" AND session rth, else `bg-amber`) + `{source} ·
    {asOf}` + ` · AH — indicative` when after-hours. The `⧉ Copy TOS order` button is
    NOT rendered anywhere in the mobile tree (D-09 — CandidateCard carries per-card copy).

    In AnalyzerMobile.tsx, mount MobileAnalyzerChart after MobileScorecard, then the
    three disclosures (`px-4`, D-10) rendered only when `selected !== null`: each
    `<details className="border-t border-line/40">` with
    `<summary className="flex min-h-11 cursor-pointer items-center gap-1.5 py-3 …">`
    styled per the Typography table (SectionLabel-style 10px tracked uppercase text),
    `▸` glyph with `[&::-webkit-details-marker]:hidden` + a `group-open:` rotate class
    (the native `open` attribute is the state — never CSS-revealed content, catch #24).
    Contents: TermStructureChart (needs snapshot !== null; props as the desktop call
    site passes today), WhyPanel (needs gex), EntryExitPlan (with sizing) — each guarded
    by `breakdown.length === 0` → the pasted-note line, exactly as RightColumn does
    (catch #23: disclosures render whenever a candidate is selected, never gated on
    scoring). With no candidate at all, hero/chart/disclosures render nothing.
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/screens/analyzer-mobile/AnalyzerMobile.test.tsx && bunx vitest run apps/web/src/screens/Analyzer.test.tsx && bun run test && bun run typecheck && bun run lint && ! rg -q 'import \{[^}]*\bPanel\b|<Panel\b' apps/web/src/screens/analyzer-mobile/AnalyzerMobile.tsx && git diff --quiet -- apps/web/src/screens/Analyzer.tsx apps/web/src/components/charts/PayoffChart.tsx apps/web/src/components/charts/PayoffControls.tsx apps/web/src/components/picker/CandidateCard.tsx apps/web/src/components/picker/WhyPanel.tsx apps/web/src/components/picker/TermStructureChart.tsx apps/web/src/components/picker/EntryExitPlan.tsx</automated>
  </verify>
  <done>
    RED shown, then J4/J8/J9/J10 green; full workspace suite green; typecheck + lint
    clean.
    Commit: `feat(36-02): AnalyzerMobile chart block + detail disclosures (D-09/D-10)`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Paste input → tos-parser | Existing boundary reused unchanged — parseTosOrder validates before any candidate is built; CALL orders never reach the analyze endpoint (D-03 heritage) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-03 | Tampering | Pasted order text (untrusted user input) | low | mitigate | Reuses the existing parseTosOrder → parse-fail → error-copy path via the shared model hook (no new parsing surface); J6 asserts the failure branch |
| T-36-04 | Info Disclosure | Mobile caption/scorecard leaking non-contract strings | low | mitigate | Copywriting Contract enforced verbatim by J6/J7 assertions |
| T-36-SC | Tampering | Supply chain (npm installs) | low | accept | Zero new dependencies (locked constraint) — no install tasks exist |
</threat_model>

<verification>
- `bun run test` full workspace green, typecheck + lint clean.
- Desktop tripwire (this plan must not touch desktop files): `git diff --quiet` against
  Analyzer.tsx, PayoffControls.tsx, CandidateCard.tsx, WhyPanel.tsx,
  TermStructureChart.tsx, EntryExitPlan.tsx, PayoffChart.tsx over the plan's commits —
  only analyzer-mobile/* files change.
- Rollback note: three additive commits confined to analyzer-mobile/*; reverting them
  restores the 36-01 skeleton without touching the switch or desktop tree.
</verification>

<success_criteria>
- J4, J5, J6, J7, J9, J10 + Analyzer-side J8 demonstrably green via the commands above.
- The mobile Analyzer acceptance geometry holds structurally: nothing below the
  candidates section renders when no candidate is selected; exactly one chrome row
  (MobileChartControls) above the chart.
</success_criteria>

<output>
Create `.planning/phases/36-analyzer-journal-mobile-redesign-same-dedicated-mobile-tree-/36-02-SUMMARY.md` when done (include RED outputs per task).
</output>
