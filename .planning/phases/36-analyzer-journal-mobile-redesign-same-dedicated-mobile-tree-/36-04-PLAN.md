---
phase: 36-analyzer-journal-mobile-redesign
plan: 04
type: execute
wave: 2
depends_on: [36-03]
files_modified:
  - apps/web/src/screens/journal-mobile/TradeCard.tsx
  - apps/web/src/screens/journal-mobile/TradeCard.test.tsx
  - apps/web/src/screens/journal-mobile/MobileLifecycle.tsx
  - apps/web/src/screens/journal-mobile/JournalMobile.tsx
  - apps/web/src/screens/journal-mobile/JournalMobile.test.tsx
autonomous: true
requirements: [MOBILE-12]
must_haves:
  truths:
    - "Trade rows are PositionCard-idiom cards: name + single OPEN badge (open) or focal sign-colored P&L (closed), one muted meta line — the triple affordance is dead (D-11)"
    - "Tap-to-select is never gated (catch #23); selected card shows ring-violet bg-violetd; History (N) folds with the existing toggle + auto-open semantics (D-11)"
    - "The lifecycle chart mounts at 840px CSS width inside a full-bleed overflow-x-auto pan container scrolled to the latest snapshots — LifecycleChart.tsx has ZERO diff (D-12)"
    - "Rebuild journal… is invisible until the ⋯ overflow dialog opens; its confirm semantics are RebuildButton verbatim (D-13)"
    - "Footnotes live behind a closed `Chart notes` details; masthead + rail cards stack verbatim with crosshair→PnlBridge sync (D-14/D-15)"
  artifacts:
    - "apps/web/src/screens/journal-mobile/TradeCard.tsx — mobile trade card"
    - "apps/web/src/screens/journal-mobile/MobileLifecycle.tsx — masthead/states/pan mount/⋯ overflow/chart-notes"
    - "apps/web/src/screens/journal-mobile/JournalMobile.tsx — full mobile flow"
    - "apps/web/src/screens/journal-mobile/TradeCard.test.tsx + JournalMobile.test.tsx — J11-J15 coverage"
  key_links:
    - "JournalMobile -> useJournalModel(trades) slices by name (D-04: no second store)"
    - "LifecycleChart onCrosshairChange -> model setHoveredIndex -> PnlBridgeCard hoveredIndex (J15)"
    - "⋯ Dialog -> <RebuildButton calendarId/> verbatim (D-13: nested confirm preserved)"
---

<objective>
Build the full JournalMobile tree on the 36-03 foundation: TradeCards (killing the
OPEN-badge/status-text/chip triple affordance), the trades section with the History
fold, the MobileLifecycle block (masthead, honest states, the D-12 840px pan mount that
kills the 60%-width bug, the ⋯ overflow demoting Rebuild, the Chart-notes disclosure),
and the verbatim rail stack with crosshair sync.

Purpose: this is the user-facing Journal deliverable of the phase (CONTEXT §Deliverable).
Output: complete mobile Journal flow, all D-11…D-15 contracts jsdom-verified,
LifecycleChart internals provably untouched.

Closes validation claims (36-VALIDATION.md): J11, J12, J13, J14, J15.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-analyzer-journal-mobile-redesign-same-dedicated-mobile-tree-/36-UI-SPEC.md
@.planning/phases/36-analyzer-journal-mobile-redesign-same-dedicated-mobile-tree-/36-VALIDATION.md
@apps/web/src/screens/journal-mobile/useJournalModel.tsx
@apps/web/src/screens/journal-mobile/JournalMobile.tsx
@apps/web/src/screens/Journal.tsx
@apps/web/src/screens/Journal.test.tsx
@apps/web/src/components/PositionCard.tsx
@apps/web/src/components/RebuildButton.tsx
@apps/web/src/components/LifecycleMasthead.tsx
@apps/web/src/components/PnlBridgeCard.tsx
@apps/web/src/lib/journal-history.ts
</context>

<repo_law>
- TDD red→green per .claude/rules/tdd.md: failing test RUN and shown first, then
  implement, then green run shown; test + implementation in one conventional commit.
- Commit with EXPLICIT paths only. Never `git add -A` or `git add .`.
- No `any`, no `as` (except `as const`), no `!`. No IIFEs in JSX.
- Copy/classes/testids VERBATIM from 36-UI-SPEC.md Copywriting Contract + §6 — never
  paraphrase. Cite D-IDs in code comments.
- Do NOT edit apps/web/src/screens/Journal.test.tsx in this plan (migrated in 36-03);
  all new mobile assertions live in the two new journal-mobile test files.
- LifecycleChart.tsx, LifecycleMasthead.tsx, PnlBridgeCard.tsx, EdgeCard.tsx,
  GreeksNowCard.tsx, BeatsCard.tsx, RebuildButton.tsx: ZERO diff this plan (tripwire).
</repo_law>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TradeCard — PositionCard idiom, single affordance (D-11, J11)</name>
  <files>apps/web/src/screens/journal-mobile/TradeCard.tsx, apps/web/src/screens/journal-mobile/TradeCard.test.tsx</files>
  <behavior>
    New suite TradeCard.test.tsx, RED first. Fully controlled — render directly with
    TradeSummary fixtures (open trade closedAt null / realizedPnl ""; closed history
    trade +395.00; closed pre-Jun-12 trade hasSnapshots false; closed trade with
    realizedPnl "" for the em-dash case).
    - J11a open: renders the `OPEN` badge with the existing classes
      (`border-cyan/30 … text-cyan`); NO P&L span, NO literal "open" status text, NO
      history/entry-exit chip node anywhere in the card.
    - J11b closed: focal span shows `+$395.00` with `font-mono text-base font-bold
      tabular-nums` + the sign class; a negative P&L gets the down class; realizedPnl
      "" → `—` with text-dim.
    - J11c meta: row 2 reads `{fmtDate(openedAt)} → {fmtDate(closedAt)}` for closed,
      `{fmtDate(openedAt)} · open` for open; the pre-Jun-12 (non-history) trade appends
      ` · entry/exit only`; the history trade does NOT.
    - J11d select: click anywhere fires onSelect(trade.id) — including on a trade whose
      hasSnapshots is false (catch #23: never gated); Enter and Space fire it too
      (role="button" tabIndex path).
    - J11e selected: `isSelected` → ring-violet + bg-violetd classes; unselected →
      ring-line bg-raise/30.
    - J11f tags: `tagLabels` non-empty + selected → the `rule-tags-pill` testid with the
      comma-joined labels + title attr; empty → absent.
  </behavior>
  <action>
    Create `TradeCard.tsx` per UI-SPEC §6 (D-11). Props (TradeRow parity):
    `{ trade: TradeSummary; isSelected: boolean; tagLabels: ReadonlyArray<string>; onSelect: (id: string) => void }`.
    Surface: `cn("rounded-lg p-3 ring-1 transition-colors cursor-pointer", isSelected ? "ring-violet bg-violetd" : "ring-line bg-raise/30")`,
    `role="button" tabIndex={0}` + the existing Enter/Space onKeyDown pattern, min-h-11,
    testid `trade-card-{trade.id}`.
    Row 1 `flex items-center justify-between gap-2`: name span
    `font-display text-sm font-bold text-txt`; right side: open (closedAt null) → ONLY
    the OPEN badge span with the verbatim existing classes; closed → the focal P&L span
    (16px mono bold tabular-nums + sign class from the parsed value; `—` in text-dim
    when fmtPnl yields it). Row 2 `mt-1 font-mono text-[10px] text-dim truncate`: dates
    via fmtDate (imported from useJournalModel.tsx — never re-implemented), plus
    ` · entry/exit only` appended when classifyTradeHistory(...) !== "history" (the
    badge dies, the fact survives as text). Tags: the existing `rule-tags-pill` block
    verbatim (selected only), moved under row 2. No other badges, chips, or status text.
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/screens/journal-mobile/TradeCard.test.tsx && bun run typecheck</automated>
  </verify>
  <done>
    RED shown, then all J11 tests green; typecheck clean.
    Commit: `feat(36-04): journal TradeCard — PositionCard idiom, single OPEN affordance (D-11)`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: JournalMobile trades section — cards, History fold, tags pill (D-11/D-15, J12)</name>
  <files>apps/web/src/screens/journal-mobile/JournalMobile.tsx, apps/web/src/screens/journal-mobile/JournalMobile.test.tsx</files>
  <behavior>
    New suite JournalMobile.test.tsx, RED first. Harness: copy the Journal.test.tsx
    mock pattern (vi.mock useLifecycle / useRuleTags / useRebuildJournal / rpc /
    supabase; QueryClientProvider wrapper; the makeSnapshot/trade fixtures) and render
    `<Journal trades={…} />` — NO matchMedia stub, so jsdom renders the mobile tree.
    - J12a layout: with one open + two closed trades → `Trades` section label +
      `SPXW put calendars` pill; the open trade renders as a TradeCard ABOVE the
      `history-toggle`; the closed cards are hidden until the toggle (aria-expanded
      false → true) reveals them.
    - J12b auto-open: with ZERO open trades → history cards visible immediately and
      `history-toggle` has aria-expanded="true".
    - J12c selection: clicking a closed card selects it (ring-violet class appears);
      `rule-tags-pill` renders on the SELECTED card only when the mocked useRuleTags
      returns tagged events, and on no other card.
    - Empty state: `trades={[]}` → the two verbatim lines, no journal-mobile-root
      sections beyond them.
  </behavior>
  <action>
    Flesh out JournalMobile.tsx trades section per UI-SPEC §6 (root + empty state exist
    from 36-03): section `px-4`; heading row → SectionLabel `Trades` + HeadingPill
    `SPXW put calendars` (both imported from useJournalModel.tsx). `flex flex-col gap-2`
    of `<TradeCard>` for the model's openTrades (isSelected/tagLabels/onSelect wired
    exactly as the desktop TradeRow wiring: tagLabels only for the selected id), then
    when closedTrades > 0 the `History ({N})` toggle — existing classes, testid
    `history-toggle`, aria-expanded from the model's historyOpen, `▸`/`▾` glyph, onClick
    sets the model's history override — revealing closed TradeCards via React state
    (catch #24: real state). Selection flows through the model's setSelectedId. Leave a
    placeholder comment where the lifecycle block (Task 3) mounts.
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/screens/journal-mobile/JournalMobile.test.tsx && bunx vitest run apps/web/src/screens/Journal.test.tsx && bun run typecheck</automated>
  </verify>
  <done>
    RED shown, then J12 green; Journal.test.tsx (desktop guard) still green unmodified.
    Commit: `feat(36-04): JournalMobile trades section — cards + History fold (D-11)`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: MobileLifecycle + rail stack — pan mount, ⋯ Rebuild, chart notes, crosshair sync (D-12/D-13/D-14/D-15, J13/J14/J15)</name>
  <files>apps/web/src/screens/journal-mobile/MobileLifecycle.tsx, apps/web/src/screens/journal-mobile/JournalMobile.tsx, apps/web/src/screens/journal-mobile/JournalMobile.test.tsx</files>
  <behavior>
    Extend JournalMobile.test.tsx, RED first (history trade + >=2 makeSnapshot fixtures
    unless stated):
    - J13a mount: `lifecycle-pan` testid has the overflow-x-auto class; its inner
      wrapper carries the fixed 840px width class; LifecycleChart is mounted inside with
      snapshots/strike/onCrosshairChange (assert via its existing rendered surface, e.g.
      the svg role="img", not implementation details).
    - J14a states: loading mock → the aria-label `Loading lifecycle` skeleton; error
      mock → `Couldn't load this calendar's lifecycle.` + Retry wired to refetch;
      pre-Jun-12 trade → the PreHistoryStub copy; history trade with 1 snapshot →
      BuildingLifecycleStub copy. No Panel wrapper around any of them.
    - J14b demotion: with the history trade, NO button with aria-label
      `Rebuild journal for {calendarId}` in the document; clicking the `⋯` button
      (aria-label `More journal actions`) opens a dialog titled `Journal` that CONTAINS
      that Rebuild button; clicking it opens the nested confirm with the verbatim
      `This overwrites all snapshot history.` copy (useRebuildJournal is mocked — assert
      the dialog copy, not the mutation).
    - J14c notes: `chart-notes` details present, closed by default (no open attribute),
      summary `Chart notes`, containing BOTH verbatim footnote lines; the pan-hint
      caption `‹ swipe for earlier days` renders above it (history-with-chart state
      only — absent for the pre-Jun-12 trade).
    - J14d kind caption: heading row reads `30-min snapshots` for the history trade and
      `entry/exit only` for the pre-Jun-12 trade.
    - J15a sync: fire LifecycleChart's onCrosshairChange path by asserting the wiring —
      mock/spy approach: spy-wrap LifecycleChart via importOriginal (Overview.test.tsx
      PayoffChart precedent), assert its onCrosshairChange prop IS the function that,
      when called with an index, re-renders PnlBridgeCard with that hoveredIndex
      (spy-wrap PnlBridgeCard the same way and read its latest props).
    - J15b rail: PnlBridgeCard → EdgeCard → GreeksNowCard → BeatsCard → Notes panel all
      mount in DOM order; the Notes panel renders the ENTER/EXIT/ROLL RuleTagChips
      blocks per the mocked rule events (`Available at close.` when no CLOSE event) and
      the textarea with the existing placeholder.
    - J13b zero-diff gate is in <verify>, not jsdom.
  </behavior>
  <action>
    Create `MobileLifecycle.tsx` per UI-SPEC §6 (renders when selectedTrade !== null;
    props = the model slices it needs — trade, snapshots, isPending, isError, refetch,
    setHoveredIndex):
    1. `LifecycleMasthead` (`px-4`) behind the same gate as desktop
       (`!isPending && !isError && kind === "history"`) with the same eyebrow string
       (kind via classifyTradeHistory, exactly as LifecycleSection computes it).
    2. Heading row (`px-4`, flex items-center justify-between): the kind caption
       verbatim (10px dim mono) + a `⋯` Button size="touch" variant="ghost" with
       aria-label `More journal actions` opening a Dialog (max-w-xs, DialogTitle
       `Journal` in the DIALOG_TITLE_CLASS style) whose body renders
       `<RebuildButton calendarId={trade.calendarId} />` VERBATIM — its own confirm
       dialog/copy/mutation untouched (nested shadcn dialogs). RebuildButton renders
       NOWHERE else in the mobile tree (D-13).
    3. States (all `px-4`, no Panel wrapper): loading skeleton div (existing classes +
       aria attrs) / error + Retry / PreHistoryStub / BuildingLifecycleStub — components
       imported from useJournalModel.tsx, copy verbatim.
    4. Chart (kind history && snapshots.length > 1, D-12): section `px-0` →
       `<div data-testid="lifecycle-pan" className="overflow-x-auto">` wrapping
       `<div className="w-[840px]">` wrapping
       `<LifecycleChart snapshots={snapshots} strike={trade.strike} onCrosshairChange={setHoveredIndex} />`,
       plus a ref + layout effect setting `el.scrollLeft = el.scrollWidth` on mount /
       trade change (initial position = latest). Pan-hint caption below (`px-4 mt-1.5`,
       9px dim mono): `‹ swipe for earlier days` — only in the history-with-chart state.
    5. `Chart notes` details (`px-4`, testid `chart-notes`, closed by default, summary
       styled per the Typography table): the two footnote lines VERBATIM (D-14).

    In JournalMobile.tsx: mount MobileLifecycle after the trades section, then the rail
    stack (`px-4 flex flex-col gap-3`, D-15): `<PnlBridgeCard snapshots hoveredIndex>` →
    `<EdgeCard snapshots>` → `<GreeksNowCard snapshots>` → `<BeatsCard beats>` → the
    Notes Panel with the ENTER/EXIT/ROLL RuleTagChips blocks + textarea, duplicated
    verbatim from the desktop tree's Notes JSX (strings identical; RuleTagChips/
    SectionLabel/HeadingPill from their D-04 home; save/retry wired to the model's
    rule-tags slice).
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/screens/journal-mobile/JournalMobile.test.tsx && bunx vitest run apps/web/src/screens/Journal.test.tsx && git diff --quiet -- apps/web/src/components/LifecycleChart.tsx apps/web/src/components/LifecycleMasthead.tsx apps/web/src/components/PnlBridgeCard.tsx apps/web/src/components/EdgeCard.tsx apps/web/src/components/GreeksNowCard.tsx apps/web/src/components/BeatsCard.tsx apps/web/src/components/RebuildButton.tsx && bun run test && bun run typecheck && bun run lint</automated>
  </verify>
  <done>
    RED shown, then J13/J14/J15 green; the seven reused journal components have ZERO
    diff (J13 grep gate); full workspace suite green; typecheck + lint clean.
    Commit: `feat(36-04): MobileLifecycle pan mount + ⋯ Rebuild demotion + rail stack (D-12/D-13/D-14/D-15)`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ⋯ overflow → RebuildButton | Destructive journal rebuild (overwrites snapshot history) moves behind an overflow dialog — the confirm gate must survive the move |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-07 | Tampering | Rebuild demotion weakening the destructive-action confirm | medium | mitigate | D-13 reuses `<RebuildButton/>` VERBATIM (its own confirm dialog + useRebuildJournal auth'd mutation unchanged, zero-diff gate in Task 3 verify); J14 asserts the confirm copy renders before any mutation path |
| T-36-08 | DoS | Pan-mount layout effect thrashing scroll on every render | low | mitigate | scrollLeft set only on mount/trade change (effect deps), not per snapshot poll |
| T-36-SC | Tampering | Supply chain (npm installs) | low | accept | Zero new dependencies (locked constraint) — no install tasks exist |
</threat_model>

<verification>
- `bun run test` full workspace green, typecheck + lint clean.
- Desktop/extraction tripwire: Journal.test.tsx green unmodified; the seven reused
  journal components + JournalContainer.tsx zero diff across the plan's commits.
- Rollback note: three additive commits confined to journal-mobile/*; reverting them
  restores the 36-03 skeleton without touching the switch or desktop tree.
</verification>

<success_criteria>
- J11, J12, J13, J14, J15 demonstrably green via the commands above.
- Acceptance geometry holds structurally: single OPEN affordance per open card;
  `Rebuild journal…` unreachable without opening ⋯; lifecycle chart mounted at 840px
  inside the pan container with internals untouched.
</success_criteria>

<output>
Create `.planning/phases/36-analyzer-journal-mobile-redesign-same-dedicated-mobile-tree-/36-04-SUMMARY.md` when done (include RED outputs per task).
</output>
