---
phase: 29-runtime-rule-settings
plan: 07
type: tdd
wave: 2
depends_on: ["29-03", "29-04"]
files_modified:
  - packages/core/src/picker/domain/rule-config.ts
  - packages/core/src/picker/domain/rule-config.test.ts
  - packages/core/src/picker/index.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "resolvePickerRuleConfig(overrides?) returns a fully-resolved PickerRuleConfig with all curated picker knobs"
    - "Omitting overrides returns a config whose every field equals its picker-domain constant"
    - "The merge is idempotent and only accepts whitelisted knob fields"
  artifacts:
    - packages/core/src/picker/domain/rule-config.ts (PickerRuleOverrides + PickerRuleConfig + resolvePickerRuleConfig)
  key_links:
    - "PickerRuleConfig is the exact object the worker wiring (29-10) destructures into each picker-domain seam param"
---

<objective>
Assemble the picker engine's single pure merge function. `resolvePickerRuleConfig(overrides?)`
takes the partial picker overrides and returns a fully-resolved `PickerRuleConfig` whose every
field defaults to the corresponding `packages/core/src/picker/domain` constant. This is the one
object the worker wiring (29-10) destructures to feed each seam: `weights` →
`scoreCalendarCandidates`; `deltaBand`/`frontDte`/`backDteGap` → `selectCandidates`;
`debitBand` → `debitFitFraction`; `vixLadder` → `resolveEntryGate` + `autoTuneTargetDelta` floor;
`sizingContracts`+`vixLadder` → `resolveSizingTier`; `maxOpenCalendars` → `maxOpenTripped`.

The exact `PickerRuleConfig` shape is pinned in Artifacts below — the wiring plan (29-10) relies
on these field names.

Purpose: single source of resolved picker config, fast-check-proven byte-identical on omission
(BT-02 leakage-oracle correctness).
Output: `resolvePickerRuleConfig` exported through the picker barrel.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-runtime-rule-settings-curated-20-knob-settings-surface-entry/29-CONTEXT.md
@.planning/phases/29-runtime-rule-settings-curated-20-knob-settings-surface-entry/29-RESEARCH.md
@.claude/rules/tdd.md
@.claude/rules/typescript.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: picker/domain/rule-config.ts — PickerRuleConfig + resolvePickerRuleConfig (fast-check)</name>
  <read_first>
    - packages/core/src/picker/domain/rules.ts (the 9 WEIGHT_* + DEBIT_IDEAL_MIN/MAX constants)
    - packages/core/src/picker/domain/candidate-selection.ts (DELTA_BAND_MIN/MAX, FRONT_DTE_MIN/MAX, BACK_DTE_MIN_GAP/MAX_GAP)
    - packages/core/src/picker/domain/entry-gate.ts (resolveVixLadder from 29-04; VixTier / VixLadderRow types)
    - packages/core/src/picker/domain/sizing.ts (DEFAULT_TIER_CONTRACTS exported by 29-04)
    - packages/core/src/picker/domain/brakes.ts (MAX_OPEN_CALENDARS)
    - packages/core/src/picker/domain/scoring.ts (BreakdownCriterion enum — the 9 weight keys)
    - packages/core/src/picker/index.ts (barrel export idiom)
  </read_first>
  <behavior>
    - resolvePickerRuleConfig() → PickerRuleConfig where weights = the 9 WEIGHT_* values, deltaBand = {min: -0.49, max: -0.3}, frontDte = {min: 21, max: 36}, backDteGap = {min: 15, max: 90}, debitBand = {idealMin: 3200, idealMax: 5000}, vixLadder = resolveVixLadder(), sizingContracts = DEFAULT_TIER_CONTRACTS, maxOpenCalendars = 6
    - resolvePickerRuleConfig({ maxOpenCalendars: 8 }) overrides only that field
    - resolvePickerRuleConfig({ weights: {...9...} }) replaces the weights map
    - resolvePickerRuleConfig({ vixLadder: { normalMin: 14 } }) yields a rebuilt contiguous ladder
    - idempotent: feeding a config's own values back as overrides yields the same config
  </behavior>
  <action>
    Create `packages/core/src/picker/domain/rule-config.ts`. Define the core-local
    `PickerRuleOverrides` type (structural mirror of the contract `picker` group) and the pinned
    `PickerRuleConfig` (see Artifacts). Implement `resolvePickerRuleConfig(overrides?:
    PickerRuleOverrides): PickerRuleConfig`: each scalar defaults `overrides?.field ?? CONSTANT`;
    `weights` merges per-criterion `?? WEIGHT_*`; `vixLadder` = `resolveVixLadder(overrides?.
    vixLadder)`; `sizingContracts` = `{ ...DEFAULT_TIER_CONTRACTS, ...overrides?.sizingContracts }`.
    Import only sibling `./` domain modules + `@morai/shared`. Export from `picker/index.ts`
    barrel. Write fast-check property tests: (1) omission deep-equals the all-constants config;
    (2) merge is idempotent; (3) any single override changes exactly its field. RED first, GREEN.
  </action>
  <verify>
    <automated>bunx vitest run packages/core/src/picker/domain/rule-config.test.ts</automated>
  </verify>
  <done>resolvePickerRuleConfig returns the pinned shape; omission byte-identical; idempotent; barrel-exported.</done>
  <acceptance_criteria>
    - `bunx vitest run packages/core/src/picker/domain/rule-config.test.ts` passes
    - Omission fast-check property: every field equals its named constant (no fresh literals)
    - Idempotency property holds; single-field overrides are isolated
    - rule-config.ts imports only picker `domain/` siblings + `@morai/shared` (no contracts, no cross-context)
    - `resolvePickerRuleConfig` + `PickerRuleConfig` + `PickerRuleOverrides` exported from `packages/core/src/picker/index.ts`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| override values → resolved picker config | The merged config drives the entire picker universe + scoring at consumption time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-05 | Tampering | Any resolved default diverging from its constant breaks BT-02 | high | mitigate | fast-check omission property proves every field equals its constant |

No package installs — T-29-SC not applicable.
</threat_model>

<verification>
- rule-config test file green (fast-check).
- `bun run typecheck && bun run lint` clean.
</verification>

<success_criteria>
One pure picker merge function producing a fully-resolved, byte-identical-on-omission config the
worker wiring destructures directly.
</success_criteria>

<artifacts>
## Artifacts this plan produces
- `PickerRuleOverrides` (core-local type)
- `PickerRuleConfig` — pinned shape:
  `{ weights: Record<BreakdownCriterion, number>; deltaBand: { min: number; max: number };
     frontDte: { min: number; max: number }; backDteGap: { min: number; max: number };
     debitBand: { idealMin: number; idealMax: number };
     vixLadder: ReadonlyArray<VixLadderRow>; sizingContracts: Record<VixTier, number>;
     maxOpenCalendars: number }`
- `resolvePickerRuleConfig(overrides?)`
- Barrel exports in `packages/core/src/picker/index.ts`
</artifacts>

<output>
Create `.planning/phases/29-runtime-rule-settings-curated-20-knob-settings-surface-entry/29-07-SUMMARY.md` when done
</output>
