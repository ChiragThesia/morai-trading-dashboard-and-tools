---
phase: 29-runtime-rule-settings
plan: 06
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/core/src/analytics/domain/regime.ts
  - packages/core/src/analytics/domain/regime.test.ts
  - packages/core/src/analytics/domain/rule-config.ts
  - packages/core/src/analytics/domain/rule-config.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "The four bandX functions accept an optional {warn,crisis} threshold pair; omission uses today's module constants"
    - "resolveRegimeRuleConfig(overrides?) produces the four threshold pairs; omission reproduces regime.ts defaults"
    - "Every pre-existing regime test passes UNMODIFIED"
  artifacts:
    - packages/core/src/analytics/domain/regime.ts (optional thresholds on all four band functions)
    - packages/core/src/analytics/domain/rule-config.ts (RegimeRuleOverrides + RegimeRuleConfig + resolveRegimeRuleConfig)
  key_links:
    - "resolveRegimeRuleConfig's four pairs feed getRegimeBoard's band calls at server request time (29-12)"
---

<objective>
Give the analyzer regime board a runtime-override seam for its four warn/crisis threshold pairs
(VIX/VIX3M term structure 0.9/0.95, VVIX 100/115, VIX9D ratio 1.0/1.1, HY OAS 3.0/5.0 —
CONTEXT.md "Analyzer regime bands"). The four `bandX(value)` functions currently read
module-PRIVATE constants; add an optional `thresholds` parameter defaulting to those constants,
and add the pure `resolveRegimeRuleConfig(overrides?)` merge function in the analytics context's
own `domain/rule-config.ts`.

Purpose: unblock the server regime-wiring (29-12), which reads overrides fresh per
GET /api/analytics/regime (and MCP get_regime) request — the SIMPLEST of the three engines
(no worker job, no persisted snapshot; changes take effect on the very next read, RESEARCH
Pitfall 3).
Output: four band functions with optional thresholds + `resolveRegimeRuleConfig`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-runtime-rule-settings-curated-20-knob-settings-surface-entry/29-CONTEXT.md
@.planning/phases/29-runtime-rule-settings-curated-20-knob-settings-surface-entry/29-RESEARCH.md
@.claude/rules/tdd.md
@.claude/rules/typescript.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: regime.ts — optional {warn,crisis} threshold param on all four band functions</name>
  <read_first>
    - packages/core/src/analytics/domain/regime.ts (VIX_TERM_STRUCTURE_WARN/CRISIS, VVIX_WARN/CRISIS, VIX9D_RATIO_WARN/CRISIS, HY_OAS_WARN/CRISIS module-private consts; bandVixTermStructure / bandVvix / bandVix9dRatio / bandHyOas single-arg functions)
    - packages/core/src/analytics/domain/regime.test.ts (fast-check monotonicity + boundary assertions — must stay green unmodified)
  </read_first>
  <behavior>
    - bandVvix(level) with no thresholds → identical band to today at every boundary (99→calm, 100→warning, 115→crisis)
    - bandVvix(level, { warn: 90, crisis: 110 }) bands at the overridden edges
    - each of the four functions keeps its monotonic non-decreasing property under any thresholds
  </behavior>
  <action>
    Export the eight threshold constants (or keep them private but referenced as defaults). Add an
    optional second parameter `thresholds?: { readonly warn: number; readonly crisis: number }` to
    each of `bandVixTermStructure`, `bandVvix`, `bandVix9dRatio`, `bandHyOas`, using
    `thresholds?.crisis ?? <CRISIS_CONST>` and `thresholds?.warn ?? <WARN_CONST>` in the existing
    `>= crisis → "crisis"`, `>= warn → "warning"`, else `"calm"` ladder. Do not change the band
    ordering or the RegimeBand type. Add omission-regression + overridden-boundary tests. RED
    first, then GREEN.
  </action>
  <verify>
    <automated>bunx vitest run packages/core/src/analytics/domain/regime.test.ts</automated>
  </verify>
  <done>All four band functions accept optional thresholds; omission byte-identical; existing regime tests green unmodified.</done>
  <acceptance_criteria>
    - `bunx vitest run packages/core/src/analytics/domain/regime.test.ts` passes
    - Each default reads the named WARN/CRISIS constant, not a literal
    - A new test proves omission → identical band at each boundary for all four functions
    - The pre-existing monotonicity fast-check property still holds and is unmodified
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: analytics/domain/rule-config.ts — RegimeRuleConfig + resolveRegimeRuleConfig</name>
  <read_first>
    - packages/core/src/analytics/domain/regime.ts (the eight defaults + the four indicator ids/order from Task 1)
    - packages/core/src/picker/domain/scoring.ts (the Partial-override `?? CONSTANT` idiom to mirror)
  </read_first>
  <behavior>
    - resolveRegimeRuleConfig() → { vixTermStructure:{warn:0.9,crisis:0.95}, vvix:{warn:100,crisis:115}, vix9dRatio:{warn:1.0,crisis:1.1}, hyOas:{warn:3.0,crisis:5.0} }
    - resolveRegimeRuleConfig({ vvixWarn: 90 }) overrides only vvix.warn, all else default
  </behavior>
  <action>
    Create `packages/core/src/analytics/domain/rule-config.ts`. Define a core-local
    `RegimeRuleOverrides` type (structural mirror of the contract's `regime` group:
    vixTermStructureWarn?/Crisis?, vvixWarn?/Crisis?, vix9dRatioWarn?/Crisis?, hyOasWarn?/Crisis?)
    and `RegimeRuleConfig` = the four `{ warn, crisis }` pairs. Implement
    `resolveRegimeRuleConfig(overrides?)` applying `overrides?.vvixWarn ?? VVIX_WARN` etc. per
    field. Import only `./regime.ts` + `@morai/shared`. Tests: omission deep-equals the four
    default pairs; single-field override touches exactly that field. RED first, then GREEN.
  </action>
  <verify>
    <automated>bunx vitest run packages/core/src/analytics/domain/rule-config.test.ts</automated>
  </verify>
  <done>resolveRegimeRuleConfig merges overrides over the four pairs; omission byte-identical.</done>
  <acceptance_criteria>
    - `bunx vitest run packages/core/src/analytics/domain/rule-config.test.ts` passes
    - Omission returns the four default pairs deep-equal to regime.ts constants
    - rule-config.ts imports only `./regime.ts` and `@morai/shared`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| override values → regime band classification | Overridden thresholds change calm/warning/crisis banding shown on the board |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-05 | Tampering | Band default diverging from constant breaks byte-identical omission | high | mitigate | Defaults read the named WARN/CRISIS constants; omission-regression tests |
| T-29-09 | Tampering | Non-numeric/degenerate threshold breaks banding | low | accept | Numeric bounds enforced at the contract (29-02); band functions are total over the reals |

No package installs — T-29-SC not applicable.
</threat_model>

<verification>
- Both analytics-domain test files green.
- Full `bun run test` shows pre-existing regime suite unmodified and passing.
- `bun run typecheck && bun run lint` clean.
</verification>

<success_criteria>
The four regime thresholds are overridable via a pure config; omission reproduces today's board
banding exactly; the merge function stays inside the analytics bounded context.
</success_criteria>

<artifacts>
## Artifacts this plan produces
- Optional `thresholds` param on `bandVixTermStructure` / `bandVvix` / `bandVix9dRatio` / `bandHyOas`
- `RegimeRuleOverrides` (core-local type) / `RegimeRuleConfig`
- `resolveRegimeRuleConfig(overrides?)`
</artifacts>

<output>
Create `.planning/phases/29-runtime-rule-settings-curated-20-knob-settings-surface-entry/29-06-SUMMARY.md` when done
</output>
