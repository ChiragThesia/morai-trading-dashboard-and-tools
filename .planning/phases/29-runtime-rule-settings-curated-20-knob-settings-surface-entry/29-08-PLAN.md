---
phase: 29-runtime-rule-settings
plan: 08
type: execute
wave: 3
depends_on: ["29-01", "29-02", "29-09"]
files_modified:
  - packages/adapters/src/postgres/schema.ts
  - packages/adapters/src/postgres/migrations/0022_rule_overrides.sql
  - packages/adapters/src/postgres/repos/rule-overrides.ts
  - packages/adapters/src/memory/rule-overrides.ts
  - packages/adapters/src/postgres/repos/rule-overrides.contract.test.ts
  - packages/adapters/src/memory/rule-overrides.contract.test.ts
  - packages/adapters/src/index.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "A rule_overrides row round-trips: write a partial blob, read it back Zod-parsed, delete a group's keys"
    - "The postgres repo and the memory twin pass the SAME contract test"
    - "The stored JSONB is Zod-parsed via the contract on BOTH write and read"
  artifacts:
    - packages/adapters/src/postgres/schema.ts (ruleOverrides pgTable)
    - packages/adapters/src/postgres/migrations/0022_rule_overrides.sql
    - packages/adapters/src/postgres/repos/rule-overrides.ts
    - packages/adapters/src/memory/rule-overrides.ts
  key_links:
    - "The repo exports functions satisfying ForReadingRuleOverrides / ForWritingRuleOverrides (29-09)"
---

<objective>
Persist the single JSONB overrides row and implement its repo pair. Follow the `broker_tokens`
singleton convention (fixed literal primary key, no DB CHECK) and the
`calendar-event-annotations` repo pattern (`onConflictDoUpdate` — settings are editable anytime,
not append-only). The stored blob is Zod-parsed via the 29-02 contract on BOTH read and write
(T-19-10 precedent). Ship the in-memory twin in the same plan (architecture rule 8).

Purpose: the durable storage backing `ForReadingRuleOverrides`/`ForWritingRuleOverrides` (29-09),
consumed fresh by the worker/server (29-10/11/12) and mutated by the PUT surface (29-13).
Output: `ruleOverrides` table + migration 0022 + postgres repo + memory twin + contract tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-runtime-rule-settings-curated-20-knob-settings-surface-entry/29-CONTEXT.md
@.planning/phases/29-runtime-rule-settings-curated-20-knob-settings-surface-entry/29-RESEARCH.md
@.planning/phases/29-runtime-rule-settings-curated-20-knob-settings-surface-entry/29-PATTERNS.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/tdd.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: [BLOCKING] ruleOverrides table + migration 0022 (generate + apply locally)</name>
  <read_first>
    - packages/adapters/src/postgres/schema.ts (brokerTokens singleton table at ~218-241 — the pattern: text primary key, no CHECK, `.enableRLS()`)
    - packages/adapters/src/postgres/migrations/0021_backtest_runs.sql (latest migration format; next number is 0022)
    - packages/adapters/src/postgres/migrations/meta (drizzle journal — regenerated by the generate script)
  </read_first>
  <action>
    Add a `ruleOverrides` pgTable to `schema.ts`: `id: text("id").primaryKey()` (app always writes
    the literal `"default"` — singleton by convention, no DB CHECK, mirrors `broker_tokens.app_id`),
    `overrides: jsonb("overrides").notNull()` (the partial deltas blob), `updatedAt:
    timestamp("updated_at", { withTimezone: true }).notNull().defaultNow()`. Call `.enableRLS()`
    like the sibling tables. Generate migration `0022_rule_overrides.sql` via the repo's
    drizzle-kit generate script (do not hand-write the journal). THEN apply it locally with
    `bun run migrate`. KNOWN GOTCHA (RESEARCH Pitfall 7): `bun run migrate` runs
    `bootWorkerConfig()` which Zod-validates ALL worker env (SIDECAR_URL, etc.) before applying —
    ensure the full worker env is set in the local shell/.env first, or apply via a
    testcontainer. This is [BLOCKING]: the migration must be generated AND applied before Task 2's
    contract tests run. Do NOT plan or run any direct prod DB push — prod migration applies at deploy.
  </action>
  <verify>
    <automated>test -f packages/adapters/src/postgres/migrations/0022_rule_overrides.sql && bunx vitest run packages/adapters/src/postgres/schema.test.ts 2>/dev/null || bun run typecheck</automated>
  </verify>
  <done>ruleOverrides table defined; 0022 migration generated and applied to the local DB without error.</done>
  <acceptance_criteria>
    - `packages/adapters/src/postgres/migrations/0022_rule_overrides.sql` exists and creates the `rule_overrides` table with a text PK, a `jsonb` overrides column, and `updated_at`
    - The drizzle journal (`migrations/meta`) includes the 0022 entry
    - `bun run migrate` completes locally without error (worker env set)
    - No prod DB push task exists
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: postgres repo + memory twin + shared contract test</name>
  <read_first>
    - packages/adapters/src/postgres/repos/calendar-event-annotations.ts (the onConflictDoUpdate upsert + Zod-parse-on-read/write pattern)
    - packages/adapters/src/memory/calendar-event-annotations.ts (the memory-twin class wrapping a Map, mirroring the port)
    - packages/adapters/src/memory/calendar-event-annotations.contract.test.ts (the shared-contract-test shape both repos must pass)
    - packages/core/src/settings/application/ports.ts (ForReadingRuleOverrides / ForWritingRuleOverrides from 29-09 — the signatures the repo satisfies)
    - packages/contracts/src/rule-settings.ts (the Zod schema to parse the stored blob through on read + write)
    - packages/adapters/src/index.ts (barrel — how repo factories are exported)
  </read_first>
  <action>
    Create `postgres/repos/rule-overrides.ts` exporting `makePostgresRuleOverridesRepo(db)` with a
    `readRuleOverrides` (SELECT the `"default"` row; if absent return `ok({})`; if present parse
    `overrides` through the contract Zod schema — a corrupt blob surfaces a StorageError, never a
    silent bad-config) and a `writeRuleOverrides(overrides)` (Zod-parse the incoming blob, then
    `insert(...).onConflictDoUpdate({ target: id, set: { overrides, updatedAt } })` with id
    `"default"`). Create `memory/rule-overrides.ts` `makeMemoryRuleOverridesRepo()` twin over an
    in-memory value. Both export functions structurally satisfying the 29-09 ports. Write ONE
    shared contract test exercised against BOTH repos (testcontainers Postgres for the pg one, per
    the Postgres-repos TDD rule — SQL never mocked): write a partial blob → read it back equal →
    overwrite with a group removed → read back the smaller blob → confirm a corrupt stored blob
    read surfaces an error. Export both factories from the adapters barrel.
  </action>
  <verify>
    <automated>bunx vitest run packages/adapters/src/postgres/repos/rule-overrides.contract.test.ts packages/adapters/src/memory/rule-overrides.contract.test.ts</automated>
  </verify>
  <done>Both repos pass the same round-trip contract test; JSONB is Zod-parsed on read + write; barrel exports both factories.</done>
  <acceptance_criteria>
    - Both contract test files pass (postgres via testcontainers, memory twin)
    - Read of an absent row returns `ok({})`; a corrupt stored blob surfaces a StorageError
    - Write uses `onConflictDoUpdate` on the fixed `"default"` key (upsert, not append)
    - Repo factories exported from `packages/adapters/src/index.ts`
    - `bun run typecheck && bun run lint` clean
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored JSONB → parsed config | A corrupt/tampered stored blob must never silently become live config |
| DB migration → live schema | A bad migration could break unrelated tables |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-12 | Tampering | Corrupt stored overrides blob read as valid config | high | mitigate | Zod-parse the JSONB on read; a bad blob surfaces StorageError, never silently applied |
| T-29-13 | Denial of Service | Migration applied to prod out-of-band | medium | mitigate | No prod-push task; prod migration applies at deploy only |

No package installs — T-29-SC not applicable (drizzle-orm/zod already installed).
</threat_model>

<verification>
- Both contract test files green; migration 0022 present + applied locally.
- `bun run typecheck && bun run lint` clean.
- Full `bun run test` shows no regressions in adapters.
</verification>

<success_criteria>
The single-row overrides table + repo pair persists and round-trips partial override blobs,
Zod-guarded on both sides, with the memory twin passing the identical contract.
</success_criteria>

<artifacts>
## Artifacts this plan produces
- `ruleOverrides` pgTable (`rule_overrides`) + migration `0022_rule_overrides.sql`
- `makePostgresRuleOverridesRepo(db)` (readRuleOverrides / writeRuleOverrides)
- `makeMemoryRuleOverridesRepo()` (memory twin)
- Adapters barrel exports
</artifacts>

<output>
Create `.planning/phases/29-runtime-rule-settings-curated-20-knob-settings-surface-entry/29-08-SUMMARY.md` when done
</output>
