---
phase: 29-runtime-rule-settings
plan: 10
type: tdd
wave: 4
depends_on: ["29-07", "29-08", "29-09"]
files_modified:
  - packages/core/src/picker/application/computePickerSnapshot.ts
  - packages/core/src/picker/application/computePickerSnapshot.test.ts
  - apps/worker/src/main.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "compute-picker reads rule overrides FRESH each run and resolves a PickerRuleConfig before selecting/scoring"
    - "The resolved config threads into selectCandidates, scoreCalendarCandidates (weights), resolveEntryGate ladder, resolveSizingTier, and maxOpenTripped"
    - "The persisted snapshot ruleSet stamps EFFECTIVE weights, not the compile-time RULE_SET_METADATA weights"
    - "With no overrides stored, the produced snapshot is byte-identical to today's"
  artifacts:
    - packages/core/src/picker/application/computePickerSnapshot.ts (readRuleOverrides dep + config threading + effective ruleSet)
    - apps/worker/src/main.ts (readRuleOverrides wired into the compute-picker deps)
  key_links:
    - "readRuleOverrides port (29-09) ← makePostgresRuleOverridesRepo (29-08) ← composition root; resolvePickerRuleConfig (29-07) called inside the use-case body, never cached at boot"
---

<objective>
Wire the resolved picker config into the compute-picker job. Add `readRuleOverrides` to
`ComputePickerSnapshotDeps`, call it FRESH inside the use-case body (mirrors the existing
`readMacroObservations()` fresh-per-invocation pattern — so a mid-day change takes effect next
cycle, never cached at boot), resolve `resolvePickerRuleConfig(overrides.picker)`, and thread the
config into every seam. Also fix RESEARCH Pitfall 4: stamp the snapshot `ruleSet` with the
EFFECTIVE weights actually used, not the compile-time `RULE_SET_METADATA` weights.

Threading map (call sites in computePickerSnapshot.ts):
- `selectCandidates(chain, events, { r, q, effectiveDeltaMin, deltaMax: config.deltaBand.max,
  frontDteMin: config.frontDte.min, frontDteMax: config.frontDte.max, backDteMinGap:
  config.backDteGap.min, backDteMaxGap: config.backDteGap.max })` (both primary + event bucket)
- `scoreCalendarCandidates(..., { r, q, realizedVol20, slopeHistory, weights: config.weights })`
- `resolveEntryGate({ ..., vixLadder: config.vixLadder })`
- `maxOpenTripped(openCount, config.maxOpenCalendars)`
- `toPickerSizing`/`resolveSizingTier(gate.vix, { ladder: config.vixLadder-derived, contracts:
  config.sizingContracts })`
- `autoTuneTargetDelta(gate.vix, config.vixLadder)` floor (feed the resolved ladder so the
  deep-edge tilt tracks overridden boundaries)
- `debitFitFraction` band flows through scoring — pass `config.debitBand` via the scoring params
  path that reaches `debitFitFraction` (add a `debitBand` field to ScoringParams if needed, or
  thread through selectCandidates — follow the existing `weights?` precedent for the plumbing)
- `ruleSet: RULE_SET_METADATA.map(rule => ({ ...rule, weight: config.weights[rule.id] ??
  rule.weight }))` — effective-value projection

Purpose: overrides actually change live picker behavior + the methodology panel reflects them.
Output: config-threaded compute-picker + worker composition wiring, byte-identical with no overrides.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-runtime-rule-settings-curated-20-knob-settings-surface-entry/29-CONTEXT.md
@.planning/phases/29-runtime-rule-settings-curated-20-knob-settings-surface-entry/29-RESEARCH.md
@.planning/phases/29-runtime-rule-settings-curated-20-knob-settings-surface-entry/29-PATTERNS.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/tdd.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: computePickerSnapshot — readRuleOverrides dep, resolve + thread config, effective ruleSet</name>
  <read_first>
    - packages/core/src/picker/application/computePickerSnapshot.ts (Deps at ~92; readMacroObservations fresh-read at ~439; the resolveEntryGate call at ~460; selectCandidates at ~507 + selectEventCandidates at ~530; scoreCalendarCandidates at ~513 + scoreEventCandidates at ~535; toPickerSizing/resolveSizingTier at ~283/~623; maxOpenTripped at ~453; ruleSet stamp at ~610)
    - packages/core/src/picker/domain/rule-config.ts (resolvePickerRuleConfig + PickerRuleConfig from 29-07)
    - packages/core/src/picker/domain/scoring.ts (ScoringParams — the weights seam + where debitFitFraction is called inside scoreOne, so debitBand plumbing lands correctly)
    - packages/core/src/picker/domain/candidate-selection.ts (the deltaMax/frontDte params from 29-03; autoTuneTargetDelta)
    - packages/core/src/settings/application/ports.ts (ForReadingRuleOverrides from 29-09)
    - packages/core/src/picker/application/computePickerSnapshot.test.ts (existing snapshot assertions — must stay green unmodified)
  </read_first>
  <behavior>
    - With readRuleOverrides returning ok({}) (no overrides), the produced snapshot is deep-equal to today's for the same fixture inputs (byte-identical omission)
    - With a picker override (e.g. maxOpenCalendars: 8, a weight shift), the gate/universe/score reflect it AND the snapshot.ruleSet weights equal the effective weights
    - readRuleOverrides is called inside the returned async body (fresh per run), not in the factory closure
    - a readRuleOverrides error degrades safely (fail-closed to defaults OR propagate as the existing gate-read-error posture — match the file's existing degradation convention; document which)
  </behavior>
  <action>
    Add `readonly readRuleOverrides: ForReadingRuleOverrides` to `ComputePickerSnapshotDeps`. At
    the top of the returned async body, call it fresh and `const config =
    resolvePickerRuleConfig(overridesResult.ok ? overridesResult.value.picker : undefined)`.
    Thread `config` into every seam per the threading map in the objective, including passing
    `weights: config.weights` into BOTH scoreCalendarCandidates and scoreEventCandidates
    ScoringParams (currently omitted). Replace the `ruleSet: RULE_SET_METADATA.map(...)` stamp
    with the effective-weight projection (`weight: config.weights[rule.id] ?? rule.weight`) —
    leave `RULE_SET_METADATA` itself untouched (rules.test.ts asserts the DEFAULTS sum to 100).
    Extend the existing test with (1) a byte-identical no-override snapshot and (2) an
    overridden-config snapshot asserting the effective ruleSet weights + a changed knob effect.
    RED first, then GREEN.
  </action>
  <verify>
    <automated>bunx vitest run packages/core/src/picker/application/computePickerSnapshot.test.ts</automated>
  </verify>
  <done>Overrides read fresh + threaded through all seams; ruleSet stamps effective weights; no-override snapshot byte-identical; existing tests green unmodified.</done>
  <acceptance_criteria>
    - `bunx vitest run packages/core/src/picker/application/computePickerSnapshot.test.ts` passes
    - `readRuleOverrides` is invoked inside the async use-case body (not the factory closure) — fresh per run
    - `weights: config.weights` is passed into scoreCalendarCandidates AND scoreEventCandidates
    - The stamped `ruleSet` uses effective weights; `RULE_SET_METADATA` constant is unchanged in source
    - A no-override run produces a snapshot deep-equal to the pre-change output
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: worker main.ts — wire readRuleOverrides into the compute-picker deps</name>
  <read_first>
    - apps/worker/src/main.ts (the makeComputePickerSnapshotUseCase deps object at ~572; how macroObsRepo/other repos are constructed at ~202 and injected; the makePostgresRuleOverridesRepo import from @morai/adapters added in 29-08)
    - packages/adapters/src/index.ts (the makePostgresRuleOverridesRepo export)
  </read_first>
  <action>
    Construct the postgres rule-overrides repo once at boot (like `macroObsRepo`) and pass its
    `readRuleOverrides` function into the `makeComputePickerSnapshotUseCase({ ... })` deps object.
    Composition-root wiring only — no business logic. (The fresh-per-run read happens inside the
    use-case body from Task 1; the boot-time construction of the repo function is correct and does
    NOT cache the data.)
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <done>Worker compose builds the rule-overrides repo and injects readRuleOverrides into the compute-picker use-case.</done>
  <acceptance_criteria>
    - `apps/worker/src/main.ts` constructs `makePostgresRuleOverridesRepo(db)` and passes `readRuleOverrides` into the compute-picker deps
    - `bun run typecheck` clean (deps object satisfies ComputePickerSnapshotDeps)
    - No business logic added to main.ts
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored overrides → live picker snapshot | Overridden knobs alter the scored/persisted candidate universe |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-05 | Tampering | No-override path diverging from today (BT-02 oracle) | high | mitigate | Byte-identical no-override snapshot test |
| T-29-14 | Tampering | Stale ruleSet stamp misrepresenting the weights used | medium | mitigate | ruleSet stamps effective weights actually passed to scoring |
| T-29-15 | Denial of Service | readRuleOverrides failure crashing the job | medium | mitigate | Read error degrades to defaults / existing gate-read-error posture, documented |

No package installs — T-29-SC not applicable.
</threat_model>

<verification>
- computePickerSnapshot test green (both no-override + overridden cases).
- `bun run typecheck && bun run lint` clean.
- Full `bun run test` shows the Phase 27 backtest reuse suite unmodified and passing.
</verification>

<success_criteria>
compute-picker reads overrides fresh, applies them across every picker seam, and stamps the
effective ruleSet — with no-override runs reproducing today's snapshot exactly.
</success_criteria>

<artifacts>
## Artifacts this plan produces
- `ComputePickerSnapshotDeps.readRuleOverrides` (new dep)
- config-threaded compute-picker body + effective ruleSet stamp
- worker composition-root wiring for the rule-overrides repo
</artifacts>

<output>
Create `.planning/phases/29-runtime-rule-settings-curated-20-knob-settings-surface-entry/29-10-SUMMARY.md` when done
</output>
