---
phase: 29-runtime-rule-settings
plan: 11
type: tdd
wave: 5
depends_on: ["29-05", "29-08", "29-09", "29-10"]
files_modified:
  - packages/core/src/exits/application/computeExitAdvice.ts
  - packages/core/src/exits/application/computeExitAdvice.test.ts
  - apps/worker/src/main.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "compute-exit-advice reads rule overrides FRESH each run and resolves an ExitRuleConfig before evaluating"
    - "evaluateExit is called with the resolved config as its 4th argument"
    - "With no overrides, verdicts are byte-identical to today's"
    - "A rung change takes effect on the next compute-exit-advice cycle (worker cadence), not on the next page load"
  artifacts:
    - packages/core/src/exits/application/computeExitAdvice.ts (readRuleOverrides dep + resolved config passed to evaluateExit)
    - apps/worker/src/main.ts (readRuleOverrides wired into the compute-exit-advice deps)
  key_links:
    - "resolveExitRuleConfig (29-05) called inside the use-case body; readRuleOverrides (29-09) ← makePostgresRuleOverridesRepo (29-08); wired at worker composition root, NOT server (RESEARCH Pitfall 2)"
---

<objective>
Wire the resolved exit config into the compute-exit-advice WORKER job (RESEARCH Pitfall 2: the
TAKE/STOP rungs are consumed by `computeExitAdvice` invoked from the worker's compute-picker job
chain, NOT by the server HTTP `getExitAdvice` path — that path re-derives from an
already-persisted verdict row and never calls `evaluateExit`). Add `readRuleOverrides` to
`ComputeExitAdviceDeps`, read it FRESH inside the use-case body, resolve
`resolveExitRuleConfig(overrides.exits)`, and pass it as the 4th argument to `evaluateExit`.

Purpose: rung overrides actually change live exit verdicts, on the worker cadence.
Output: config-threaded compute-exit-advice + worker composition wiring; byte-identical with no
overrides. This plan wires into `apps/worker/src/main.ts` AFTER 29-10 (shared file — sequential).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-runtime-rule-settings-curated-20-knob-settings-surface-entry/29-CONTEXT.md
@.planning/phases/29-runtime-rule-settings-curated-20-knob-settings-surface-entry/29-RESEARCH.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/tdd.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: computeExitAdvice — readRuleOverrides dep + resolved config as evaluateExit 4th arg</name>
  <read_first>
    - packages/core/src/exits/application/computeExitAdvice.ts (ComputeExitAdviceDeps at ~41; the per-position loop calling evaluateExit at ~131; the deps.now() fresh-read idiom at ~91)
    - packages/core/src/exits/domain/rule-config.ts (resolveExitRuleConfig + ExitRuleConfig from 29-05)
    - packages/core/src/exits/domain/evaluate-exit.ts (evaluateExit's new optional 4th config arg from 29-05)
    - packages/core/src/settings/application/ports.ts (ForReadingRuleOverrides from 29-09)
    - packages/core/src/exits/application/computeExitAdvice.test.ts (existing verdict assertions — must stay green unmodified)
  </read_first>
  <behavior>
    - readRuleOverrides returning ok({}) → verdicts deep-equal to today for the same fixtures (byte-identical omission)
    - an exits override (e.g. plus5Arm: 0.03) changes the fired rung on the next run
    - readRuleOverrides is called once per run inside the body (fresh), before the per-position loop; the same resolved config is used for every position that run
    - a readRuleOverrides error degrades to defaults (verdicts still computed with constant rungs) OR propagates per the file's existing read-error posture (document which)
  </behavior>
  <action>
    Add `readonly readRuleOverrides: ForReadingRuleOverrides` to `ComputeExitAdviceDeps`. At the
    top of the returned async body (alongside the other reads), call it fresh and `const
    exitConfig = resolveExitRuleConfig(overridesResult.ok ? overridesResult.value.exits :
    undefined)`. Pass `exitConfig` as the 4th argument to `evaluateExit(position, context,
    previousVerdict, exitConfig)`. Extend the test with a no-override byte-identical run and an
    overridden-rung case. RED first, then GREEN.
  </action>
  <verify>
    <automated>bunx vitest run packages/core/src/exits/application/computeExitAdvice.test.ts</automated>
  </verify>
  <done>Overrides read fresh once per run + passed to evaluateExit; no-override verdicts byte-identical; existing tests green unmodified.</done>
  <acceptance_criteria>
    - `bunx vitest run packages/core/src/exits/application/computeExitAdvice.test.ts` passes
    - `readRuleOverrides` invoked inside the async body (fresh per run), before the per-position loop
    - `evaluateExit` receives the resolved config as its 4th argument
    - A no-override run produces verdicts deep-equal to the pre-change output
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: worker main.ts — wire readRuleOverrides into the compute-exit-advice deps</name>
  <read_first>
    - apps/worker/src/main.ts (makeComputeExitAdviceUseCase at ~669; the rule-overrides repo constructed in 29-10 — reuse the SAME instance, do not re-construct)
  </read_first>
  <action>
    Pass the already-constructed rule-overrides repo's `readRuleOverrides` into the
    `makeComputeExitAdviceUseCase({ ... })` deps object. Reuse the single repo instance created in
    29-10's main.ts wiring. Composition-root only, no business logic.
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <done>Worker compose injects readRuleOverrides into the compute-exit-advice use-case, reusing the shared repo.</done>
  <acceptance_criteria>
    - `apps/worker/src/main.ts` passes `readRuleOverrides` into the compute-exit-advice deps
    - The rule-overrides repo is constructed once and shared with the compute-picker wiring
    - `bun run typecheck` clean (deps satisfy ComputeExitAdviceDeps)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored overrides → live exit verdicts | Overridden rungs drive actionable STOP/TAKE advice on real open positions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-05 | Tampering | No-override verdicts diverging from today | high | mitigate | Byte-identical no-override verdict test |
| T-29-16 | Tampering | Rungs wired into the wrong (server GET) path so overrides never take effect | high | mitigate | Wired into ComputeExitAdviceDeps at the worker root, not GetExitAdviceDeps (verified via which use-case imports evaluate-exit) |

No package installs — T-29-SC not applicable.
</threat_model>

<verification>
- computeExitAdvice test green (no-override + overridden).
- `bun run typecheck && bun run lint` clean.
- Confirm `getExitAdvice.ts` (server path) is NOT modified — rungs are a worker-job concern.
</verification>

<success_criteria>
compute-exit-advice reads overrides fresh and applies overridden rungs via evaluateExit's 4th
arg, on the worker cadence, with no-override verdicts identical to today.
</success_criteria>

<artifacts>
## Artifacts this plan produces
- `ComputeExitAdviceDeps.readRuleOverrides` (new dep)
- config-threaded compute-exit-advice body (evaluateExit 4th arg)
- worker composition-root wiring (exit path, shared repo)
</artifacts>

<output>
Create `.planning/phases/29-runtime-rule-settings-curated-20-knob-settings-surface-entry/29-11-SUMMARY.md` when done
</output>
