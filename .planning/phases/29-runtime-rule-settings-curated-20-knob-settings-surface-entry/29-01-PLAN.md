---
phase: 29-runtime-rule-settings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture/stack-decisions.md
  - docs/architecture/rule-overrides.md
  - docs/TOPIC-MAP.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "docs/architecture/ records the rule_overrides table decision BEFORE any schema/migration lands"
    - "The T-28-11 governance override (constants-as-defaults + explicit overrides layer) is documented"
  artifacts:
    - docs/architecture/rule-overrides.md
    - docs/architecture/stack-decisions.md (rule_overrides row added)
    - docs/TOPIC-MAP.md (new doc indexed)
  key_links:
    - "rule-overrides.md ↔ stack-decisions.md decision row ↔ TOPIC-MAP index entry stay cross-referenced"
---

<objective>
Satisfy the workflow "Docs Before Code" rule (`.claude/rules/workflow.md`) for the new
`rule_overrides` table and the runtime-override architecture. This is the FIRST plan of the
phase and gates the schema plan (29-08) — no table lands before the decision is documented.

Records the CONTEXT.md governance override (per CONTEXT `### Governance override`): this phase
overrides Phase 28 decision T-28-11 ("constants file is the only user-editable source of
truth"). The constants remain the DEFAULTS; the `rule_overrides` JSONB row is an explicit,
visible layer on top — merged over defaults at consumption time.

Purpose: architecture docs are the source of truth (rule "Docs Before Code"); code that
contradicts them is a bug in one of them.
Output: a new `docs/architecture/rule-overrides.md`, a decision row in `stack-decisions.md`,
and a TOPIC-MAP index entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-runtime-rule-settings-curated-20-knob-settings-surface-entry/29-CONTEXT.md
@.claude/rules/workflow.md
@.claude/rules/docs.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the rule_overrides decision in stack-decisions.md + a dedicated architecture doc</name>
  <read_first>
    - docs/architecture/stack-decisions.md (the decision table + format to append to)
    - docs/architecture/rule-overrides.md (does not exist yet — create it)
    - docs/docs-on-docs/hemingway-style.md (prose style all docs must follow)
    - .planning/phases/29-runtime-rule-settings-curated-20-knob-settings-surface-entry/29-CONTEXT.md (Governance override section, Storage shape, Excluded Knobs)
    - .planning/phases/29-runtime-rule-settings-curated-20-knob-settings-surface-entry/29-RESEARCH.md (Architectural Responsibility Map, Pattern 1/2)
  </read_first>
  <action>
    Add a decision row to `docs/architecture/stack-decisions.md` for the `rule_overrides` table:
    decision = single-row JSONB overrides layer keyed by a fixed literal id "default" (mirrors
    the `broker_tokens.app_id` singleton convention, no DB CHECK constraint); why = curated ~20
    runtime-editable knobs across the picker, exits, and analytics engines without a per-value
    column explosion; swap cost = drop the row, code defaults remain authoritative. Explicitly
    state this OVERRIDES Phase 28 T-28-11 — constants stay the DEFAULTS, the row is an explicit
    visible layer merged over them at consumption time (worker compute-picker/compute-exit-advice
    job start, server regime request time).

    Create `docs/architecture/rule-overrides.md` (Hemingway style, 20-250 lines) describing: the
    override layer sits between compile-time constants and their consuming pure functions; each
    engine owns its own pure `resolveXConfig(overrides?)` merge function in its own `domain/`
    (never a cross-context settings god-module — architecture rule 7); the merge seam is the
    optional-param-defaulting-to-constant idiom (Pattern 1) so omitting an override reproduces
    today's output byte-identically (required by the Phase 27 backtest leakage oracle); the
    curated knob list and the excluded (code-only) knob list from CONTEXT.md; reset-per-group
    semantics (a `{ group: null }` PUT deletes that group's override keys). Do NOT restate table
    DDL — reference the schema file by path.
  </action>
  <verify>
    <automated>test -f docs/architecture/rule-overrides.md && rg -q "rule_overrides" docs/architecture/stack-decisions.md && rg -q "T-28-11" docs/architecture/rule-overrides.md</automated>
  </verify>
  <done>
    stack-decisions.md has a rule_overrides decision row citing the T-28-11 override;
    rule-overrides.md exists and explains the override-layer architecture and curated/excluded
    knob split.
  </done>
  <acceptance_criteria>
    - `docs/architecture/rule-overrides.md` exists and names all three engine merge functions (picker, exits, regime)
    - `docs/architecture/stack-decisions.md` contains a `rule_overrides` decision row referencing the single-row JSONB + "default" key convention
    - The T-28-11 governance override is stated in prose (constants = defaults, row = explicit layer)
    - The excluded/code-only knob list (normalizers, penalties, hysteresis internals, etc.) is documented as NOT overridable
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Index the new doc in TOPIC-MAP.md</name>
  <read_first>
    - docs/TOPIC-MAP.md (the documentation index — the single source for "what docs exist")
    - docs/architecture/rule-overrides.md (the doc created in Task 1, to describe it)
  </read_first>
  <action>
    Add a TOPIC-MAP entry pointing at `docs/architecture/rule-overrides.md` in the architecture
    section, with a one-line description ("runtime rule overrides: JSONB layer merged over
    compile-time constants at consumption time"). Keep the existing ordering/format of the
    architecture entries.
  </action>
  <verify>
    <automated>rg -q "rule-overrides" docs/TOPIC-MAP.md</automated>
  </verify>
  <done>TOPIC-MAP.md references the new rule-overrides.md doc.</done>
  <acceptance_criteria>
    - `docs/TOPIC-MAP.md` contains a link to `docs/architecture/rule-overrides.md`
    - The entry sits in the architecture section, matching surrounding entry format
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → implementation | Docs are the source of truth; drift between doc and code is the risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-01 | Repudiation | Undocumented override of a prior locked decision (T-28-11) | low | mitigate | This plan documents the override explicitly in stack-decisions.md + rule-overrides.md before code lands |

No package installs in this plan — T-29-SC not applicable (zero external dependencies added, per RESEARCH Package Legitimacy Audit).
</threat_model>

<verification>
- `docs/architecture/rule-overrides.md` exists; `stack-decisions.md` and `TOPIC-MAP.md` updated and cross-referenced.
- `bun run lint` (if it covers markdown) or manual link check passes.
</verification>

<success_criteria>
Docs-before-code obligation met for the `rule_overrides` table and the runtime-override
architecture; downstream schema plan (29-08) is unblocked.
</success_criteria>

<artifacts>
## Artifacts this plan produces
- `docs/architecture/rule-overrides.md` (new doc)
- `rule_overrides` decision row in `docs/architecture/stack-decisions.md`
- TOPIC-MAP index entry for rule-overrides.md
</artifacts>

<output>
Create `.planning/phases/29-runtime-rule-settings-curated-20-knob-settings-surface-entry/29-01-SUMMARY.md` when done
</output>
