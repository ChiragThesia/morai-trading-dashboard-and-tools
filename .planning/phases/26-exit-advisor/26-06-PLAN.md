---
phase: 26-exit-advisor
plan: 06
type: execute
wave: 6
depends_on: ["26-05"]
files_modified:
  - apps/web/src/hooks/useExits.ts
  - apps/web/src/screens/Analyzer.tsx
  - apps/web/src/screens/HeldPositionsPanel.tsx
  - apps/web/src/screens/ExitRulesPanel.tsx
  - apps/web/src/screens/Analyzer.test.tsx
autonomous: true
requirements: [EXIT-07, EXIT-09, EXIT-10]

must_haves:
  truths:
    - "A new full-width 'Held positions' panel below the existing 3-col grid shows one row per open calendar: name + verdict chip (severity-colored) + rule/metric line, per 26-UI-SPEC"
    - "STOP and EXIT — pre-event render on filled/alert chips (red-fill vs amber-fill), distinct from each other; HOLD/TAKE/ROLL render on plain chips — the single fill-vs-no-fill 'is this urgent' signal"
    - "An indicative verdict is FORCED to the INDICATIVE treatment (amber 'AH — indicative' / 'STALE — indicative') regardless of the underlying verdict — an indicative STOP/TAKE never renders in its escalated colors"
    - "changed:true adds a 'CHANGED' marker colored to the verdict's own value color — the entire EXIT-09 alert surface (no toast, no banner); the panel has no action/order affordance (EXIT-10)"
    - "A second 'Exit rules' panel renders the payload's ruleSet in the engine's precedence order (never a client-side copy or re-sort)"
  artifacts:
    - apps/web/src/screens/HeldPositionsPanel.tsx
    - apps/web/src/screens/ExitRulesPanel.tsx
    - apps/web/src/hooks/useExits.ts
  key_links:
    - "useExits(GET /api/exits) → HeldPositionsPanel + ExitRulesPanel — the panel renders the engine payload verbatim (rationale + ruleSet carried in the payload, never hardcoded UI prose)"
    - "indicative flag → forced INDICATIVE chip treatment (the render-time guard that keeps a stale verdict non-actionable)"
---

<objective>
The Analyzer held-positions panel (EXIT-07) + the verdict-change alert surfacing (EXIT-09),
implemented exactly to the APPROVED 26-UI-SPEC: a full-width "Held positions" panel (one row per
open calendar, verdict chips with severity/escalation/indicative treatment + rule-metric line +
ROLL detail) and an "Exit rules" panel rendering the engine's ruleSet in precedence order, placed
below the existing 3-col grid without touching it.

Purpose: the human-facing surface. Entry-methodology symmetry — the exit ruleSet renders the way
the picker's scoring methodology does. Advise + alert only, no order affordance (EXIT-10).

Output: the useExits hook, the two panel components, the Analyzer wiring, and rendering tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

UI + wiring is execute (not TDD), but ships component rendering tests (EXIT-07 test map). Every
visual value comes from 26-UI-SPEC — no new tokens, no new shadcn atoms, no new route.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-exit-advisor/26-CONTEXT.md
@.planning/phases/26-exit-advisor/26-UI-SPEC.md
@packages/contracts/src/exits.ts
@apps/web/src/screens/Analyzer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: useExits hook + HeldPositionsPanel + ExitRulesPanel components (+ rendering tests)</name>
  <files>apps/web/src/hooks/useExits.ts, apps/web/src/screens/HeldPositionsPanel.tsx, apps/web/src/screens/ExitRulesPanel.tsx, apps/web/src/screens/Analyzer.test.tsx</files>
  <read_first>
    - .planning/phases/26-exit-advisor/26-UI-SPEC.md (Color contract verdict-severity palette; Copywriting Contract exact strings; Component & Layout Contract → held-position row anatomy + data-testid convention + Exit rules panel + States)
    - apps/web/src/screens/Analyzer.tsx (usePicker hook null-on-404 cold-start pattern; ScoringMethodologyPanel to mirror for ExitRulesPanel; CandidateCard row anatomy + MetricChip alert + session-badge AH-indicative chip; CandidateRail loading/error/Retry pattern)
    - packages/contracts/src/exits.ts (the exitsResponse payload shape the panels render)
  </read_first>
  <action>
    Create useExits.ts mirroring usePicker (react-query GET /api/exits; queryFn returns null on a
    404 cold-start, distinct from a fetch error). Create HeldPositionsPanel.tsx: a full-width Panel
    titled "Held positions" rendering one non-clickable CandidateCard-style row per open calendar
    per the UI-SPEC anatomy — header line (name/strike/expiries + optional "CHANGED" marker in the
    verdict's own value color + verdict MetricChip right-aligned), rule/metric line
    ("{ruleId} · {metricName} {value}"), session/staleness dot, and a ROLL PlanRow detail line only
    when verdict is ROLL. Chip treatment strictly per the Color contract: HOLD/TAKE/ROLL plain
    MetricChip (txt/up/amber value); STOP alert chip (down, bold); EXIT — pre-event filled-amber
    chip (bg-amber/15 ring-amber/40, bold) — distinct hue from STOP at the same filled weight; when
    indicative:true, FORCE the INDICATIVE treatment (alert chip, amber "AH — indicative" /
    "STALE — indicative") regardless of the underlying verdict. Use the exact verdict label strings
    and the data-testid convention (held-position-{calendarId}, -verdict-, -rule-, -changed-,
    -indicative-, -roll-) from the UI-SPEC. Create ExitRulesPanel.tsx: a Panel titled "Exit rules"
    rendering the payload's ruleSet in its own array (precedence) order — one flat row per entry
    (SectionLabel id/kind + the registry's rationale field verbatim), never a client-side copy or
    re-sort. Add Analyzer.test.tsx cases: held-positions rows render from a fixture payload; an
    indicative STOP renders the INDICATIVE treatment and NOT the escalated STOP colors; a changed
    verdict shows the CHANGED marker; STOP and EXIT — pre-event render on filled chips; the panel
    contains no button/action affordance (EXIT-10); empty and cold-start states render their
    UI-SPEC copy. Use fixtures with distinct timestamps.
    Note: never introduce a violet verdict color (violet is reserved for focus/selection per the
    UI-SPEC lock) and never add a toast/banner — the CHANGED marker + escalated chip fills are the
    entire EXIT-09 alert surface.
  </action>
  <verify>
    <automated>bun run vitest run apps/web/src/screens/Analyzer.test.tsx -t "held positions"</automated>
  </verify>
  <done>The two panel components + useExits render the engine payload per the UI-SPEC (severity/escalation/indicative/changed/ROLL) with correct data-testids; indicative never renders as actionable; no action affordance; rendering tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire both panels into Analyzer.tsx below the 3-col grid (states, no layout change)</name>
  <files>apps/web/src/screens/Analyzer.tsx</files>
  <read_first>
    - apps/web/src/screens/Analyzer.tsx (the existing 3-col grid: Suggested calendars | Risk profile | Why this calendar — the panels mount BELOW it as new sibling sections, no change to those columns)
    - .planning/phases/26-exit-advisor/26-UI-SPEC.md (Placement diagram + States: Loading / Error+Retry / Cold-start / Empty / Loaded)
  </read_first>
  <action>
    In Analyzer.tsx, call useExits and mount HeldPositionsPanel then ExitRulesPanel as new full-width
    sibling sections BELOW the existing 3-col grid (the placement diagram in the UI-SPEC) — do NOT
    alter the existing rail/risk-profile/why-panel columns (PICK-02 "no layout change" guarantee).
    Handle the five states with the UI-SPEC copy: Loading ("Loading exit verdicts…"), Error
    ("Couldn't load exit verdicts." + Retry wired to the query refetch), Cold-start ("Exit advisor
    warming up" / "First verdict pending…") when open calendars exist but the snapshot is null,
    Empty ("No open positions" / "Nothing to advise on…") when there are zero open calendars, and
    Loaded (rows in the payload's own array order, no client-side re-sort). Keep the diff surgical —
    add the two sections and their state handling, nothing else.
  </action>
  <verify>
    <automated>bun run vitest run apps/web/src/screens/Analyzer.test.tsx && bun run typecheck</automated>
  </verify>
  <done>Analyzer renders the two new panels below the unchanged 3-col grid with all five states wired to the UI-SPEC copy; existing columns untouched; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine payload → rendered verdict | The render must never upgrade an indicative verdict into an actionable one, and must never present an order affordance |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-16 | Spoofing | indicative verdict rendered as actionable | high | mitigate | The row forces the INDICATIVE (amber, display-only) chip treatment whenever indicative:true, regardless of the underlying verdict — a test asserts an indicative STOP does not render escalated STOP colors |
| T-26-17 | Elevation of Privilege | order/action affordance in the panel (EXIT-10) | high | mitigate | Panel is display-only — no button, no onSelect, no order surface; a test asserts no action affordance exists in the rendered rows |
| T-26-18 | Tampering | client-side rule copy / re-sort drifting from the engine | low | mitigate | ExitRulesPanel renders the payload's ruleSet (rationale + order) verbatim — never hardcoded UI prose, never re-sorted |
| T-26-SC | Tampering | package installs | low | accept | Zero new packages / no new shadcn atom (26-UI-SPEC Registry Safety: not triggered) |
</threat_model>

<verification>
- `bun run vitest run apps/web/src/screens/Analyzer.test.tsx` green (held-positions + states + indicative-not-actionable + EXIT-10 no-affordance)
- `bun run typecheck && bun run lint` clean
- Human visual UAT (drive via chrome-devtools per standing permission): verdict chips + escalation + CHANGED marker render per 26-UI-SPEC on the Analyzer screen
</verification>

<success_criteria>
The Analyzer shows a held-positions panel with per-calendar verdict chips and the exit ruleSet
(entry-methodology symmetry); STOP/EXIT-pre-event escalate distinctly; indicative verdicts are
never actionable; verdict changes are marked; the advisor surfaces advice with zero execution
affordance.
</success_criteria>

<output>
Create `.planning/phases/26-exit-advisor/26-06-SUMMARY.md` when done.
</output>
