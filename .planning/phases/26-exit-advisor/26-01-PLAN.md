---
phase: 26-exit-advisor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture/exit-rules.md
  - docs/architecture/jobs.md
  - docs/TOPIC-MAP.md
  - packages/core/src/picker/domain/candidate-selection.ts
  - packages/core/src/picker/domain/candidate-selection.test.ts
  - packages/contracts/src/exits.ts
  - packages/contracts/src/exits.test.ts
  - packages/contracts/src/index.ts
  - packages/core/src/exits/domain/types.ts
  - packages/core/src/exits/application/ports.ts
  - packages/core/src/exits/index.ts
  - packages/core/src/index.ts
autonomous: true
requirements: [EXIT-01, EXIT-04, EXIT-06]

must_haves:
  truths:
    - "docs/architecture/exit-rules.md exists and documents the ladder + precedence order + hysteresis before any exits code lands (docs-before-code rule)"
    - "haircutFill(quote, side) is an EXPORTED pure function; picker entry pricing is unchanged (all picker tests stay green)"
    - "The exits contract surface (exitVerdict/heldPosition/exitRuleSetEntry/exitsResponse) parses a verdict payload that always carries ruleId + a raw metric {name,value,threshold} and never a confidence field"
  artifacts:
    - docs/architecture/exit-rules.md
    - packages/contracts/src/exits.ts
    - packages/core/src/exits/domain/types.ts
    - packages/core/src/exits/application/ports.ts
  key_links:
    - "haircutFill export ← ROLL pricing (26-02 evaluator imports it, never re-derives the 0.66 formula)"
    - "contracts/exits.ts exitsResponse ← the single Zod schema shared by the repo (write+read), the HTTP route, the MCP tool, and the Analyzer panel"
    - "exits/application/ports.ts port declarations ← implemented by 26-03 (repo, journal port) and consumed by 26-04 (use-case)"
---

<objective>
Lay the interface + docs foundation for the new `exits` bounded context: write the exit-rules
architecture doc FIRST (docs-before-code, `.claude/rules/workflow.md`), promote the picker's
private ORATS haircut closure to a shared exported pure function (ROLL pricing prerequisite,
RESEARCH Pitfall 2), and define the whole exits interface surface — Zod contracts + domain types
+ application port declarations — that every later plan in this phase consumes.

Purpose: everything downstream (evaluator, repo, use-case, route, MCP, UI) references these
types and this contract. Getting the interface right once, up front, is interface-first ordering.

Output (Artifacts this phase produces, from this plan):
- `docs/architecture/exit-rules.md` (mirrors `picker-rules.md`) + `jobs.md` chain-line extension
- Exported `haircutFill` in `candidate-selection.ts`
- `packages/contracts/src/exits.ts` (Zod schemas, validated both ways)
- `packages/core/src/exits/domain/types.ts` + `application/ports.ts` + `index.ts` barrel (declarations only, no logic yet)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

Project TDD rule (`.claude/rules/tdd.md`): commit ONLY at green. RED is verified by running the
suite (show the failing run) then implementing; do NOT create a separate RED-only commit — one
commit per task at green, matching Phase 25's precedent.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-exit-advisor/26-CONTEXT.md
@.planning/phases/26-exit-advisor/26-RESEARCH.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/workflow.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Docs-first — exit-rules.md + jobs.md chain line + TOPIC-MAP</name>
  <files>docs/architecture/exit-rules.md, docs/architecture/jobs.md, docs/TOPIC-MAP.md</files>
  <read_first>
    - docs/architecture/picker-rules.md (the exact section structure to mirror: rule kinds, position context, ladder table, "how to add a rule", refuted-criteria section)
    - docs/architecture/jobs.md §75-78 (the pg-boss chain line to extend: fetch-schwab-chain → … → compute-picker; append → compute-exit-advice as the NEW terminal step)
    - docs/TOPIC-MAP.md (add the new doc row)
    - .planning/phases/26-exit-advisor/26-RESEARCH.md "Precedence Ladder" + "Hysteresis Recommendation" sections (the content this doc records)
  </read_first>
  <action>
    Create docs/architecture/exit-rules.md mirroring picker-rules.md's structure. Document: the
    seven exit rule kinds (STOP, EVT, GAMMA, TERM, TAKE, ROLL, HOLD); the USER-LOCKED thresholds
    verbatim (TAKE +5/+10/+15%; STOP −25/−50%; TERM front−back IV inversion ≥0.5pp i.e. ≥0.005 IV
    points; GAMMA spot >2% off strike AND front <7 DTE; EVT tier-1 event ≤3 days from front expiry;
    ROLL front <14 DTE AND spot within ±1% strike AND profit <15% AND no blocking event); the
    deterministic precedence order STOP > EVT > GAMMA > TERM > TAKE > ROLL > HOLD with the
    one-line rationale + citation per rung from RESEARCH; the per-rung hysteresis arm/disarm band
    table from RESEARCH (TAKE +5/+3, +10/+8, +15/+13; STOP −25/−23, −50/−48; TERM 0.005/0.003;
    GAMMA 0.02/0.015; EVT date-based no-hysteresis); the read-only + never-execute boundary
    (STRM-04, EXIT-10); and a "how to add a rule" note pointing at the registry array. Prose in
    Hemingway style (`docs/docs-on-docs/hemingway-style.md`), 20-250 lines. Then extend
    jobs.md's chain-line to show compute-picker → compute-exit-advice (NEW terminal, chain-triggered
    no cron) and add a Job Catalog row for compute-exit-advice describing it as a thin terminal
    handler that reads latest snapshot + events + previous verdicts, evaluates, and appends
    exit_verdicts. Add the exit-rules.md row to TOPIC-MAP.md.
  </action>
  <verify>
    <automated>test -f docs/architecture/exit-rules.md && grep -q "compute-exit-advice" docs/architecture/jobs.md && grep -q "exit-rules.md" docs/TOPIC-MAP.md && grep -q "STOP > EVT > GAMMA > TERM > TAKE > ROLL > HOLD" docs/architecture/exit-rules.md</automated>
  </verify>
  <done>exit-rules.md exists with the ladder, the precedence order, and the hysteresis band table; jobs.md chain line and catalog name compute-exit-advice; TOPIC-MAP indexes the new doc.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract haircutFill from picker's private closures (behavior-identical)</name>
  <files>packages/core/src/picker/domain/candidate-selection.ts, packages/core/src/picker/domain/candidate-selection.test.ts</files>
  <read_first>
    - packages/core/src/picker/domain/candidate-selection.ts:73 (FILL_WIDTH_FRACTION = 0.66, already exported) and :224-227 (the private buyFill/sellFill closures inside selectCandidates)
    - packages/core/src/picker/domain/candidate-selection.test.ts (the existing picker suite — the safety net that must stay green)
  </read_first>
  <behavior>
    - haircutFill({bid, ask}, "buy") returns bid + (ask − bid) * FILL_WIDTH_FRACTION (a buyer pays UP toward the ask)
    - haircutFill({bid, ask}, "sell") returns ask − (ask − bid) * FILL_WIDTH_FRACTION (a seller receives DOWN toward the bid)
    - For a zero-width market (bid === ask) both sides return that price exactly
    - selectCandidates produces byte-identical debits after refactoring to call haircutFill (existing picker candidate tests unchanged and green)
  </behavior>
  <action>
    Promote the two private buyFill/sellFill closures in selectCandidates to a single exported pure
    function haircutFill(quote: {bid: number; ask: number}, side: "buy" | "sell"): number in
    candidate-selection.ts, using the exported FILL_WIDTH_FRACTION constant (do NOT hardcode 0.66).
    Rewrite selectCandidates to call haircutFill(quote, "buy")/haircutFill(quote, "sell") in place
    of the inline closures — a pure mechanical extraction, no formula change, no behavior change.
    Add a small unit test for haircutFill (buy/sell direction + zero-width market) to
    candidate-selection.test.ts. The existing candidate-selection tests are the regression oracle:
    they MUST pass unchanged. This is surgical — touch only the closure extraction and its call
    sites; do not otherwise refactor selectCandidates.
  </action>
  <verify>
    <automated>bun run vitest run packages/core/src/picker/domain/candidate-selection.test.ts</automated>
  </verify>
  <done>haircutFill is exported and unit-tested for both sides; selectCandidates calls it; every pre-existing picker candidate-selection test passes unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Exits interface surface — contracts + domain types + application ports</name>
  <files>packages/contracts/src/exits.ts, packages/contracts/src/exits.test.ts, packages/contracts/src/index.ts, packages/core/src/exits/domain/types.ts, packages/core/src/exits/application/ports.ts, packages/core/src/exits/index.ts, packages/core/src/index.ts</files>
  <read_first>
    - packages/contracts/src/picker.ts (ruleSetEntry + pickerSnapshotResponse Zod shape to mirror)
    - packages/core/src/picker/application/ports.ts:86-181 (RuleSetEntry, PickerSnapshot, PickerSnapshotRow, ForPersisting/ForReading port shapes to mirror)
    - packages/core/src/picker/domain/types.ts (domain-type declaration style)
    - .planning/phases/26-exit-advisor/26-CONTEXT.md "Specific Ideas" (the verdict payload shape) + 26-UI-SPEC.md "Copywriting Contract" (exact verdict label strings)
  </read_first>
  <behavior>
    - exitsResponse.parse accepts a well-formed payload: { asOf, observedAt, marketSession, positions: [{ calendarId, name, verdict, rung?, ruleId, metric: {name, value, threshold}, indicative, changed, escalate, pnlPct, basis: {openNetDebit, netMark}, roll?: {suggestedFrontExpiry, estDebit} }], ruleSet: [{ id, kind, rationale }] }
    - The schema has NO confidence/probability field anywhere (EXIT-04) — a payload with an extra `confidence` key is rejected by strict parsing OR simply has no place to carry it
    - verdict is a closed enum: "HOLD" | "TAKE" | "STOP" | "ROLL" | "EXIT_PRE_EVENT"
    - metric is always required on every position (never a bare verdict)
    - Round-trip: exitsResponse.parse(validPayload) succeeds; a payload missing metric fails
  </behavior>
  <action>
    Create packages/contracts/src/exits.ts with Zod schemas: exitMetric ({name: string, value:
    number, threshold: number}), exitVerdictEnum (HOLD/TAKE/STOP/ROLL/EXIT_PRE_EVENT), exitRollDetail
    ({suggestedFrontExpiry: string, estDebit: number}), heldPositionVerdict ({calendarId, name,
    verdict, rung: nullable, ruleId, metric: exitMetric, indicative: boolean, changed: boolean,
    escalate: boolean, pnlPct, basis: {openNetDebit, netMark}, roll: exitRollDetail.nullable()}),
    exitRuleSetEntry ({id, kind: enum gate|trigger|profit-take|roll|hold or the kinds the registry
    uses, rationale}) mirroring picker's ruleSetEntry, and exitsResponse ({asOf, observedAt,
    marketSession: enum rth|after-hours, positions: array, ruleSet: array}). No confidence field
    anywhere (EXIT-04). Export inferred types. Add exits.test.ts asserting parse-accepts a valid
    payload and parse-rejects one missing metric and one missing ruleId. Re-export from
    contracts/src/index.ts.
    Then mirror the wire types as readonly domain types in packages/core/src/exits/domain/types.ts:
    HeldPosition (the open-calendar inputs the evaluator reads: calendarId, name, strike, qty,
    openNetDebit, frontExpiry, backExpiry), MarketContext (netMark, pnlOpen, spot, frontIv, backIv,
    dteFront, dteBack, snapshotTime, cohortNow, marketSession, tier1Events, and a RollChainContext
    carrying the +14-21 DTE candidate replacement fronts with quotes for ROLL pricing), ExitVerdict
    (the evaluator's output: verdict, rung, ruleId, metric, indicative, escalate, roll?), and
    PreviousVerdict ({verdict, rung, ruleId, armedAt} | null). In packages/core/src/exits/
    application/ports.ts declare ExitVerdictRow ({observedAt: Date, calendarId, verdict: ExitVerdict})
    and the driven port TYPE declarations only (no implementations): ForReadingHeldPositions,
    ForReadingLatestSnapshotPerOpenCalendar (declared as an exits-owned re-declaration of the shape,
    never a foreign journal domain import), ForReadingEconomicEvents (exits-owned re-declaration of
    the EconomicEvent shape — tier-1 name enum FOMC|CPI|NFP), ForReadingChainForRoll (exits-owned
    re-declaration of the chain-read shape), ForReadingLatestVerdictsPerCalendar (self-read on
    exit_verdicts), and ForPersistingExitVerdict. Add exits/index.ts barrel and re-export through
    packages/core/src/index.ts. This task is declarations + schemas ONLY — no evaluator/use-case
    logic (those are 26-02/26-04).
  </action>
  <verify>
    <automated>bun run vitest run packages/contracts/src/exits.test.ts && bun run typecheck</automated>
  </verify>
  <done>exitsResponse parses a valid verdict payload and rejects one missing its metric/ruleId; the schema carries no confidence field; core exits types.ts + ports.ts declare HeldPosition/MarketContext/ExitVerdict/PreviousVerdict + all driven ports; typecheck is clean; barrels export the new surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| picker domain → exits domain | The shared haircutFill formula crosses from entry pricing into exit (ROLL) pricing — one source of truth required |
| stored/computed verdict blob → contract schema | Every verdict payload is Zod-validated at the edge; the schema is the shape guarantee |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-01 | Tampering | haircutFill extraction (candidate-selection.ts) | high | mitigate | Behavior-identical mechanical extraction using FILL_WIDTH_FRACTION; the full pre-existing picker candidate-selection suite is the regression oracle and must stay green |
| T-26-02 | Tampering | exitsResponse schema (EXIT-04) | medium | mitigate | Schema has no confidence/probability field; metric + ruleId required on every position, enforced by the exits.test.ts reject cases |
| T-26-SC | Tampering | package installs | low | accept | Zero new packages this phase (RESEARCH Package Legitimacy Audit: N/A) — no supply-chain checkpoint required |
</threat_model>

<verification>
- `bun run vitest run packages/contracts/src/exits.test.ts` green
- `bun run vitest run packages/core/src/picker/domain/candidate-selection.test.ts` green (haircut regression)
- `bun run typecheck && bun run lint` clean
- docs: exit-rules.md, jobs.md chain line, TOPIC-MAP row present
</verification>

<success_criteria>
exit-rules.md documents the full ladder/precedence/hysteresis before code; haircutFill is exported and picker pricing is unchanged; the exits contract + type + port surface is defined and typechecks, ready for 26-02 to fill in the registry + evaluator.
</success_criteria>

<output>
Create `.planning/phases/26-exit-advisor/26-01-SUMMARY.md` when done.
</output>
