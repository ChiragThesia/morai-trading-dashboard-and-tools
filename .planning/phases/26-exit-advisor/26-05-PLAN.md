---
phase: 26-exit-advisor
plan: 05
type: tdd
wave: 5
depends_on: ["26-04"]
files_modified:
  - apps/server/src/adapters/http/exits.routes.ts
  - apps/server/src/adapters/http/exits.routes.test.ts
  - apps/server/src/main.ts
  - apps/server/src/adapters/mcp/tools.ts
  - apps/server/src/adapters/mcp/tools.test.ts
autonomous: true
requirements: [EXIT-08]

must_haves:
  truths:
    - "GET /api/exits returns the exitsResponse payload from the getExitAdvice use-case, mounted inside the existing authenticated apiRouter (same Bearer group as /api/picker/candidates)"
    - "The get_exit_advice MCP tool returns the SAME schema as GET /api/exits (MCP-02 parity), proven by a test that exercises a real McpServer over InMemoryTransport"
    - "A StorageError maps to a flat { error: 'internal' } — no DB internals leak through either surface"
  artifacts:
    - apps/server/src/adapters/http/exits.routes.ts
    - apps/server/src/adapters/mcp/tools.ts
  key_links:
    - "both surfaces call the one getExitAdvice use-case and emit the one contracts exitsResponse schema — a single source of truth for the verdict payload (MCP-02)"
---

<objective>
Expose the exit verdicts read-only: a GET /api/exits HTTP route and a get_exit_advice MCP tool,
both calling the getExitAdvice use-case and emitting the same contracts exitsResponse — answering
"what should I do with my open calendars?" identically for the browser and for Claude Code
(MCP-02 parity).

Purpose: EXIT-08 and the Analyzer panel's data source. Read-only, no user input, no new auth.

Output: the route + its test, the MCP tool + a parity test, and the server composition wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

TDD (`.claude/rules/tdd.md`): commit at green only. The MCP tool is tested through a real McpServer
+ InMemoryTransport-linked Client (genuine handler invocation), mirroring the get_picker_candidates
test precedent rather than calling the use-case directly.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-exit-advisor/26-CONTEXT.md
@.planning/phases/26-exit-advisor/26-RESEARCH.md
@packages/contracts/src/exits.ts
@packages/core/src/exits/application/getExitAdvice.ts
@apps/server/src/adapters/http/picker.routes.ts
@apps/server/src/adapters/mcp/tools.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GET /api/exits route + server wiring</name>
  <files>apps/server/src/adapters/http/exits.routes.ts, apps/server/src/adapters/http/exits.routes.test.ts, apps/server/src/main.ts</files>
  <read_first>
    - apps/server/src/adapters/http/picker.routes.ts (the route family pattern: handler shape, ok/null → 200/cold-start, StorageError → :32-35 flat { error: "internal" } mapping)
    - apps/server/src/main.ts (how picker.routes is mounted inside the authenticated apiRouter and how getPicker is wired — mount /api/exits the same way)
    - packages/core/src/exits/application/getExitAdvice.ts + packages/contracts/src/exits.ts (the use-case + response schema)
  </read_first>
  <behavior>
    - GET /api/exits returns 200 with an exitsResponse body from getExitAdvice on success
    - Cold start (getExitAdvice returns null) returns the picker route's cold-start convention (null/empty payload, not a 500)
    - A StorageError returns the flat { error: "internal" } shape with the picker route's status code — no DB internals in the body
    - The route is mounted inside the existing authenticated apiRouter (same Bearer-token group as /api/picker/candidates) — no new auth code
  </behavior>
  <action>
    Create exits.routes.ts mirroring picker.routes.ts: a GET handler that calls the injected
    getExitAdvice use-case, returns the exitsResponse body on ok, applies the same cold-start
    handling picker uses for a null result, and maps StorageError to the flat { error: "internal" }
    response (copy the picker route's error mapping verbatim — no message passthrough). Mount it at
    GET /api/exits inside the same authenticated apiRouter group as picker in main.ts, wiring the
    getExitAdvice use-case constructed from the exits read ports. Add exits.routes.test.ts asserting
    the 200 payload, the cold-start branch, and the internal-error mapping (no DB detail in the
    body).
  </action>
  <verify>
    <automated>bun run vitest run apps/server/src/adapters/http/exits.routes.test.ts</automated>
  </verify>
  <done>GET /api/exits returns the exitsResponse (200 / cold-start / flat internal error) inside the authenticated apiRouter; wired in main.ts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: get_exit_advice MCP tool + MCP-02 parity test</name>
  <files>apps/server/src/adapters/mcp/tools.ts, apps/server/src/adapters/mcp/tools.test.ts</files>
  <read_first>
    - apps/server/src/adapters/mcp/tools.ts:561-616 (registerGetPickerCandidatesTool — the exact registration pattern: inputSchema {}, use-case call, StorageError → flat internal at :590-592)
    - apps/server/src/adapters/mcp/tools.test.ts (the get_picker_candidates test that spins a real McpServer + InMemoryTransport-linked Client)
    - packages/contracts/src/exits.ts (the shared schema both surfaces emit)
  </read_first>
  <behavior>
    - get_exit_advice takes no input (inputSchema {}) and returns the SAME exitsResponse schema as GET /api/exits — a parity assertion checks the tool's payload validates against the same contracts schema the route uses
    - The tool is invoked through a real McpServer over InMemoryTransport (genuine handler path), not by calling the use-case directly
    - A StorageError maps to the flat internal-error shape (tools.ts convention), no DB detail
  </behavior>
  <action>
    Add registerGetExitAdviceTool to tools.ts mirroring registerGetPickerCandidatesTool: no input
    schema, call the injected getExitAdvice use-case, emit the exitsResponse payload, map
    StorageError to the flat internal error (copy the picker tool's mapping). Register it in the
    per-request tool closure the same way get_picker_candidates is registered. Add a tools.test.ts
    case that connects a real McpServer + InMemoryTransport Client, calls get_exit_advice, and
    asserts the returned payload parses against the SAME contracts exitsResponse schema the HTTP
    route emits (MCP-02 parity) plus the internal-error mapping.
  </action>
  <verify>
    <automated>bun run vitest run apps/server/src/adapters/mcp/tools.test.ts -t "get_exit_advice"</automated>
  </verify>
  <done>get_exit_advice returns the exitsResponse schema over a real MCP transport, parity-asserted against the HTTP route's contract, with the flat internal-error mapping.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → client (HTTP + MCP) | Error bodies must not leak DB internals; both surfaces are read-only with no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-13 | Information Disclosure | StorageError message via route/MCP | medium | mitigate | Flat { error: "internal" } mapping copied verbatim from picker.routes.ts:32-35 / tools.ts:590-592 — no message passthrough |
| T-26-14 | Tampering | schema drift between route and MCP (MCP-02) | medium | mitigate | Both emit the single contracts exitsResponse; the MCP test parity-asserts against the same schema the route uses |
| T-26-15 | Spoofing | unauthenticated read of verdicts | low | mitigate | Route mounts inside the existing Bearer-token apiRouter group; no new/relaxed auth (ASVS V2: reuse) |
| T-26-SC | Tampering | package installs | low | accept | Zero new packages (RESEARCH Package Legitimacy: N/A) |
</threat_model>

<verification>
- `bun run vitest run apps/server/src/adapters/http/exits.routes.test.ts apps/server/src/adapters/mcp/tools.test.ts` green
- `bun run typecheck && bun run lint` clean
- MCP-02 parity assertion present (tool payload validates against the route's contracts schema)
</verification>

<success_criteria>
The browser and Claude Code both read the same exit verdicts through /api/exits and get_exit_advice,
emitting one shared schema, with no DB internals leaking and no new auth surface.
</success_criteria>

<output>
Create `.planning/phases/26-exit-advisor/26-05-SUMMARY.md` when done.
</output>
