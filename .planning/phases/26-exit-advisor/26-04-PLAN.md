---
phase: 26-exit-advisor
plan: 04
type: tdd
wave: 4
depends_on: ["26-03"]
files_modified:
  - packages/core/src/exits/application/computeExitAdvice.ts
  - packages/core/src/exits/application/computeExitAdvice.test.ts
  - packages/core/src/exits/application/getExitAdvice.ts
  - packages/core/src/exits/application/getExitAdvice.test.ts
  - packages/core/src/exits/index.ts
  - packages/core/src/index.ts
  - apps/worker/src/handlers/compute-exit-advice.ts
  - apps/worker/src/handlers/compute-picker.ts
  - apps/worker/src/schedule.ts
  - apps/worker/src/main.ts
autonomous: true
requirements: [EXIT-01, EXIT-02, EXIT-06, EXIT-09, EXIT-10]

must_haves:
  truths:
    - "Each cycle: read open calendars → latest snapshot per calendar → latest verdict per calendar (hysteresis self-read) → economic events → chain-for-roll → evaluateExit per calendar → persist — one verdict row per open calendar"
    - "MarketContext is built from a SINGLE read of the validated ledger snapshot (pnlOpen/netMark/openNetDebit) — the use-case never runs a parallel P&L computation"
    - "The use-case flags changed:true when (verdict, rung/ruleId) differs from the previous verdict; STOP and EXIT_PRE_EVENT carry escalate:true"
    - "compute-picker sends compute-exit-advice on success (WR-01-style singletonKey); compute-exit-advice is the terminal handler (no further enqueue)"
    - "No file under packages/core/src/exits/ imports any order-placement/brokerage-write port (EXIT-10 static guard)"
  artifacts:
    - packages/core/src/exits/application/computeExitAdvice.ts
    - packages/core/src/exits/application/getExitAdvice.ts
    - apps/worker/src/handlers/compute-exit-advice.ts
  key_links:
    - "compute-picker → compute-exit-advice chain (boss.send on success) — the single-trigger chain convention"
    - "composition root wires the existing picker-chain + economic-events adapters to the exits' re-declared ForReadingChainForRoll/ForReadingEconomicEvents ports (structural compatibility, no new adapter)"
    - "getExitAdvice ← the read use-case the HTTP route + MCP tool (26-05) both call"
---

<objective>
Wire the exits domain into the running pipeline: the computeExitAdvice use-case (orchestrates the
per-cycle read → evaluate → persist), the getExitAdvice read use-case (latest verdicts for the
surfaces), and the worker chain — compute-picker gains a boss dep and enqueues compute-exit-advice
on success, a new thin terminal handler runs the use-case, and the composition root wires the
driven ports (reusing existing adapters where the port shapes match).

Purpose: this is the "every open calendar gets a verdict each cycle" machinery (EXIT-01) plus the
never-execute boundary guard (EXIT-10). It consumes the evaluator (26-02) and the repos (26-03).

Output: the two application use-cases + their tests, the new terminal worker handler, the
compute-picker chain-trigger edit, and the composition wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

TDD (`.claude/rules/tdd.md`): commit at green only; the use-case is tested with in-memory ports.
Worker wiring is glue — verified by typecheck + a handler unit test, no separate RED commit.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-exit-advisor/26-CONTEXT.md
@.planning/phases/26-exit-advisor/26-RESEARCH.md
@packages/core/src/exits/domain/evaluate-exit.ts
@packages/core/src/exits/application/ports.ts
@packages/core/src/picker/application/computePickerSnapshot.ts
@apps/worker/src/handlers/compute-gex-snapshot.ts
@apps/worker/src/handlers/compute-picker.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: computeExitAdvice + getExitAdvice use-cases (+ EXIT-10 guard)</name>
  <files>packages/core/src/exits/application/computeExitAdvice.ts, packages/core/src/exits/application/computeExitAdvice.test.ts, packages/core/src/exits/application/getExitAdvice.ts, packages/core/src/exits/application/getExitAdvice.test.ts, packages/core/src/exits/index.ts, packages/core/src/index.ts</files>
  <read_first>
    - packages/core/src/picker/application/computePickerSnapshot.ts:357-358 (isWithinRth/isNyseHoliday marketSession derivation to reuse) + its overall read→compute→persist use-case shape
    - packages/core/src/picker/application/getPicker.ts (the read use-case shape to mirror for getExitAdvice)
    - packages/core/src/exits/domain/evaluate-exit.ts + types.ts (evaluateExit signature, MarketContext/RollChainContext shape to populate)
    - packages/core/src/exits/application/ports.ts (all driven ports declared in 26-01)
    - .planning/phases/26-exit-advisor/26-RESEARCH.md "Hysteresis Recommendation" (the exact per-cycle read order) + Pitfall 6 (EXIT-10 guard)
  </read_first>
  <behavior>
    - computeExitAdvice reads in order: open calendars → latest snapshot per calendar → latest verdict per calendar → economic events → chain-for-roll; then per open calendar builds MarketContext (deriving marketSession the picker way via isWithinRth/isNyseHoliday on the snapshot time, staleness vs the cohort now) and calls evaluateExit(position, context, previousVerdict); then persists one exit_verdicts row per calendar via ForPersistingExitVerdict
    - MarketContext.pnl fields come from the single snapshot read (pnlOpen/netMark) + the calendar's openNetDebit — no second P&L computation anywhere in the use-case
    - A calendar with no previous verdict passes previousVerdict = null (cold start)
    - changed:true is set when (verdict, rung/ruleId) differs from the previous verdict row; escalate:true rides STOP/EXIT_PRE_EVENT
    - A persist error on one calendar is surfaced as err; onConflictDoNothing makes a retry after partial success a safe no-op (resume fills the missing calendars)
    - getExitAdvice returns the latest verdict per open calendar shaped as the contracts exitsResponse payload (ok/null cold-start when nothing computed yet)
    - EXIT-10 guard: an automated test asserts no non-test source file under packages/core/src/exits/ imports any order-placement/brokerage-write port
  </behavior>
  <action>
    Create computeExitAdvice.ts: a factory makeComputeExitAdviceUseCase(deps) returning a
    ForRunningComputeExitAdvice () => Promise<Result<void, StorageError>>. Implement the exact
    read order from RESEARCH, derive marketSession by reusing isWithinRth/isNyseHoliday from
    @morai/shared (NOT a new session clock), build MarketContext per calendar from the single
    validated snapshot read (pnlOpen/netMark + openNetDebit — never a parallel P&L), populate
    RollChainContext from the chain-for-roll read (candidate replacement fronts in the 14-21 DTE
    window with quotes), call evaluateExit, compute changed by comparing to the previous verdict
    row, and persist each verdict. Create getExitAdvice.ts mirroring getPicker.ts: read latest
    verdicts per open calendar, shape into the contracts exitsResponse (asOf/observedAt/
    marketSession/positions/ruleSet), return ok(null) at cold start. Test both with in-memory ports
    (computeExitAdvice.test.ts: read-order, one-verdict-per-open-calendar, change detection,
    indicative pass-through from the evaluator, partial-failure/resume; getExitAdvice.test.ts:
    shape + cold-start). Add the EXIT-10 static guard test: scan the exits source tree (exclude
    *.test.ts) and fail if any file imports an order-placement/brokerage-write port — build the
    forbidden-token match so the token is not itself a self-invalidating literal left in a scanned
    file (assemble it from fragments or scan only non-test sources). Re-export the use-cases through
    exits/index.ts and the @morai/core barrel.
  </action>
  <verify>
    <automated>bun run vitest run packages/core/src/exits/application</automated>
  </verify>
  <done>computeExitAdvice runs the locked read order, builds context from a single ledger read, writes one verdict per open calendar with change/escalate flags; getExitAdvice returns the exitsResponse shape (or cold-start null); the EXIT-10 guard test passes; barrels export both use-cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: compute-exit-advice terminal handler + compute-picker chain trigger + composition wiring</name>
  <files>apps/worker/src/handlers/compute-exit-advice.ts, apps/worker/src/handlers/compute-picker.ts, apps/worker/src/schedule.ts, apps/worker/src/main.ts</files>
  <read_first>
    - apps/worker/src/handlers/compute-gex-snapshot.ts (the boss.send-on-success chain pattern to copy; :42-46 send + catch)
    - apps/worker/src/handlers/compute-picker.ts (currently terminal — gains a boss dep and the send, mirroring compute-gex-snapshot)
    - apps/worker/src/schedule.ts (createQueue("compute-picker") chain-triggered no-cron line to mirror for compute-exit-advice)
    - apps/worker/src/main.ts (composition root ~lines 230-300: how compute-picker's deps + adapters are wired; the picker-chain adapter and economic-events adapter instances to reuse)
    - .planning/phases/26-exit-advisor/26-RESEARCH.md "Code Examples" (makeComputeExitAdviceHandler + the compute-picker boss.send snippet)
  </read_first>
  <behavior>
    - makeComputeExitAdviceHandler runs the computeExitAdvice use-case and throws on !ok (pg-boss retry) — terminal, no further enqueue
    - compute-picker, on a successful compute, calls boss.send("compute-exit-advice", {}, { singletonKey: "triggered-by-picker" }) with a catch that warns and does not fail the picker job (WR-01-style dedup, mirrors compute-gex-snapshot)
    - schedule.ts registers a chain-triggered (no cron) compute-exit-advice queue
    - main.ts wires the use-case deps: the exit-verdicts repo (persist + latest-verdicts self-read, 26-03), the journal latest-snapshot read (26-03), and — reusing the EXISTING picker-chain adapter and economic-events adapter — the exits' re-declared ForReadingChainForRoll and ForReadingEconomicEvents ports (structural compatibility; no new adapter, no `as`)
  </behavior>
  <action>
    Create compute-exit-advice.ts with makeComputeExitAdviceHandler(deps) exactly per the RESEARCH
    code example: run deps.computeExitAdviceUseCase(), throw new Error(result.error.message) on
    failure, terminal (no enqueue). Modify compute-picker.ts to accept a boss dep and, after a
    successful compute, void deps.boss.send("compute-exit-advice", {}, { singletonKey:
    "triggered-by-picker" }).catch(...) — copy compute-gex-snapshot's shape; keep the picker's own
    result unchanged. Add the createQueue("compute-exit-advice") registration in schedule.ts
    (chain-triggered, no cron, mirroring compute-picker). Wire everything in main.ts: construct the
    computeExitAdvice use-case with the 26-03 repos + the journal latest-snapshot read, and pass the
    already-constructed picker-chain adapter and economic-events adapter to the exits'
    ForReadingChainForRoll / ForReadingEconomicEvents ports (they satisfy the re-declared shapes
    structurally); register the new handler and give compute-picker its boss dep. Add a small unit
    test for the handler (runs the use-case, throws on err, terminal). Keep the edits surgical —
    mirror the compute-gex-snapshot → compute-picker precedent line for line.
  </action>
  <verify>
    <automated>bun run typecheck && bun run vitest run apps/worker/src/handlers/compute-exit-advice.test.ts</automated>
  </verify>
  <done>compute-picker enqueues compute-exit-advice on success; the new terminal handler runs the use-case and throws on failure; schedule + main.ts register and wire it (reusing existing chain + events adapters); typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| exits context → order execution | The durable never-execute boundary: nothing under exits/ may reach an order-placement capability |
| validated ledger → verdict context | The use-case must feed the evaluator the validated fill-ledger P&L, never a parallel recompute |
| job retry → cohort persistence | A retry after partial success must be a safe resume, not a corruption |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-10 | Elevation/Tampering (financial) | order-execution creep | critical | mitigate | EXIT-10 static guard test: no non-test file under exits/ imports an order-placement/brokerage-write port; no such port exists in the repo today (verified) |
| T-26-11 | Tampering | parallel P&L drift in the use-case | high | mitigate | MarketContext P&L fields come from the single validated snapshot read (pnlOpen/netMark) + openNetDebit; no second P&L path — asserted by the read-order test |
| T-26-12 | DoS | partial-failure double-run | medium | mitigate | onConflictDoNothing (26-03) makes a retry a safe no-op; the use-case surfaces err so pg-boss retries and resumes the unwritten calendars |
| T-26-SC | Tampering | package installs | low | accept | Zero new packages (RESEARCH Package Legitimacy: N/A) |
</threat_model>

<verification>
- `bun run vitest run packages/core/src/exits` green (application + domain)
- `bun run vitest run apps/worker/src/handlers/compute-exit-advice.test.ts` green
- EXIT-10 guard test present and green
- `bun run typecheck && bun run lint` clean
</verification>

<success_criteria>
Every picker cycle now enqueues a terminal compute-exit-advice run that writes one hysteresis-aware,
change-flagged verdict per open calendar from a single validated ledger read — and no path under
exits/ can ever place an order.
</success_criteria>

<output>
Create `.planning/phases/26-exit-advisor/26-04-SUMMARY.md` when done.
</output>
