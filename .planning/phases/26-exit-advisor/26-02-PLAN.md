---
phase: 26-exit-advisor
plan: 02
type: tdd
wave: 2
depends_on: ["26-01"]
files_modified:
  - packages/core/src/exits/domain/exit-rules.ts
  - packages/core/src/exits/domain/exit-rules.test.ts
  - packages/core/src/exits/domain/evaluate-exit.ts
  - packages/core/src/exits/domain/evaluate-exit.test.ts
  - packages/core/src/exits/index.ts
autonomous: true
requirements: [EXIT-01, EXIT-02, EXIT-03, EXIT-04, EXIT-05, EXIT-06, EXIT-09]

must_haves:
  truths:
    - "A single open calendar always resolves to exactly one verdict from the typed registry, chosen by the deterministic precedence order STOP > EVT > GAMMA > TERM > TAKE > ROLL > HOLD (first match wins)"
    - "TERM fires at front−back IV inversion ≥0.005; GAMMA at spot >2% off strike AND front <7 DTE; EVT at a tier-1 event ≤3 days from front expiry — at the exact locked thresholds, boundary-proven by fast-check"
    - "Across ≥3 consecutive cycles hovering a threshold, the verdict does NOT flap: hysteresis arms at the trigger and disarms only past the documented disarm band, both directions"
    - "An after-hours / stale / NaN-greek cohort yields indicative:true and NEVER an actionable STOP/TAKE (the gate is the evaluator's first step)"
    - "pnlPct is derived ONLY from (netMark − openNetDebit)/openNetDebit passed in — never a parallel recompute"
    - "ROLL suggestion pricing calls the shared haircutFill (26-01), never a re-derived 0.66 formula"
  artifacts:
    - packages/core/src/exits/domain/exit-rules.ts
    - packages/core/src/exits/domain/evaluate-exit.ts
  key_links:
    - "EXIT_PRECEDENCE constant ← the ordered ladder the evaluator walks (reviewable/testable line, not implicit if/else order)"
    - "evaluateExit(position, context, previousVerdict) ← the 3-arg pure entrypoint the use-case (26-04) and the backtest (Phase 27) both reuse"
    - "haircutFill import ← ROLL est. debit pricing"
---

<objective>
The heart of the phase: the exit-rule registry + the pure `evaluateExit` evaluator. A typed
registry (rule rows + the deterministic precedence order + hysteresis band constants) and one pure
3-argument function that turns (HeldPosition, MarketContext, PreviousVerdict|null) into a single
ExitVerdict — gated for session/staleness, hysteresis-banded so it never flaps, precedence-ordered
so simultaneous triggers resolve deterministically.

Purpose: this is the only genuinely new domain logic in the phase (RESEARCH: "the only real new
domain logic is the STOP/TAKE ladder thresholds and the hysteresis/precedence layer"). Everything
else wires existing data through it. It gets its own plan and full TDD with fast-check boundary
properties.

Output: `exit-rules.ts` (registry + EXIT_PRECEDENCE + hysteresis constants) and `evaluate-exit.ts`
(the pure evaluator), both fully unit- and property-tested.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

TDD discipline (`.claude/rules/tdd.md`): RED→GREEN→REFACTOR, commit ONLY at green (verify RED by
running the suite and showing failures first; no separate RED-only commit — Phase 25 precedent).
Fixtures MUST use distinct timestamps (green-suite lesson #1) and must cover: multiple-rules-fire
simultaneously (precedence), hysteresis arm/disarm both directions, AH/stale → indicative never
actionable, and verdict-change vs the previous row.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-exit-advisor/26-CONTEXT.md
@.planning/phases/26-exit-advisor/26-RESEARCH.md
@packages/core/src/exits/domain/types.ts
@packages/core/src/exits/application/ports.ts
@packages/core/src/picker/domain/rules.ts
@packages/core/src/picker/domain/candidate-selection.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Exit-rule registry — rows + EXIT_PRECEDENCE + hysteresis constants</name>
  <files>packages/core/src/exits/domain/exit-rules.ts, packages/core/src/exits/domain/exit-rules.test.ts, packages/core/src/exits/index.ts</files>
  <read_first>
    - packages/core/src/picker/domain/rules.ts:244-390 (RuleMetadata + RULE_SET_METADATA array — the registry-row style to mirror: id, label, kind, rationale, source)
    - packages/core/src/exits/domain/types.ts (ExitVerdict/rule-id union defined in 26-01)
    - .planning/phases/26-exit-advisor/26-RESEARCH.md "Precedence Ladder" (order + citations) and "Hysteresis Recommendation" (per-rung band table)
    - .planning/phases/26-exit-advisor/26-CONTEXT.md "The playbook ladder" (USER-LOCKED thresholds — encode EXACTLY, no re-derivation)
  </read_first>
  <behavior>
    - EXIT_PRECEDENCE is a ReadonlyArray of the rule ids in the exact order STOP, EVT, GAMMA, TERM, TAKE, ROLL, HOLD — a test asserts it is exhaustive over every rule id in the registry and contains no duplicates and no unknown id
    - Threshold constants equal the locked values EXACTLY: TAKE rungs 0.05/0.10/0.15; STOP rungs −0.25/−0.50; TERM inversion 0.005; GAMMA 0.02 off strike + 7 DTE; EVT 3 days; ROLL 14 DTE / ±0.01 spot band / 0.15 profit
    - Hysteresis disarm bands equal the documented values: TAKE +5/disarm<+3, +10/<+8, +15/<+13; STOP −25/disarm>−23, −50/>−48; TERM arm 0.005/disarm<0.003; GAMMA arm 0.02/disarm<0.015
    - A registry-invariants test asserts none of the REFUTED picker criteria ids (IV-rank gates, −1..−3% IV-diff band, debit-%-of-back band, per-pair crisis gates) appear as an exit rule id
  </behavior>
  <action>
    Create exit-rules.ts mirroring rules.ts's registry style: a typed ExitRuleMetadata row
    (id, label, kind, rationale, source) and an EXIT_RULE_METADATA ReadonlyArray with one row per
    rule (STOP, EVT, GAMMA, TERM, TAKE, ROLL, HOLD), each rationale/source lifted from RESEARCH's
    precedence-ladder citations. Export EXIT_PRECEDENCE (the ordered rule-id array) as an explicit
    constant — the reviewable, testable ladder line, NOT an implicit if/else order. Export the
    named threshold constants (TAKE_RUNGS, STOP_RUNGS, TERM_INVERSION_MIN, GAMMA_OFF_STRIKE,
    GAMMA_FRONT_DTE_MAX, EVT_BLACKOUT_DAYS, ROLL_FRONT_DTE_MAX, ROLL_SPOT_BAND, ROLL_PROFIT_MAX,
    ROLL_REPLACEMENT_DTE_MIN/MAX = 14/21) and the hysteresis disarm-band constants, values EXACTLY
    per CONTEXT + RESEARCH — document each with a one-line comment naming its source. Reuse the
    picker's EVENT_BLACKOUT_DAYS value (3) by matching it, and note EVT reuses the same day-before
    stamp math as candidate-selection.ts. Add exit-rules.test.ts: EXIT_PRECEDENCE exhaustiveness +
    no-duplicates + no-unknown-id; threshold constants equal the locked literals; refuted-criteria
    ids absent. Export the registry through exits/index.ts.
  </action>
  <verify>
    <automated>bun run vitest run packages/core/src/exits/domain/exit-rules.test.ts</automated>
  </verify>
  <done>The registry array + EXIT_PRECEDENCE + every threshold/hysteresis constant exist at the locked values; the precedence order is exhaustive and duplicate-free; no refuted picker criterion appears as an exit rule.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: evaluateExit — the pure 3-arg evaluator (gating, precedence, hysteresis, P&L, ROLL)</name>
  <files>packages/core/src/exits/domain/evaluate-exit.ts, packages/core/src/exits/domain/evaluate-exit.test.ts, packages/core/src/exits/index.ts</files>
  <read_first>
    - packages/core/src/exits/domain/exit-rules.ts (Task 1 registry + constants)
    - packages/core/src/exits/domain/types.ts (HeldPosition, MarketContext, ExitVerdict, PreviousVerdict)
    - packages/core/src/picker/domain/candidate-selection.ts (haircutFill from 26-01; EVENT_BLACKOUT_DAYS/exitBeforeIso day-before stamp math at :239-252 to mirror)
    - .planning/phases/26-exit-advisor/26-RESEARCH.md "Common Pitfalls" 3 (stateless-hysteresis trap) + 4 (actionable-on-stale) + "Code Examples" (P&L basis, session/staleness gate, EVT stamp)
  </read_first>
  <behavior>
    - Gate first: when marketSession is after-hours, OR snapshotTime is older than the staleness tolerance (45 min, matching SNAPSHOT_LEG_STALENESS_TOLERANCE_MS) relative to cohortNow, OR any of frontIv/backIv/net-greeks is the NaN sentinel → verdict is indicative:true and MUST NOT be an actionable STOP/TAKE (it may still name the would-be rule/metric for display, but escalate:false and the verdict is gated to a display-only form)
    - Precedence: given a context where multiple rules qualify simultaneously, the returned verdict is the highest one in EXIT_PRECEDENCE (STOP beats EVT beats GAMMA beats TERM beats TAKE beats ROLL beats HOLD) — fast-check over randomized multi-trigger contexts
    - Hysteresis (fast-check, both directions): feed ≥3 consecutive contexts whose pnlPct hovers inside a rung's [disarm, arm] band with previousVerdict carried forward; the verdict does not flip on/off within the band — it flips only when crossing arm (to on) or disarm (to off)
    - P&L basis: pnlPct === (netMark − openNetDebit)/openNetDebit for the passed values; an oracle test uses a known trade's numbers and asserts the rung selection matches
    - TAKE returns the HIGHEST qualifying rung (+15 over +10 over +5); STOP returns the deeper qualifying rung (−50 over −25)
    - TERM fires at frontIv−backIv ≥ 0.005 (boundary: 0.005 fires, 0.00499 does not, subject to hysteresis on repeats)
    - GAMMA fires only when |spot − strike|/strike > 0.02 AND dteFront < 7 (both halves)
    - EVT fires when a tier-1 event is ≤3 days before frontExpiry, stamping EXIT_PRE_EVENT the day before (reusing the candidate-selection day-before math)
    - ROLL fires only when dteFront < 14 AND spot within ±1% of strike AND pnlPct < 0.15 AND no blocking event; when it fires it picks the nearest +14-21 DTE replacement front from RollChainContext and prices est. debit via haircutFill — a test asserts the priced value equals a haircutFill call, not a re-derived formula
    - HOLD is the default when nothing else fires
    - Change detection: the ExitVerdict carries enough for the use-case to compare (verdict, rung/ruleId) against previousVerdict; STOP and EXIT_PRE_EVENT set escalate:true
  </behavior>
  <action>
    Create evaluate-exit.ts exporting the pure function
    evaluateExit(position: HeldPosition, context: MarketContext, previousVerdict: PreviousVerdict | null): ExitVerdict.
    Structure the body as: (1) compute the session/staleness/NaN indicative gate FIRST — reuse the
    45-min staleness tolerance and the isWithinRth/isNyseHoliday-derived marketSession already in
    context (do NOT reimplement session math here; MarketContext carries marketSession, computed by
    the use-case the picker way); (2) walk EXIT_PRECEDENCE in order, evaluating each rule's
    condition against the position+context with hysteresis applied using previousVerdict (arm at the
    threshold, disarm only past the documented band); first qualifying rule wins. Derive pnlPct
    ONLY as (netMark − openNetDebit)/openNetDebit from the passed fields — never recompute P&L.
    For EVT reuse the day-before-stamp math from candidate-selection.ts applied to the fixed
    frontExpiry. For ROLL, when the gate passes, select the nearest replacement front in
    [14,21] DTE from context.rollChain and price est. debit by calling haircutFill (imported from
    the picker domain) — assert-by-test it is the shared function, not a copy. Every returned
    ExitVerdict names ruleId + a raw metric {name, value, threshold} (EXIT-04) and sets escalate
    true only for STOP/EXIT_PRE_EVENT. When the indicative gate is on, force indicative:true and
    never emit an actionable STOP/TAKE escalation. Add evaluate-exit.test.ts with the fast-check
    properties and oracle cases from the behavior block; use distinct timestamps in every fixture.
    Export evaluateExit through exits/index.ts. Keep the evaluator PURE — no I/O, no Date.now();
    cohortNow is passed in context.
  </action>
  <verify>
    <automated>bun run vitest run packages/core/src/exits</automated>
  </verify>
  <done>evaluateExit is pure and 3-arg; the indicative gate runs first; precedence is deterministic; hysteresis holds across ≥3 near-threshold cycles both directions; pnlPct is the passed-ledger derivation only; ROLL prices via the shared haircutFill; every verdict names ruleId + raw metric with no confidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stale/AH market data → actionable verdict | The most dangerous crossing: a verdict computed off after-hours or stale marks must never render as an actionable STOP/TAKE |
| validated ledger P&L → verdict math | pnlPct must ride the validated fill-ledger basis, never a parallel recompute (the −$319,850 incident class) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-03 | Spoofing/Tampering | evaluateExit indicative gate | critical | mitigate | Session/staleness/NaN gate is the FIRST step; an indicative cohort can never return an actionable STOP/TAKE — fast-check + explicit indicative unit cases prove it |
| T-26-04 | Tampering | pnlPct derivation | high | mitigate | pnlPct computed only from passed (netMark, openNetDebit); no parallel P&L path exists in the evaluator; oracle test pins it to a known trade |
| T-26-05 | Tampering | ROLL pricing drift | medium | mitigate | ROLL est. debit calls the shared haircutFill (26-01); a test asserts the value equals a haircutFill call, forbidding a re-derived 0.66 formula |
| T-26-06 | Tampering | verdict flapping on noise | medium | mitigate | Hysteresis bands + previousVerdict; fast-check asserts no flip inside the [disarm, arm] band across ≥3 cycles both directions |
</threat_model>

<verification>
- `bun run vitest run packages/core/src/exits` green (registry invariants + evaluator properties + oracle)
- `bun run typecheck && bun run lint` clean
- Fast-check properties present for: hysteresis no-flap (both directions), precedence first-match-wins, indicative gating, ROLL boundary
</verification>

<success_criteria>
A pure, fully-tested evaluateExit that resolves every open calendar to one deterministic,
non-flapping, session-gated verdict naming its rule + raw metric — the reusable entrypoint the
use-case (26-04) and the Phase-27 backtest replay through.
</success_criteria>

<output>
Create `.planning/phases/26-exit-advisor/26-02-SUMMARY.md` when done.
</output>
