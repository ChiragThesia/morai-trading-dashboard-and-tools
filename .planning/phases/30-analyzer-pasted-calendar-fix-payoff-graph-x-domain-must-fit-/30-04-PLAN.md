---
phase: 30-analyzer-pasted-calendar-fix
plan: 04
type: tdd
wave: 2
depends_on: ["30-03"]
files_modified:
  - packages/core/src/picker/application/analyzeAdHocCalendar.ts
  - packages/core/src/picker/application/analyzeAdHocCalendar.test.ts
  - packages/core/src/picker/application/ports.ts
  - packages/core/src/picker/application/computePickerSnapshot.ts
  - packages/core/src/picker/application/index.ts
  - packages/core/src/picker/index.ts
autonomous: true
requirements: [D-02]
must_haves:
  truths:
    - "makeAnalyzeAdHocCalendarUseCase scores ONE ad-hoc PUT calendar with score/breakdown/exitPlan byte-identical to what scoreOne produces for the equivalent RawCandidate (D-02, parity)"
    - "The gate penaltyMultiplier and gate/sizing come verbatim from the latest snapshot — resolveEntryGate is NEVER called, no macro/open-calendar/recent-closed/chain reads happen (D-02, binding #4, T-28-10)"
    - "The effective rule config is resolved FRESH via readRuleOverrides so ad-hoc scoring matches engine scoring under active Phase-29 overrides (D-02, binding #4)"
    - "No snapshot yet → {scored:false, reason:'no-snapshot'}; the use-case never throws and never persists (D-02, binding #2, binding #8/T-19-17)"
    - "Zero new driven ports are introduced (every read already has a port + repos + in-memory twin)"
  artifacts:
    - "packages/core/src/picker/application/analyzeAdHocCalendar.ts — makeAnalyzeAdHocCalendarUseCase, ForAnalyzingAdHocCalendar, AdHocCalendarInput, AdHocCalendarAnalysis"
    - "computePickerSnapshot.ts — toPickerCandidateDomain / applyGatePenalty / zeroEventAdjustment exported for reuse"
  key_links:
    - "readPickerSnapshot → gate/sizing/spot/asOf (reused, never recomputed)"
    - "resolveEventExit + legSpansEvents + bsmGreeks → one RawCandidate → scoreCalendarCandidates → applyGatePenalty → toPickerCandidateDomain"
---

<objective>
Build the heart of Defect 2 (D-02, USER LOCKED): a hexagonal use-case that scores ONE
user-pasted PUT calendar through the SAME engine path as auto-surfaced candidates, reusing
the latest snapshot's gate/sizing/context and the fresh Phase-29 rule overrides. The whole
task is wiring — every math piece (scoreCalendarCandidates, findBreakevens, bsmGreeks,
resolveEventExit, resolvePickerRuleConfig) already exists and is tested.

Purpose: a byte-parity guarantee — an ad-hoc calendar and an identical engine candidate must
score the same. The gate is cohort-level and reused verbatim (T-28-10), never recomputed.
Output: `makeAnalyzeAdHocCalendarUseCase` + a `Result`-returning driver port; adapters in 30-05.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-analyzer-pasted-calendar-fix-payoff-graph-x-domain-must-fit-/30-RESEARCH.md
@.planning/phases/30-analyzer-pasted-calendar-fix-payoff-graph-x-domain-must-fit-/30-PATTERNS.md
@packages/core/src/picker/domain/scoring.ts
@packages/core/src/picker/application/computePickerSnapshot.ts
@packages/core/src/picker/application/ports.ts
</context>

<artifacts>
## Artifacts this plan produces
- `AdHocCalendarInput` = `{ putCall: "P"; strike; frontDte; backDte; frontIv; backIv; debit; qty; frontExpiry; backExpiry }` — application type
- `AdHocCalendarAnalysis` = `{ scored: true; candidate: PickerCandidateDomain } | { scored: false; reason: string }`
- `ForAnalyzingAdHocCalendar = (input: AdHocCalendarInput) => Promise<Result<AdHocCalendarAnalysis, StorageError>>` — driver port (ports.ts)
- `makeAnalyzeAdHocCalendarUseCase(deps): ForAnalyzingAdHocCalendar` — `analyzeAdHocCalendar.ts`
- `toPickerCandidateDomain`, `applyGatePenalty`, `zeroEventAdjustment` — now exported from `computePickerSnapshot.ts`
- barrel re-exports in `application/index.ts` + `picker/index.ts`
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: makeAnalyzeAdHocCalendarUseCase — build one RawCandidate + score with parity</name>
  <files>packages/core/src/picker/application/analyzeAdHocCalendar.ts, packages/core/src/picker/application/analyzeAdHocCalendar.test.ts, packages/core/src/picker/application/ports.ts, packages/core/src/picker/application/computePickerSnapshot.ts</files>
  <read_first>
    - packages/core/src/picker/domain/candidate-selection.ts (RawCandidate construction :370-411 — mirror this shape; resolveEventExit from 30-03; legSpansEvents)
    - packages/core/src/picker/domain/scoring.ts (scoreCalendarCandidates :308-314, scoreOne, ScoringParams :86-108)
    - packages/core/src/picker/application/computePickerSnapshot.ts (toPickerCandidateDomain :391-427, applyGatePenalty + zeroEventAdjustment usage :621-626, the read/score wiring :584-620)
    - packages/core/src/picker/domain/rule-config.ts (resolvePickerRuleConfig), packages/core/src/picker/application/ports.ts (the 6 read ports :230-303; settings ForReadingRuleOverrides)
    - .planning/phases/30-.../30-RESEARCH.md (Pattern 2, Anti-Patterns, Assumption A3, Pitfall 6)
  </read_first>
  <behavior>
    - Parity: for legs L, `analyzeAdHocCalendar(L)` returns a candidate whose score/breakdown/fwdIv/fwdEdge/exitPlan equal `scoreOne(equivalentRawCandidate, gexContext, params)` mapped via toPickerCandidateDomain (fast-check over strike/iv/dte/debit ranges).
    - RawCandidate.debit = input.debit * 100 (engine per-contract dollar convention — matches candidate-selection's *100); qty is NOT folded into the scored debit (parity).
    - Flat-IV paste (frontIv === backIv) → slope contribution ≈ 0, surfaced honestly (Pitfall 6 — documented, not hidden).
    - Gate penalty: with a snapshot gate penaltyMultiplier of 0.5, the returned score = round(0.5 × base score) — parity with applyGatePenalty (A3).
    - Events context stale (snapshot.eventsContextStatus !== "ok") → eventAdjustment zeroed via zeroEventAdjustment.
  </behavior>
  <action>
    Create `makeAnalyzeAdHocCalendarUseCase(deps)` returning `ForAnalyzingAdHocCalendar`
    (add both the type and driver port to ports.ts). deps: readPickerSnapshot, readGexContext,
    readEconomicEvents, readDailySpotCloses, readPickerSlopeHistory, readRuleOverrides, plus
    `rate`/`dividendYield` (mirror computePickerSnapshot deps — NO new driven ports).
    Flow: (1) readPickerSnapshot → null ⇒ ok({scored:false, reason:"no-snapshot"}); read
    errors on the critical reads ⇒ err(StorageError). (2) spot = snapshot.spot; asOfIso =
    snapshot.asOf; gate = snapshot.gate; use snapshot.gexContextStatus/eventsContextStatus as
    the freshness verdict (reuse latest context — never re-derive with now()). (3) config =
    resolvePickerRuleConfig(overrides.picker) fresh. (4) build ONE RawCandidate mirroring
    candidate-selection.ts:370-411: bsmGreeks(spot,K,dte/365,iv,r,q,"P") for theta/vega/delta
    (*100), slope=((ivB-ivF)/(tb-tf))*365, frontEvents/backEvents via legSpansEvents,
    {exitBeforeIso,eventInPeakTheta} via resolveEventExit(frontExpiry, events), debit=input.debit*100,
    id/name synthesized. (5) scored = scoreCalendarCandidates([raw], gexStatus==="ok"?gex:null,
    {r,q,realizedVol20:realizedVol(closes),slopeHistory,weights:config.weights,debitBand:config.debitBand})[0];
    if eventsStatus!=="ok" → zeroEventAdjustment; then applyGatePenalty(scored, gate.penaltyMultiplier).
    (6) return ok({scored:true, candidate: toPickerCandidateDomain(scored, "standard")}).
    EXPORT toPickerCandidateDomain/applyGatePenalty/zeroEventAdjustment from computePickerSnapshot.ts
    (currently private) — reuse, never a second formula (parity). NEVER call resolveEntryGate,
    readChainForPicker, readMacroObservations, readOpenCalendars, or readRecentClosedCalendars
    (T-28-10). NEVER persist (T-19-17). Result<AdHocCalendarAnalysis, StorageError>.
  </action>
  <verify>
    <automated>bunx vitest run packages/core/src/picker/application/analyzeAdHocCalendar.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bunx vitest run packages/core/src/picker/application/analyzeAdHocCalendar.test.ts` green — includes the fast-check parity property vs scoreOne, the gate-penalty parity case, and the stale-events zeroing case, using in-memory port fakes.
    - Parity test constructs the equivalent RawCandidate with debit = input.debit*100 and asserts equal score+breakdown.
    - `bun run typecheck && bun run lint` clean (no any/as/!; Result<T,E> return).
  </acceptance_criteria>
  <done>The use-case scores one ad-hoc PUT calendar with byte-parity to the engine, applying the reused gate penalty + fresh overrides.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Degradation + port-hygiene guarantees; barrel exports</name>
  <files>packages/core/src/picker/application/analyzeAdHocCalendar.test.ts, packages/core/src/picker/application/index.ts, packages/core/src/picker/index.ts</files>
  <read_first>
    - packages/core/src/picker/application/index.ts + packages/core/src/picker/index.ts (existing barrel exports for compute/get-picker use-cases)
    - .planning/phases/30-.../30-RESEARCH.md (Open Question 1 — gate-blocked still scores; binding #1/#8)
  </read_first>
  <behavior>
    - No-snapshot: readPickerSnapshot ok(null) → ok({scored:false, reason:"no-snapshot"}), and the gate/sizing/gex reads are irrelevant (no throw).
    - Gate BLOCKED (state "blocked", entries not allowed) → STILL returns scored:true with the penalty-applied score (binding #1 — never hide the analysis; the chip is the client's concern).
    - Port hygiene: a spy/fake asserts resolveEntryGate is never invoked and none of readChainForPicker/readMacroObservations/readOpenCalendars/readRecentClosedCalendars are called (they are not even in deps).
    - Barrels export makeAnalyzeAdHocCalendarUseCase + ForAnalyzingAdHocCalendar + the input/analysis types so @morai/core re-exports them for the server root.
  </behavior>
  <action>
    Add the degradation + gate-blocked + port-hygiene tests. Because the use-case's deps do
    not even include the cohort-gate reads, the hygiene guarantee is structural — assert it by
    the deps shape (the fake object only provides the 6 reads + rate/dividendYield) and a
    call-count spy on the provided reads. Re-export makeAnalyzeAdHocCalendarUseCase, the driver
    port type, AdHocCalendarInput, and AdHocCalendarAnalysis through application/index.ts and
    picker/index.ts (the ONE apps→core wiring point 30-05 needs), matching the 19-07 barrel
    precedent.
  </action>
  <verify>
    <automated>bunx vitest run packages/core/src/picker/application/analyzeAdHocCalendar.test.ts && bun run test -- --project=core</automated>
  </verify>
  <acceptance_criteria>
    - No-snapshot + gate-blocked + port-hygiene cases green.
    - `rg -n "makeAnalyzeAdHocCalendarUseCase|ForAnalyzingAdHocCalendar" packages/core/src/picker/index.ts` shows the re-exports.
    - `bun run test -- --project=core && bun run typecheck && bun run lint` clean — full core suite (Phase-27 backtest replay + scoring suites) unchanged/green.
  </acceptance_criteria>
  <done>Failure posture, gate-blocked-still-scores, and no-recompute hygiene are proven; the use-case is reachable from @morai/core.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| core use-case | Input arrives already Zod-validated at the 30-05 route; internal reads are trusted DB ports. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-09 | Tampering | Ad-hoc path recomputes the entry gate per request (retired-gate scar) | high | mitigate | resolveEntryGate + cohort reads are NOT in the use-case deps; gate/sizing read verbatim off snapshot (T-28-10); port-hygiene test |
| T-30-10 | Repudiation | A second "good enough" scoring formula drifts from the engine | high | mitigate | scoreCalendarCandidates/toPickerCandidateDomain/applyGatePenalty reused verbatim; fast-check parity vs scoreOne |
| T-30-11 | Information Disclosure | NaN-poisoned score from a degenerate leg surfaces as a fabricated-clean number | medium | mitigate | Upstream Zod (30-03/30-05) rejects non-finite input; scoreOne never propagates NaN (each degraded term zeroes itself) |
| T-30-12 | Denial of Service | Persisting/recomputing the snapshot on an analyze request | high | mitigate | Use-case has no persist port and never reads the chain cohort; single bounded reads only (T-19-17) |
</threat_model>

<verification>
- `bun run test -- --project=core` green (new use-case suite + unchanged scoring/backtest suites).
- `bun run typecheck && bun run lint` clean.
- Port-hygiene: the use-case deps object structurally excludes the cohort-gate reads.
</verification>

<success_criteria>
An ad-hoc PUT calendar scores with byte-parity to the engine, reuses the snapshot gate/sizing verbatim, honors fresh Phase-29 overrides, and degrades to scored:false with no throw and no persist.
</success_criteria>

<output>
Create `.planning/phases/30-analyzer-pasted-calendar-fix-payoff-graph-x-domain-must-fit-/30-04-SUMMARY.md` when done.
</output>
