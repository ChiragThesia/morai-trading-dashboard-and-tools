---
phase: 30-analyzer-pasted-calendar-fix
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/picker.ts
  - packages/contracts/src/picker.test.ts
  - packages/core/src/picker/domain/candidate-selection.ts
  - packages/core/src/picker/domain/candidate-selection.test.ts
autonomous: true
requirements: [D-02]
must_haves:
  truths:
    - "analyzeAdHocCalendarRequest rejects non-finite/non-positive leg numbers, non-put calendars, and backDte ≤ frontDte (D-02, puts-only D-03, V5 input validation)"
    - "The request schema does NOT accept a client-supplied spot (server derives it — threat mitigation)"
    - "analyzeAdHocCalendarResponse carries {scored, candidate, reason} — candidate is a full pickerCandidate when scored, null otherwise (D-02, binding #2)"
    - "resolveEventExit is one exported pure function reused by BOTH selectCandidates and the ad-hoc use-case — event-blackout logic is not duplicated (D-02, Don't Hand-Roll)"
    - "selectCandidates behavior is unchanged after the extraction — existing suite green (regression neutrality)"
  artifacts:
    - "packages/contracts/src/picker.ts — analyzeAdHocCalendarRequest, analyzeAdHocCalendarResponse (additive)"
    - "packages/core/src/picker/domain/candidate-selection.ts — exported resolveEventExit(feIso, events): {exitBeforeIso, eventInPeakTheta}"
  key_links:
    - "one Zod request schema → shared by HTTP route + MCP tool (MCP-02, 30-05)"
    - "resolveEventExit → consumed by selectCandidates (this plan) + analyzeAdHocCalendar (30-04)"
---

<objective>
Lay the Defect-2 foundations (D-02, USER LOCKED — pasted calendars get REAL engine scoring):
(1) the additive contract schemas the new `POST /picker/analyze` route + MCP tool share
(MCP-02), and (2) an extracted, exported `resolveEventExit` helper so the ad-hoc use-case
reuses the engine's exact event-blackout → `exitBeforeIso`/`eventInPeakTheta` logic instead
of a second copy (Don't Hand-Roll). Both are Wave-1 foundations with zero cross-dependency.

Purpose: the ad-hoc use-case (30-04) and adapters (30-05) build on these; extracting the
helper here keeps the use-case pure wiring.
Output: two Zod schemas + one shared pure helper, both TDD-covered, both behavior-neutral to
the live engine.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-analyzer-pasted-calendar-fix-payoff-graph-x-domain-must-fit-/30-RESEARCH.md
@.planning/phases/30-analyzer-pasted-calendar-fix-payoff-graph-x-domain-must-fit-/30-PATTERNS.md
@packages/contracts/src/picker.ts
@packages/contracts/src/settings.ts
</context>

<artifacts>
## Artifacts this plan produces
- `analyzeAdHocCalendarRequest` (Zod) — `packages/contracts/src/picker.ts`; fields: `putCall: z.literal("P")`, `strike`/`frontIv`/`backIv` finite-positive, `frontDte`/`backDte`/`qty` positive ints, `debit` finite, `frontExpiry`/`backExpiry` ISO date strings; `.strict()`; refined `backDte > frontDte`. NO `spot` field.
- `analyzeAdHocCalendarResponse` (Zod) — `{ scored: z.boolean(), candidate: pickerCandidate.nullable(), reason: z.string().nullable() }`
- `AnalyzeAdHocCalendarRequest` / `AnalyzeAdHocCalendarResponse` — inferred types
- `resolveEventExit(frontExpiryIso: string, events: ReadonlyArray<EconomicEvent>): { exitBeforeIso: string | null; eventInPeakTheta: boolean }` — exported from `candidate-selection.ts`
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Additive analyze request/response contract schemas</name>
  <files>packages/contracts/src/picker.ts, packages/contracts/src/picker.test.ts</files>
  <read_first>
    - packages/contracts/src/picker.ts (pickerCandidateLeg :12-21 constraints, pickerCandidate :102-143)
    - packages/contracts/src/settings.ts (setRuleOverridesRequest/Response — additive-schema + .strict() precedent)
    - .planning/phases/30-.../30-RESEARCH.md (Security Domain V5; threat: no client spot)
  </read_first>
  <behavior>
    - Parses a valid PUT ad-hoc calendar body (strike 7500, frontDte 7, backDte 35, frontIv/backIv 0.15, debit 12.5, qty 1, frontExpiry/backExpiry ISO) → ok.
    - Rejects: putCall "C"; strike ≤ 0; non-finite iv/debit; frontDte/backDte non-integer or ≤ 0; backDte ≤ frontDte; an extra `spot` key (`.strict()`).
    - analyzeAdHocCalendarResponse parses `{scored:true, candidate:<valid pickerCandidate>, reason:null}` and `{scored:false, candidate:null, reason:"no-snapshot"}`.
  </behavior>
  <action>
    Add `analyzeAdHocCalendarRequest` = a `.strict()` z.object with: `putCall: z.literal("P")`
    (puts only this phase — D-03/Pitfall 5), `strike: z.number().finite().positive()`,
    `frontDte`/`backDte`/`qty: z.number().int().positive()`, `frontIv`/`backIv:
    z.number().finite().positive()`, `debit: z.number().finite()`, `frontExpiry`/`backExpiry:
    z.string()` (ISO date), refined so `backDte > frontDte`. Deliberately NO `spot` field —
    the server derives spot from the latest snapshot (threat mitigation, never trust a
    client price). Add `analyzeAdHocCalendarResponse` = z.object `{ scored: z.boolean(),
    candidate: pickerCandidate.nullable(), reason: z.string().nullable() }`. Export both +
    inferred types. Additive only — do not touch existing picker schemas.
  </action>
  <verify>
    <automated>bunx vitest run packages/contracts/src/picker.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bunx vitest run packages/contracts/src/picker.test.ts` green — valid-parse + every reject case above.
    - `rg -n "analyzeAdHocCalendarRequest|analyzeAdHocCalendarResponse" packages/contracts/src/picker.ts` shows both exported.
    - Request schema has no `spot` field; a body with `spot` fails `.strict()`.
    - `bun run typecheck` clean.
  </acceptance_criteria>
  <done>Both schemas exist, reject adversarial/non-put/spot-bearing input, and share pickerCandidate for the scored payload.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract the event-blackout helper resolveEventExit</name>
  <files>packages/core/src/picker/domain/candidate-selection.ts, packages/core/src/picker/domain/candidate-selection.test.ts</files>
  <read_first>
    - packages/core/src/picker/domain/candidate-selection.ts (inline event-blackout loop :330-343, EVENT_BLACKOUT_DAYS :81, PEAK_THETA_DAYS :88, isoDayNumber :140)
    - .planning/phases/30-.../30-RESEARCH.md (Don't Hand-Roll — extract resolveEventExit; State of the Art — EVT stamps exitBeforeIso, not a block)
  </read_first>
  <behavior>
    - A tier-1 event within EVENT_BLACKOUT_DAYS before the front expiry → exitBeforeIso = the day BEFORE the earliest such event.
    - No event within the window → exitBeforeIso = null.
    - An event within PEAK_THETA_DAYS before front expiry → eventInPeakTheta = true.
    - Called from selectCandidates, the produced candidates are byte-identical to before the extraction (regression neutrality — existing candidate-selection suite green).
  </behavior>
  <action>
    Extract the inline loop at candidate-selection.ts:330-343 into an exported pure function
    `resolveEventExit(frontExpiryIso: string, events: ReadonlyArray<EconomicEvent>):
    { exitBeforeIso: string | null; eventInPeakTheta: boolean }`, preserving the exact
    day-number math (isoDayNumber, EVENT_BLACKOUT_DAYS, PEAK_THETA_DAYS, the `dayBefore`
    computation, the earliest-event `<` selection). Replace the inline block in
    `selectCandidates` with a call to it. Pure, no I/O — mirrors `legSpansEvents`'s exported
    shape. This is a behavior-preserving refactor; add direct unit tests for the helper AND
    confirm the existing selection suite stays green.
  </action>
  <verify>
    <automated>bunx vitest run packages/core/src/picker/domain/candidate-selection.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bunx vitest run packages/core/src/picker/domain/candidate-selection.test.ts` green — new resolveEventExit direct tests + unchanged selectCandidates cases.
    - `rg -n "export function resolveEventExit" packages/core/src/picker/domain/candidate-selection.ts` = 1 match; selectCandidates calls it.
    - `bun run test -- --project=core && bun run typecheck && bun run lint` clean (no behavior drift in the live engine).
  </acceptance_criteria>
  <done>resolveEventExit is a shared exported pure function; selectCandidates uses it with zero behavior change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client→API (schema) | analyzeAdHocCalendarRequest is the FIRST user-typed numeric input this bounded context accepts; the schema IS the validation control (V5). |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-05 | Tampering | Adversarial leg data (iv:0, dte:0, negative strike, Infinity) → NaN scores | high | mitigate | `.finite().positive()` / `.int().positive()` + `backDte > frontDte` refine reject before any BSM math (enforced at 30-05 route via zValidator) |
| T-30-06 | Tampering | Client-supplied spot spoofs pricing-critical greeks | high | mitigate | Request schema omits `spot` entirely + `.strict()` rejects it; server derives spot from readPickerSnapshot (30-04) |
| T-30-07 | Tampering | Call calendar smuggled into the put-only scoring pipeline (RawCandidateLeg is "P"-only) | medium | mitigate | `putCall: z.literal("P")` rejects calls at the boundary (D-03); calls keep the client unscored fallback (30-06) |
| T-30-08 | Repudiation | Extraction silently changes live selectCandidates scoring | high | mitigate | Behavior-preserving refactor proven by the unchanged existing selection suite staying green |
</threat_model>

<verification>
- `bunx vitest run packages/contracts/src/picker.test.ts packages/core/src/picker/domain/candidate-selection.test.ts` green.
- `bun run test -- --project=core --project=contracts && bun run typecheck && bun run lint` clean.
</verification>

<success_criteria>
The shared analyze schemas and the reusable event-blackout helper exist and are validated; the live engine's selection behavior is unchanged.
</success_criteria>

<output>
Create `.planning/phases/30-analyzer-pasted-calendar-fix-payoff-graph-x-domain-must-fit-/30-03-SUMMARY.md` when done.
</output>
