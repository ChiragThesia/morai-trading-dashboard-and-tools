---
phase: 30-analyzer-pasted-calendar-fix
plan: 05
type: tdd
wave: 3
depends_on: ["30-04", "30-03"]
files_modified:
  - apps/server/src/adapters/http/picker.routes.ts
  - apps/server/src/adapters/http/picker.routes.test.ts
  - apps/server/src/adapters/mcp/tools.ts
  - apps/server/src/adapters/mcp/tools.test.ts
  - apps/server/src/main.ts
autonomous: true
requirements: [D-02]
must_haves:
  truths:
    - "POST /api/picker/analyze Zod-validates the body, calls the use-case, and returns 200 {scored, candidate, reason} — scored:false (200, reason string) when context is unavailable, never a hard error for the paste flow (D-02, binding #2)"
    - "The route mounts inside the authenticated apiRouter (same Bearer group as /api/picker/candidates) — no new unauthenticated surface (ASVS V2)"
    - "The MCP analyze_ad_hoc_calendar tool uses the SAME analyzeAdHocCalendarRequest schema (MCP-02, one schema two adapters)"
    - "Server never trusts a client spot; the route derives it via the use-case's snapshot read (threat mitigation)"
    - "A StorageError maps to flat {error:'internal'} 500 — no DB internals leaked"
  artifacts:
    - "apps/server/src/adapters/http/picker.routes.ts — POST /picker/analyze handler"
    - "apps/server/src/adapters/mcp/tools.ts — registerAnalyzeAdHocCalendarTool"
    - "apps/server/src/main.ts — analyzeAdHocCalendar use-case composed with all 6 read ports + route/tool wiring"
  key_links:
    - "analyzeAdHocCalendarRequest → zValidator (HTTP) + inputSchema (MCP), one schema"
    - "server composition root builds economicEventsRepo + pickerHistoryRepo + readGexContextForPicker (mirror worker main.ts)"
---

<objective>
Expose the ad-hoc scoring use-case (D-02) over both adapter surfaces in the same PR
(architecture rule 9): a thin `POST /api/picker/analyze` HTTP route and an
`analyze_ad_hoc_calendar` MCP tool, both driven by one `analyzeAdHocCalendarRequest` schema
(MCP-02). Per binding #2 the endpoint ALWAYS returns 200 with `{scored, candidate, reason}` —
`scored:false` (+ reason) when the scoring context is unavailable, so the paste flow never
sees a hard error.

Purpose: this is the authenticated compute endpoint — the phase's one real trust boundary.
Zod strict, no client spot, bounded reads, flat error mapping.
Output: route + MCP tool + server composition-root wiring, all TDD/integration-tested.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-analyzer-pasted-calendar-fix-payoff-graph-x-domain-must-fit-/30-RESEARCH.md
@.planning/phases/30-analyzer-pasted-calendar-fix-payoff-graph-x-domain-must-fit-/30-PATTERNS.md
@apps/server/src/adapters/http/picker.routes.ts
@apps/server/src/adapters/http/settings.routes.ts
@apps/server/src/adapters/mcp/tools.ts
</context>

<artifacts>
## Artifacts this plan produces
- `POST /picker/analyze` handler in `pickerRoutes(...)` — `apps/server/src/adapters/http/picker.routes.ts` (pickerRoutes now also takes the analyze use-case)
- `registerAnalyzeAdHocCalendarTool(server, analyzeAdHocCalendar)` — `apps/server/src/adapters/mcp/tools.ts`
- `apps/server/src/main.ts` — `analyzeAdHocCalendar = makeAnalyzeAdHocCalendarUseCase({...})` + route arg + MCP registration; new server-root repos: `economicEventsRepo`, `pickerHistoryRepo`, `readGexContextForPicker`
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: POST /picker/analyze route (Zod → use-case → Result mapping)</name>
  <files>apps/server/src/adapters/http/picker.routes.ts, apps/server/src/adapters/http/picker.routes.test.ts</files>
  <read_first>
    - apps/server/src/adapters/http/picker.routes.ts (existing GET handler + T-19-16/17 comments)
    - apps/server/src/adapters/http/settings.routes.ts (zValidator("json", schema) → use-case → Result → parse-through-response precedent)
    - .planning/phases/30-.../30-RESEARCH.md (Code Examples — HTTP thin-adapter; Open Question 2; Security Domain V2/V5)
    - .planning/phases/30-.../30-PATTERNS.md (picker.routes.ts analog)
  </read_first>
  <behavior>
    - Valid PUT body + a snapshot present → 200 `{scored:true, candidate:<pickerCandidate>, reason:null}`.
    - Valid body but no snapshot / stale context → 200 `{scored:false, candidate:null, reason:"no-snapshot"}` (binding #2 — NOT 404, never a hard error for paste).
    - Invalid body (call, negative strike, extra spot key) → 400 from zValidator.
    - Use-case StorageError → 500 `{error:"internal"}` (no DB internals).
    - The response is parsed through analyzeAdHocCalendarResponse before sending.
  </behavior>
  <action>
    Extend `pickerRoutes` to also accept the `ForAnalyzingAdHocCalendar` use-case, and add
    `router.post("/picker/analyze", zValidator("json", analyzeAdHocCalendarRequest), handler)`.
    Handler (zero business logic): `const body = c.req.valid("json"); const result = await
    analyzeAdHocCalendar(body); if (!result.ok) return c.json({error:"internal"}, 500);
    return c.json(analyzeAdHocCalendarResponse.parse(result.value));`. Both `{scored:true,...}`
    and `{scored:false, reason}` are `result.ok` → both return 200 (binding #2). Document in a
    route comment that this endpoint computes ONE ad-hoc candidate synchronously (bounded reads,
    no chain scan) and does NOT recompute/persist the snapshot (distinct from GET, T-19-17).
    Spot is never in the body — the use-case derives it from the snapshot.
  </action>
  <verify>
    <automated>bunx vitest run apps/server/src/adapters/http/picker.routes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bunx vitest run apps/server/src/adapters/http/picker.routes.test.ts` green — 200-scored, 200-scored:false, 400-invalid, 500-storage cases (Hono route test with a fake use-case, mirrors settings.routes test precedent).
    - `rg -n "picker/analyze" apps/server/src/adapters/http/picker.routes.ts` = 1; handler has no business logic.
    - `bun run typecheck && bun run lint` clean.
  </acceptance_criteria>
  <done>The route validates, delegates, and maps Result → 200/400/500 with scored:false as a 200 non-error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: analyze_ad_hoc_calendar MCP tool + server composition-root wiring</name>
  <files>apps/server/src/adapters/mcp/tools.ts, apps/server/src/adapters/mcp/tools.test.ts, apps/server/src/main.ts</files>
  <read_first>
    - apps/server/src/adapters/mcp/tools.ts (registerGetPickerCandidatesTool :582-620 — copy this shape)
    - apps/server/src/main.ts (getPicker wiring :295-301, ruleOverridesRepo :200, mount + MCP registration :468/:516-531)
    - apps/worker/src/main.ts (readGexContextForPicker :579, economicEventsRepo :580, pickerHistoryRepo :582-583 — the repos the server root must ALSO build)
    - .planning/phases/30-.../30-PATTERNS.md (MCP tool analog; MCP-02 one-schema-two-adapters)
  </read_first>
  <behavior>
    - The MCP tool's inputSchema IS analyzeAdHocCalendarRequest (a one-sided field rename fails typecheck).
    - Invoked via a real McpServer + InMemoryTransport-linked Client (19-07 precedent, not a direct use-case call): a valid PUT input returns the scored candidate JSON; no-snapshot returns {scored:false,...} JSON; never throws.
    - main.ts composes analyzeAdHocCalendar with all 6 read ports + rate/dividendYield and passes it to pickerRoutes + registerAnalyzeAdHocCalendarTool.
  </behavior>
  <action>
    Add `registerAnalyzeAdHocCalendarTool(server, analyzeAdHocCalendar)` mirroring
    registerGetPickerCandidatesTool: `inputSchema` = the `analyzeAdHocCalendarRequest` shape
    (MCP-02), body calls the use-case, maps `!result.ok` → flat error content, and JSON-parses
    the value through analyzeAdHocCalendarResponse. In main.ts: build the server-root repos the
    use-case needs that are not yet composed — `economicEventsRepo`, `pickerHistoryRepo`
    (readDailySpotCloses/readPickerSlopeHistory), and the `readGexContextForPicker` adapter —
    mirroring apps/worker/src/main.ts:579-588; reuse the existing `pickerSnapshotRepo` and
    `ruleOverridesRepo` and the existing rate/dividendYield config. Compose
    `analyzeAdHocCalendar = makeAnalyzeAdHocCalendarUseCase({...})`, pass it into
    `pickerRoutes(getPicker, analyzeAdHocCalendar)` (still mounted in the authenticated
    apiRouter — ASVS V2), and register the MCP tool alongside get_picker_candidates.
  </action>
  <verify>
    <automated>bunx vitest run apps/server/src/adapters/mcp/tools.test.ts && bun run test -- --project=server</automated>
  </verify>
  <acceptance_criteria>
    - `bunx vitest run apps/server/src/adapters/mcp/tools.test.ts` green — real McpServer + InMemoryTransport invocation of analyze_ad_hoc_calendar (scored + no-snapshot).
    - `bun run test -- --project=server` green (full server suite).
    - `rg -n "registerAnalyzeAdHocCalendarTool|makeAnalyzeAdHocCalendarUseCase" apps/server/src/main.ts` shows both wired; the route stays inside the authenticated apiRouter.
    - `bun run typecheck && bun run lint` clean.
  </acceptance_criteria>
  <done>Both adapter surfaces expose ad-hoc scoring over one schema; the server root composes the use-case with every read port.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → API | POST /api/picker/analyze — the first authenticated compute endpoint that accepts user-typed leg data; the MCP tool shares the same boundary. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-13 | Spoofing / Elevation | Unauthenticated access to the compute endpoint | high | mitigate | Mount inside the existing authenticated apiRouter (Bearer group), same as /api/picker/candidates — no new public surface (ASVS V2) |
| T-30-14 | Tampering | Malformed/non-finite/non-put leg data reaching BSM math | high | mitigate | zValidator("json", analyzeAdHocCalendarRequest) `.strict()` finite/positive/int + backDte>frontDte reject at the edge (V5) |
| T-30-15 | Tampering | Client-supplied spot spoofs pricing | high | mitigate | Body has no spot field; use-case derives spot from readPickerSnapshot |
| T-30-16 | Information Disclosure | StorageError message leaks DB internals | medium | mitigate | Flat `{error:"internal"}` 500 (T-19-16 precedent) |
| T-30-17 | Denial of Service | Unbounded authenticated calls (each = ~5-6 single-row reads + BSM math) | low | accept | Authenticated internal tool, per-request cost bounded (no chain scan); no rate-limiting exists for peer endpoints (/picker/candidates, /settings/rules) — YAGNI, cost profile documented in the route comment |
| T-30-SC | Tampering | Supply-chain (package installs) | n/a | accept | No npm/pip/cargo installs this phase — all internal first-party modules (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `bun run test -- --project=server` green (route + MCP integration).
- `bun run typecheck && bun run lint` clean.
- `rg -n "picker/analyze" apps/server/src` shows the route inside the authenticated apiRouter mount.
</verification>

<success_criteria>
The ad-hoc scoring use-case is reachable over authenticated HTTP + MCP with one shared strict schema; context-unavailable returns 200 scored:false; the server root composes every read port.
</success_criteria>

<output>
Create `.planning/phases/30-analyzer-pasted-calendar-fix-payoff-graph-x-domain-must-fit-/30-05-SUMMARY.md` when done.
</output>
