---
phase: 30-analyzer-pasted-calendar-fix
plan: 06
type: execute
wave: 4
depends_on: ["30-05", "30-02"]
files_modified:
  - apps/web/src/lib/tos-parser.ts
  - apps/web/src/lib/tos-parser.test.ts
  - apps/web/src/hooks/useAnalyzeCalendar.ts
  - apps/web/src/hooks/useAnalyzeCalendar.test.ts
  - apps/web/src/screens/Analyzer.tsx
  - apps/web/src/components/picker/CandidateCard.tsx
  - apps/web/src/components/picker/CandidateCard.test.tsx
autonomous: true
requirements: [D-02]
must_haves:
  truths:
    - "Pasting a PUT calendar order fires POST /api/picker/analyze; a scored response renders real factor bars, θ GATE chip, WHY THIS CALENDAR, and ENTRY/EXIT PLAN — the 'not engine-scored' placeholder disappears (D-02, USER LOCKED)"
    - "scored:false (or a pasted CALL calendar) falls back to the existing parsedCalendarToPickerCandidate unscored display with the existing note — never a hard error (D-02, binding #2, binding #3)"
    - "The scored-vs-unscored branches key off candidate.breakdown.length (> 0 = scored), not the pasted id or a pasted boolean (D-02, binding #7, Pitfall 8)"
    - "CandidateCard shows a scored pasted card's real score/theta/vega while keeping the PASTED provenance badge (D-02, Pitfall 8/A4)"
  artifacts:
    - "apps/web/src/lib/tos-parser.ts — ParsedCalendar gains frontExpiry/backExpiry ISO dates"
    - "apps/web/src/hooks/useAnalyzeCalendar.ts — POST /api/picker/analyze mutation hook"
    - "apps/web/src/screens/Analyzer.tsx — handlePasteAnalyze calls the hook; note-gates switch to breakdown.length"
    - "apps/web/src/components/picker/CandidateCard.tsx — score/subline branch on breakdown.length>0"
  key_links:
    - "parseTosOrder → {legs + frontExpiry/backExpiry} → useAnalyzeCalendar → scored candidate → shared candidate→position→PayoffChart path"
    - "scored:false / call → parsedCalendarToPickerCandidate fallback (unchanged)"
---

<objective>
Close Defect 2 (D-02, USER LOCKED) on the client: pasting a TOS PUT calendar now POSTs to
`/api/picker/analyze` and, on success, renders the SAME entry analysis an engine candidate
gets. Parsing stays client-side (binding #6); the endpoint receives parsed legs. Calls and
context-unavailable pastes keep today's unscored fallback (binding #2/#3). The "not
engine-scored" gates flip from the pasted id to `candidate.breakdown.length` (binding #7),
and CandidateCard stops hiding a scored pasted card's numbers (Pitfall 8).

Purpose: this is the user-visible payoff of the whole Defect-2 chain — the placeholder
disappears for successfully scored pastes.
Output: an expanded parser, a mutation hook, rewired paste flow, and a corrected CandidateCard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-analyzer-pasted-calendar-fix-payoff-graph-x-domain-must-fit-/30-RESEARCH.md
@.planning/phases/30-analyzer-pasted-calendar-fix-payoff-graph-x-domain-must-fit-/30-PATTERNS.md
@apps/web/src/lib/tos-parser.ts
@apps/web/src/hooks/usePicker.ts
@apps/web/src/lib/parsed-calendar-to-candidate.ts
</context>

<artifacts>
## Artifacts this plan produces
- `ParsedCalendar.frontExpiry` / `.backExpiry` (ISO `YYYY-MM-DD`) — `apps/web/src/lib/tos-parser.ts`
- `useAnalyzeCalendar()` — mutation hook POSTing `analyzeAdHocCalendarRequest`, returning `analyzeAdHocCalendarResponse` — `apps/web/src/hooks/useAnalyzeCalendar.ts`
- rewired `handlePasteAnalyze` in `apps/web/src/screens/Analyzer.tsx`; note-gates on `candidate.breakdown.length === 0`
- `CandidateCard.tsx` — score/subline branch on `candidate.breakdown.length > 0`, PASTED badge retained
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ParsedCalendar carries ISO expiry dates</name>
  <files>apps/web/src/lib/tos-parser.ts, apps/web/src/lib/tos-parser.test.ts</files>
  <read_first>
    - apps/web/src/lib/tos-parser.ts (ParsedCalendar type :53-73, parseTosOrder — frontMs/backMs computed internally then discarded, Rule 5/6)
    - .planning/phases/30-.../30-RESEARCH.md (Pitfall 7 — reconstruct expiry server-side is a calendar-day bug class; add it to the parser instead)
  </read_first>
  <behavior>
    - parseTosOrder on an order with `19 JUL 26 / 16 AUG 26` returns frontExpiry "2026-07-19" and backExpiry "2026-08-16" (ISO date, from the same frontMs/backMs already computed).
    - frontExpiry < backExpiry always; unchanged on all existing parse cases (additive fields).
  </behavior>
  <action>
    Add `readonly frontExpiry: string` and `readonly backExpiry: string` (ISO `YYYY-MM-DD`) to
    `ParsedCalendar`, populated from the existing internal `frontMs`/`backMs` before they are
    reduced to day counts (they already exist — do NOT reconstruct from `asOf + dte`
    server-side, Pitfall 7). Additive only — existing frontDte/backDte/iv/etc. unchanged.
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/lib/tos-parser.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bunx vitest run apps/web/src/lib/tos-parser.test.ts` green — new case asserts exact ISO frontExpiry/backExpiry; existing cases unchanged.
    - `rg -n "frontExpiry|backExpiry" apps/web/src/lib/tos-parser.ts` shows both on ParsedCalendar.
    - `bun run typecheck` clean.
  </acceptance_criteria>
  <done>ParsedCalendar exposes real ISO expiry dates for the endpoint's exitPlan/event-span logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useAnalyzeCalendar hook + rewired paste flow</name>
  <files>apps/web/src/hooks/useAnalyzeCalendar.ts, apps/web/src/hooks/useAnalyzeCalendar.test.ts, apps/web/src/screens/Analyzer.tsx</files>
  <read_first>
    - apps/web/src/hooks/usePicker.ts (404/non-error convention; RPC client shape)
    - apps/web/src/screens/Analyzer.tsx (handlePasteAnalyze :532-546, isPastedId render gates :306, :414, :422, PASTED_NOT_SCORED_NOTE :69, parsedCalendarToPickerCandidate usage)
    - apps/web/src/lib/parsed-calendar-to-candidate.ts (the fallback builder — now fallback-only)
    - .planning/phases/30-.../30-PATTERNS.md (useAnalyzeCalendar analog; Analyzer paste-flow section)
  </read_first>
  <behavior>
    - Pasting a PUT order calls useAnalyzeCalendar with the parsed legs + ISO expiries; on `{scored:true}` the returned candidate (breakdown populated) is added to the rail and selected.
    - On `{scored:false}` OR a parsed CALL calendar (never sent to the endpoint — binding #3), it falls back to parsedCalendarToPickerCandidate (breakdown:[]) with the existing note.
    - The three "not engine-scored" render gates (scorecard, WHY, ENTRY/EXIT) show the note only when `candidate.breakdown.length === 0` (binding #7) — a scored pasted candidate renders full panels.
    - A network error (not scored:false) surfaces the existing paste-error copy, not a crash.
  </behavior>
  <action>
    Create `useAnalyzeCalendar()` — a mutation hook (mirror useRepullChains/usePicker shape)
    POSTing `analyzeAdHocCalendarRequest` to `/api/picker/analyze` and returning the parsed
    `analyzeAdHocCalendarResponse`; treat `scored:false` as a normal (non-error) result.
    Rewire `handlePasteAnalyze`: after parseTosOrder, if `parsed.type === "P"` call the hook
    with `{putCall:"P", strike, frontDte, backDte, frontIv:parsed.iv, backIv:parsed.iv, debit:
    parsed.debit ?? 0, qty, frontExpiry, backExpiry}`; on scored:true use the returned
    candidate, else fall back to parsedCalendarToPickerCandidate. For `parsed.type === "C"`
    use the fallback directly (D-03). Keep the pasted-prefix id for provenance. Replace the
    three `isPastedId(candidate.id)` note-gates (:306, :414, :422) with
    `candidate.breakdown.length === 0`. Do not re-introduce a per-call gate recompute (the gate
    chip/entry-gate panel already read the snapshot the Analyzer holds via usePicker).
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/hooks/useAnalyzeCalendar.test.ts apps/web/src/screens/Analyzer.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `bunx vitest run apps/web/src/hooks/useAnalyzeCalendar.test.ts apps/web/src/screens/Analyzer.test.tsx` green — hook success/scored:false/network-error (msw) + Analyzer scored-paste renders panels, unscored-paste shows the note, call-paste uses fallback.
    - `rg -n "isPastedId\(candidate.id\)" apps/web/src/screens/Analyzer.tsx` on the three render gates returns 0 (replaced by breakdown.length).
    - `bun run typecheck && bun run lint` clean.
  </acceptance_criteria>
  <done>Pasting a PUT calendar scores it via the endpoint and renders full engine panels; calls/scored:false keep the unscored fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CandidateCard shows a scored pasted card's real numbers (Pitfall 8)</name>
  <files>apps/web/src/components/picker/CandidateCard.tsx, apps/web/src/components/picker/CandidateCard.test.tsx</files>
  <read_first>
    - apps/web/src/components/picker/CandidateCard.tsx (pasted && header-pill branch :161-183, subline branch :195-226, factor bars :228-252)
    - .planning/phases/30-.../30-RESEARCH.md (Pitfall 8, Assumption A4 — keep the badge, show the score)
  </read_first>
  <behavior>
    - A pasted card with `breakdown.length > 0` shows its numeric score + theta/vega subline (like an engine card) AND keeps a small "PASTED" provenance badge.
    - A pasted card with `breakdown.length === 0` keeps today's PASTED-pill-only, no-score display.
    - The factor-bars block (already unconditional on breakdown) is untouched.
  </behavior>
  <action>
    Change the `pasted &&` header-pill and subline branches (:161-183, :195-226) to gate the
    score/theta/vega/events display on `candidate.breakdown.length > 0` rather than the raw
    `pasted` boolean. Keep rendering a "PASTED" identification badge when `pasted` is true (A4
    — provenance retained), but no longer suppress the real score/subline once the card is
    scored. Do NOT touch the breakdown-bars block (:228-252).
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/components/picker/CandidateCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `bunx vitest run apps/web/src/components/picker/CandidateCard.test.tsx` green — scored-pasted (score + subline + badge) and unscored-pasted (pill only) cases.
    - `bun run test -- --project=web && bun run typecheck && bun run lint` clean (full web suite; Overview + Analyzer + charts non-regression).
  </acceptance_criteria>
  <done>A scored pasted card reads like an engine card (score + subline) while keeping the PASTED provenance badge; unscored pastes unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser → API | The paste hook is the client caller of the 30-05 endpoint; TOS parsing stays client-side (existing T-09-06 controls: string-only, no eval, no DOM write). |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-18 | Tampering | Parsed leg data sent to the endpoint could be malformed | medium | mitigate | The server re-validates via zValidator (30-05); the client sends only parser-produced finite fields |
| T-30-19 | Denial of Service | A scoring outage hard-blocks the paste flow | low | mitigate | scored:false + network-error both fall back to the unscored display (binding #2) — the paste flow never hard-fails |
| T-30-20 | Information Disclosure | Rendering a fabricated score for an unscored paste | low | mitigate | Score/subline gate on breakdown.length>0 (Pitfall 8) — an unscored card never shows a fake score |
</threat_model>

<verification>
- `bun run test -- --project=web` green (tos-parser, useAnalyzeCalendar, Analyzer, CandidateCard, Overview non-regression).
- `bun run typecheck && bun run lint` clean.
- Manual (UAT, /gsd-verify-work): paste the user's 7500P order on morai.wtf → full tent + real score card (factor bars + θ GATE + WHY + ENTRY/EXIT); with a Phase-29 override active, the pasted score reflects it.
</verification>

<success_criteria>
Pasting a PUT calendar produces the same entry analysis as an engine candidate (score + bars + θ GATE + gate verdict + exit plan); calls and context-unavailable pastes keep the honest unscored fallback. Defect 2 is fully delivered.
</success_criteria>

<output>
Create `.planning/phases/30-analyzer-pasted-calendar-fix-payoff-graph-x-domain-must-fit-/30-06-SUMMARY.md` when done.
</output>
