---
phase: 30-analyzer-pasted-calendar-fix
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - apps/web/src/screens/Analyzer.tsx
  - apps/web/src/screens/Overview.tsx
  - apps/web/src/screens/Overview.test.tsx
autonomous: true
requirements: [D-01]
must_haves:
  truths:
    - "The Analyzer payoff chart fits the selected/combined book's full tent — no clipped tails or BEs, for pasted or engine calendars (D-01)"
    - "The Overview combined-book payoff chart still renders correctly for a multi-strike live book — no regression (D-01, Pitfall 4)"
    - "Both screens compute ONE domain via computePayoffDomain and pass it to BOTH repriceScenario and <PayoffChart domain=> (D-01, Pitfall 1)"
  artifacts:
    - "apps/web/src/screens/Analyzer.tsx — domain memo threaded into repriceScenario + PayoffChart"
    - "apps/web/src/screens/Overview.tsx — domain memo threaded into repriceScenario + PayoffChart"
  key_links:
    - "combinedPositions/calendarPositions + spot → computePayoffDomain → repriceScenario(domain) + <PayoffChart domain=>"
---

<objective>
Wire the Plan-01 domain primitives into the two real `<PayoffChart>` consumers (Analyzer's
selected/combined book, Overview's live combined book) so the payoff graph fits the full
tent on screen (D-01, USER LOCKED). This is the visible half of Defect 1: the user's 7500P
paste now shows its left tail and both breakevens.

Purpose: PayoffChart's `domain` prop is now required (30-01) — both screens must supply it,
and the SAME `{min,max}` must feed `repriceScenario`'s data grid or the curve data still
clips (Pitfall 1).
Output: two screens compute and thread the domain; Overview combined-book non-regression test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-analyzer-pasted-calendar-fix-payoff-graph-x-domain-must-fit-/30-RESEARCH.md
@.planning/phases/30-analyzer-pasted-calendar-fix-payoff-graph-x-domain-must-fit-/30-PATTERNS.md
@apps/web/src/lib/payoff-domain.ts
</context>

<artifacts>
## Artifacts this plan produces
- `apps/web/src/screens/Analyzer.tsx` — `payoffDomain = useMemo(computePayoffDomain(combinedPositions, spot, params))`, threaded to `repriceScenario(..., payoffDomain)` and `<PayoffChart domain={payoffDomain} />`
- `apps/web/src/screens/Overview.tsx` — same wiring for the combined-book `<PayoffChart>` (:1138)
- `apps/web/src/screens/Overview.test.tsx` — combined-book multi-strike non-regression test
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Thread the domain through the Analyzer payoff path</name>
  <files>apps/web/src/screens/Analyzer.tsx</files>
  <read_first>
    - apps/web/src/screens/Analyzer.tsx (params memo :527-530, combinedPositions :585-588, scenarioResult :590-593, `<PayoffChart` :761)
    - apps/web/src/lib/payoff-domain.ts (computePayoffDomain signature)
    - .planning/phases/30-.../30-PATTERNS.md (Analyzer/Overview call-site section)
  </read_first>
  <action>
    Add a `payoffDomain` useMemo = `computePayoffDomain(combinedPositions, spot, params)`
    (deps `[combinedPositions, spot, params]`). Pass it as the 3rd arg to `repriceScenario`
    in the `scenarioResult` memo. Pass `domain={payoffDomain}` to `<PayoffChart>` at :761.
    Empty-book case (`combinedPositions.length === 0`, `scenarioResult === null`) still needs
    a domain for the chart if it renders — reuse computePayoffDomain's empty fallback. Do NOT
    touch the paste flow (`handlePasteAnalyze`) or `isPastedId` gates — those are 30-06.
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/screens/Analyzer.test.tsx && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `bunx vitest run apps/web/src/screens/Analyzer.test.tsx` green (existing suite).
    - `rg -n "computePayoffDomain|domain=\{payoffDomain\}" apps/web/src/screens/Analyzer.tsx` shows the memo + the prop pass.
    - `repriceScenario(combinedPositions, params, payoffDomain)` — domain is the 3rd arg.
    - `bun run typecheck` clean (PayoffChart's required domain prop satisfied).
  </acceptance_criteria>
  <done>Analyzer computes one domain and threads it into both the data grid and the chart scale.</done>
</task>

<task type="auto">
  <name>Task 2: Thread the domain through the Overview combined book + non-regression test</name>
  <files>apps/web/src/screens/Overview.tsx, apps/web/src/screens/Overview.test.tsx</files>
  <read_first>
    - apps/web/src/screens/Overview.tsx (scenario source + `<PayoffChart` :1138)
    - .planning/phases/30-.../30-RESEARCH.md (Pitfall 4 — multi-strike combined book)
  </read_first>
  <action>
    Mirror Task 1 in Overview: compute `payoffDomain = computePayoffDomain(<the same
    positions array that feeds this scenario>, spot, params)`, pass it as the 3rd arg to the
    Overview `repriceScenario` call, and add `domain={payoffDomain}` to the `<PayoffChart>` at
    :1138. The positions array here is the FULL live combined book (possibly several strikes)
    — pass all of them, never a single-candidate slice (Pitfall 4). Add an Overview test that
    renders the combined book with two calendars at widely different strikes (e.g. 7000 and
    7600) and asserts the chart's domain brackets both (walls pin correctly, no clip).
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/screens/Overview.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `bunx vitest run apps/web/src/screens/Overview.test.tsx` green including the new multi-strike combined-book case.
    - `rg -n "computePayoffDomain|domain=\{payoffDomain\}" apps/web/src/screens/Overview.tsx` shows memo + prop.
    - `bun run test -- --project=web && bun run typecheck && bun run lint` clean (whole web project — Overview non-regression proven).
  </acceptance_criteria>
  <done>Overview combined-book chart fits a multi-strike book with no regression; both screens share the one-domain discipline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none new | Client screen wiring of an already-tested pure function; no new input or I/O surface. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-03 | Tampering | Overview book regression (domain fit for Analyzer clips Overview's multi-strike book) | medium | mitigate | computePayoffDomain takes N positions (Pitfall 4); explicit Overview multi-strike non-regression test in Task 2 |
| T-30-04 | Denial of Service | domain memo recomputes the wide-pass reprice on every render | low | accept | Memoized on `[positions, spot, params]` — recomputes only on book/spot change, not per frame |
</threat_model>

<verification>
- `bun run test -- --project=web` green (Analyzer + Overview + PayoffChart + payoff-domain suites).
- `bun run typecheck && bun run lint` clean.
- Manual (UAT, deferred to /gsd-verify-work): paste the user's 7500P order → left BE 7150 + full left tail visible.
</verification>

<success_criteria>
Both payoff charts fit the full tent on screen; Overview's combined book does not regress. Defect 1 is visibly fixed.
</success_criteria>

<output>
Create `.planning/phases/30-analyzer-pasted-calendar-fix-payoff-graph-x-domain-must-fit-/30-02-SUMMARY.md` when done.
</output>
