---
phase: 40-journal-history-repair-never-lose-a-calendars-greek-vol-story
plan: 08
type: execute
wave: 7
depends_on: ["40-06", "40-07"]
files_modified:
  - .planning/phases/40-journal-history-repair-never-lose-a-calendars-greek-vol-story/40-08-SUMMARY.md
autonomous: false
requirements: [HIST-01, HIST-02, HIST-03, HIST-04, HIST-05]
must_haves:
  truths:
    - "Full suite + typecheck + lint are green, and the D-09 regression gate (backtest + exit suites) stays green — with a SUMMARY note that historical exit/backtest verdicts may shift in prod after the repair changes journal inputs (D-09)"
    - "The live diagnostic SQL confirms whether the SPXW-rooted back-leg data exists in prod leg_observations (RESEARCH Open Question 3) BEFORE the repair run — and the outcome is handled both ways (data exists → rebuild heals; data absent → the D-04 targeted fetch starts capturing it correctly from the next cycle, gaps stay honest)"
    - "The one-time prod repair runs via trigger_job repair-journal-history after deploy, with per-calendar before/after coverage counts (rows, non-gap rows, days covered) captured (D-10)"
    - "Both open calendars' lifecycle series show non-gap greek/vol/price/term runs wherever leg_observations has both legs' marks (the Jul-06→Jul-08 window is the concrete testbed)"
  artifacts:
    - .planning/phases/40-journal-history-repair-never-lose-a-calendars-greek-vol-story/40-08-SUMMARY.md
  key_links:
    - "the prod repair is enqueued via the existing trigger_job MCP tool (repair-journal-history, heal-only), NOT direct psql — the mechanism plan 07 shipped"
    - "the lifecycle-chart UI is unchanged (D-01): the acceptance is that the repaired DATA lights up the four panels that already render gaps honestly"
---

<objective>
Close Phase 40: prove the code green, gather the live evidence RESEARCH deferred (the diagnostic SQL for Open Question 3), run the one-time prod repair through `trigger_job` after deploy, and confirm the two open calendars' lifecycle series come back to life. This is the integration gate — no new feature code; it verifies, deploys, repairs, and records.

Per the orchestration constraints: the diagnostic SQL is evidence-gathering here (it could not run pre-execution), the prod repair RUN is the post-deploy operator/orchestrator step (not a mid-plan autonomous write), and the fix is designed to handle Open Question 3 BOTH ways — if the archive already has the SPXW back leg, the rebuild heals it; if not, plan 02's root fix makes the D-04 targeted fetch start capturing it correctly from the next chain cycle, and the gaps stay honest (D-04) until it does.

D-09: the backtest/exit suites read journal rows, so the repaired history changes their inputs. That is the point (better data). The suites must stay green on fixtures, and the SUMMARY must note that prod exit/backtest verdicts may shift after the repair.

Purpose: Verified, deployed, repaired — the core-value data layer restored.
Output: 40-08-SUMMARY.md with suite results, diagnostic evidence, before/after coverage counts, and the lifecycle-chart confirmation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-journal-history-repair-never-lose-a-calendars-greek-vol-story/40-CONTEXT.md
@.planning/phases/40-journal-history-repair-never-lose-a-calendars-greek-vol-story/40-RESEARCH.md
@.planning/phases/40-journal-history-repair-never-lose-a-calendars-greek-vol-story/40-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration gate — full suite + typecheck + lint + D-09 regression</name>
  <files>.planning/phases/40-journal-history-repair-never-lose-a-calendars-greek-vol-story/40-08-SUMMARY.md</files>
  <read_first>
    - .planning/phases/40-.../40-VALIDATION.md (Sampling Rate + D-09 regression gate command)
    - .planning/phases/40-.../40-RESEARCH.md §Common Pitfalls (the four to spot-check did not regress)
  </read_first>
  <action>
    Run the full workspace suite (`bun run test`), `bun run typecheck`, and `bun run lint`; confirm all green. Run the D-09 regression gate explicitly (`bun run test -- packages/core/src/backtest packages/core/src/exits`) and confirm the backtest + exit suites are green WITHOUT changing their assertions (a genuine behavior change would be intentional and called out). Record the results in 40-08-SUMMARY.md, including an explicit D-09 note: historical exit/backtest verdicts may shift in prod once the repair backfills journal history, since those consumers read journal rows — this is expected (better inputs), not a regression. Do not run the prod repair in this task.
  </action>
  <verify>
    <automated>bun run test &amp;&amp; bun run typecheck &amp;&amp; bun run lint &amp;&amp; bun run test -- packages/core/src/backtest packages/core/src/exits</automated>
  </verify>
  <acceptance_criteria>
    - Full suite, typecheck, lint all green (output recorded in SUMMARY).
    - Backtest + exit suites green with unchanged assertions.
    - SUMMARY carries the D-09 verdict-shift note.
  </acceptance_criteria>
  <done>The phase's code is proven green, including the D-09 consumer suites, with the verdict-shift caveat recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy + diagnostic SQL + one-time prod repair (before/after coverage)</name>
  <action>
    Operator/orchestrator step (post-deploy, per D-10): deploy worker + server, run the live diagnostic SQL to confirm Open Question 3, then enqueue the heal-only prod repair via trigger_job repair-journal-history and capture per-calendar before/after coverage counts. Follow the numbered how-to-verify steps exactly; record all evidence in 40-08-SUMMARY.md.
  </action>
  <what-built>
    Deploy the phase to prod and run the one-time repair via the mechanism plan 07 shipped: the repair-journal-history pg-boss job, enqueueable through the existing trigger_job MCP tool (heal-only), plus the operator CLI for the CLI-only trim. Before repairing, run the live diagnostic SQL to confirm the root-cause hypothesis (Open Question 3).
  </what-built>
  <how-to-verify>
    1. Deploy worker + server to Railway (per project memory: `railway up --service worker` and `railway up --service server` — `railway up` alone can SKIP a service; verify each by deploy timestamp, not git sha). The self-heal-journal + repair-journal-history queues register idempotently on worker boot.
    2. Run RESEARCH §Root Cause Chain diagnostic queries 1-2 against prod (via the available DB/MCP path):
       - Query 1: does the back leg's real contract exist, and under which root? Expect `occ_symbol='SPXW261130P07600000', root='SPXW'`.
       - Query 2: does leg_observations have healthy, BSM-processed rows under the SPXW root vs zero under the wrongly-queried `SPX   261130...`? Record the finding.
       - Handle BOTH outcomes explicitly: if SPXW data exists → the rebuild will heal the Jul-06→Jul-08 window; if it is absent → plan 02's root fix makes the next chain cycle capture it, and the gaps stay honest (D-04) until then — note which case is live.
    3. Enqueue the repair heal-only: `trigger_job repair-journal-history` with no calendarId (all calendars, heal-only — trim is NOT reachable via trigger_job). For each of the 17 calendars, capture before/after coverage counts (rows, non-gap rows, days covered) as printed/reported by the job.
    4. Confirm the worker logs show the repair completed without overwriting live rows and without writing outside any calendar's openedAt..closedAt.
    Record queries' results, the Open-Question-3 outcome, and the per-calendar before/after coverage table in 40-08-SUMMARY.md.
  </how-to-verify>
  <resume-signal>Type "approved" with the before/after coverage counts and the Open-Question-3 outcome, or describe issues.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Lifecycle-chart evidence — healed non-gap series for both open calendars</name>
  <action>
    Visual verification (D-01, UI unchanged): on morai.wtf confirm the repaired data lights up the four lifecycle panels for both open calendars over the Jul-06→Jul-08 testbed window, that genuinely-absent slots stay honest gaps, that the series is one row per 30-min slot, and that the self-heal-journal cron is registered/firing. Record which panels lit up and which windows stayed honestly gapped in 40-08-SUMMARY.md.
  </action>
  <what-built>
    The lifecycle chart is unchanged (D-01) — the acceptance is that the repaired DATA now lights up the four panels (greeks, front/back IV + term slope, price/strike, P&L) that already draw gaps honestly. Both open SPX calendars (7600P / 7200P) are the concrete testbed.
  </what-built>
  <how-to-verify>
    1. On morai.wtf, open Journal → the two open calendars (7600P and 7200P). For the Jul-06→Jul-08 window (the 49 previously all-gap rows), confirm the greek/vol/term/price panels now render continuous NON-GAP runs wherever leg_observations had both legs' marks — the "why did my calendar act that way" story is answered by SERIES, not points.
    2. Confirm any slot the archive genuinely lacks still renders as an honest gap (D-04) — not an interpolated/fabricated value.
    3. Confirm the series is one scheduled row per 30-min slot (HIST-05) — no top-of-hour duplicate/frozen pairs.
    4. Confirm the self-heal-journal cron is registered and firing on the worker (a QUEUED/completed run in pgboss), so future skips heal automatically.
    Record the visual confirmation (which panels lit up, which windows stayed honestly gapped) in 40-08-SUMMARY.md.
  </how-to-verify>
  <resume-signal>Type "approved" describing the healed panels + any windows that stayed honestly gapped, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy + prod repair trigger | A destructive or unbounded repair must not run silently; the diagnostic must precede the write |
| repaired journal → backtest/exit consumers | Changed inputs shift historical verdicts — expected, but must be visible |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-17 | Tampering (prod data) | one-time repair run | high | mitigate | Diagnostic SQL confirms the hypothesis BEFORE the write; the trigger_job path is heal-only (fill-only, never overwrites a live row, never writes outside the life window); before/after counts recorded (D-10) |
| T-40-18 | Repudiation | verdict shift in exit/backtest | medium | accept | D-09: verdict shifts are the intended consequence of better inputs; recorded in the SUMMARY, suites stay green on fixtures |
| T-40-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase (RESEARCH §Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `bun run test` + `bun run typecheck` + `bun run lint` green.
- `bun run test -- packages/core/src/backtest packages/core/src/exits` green (D-09).
- Diagnostic SQL run + Open-Question-3 outcome recorded.
- Prod repair run via trigger_job with before/after coverage counts recorded.
- Lifecycle chart shows healed non-gap series for both open calendars (Jul-06→Jul-08 testbed).
</verification>

<success_criteria>
- All suites/typecheck/lint green; D-09 regression suites green with the verdict-shift note.
- Deployed (worker + server); diagnostic SQL evidence captured; repair run with before/after coverage counts.
- Both open calendars' lifecycle panels show non-gap runs wherever the archive has both legs; honest gaps preserved; one row per 30-min slot; self-heal cron live.
</success_criteria>

## Artifacts this plan produces

- `.planning/phases/40-.../40-08-SUMMARY.md` — suite results, diagnostic-SQL evidence + Open-Question-3 outcome, per-calendar before/after coverage table, lifecycle-chart confirmation, D-09 verdict-shift note, deploy record

<output>
Create `.planning/phases/40-journal-history-repair-never-lose-a-calendars-greek-vol-story/40-08-SUMMARY.md` when done.
</output>
