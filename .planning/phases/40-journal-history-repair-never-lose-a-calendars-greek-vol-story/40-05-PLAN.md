---
phase: 40-journal-history-repair-never-lose-a-calendars-greek-vol-story
plan: 05
type: tdd
wave: 4
depends_on: ["40-04"]
files_modified:
  - packages/core/src/journal/application/rebuildCalendarHistory.ts
  - packages/core/src/journal/application/rebuildCalendarHistory.test.ts
  - packages/core/src/index.ts
autonomous: true
requirements: [HIST-02]
must_haves:
  truths:
    - "For each 30-min RTH slot in the clamped life window [max(openedAt, from) .. min(closedAt ?? now, to)], the rebuild resolves both legs' observations for that slot and builds the row with the SAME pure functions the live writer uses (computeLegPairMetrics + computeSnapshotPnl) — byte-identical, no formula drift (D-02)"
    - "A slot with no usable observation for either leg produces NOTHING — never an interpolated or fabricated row (D-04 honest-gap law)"
    - "The rebuild persists via the fill-only heal-write port only, so it can never overwrite a live (non-gap) row (D-03), and never writes a slot outside openedAt..closedAt (D-08)"
    - "The rebuild returns per-calendar coverage counts (slots considered, rows healed, honest-gap slots) for the operator before/after report"
  artifacts:
    - packages/core/src/journal/application/rebuildCalendarHistory.ts
  key_links:
    - "rebuildCalendarHistory consumes ForResolvingLegObservationForSlot + ForHealingSnapshot (plan 04) and the exported computeLegPairMetrics/computeSnapshotPnl (snapshotCalendars.ts) — one row-derivation path shared with the live writer"
---

<objective>
Build HIST-02's pure rebuild use-case: derive `calendar_snapshots` rows for a calendar from historical `leg_observations`, reusing the EXACT pure functions the live writer uses (D-02, no formula drift) and the fill-only + windowed storage ports from plan 04. This is the single derivation engine the self-heal job (plan 06) and the operator repair (plan 07) both call — parametrized by a date-range window (bounded 7-day for self-heal, unbounded for the CLI), clamped internally to the life window so it can never write outside `openedAt..closedAt` (D-08).

D-04's honest-gap law is enforced by construction: where `leg_observations` has no usable observation for a slot, the use-case produces nothing — never an interpolated value. D-03's "live rows always win" is enforced by the port (plan 04's fill-only heal-write), not re-litigated here.

Purpose: One tested, drift-free rebuild engine the CLI/job/self-heal all reuse (RESEARCH Open Question 2 — share one use-case, not three implementations).
Output: `rebuildCalendarHistory` use-case + coverage counts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-journal-history-repair-never-lose-a-calendars-greek-vol-story/40-CONTEXT.md
@.planning/phases/40-journal-history-repair-never-lose-a-calendars-greek-vol-story/40-RESEARCH.md
@.planning/phases/40-journal-history-repair-never-lose-a-calendars-greek-vol-story/40-PATTERNS.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Pure slot-window enumeration (clamped to the life window, D-08)</name>
  <files>packages/core/src/journal/application/rebuildCalendarHistory.ts, packages/core/src/journal/application/rebuildCalendarHistory.test.ts</files>
  <read_first>
    - packages/core/src/journal/domain/rth-slot.ts (roundDownToRthSlot — plan 01; the slot-boundary definition to enumerate against)
    - packages/shared/src/rth-window.ts (isWithinRth — RTH membership filter for enumerated anchors)
    - packages/core/src/journal/application/ports.ts (Calendar — openedAt/closedAt fields that bound the window)
  </read_first>
  <behavior>
    - Given a Calendar and a requested window {from, to} plus now, enumerate the 30-min RTH slot anchors in [max(openedAt, from) .. min(closedAt ?? now, to)] — inclusive of the clamped bounds' slots, RTH-only (isWithinRth), sorted ascending, no duplicates.
    - No anchor is ever < openedAt or > (closedAt ?? now) — the D-08 write-window guarantee, proven by property test for ANY from/to (including from before openedAt and to after closedAt).
    - A window that clamps to empty (e.g. from > closedAt) yields zero anchors.
    - Pure: takes now explicitly, no Date.now().
  </behavior>
  <action>
    Add a private pure helper in rebuildCalendarHistory.ts that enumerates the 30-min RTH slot anchors within the clamped life window, reusing roundDownToRthSlot (slot definition) and isWithinRth (RTH filter). Export it for the test (test-only export line, like the codebase's existing test re-exports). Write the RED test first — example windows plus a fast-check property that every enumerated anchor lies within [max(openedAt,from), min(closedAt??now, to)] and is a valid RTH 30-min slot — and show it failing before implementing.
  </action>
  <verify>
    <automated>bun run test -- packages/core/src/journal/application/rebuildCalendarHistory.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Enumeration property (in-window, valid slots, sorted, dedup, empty-when-clamped) shown RED first, then green.
    - No anchor outside [openedAt, closedAt ?? now] for any from/to (fast-check).
    - No Date.now(); typecheck + lint clean.
  </acceptance_criteria>
  <done>The slot-window enumerator exists, is property-tested, and structurally guarantees D-08's write window.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: rebuildCalendarHistory use-case — D-02 reuse, D-04 honest gaps, fill-only heal</name>
  <files>packages/core/src/journal/application/rebuildCalendarHistory.ts, packages/core/src/journal/application/rebuildCalendarHistory.test.ts, packages/core/src/index.ts</files>
  <read_first>
    - packages/core/src/journal/application/snapshotCalendars.ts (computeLegPairMetrics + computeSnapshotPnl + buildSnapshotRow — the EXACT pure functions to reuse, D-02; the loop-and-gate shape to mirror)
    - packages/core/src/journal/application/ports.ts (ForResolvingLegObservationForSlot, ForHealingSnapshot, LegSnapshot, SnapshotRow, Calendar — plan 04's ports)
    - packages/core/src/journal/domain/attribution.ts (isGapRow — for the test's "row is non-gap" assertion; the port already uses it for the write decision)
    - .planning/phases/40-.../40-PATTERNS.md §Pattern C (rebuild mirrors the live writer; deps swap; D-02 reuse)
  </read_first>
  <behavior>
    - For each enumerated slot: resolve front + back via resolveLegObservationForSlot; when BOTH resolve to usable observations, build the row via computeLegPairMetrics(now=slotAnchor, front, back, qty, frontExpiry, backExpiry) + computeSnapshotPnl, stamp trigger='scheduled' and time=slotAnchor, and call healSnapshot(row). The built row is byte-identical to what the live writer's buildSnapshotRow would produce for the same legs at the same instant (assert field-by-field — D-02 no drift).
    - Honest gap (D-04): a slot where either leg has no usable observation produces NO healSnapshot call for that slot.
    - Fill-only (D-03): the use-case calls healSnapshot (never persistSnapshot), so a live non-gap row at a slot is preserved by the port.
    - Returns Result<RebuildCoverage, StorageError> where RebuildCoverage carries slotsConsidered, rowsHealed, honestGapSlots. Propagates StorageError from either port via err(...). Result-threaded, no try/catch in the use-case.
    - Pure clock injection (now passed in); no Date.now().
  </behavior>
  <action>
    Implement makeRebuildCalendarHistoryUseCase(deps) returning ForRunningRebuildCalendarHistory((calendar, window) => Result<RebuildCoverage, StorageError>). deps = { resolveLegObservationForSlot, healSnapshot, now }. Enumerate slots via Task 1's helper, resolve both legs per slot, reuse computeLegPairMetrics + computeSnapshotPnl verbatim (import from snapshotCalendars.ts — extract/move only if a pure re-export is needed, tests unchanged, D-02), skip honest-gap slots, heal the rest, accumulate coverage counts. Export the factory + ForRunningRebuildCalendarHistory + RebuildCoverage via @morai/core (top barrel; add to the journal application barrel if one exists). Write the RED tests first — byte-identical-to-live-writer for a fixture slot, honest-gap skip, coverage counts, StorageError propagation — and show them failing before implementing.
  </action>
  <verify>
    <automated>bun run test -- packages/core/src/journal/application/rebuildCalendarHistory.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Byte-identical-to-live-writer, honest-gap-skip, coverage-count, and error-propagation cases shown RED first, then green.
    - The rebuilt row equals buildSnapshotRow's output field-by-field for the same legs (D-02 proven, not asserted loosely).
    - No healSnapshot call for an honest-gap slot (D-04).
    - The use-case calls healSnapshot only (never persistSnapshot).
    - makeRebuildCalendarHistoryUseCase importable from @morai/core; typecheck + lint clean; no any/as/!.
  </acceptance_criteria>
  <done>The drift-free, honest-gap, fill-only rebuild engine exists and is exported for plans 06/07 to reuse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| leg_observations archive → derived snapshot row | A drifted formula or a fabricated gap-fill would corrupt the journal's greek/vol story |
| requested repair window → written slots | A window wider than the life window could write rows outside openedAt..closedAt |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-13 | Tampering (formula drift) | row derivation | high | mitigate | Reuse computeLegPairMetrics + computeSnapshotPnl VERBATIM; byte-identical-to-live-writer test is the proof (D-02) |
| T-40-03 | Tampering (honest-gap) | per-slot leg resolution | high | mitigate | No usable observation → produce nothing; test asserts zero heal calls for a gap slot (D-04) |
| T-40-01 | Tampering (out-of-window write) | slot enumeration | high | mitigate | Enumeration clamps to [max(openedAt,from), min(closedAt??now, to)]; fast-check proves no anchor escapes the life window (D-08) |
| T-40-02 | Tampering (overwrite live row) | heal write | high | mitigate | Use-case calls the fill-only heal-write port only (D-03) — never persistSnapshot; the port (plan 04) enforces no-op-when-live |
</threat_model>

<verification>
- `bun run test -- packages/core/src/journal/application/rebuildCalendarHistory.test.ts` green (example + property).
- `bun run typecheck` + `bun run lint` clean.
- Byte-identical-to-live-writer assertion present (D-02); honest-gap zero-heal assertion present (D-04).
</verification>

<success_criteria>
- rebuildCalendarHistory derives rows via the live writer's exact formulas, skips honest gaps, heals fill-only, stays within the life window, and reports coverage counts.
- Exported from @morai/core for reuse by the self-heal job and operator repair.
- No new dependency, no schema change, no any/as/!.
</success_criteria>

## Artifacts this plan produces

- Factory `makeRebuildCalendarHistoryUseCase(deps)` + driver `ForRunningRebuildCalendarHistory` — `packages/core/src/journal/application/rebuildCalendarHistory.ts` (via `@morai/core`)
- Type `RebuildCoverage` (slotsConsidered, rowsHealed, honestGapSlots)
- Private pure slot-window enumerator (test-exported)

<output>
Create `.planning/phases/40-journal-history-repair-never-lose-a-calendars-greek-vol-story/40-05-SUMMARY.md` when done.
</output>
