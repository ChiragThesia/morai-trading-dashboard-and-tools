---
phase: 23-vix3m-ingestion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/journal/application/fetchMacroSeries.ts
  - packages/core/src/journal/application/fetchMacroSeries.test.ts
  - packages/contracts/src/macro.ts
  - packages/contracts/src/macro.test.ts
  - packages/adapters/src/memory/fred-series.test.ts
  - packages/adapters/src/__contract__/macro-observations.contract.ts
  - apps/server/src/adapters/mcp/tools.ts
  - docs/architecture/data-model.md
  - docs/architecture/jobs.md
autonomous: true
requirements: [MACRO-01]

must_haves:
  truths:
    - "GET /api/analytics/macro returns a VXVCLS series and accepts ?series=VXVCLS without rejecting it (T-14-01 gate)"
    - "MCP get_macro returns VXVCLS alongside the existing 8 series, same twice-daily cadence"
    - "The existing fetch-rates cron ingests VXVCLS every run — no new job, no migration"
    - "A failed VXVCLS fetch names VXVCLS in the fail-loud finish (D-07); the other 8 series still persist"
    - "apps/web typecheck stays green — the closed enum widens safely, no web change"
  artifacts:
    - packages/core/src/journal/application/fetchMacroSeries.ts
    - packages/contracts/src/macro.ts
    - packages/adapters/src/memory/fred-series.test.ts
    - packages/adapters/src/__contract__/macro-observations.contract.ts
  key_links:
    - "MACRO_SERIES_IDS Zod enum MUST include VXVCLS — else macroQuery rejects ?series=VXVCLS and criterion 1 fails for the explicit-series path (T-14-01)"
    - "DEFAULT_FRED_SERIES_IDS MUST include VXVCLS — the worker composition root (apps/worker/src/main.ts) passes no fredSeriesIds, so this default array is the ONLY wire into the twice-daily cron"
---

<objective>
Make VIX3M (FRED series `VXVCLS`) a first-class member of the macro pipeline: append one id to
the core default-series constant and one member to the contracts Zod enum, driven by TDD test-
expectation updates, plus the memory-twin + contract-suite parity rows and honest doc counts.

Purpose: `macro_observations` has NO backfill — every day VXVCLS is not ingested is permanently
lost crisis-gate/regime-board/backtest history. This phase ships first and alone so accrual
starts on day one, before any consumer (Phase 24/28) needs the history.

Output: `VXVCLS` ingested by the existing twice-daily fetch-rates cron into `macro_observations`,
queryable via the existing `GET /api/analytics/macro` + MCP `get_macro` with zero new endpoint,
job, or migration. Failure degrades exactly like the other 8 series (D-07 fail-loud, never silent).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-vix3m-ingestion/23-CONTEXT.md
@CLAUDE.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/tdd.md
@.claude/rules/typescript.md

# The FRED adapter is already parameterized on seriesId (raw value, no /100, D-14) — VXVCLS
# flows through it unchanged. Read it only to confirm no adapter change is needed.
@packages/adapters/src/http/fred.ts
</context>

<artifacts_this_phase_produces>
Purely additive to existing constants/enum/tests/docs — NO new symbols or types created.

| Symbol / surface | File | Change |
|---|---|---|
| `DEFAULT_FRED_SERIES_IDS` | packages/core/src/journal/application/fetchMacroSeries.ts | append `"VXVCLS"` (7→8 FRED ids) + header/jsdoc count |
| `MACRO_SERIES_IDS` / `macroSeriesId` / `MacroSeriesId` | packages/contracts/src/macro.ts | append `"VXVCLS"` (8→9 series) + jsdoc count |
| fetchMacroSeries suite expectations | packages/core/src/journal/application/fetchMacroSeries.test.ts | persist-count expectations 8→9 / 6→7 / 7→8 + titles |
| MACRO_SERIES_IDS suite expectation | packages/contracts/src/macro.test.ts | literal array gains `"VXVCLS"`, title eight→nine |
| memory-twin VXVCLS seed test | packages/adapters/src/memory/fred-series.test.ts | new test: seed+read a VXVCLS index-level row |
| contract-suite VXVCLS row | packages/adapters/src/__contract__/macro-observations.contract.ts | assert a VXVCLS row persists+reads (no DB enum) |
| `get_macro` description string | apps/server/src/adapters/mcp/tools.ts | enumerate VXVCLS (cosmetic, D-discretion) |
| macro series counts | docs/architecture/data-model.md, docs/architecture/jobs.md | eight→nine series, 7→8 FRED |

Explicitly NOT changed (verified): `packages/adapters/src/http/fred.ts` (parameterized),
`apps/worker/src/main.ts` + `apps/worker/src/schedule.ts` (use the default, auto-picks up),
`apps/web/src/components/MacroCard.tsx` + `Overview.tsx` (Phase 24 owns display; `MacroSeriesId`
is used only as subset types — no `Record`/exhaustive switch — so widening keeps typecheck green).
</artifacts_this_phase_produces>

<tasks>

<task type="execute">
  <name>Task 1: Docs-first — correct macro series counts + get_macro description</name>
  <files>docs/architecture/data-model.md, docs/architecture/jobs.md, apps/server/src/adapters/mcp/tools.ts</files>
  <read_first>
    - docs/architecture/data-model.md around the `macro_observations` section ("Eight series land here…")
    - docs/architecture/jobs.md around the fetch-rates / macro fetch section ("7 FRED series…", "ANY of the 8 series…")
    - apps/server/src/adapters/mcp/tools.ts registerGetMacroTool description string
  </read_first>
  <action>
    Honor workflow.md docs-before-code. In data-model.md change "Eight series land here" to nine
    and add VXVCLS to the fred-sourced list next to VIXCLS, noting it is an index level stored raw
    (no /100, D-14) — the FRED id for 3-month VIX (VIX3M). In jobs.md change "7 FRED series (DFF,
    …, VIXCLS)" to eight and append VXVCLS to that id list, and change the "ANY of the 8 series"
    failure-policy count to nine. In tools.ts registerGetMacroTool, add VXVCLS to the enumerated
    FRED ids in the description string (cosmetic doc-string only, no logic — the get_macro handler
    is unchanged). Do NOT touch DGS3MO's double-write note or the D-07 wording. Match existing prose
    style (Hemingway); change only the counts and the id lists.
  </action>
  <verify>
    <automated>rg -c 'VXVCLS' docs/architecture/data-model.md docs/architecture/jobs.md apps/server/src/adapters/mcp/tools.ts</automated>
  </verify>
  <acceptance_criteria>
    data-model.md and jobs.md say "nine series" / "8 FRED series" with VXVCLS listed among the
    fred-sourced ids; the get_macro description enumerates VXVCLS; each of the three files contains
    at least one "VXVCLS" occurrence; no other prose changed.
  </acceptance_criteria>
  <done>data-model.md, jobs.md, and the get_macro description all name VXVCLS with corrected counts; no other prose touched.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Core ingestion — add VXVCLS to DEFAULT_FRED_SERIES_IDS (RED→GREEN)</name>
  <files>packages/core/src/journal/application/fetchMacroSeries.test.ts, packages/core/src/journal/application/fetchMacroSeries.ts</files>
  <read_first>
    - packages/core/src/journal/application/fetchMacroSeries.ts (DEFAULT_FRED_SERIES_IDS constant + header comment)
    - packages/core/src/journal/application/fetchMacroSeries.test.ts (the four suite cases and their hardcoded persist counts)
  </read_first>
  <behavior>
    - With VXVCLS in the default set, an all-success run persists 9 FRED + VVIX = 9 rows and returns ok(void)
    - The "all succeed" case asserts persisted ids equal `[...DEFAULT_FRED_SERIES_IDS, "VVIX"]` (already constant-driven — count must move 8→9)
    - Two fetch failures → 7 successes still persisted, err names both failed ids (D-07 unchanged)
    - One persist failure → 8 of 9 persisted; a rejected fetch promise → 8 persisted, absorbed not crashed
  </behavior>
  <action>
    RED first: in fetchMacroSeries.test.ts update the hardcoded persist-count expectations to the
    new totals — the all-success case from 8 to 9, the two-failure case from 6 to 7, the persist-
    failure and rejected-promise cases from 7 to 8 — and update the case titles/comments that name
    those counts (e.g. "all 8 series" → nine, "the 6 successes" → seven, "7 of 8" → eight of nine).
    Leave the `[...DEFAULT_FRED_SERIES_IDS, "VVIX"]` assertion referencing the constant (it follows
    automatically). Run the suite and confirm it FAILS for the right reason (count mismatch — the
    constant still has 7 FRED ids), showing the failure output. GREEN: append `"VXVCLS"` to
    DEFAULT_FRED_SERIES_IDS in fetchMacroSeries.ts (per D-06 CONTEXT / MACRO-01) and update the
    file header + constant jsdoc that say "seven FRED series"/"the seven FRED ids" to eight. Re-run;
    confirm green. Commit at green only (tdd.md). No production logic changes — the orchestration,
    Promise.allSettled independence, and D-07 finish are untouched.
  </action>
  <verify>
    <automated>bun run test -- packages/core/src/journal/application/fetchMacroSeries.test.ts</automated>
  </verify>
  <acceptance_criteria>
    DEFAULT_FRED_SERIES_IDS lists 8 FRED ids ending with VXVCLS; fetchMacroSeries.test.ts passes
    with the updated counts (9/7/8/8); the RED run was shown failing on the count before the
    constant changed; typecheck clean for @morai/core.
  </acceptance_criteria>
  <done>DEFAULT_FRED_SERIES_IDS ends with VXVCLS; the four fetchMacroSeries cases pass on counts 9/7/8/8 with the RED failure shown first.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Contracts enum + adapter parity — VXVCLS in MACRO_SERIES_IDS, memory-twin + contract rows (RED→GREEN)</name>
  <files>packages/contracts/src/macro.test.ts, packages/contracts/src/macro.ts, packages/adapters/src/memory/fred-series.test.ts, packages/adapters/src/__contract__/macro-observations.contract.ts</files>
  <read_first>
    - packages/contracts/src/macro.ts (MACRO_SERIES_IDS array + macroSeriesId enum + jsdoc)
    - packages/contracts/src/macro.test.ts ("contains all eight series ids" literal-array case)
    - packages/adapters/src/memory/fred-series.test.ts (seed/read twin cases, D-09 parity)
    - packages/adapters/src/__contract__/macro-observations.contract.ts (the "returns ALL rows across multiple series" case)
  </read_first>
  <behavior>
    - MACRO_SERIES_IDS equals the 9-element list ending with VXVCLS; macroQuery accepts series=VXVCLS
    - Memory FRED twin: a seeded VXVCLS index-level row reads back exactly (source fred, raw value, no /100 — D-09/D-14 parity)
    - Persistence contract: a VXVCLS row inserts and reads back across both the Postgres adapter and the in-memory twin (macro_observations.series is text, no DB enum constraint)
  </behavior>
  <action>
    RED first across the three test files: (a) in macro.test.ts add `"VXVCLS"` to the expected
    MACRO_SERIES_IDS literal array and change the title "eight" to "nine"; (b) in fred-series.test.ts
    add a case that seeds a VXVCLS row (an index level near 19, source `fred`, a YYYY-MM-DD date) and
    asserts fetchFredSeries("VXVCLS") returns ok with that exact raw value — proving index-level parity
    with VIXCLS, no /100; (c) in macro-observations.contract.ts include a VXVCLS row in the multi-series
    "returns ALL rows" case (source `fred`) and assert the read set contains VXVCLS — proving the DB
    accepts an arbitrary series id with no migration. Run the contracts + adapters suites and confirm
    they FAIL for the right reason (macro.test.ts on the missing id; the new rows exercising a series
    not yet in the enum), showing the failure output. GREEN: append `"VXVCLS"` to MACRO_SERIES_IDS in
    macro.ts and update the jsdoc "the eight series" to nine. Re-run all three suites; confirm green.
    Commit at green only. Ship the memory twin + contract row in the SAME change (architecture rule 8).
  </action>
  <verify>
    <automated>bun run test -- packages/contracts/src/macro.test.ts packages/adapters/src/memory/fred-series.test.ts packages/adapters/src/__contract__</automated>
  </verify>
  <acceptance_criteria>
    MACRO_SERIES_IDS is the 9-element enum ending with VXVCLS; macro.test.ts, fred-series.test.ts
    (new VXVCLS seed case), and the macro-observations contract suite (VXVCLS row) all pass against
    both memory and Postgres twins; the RED runs were shown failing first; `bun run typecheck` is
    clean across contracts, adapters, server, and web (closed enum widened without a web change).
  </acceptance_criteria>
  <done>MACRO_SERIES_IDS ends with VXVCLS; contracts + memory-twin + contract-suite VXVCLS cases pass (RED first); full typecheck incl. web is clean.</done>
</task>

</tasks>

<threat_model>
This phase adds ONE public FRED series id to an existing, already-hardened fetch pipeline. It
installs no packages and writes no new code path — every mitigation below is INHERITED from the
Phase 14 macro pipeline and requires no new work. ASVS L1, block on high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FRED HTTP → makeFredSeriesAdapter | Untrusted external JSON for VXVCLS crosses into the app (same path as the existing 7 FRED ids) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-01 | Tampering | fred.ts FredResponseSchema parse for VXVCLS | low | accept | Inherited: existing Zod parse + '.' sentinel filter + Number.isFinite guard reject malformed VXVCLS payloads exactly as for VIXCLS — no new code |
| T-23-02 | Denial of Service | fetchMacroSeries orchestration | low | accept | Inherited: Promise.allSettled per-series independence + per-task err absorption (D-07) — a hung/failing VXVCLS fetch never cascades to the other 8 |
| T-23-03 | Information Disclosure | FRED api_key in logs | low | accept | Inherited: makeFredSeriesAdapter logs static warn text only, never the key value (T-02-11/T-02-12); no change |
| T-23-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase (zero new dependencies, confirmed by research SUMMARY) — no supply-chain surface added |
</threat_model>

<verification>
Executor-run (automated, in-plan):
- `bun run test` for the three touched suites (Tasks 2 + 3) is green, with the RED failures shown first.
- `bun run typecheck` clean across core, contracts, adapters, server, AND web — the closed
  `macroSeriesId` enum widened with VXVCLS without forcing any web change (no `Record<MacroSeriesId>`
  or exhaustive switch exists; MacroCard/Overview use it only as subset types).
- `bun run lint` clean (no new lint surface — additive constant/enum members only).
- `rg -c '"VXVCLS"'` shows the id present in fetchMacroSeries.ts and macro.ts.

Post-deploy operator verification (phase-level — NOT part of the autonomous plan; deploy is a
separate `railway up` operator action). DEPLOY SAME DAY AS MERGE — un-ingested days are permanently
lost:
1. After the next scheduled `fetch-rates` run (09:00 or 18:30 ET, Mon-Fri), read prod:
   `SELECT series_id, date, value FROM macro_observations WHERE series_id = 'VXVCLS' ORDER BY date DESC LIMIT 3`
   → returns a fresh row with an index-level value (~19), source `fred`.
2. `GET /api/analytics/macro?series=VXVCLS` on prod returns a non-empty VXVCLS point array
   (proves the enum accepts the explicit-series filter — criterion 1).
3. MCP `get_macro` returns VXVCLS among the series keys.
</verification>

<success_criteria>
- VXVCLS is queryable via the existing `GET /api/analytics/macro` and MCP `get_macro` alongside the
  other 8 series, on the same twice-daily cadence (criterion 1) — enum accepts `?series=VXVCLS`.
- VXVCLS accrues daily in `macro_observations` from deploy — the cron picks it up with no new job,
  proven by the post-deploy prod DB read (criterion 2).
- A failed VXVCLS FRED fetch names VXVCLS in the fail-loud finish and never silently skips; the other
  8 series still persist (criterion 3, D-07) — covered by the fetchMacroSeries failure-case tests.
- All three touched test suites green (RED shown first); typecheck + lint clean incl. web; docs
  counts corrected.
</success_criteria>

<output>
Create `.planning/phases/23-vix3m-ingestion/23-01-SUMMARY.md` when done.
</output>
