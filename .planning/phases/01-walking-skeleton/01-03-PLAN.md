---
phase: 01-walking-skeleton
plan: 03
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified:
  - packages/contracts/src/status.ts
  - packages/contracts/src/index.ts
  - packages/contracts/src/status.test.ts
  - packages/contracts/vitest.config.ts
  - packages/core/src/journal/application/ports.ts
  - packages/core/src/journal/application/getStatus.ts
  - packages/core/src/journal/application/getStatus.test.ts
  - packages/core/src/journal/index.ts
  - packages/core/src/index.ts
  - packages/core/vitest.config.ts
autonomous: true
requirements: [DATA-03, MCP-02]
must_haves:
  truths:
    - "`packages/contracts` exports ONE `statusResponse` Zod schema + inferred `StatusResponse` type, importing only zod + shared"
    - "`packages/core` defines the `calendars` repository port (`ForGettingOpenCalendars`) and a DB-ping port — function-type ports, no framework imports"
    - "`makeGetStatusUseCase(deps)` returns a `ForGettingStatus` driver port that pings the DB and returns a `Result<StatusPayload, StatusError>`"
    - "The `get_status` use-case is unit-tested against in-memory port doubles (plain functions), no Docker"
  artifacts:
    - path: "packages/contracts/src/status.ts"
      provides: "statusResponse Zod schema (db, tokenFreshness, lastJobRuns, version, uptime) + StatusResponse type"
      exports: ["statusResponse", "StatusResponse"]
    - path: "packages/core/src/journal/application/ports.ts"
      provides: "ForGettingOpenCalendars, ForPingingDb driven ports"
      exports: ["ForGettingOpenCalendars", "ForPingingDb"]
    - path: "packages/core/src/journal/application/getStatus.ts"
      provides: "makeGetStatusUseCase factory + ForGettingStatus driver port"
      exports: ["makeGetStatusUseCase", "ForGettingStatus"]
  key_links:
    - from: "packages/core/src/journal/application/getStatus.ts"
      to: "ForPingingDb port"
      via: "deps injection in makeGetStatusUseCase"
      pattern: "ForPingingDb"
    - from: "packages/contracts/src/index.ts"
      to: "status.ts"
      via: "re-export of statusResponse"
      pattern: "statusResponse"
---

<objective>
Build the hexagon core of the status slice test-first, with no framework or vendor imports: the
single `statusResponse` Zod contract in `packages/contracts` (the MCP-02 one-schema source consumed
by BOTH the HTTP route and the MCP tool), the `calendars` repository port + a DB-ping port in
`packages/core`, and the `makeGetStatusUseCase` factory that produces the `get_status` use-case.
Everything here is proven against in-memory function-type doubles — no Postgres, no Docker.

Purpose: DATA-03 (define the ONE driven repository port — `calendars` — in core, to be implemented
by both adapters in plan 04) and MCP-02 (the single shared `statusResponse` schema that both
adapters in plan 05 derive from). This plan establishes the port+use-case contracts that plans 04
and 05 implement and wire.
Output: `@morai/contracts` exporting `statusResponse`; `@morai/core` exporting the journal ports and
`makeGetStatusUseCase`; both suites green against in-memory doubles.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-walking-skeleton/01-SPEC.md
@.planning/phases/01-walking-skeleton/01-CONTEXT.md
@.planning/phases/01-walking-skeleton/01-RESEARCH.md
@docs/architecture/hexagonal-ddd.md
@docs/architecture/api-design.md
@docs/architecture/mcp-and-plugins.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/tdd.md
@.claude/rules/typescript.md
</context>

<feature>
  <name>statusResponse contract + calendars port + get_status use-case</name>
  <files>packages/contracts/src/status.ts + packages/core/src/journal/application/{ports,getStatus}.ts + colocated tests</files>
  <behavior>
    statusResponse = z.object({ db: enum["ok","down"], tokenFreshness: literal "none yet",
    lastJobRuns: literal "none yet", version: string, uptime: number }). One schema, both adapters use it.
    Ports (function types, ForVerbingNoun): ForGettingOpenCalendars = () => Promise<Result<ReadonlyArray<Calendar>, StorageError>>;
    ForPingingDb = () => Promise<Result<void, StorageError>>.
    getStatus: makeGetStatusUseCase({ pingDb, version, startedAt }) → ForGettingStatus = () => Promise<Result<StatusPayload, never>>,
    where db = "ok" if pingDb() ok else "down"; tokenFreshness/lastJobRuns = "none yet"; uptime derived from startedAt.
  </behavior>
  <implementation>Per hexagonal-ddd.md port-naming + factory conventions and RESEARCH Patterns 7/8. Core imports ONLY @morai/shared; contracts imports ONLY zod + @morai/shared (boundary law).</implementation>
</feature>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: statusResponse Zod contract (RED→GREEN)</name>
  <files>packages/contracts/src/status.ts, packages/contracts/src/index.ts, packages/contracts/src/status.test.ts, packages/contracts/vitest.config.ts</files>
  <read_first>
    - .planning/phases/01-walking-skeleton/01-RESEARCH.md (Pattern 8: MCP-02 dual-adapter one-schema — the exact statusResponse z.object shape and StatusResponse = z.infer; Pattern 7 for zod import conventions)
    - .planning/phases/01-walking-skeleton/01-SPEC.md (req 11 + 13: status payload shape { db, tokenFreshness:"none yet", lastJobRuns:"none yet", version, uptime }; one-sided schema change must fail typecheck)
    - docs/architecture/api-design.md (contracts are the single source shared by validation + types; routes parse with them)
    - .claude/rules/architecture-boundaries.md (contracts may import zod + shared ONLY)
  </read_first>
  <behavior>
    - statusResponse.parse of a valid payload succeeds; parse of `{ db: "maybe" }` fails (enum rejects).
    - StatusResponse type (z.infer) has fields db:"ok"|"down", tokenFreshness:"none yet", lastJobRuns:"none yet", version:string, uptime:number.
    - tokenFreshness and lastJobRuns are literal "none yet" (present-but-empty placeholders, SPEC req 11).
  </behavior>
  <action>
    Create `packages/contracts/vitest.config.ts`. RED: write `status.test.ts` asserting statusResponse
    accepts a valid payload and rejects an invalid `db` value and a missing field; run
    `vitest run --project packages/contracts` and SHOW it fail (missing module). GREEN: implement
    `status.ts` exporting `statusResponse` (the z.object from RESEARCH Pattern 8) and
    `export type StatusResponse = z.infer<typeof statusResponse>`; re-export from `index.ts`. Re-run, SHOW green.
    The `tokenFreshness`/`lastJobRuns` literals are the present-but-empty Phase-1 placeholders (SPEC req 11);
    do NOT add token/job real fields (those are Phases 4/5). Import only `zod` (and `@morai/shared` if needed).
    Commit at green only. No any/as/!.
  </action>
  <verify>
    <automated>cd /Users/chiragpersonalmac/Desktop/morai-trading-dashboard-and-tools && bunx vitest run --project packages/contracts status.test && bun run lint packages/contracts</automated>
  </verify>
  <acceptance_criteria>
    - RED output shown in SUMMARY: status.test fails before status.ts exists.
    - `bunx vitest run --project packages/contracts status.test` exits 0 after implementation.
    - `packages/contracts/src/index.ts` re-exports `statusResponse` and `StatusResponse`.
    - `bun run lint packages/contracts` exits 0 — confirms contracts imports only zod + shared (no boundary violation).
  </acceptance_criteria>
  <done>One `statusResponse` schema exists in `@morai/contracts`, tested, importing only zod/shared — the single source both adapters will derive from (MCP-02).</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: calendars port + get_status use-case against in-memory doubles (RED→GREEN→REFACTOR)</name>
  <files>packages/core/src/journal/application/ports.ts, packages/core/src/journal/application/getStatus.ts, packages/core/src/journal/application/getStatus.test.ts, packages/core/src/journal/index.ts, packages/core/src/index.ts, packages/core/vitest.config.ts</files>
  <read_first>
    - docs/architecture/hexagonal-ddd.md (Ports: fine-grained function types, ForVerbingNoun naming, makeXxx(deps) factories; the journal bounded-context directory shape; "test double is a plain function async () => ok(fixture)")
    - .planning/phases/01-walking-skeleton/01-RESEARCH.md (System Architecture Diagram: ports.ts = ForGettingCalendars, ForPingingDb; getStatus.ts = makeGetStatusUseCase(deps) → ForGettingStatus; uptime/version derivation)
    - .planning/phases/01-walking-skeleton/01-SPEC.md (req 8 DATA-03: exactly ONE repository port — calendars — defined in packages/core; req 11/13 status payload)
    - .claude/rules/architecture-boundaries.md (core imports shared ONLY; ports define the world; use-cases are factories)
    - .claude/rules/tdd.md (new port ⇒ in-memory implementation + use-case test in the same change)
  </read_first>
  <behavior>
    - ForGettingOpenCalendars and ForPingingDb are exported function-type ports (driven).
    - makeGetStatusUseCase({ pingDb, version, startedAt }) returns ForGettingStatus.
    - When pingDb() returns ok → status.db === "ok"; when pingDb() returns err → status.db === "down" (use-case never throws on a down DB).
    - tokenFreshness === "none yet"; lastJobRuns === "none yet"; uptime is a non-negative number derived from startedAt; version is the injected string.
    - The use-case is exercised with plain-function doubles: `const pingDb = async () => ok(undefined)` and `async () => err(...)`.
  </behavior>
  <action>
    Create `packages/core/vitest.config.ts`. RED: write `getStatus.test.ts` that injects an in-memory
    `pingDb` double returning `ok(undefined)` (asserts db:"ok") and a second double returning `err(...)`
    (asserts db:"down"), and asserts the placeholder + uptime/version fields; run
    `vitest run --project packages/core` and SHOW it fail. GREEN: implement `ports.ts` (`ForGettingOpenCalendars`,
    `ForPingingDb` — the calendars repo port is DATA-03's single port; even though get_status only needs the
    DB ping, define `ForGettingOpenCalendars` here so plan 04 implements it against both adapters) and
    `getStatus.ts` (`makeGetStatusUseCase(deps): ForGettingStatus` mapping pingDb Result → db field, filling
    placeholders, computing uptime from startedAt). The use-case returns a domain `StatusPayload` shaped to
    match `statusResponse` — but core must NOT import `@morai/contracts` (boundary: core→shared only), so define
    the payload as a plain core type whose fields line up with the contract; the adapters (plan 05) parse it
    through `statusResponse`. Re-run, SHOW green. REFACTOR with suite green. Wire `journal/index.ts` and
    `core/src/index.ts` to export the ports + factory. Commit at green only. No any/as/!; use Result, not exceptions.
  </action>
  <verify>
    <automated>cd /Users/chiragpersonalmac/Desktop/morai-trading-dashboard-and-tools && bunx vitest run --project packages/core getStatus.test && bun run lint packages/core && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - RED output shown in SUMMARY: getStatus.test fails before getStatus.ts exists.
    - `bunx vitest run --project packages/core getStatus.test` exits 0 — both db:"ok" (pingDb ok) and db:"down" (pingDb err) paths pass with in-memory doubles, no Docker.
    - `ports.ts` exports `ForGettingOpenCalendars` and `ForPingingDb`; `getStatus.ts` exports `makeGetStatusUseCase` and `ForGettingStatus`; `core/src/index.ts` re-exports them.
    - `bun run lint packages/core` exits 0 — confirms core imports only `@morai/shared` (no hono/drizzle/contracts import; boundary law holds).
    - `bun run typecheck` exits 0.
  </acceptance_criteria>
  <done>The `calendars` port and the `get_status` use-case exist in the pure hexagon, tested against plain-function in-memory doubles; core imports shared only; commits exist at green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter → use-case | Adapters (plan 05) call makeGetStatusUseCase; the use-case is pure, no external input crosses here directly |
| schema consumer → statusResponse | Both HTTP + MCP adapters parse output through this one schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | Status payload schema drift between HTTP and MCP adapters | mitigate | ONE `statusResponse` schema in contracts; both adapters import it; a one-sided change fails typecheck (MCP-02, verified in plan 05) |
| T-01-06 | Denial of Service | A down DB throwing and crashing the status use-case | mitigate | getStatus maps pingDb `Result.err` → `db:"down"` and returns ok; never throws on DB unavailability (tested) |
| T-01-07 | Information Disclosure | Status payload leaking secrets/internals | accept | Phase-1 payload is db/tokenFreshness/lastJobRuns/version/uptime — no secrets (RESEARCH Security Domain) |
</threat_model>

<verification>
- `vitest run --project packages/contracts` + `--project packages/core` green (DATA-03 port + use-case logic).
- `bun run lint` confirms contracts→zod+shared and core→shared boundaries hold.
- The single `statusResponse` schema is the shared source for plan 05's two adapters (MCP-02).
</verification>

<success_criteria>
- [ ] `packages/contracts` exports one `statusResponse` schema (zod+shared only) — tested.
- [ ] `packages/core` defines the `calendars` repository port + DB-ping port + `makeGetStatusUseCase`.
- [ ] The use-case is green against in-memory function doubles for both db:"ok" and db:"down".
- [ ] Every test RED-first with failure shown; commits at green only; no any/as/! anywhere.
</success_criteria>

<output>
Create `.planning/phases/01-walking-skeleton/01-03-SUMMARY.md` when done.
</output>
