---
phase: 01-walking-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .python-version
  - pyproject.toml
  - uv.lock
  - docker-compose.yml
  - tools/gate.sh
  - .env.example
  - src/morai/__init__.py
  - src/morai/py.typed
  - src/morai/settings.py
  - tests/__init__.py
  - tests/test_settings.py
  - alembic.ini
  - alembic/env.py
  - alembic/script.py.mako
autonomous: true
requirements: [OPS-01, OPS-02, OPS-04]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "uv sync installs the pinned stack on Python 3.13 and uv.lock is committed"
    - "tools/gate.sh runs basedpyright, mypy, ruff and pytest and exits non-zero if any fails"
    - "docker compose up -d starts a Postgres the test suite can reach"
    - "A missing or malformed environment variable kills process boot and names the field (D-15)"
    - "No Python build artifact can reach git history (D-20)"
  artifacts:
    - path: "pyproject.toml"
      provides: "Pinned dependencies plus the basedpyright/mypy/ruff/pytest gate configuration (D-05, D-06)"
      contains: "reportExplicitAny"
    - path: ".python-version"
      provides: "Interpreter pin so the build never runs on the ambient 3.14"
      contains: "3.13"
    - path: "tools/gate.sh"
      provides: "The single gate script CI and local hooks both call"
    - path: "docker-compose.yml"
      provides: "Local Postgres pinned to major version 18, matching the live Railway Postgres (D-17)"
      contains: "postgres:18"
    - path: "src/morai/settings.py"
      provides: "pydantic-settings model, extra=forbid, SecretStr secrets (D-15)"
      contains: "SecretStr"
    - path: "alembic/env.py"
      provides: "Async Alembic environment reading the DSN from settings, never from a committed literal"
  key_links:
    - from: "alembic/env.py"
      to: "src/morai/settings.py"
      via: "reads the database DSN from the settings model, not from alembic.ini"
      pattern: "from morai.settings import"
    - from: "tools/gate.sh"
      to: "pyproject.toml"
      via: "invokes basedpyright/mypy/ruff/pytest against the config blocks declared there"
      pattern: "basedpyright"
---

<objective>
Stand up the build: dependency pins, the strict type-gate configuration, the local Postgres, the
single gate script, the configuration model, and the Alembic environment. Nothing in this plan
serves a request — it is the floor the tracer in 01-02 stands on.

Purpose: every later plan in this phase and every later phase inherits these gates. Getting the
type-gate config wrong here silently disables OPS-01 everywhere (research measured that
`typeCheckingMode: "strict"` alone does NOT enable `reportAny`/`reportExplicitAny`).

Output: a repository that installs, type-checks, lints and tests on a pinned interpreter, with a
local Postgres and an Alembic environment ready for the first migration.

Reversibility: D-18 (`src/` layout, uv, one installable package `morai`) is rated **costly** —
moving the package root later touches every import and both Railway start commands. Per the
user's explicit instruction this run is unattended, so no `checkpoint:decision` gate is emitted;
the rating is recorded here so the weight survives in the record.
</objective>

<execution_context>
@/Users/chiragpersonalmac/Desktop/morai-trading-dashboard-and-tools/.claude/gsd-core/workflows/execute-plan.md
@/Users/chiragpersonalmac/Desktop/morai-trading-dashboard-and-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-walking-skeleton/01-CONTEXT.md
@.planning/phases/01-walking-skeleton/01-RESEARCH.md
@.claude/rules/workflow.md
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin the interpreter and the stack, and configure both type checkers to actually fire</name>
  <reversibility rating="costly">D-18 fixes the package root and the entry-point names that both Railway start commands and every later import are written against.</reversibility>
  <files>.gitignore, .python-version, pyproject.toml, uv.lock, src/morai/__init__.py, src/morai/py.typed, tests/__init__.py</files>
  <read_first>.gitignore (it already carries the iCloud collision patterns from D-20 — extend it, do not rewrite it)</read_first>
  <action>
Extend `.gitignore` FIRST, before any Python file exists, per research finding 8. Append a Python
section covering `.venv/`, `__pycache__/`, `*.pyc`, `*.pyo`, `.pytest_cache/`, `.mypy_cache/`,
`.ruff_cache/`, `.basedpyright/`, `*.egg-info/`, `build/`, `.coverage`. Leave the existing
`* 2` / `* 2.*` collision lines and every other existing entry untouched (D-20, minimal-impact rule).

Write `.python-version` containing `3.13`. Set `requires-python = ">=3.12,<3.14"` in
`pyproject.toml`. The ambient interpreter on this machine is 3.14.7; every command in this phase
runs through `uv run`, which honours the pin. A behaviour difference between 3.14 and 3.13 must
not enter the build.

Create the `src/` layout per D-18: package `morai` at `src/morai/`, with an empty
`src/morai/__init__.py` and an empty `src/morai/py.typed` marker. Create `tests/__init__.py`.

Author `pyproject.toml` with the hatchling (or uv default) build backend pointed at `src/morai`,
and these exact pins, every one live-verified against PyPI on 2026-08-30 in RESEARCH.md's Standard
Stack table:

  runtime: fastapi==0.141.1, pydantic==2.13.5, sqlalchemy[asyncpg]==2.0.52, asyncpg==0.31.0,
           alembic==1.19.1, procrastinate==3.9.0, hypercorn==0.18.0
  dev:     pytest==9.1.1, pytest-asyncio==1.4.0, basedpyright==1.39.10, mypy==2.3.1,
           ruff==0.16.5, uvicorn==0.52.4

Two packages the phase needs are NOT in RESEARCH.md's 17-package legitimacy audit and must be
resolved live at install time rather than pinned from memory: `pydantic-settings` (required by
D-15) and, if `procrastinate` does not already pull it, `psycopg[binary,pool]` (required by
`PsycopgConnector`, per the connector-mismatch finding). Resolve each with `uv add`, record the
version `uv` selects, and confirm each exists on `pypi.org/project/<name>` before committing.
Do NOT add `hypothesis` — no property test ships in this phase; Phase 5 adds it when the ledger
needs it.

Write the tool configuration blocks. basedpyright, per D-05 and the measurement in RESEARCH.md's
type-gate section — `typeCheckingMode = "strict"` alone leaves both rules off:

  [tool.basedpyright] include = ["src", "tests"]; exclude = ["tests/gate/fixtures"];
  typeCheckingMode = "strict"; reportAny = "error"; reportExplicitAny = "error";
  reportIgnoreCommentWithoutRule = "error"

mypy, per D-05: `strict = true` and `disallow_any_explicit = true`, `plugins = []`, and an
exclude for `tests/gate/fixtures`. **Deliberate deviation from RESEARCH.md's suggested block:**
research also listed `disallow_any_expr = true`. D-05 names only `disallow_any_explicit`, and
`disallow_any_expr` flags every expression touching an untyped third-party call, which is
basedpyright's `reportAny`'s job under D-05's own division of labour. Omit it. If a later phase
wants it, that is a decision with evidence behind it, not a default.

`[tool.pydantic-mypy]`: `init_forbid_extra = true`, `init_typed = true`,
`warn_required_dynamic_aliases = true`.

ruff, per D-06: `[tool.ruff.lint] select = ["E", "F", "PGH", "TID"]` (PGH003 forces a rule code on
every suppression; TID enables banned-api), plus `[tool.ruff.lint.flake8-tidy-imports.banned-api]`
entries banning `typing.Any` and `typing.cast` by name, each with a message pointing at D-06 and
naming the escape hatch (a rule-coded suppression carrying a reason comment on the same line).
Exclude `tests/gate/fixtures` from ruff too.

pytest: `[tool.pytest.ini_options]` with `testpaths = ["tests"]`, `asyncio_mode = "auto"`, and
`addopts = "-q"`.

Run `uv lock` and commit `uv.lock`. This single clean resolution is the package-legitimacy
verification for this phase: RESEARCH.md's audit returned `SUS` for all 17 packages driven by a
`weeklyDownloads` signal that returned null for every PyPI package checked — a tool calibration
gap, not 17 findings — and every package was independently corroborated as ecosystem-standard in
the same session. The user's standing instruction is an unattended run, so per-package human
checkpoints are not emitted. Nothing was flagged `SLOP`.
  </action>
  <verify>
    <automated>uv sync --frozen &amp;&amp; uv run python -c "import sys; assert sys.version_info[:2] == (3, 13), sys.version; print(sys.version)" &amp;&amp; uv lock --check &amp;&amp; uv run ruff check src tests &amp;&amp; git check-ignore -q .venv __pycache__ && echo GATE_CONFIG_OK</automated>
  </verify>
  <done>`uv sync --frozen` succeeds, the interpreter reports 3.13.x, `uv lock --check` reports the lockfile current, and `git check-ignore` confirms `.venv` and `__pycache__` are ignored. `uv.lock` is committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Local Postgres, the single gate script, and a configuration model that fails loudly</name>
  <files>docker-compose.yml, tools/gate.sh, .env.example, src/morai/settings.py, tests/test_settings.py</files>
  <behavior>
    - `Settings()` with `DATABASE_URL` set returns a model whose `database_url` is a `SecretStr`
    - `repr(settings)` and `str(settings.database_url)` contain no substring of the password
    - `Settings()` with `DATABASE_URL` absent raises `ValidationError` naming the field `database_url`
    - `Settings()` with an unknown extra environment key set raises `ValidationError` (extra="forbid")
    - `settings.async_dsn` returns the same DSN with the scheme `postgresql+asyncpg://`
    - `settings.sync_dsn` returns the same DSN with the scheme `postgresql://`
  </behavior>
  <action>
Write `docker-compose.yml` with one service: `postgres:18-alpine`, port 5432 published, POSTGRES_USER
/PASSWORD/DB set to local-only development values, and a healthcheck using `pg_isready`. Pin the
image tag exactly — D-17 requires the local and CI Postgres to be the same version.

Major version 18 is measured, not guessed: the existing Railway Postgres service runs
`ghcr.io/railwayapp-templates/postgres-ssl:18`, read from the service's own deploy configuration on
2026-08-30. CI pins the same major in plan 01-07, and plan 01-08 re-confirms it against the live
database with `SELECT version()` and corrects both pins in one commit if Railway has moved.
Add no volume: a throwaway database is the point.

Write `.env.example` documenting `DATABASE_URL` with a placeholder value pointing at the
docker-compose instance. Placeholders only. The repository is public (research finding 8) — no real
credential reaches a committed file, and the live Railway Postgres password is readable from
`railway variables` but must never be written down anywhere in this repo.

Write `tools/gate.sh`: `set -euo pipefail`, then in order `uv run ruff check src tests`,
`uv run ruff format --check src tests`, `uv run basedpyright`, `uv run mypy src tests`,
`uv run pytest`. One script, so CI and any local hook call the same thing and cannot drift. Make it
executable.

Write `src/morai/settings.py` per D-15. A `pydantic-settings` `BaseSettings` subclass with
`SettingsConfigDict(extra="forbid", env_file=".env", case_sensitive=False)`. Field
`database_url: SecretStr` — the raw Railway-shaped `postgresql://user:pass@host:5432/db`. Expose
two computed properties, `async_dsn` and `sync_dsn`, that swap the scheme to
`postgresql+asyncpg://` and `postgresql://` respectively, both reading through
`get_secret_value()`. One environment variable, two forms — a second variable would drift.
Instantiate the settings object once at import as a module-level `settings`, so a missing or
malformed variable kills boot and names the field instead of surfacing as a 500 on the first
request that needs it.

Write the test first and observe it fail (D-08). Use `monkeypatch.setenv`/`delenv` and reconstruct
`Settings()` inside each test rather than relying on the module-level singleton.
  </action>
  <verify>
    <automated>docker compose up -d &amp;&amp; docker compose exec -T postgres pg_isready -U postgres &amp;&amp; uv run pytest tests/test_settings.py -x &amp;&amp; bash tools/gate.sh</automated>
  </verify>
  <done>Postgres answers `pg_isready`; `tests/test_settings.py` passes with the boot-failure and extra-key cases green; `tools/gate.sh` exits 0. The failing-test commit and the implementation commit are two separate commits (D-08).</done>
</task>

<task type="auto">
  <name>Task 3: Alembic environment, async, with no DSN literal in a committed file</name>
  <files>alembic.ini, alembic/env.py, alembic/script.py.mako</files>
  <action>
Run `uv run alembic init -t async alembic` to generate the async template, then edit it down.

In `alembic.ini`, leave `sqlalchemy.url` empty. The repository is public; a DSN carrying a password
must never be committed (research finding 8). `alembic/env.py` imports `settings` from
`morai.settings` and sets the URL from `settings.async_dsn` at runtime.

Point `target_metadata` at the declarative `Base.metadata` that plan 01-02 creates. Until that file
exists this import fails, which is correct and expected — the tracer plan lands the model and the
first revision together. Guard nothing; a broken import here is a loud, honest failure, not a case
for a try/except.

Keep `script.py.mako` as generated except for adding `from __future__ import annotations` and typed
`def upgrade() -> None:` / `def downgrade() -> None:` signatures, so generated revisions pass the
type gate without hand-editing every time.
  </action>
  <verify>
    <automated>test -f alembic/env.py &amp;&amp; grep -q 'morai.settings' alembic/env.py &amp;&amp; grep -c '^sqlalchemy.url = *$' alembic.ini &amp;&amp; grep -rn 'postgresql://[^ ]*:[^ ]*@' alembic.ini | grep -c . | grep -qx 0 &amp;&amp; echo ALEMBIC_ENV_OK</automated>
  </verify>
  <done>`alembic/env.py` reads its DSN from `morai.settings`; `alembic.ini` carries an empty `sqlalchemy.url` and no credential-bearing URL anywhere in it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository → public internet | The GitHub repository is public. Every committed byte is world-readable. |
| environment → process | Railway and local `.env` inject configuration, including the database password. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `alembic.ini`, `docker-compose.yml`, `.env.example` | high | mitigate | No credential literal in any committed file; `alembic.ini`'s `sqlalchemy.url` stays empty and `env.py` reads `settings.async_dsn` at runtime. Verified by a grep in Task 3's automated check and again by the repo-hygiene test in plan 01-04. |
| T-01-02 | Information Disclosure | `src/morai/settings.py` | high | mitigate | `database_url` typed `SecretStr` so a stray log line or `repr` masks it (D-15). Asserted in `tests/test_settings.py`. |
| T-01-03 | Tampering | dependency resolution | high | mitigate | Exact `==` pins for every runtime and dev dependency; `uv.lock` committed; `uv lock --check` in the gate. RESEARCH.md's legitimacy audit returned `SUS` on all 17 packages for a documented tool-calibration reason (null PyPI download signal), no `SLOP`; the single-resolution verification replaces per-package checkpoints per the researcher's stated recommendation and the user's unattended-run instruction. |
| T-01-04 | Tampering | `pyproject.toml` gate config | high | mitigate | `reportAny`/`reportExplicitAny` set explicitly because `strict` alone does not enable them (measured). Plan 01-04 installs the negative controls that prove the config still has teeth. |
</threat_model>

<verification>
- `uv sync --frozen` installs cleanly and the interpreter is 3.13.x, not the ambient 3.14.7.
- `bash tools/gate.sh` exits 0 on a repository containing only the scaffold.
- `docker compose up -d` yields a Postgres that answers `pg_isready`.
- No committed file contains a Postgres URL with a password.
</verification>

<success_criteria>
- `uv.lock` committed, `uv lock --check` clean.
- Both checkers configured per D-05, with `reportAny` and `reportExplicitAny` at error.
- `typing.Any` and `typing.cast` banned by name in ruff (D-06).
- Settings model fails boot on a missing or unknown variable and names the field.
- `.gitignore` covers Python build artifacts and still covers the iCloud collision patterns.
</success_criteria>

<output>
Create `.planning/phases/01-walking-skeleton/01-01-SUMMARY.md` when done.
</output>
