---
phase: 01-walking-skeleton
plan: 04
type: tdd
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - packages/adapters/src/postgres/schema.ts
  - packages/adapters/src/postgres/db.ts
  - packages/adapters/src/postgres/migrate.ts
  - packages/adapters/src/postgres/migrations/
  - packages/adapters/src/postgres/repos/calendars.ts
  - packages/adapters/src/memory/calendars.ts
  - packages/adapters/src/__contract__/calendars.contract.ts
  - packages/adapters/src/postgres/repos/calendars.contract.test.ts
  - packages/adapters/src/memory/calendars.contract.test.ts
  - packages/adapters/src/postgres/migrate.idempotent.test.ts
  - packages/adapters/src/index.ts
  - packages/adapters/vitest.config.ts
  - packages/adapters/test/globalSetup.ts
  - drizzle.config.ts
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03]
must_haves:
  truths:
    - "Drizzle schema defines all 7 journal-context tables (calendars, calendar_snapshots, leg_observations, contracts, fills, orders, rate_observations) with time-leading composite PKs on observation tables"
    - "`drizzle-kit generate` produces an initial migration whose SQL shows the time-leading composite keys"
    - "Boot-time `migrate()` applies pending migrations idempotently — a second run applies 0 and exits 0"
    - "The `calendars` repo has BOTH a Postgres (Drizzle/postgres.js) and an in-memory implementation, and one shared contract suite passes against both"
  artifacts:
    - path: "packages/adapters/src/postgres/schema.ts"
      provides: "7 Drizzle pgTable definitions with time-leading composite PKs"
      contains: "primaryKey"
    - path: "packages/adapters/src/postgres/migrate.ts"
      provides: "idempotent boot migrator over the direct connection (max:1)"
      exports: ["runMigrations"]
    - path: "packages/adapters/src/postgres/repos/calendars.ts"
      provides: "makePostgresCalendarsRepo implementing ForGettingOpenCalendars + ForPingingDb"
      exports: ["makePostgresCalendarsRepo"]
    - path: "packages/adapters/src/memory/calendars.ts"
      provides: "makeMemoryCalendarsRepo implementing the same ports"
      exports: ["makeMemoryCalendarsRepo"]
    - path: "packages/adapters/src/__contract__/calendars.contract.ts"
      provides: "shared contract-test suite run against both adapters (reusable harness)"
      exports: ["runCalendarsContractTests"]
  key_links:
    - from: "packages/adapters/src/postgres/repos/calendars.ts"
      to: "packages/core ForGettingOpenCalendars port"
      via: "implements the port type from @morai/core"
      pattern: "ForGettingOpenCalendars"
    - from: "packages/adapters/src/postgres/migrate.ts"
      to: "Supabase direct connection (postgres max:1)"
      via: "migrate() from drizzle-orm/postgres-js/migrator"
      pattern: "migrate"
    - from: "both *.contract.test.ts"
      to: "__contract__/calendars.contract.ts"
      via: "runCalendarsContractTests(factory)"
      pattern: "runCalendarsContractTests"
---

<objective>
Build the persistence layer that proves the port pattern end-to-end: the full Drizzle schema (all 7
journal-context tables, time-leading composite PKs on the observation tables), the idempotent boot
migrator over the Supabase direct/session connection, the Postgres AND in-memory implementations of
the `calendars` repository port, and ONE shared contract-test suite that runs the same assertions
against both adapters (Postgres via testcontainers, in-memory with no Docker).

Purpose: DATA-01 (schema + composite keys), DATA-02 (idempotent migrator), DATA-03 (one repo, both
adapters, one contract test). This is the reusable contract-test harness every later repository
reuses, and the real DB read/write of the walking skeleton.
Output: `@morai/adapters` exporting the schema, migrator, both calendars repos, and the contract
suite; `vitest run --project packages/adapters` green against both adapters.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-walking-skeleton/01-SPEC.md
@.planning/phases/01-walking-skeleton/01-CONTEXT.md
@.planning/phases/01-walking-skeleton/01-RESEARCH.md
@docs/architecture/data-model.md
@docs/architecture/testing-tdd.md
@.claude/rules/tdd.md
@.claude/rules/architecture-boundaries.md
@.planning/phases/01-walking-skeleton/01-03-SUMMARY.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Drizzle schema (7 tables, time-leading composite keys) + drizzle.config + generated migration</name>
  <files>packages/adapters/src/postgres/schema.ts, drizzle.config.ts, packages/adapters/src/postgres/migrations/, packages/adapters/src/postgres/db.ts</files>
  <read_first>
    - docs/architecture/data-model.md (the 7 tables and their exact columns; append-only observation tables; time-leading composite keys (time, calendar_id) and (time, contract); strike ×1000 int; partial index WHERE bsm_iv IS NULL AND mark IS NOT NULL on leg_observations)
    - .planning/phases/01-walking-skeleton/01-RESEARCH.md (Pattern 3: Drizzle + postgres.js + Supabase — composite primaryKey({columns:[...]}) syntax; direct vs pooler; Pitfall 6: exactOptionalPropertyTypes vs Drizzle inferred types — use explicit annotations/satisfies, never disable the flag)
    - .planning/phases/01-walking-skeleton/01-SPEC.md (req 6 DATA-01: schema.ts defines all 7 tables with time-leading composite PKs; acceptance: drizzle-kit generate emits SQL showing those keys)
    - .claude/rules/architecture-boundaries.md (Drizzle confined to packages/adapters/postgres; never in core/routes)
  </read_first>
  <action>
    Create `packages/adapters/src/postgres/schema.ts` with all 7 `pgTable` definitions from data-model.md:
    `calendars` (uuid PK), `calendar_snapshots` (composite PK `primaryKey({columns:[table.time, table.calendarId]})`),
    `leg_observations` (composite PK `(time, contract)` + the partial index `WHERE bsm_iv IS NULL AND mark IS NOT NULL`),
    `contracts` (occ_symbol varchar PK), `fills`, `orders`, `rate_observations` (date PK). Encode the
    ×1000-int strike convention as `integer`. Create `db.ts` exporting a `makeDb(connectionString)` that builds
    `postgres(url)` + `drizzle({ client })` for the API/query path (full pool, direct URL). Create root
    `drizzle.config.ts` pointing `schema` at `packages/adapters/src/postgres/schema.ts` and `out` at
    `packages/adapters/src/postgres/migrations`, dialect `postgresql`. Run `drizzle-kit generate` to emit the
    initial SQL migration into `migrations/`. Handle the `exactOptionalPropertyTypes` vs Drizzle inference
    conflict (Pitfall 6) with explicit type annotations or `satisfies` — never relax the tsconfig flag. This is
    schema/config scaffolding (execute type, not TDD) but is verified by the generated-SQL assertion below.
  </action>
  <verify>
    <automated>cd /Users/chiragpersonalmac/Desktop/morai-trading-dashboard-and-tools && bunx drizzle-kit generate && ls packages/adapters/src/postgres/migrations/*.sql && grep -iE 'PRIMARY KEY[^,]*"time"|"time"[^)]*"calendar_id"|"time"[^)]*"contract"' packages/adapters/src/postgres/migrations/*.sql && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `drizzle-kit generate` exits 0 and writes at least one numbered `.sql` file into `packages/adapters/src/postgres/migrations/`.
    - The generated SQL contains a composite primary key leading with `time` on the observation tables (`calendar_snapshots` and `leg_observations`) — grep confirms a `time`-leading composite key.
    - `schema.ts` defines all 7 tables (grep: `grep -c 'pgTable(' packages/adapters/src/postgres/schema.ts` === 7).
    - `bun run typecheck` exits 0 (no exactOptionalPropertyTypes conflict left unresolved).
  </acceptance_criteria>
  <done>All 7 journal-context tables are defined with time-leading composite PKs on the observation tables; `drizzle-kit generate` produces an initial migration whose SQL shows those keys; typecheck is clean.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: calendars repo — both adapters + shared contract suite (testcontainers + memory), RED→GREEN</name>
  <files>packages/adapters/src/postgres/repos/calendars.ts, packages/adapters/src/memory/calendars.ts, packages/adapters/src/__contract__/calendars.contract.ts, packages/adapters/src/postgres/repos/calendars.contract.test.ts, packages/adapters/src/memory/calendars.contract.test.ts, packages/adapters/vitest.config.ts, packages/adapters/test/globalSetup.ts, packages/adapters/src/index.ts</files>
  <read_first>
    - .planning/phases/01-walking-skeleton/01-RESEARCH.md (Pattern 5: testcontainers Postgres + Vitest contract tests — the shared runCalendarsContractTests harness imported by BOTH test files; globalSetup + provide/inject for ONE container; testTimeout/hookTimeout=60_000; skipIf(!dockerAvailable) guard; Pitfall 5: container-per-worker)
    - .planning/phases/01-walking-skeleton/01-SPEC.md (req 8 DATA-03: one shared contract-test suite runs the same assertions against Postgres + in-memory; in-memory requires no Docker; req 7 DATA-02 idempotent migrator)
    - .planning/phases/01-walking-skeleton/01-03-SUMMARY.md (the exact ForGettingOpenCalendars + ForPingingDb port signatures defined in core that both adapters implement)
    - docs/architecture/testing-tdd.md (testcontainers for repos — SQL is never mocked; in-memory twin maintained alongside)
    - .claude/rules/tdd.md (new port ⇒ in-memory implementation + use-case test same change; postgres repos ⇒ testcontainers)
  </read_first>
  <behavior>
    - runCalendarsContractTests(makeRepo) asserts the port contract: getOpenCalendars() returns ok([]) on an empty store; after a seeded open calendar it returns ok([thatCalendar]); pingDb() returns ok(undefined) against a reachable store.
    - The SAME suite function is imported by both calendars.contract.test.ts files (postgres + memory) — identical assertions, two implementations.
    - Postgres test: globalSetup starts ONE testcontainers postgres:16, runs migrate() against it, provides the connection URI; the suite skips cleanly (skipIf) when Docker is unavailable locally.
    - Memory test: runs with no Docker, always.
    - Idempotent migrator test: running migrate() twice against the container applies the migration once; the second run reports 0 applied and exits without error.
  </behavior>
  <action>
    Create `packages/adapters/src/postgres/migrate.ts` exporting `runMigrations(connectionString)` that builds
    a dedicated `postgres(url, { max: 1 })` client (Pitfall 3 — migrate needs max:1, never the pooled API client),
    calls `migrate(drizzle({client}), { migrationsFolder })` over the DIRECT connection, then `await client.end()`.
    Create the `vitest.config.ts` for adapters (testTimeout/hookTimeout 60_000, `globalSetup: ['./test/globalSetup.ts']`,
    pool/forks to avoid container-per-worker — Pitfall 5) and `test/globalSetup.ts` that starts ONE
    `PostgreSqlContainer("postgres:16")`, runs `runMigrations` against it, and `provide('dbUrl', uri)`; guard with a
    docker-availability check so a missing daemon causes the Postgres contract test to `skipIf`, never fail.
    RED: write `__contract__/calendars.contract.ts` exporting `runCalendarsContractTests(makeRepo)` with the port
    assertions; write both `calendars.contract.test.ts` files calling it (postgres injects a repo over the injected
    dbUrl; memory injects `makeMemoryCalendarsRepo()`); write `migrate.idempotent.test.ts` (run migrate twice, assert
    second run applies 0). Run `vitest run --project packages/adapters` and SHOW failure (repos not implemented).
    GREEN: implement `makePostgresCalendarsRepo(db)` (Drizzle queries implementing `ForGettingOpenCalendars` +
    `ForPingingDb`; pingDb runs a trivial `SELECT 1`) and `makeMemoryCalendarsRepo()` (a Map-backed twin with the same
    port types). Re-run, SHOW both adapters green. Wire `packages/adapters/src/index.ts` to export schema, migrate
    (`runMigrations`), `makeDb`, `makePostgresCalendarsRepo`, `makeMemoryCalendarsRepo`. Commit at green only.
    No any/as/!; Drizzle stays inside `postgres/`; map DB errors to the port's `StorageError`, never throw across the port.
  </action>
  <verify>
    <automated>cd /Users/chiragpersonalmac/Desktop/morai-trading-dashboard-and-tools && bunx vitest run --project packages/adapters && bun run lint packages/adapters</automated>
  </verify>
  <acceptance_criteria>
    - RED output shown in SUMMARY: the contract tests fail before the repos exist.
    - `bunx vitest run --project packages/adapters` exits 0 with the `calendars` contract suite GREEN against BOTH the Postgres (testcontainers) and in-memory adapters; the memory run executes even with Docker absent.
    - The idempotent-migrator test passes: a second `migrate()` applies 0 migrations and exits 0 (DATA-02).
    - Exactly one shared suite file (`__contract__/calendars.contract.ts`) is imported by both `*.contract.test.ts` files (grep: both test files reference `runCalendarsContractTests`).
    - `bun run lint packages/adapters` exits 0 — Drizzle/postgres confined to `postgres/`; adapters import core ports + shared only.
  </acceptance_criteria>
  <done>One `calendars` port has a Postgres adapter and an in-memory adapter; one shared contract suite passes against both; the migrator is proven idempotent; the contract-test harness is reusable for every later repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migrator → Supabase direct connection | Migrations run with elevated DDL rights over the direct/session URL |
| repo → Postgres | All SQL crosses here; only place Drizzle/postgres.js run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Tampering | Migration corruption from pooled connection interleaving | mitigate | migrate() uses a dedicated `postgres(url,{max:1})` client over the DIRECT connection, closed after; never the pooled API client (RESEARCH Pitfall 3) |
| T-01-09 | Tampering | SQL injection via Drizzle | mitigate | Drizzle uses parameterized queries; no raw template interpolation in any repo (RESEARCH Security Domain) |
| T-01-10 | Denial of Service | Container-per-worker exhausting CI Docker | mitigate | globalSetup starts ONE container, provide/inject shares the URL; forks pool single-fork (RESEARCH Pitfall 5) |
| T-01-SC | Tampering | Installs of drizzle-orm, drizzle-kit, postgres, @testcontainers/postgresql | accept | All [ASSUMED]-approved well-known packages (RESEARCH Package Legitimacy Audit); no [SUS]/[SLOP]; registry+official-docs signal accepted |
</threat_model>

<verification>
- `drizzle-kit generate` emits 7-table SQL with time-leading composite keys (DATA-01).
- `migrate()` is idempotent: second run applies 0 (DATA-02).
- The `calendars` contract suite is green against BOTH Postgres (testcontainers) and in-memory (DATA-03).
- `bun run lint packages/adapters` confirms Drizzle confinement + adapter boundary.
</verification>

<success_criteria>
- [ ] Schema defines all 7 tables with time-leading composite PKs; generated SQL proves the keys.
- [ ] Boot migrator is idempotent (second run = 0 applied, exit 0).
- [ ] ONE contract suite passes against the Postgres AND in-memory `calendars` adapters; memory needs no Docker.
- [ ] Every test RED-first; commits at green; no any/as/!; Drizzle confined to postgres/.
</success_criteria>

<output>
Create `.planning/phases/01-walking-skeleton/01-04-SUMMARY.md` when done.
</output>
