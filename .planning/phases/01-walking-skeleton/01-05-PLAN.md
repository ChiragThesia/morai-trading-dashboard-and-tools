---
phase: 01-walking-skeleton
plan: 05
type: tdd
wave: 4
depends_on: ["01-02", "01-03", "01-04"]
files_modified:
  - apps/server/src/config.ts
  - apps/server/src/config.test.ts
  - apps/server/src/main.ts
  - apps/server/src/adapters/http/status.routes.ts
  - apps/server/src/adapters/http/status.routes.test.ts
  - apps/server/src/adapters/mcp/tools.ts
  - apps/server/src/adapters/mcp/server.ts
  - apps/server/src/adapters/mcp/bearer.ts
  - apps/server/src/adapters/mcp/mcp.test.ts
  - apps/server/vitest.config.ts
  - apps/worker/src/config.ts
  - apps/worker/src/main.ts
  - apps/worker/src/migrate.ts
autonomous: true
requirements: [DATA-04, DEPLOY-02, DEPLOY-03, MCP-02]
must_haves:
  truths:
    - "Config Zod-parses env once at the composition root; booting with a required var missing exits non-zero with a Zod error naming the offending var"
    - "`GET /api/status` returns 200 with `{ db, tokenFreshness:'none yet', lastJobRuns:'none yet', version, uptime }`; a DB-unreachable case returns `db:'down'`"
    - "The MCP `get_status` tool is mounted at `/mcp`, bearer-protected (bad/missing bearer → 401), and returns the same statusResponse payload as the HTTP route"
    - "Both adapters derive their response shape from the single `statusResponse` schema; a one-sided change fails `bun run typecheck` (MCP-02)"
    - "`apps/worker` boots, runs the idempotent migrator over the direct connection, then idles (no real jobs this phase)"
  artifacts:
    - path: "apps/server/src/config.ts"
      provides: "parseConfig() Zod env schema (DATABASE_URL direct, DATABASE_POOL_URL optional, MCP_BEARER_TOKEN, PORT, TZ)"
      exports: ["parseConfig", "Config"]
    - path: "apps/server/src/adapters/http/status.routes.ts"
      provides: "Hono GET /api/status route → get_status use-case → statusResponse"
      exports: ["statusRoutes"]
    - path: "apps/server/src/adapters/mcp/tools.ts"
      provides: "MCP get_status tool backed by the same use-case + statusResponse"
      exports: ["registerStatusTool"]
    - path: "apps/server/src/main.ts"
      provides: "server composition root: config → db → calendars repo → get_status → Hono + MCP"
      contains: "parseConfig"
    - path: "apps/worker/src/main.ts"
      provides: "worker composition root: config → migrate → idle"
      contains: "runMigrations"
  key_links:
    - from: "apps/server/src/adapters/http/status.routes.ts"
      to: "packages/contracts statusResponse"
      via: "statusResponse.parse on the response"
      pattern: "statusResponse"
    - from: "apps/server/src/adapters/mcp/tools.ts"
      to: "packages/contracts statusResponse"
      via: "same statusResponse.parse"
      pattern: "statusResponse"
    - from: "apps/server/src/main.ts"
      to: "makePostgresCalendarsRepo + makeGetStatusUseCase"
      via: "composition-root wiring"
      pattern: "makeGetStatusUseCase"
    - from: "apps/server/src/adapters/mcp/server.ts"
      to: "StreamableHTTPServerTransport via fetch-to-node"
      via: "toReqRes/toFetchResponse bridge"
      pattern: "toReqRes"
---

<objective>
Wire the two composition roots and establish the MCP-02 dual-adapter pattern on `get_status`: the
Zod env config (parsed once, fails boot loudly), the Hono `GET /api/status` route, and the MCP
`get_status` tool at `/mcp` (bearer-protected, via the fetch-to-node bridge) — both deriving their
response from the SINGLE `statusResponse` schema and both backed by the same `makeGetStatusUseCase`.
The `apps/worker` root boots, runs the idempotent migrator over the direct connection, then idles.

Purpose: DATA-04 (Zod config fails loudly), DEPLOY-02 (status route logic, real DB ping), DEPLOY-03
(MCP endpoint + bearer auth logic — provisioned live in plan 06), MCP-02 (one schema, two adapters,
drift caught at compile time). This is the request → use-case → DB → response slice of the walking
skeleton, locally testable in full before the production deploy in plan 06.
Output: a locally runnable server (`bun run apps/server/src/main.ts`) serving `/api/status` + `/mcp`,
a worker that migrates and idles; all unit/integration suites green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-walking-skeleton/01-SPEC.md
@.planning/phases/01-walking-skeleton/01-CONTEXT.md
@.planning/phases/01-walking-skeleton/01-RESEARCH.md
@docs/architecture/api-design.md
@docs/architecture/mcp-and-plugins.md
@docs/architecture/deployment.md
@docs/architecture/hexagonal-ddd.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/tdd.md
@.planning/phases/01-walking-skeleton/01-03-SUMMARY.md
@.planning/phases/01-walking-skeleton/01-04-SUMMARY.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Zod env config (fails boot loudly) + worker composition root (migrate + idle)</name>
  <files>apps/server/src/config.ts, apps/server/src/config.test.ts, apps/server/vitest.config.ts, apps/worker/src/config.ts, apps/worker/src/main.ts, apps/worker/src/migrate.ts</files>
  <read_first>
    - .planning/phases/01-walking-skeleton/01-RESEARCH.md (Pattern 7: Zod env config — the configSchema with DATABASE_URL (direct, required), DATABASE_POOL_URL (optional), MCP_BEARER_TOKEN (min 16), PORT (coerce, default 3000), TZ; parseConfig() with safeParse + loud process.exit(1) naming the var; the Railway restart-loop gotcha)
    - docs/architecture/deployment.md (required vs optional env; DATABASE_URL is the direct/session connection used by migrator + worker; DATABASE_POOL_URL never used for migrations; TZ=America/New_York for worker)
    - .planning/phases/01-walking-skeleton/01-SPEC.md (req 9 DATA-04: config Zod-parses env once at composition root, fails boot loudly naming the offending var; req 7 worker boots + migrates)
    - .planning/phases/01-walking-skeleton/01-04-SUMMARY.md (the runMigrations(connectionString) signature from the adapters package)
    - .claude/rules/architecture-boundaries.md (process.env read ONCE in the composition root, Zod-parsed; typed config flows inward)
  </read_first>
  <behavior>
    - parseConfig(env) returns a typed Config for a valid env (DATABASE_URL, MCP_BEARER_TOKEN present).
    - parseConfig(env) with DATABASE_URL missing fails: the failure path is exercised so the error message names `DATABASE_URL` (test the parse function returns/throws a Zod error naming the var, rather than asserting process exit).
    - MCP_BEARER_TOKEN shorter than the minimum length is rejected.
    - Worker main: parses config, calls runMigrations(config.DATABASE_URL), logs "ready", then idles (no pg-boss this phase).
  </behavior>
  <action>
    Create `apps/server/vitest.config.ts`. RED: write `config.test.ts` asserting (a) a valid env parses to a
    typed Config, (b) missing `DATABASE_URL` yields a Zod error whose message names `DATABASE_URL`, (c) a too-short
    `MCP_BEARER_TOKEN` is rejected. Structure `config.ts` so the schema-parse is a pure testable function
    (`parseConfig(env: NodeJS.ProcessEnv)` returning `Result` or throwing a Zod error) and the `process.exit(1)`
    loud-boot wrapper is a thin separate caller — so the test exercises the parse without killing the test process.
    Run `vitest run --project apps/server config.test`, SHOW failure. GREEN: implement `config.ts` per RESEARCH
    Pattern 7 (DATABASE_URL required direct URL, DATABASE_POOL_URL optional, MCP_BEARER_TOKEN min 16, PORT coerce
    default 3000, TZ default America/New_York); log a clear message before `process.exit(1)` (Railway restart-loop
    gotcha). Re-run, SHOW green. Then build `apps/worker/src/config.ts` (its own Zod parse of the worker's env subset),
    `apps/worker/src/migrate.ts` (thin: parse config → call `runMigrations(config.DATABASE_URL)` from `@morai/adapters`
    — this is also what root `bun run migrate` invokes), and `apps/worker/src/main.ts` (parse config → migrate → log
    "ready" → idle). Commit at green. No any/as/!; never log config values (secrets), only field names on failure.
  </action>
  <verify>
    <automated>cd /Users/chiragpersonalmac/Desktop/morai-trading-dashboard-and-tools && bunx vitest run --project apps/server config.test && bun run lint apps/server apps/worker && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - RED output shown: config.test fails before config.ts exists.
    - `bunx vitest run --project apps/server config.test` exits 0: valid env parses; missing `DATABASE_URL` produces a Zod error naming `DATABASE_URL`; short `MCP_BEARER_TOKEN` rejected (DATA-04).
    - `apps/worker/src/migrate.ts` calls `runMigrations` from `@morai/adapters` over `config.DATABASE_URL` (the direct connection).
    - `bun run lint apps/server apps/worker` and `bun run typecheck` exit 0.
  </acceptance_criteria>
  <done>Config Zod-parses once and fails boot loudly naming a missing var; the worker root parses config, runs the idempotent migrator over the direct connection, and idles.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Hono GET /api/status route + server composition root, RED→GREEN</name>
  <files>apps/server/src/adapters/http/status.routes.ts, apps/server/src/adapters/http/status.routes.test.ts, apps/server/src/main.ts</files>
  <read_first>
    - docs/architecture/api-design.md (route law: Zod-validate → call use-case → map Result → respond; routes never touch Drizzle/fetch; c.json(contracts.parse(result.value)); GET /api/status in the route table)
    - .planning/phases/01-walking-skeleton/01-RESEARCH.md (System Architecture Diagram: main.ts composition root wiring config → postgres pool → drizzle → makePostgresCalendarsRepo → makeGetStatusUseCase → Hono router; Pattern 8 statusResponse on the route)
    - .planning/phases/01-walking-skeleton/01-SPEC.md (req 11 DEPLOY-02: GET /api/status returns JSON with real DB ping → db ok/down + present-but-empty token/job fields + version + uptime; forced DB-down returns db:"down")
    - .planning/phases/01-walking-skeleton/01-03-SUMMARY.md (makeGetStatusUseCase + ForGettingStatus signatures)
    - .planning/phases/01-walking-skeleton/01-04-SUMMARY.md (makeDb, makePostgresCalendarsRepo, makeMemoryCalendarsRepo exports)
  </read_first>
  <behavior>
    - GET /api/status with a healthy (in-memory or reachable) pingDb returns HTTP 200 and a body that `statusResponse.parse` accepts, with db:"ok".
    - GET /api/status with a pingDb double that returns err returns HTTP 200 with db:"down" (status endpoint reports health, does not 500 on a down DB).
    - The route imports `statusResponse` from @morai/contracts and parses the response through it (MCP-02 source).
    - main.ts wires config → db (from DATABASE_URL) → makePostgresCalendarsRepo → makeGetStatusUseCase → mounts the route under /api.
  </behavior>
  <action>
    RED: write `status.routes.test.ts` building the Hono app/route with an injected `getStatus` use-case (constructed
    over an in-memory pingDb double — no real DB needed for the route test), asserting a 200 + db:"ok" for a healthy
    ping and a 200 + db:"down" for an err ping, and that the body passes `statusResponse.parse`. Use Hono's
    `app.request()` test helper. Run `vitest run --project apps/server status.routes.test`, SHOW failure. GREEN:
    implement `status.routes.ts` (a `Hono` router exporting `statusRoutes`, following api-design.md: call the injected
    use-case → on Result map to `c.json(statusResponse.parse(payload))`; deps injected via `c.var` or a factory, not
    imported globally). Implement `apps/server/src/main.ts` as the composition root per the RESEARCH diagram: parse
    config, build the db with `makeDb(config.DATABASE_URL)`, construct `makePostgresCalendarsRepo` + the DB-ping,
    build `makeGetStatusUseCase`, mount `statusRoutes` under `/api`, read PORT from config. Re-run, SHOW green.
    Commit at green. No any/as/!; the route contains ZERO business logic (boundary law); no Drizzle import in the route.
  </action>
  <verify>
    <automated>cd /Users/chiragpersonalmac/Desktop/morai-trading-dashboard-and-tools && bunx vitest run --project apps/server status.routes.test && bun run lint apps/server</automated>
  </verify>
  <acceptance_criteria>
    - RED output shown: status.routes.test fails before the route exists.
    - `bunx vitest run --project apps/server status.routes.test` exits 0: 200 + db:"ok" on healthy ping, 200 + db:"down" on err ping, body passes `statusResponse.parse` (DEPLOY-02 logic).
    - `status.routes.ts` imports `statusResponse` from `@morai/contracts` and parses the response through it.
    - `main.ts` wires config → makeDb → makePostgresCalendarsRepo → makeGetStatusUseCase → Hono `/api`.
    - `bun run lint apps/server` exits 0 — no Drizzle in the route, no business logic in the adapter.
  </acceptance_criteria>
  <done>`GET /api/status` returns the real status payload (db ok/down via a real ping), parsed through the shared schema, wired through the composition root from config to DB and back.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: MCP /mcp endpoint + get_status tool (bearer-protected, same schema), RED→GREEN</name>
  <files>apps/server/src/adapters/mcp/tools.ts, apps/server/src/adapters/mcp/server.ts, apps/server/src/adapters/mcp/bearer.ts, apps/server/src/adapters/mcp/mcp.test.ts</files>
  <read_first>
    - .planning/phases/01-walking-skeleton/01-RESEARCH.md (Pattern 4: MCP StreamableHTTPServerTransport on Hono — fetch-to-node toReqRes/toFetchResponse bridge; stateless sessionIdGenerator:undefined; bearer middleware on /mcp/*; makeMcpServer factory registering get_status; handle BOTH POST and GET; Pitfall 2: Fetch vs Node req/res; assumption A2/A6 fetch-to-node@2.1.0 chosen over alpha @modelcontextprotocol/hono)
    - docs/architecture/mcp-and-plugins.md (MCP server = inbound adapter over the SAME use-cases; bearer token from env; tool input schemas derive from contracts; zero business logic in tool handlers; new use-case ⇒ both adapters same PR)
    - .planning/phases/01-walking-skeleton/01-SPEC.md (req 12 DEPLOY-03: MCP at /mcp via @modelcontextprotocol/sdk, bearer-protected, get_status backed by the same use-case; missing/incorrect bearer → 401; req 13 MCP-02: get_status ships both Hono route + MCP tool deriving from one statusResponse)
    - .planning/phases/01-walking-skeleton/01-RESEARCH.md (Package Legitimacy Audit: @modelcontextprotocol/sdk + fetch-to-node are [ASSUMED]/[VERIFIED] approved; @modelcontextprotocol/hono is [SUS] alpha — DO NOT use)
  </read_first>
  <behavior>
    - A request to /mcp with no Authorization header → 401.
    - A request to /mcp with a wrong bearer token → 401.
    - A request to /mcp with the correct `Bearer <MCP_BEARER_TOKEN>` reaches the transport and the get_status tool returns content whose JSON parses against `statusResponse` — the SAME payload as the HTTP route for the same use-case.
    - The get_status tool derives its output from the single `statusResponse` schema (MCP-02).
  </behavior>
  <action>
    RED: write `mcp.test.ts` exercising the bearer middleware behavior on /mcp (no header → 401, wrong token → 401,
    correct token → not-401) and a unit test of the get_status tool handler returning `statusResponse`-valid content
    for a healthy injected use-case. Run `vitest run --project apps/server mcp.test`, SHOW failure. GREEN: implement
    `bearer.ts` (a Hono `MiddlewareHandler` comparing `Authorization` against `Bearer ${config.MCP_BEARER_TOKEN}`,
    returning 401 otherwise), `tools.ts` (`registerStatusTool(server, getStatus)` registering `get_status` whose
    handler calls the injected use-case and returns `{ content:[{ type:"text", text: JSON.stringify(statusResponse.parse(payload)) }] }`),
    and `server.ts` (`makeMcpServer(getStatus)` factory + the Hono mount: `app.use("/mcp/*", bearerAuth)`,
    `app.post("/mcp", ...)` and `app.get("/mcp", ...)` using `toReqRes(c.req.raw)` → fresh
    `StreamableHTTPServerTransport({ sessionIdGenerator: undefined })` per request → `server.connect` →
    `transport.handleRequest` → `toFetchResponse(res)` per RESEARCH Pattern 4). Mount the MCP routes in `main.ts`
    alongside the HTTP routes on the same Hono app/port, injecting the SAME `getStatus` use-case as the route. Use
    `fetch-to-node@2.1.0`; do NOT use `@modelcontextprotocol/hono` (alpha [SUS]). Re-run, SHOW green. Commit at green.
    No any/as/!; the tool handler contains ZERO business logic (boundary law); never log the bearer token.
  </action>
  <verify>
    <automated>cd /Users/chiragpersonalmac/Desktop/morai-trading-dashboard-and-tools && bunx vitest run --project apps/server mcp.test && bun run lint apps/server && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - RED output shown: mcp.test fails before the MCP adapter exists.
    - `bunx vitest run --project apps/server mcp.test` exits 0: /mcp returns 401 with no/wrong bearer, reaches the tool with the correct bearer, and get_status output parses against `statusResponse` (DEPLOY-03 + MCP-02 logic).
    - Both the HTTP route and the MCP tool import the SAME `statusResponse` from `@morai/contracts`; renaming a field in the schema would fail `bun run typecheck` on both sides (MCP-02 drift — verified by the shared-import + typecheck).
    - `apps/server/src/main.ts` mounts both `/api/status` and `/mcp` on one Hono app with one injected `getStatus`.
    - `bun run lint apps/server` and `bun run typecheck` exit 0.
  </acceptance_criteria>
  <done>The MCP `get_status` tool is live at `/mcp`, bearer-protected (401 on bad/missing token), returns the same `statusResponse` payload as the HTTP route, and both adapters derive from the one schema — the MCP-02 pattern is established on `get_status`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet → /mcp | Public MCP endpoint on Railway — untrusted requests, bearer-gated |
| internet → /api/status | Public health endpoint — unauthenticated, must contain no secrets |
| env → composition root | All secrets (DATABASE_URL, MCP_BEARER_TOKEN) enter here, Zod-parsed, once |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11 | Spoofing / Elevation of Privilege | Unauthenticated access to /mcp | mitigate | Bearer middleware on `/mcp/*` compares `Authorization` against `Bearer ${MCP_BEARER_TOKEN}`; no/wrong token → 401 (tested) — DEPLOY-03 |
| T-01-12 | Information Disclosure | MCP_BEARER_TOKEN / DATABASE_URL in logs | mitigate | Config flows inward without log calls; on Zod failure log field NAMES only, never values; bearer never logged (RESEARCH Security Domain) |
| T-01-13 | Elevation of Privilege | DNS rebinding against /mcp | mitigate | StreamableHTTPServerTransport validates Origin; bearer required regardless (RESEARCH Security Domain) |
| T-01-14 | Information Disclosure | /api/status leaking internals | accept | Phase-1 status payload is db/tokenFreshness/lastJobRuns/version/uptime — no secrets; bearer is on /mcp, status is intentionally public |
| T-01-SC | Tampering | Installs of hono, @hono/zod-validator, @modelcontextprotocol/sdk, fetch-to-node | accept | All [ASSUMED]/[VERIFIED]-approved (RESEARCH Audit); `@modelcontextprotocol/hono` [SUS] alpha is explicitly NOT used; fetch-to-node@2.1.0 is [VERIFIED] |
</threat_model>

<verification>
- `vitest run --project apps/server` green across config, status.routes, mcp suites.
- Missing-var boot fails loudly naming the var (DATA-04).
- `/api/status` returns db ok/down via a real ping; body parses against statusResponse (DEPLOY-02).
- `/mcp` is bearer-gated (401 on bad/missing) and returns the same payload as the route (DEPLOY-03).
- Both adapters import one `statusResponse`; typecheck catches one-sided drift (MCP-02).
- Worker migrates over the direct connection and idles.
</verification>

<success_criteria>
- [ ] Booting with a required env var missing exits non-zero with a Zod error naming the var.
- [ ] `GET /api/status` returns 200 with db ok/down + present-but-empty token/job fields, parsed via statusResponse.
- [ ] MCP `get_status` at `/mcp` is bearer-protected (bad bearer → 401) and returns the same payload as the HTTP route.
- [ ] One `statusResponse` schema feeds both adapters; a one-sided change fails `bun run typecheck`.
- [ ] Worker boots, migrates (direct connection), idles.
- [ ] Every test RED-first; commits at green; no any/as/!; adapters carry zero business logic.
</success_criteria>

<output>
Create `.planning/phases/01-walking-skeleton/01-05-SUMMARY.md` when done.
</output>
