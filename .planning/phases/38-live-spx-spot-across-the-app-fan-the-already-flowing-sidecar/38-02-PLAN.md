---
phase: 38-live-spx-spot-across-the-app-fan-the-already-flowing-sidecar
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/sidecar/streamer.py
  - apps/sidecar/main.py
  - apps/sidecar/tests/test_streamer.py
autonomous: false
requirements: [LIVE-03]
must_haves:
  truths:
    - "A background poll task fetches the VIX family via get_quotes on a fixed ~20s interval"
    - "One Z-suffixed indices frame is pushed onto event_queue per successful poll"
    - "A single unrecognized/failed symbol never drops the other three, and never kills the loop"
    - "The poll runs on the SAME interval regardless of RTH (no is_rth() guard)"
  artifacts:
    - path: apps/sidecar/streamer.py
      provides: "start_indices_poll(app) + INDICES_POLL_INTERVAL_S + response parser"
      contains: "start_indices_poll"
    - path: apps/sidecar/main.py
      provides: "indices_poll_task lifecycle alongside keepalive_task/streamer_task"
      contains: "start_indices_poll"
  key_links:
    - from: apps/sidecar/streamer.py
      to: apps/sidecar/stream_proxy.py
      via: "event_queue.put_nowait({type:'indices', ...}) drained by the unchanged /sidecar/events generator"
      pattern: "event_queue.put_nowait"
    - from: apps/sidecar/main.py
      to: apps/sidecar/streamer.py
      via: "asyncio.create_task(start_indices_poll(app)) in _acquire_lock_and_init"
      pattern: "start_indices_poll"
user_setup:
  - service: schwab
    why: "The A1 checkpoint smoke-test requires a running, Schwab-authenticated sidecar to confirm the live $VIX/$VVIX/$VIX9D/$VIX3M quote symbols and response shape."
    dashboard_config:
      - task: "Have a live authenticated sidecar reachable (local or the deployed instance) so `get_quotes([\"$SPX\",\"$VIX\",\"$VVIX\",\"$VIX9D\",\"$VIX3M\"])` can be run once"
        location: "Running sidecar with a seeded market token (the same instance the chain fetch uses)"
---

<objective>
Add the sidecar's only genuinely-new code this phase: a REST `get_quotes` poll loop that
fetches the VIX family (`$VIX/$VVIX/$VIX9D/$VIX3M`) every ~20s and emits one Z-suffixed
`indices` frame onto the existing `event_queue`, plus its lifecycle wiring (LIVE-03). Because
the exact Schwab ticker strings/response shape for VIX3M/VIX9D/VVIX are unverified
(Assumption A1, Open Question 1), the FIRST step is a live smoke-test checkpoint — confirm
the real symbols BEFORE building the parser around an assumed shape.

Purpose: make the sidecar the sole live source for the regime rail's VIX-family values,
pulled through the sidecar per the user's architecture intent.
Output: `start_indices_poll(app)`, `INDICES_POLL_INTERVAL_S`, the response parser, and the
`main.py` task lifecycle.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-live-spx-spot-across-the-app-fan-the-already-flowing-sidecar/38-CONTEXT.md
@.planning/phases/38-live-spx-spot-across-the-app-fan-the-already-flowing-sidecar/38-RESEARCH.md
@.planning/phases/38-live-spx-spot-across-the-app-fan-the-already-flowing-sidecar/38-PATTERNS.md

# The emit-to-queue + Z-suffix + reconnect-loop analogs to copy:
@apps/sidecar/streamer.py
# The task lifecycle to mirror (keepalive_task / streamer_task creation + finally-cancel):
@apps/sidecar/main.py
# The market-client + $SPX symbol + get_option_chain access pattern (get_quotes analog):
@apps/sidecar/chain_proxy.py
# The pytest harness (AsyncMock/MagicMock, event_queue drain, ts.endswith("Z")):
@apps/sidecar/tests/test_streamer.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: A1 checkpoint — confirm VIX-family Schwab quote symbols + response shape</name>
  <action>
    Pause for a one-time live smoke-test BEFORE the parser is built. Have the operator run a single
    `get_quotes` call against a running Schwab-authenticated sidecar and report the confirmed
    symbol strings, batch-vs-per-symbol failure behavior, and the level field name (per the
    how-to-verify steps). Do not proceed to Task 2's parser until the operator responds with the
    resume signal; if the sidecar is unreachable, proceed with the documented defaults.
  </action>
  <what-built>
    Nothing yet — this is the Assumption A1 de-risking step. `$SPX` is proven live in this
    codebase (chain_proxy.py's `$SPX`), but the exact Schwab quote tickers and response shape
    for VIX3M/VIX9D/VVIX on `/marketdata/v1/quotes` are unverified (Open Question 1). Confirm them
    once against the live API before the parser is written around an assumed shape.
  </what-built>
  <how-to-verify>
    Against a running, Schwab-authenticated sidecar (local or deployed), run ONE call:
    `market_client.get_quotes(["$SPX","$VIX","$VVIX","$VIX9D","$VIX3M"])` and record (do NOT
    persist) the raw JSON response shape. Confirm:
      1. Which of the four VIX-family symbols Schwab recognizes, and the EXACT ticker strings
         (is VIX3M `$VIX3M` or something else?).
      2. Whether one invalid symbol 400s the WHOLE batch or returns a per-symbol error entry
         (Assumption A3) — this decides whether the loop needs a per-symbol fallback path.
      3. Which field carries the index level — `quote.lastPrice` (expected; bid/ask/mark may be
         null/absent for cash indices, Pitfall 3).
    Report the confirmed symbol strings, batch-vs-per-symbol failure behavior, and the level
    field name. If a symbol is wrong, give the corrected string; if unreachable, say so and the
    executor proceeds with the documented defaults ($-prefix, lastPrice, per-symbol fallback).
  </how-to-verify>
  <resume-signal>Type "confirmed: &lt;symbols/field/failure-mode&gt;" or "unreachable — use defaults"</resume-signal>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: start_indices_poll — get_quotes poll loop + Z-suffixed indices frame</name>
  <files>apps/sidecar/streamer.py, apps/sidecar/tests/test_streamer.py</files>
  <behavior>
    - Given a mocked `app.state.market_client.get_quotes` returning a shaped response for the
      confirmed symbols, one iteration pushes exactly one dict onto event_queue of shape
      { "type": "indices", "vix": <float|None>, "vvix": …, "vix9d": …, "vix3m": …, "ts": "…Z" }
      (ts ends in "Z", via utc_now_z — assert `ts.endswith("Z")`).
    - A symbol missing from the response (or carrying a per-symbol error entry) maps to None for
      that field only; the other three are present (omit-not-fabricate).
    - The level is read from `lastPrice` (mark/bid/ask absence does not blank the value) — mirror
      the existing `item.get(NAMED, item.get(NUMERIC))` degrade-gracefully idiom.
    - A `get_quotes` exception is caught, logged by type name only, and the loop continues (does
      not raise); a QueueFull on put_nowait is caught and logged, dropping only that frame.
  </behavior>
  <action>
    Add `INDICES_POLL_INTERVAL_S = 20.0` (module constant, matching main.py's HEARTBEAT_SECONDS
    style — not a SidecarConfig field, per RESEARCH placement guidance) and the symbol list
    (use the strings CONFIRMED by the checkpoint; default to `["$VIX","$VVIX","$VIX9D","$VIX3M"]`
    with `$`-prefix if unreachable). Write `async def start_indices_poll(app)` co-located with
    the other streamer functions: a `while True:` loop that reads `app.state.market_client`
    (NOT trader_client — quotes are a market-data call, per chain_proxy.py), calls `get_quotes`,
    parses each symbol's `lastPrice` into a nullable float, builds the Z-suffixed `indices` dict
    via `utc_now_z()`, `event_queue.put_nowait(...)` inside a try/except QueueFull (mirror
    `_on_level_one_option`'s guard), then `await asyncio.sleep(INDICES_POLL_INTERVAL_S)`. Degrade
    gracefully when `market_client` is None (log warning, keep looping so it recovers on
    re-init). NEVER add an `if is_rth():` guard — poll on the fixed interval regardless of market
    state (Pitfall 6, CONTEXT Area 1 Q4); let the client-side stale badge own honesty. If the
    checkpoint reported whole-batch-400-on-bad-symbol, add a per-symbol `get_quote` fallback,
    each independently try/excepted so one bad symbol never drops the others (Assumption A3).
    Log only `type(exc).__name__` — never `str(exc)` or token/response body (self-healing +
    .name-only discipline). This task is `tdd="true"`: write the failing pytest first against a
    mocked `get_quotes`, run it red, then implement to green (project tdd.md).
  </action>
  <verify>
    <automated>cd apps/sidecar &amp;&amp; .venv/bin/pytest tests/test_streamer.py -x -q</automated>
  </verify>
  <done>A mocked poll iteration emits one Z-suffixed indices frame; a missing symbol → null for that field only; a get_quotes throw is swallowed and the loop continues; no is_rth guard exists.</done>
</task>

<task type="auto">
  <name>Task 3: main.py — indices_poll_task lifecycle alongside streamer/keepalive</name>
  <files>apps/sidecar/main.py</files>
  <action>
    In `_acquire_lock_and_init`, create `indices_poll_task = asyncio.create_task(start_indices_poll(app))`
    AFTER `_init_schwab_clients` and alongside the existing `keepalive_task`/`streamer_task`
    creations (import `start_indices_poll` next to the existing `from streamer import start_streamer`).
    In the SAME `finally` block that cancels keepalive_task and streamer_task, cancel and await
    `indices_poll_task` with `suppress(asyncio.CancelledError)` — identical lifecycle: created
    after clients init, cancelled on lock loss OR shutdown. Pure composition-root wiring — no
    business logic (architecture-boundaries: composition roots are wiring-only, exempt from TDD).
    Do not change lock/heartbeat/streamer behavior.
  </action>
  <verify>
    <automated>cd apps/sidecar &amp;&amp; .venv/bin/pytest tests/ -x -q</automated>
  </verify>
  <done>indices_poll_task is created after client init and cancelled+awaited in the finally block; the full sidecar suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schwab REST → sidecar | untrusted quote response parsed field-by-field, per-symbol nullable |
| sidecar → event_queue → /sidecar/events | private-network-only (GW-05); any dict framed by the generator |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-02 | Denial of Service | start_indices_poll loop | high | mitigate | A `get_quotes` throw or malformed response is caught (type-name-only log) and the loop continues; a QueueFull drops only the current frame — a bad poll never kills the task or floods the queue |
| T-38-03 | Information Disclosure | error/response logging | medium | mitigate | Log `type(exc).__name__` only — never `str(exc)`, response body, or token-adjacent content (chain_proxy.py T-11-05-02 precedent) |
| T-38-06 | Information Disclosure | /sidecar/events exposure of VIX/spot | low | accept | GW-05 private-network-only is unchanged; this task adds a payload shape to the existing internal endpoint, no new route |
| T-38-SC | Tampering | pip installs | low | accept | No new pip packages — schwab-py stays pinned at 1.5.1 (D22, never upgrade); `get_quotes` already exists in the installed client |
</threat_model>

<verification>
- `cd apps/sidecar && .venv/bin/pytest tests/test_streamer.py -x -q` green (new poll tests).
- `cd apps/sidecar && .venv/bin/pytest tests/ -q` full sidecar suite green.
- Grep confirms no `is_rth` / `is_open` guard inside `start_indices_poll`.
- Grep confirms `start_indices_poll` is created AND cancelled in `main.py`'s `_acquire_lock_and_init`.
</verification>

<success_criteria>
The sidecar polls the VIX family on a fixed ~20s interval and emits one Z-suffixed indices frame per poll; a per-symbol failure degrades to null; the poll task shares the streamer's lock-scoped lifecycle; symbols confirmed live before the parser was built.
</success_criteria>

<output>
Create `.planning/phases/38-live-spx-spot-across-the-app-fan-the-already-flowing-sidecar/38-02-SUMMARY.md` when done. Record the CONFIRMED VIX-family symbol strings + failure mode from the checkpoint.
</output>
