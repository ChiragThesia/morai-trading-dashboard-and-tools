---
phase: 38-live-spx-spot-across-the-app-fan-the-already-flowing-sidecar
plan: 03
type: tdd
wave: 2
depends_on: ["38-01"]
files_modified:
  - apps/server/src/adapters/http/stream-fan-out.ts
  - apps/server/src/adapters/http/stream-fan-out.test.ts
  - apps/server/src/adapters/http/strm04-regression.test.ts
  - apps/server/src/adapters/http/sidecar-sse.ts
  - apps/server/src/adapters/http/sidecar-sse.test.ts
  - apps/server/src/main.ts
autonomous: true
requirements: [LIVE-02]
must_haves:
  truths:
    - "A valid option tick's underlyingPrice is broadcast as a named 'spot' SSE event (zero new Schwab calls)"
    - "A repeated spot value within the flush interval is NOT re-sent (on-change, max ~1/sec per symbol)"
    - "A sidecar 'indices' frame Zod-parses and is broadcast as a named 'indices' SSE event"
    - "A malformed spot/indices frame or a throwing broadcast callback never severs the stream"
    - "The spot/indices lanes write zero leg_observations rows (STRM-04 pure in-memory)"
  artifacts:
    - path: apps/server/src/adapters/http/stream-fan-out.ts
      provides: "bufferSpot/flushSpot (on-change) + bufferIndices/flushIndices, registered on the existing 1s interval"
      contains: "bufferSpot"
    - path: apps/server/src/adapters/http/sidecar-sse.ts
      provides: "broadcastSpot at the observeSpot site + sidecarIndicesSchema branch → broadcastIndices"
      contains: "broadcastSpot"
    - path: apps/server/src/main.ts
      provides: "broadcastSpot/broadcastIndices wired into the runSidecarStreamWithReconnect deps"
      contains: "broadcastSpot"
  key_links:
    - from: apps/server/src/adapters/http/sidecar-sse.ts
      to: apps/server/src/adapters/http/stream-fan-out.ts
      via: "deps.broadcastSpot/broadcastIndices wired to bufferSpot/bufferIndices in main.ts"
      pattern: "bufferSpot|bufferIndices"
    - from: apps/server/src/adapters/http/stream-fan-out.ts
      to: apps/web/src/hooks/useLiveStream.ts
      via: "named 'spot'/'indices' SSE events consumed by es.addEventListener (38-04)"
      pattern: "event: \"spot\"|event: \"indices\""
---

<objective>
Extend the existing SSE fan-out hub and sidecar consumer with a spot lane and an indices
lane, riding the SAME `event_queue → /sidecar/events → sidecar-sse → stream-fan-out →
/api/stream` pipe the greeks already use (LIVE-02). SPX spot needs ZERO new Schwab calls —
the `underlyingPrice` already arrives at the `observeSpot` guard site; add a sibling
broadcast. The one genuinely-new bit is the on-change throttle. The `indices` frame gets its
own local wire schema + dispatch branch. No new sidecar route, no new server route, no
second EventSource.

Purpose: fan the already-flowing spot + the new VIX-family frames out to every browser,
coalesced and honest.
Output: `bufferSpot`/`flushSpot` (on-change), `bufferIndices`/`flushIndices`, the
`broadcastSpot`/`broadcastIndices` deps + dispatch, and the `main.ts` wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-live-spx-spot-across-the-app-fan-the-already-flowing-sidecar/38-CONTEXT.md
@.planning/phases/38-live-spx-spot-across-the-app-fan-the-already-flowing-sidecar/38-PATTERNS.md

# The coalescer to mirror (bufferTick/flushTicks/startFlushInterval/resetForTesting):
@apps/server/src/adapters/http/stream-fan-out.ts
@apps/server/src/adapters/http/stream-fan-out.test.ts
# The no-persistence gate to extend for the spot lane:
@apps/server/src/adapters/http/strm04-regression.test.ts
# The dispatchFrame + observeSpot guard site + ConnectToSidecarStreamDeps to extend:
@apps/server/src/adapters/http/sidecar-sse.ts
@apps/server/src/adapters/http/sidecar-sse.test.ts
# Interface note: the new browser-facing shapes MUST serialize to the 38-01 contract
# schemas (streamSpotEvent {spot,ts} / streamIndicesEvent {vix,vvix,vix9d,vix3m,ts}).
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: fan-out spot + indices lanes with on-change throttle</name>
  <files>apps/server/src/adapters/http/stream-fan-out.ts, apps/server/src/adapters/http/stream-fan-out.test.ts, apps/server/src/adapters/http/strm04-regression.test.ts</files>
  <behavior>
    - bufferSpot(spot, ts) then flushSpot() sends ONE named "spot" SSE write to each live client,
      data = JSON of { spot, ts } (matches streamSpotEvent).
    - Buffering the SAME spot value again and flushing is a NO-OP (on-change: hold a
      lastSentSpot; skip the write when the buffered value equals the last sent). A CHANGED value
      sends again. This caps output to max ~1 frame/sec (the existing flush interval) AND drops
      unchanged repeats (CONTEXT Area 1 Q2 — no keepalives).
    - bufferIndices(values, ts) then flushIndices() sends ONE named "indices" SSE write with data
      = JSON of { vix, vvix, vix9d, vix3m, ts }; an unchanged indices payload is skipped the same
      way (compare against the last-sent snapshot).
    - Dead-client cleanup carries to both lanes: an aborted client is removed before write; a
      writeSSE rejection removes the client in .catch (both existing paths).
    - resetForTesting() clears the new spot/indices buffers AND the last-sent trackers.
  </behavior>
  <action>
    Mirror `bufferTick`/`flushTicks` exactly for a spot lane and an indices lane. Add a
    `latestSpot: { spot: number; ts: string } | null` staging slot + a `lastSentSpot: number |
    null` on-change tracker; `flushSpot()` no-ops when there are no clients OR when
    `latestSpot.spot === lastSentSpot`, else writes `{ event: "spot", data }`, sets lastSentSpot,
    clears the slot. Same shape for indices with a last-sent snapshot compared field-by-field (or
    by serialized equality). Register `flushSpot`/`flushIndices` on the EXISTING 1s timer — extend
    `startFlushInterval` to also call them (one timer, siblings) rather than adding a second
    interval. Keep the two dead-client cleanup paths. Update `resetForTesting` to clear the new
    state. STRM-04: no Postgres/Drizzle import — pure in-memory. Add the fan-out tests from the
    behavior block (copy the strm04-regression `makeSilentClient` harness), and extend
    strm04-regression.test.ts to assert the spot/indices lanes write zero leg_observations rows.
    `// ponytail: value-equality skip, no epsilon — Schwab spot is already conflated ~1/sec`.
  </action>
  <verify>
    <automated>bun run test -- --run apps/server/src/adapters/http/stream-fan-out.test.ts apps/server/src/adapters/http/strm04-regression.test.ts</automated>
  </verify>
  <done>Spot/indices flush send one named event per changed value, no-op on unchanged, clean up dead clients, and write zero DB rows.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: sidecar-sse broadcastSpot at the observeSpot site + indices frame dispatch</name>
  <files>apps/server/src/adapters/http/sidecar-sse.ts, apps/server/src/adapters/http/sidecar-sse.test.ts</files>
  <behavior>
    - On a valid option tick with underlyingPrice > 0, deps.broadcastSpot(underlyingPrice, ts) is
      called alongside the existing deps.observeSpot — same guard, both fire.
    - A broadcastSpot that throws is swallowed (log name only) and does NOT reject
      connectToSidecarStream (stream survives) — same discipline as observeSpot's swallow.
    - A frame with { type: "indices", vix, vvix, vix9d, vix3m, ts } Zod-parses against a NEW local
      sidecarIndicesSchema and calls deps.broadcastIndices(values, ts), then returns early (does
      NOT fall through to the option-tick schema, which would reject+drop it).
    - A malformed indices frame (bad shape) is dropped silently; a throwing broadcastIndices is
      swallowed. observeSpot behavior is unchanged (SNAP-01 still fed).
  </behavior>
  <action>
    Add optional `broadcastSpot?: (spot: number, ts: string) => void` and
    `broadcastIndices?: (values: { vix: number|null; vvix: number|null; vix9d: number|null;
    vix3m: number|null }, ts: string) => void` to `ConnectToSidecarStreamDeps`. At the existing
    `underlyingPrice !== null && > 0` guard, add a second try/catch calling `deps.broadcastSpot?.`
    beside `deps.observeSpot?.` — swallow-and-log name only (CR-01: a throw here permanently kills
    the stream). In `dispatchFrame`, add a NEW local `sidecarIndicesSchema` (raw wire, nullable
    number fields + `ts: z.string()`) and try its `safeParse` BEFORE the option-tick parse (order
    matters — the tick schema would reject an indices frame and drop it); on success call
    `deps.broadcastIndices?.` inside try/catch (swallow) and `return`. No `any`/`as`/`!` — Zod at
    the boundary (typescript.md). Extend sidecar-sse.test.ts with the behavior-block cases (assert
    a mock broadcastSpot/broadcastIndices is called with the right args; assert a throwing
    callback does not reject the stream; assert an indices frame does not reach the tick path).
  </action>
  <verify>
    <automated>bun run test -- --run apps/server/src/adapters/http/sidecar-sse.test.ts</automated>
  </verify>
  <done>broadcastSpot fires at the guarded tick site; the indices frame routes to broadcastIndices and returns early; a throwing callback never severs the stream; observeSpot unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: main.ts — wire broadcastSpot/broadcastIndices into the stream deps</name>
  <files>apps/server/src/main.ts</files>
  <action>
    Import `bufferSpot`/`bufferIndices` from stream-fan-out.ts alongside the existing
    `bufferTick`. In the `runSidecarStreamWithReconnect` deps object, add
    `broadcastSpot: (spot, ts) => bufferSpot(spot, ts)` and
    `broadcastIndices: (values, ts) => bufferIndices(values, ts)` beside the existing
    `observeSpot` line (observeSpot stays — the piggyback spot source feeds SNAP-01, a different
    consumer; broadcastSpot is the NEW dedicated fan-out lane). Composition-root wiring only —
    no business logic (exempt from TDD; verified by the server suite booting). `startFlushInterval`
    already runs; no second call needed since Task 1 folded the new flushes into it.
  </action>
  <verify>
    <automated>bun run test -- --run apps/server/src/adapters/http/stream-fan-out.test.ts apps/server/src/adapters/http/sidecar-sse.test.ts &amp;&amp; bun run typecheck</automated>
  </verify>
  <done>main.ts wires broadcastSpot→bufferSpot and broadcastIndices→bufferIndices; observeSpot retained; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sidecar SSE → server dispatchFrame | untrusted frame Zod-parsed before any broadcast |
| server fan-out → N browsers | in-memory pub-sub; no DB, no account data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-01 | Denial of Service | dispatchFrame indices branch + broadcast callbacks | high | mitigate | New `sidecarIndicesSchema` `safeParse` drops malformed frames; broadcastSpot/broadcastIndices wrapped in try/catch that logs `.name` only — a throw can never reject connectToSidecarStream and sever the live stream for all browsers (CR-01) |
| T-38-04 | Tampering | STRM-04 no-persistence boundary | medium | mitigate | Spot/indices lanes are pure in-memory (no Postgres/Drizzle import); strm04-regression.test.ts asserts zero leg_observations rows written by the new lanes |
| T-38-05 | Information Disclosure | on-change throttle memory | low | accept | Only the latest per-symbol value is held (bounded Map/slot) + a last-sent scalar; no unbounded growth, no sensitive data |
| T-38-SC | Tampering | npm installs | low | accept | No new npm packages — zod/hono already present |
</threat_model>

<verification>
- `bun run test -- --run apps/server/src/adapters/http/stream-fan-out.test.ts apps/server/src/adapters/http/sidecar-sse.test.ts apps/server/src/adapters/http/strm04-regression.test.ts` green.
- `bun run typecheck && bun run lint` clean (no any/as/! on the new frame shapes).
- Grep confirms `stream.routes.ts` and `/sidecar/events` proxy are UNCHANGED (additive lanes only).
</verification>

<success_criteria>
Spot broadcasts on-change at ~1/sec with zero new Schwab calls; the indices frame routes to its own broadcast; a bad frame or throwing callback never severs the stream; no DB writes.
</success_criteria>

<output>
Create `.planning/phases/38-live-spx-spot-across-the-app-fan-the-already-flowing-sidecar/38-03-SUMMARY.md` when done.
</output>
