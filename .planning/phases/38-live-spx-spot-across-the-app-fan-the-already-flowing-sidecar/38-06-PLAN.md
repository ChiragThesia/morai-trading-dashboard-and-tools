---
phase: 38-live-spx-spot-across-the-app-fan-the-already-flowing-sidecar
plan: 06
type: execute
wave: 4
depends_on: ["38-05"]
files_modified:
  - apps/web/src/components/RegimeBoard.tsx
  - apps/web/src/components/RegimeBoard.test.tsx
  - apps/web/src/screens/MarketRail.tsx
  - apps/web/src/screens/MarketRail.test.tsx
  - apps/web/src/screens/Overview.tsx
autonomous: true
requirements: [LIVE-05]
must_haves:
  truths:
    - "While status==='live', the vix-term-structure / vvix / vix9d-vix gauges DISPLAY the live value with a client-recomputed band tint"
    - "The entry-gate verdict chip, the stored indicator.band gate path, and the /api/analytics/regime EOD source are untouched"
    - "hy-oas stays FRED (never live); FRED ingestion untouched"
    - "On quiet/stalled the gauges revert to the EOD indicator.value and the 'EOD · as of …' footer"
    - "MarketRail receives liveIndices as a prop and never calls useLiveStream (D-06)"
  artifacts:
    - path: apps/web/src/components/RegimeBoard.tsx
      provides: "optional liveIndices/liveStatus props; live display value + client band recompute for the 3 broker-quotable rows"
      contains: "liveIndices"
    - path: apps/web/src/screens/MarketRail.tsx
      provides: "liveIndices/liveStatus props forwarded to RegimeBoard (no hook call)"
      contains: "liveIndices"
  key_links:
    - from: apps/web/src/screens/overview-mobile/useOverviewModel.ts
      to: apps/web/src/screens/MarketRail.tsx
      via: "Overview.tsx passes model.liveIndices/liveStatus into <MarketRail>; MarketRail forwards to RegimeBoard"
      pattern: "liveIndices"
    - from: apps/web/src/components/RegimeBoard.tsx
      to: "@morai/core"
      via: "bandVixTermStructure/bandVvix/bandVix9dRatio recompute the LIVE display band (T-31-05 display-only exception)"
      pattern: "bandVvix|bandVixTermStructure|bandVix9dRatio"
---

<objective>
Enforce the DISPLAY-LIVE / GATE-EOD LAW on the regime rail (LIVE-05). The three
broker-quotable gauges — `vix-term-structure` (VIX/VIX3M), `vvix`, `vix9d-vix` (VIX9D/VIX) —
DISPLAY live values (with band coloring recomputed client-side from the live value against the
response's effective thresholds), while the entry-gate verdict chip, the stored
`indicator.band` gate path, the `/api/analytics/regime` EOD source, and `hy-oas` stay exactly
as they are. This kills the "as of 2026-07-09" staleness on the rail for DISPLAY only —
feeding intraday ticks into gates would recreate the v1.3 flapping failure.

Purpose: a live-tinted regime rail that never lies about which values drive the gate.
Output: `RegimeBoard` live-display props + client band recompute; `MarketRail` prop
forwarding; the `Overview.tsx` MarketRail wiring line.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-live-spx-spot-across-the-app-fan-the-already-flowing-sidecar/38-CONTEXT.md
@.planning/phases/38-live-spx-spot-across-the-app-fan-the-already-flowing-sidecar/38-PATTERNS.md

# The rail — Row gauge (value ~153, marker ~174-181), GateChip (SEPARATE source, untouched),
# hy-oas (stays FRED), the "EOD · as of …" footer (~366-382), bandWarn/bandCrisis on the payload:
@apps/web/src/components/RegimeBoard.tsx
@apps/web/src/components/RegimeBoard.test.tsx
# The wiring component — mounts <RegimeBoard dense>; must take props, not call the hook (D-06):
@apps/web/src/screens/MarketRail.tsx
@apps/web/src/screens/MarketRail.test.tsx
# The <MarketRail> mount site (~639) + the model destructure (liveIndices/liveStatus available):
@apps/web/src/screens/Overview.tsx
# The EXACT client-side band-recompute precedent to copy (imports + BANDERS map + call shape):
@apps/web/src/hooks/useRuleSettingsPreview.ts
# Interface: liveIndices = StreamIndicesEvent {vix,vvix,vix9d,vix3m, ts} (nullable), from 38-04's
# model.liveIndices. Ratios: vix-term-structure = vix/vix3m; vvix = vvix; vix9d-vix = vix9d/vix
# (confirmed against getRegimeBoard.ts: VIXCLS/VXVCLS, raw VVIX, VIX9D/VIXCLS).
</context>

<tasks>

<task type="auto">
  <name>Task 1: RegimeBoard — live display value + client band recompute (display-live/gate-EOD)</name>
  <files>apps/web/src/components/RegimeBoard.tsx, apps/web/src/components/RegimeBoard.test.tsx</files>
  <action>
    Add optional props `liveIndices?: StreamIndicesEvent | null` and `liveStatus?: LiveStreamStatus`
    to `RegimeBoard`. Import the three banders from `@morai/core` and build the id→bander map
    exactly as `useRuleSettingsPreview.ts` does (`{ "vix-term-structure": bandVixTermStructure,
    vvix: bandVvix, "vix9d-vix": bandVix9dRatio }`). For each of the THREE broker-quotable rows
    only, compute the live value when `liveStatus === "live"` and the required liveIndices inputs
    are finite:
      - vix-term-structure → liveIndices.vix / liveIndices.vix3m (both non-null/finite)
      - vvix → liveIndices.vvix
      - vix9d-vix → liveIndices.vix9d / liveIndices.vix (both non-null/finite)
    When a live value exists, pass it (and a client-recomputed band = `bander(liveValue, { warn:
    indicator.bandWarn, crisis: indicator.bandCrisis })`) into `Row` as optional `liveValue`/
    `liveBand` props; `Row` renders `liveValue.toFixed(2)` + the live band's color/marker when
    present, else the unchanged `indicator.value`/`indicator.band`. If a required input is null
    (per-symbol failure) OR status is quiet/stalled, the row shows the EOD value/band untouched
    (honest — never fabricate). This client band recompute is a DELIBERATE, scoped exception to
    T-31-05 ("never recompute band client-side"), allowed ONLY for the live-display tint and
    approved by CONTEXT Area 2 Q1 — add a comment saying so.
    DO NOT touch: `GateChip`/`usePicker().gate` (separate source — the entry-gate verdict stays
    EOD), the `hy-oas` row (stays FRED — not in the bander map), or the server's `indicator.band`
    for anything other than the explicit live-tint overlay. Flip the freshness footer to a live
    marker while `liveStatus === "live"` and at least one live value renders; revert to the exact
    existing `"EOD · as of {newest}"` string (with the stale-mix suffix) on quiet/stalled/no-live
    (catch #26 — never silently mix). Extend RegimeBoard.test.tsx: (a) live vvix value + client
    band shown under `liveStatus="live"`; (b) quiet → EOD value + "EOD · as of …" footer; (c)
    GateChip + hy-oas never overridden by liveIndices; (d) a per-symbol-null input degrades that
    one row to EOD. Use live values distinct from the EOD fixture values (catch #20).
  </action>
  <verify>
    <automated>bun run test -- --run apps/web/src/components/RegimeBoard.test.tsx</automated>
  </verify>
  <done>The 3 broker-quotable gauges display live values with a client-recomputed band while live; gate chip, indicator.band gate path, hy-oas, and the EOD footer revert are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: MarketRail forwards liveIndices/liveStatus (no hook) + Overview wiring</name>
  <files>apps/web/src/screens/MarketRail.tsx, apps/web/src/screens/MarketRail.test.tsx, apps/web/src/screens/Overview.tsx</files>
  <action>
    Add optional `liveIndices?: StreamIndicesEvent | null` and `liveStatus?: LiveStreamStatus`
    props to `MarketRail` and forward them to `<RegimeBoard dense liveIndices={liveIndices}
    liveStatus={liveStatus} />`. MarketRail MUST NOT import or call `useLiveStream` — it receives
    the values as props from the surface's single consumer (D-06 one-hook-per-surface; a second
    hook opens a second EventSource). In `Overview.tsx`, destructure `liveIndices` from the model
    (liveStatus is already destructured) and pass both into the `<MarketRail … />` mount
    (`liveIndices={liveIndices} liveStatus={liveStatus}`). This is the ONLY Overview.tsx edit in
    this plan — it shares the file with 38-05, hence this plan's later wave. Extend
    MarketRail.test.tsx: render MarketRail with liveIndices + `liveStatus="live"` and assert the
    live regime value reaches the rail (integration through RegimeBoard); assert MarketRail's
    source contains no `useLiveStream` import (grep-style guard or a render-without-EventSource
    assertion).
  </action>
  <verify>
    <automated>bun run test -- --run apps/web/src/screens/MarketRail.test.tsx apps/web/src/screens/Overview.test.tsx</automated>
  </verify>
  <done>MarketRail forwards liveIndices/liveStatus to RegimeBoard without calling the hook; Overview passes the model's values in; the existing Overview suite stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live display value vs EOD gate source | two visibly distinct sources by design; only display is live |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-09 | Tampering (gate corruption) | display-live/gate-EOD boundary | high | mitigate | Live values touch DISPLAY only: GateChip/usePicker().gate, the stored indicator.band gate path, and the /api/analytics/regime EOD source are never fed intraday values (prevents the v1.3 flapping failure). Tests assert the gate chip + hy-oas are unaffected by liveIndices |
| T-38-07 | Spoofing (stale-as-live) | gauge value + footer | high | mitigate | Live value/band shown ONLY while `liveStatus === "live"` and inputs finite; the footer reverts to "EOD · as of …" on quiet/stalled — never a silent live/EOD mix (catch #26) |
| T-38-10 | Tampering | second EventSource (D-06) | medium | mitigate | MarketRail receives liveIndices as a prop and never calls useLiveStream — no second stream consumer opens on the surface |
| T-38-SC | Tampering | npm installs | low | accept | No new npm packages — banders already exported from @morai/core |
</threat_model>

<verification>
- `bun run test -- --run apps/web/src/components/RegimeBoard.test.tsx apps/web/src/screens/MarketRail.test.tsx apps/web/src/screens/Overview.test.tsx` green.
- `bun run typecheck && bun run lint` clean.
- Grep confirms `MarketRail.tsx` has no `useLiveStream` import; `useRegimeBoard`/`/api/analytics/regime`/`getRegimeBoard.ts` are untouched (EOD source unchanged).
</verification>

<success_criteria>
The regime rail's three broker-quotable gauges read live while the stream is live (client-recomputed band), the gate/verdict/stored-band/EOD source stay EOD, hy-oas stays FRED, and the footer honestly reverts on quiet/stalled.
</success_criteria>

<output>
Create `.planning/phases/38-live-spx-spot-across-the-app-fan-the-already-flowing-sidecar/38-06-SUMMARY.md` when done.
</output>
