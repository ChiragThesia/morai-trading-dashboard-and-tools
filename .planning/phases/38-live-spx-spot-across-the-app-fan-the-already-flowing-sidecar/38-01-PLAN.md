---
phase: 38-live-spx-spot-across-the-app-fan-the-already-flowing-sidecar
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/stream-events.ts
  - packages/contracts/src/stream-events.test.ts
autonomous: true
requirements: [LIVE-01]
must_haves:
  truths:
    - "A well-formed spot frame {spot, ts:'…Z'} parses; a +00:00 ts is rejected"
    - "A well-formed indices frame {vix,vvix,vix9d,vix3m, ts:'…Z'} parses; a +00:00 ts is rejected"
    - "Existing streamLiveGreekEvent / streamPingEvent schemas are unchanged (old clients unaffected)"
  artifacts:
    - path: packages/contracts/src/stream-events.ts
      provides: "streamSpotEvent + streamIndicesEvent schemas + inferred types"
      contains: "streamSpotEvent"
    - path: packages/contracts/src/stream-events.test.ts
      provides: "Z-suffix accept + +00:00 reject cases for both new events"
      contains: "streamIndicesEvent"
  key_links:
    - from: packages/contracts/src/stream-events.ts
      to: apps/web/src/hooks/useLiveStream.ts
      via: "streamSpotEvent/streamIndicesEvent imported and .safeParse'd per frame (38-04)"
      pattern: "streamSpotEvent|streamIndicesEvent"
---

<objective>
Add two additive browser-facing SSE event schemas — `streamSpotEvent` and
`streamIndicesEvent` — to the ONE stream-events contract module, each enforcing the
Z-suffix timestamp law. This is the interface-first foundation both the server fan-out
(38-03) and the web hook (38-04) build against, so it ships alone and first (LIVE-01).

Purpose: a single Zod source of truth for the new spot + VIX-family payloads, parsed at
every trust boundary, additive so no existing client breaks.
Output: `streamSpotEvent`, `streamIndicesEvent`, their `z.infer` types, and test cases.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-live-spx-spot-across-the-app-fan-the-already-flowing-sidecar/38-CONTEXT.md
@.planning/phases/38-live-spx-spot-across-the-app-fan-the-already-flowing-sidecar/38-PATTERNS.md

# The file to extend — read the existing streamLiveGreekEvent / streamPingEvent shapes
# and the header timestamp-discipline comment (lines 5-15) before adding.
@packages/contracts/src/stream-events.ts
@packages/contracts/src/stream-events.test.ts
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: streamSpotEvent — additive spot schema + Z-suffix tests</name>
  <files>packages/contracts/src/stream-events.ts, packages/contracts/src/stream-events.test.ts</files>
  <behavior>
    - Accepts { spot: 5842.375, ts: "2026-07-13T14:30:00.000Z" } (a spot value deliberately
      NOT a round number, to keep downstream jsdom tests honest per Pitfall 5 / catch #20).
    - Rejects a ts ending in "+00:00" (mirror the existing streamLiveGreekEvent +00:00 case).
    - Rejects a non-number spot; rejects a missing field (spot or ts absent).
    - streamLiveGreekEvent and streamPingEvent parse exactly as before (add one assertion
      that a known-good greek payload still parses — proves additive, not breaking).
  </behavior>
  <action>
    Add `streamSpotEvent = z.object({ spot: z.number(), ts: z.string().datetime() })` plus
    `export type StreamSpotEvent = z.infer<typeof streamSpotEvent>` beside the existing events,
    following the module's header timestamp-discipline comment verbatim (ts REQUIRES a trailing
    "Z"; "+00:00" is rejected). Keep the same section-comment style as the surrounding schemas.
    In the test file, add a `describe("streamSpotEvent")` with the accept/reject cases in the
    behavior block, copying the assertion grammar of the existing streamLiveGreekEvent suite.
    Do NOT touch streamLiveGreekEvent / streamPingEvent / streamReconcileEvent / streamFillEvent
    definitions — additive only (LIVE-01, CONTEXT Area 1 Q1).
  </action>
  <verify>
    <automated>bun run test -- --run packages/contracts/src/stream-events.test.ts</automated>
  </verify>
  <done>streamSpotEvent parses a Z-suffixed frame, rejects "+00:00" and non-number/missing fields; existing greek/ping schemas still parse.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: streamIndicesEvent — additive VIX-family schema + Z-suffix tests</name>
  <files>packages/contracts/src/stream-events.ts, packages/contracts/src/stream-events.test.ts</files>
  <behavior>
    - Accepts { vix: 15.2, vvix: 92.1, vix9d: 14.8, vix3m: 16.5, ts: "2026-07-13T14:30:00.000Z" }.
    - Each of vix/vvix/vix9d/vix3m is nullable (a symbol Schwab does not return degrades to null,
      never fabricated — mirrors getRegimeBoard.ts "omit rather than fabricate"): accepts
      { vix: 15.2, vvix: null, vix9d: null, vix3m: 16.5, ts: "…Z" }.
    - Rejects a ts ending in "+00:00".
    - Rejects a string where a number|null is expected (e.g. vix: "15.2").
  </behavior>
  <action>
    Add `streamIndicesEvent = z.object({ vix: z.number().nullable(), vvix: z.number().nullable(),
    vix9d: z.number().nullable(), vix3m: z.number().nullable(), ts: z.string().datetime() })` and
    `export type StreamIndicesEvent = z.infer<typeof streamIndicesEvent>`. One batched event keyed
    by the four regime symbols (Claude's discretion per CONTEXT Area 1 Q1; RESEARCH recommends one
    batched `indices` event — all four always poll together). The four fields are `.nullable()` so
    a per-symbol Schwab failure (Pitfall 2/3) is representable without dropping the frame. Add the
    matching `describe("streamIndicesEvent")` test cases from the behavior block.
  </action>
  <verify>
    <automated>bun run test -- --run packages/contracts/src/stream-events.test.ts</automated>
  </verify>
  <done>streamIndicesEvent parses a Z-suffixed frame with all-present or per-symbol-null values, rejects "+00:00" and a non-number/non-null field.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sidecar JSON → server Zod | raw wire frame parsed before use (38-03 consumes this schema) |
| server JSON → browser Zod | browser-facing event parsed in useLiveStream (38-04 consumes this schema) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-01 | Tampering | streamSpotEvent / streamIndicesEvent shape | medium | mitigate | Strict `z.object` with `z.string().datetime()` (Z-suffix) + typed nullable fields; a malformed frame `.safeParse`-fails and is dropped downstream (T-12-05-04) rather than casting bad data into state |
| T-38-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase (RESEARCH: "installs no new packages in any ecosystem") — no supply-chain surface added |
</threat_model>

<verification>
- `bun run test -- --run packages/contracts/src/stream-events.test.ts` green.
- `bun run typecheck` clean (new exported types resolve).
- Grep confirms `streamLiveGreekEvent`/`streamPingEvent` definitions are byte-unchanged.
</verification>

<success_criteria>
Both new events accept a Z-suffixed frame and reject "+00:00"; existing events untouched; typecheck clean.
</success_criteria>

<output>
Create `.planning/phases/38-live-spx-spot-across-the-app-fan-the-already-flowing-sidecar/38-01-SUMMARY.md` when done.
</output>
