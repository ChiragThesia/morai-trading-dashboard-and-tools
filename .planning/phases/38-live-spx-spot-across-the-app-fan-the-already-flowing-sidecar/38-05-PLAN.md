---
phase: 38-live-spx-spot-across-the-app-fan-the-already-flowing-sidecar
plan: 05
type: execute
wave: 3
depends_on: ["38-04"]
files_modified:
  - apps/web/src/screens/overview-mobile/useOverviewModel.ts
  - apps/web/src/screens/Overview.tsx
  - apps/web/src/screens/Overview.test.tsx
  - apps/web/src/screens/overview-mobile/OverviewMobile.tsx
  - apps/web/src/screens/overview-mobile/MobileHero.tsx
  - apps/web/src/screens/overview-mobile/MobileHero.test.tsx
  - apps/web/src/screens/overview-mobile/MobileMarketSection.tsx
  - apps/web/src/screens/overview-mobile/MobileMarketSection.test.tsx
autonomous: true
requirements: [LIVE-04]
must_haves:
  truths:
    - "The desktop header SPX chip shows the live spot while status==='live', and the EOD gex.spot (stale-styled) otherwise"
    - "The desktop gamma-profile + GEX-by-strike spot markers track the live-aware spot"
    - "The mobile hero SPX shows the live spot while live, else gex.spot, else — (never 5800)"
    - "The Key-levels 'Spot' row reads the live-aware spot at BOTH call sites (desktop GexRail + mobile MobileMarketSection), not raw gex.spot"
    - "No direct gex.spot / gex?.spot read (including via keyLevelsFor) survives outside an honest-badge fallback on the in-scope surfaces"
  artifacts:
    - path: apps/web/src/screens/Overview.tsx
      provides: "PillHeader SPX chip + GexRail markers read the model's live-aware spot/displaySpot"
      contains: "displaySpot"
    - path: apps/web/src/screens/overview-mobile/MobileHero.tsx
      provides: "SPX segment reads live-aware spot with an honest live/EOD tint"
      contains: "liveStatus"
    - path: apps/web/src/screens/overview-mobile/useOverviewModel.ts
      provides: "keyLevelsFor gains an optional spot override so its 'Spot' row is live-aware"
      contains: "keyLevelsFor"
  key_links:
    - from: apps/web/src/screens/overview-mobile/useOverviewModel.ts
      to: apps/web/src/screens/Overview.tsx
      via: "displaySpot/spot/liveStatus threaded into PillHeader + GexRail; keyLevelsFor(gex, spot) at :412"
      pattern: "displaySpot|liveStatus|keyLevelsFor"
    - from: apps/web/src/screens/overview-mobile/useOverviewModel.ts
      to: apps/web/src/screens/overview-mobile/MobileHero.tsx
      via: "m.displaySpot + m.liveStatus passed to MobileHero"
      pattern: "displaySpot"
    - from: apps/web/src/screens/overview-mobile/useOverviewModel.ts
      to: apps/web/src/screens/overview-mobile/MobileMarketSection.tsx
      via: "spot prop passed from OverviewMobile; keyLevelsFor(gex, spot) at :53"
      pattern: "keyLevelsFor"
---

<objective>
Collapse BOTH of Overview's spot sources onto the one live-aware seam and render the honest
live/EOD badge (LIVE-04). Today `useOverviewModel.spot` and the DIRECT `gex.spot` reads in
`Overview.tsx` (PillHeader SPX chip, GexRail markers) and the mobile hero coexist — if only
one goes live, two different "SPX" numbers appear on screen at once (Pitfall 1). This plan
re-points every in-scope spot reader to the model's `spot` (engine, live-aware) / `displaySpot`
(honest nullable) and tints the value only while the stream is live.

Purpose: one SPX number everywhere, live when live, honestly EOD otherwise.
Output: PillHeader + GexRail + MobileHero reading the live-aware seam with a catch-#26 badge.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-live-spx-spot-across-the-app-fan-the-already-flowing-sidecar/38-CONTEXT.md
@.planning/phases/38-live-spx-spot-across-the-app-fan-the-already-flowing-sidecar/38-PATTERNS.md

# The direct gex.spot reads to re-point (PillHeader SPX chip ~468; GexRail GammaProfile ~395,
# GexBars ~402; payoff hero ~728/774 ALREADY uses model.spot) + the model destructure ~525-555:
@apps/web/src/screens/Overview.tsx
# The existing useLiveStream mock + live/stalled override helper to extend:
@apps/web/src/screens/Overview.test.tsx
# The mobile hero SPX segment (~44-47) + its prop shape; OverviewMobile passes spot ~111:
@apps/web/src/screens/overview-mobile/MobileHero.tsx
@apps/web/src/screens/overview-mobile/MobileHero.test.tsx
@apps/web/src/screens/overview-mobile/OverviewMobile.tsx
# Interface from 38-04: model exposes `spot` (live-aware number), `displaySpot` (number|null,
# never 5800), `liveStatus` ("live"|"quiet"|"stalled").
</context>

<tasks>

<task type="auto">
  <name>Task 1: Desktop — keyLevelsFor spot override + PillHeader SPX chip + GexRail markers</name>
  <files>apps/web/src/screens/overview-mobile/useOverviewModel.ts, apps/web/src/screens/Overview.tsx, apps/web/src/screens/Overview.test.tsx</files>
  <action>
    FIRST, give `keyLevelsFor` a live-spot override (shared by the desktop rail here AND the mobile
    Key-levels list in Task 2). In useOverviewModel.ts change the signature to
    `keyLevelsFor(gex, spot?: number)` and make the "Spot" row `value: spot ?? gex.spot` (default
    preserves every existing caller's behavior; a passed live-aware `spot` makes the row live). This
    is the ONLY useOverviewModel.ts edit in this plan — it shares the file with 38-04 (model seam),
    hence this wave. Then thread `displaySpot` + `liveStatus` (already in the model return,
    destructured in OverviewDesktop) into `PillHeader` and `spot` into `GexRail`:
      - PillHeader SPX chip (currently `value={gex !== undefined ? gex.spot.toFixed(1) : "—"}`):
        render `displaySpot !== null ? displaySpot.toFixed(1) : "—"` and drive its `valueClassName`
        by `liveStatus` — a live tint while `liveStatus === "live"`, the existing dim/EOD styling
        on quiet/stalled (honest badge, catch #26; never show a stale value as live). Add a compact
        live/EOD marker (Claude's discretion on exact styling per CONTEXT — reuse the app's existing
        live/stale grammar, do not invent a bespoke badge). Leave the other 9 chips (net γ, VIX,
        etc.) exactly as-is.
      - GexRail GammaProfile `spot={gex.spot}` → `spot={spot}` and GexBars `spot={gex.spot}` →
        `spot={spot}` (the model's live-aware engine spot; GexRail only renders inside the
        gex-defined branch, so `spot` is always a real number there — never 5800). The walls/flip/
        profile stay on the 30-min snapshot (untouched); only the marker position moves live.
      - Key levels (Overview.tsx:412): change `keyLevelsFor(gex)` → `keyLevelsFor(gex, spot)` so the
        "Spot" row matches the chip + markers (one SPX number across the whole column).
    After the change, grep `Overview.tsx` for `gex.spot`/`gex?.spot` AND `keyLevelsFor(gex)` (no
    second arg) — any remaining bare read outside an honest-badge fallback is a missed live surface
    (Pitfall 1). Update the `Overview.test.tsx` useLiveStream mock to include `liveSpot`/`liveIndices`
    and add cases: with `{ liveSpot: <non-round>, status: "live" }` the pill-header SPX chip AND the
    Key-levels "Spot" row (data-testid / row text) render the live value, and the mocked PayoffChart
    receives the live-aware spot; with `status: "quiet"` both render `GEX_FIXTURE.spot`. Use a
    liveSpot literal distinct from GEX_FIXTURE.spot (catch #20).
  </action>
  <verify>
    <automated>bun run test -- --run apps/web/src/screens/Overview.test.tsx</automated>
  </verify>
  <done>keyLevelsFor takes a spot override; the desktop SPX chip + GEX-rail markers + Key-levels "Spot" row all read the live-aware spot; the chip tints live only when status is live; grep shows no bare gex.spot or one-arg keyLevelsFor(gex) left on the desktop surface.</done>
</task>

<task type="auto">
  <name>Task 2: Mobile — hero SPX + MobileMarketSection Key-levels on the live-aware seam</name>
  <files>apps/web/src/screens/overview-mobile/OverviewMobile.tsx, apps/web/src/screens/overview-mobile/MobileHero.tsx, apps/web/src/screens/overview-mobile/MobileHero.test.tsx, apps/web/src/screens/overview-mobile/MobileMarketSection.tsx, apps/web/src/screens/overview-mobile/MobileMarketSection.test.tsx</files>
  <action>
    Hero: in OverviewMobile, change the MobileHero spot prop from `spot={m.gex?.spot ?? null}` to
    `spot={m.displaySpot}` (the honest live-or-snapshot value, still null → "—" when there's no
    data, preserving the 35.1 "never the 5800 engine fallback" contract) and pass
    `liveStatus={m.liveStatus}`. In MobileHero, add an optional `liveStatus?: LiveStreamStatus` prop;
    render the SPX value from `spot` (unchanged null → "—") and add the same honest live/EOD tint on
    the SPX segment (live only while `liveStatus === "live"`). The mobile hero VIX segment stays EOD
    (macroValues.vix) — the live VIX-family gauges are the desktop regime rail (RegimeBoard does not
    render on mobile), out of scope.
    Mobile Key-levels seam (the OTHER keyLevelsFor call site, mirrors Task 1): add a
    `spot: number | null` prop to `MobileMarketSectionProps`, pass `spot={m.spot}` from OverviewMobile,
    and change `keyLevelsFor(gex)` at MobileMarketSection.tsx:53 to `keyLevelsFor(gex, spot ??
    undefined)` so the mobile "Spot" row is live-aware too (MobileMarketSection renders inside the
    gex-defined branch, so `m.spot` is a real number there). Update the tests: MobileHero.test.tsx —
    add `liveStatus` to BASE + a case asserting the SPX segment tints live under `liveStatus="live"`
    and stays plain/EOD under `"quiet"`. MobileMarketSection.test.tsx — add a case passing a
    non-round `spot` distinct from the gex fixture spot and assert the "Spot" key-level row renders
    that live value (catch #20).
  </action>
  <verify>
    <automated>bun run test -- --run apps/web/src/screens/overview-mobile/MobileHero.test.tsx apps/web/src/screens/overview-mobile/MobileMarketSection.test.tsx</automated>
  </verify>
  <done>The mobile hero SPX shows the live value while live (else gex.spot, else "—"); the MobileMarketSection Key-levels "Spot" row reads m.spot (live-aware); VIX segment unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model → view | live-aware spot vs honest displaySpot; the badge decides which is shown |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-07 | Spoofing (stale-as-live) | SPX chip + mobile hero + GEX markers | high | mitigate | Every in-scope spot reader routes through the model's live-gated seam; the live tint is gated on `liveStatus === "live"`; quiet/stalled falls back to gex.spot with EOD styling — never a silent stale-as-live number (catch #26). Post-change grep proves no bare gex.spot read survives on these surfaces (Pitfall 1) |
| T-38-08 | Tampering | jsdom test honesty | low | mitigate | Test fixtures use a liveSpot literal distinct from gex.spot/5800 so an assertion can only pass on the real live path (catch #20) |
| T-38-SC | Tampering | npm installs | low | accept | No new npm packages |
</threat_model>

<verification>
- `bun run test -- --run apps/web/src/screens/Overview.test.tsx apps/web/src/screens/overview-mobile/MobileHero.test.tsx apps/web/src/screens/overview-mobile/MobileMarketSection.test.tsx` green.
- `bun run typecheck && bun run lint` clean.
- `grep -nE 'gex\??\.spot' apps/web/src/screens/Overview.tsx` shows only honest-badge fallback expressions (no bare live-surface read).
- `grep -n 'keyLevelsFor(gex)' apps/web/src/screens/Overview.tsx apps/web/src/screens/overview-mobile/MobileMarketSection.tsx` returns NOTHING — every call passes the spot override (the bare grep above cannot see the keyLevelsFor(gex) Spot-row read, so this is the gate that actually catches it). The Spot-row live-value test in Overview.test.tsx + MobileMarketSection.test.tsx is the behavioral proof.
</verification>

<success_criteria>
One SPX number across the desktop header, GEX rail, and mobile hero — live when the stream is live, honestly EOD/"—" otherwise; the payoff hero's T+0 already recomputes on the live spot via the model.
</success_criteria>

<output>
Create `.planning/phases/38-live-spx-spot-across-the-app-fan-the-already-flowing-sidecar/38-05-SUMMARY.md` when done.
</output>
