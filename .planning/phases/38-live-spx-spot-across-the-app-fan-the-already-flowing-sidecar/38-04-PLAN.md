---
phase: 38-live-spx-spot-across-the-app-fan-the-already-flowing-sidecar
plan: 04
type: tdd
wave: 2
depends_on: ["38-01"]
files_modified:
  - apps/web/src/hooks/useLiveStream.ts
  - apps/web/src/hooks/useLiveStream.test.ts
  - apps/web/src/screens/overview-mobile/useOverviewModel.ts
  - apps/web/src/screens/overview-mobile/useOverviewModel.test.ts
autonomous: true
requirements: [LIVE-04]
must_haves:
  truths:
    - "A 'spot' SSE event updates liveSpot; a 'indices' SSE event updates liveIndices"
    - "A malformed spot/indices frame is dropped, retaining last-known-good"
    - "liveSpot has its own freshness stamp — a spot-only feed does not paint the greeks badge live"
    - "The model's engine spot is live-aware (live only while status==='live'), else gex.spot"
    - "The model exposes a nullable displaySpot (live→liveSpot, else gex.spot, else null) — never the 5800 engine fallback for the header/hero chip"
  artifacts:
    - path: apps/web/src/hooks/useLiveStream.ts
      provides: "liveSpot/liveIndices state + named 'spot'/'indices' listeners + own freshness ref"
      contains: "liveSpot"
    - path: apps/web/src/screens/overview-mobile/useOverviewModel.ts
      provides: "live-aware engine spot + displaySpot + liveSpot/liveIndices on OverviewModel"
      contains: "displaySpot"
  key_links:
    - from: apps/web/src/hooks/useLiveStream.ts
      to: apps/web/src/screens/overview-mobile/useOverviewModel.ts
      via: "useLiveStream() destructures liveSpot/liveIndices; model computes the live-aware spot seam"
      pattern: "liveSpot|liveIndices"
---

<objective>
Expose the new live values inward: `useLiveStream` parses the named `spot`/`indices` events
into `liveSpot`/`liveIndices` (each on its own freshness stamp), and `useOverviewModel`
collapses the app's spot to ONE live-aware seam plus a nullable `displaySpot` for honest
header/hero rendering (LIVE-04). This is the single hook+model surface every spot and
VIX-family consumer reads — building it here (interface-first) means the Wave-3/4 consumer
plans receive the contract instead of hunting for it.

Purpose: one live-aware spot value for engine pricing, one honest display value for chips,
and the live VIX-family values for the regime gauges — all from the surface's single stream
consumer (D-06).
Output: `liveSpot`/`liveIndices` on `UseLiveStreamResult`; `displaySpot` + live-aware `spot`
+ `liveSpot`/`liveIndices` on `OverviewModel`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-live-spx-spot-across-the-app-fan-the-already-flowing-sidecar/38-CONTEXT.md
@.planning/phases/38-live-spx-spot-across-the-app-fan-the-already-flowing-sidecar/38-PATTERNS.md

# The hook to extend — copy the "ticks"/"ping" addEventListener + Zod-parse-per-frame blocks:
@apps/web/src/hooks/useLiveStream.ts
@apps/web/src/hooks/useLiveStream.test.ts
# The model — the single spot seam (line ~392: const spot = gex?.spot ?? 5800) + the existing
# useLiveStream destructure (~420-428) + OverviewModel interface (~315-384):
@apps/web/src/screens/overview-mobile/useOverviewModel.ts
# Interface: the new events come from 38-01 (@morai/contracts):
#   streamSpotEvent {spot, ts}; streamIndicesEvent {vix,vvix,vix9d,vix3m, ts} (all nullable).
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: useLiveStream — liveSpot/liveIndices listeners + own freshness stamp</name>
  <files>apps/web/src/hooks/useLiveStream.ts, apps/web/src/hooks/useLiveStream.test.ts</files>
  <behavior>
    - Dispatching a named "spot" MessageEvent with { spot: 5842.375, ts: "…Z" } sets liveSpot to
      5842.375 (use a NON-round value distinct from any gex/DEFAULT fallback so the test can only
      pass on the live path — Pitfall 5 / catch #20).
    - Dispatching a named "indices" MessageEvent with { vix, vvix, vix9d, vix3m, ts } sets
      liveIndices to that object; a per-symbol null is preserved.
    - Malformed JSON or a bad-shape (e.g. +00:00 ts) "spot"/"indices" frame is dropped; the
      last-known-good liveSpot/liveIndices is retained (never cast into state).
    - A "spot" frame stamps its OWN lastSpotAt ref and does NOT bump the greeks
      lastTickOrConnectAtRef — so a spot-only feed never flips the greeks badge to live
      (honest-badge separation, catch #26).
  </behavior>
  <action>
    Add `liveSpot: number | null` and `liveIndices: StreamIndicesEvent | null` state (initial
    null) and add them to `UseLiveStreamResult`. Add two `es.addEventListener` blocks copying the
    existing "ticks" block: parse with `streamSpotEvent.safeParse` / `streamIndicesEvent.safeParse`
    (imported from @morai/contracts), drop on failure, else `setLiveSpot`/`setLiveIndices`. Keep a
    SEPARATE `lastSpotAtRef`/`setLastSpotAt` freshness stamp for spot — do NOT reuse the greeks
    `lastTickOrConnectAtRef` (a spot-only feed must not paint the greeks badge live). Named events
    go to `addEventListener`, never `onmessage`; never set `status` in these handlers (the shared
    interval owns status — Pitfall 1). Return `liveSpot`/`liveIndices` (and lastSpotAt if a
    consumer needs the spot-freshness). Add the behavior-block cases to the jsdom test using the
    existing EventSource harness; use DELIBERATELY non-round fixture values (catch #20).
  </action>
  <verify>
    <automated>bun run test -- --run apps/web/src/hooks/useLiveStream.test.ts</automated>
  </verify>
  <done>A "spot"/"indices" event updates liveSpot/liveIndices; malformed frames drop and retain last-good; spot has its own freshness stamp separate from greeks.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: useOverviewModel — live-aware engine spot + nullable displaySpot + expose liveSpot/liveIndices</name>
  <files>apps/web/src/screens/overview-mobile/useOverviewModel.ts, apps/web/src/screens/overview-mobile/useOverviewModel.test.ts</files>
  <behavior>
    - When liveStatus==='live' and liveSpot!==null, the model's engine `spot` equals liveSpot;
      the payoff/scenario/greeks memos recompute on it (they already useMemo on spot — no wiring
      change). Use a liveSpot deliberately different from gex.spot so the test proves the live
      path (catch #20).
    - When status is quiet/stalled OR liveSpot is null, engine `spot` falls back to gex.spot ?? 5800
      (unchanged engine behavior — never a silent live claim).
    - displaySpot is nullable and NEVER 5800: live→liveSpot; else gex.spot; else null (for the
      "—" header/hero state). This is the one honest display seam both the desktop SPX chip and
      the mobile hero read (Pitfall 1 collapse).
    - liveSpot and liveIndices are exposed on OverviewModel for the display + regime plans.
  </behavior>
  <action>
    Destructure `liveSpot`/`liveIndices` from the existing `useLiveStream()` call. Change the
    single spot line to `const spot = (liveStatus === "live" && liveSpot !== null) ? liveSpot :
    (gex?.spot ?? 5800);` (live-aware engine spot — feeds buildCalendarPosition/payoffDomain/
    scenario/railGreeks unchanged, so the T+0 recompute CONTEXT asks for is automatic). Add
    `const displaySpot = (liveStatus === "live" && liveSpot !== null) ? liveSpot : (gex?.spot ??
    null);` (honest, no engine fallback). Add `spot` order note: the live gate reads `liveStatus`
    which is already destructured. Add `liveSpot`, `liveIndices`, and `displaySpot` to the
    `OverviewModel` interface and the returned object (liveStatus is already exposed). Do NOT feed
    liveSpot into any stored/gate path — engine spot drives display curves/greeks only.
    `useOverviewModel.test.ts` does not exist yet — CREATE it as a `renderHook(() =>
    useOverviewModel())` unit test, copying the `vi.mock(...)` hook-mocking block from
    `Overview.test.tsx` (usePositions/useGex/useCot/useMacro/useExits/useLiveStream/
    usePayoffDateControl) so the hook resolves without a screen render. Assert `result.current.spot`
    and `result.current.displaySpot` across the live branch (status "live" + non-round liveSpot →
    both equal liveSpot) and the fallback branch (status "quiet" → both equal gex.spot; gex
    undefined → spot 5800, displaySpot null). Use a liveSpot literal distinct from gex.spot and
    5800 (catch #20).
  </action>
  <verify>
    <automated>bun run test -- --run apps/web/src/screens/overview-mobile/useOverviewModel.test.ts</automated>
  </verify>
  <done>Engine spot is live-aware with the honest fallback; displaySpot is nullable (never 5800); liveSpot/liveIndices exposed on the model.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server SSE → browser useLiveStream | named 'spot'/'indices' frames Zod-parsed before entering state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-01 | Tampering | 'spot'/'indices' event handlers | medium | mitigate | `streamSpotEvent`/`streamIndicesEvent` `safeParse` per frame; malformed JSON or bad shape dropped, last-known-good retained — never cast into state (no any/as/!) |
| T-38-07 | Spoofing (stale-as-live) | live-aware spot seam | high | mitigate | Live value gated on `liveStatus === "live"` AND `liveSpot !== null`; a spot-only feed keeps its own freshness stamp so it never flips the greeks badge; displaySpot degrades to gex.spot/null, never a `liveSpot ?? gex.spot` silent lie (catch #26) |
| T-38-SC | Tampering | npm installs | low | accept | No new npm packages |
</threat_model>

<verification>
- `bun run test -- --run apps/web/src/hooks/useLiveStream.test.ts apps/web/src/screens/overview-mobile/useOverviewModel.test.ts` green.
- `bun run typecheck && bun run lint` clean.
- Fixture check: no test where liveSpot equals a gex.spot/DEFAULT fallback literal (catch #20 honesty).
</verification>

<success_criteria>
useLiveStream exposes liveSpot/liveIndices on their own freshness; the model's engine spot is live-aware and displaySpot is honest-nullable; both flow to the Wave-3/4 consumer plans.
</success_criteria>

<output>
Create `.planning/phases/38-live-spx-spot-across-the-app-fan-the-already-flowing-sidecar/38-04-SUMMARY.md` when done. Record the exact model field names/shapes (liveSpot, liveIndices, displaySpot) for the consumer plans.
</output>
