---
phase: 38-live-spx-spot-across-the-app-fan-the-already-flowing-sidecar
plan: 07
type: execute
wave: 5
depends_on: ["38-01", "38-02", "38-03", "38-04", "38-05", "38-06"]
files_modified:
  - docs/architecture/stack-decisions.md
autonomous: false
requirements: [LIVE-01, LIVE-02, LIVE-03, LIVE-04, LIVE-05]
must_haves:
  truths:
    - "The full TS workspace suite + the sidecar pytest suite are green, typecheck + lint clean"
    - "The new sidecar (poll) + server (fan-out) images are deployed to Railway; web is on Vercel"
    - "During RTH, the header SPX chip moves ~1/sec matching the positions LIVE cadence"
    - "The regime rail shows live VIX-family values while live and reverts to EOD on quiet/stalled"
  artifacts:
    - path: docs/architecture/stack-decisions.md
      provides: "recorded live-market-data flow + display-live/gate-EOD LAW"
      contains: "display-live"
  key_links:
    - from: apps/sidecar
      to: apps/web
      via: "sidecar poll → server fan-out → browser hook → live SPX chip + regime gauges (end-to-end RTH proof)"
      pattern: "indices|spot"
---

<objective>
Prove the whole live-market-data flow end to end and ship it (LIVE-01..05). Run both language
suites + typecheck + lint as the phase gate, record the new data flow + the display-live/gate-EOD
LAW in the architecture docs, deploy the sidecar + server to Railway and web to Vercel, then run
the live RTH human UAT: the header SPX chip ticking ~1/sec (positions-LIVE cadence) and the
regime rail showing live VIX-family values with honest stale-fallback.

Purpose: the phase's acceptance feel — "live across the whole app" — verified against the real
feed, not just green tests.
Output: green gate, docs, deployed images, and a passing live UAT.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-live-spx-spot-across-the-app-fan-the-already-flowing-sidecar/38-CONTEXT.md
@.planning/phases/38-live-spx-spot-across-the-app-fan-the-already-flowing-sidecar/38-RESEARCH.md
@docs/architecture/stack-decisions.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration gate — full suites + typecheck + lint + docs</name>
  <files>docs/architecture/stack-decisions.md</files>
  <action>
    Run the full TypeScript workspace suite (`bun run test`) AND the sidecar pytest suite
    (`cd apps/sidecar && .venv/bin/pytest`), then `bun run typecheck && bun run lint`. All must be
    green before deploy. If anything fails, fix in the owning file (do not mark the gate passed on
    a red suite). Record the new architecture in `docs/architecture/stack-decisions.md`: the live
    market-data flow (sidecar is the sole LIVE source — SPX spot fanned via the existing greeks
    pipe with zero new Schwab calls; VIX family via a ~20s `get_quotes` poll) and the
    DISPLAY-LIVE / GATE-EOD LAW (regime/entry gates, crisis bands, and hysteresis keep consuming
    stored EOD `macro_observations`; only displayed values live-tint; FRED ingestion unchanged).
    Docs-before-architecture is satisfied here because this plan gates the whole phase; keep the
    entry to the decision table's existing format.
  </action>
  <verify>
    <automated>bun run test &amp;&amp; bun run typecheck &amp;&amp; bun run lint &amp;&amp; cd apps/sidecar &amp;&amp; .venv/bin/pytest -q</automated>
  </verify>
  <done>Both suites green, typecheck + lint clean, and the live-data flow + display-live/gate-EOD LAW recorded in stack-decisions.md.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy — Railway sidecar + server, Vercel web</name>
  <files>docs/architecture/stack-decisions.md</files>
  <action>
    Deploy the new images. Railway hits ALL services on a push, but a targeted deploy is the
    reliable path (prod-deploy memory): `railway up --service sidecar` (the VIX-family poll loop)
    and `railway up --service server` (the spot/indices fan-out); web deploys via Vercel (root
    `vercel.json` monorepo deploy, morai.wtf). No new env var is required — the poll uses the
    existing seeded market client; no secret to set (contrast Phase 37's SIDECAR_ADMIN_TOKEN). If a
    `railway`/`vercel` CLI call returns an auth error, that surfaces a dynamic auth checkpoint —
    pause for the operator to authenticate, then retry (do not pre-plan the auth gate). Prove
    deploy identity by the createdAt timestamp correlation, NEVER by git sha (`railway up` reports
    commitHash null — Phase 16 Pitfall 1). Do NOT run `railway domain` against the sidecar
    (creates-on-first-use even with --service — Phase 16 trap). Record the deployed image
    timestamps in the SUMMARY.
  </action>
  <verify>
    <automated>echo "Deploy verification is the live RTH UAT in Task 3 — record deployed-image createdAt timestamps in the SUMMARY (not a git sha)."</automated>
  </verify>
  <done>sidecar + server redeployed on Railway (verified by createdAt timestamp), web live on Vercel; deployed-image timestamps recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live RTH UAT — SPX chip ~1/sec + regime gauges live + honest fallback</name>
  <action>
    Pause for the human live-verification during RTH. Present the deployed morai.wtf app (with an
    open position so the greeks stream is warm) and walk the operator through the how-to-verify
    items below (SPX chip cadence + one-number consistency, mobile hero, regime gauges live with
    the gate untouched, and the quiet/stalled honesty revert). Do not mark the phase complete until
    the operator responds with the resume signal.
  </action>
  <what-built>
    Live SPX spot fanned to every browser (on-change ~1/sec) and live VIX-family values on the
    regime rail, both with honest stale-fallback badges — deployed to prod. This must be verified
    DURING RTH (the poll/stream only carry fresh data when the market is open).
  </what-built>
  <how-to-verify>
    On morai.wtf during RTH (with an open position so the greeks stream is warm):
      1. Header SPX chip: the value moves ~1/sec, at the SAME cadence as the positions LIVE feed,
         and matches the payoff hero's spot marker + the GEX-rail Spot marker (one SPX number, not
         two). The chip shows its live tint.
      2. Mobile hero (narrow viewport): the SPX number is live-tinted and moving; VIX stays EOD.
      3. Regime rail (desktop): VIX/VVIX/VIX9D-VIX gauges show live values (not "as of 2026-07-…");
         the entry-gate verdict chip is UNCHANGED (still EOD-driven); hy-oas is unchanged (FRED).
      4. Honesty check: when the stream goes quiet/stalled (or outside RTH), the SPX chip + regime
         gauges revert to the stored EOD/snapshot value with the stale styling and the "EOD · as
         of …" footer — never a stale value shown as live.
    Report pass/fail per item. If a gauge stays stale during RTH, note which symbol (likely the A1
    ticker-string check from 38-02).
  </how-to-verify>
  <resume-signal>Type "approved" or describe the item(s) that failed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deployed prod → live feed | end-to-end path exercised against the real Schwab feed during RTH |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-11 | Spoofing (stale-as-live in prod) | live SPX chip + regime gauges | high | mitigate | The RTH UAT explicitly checks the quiet/stalled revert (item 4) — a stale-as-live regression fails the human checkpoint before phase close |
| T-38-12 | Denial of Service (deploy regression) | mid-RTH worker/server deploy | medium | accept | Known ops pattern: a mid-RTH server deploy briefly interrupts the stream; pg-boss/stream self-heal on reconnect (documented, not introduced here) |
| T-38-SC | Tampering | package installs at deploy | low | accept | No new packages in any ecosystem this phase — nothing new to audit at deploy |
</threat_model>

<verification>
- `bun run test` + `cd apps/sidecar && .venv/bin/pytest` both green; `bun run typecheck && bun run lint` clean.
- Railway sidecar + server deployed (createdAt timestamp correlation); Vercel web live.
- Live RTH human UAT: all four items pass (SPX chip ~1/sec, mobile hero live, regime gauges live, honest quiet/stalled revert).
</verification>

<success_criteria>
Both suites green + clean; new images deployed; the app is verifiably "live across the whole app" during RTH with honest stale-fallback and an untouched EOD gate.
</success_criteria>

<output>
Create `.planning/phases/38-live-spx-spot-across-the-app-fan-the-already-flowing-sidecar/38-07-SUMMARY.md` when done. Record deployed-image createdAt timestamps + the live RTH UAT dispositions.
</output>
