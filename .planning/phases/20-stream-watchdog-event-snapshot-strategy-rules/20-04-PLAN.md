---
phase: 20-stream-watchdog-event-snapshot-strategy-rules
plan: 04
type: tdd
wave: 3
depends_on: ["20-02", "20-03"]
files_modified:
  - packages/core/src/streaming/domain/spot-move-detector.ts
  - packages/core/src/streaming/domain/spot-move-detector.test.ts
  - packages/core/src/journal/domain/snapshot-cooldown.ts
  - packages/core/src/journal/domain/snapshot-cooldown.test.ts
  - packages/core/src/journal/application/ports.ts
  - packages/core/src/journal/application/snapshotCalendars.ts
  - packages/core/src/journal/application/snapshotCalendars.test.ts
autonomous: true
requirements: [SNAP-01]
must_haves:
  truths:
    - "detectLargeMove prunes samples older than windowMs, appends the new sample, and returns triggered=true iff |newPrice-oldestInWindow|/oldestInWindow >= thresholdPct."
    - "detectLargeMove returns triggered=false when the pruned window is empty (cold start)."
    - "isWithinCooldown(now,null,ms) is false; isWithinCooldown is true iff now-last < cooldownMs (boundary: ==cooldownMs is NOT within cooldown)."
    - "ForReadingLatestSnapshotTime port type exists returning Result<Date|null, StorageError>."
    - "snapshotCalendars use-case stamps each row with a trigger provenance value, defaulting to 'scheduled' when none supplied."
  artifacts:
    - packages/core/src/streaming/domain/spot-move-detector.ts (detectLargeMove + SpotSample)
    - packages/core/src/journal/domain/snapshot-cooldown.ts (isWithinCooldown)
    - packages/core/src/journal/application/ports.ts (ForReadingLatestSnapshotTime + SnapshotRow.trigger)
    - packages/core/src/journal/application/snapshotCalendars.ts (trigger passthrough)
  key_links:
    - "SnapshotRow.trigger is read by the Postgres repo mapping (20-05) and set by the worker handler payload (20-06)."
    - "detectLargeMove + isWithinCooldown are the pure logic the server detector (20-06) composes."
  prohibitions:
    - "detectLargeMove and isWithinCooldown MUST NOT call Date.now() — caller passes nowMs / now."
    - "These core modules MUST import only @morai/shared — no drizzle, pg-boss, hono, process.env (architecture-boundaries #2)."
    - "The trigger column MUST be additive/nullable-friendly — default 'scheduled' at the application layer, not a destructive change to existing rows (D-12)."
    - "MUST NOT replace the composite-PK idempotency of persistSnapshot — trigger is an extra field, not a new key."
---

<objective>
SNAP-01 core: the two pure detectors plus the core-side provenance + read-port plumbing. All
framework-free, test-first. `detectLargeMove` (rolling-window % move, Pattern 2), `isWithinCooldown`
(cross-process cooldown check), the `ForReadingLatestSnapshotTime` driven-port type, the additive
`SnapshotRow.trigger` provenance field (D-12), and the `snapshotCalendars` use-case passing a
trigger through to each row.

Purpose: Keep every fallible/threshold decision in pure core so it is property-testable and
process-agnostic (the cooldown must be correct across the apps/server detector and the apps/worker
cron chain — see Pitfall 2). Adapters (20-05) and wiring (20-06) build on these.
Output: two pure domain functions, one new port type, and a trigger-aware snapshot use-case.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-RESEARCH.md
@.planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-PATTERNS.md
@packages/core/src/journal/domain/rth-window.ts
@packages/core/src/journal/domain/dedupe-key.ts
@packages/core/src/journal/application/ports.ts
@packages/core/src/journal/application/snapshotCalendars.ts
</context>

## Artifacts this plan produces

| Symbol | File | Kind |
|--------|------|------|
| `detectLargeMove` + `SpotSample` | `packages/core/src/streaming/domain/spot-move-detector.ts` | pure detector |
| `isWithinCooldown` | `packages/core/src/journal/domain/snapshot-cooldown.ts` | pure predicate |
| `ForReadingLatestSnapshotTime` | `packages/core/src/journal/application/ports.ts` | driven port type |
| `SnapshotRow.trigger` | `packages/core/src/journal/application/ports.ts` | additive field |
| trigger passthrough | `packages/core/src/journal/application/snapshotCalendars.ts` | use-case change |

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: detectLargeMove rolling-window detector (D-05, Pattern 2)</name>
  <files>packages/core/src/streaming/domain/spot-move-detector.ts, packages/core/src/streaming/domain/spot-move-detector.test.ts</files>
  <read_first>
    - .planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-RESEARCH.md § Pattern 2 (the exact detectLargeMove body + tunable justification) and Assumption A3 / Open Question 1 (oldest-in-window comparison).
    - .planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-PATTERNS.md § spot-move-detector.ts.
    - packages/core/src/journal/domain/rth-window.ts — purity idiom analog.
  </read_first>
  <behavior>
    - `SpotSample = { readonly ts: number; readonly price: number }`; signature `(window, newSample, windowMs, thresholdPct) => { triggered, nextWindow }`.
    - Prunes samples where `newSample.ts - s.ts > windowMs`; nextWindow = pruned + newSample.
    - Empty pruned window (cold start) ⇒ triggered=false.
    - pctMove = |newSample.price - oldest.price| / oldest.price; triggered iff pctMove >= thresholdPct (boundary exactly == thresholdPct triggers).
    - Property (fast-check): every sample in nextWindow satisfies newSample.ts - s.ts <= windowMs (pruning invariant); a move of exactly thresholdPct triggers; symmetric — a downward move of the same magnitude triggers identically (either direction, D-15).
  </behavior>
  <action>
    Create `detectLargeMove` and `SpotSample` exactly per RESEARCH Pattern 2 — pure, no Date.now(),
    no I/O, returns a plain object (no Result needed — cannot fail). Export named tunable constants
    `MOVE_WINDOW_MS = 5 * 60_000` and `MOVE_THRESHOLD_PCT = 0.01` with a one-line comment each citing
    D-05 (index-level 1% over ~5min catches fast spikes, excludes slow drift). Write RED tests first
    (example: 1% move triggers, 0.9% does not, cold start does not; fast-check: pruning invariant,
    threshold boundary, direction symmetry), run, confirm failure, implement.
  </action>
  <verify>
    <automated>bun run test -- packages/core/src/streaming/domain/spot-move-detector.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Threshold-boundary example + pruning-invariant + direction-symmetry properties pass; cold-start
    returns triggered=false; module imports nothing framework-bound.
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: isWithinCooldown pure predicate (D-06, Pitfall 2)</name>
  <files>packages/core/src/journal/domain/snapshot-cooldown.ts, packages/core/src/journal/domain/snapshot-cooldown.test.ts</files>
  <read_first>
    - .planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-RESEARCH.md § Pattern 2 cooldown subsection + Pitfall 2.
    - .planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-PATTERNS.md § snapshot-cooldown.ts (analog dedupe-key.ts).
  </read_first>
  <behavior>
    - `isWithinCooldown(now: Date, lastSnapshotAt: Date | null, cooldownMs: number): boolean`.
    - lastSnapshotAt===null ⇒ false (no prior snapshot ⇒ never suppressed).
    - now - last < cooldownMs ⇒ true; == cooldownMs ⇒ false; > cooldownMs ⇒ false.
    - Property (fast-check): for a fixed last+cooldownMs, monotonic in now — once false (elapsed >= cooldown) it never returns true as now increases.
  </behavior>
  <action>
    Create `isWithinCooldown` exactly per RESEARCH. Export a named constant `SNAPSHOT_COOLDOWN_MS =
    15 * 60_000` with a one-line comment (D-06: at most one supplemental between 30-min scheduled
    runs). Pure, Date passed in. Write RED tests first (null-last ⇒ false, boundary == cooldown ⇒
    false, within ⇒ true; fast-check monotonic-in-now), run, confirm failure, implement.
  </action>
  <verify>
    <automated>bun run test -- packages/core/src/journal/domain/snapshot-cooldown.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Null-last false, boundary-not-within, within-true asserted; monotonic-in-now property passes.
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: ForReadingLatestSnapshotTime port + SnapshotRow.trigger + use-case passthrough (D-12)</name>
  <files>packages/core/src/journal/application/ports.ts, packages/core/src/journal/application/snapshotCalendars.ts, packages/core/src/journal/application/snapshotCalendars.test.ts</files>
  <read_first>
    - packages/core/src/journal/application/ports.ts — the `SnapshotRow` type (around the `source: "cboe" | "schwab_chain"` field), the `ForPersistingSnapshot`/`ForReadingJournal` port style, and `ForRunningSnapshotCalendars`.
    - packages/core/src/journal/application/snapshotCalendars.ts — `makeSnapshotCalendarsUseCase`, the `buildSnapshotRow(now, calendar, front, back)` call site (~line 165), and its returned `() => Promise<Result<void, StorageError>>` shape.
    - .planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-RESEARCH.md § Pattern 2 (port signature) + § State of the Art (trigger provenance).
  </read_first>
  <behavior>
    - `ForReadingLatestSnapshotTime = () => Promise<Result<Date | null, StorageError>>` exported in ports.ts.
    - `SnapshotRow` gains `readonly trigger: "scheduled" | "event-move"`.
    - `ForRunningSnapshotCalendars` accepts an optional arg `{ trigger?: "scheduled" | "event-move" }`; the use-case defaults to "scheduled" when omitted and stamps every built row with the resolved trigger.
    - Existing snapshotCalendars behavior (iterate open calendars, null-leg policy, composite-PK idempotency) is otherwise unchanged.
  </behavior>
  <action>
    Add the `ForReadingLatestSnapshotTime` port type (JSDoc: "latest calendar_snapshots time =
    MAX(time); null on cold start; SNAP-01 cooldown ground truth, internal driven port — no
    HTTP/MCP surface"). Add `trigger` to `SnapshotRow`. Extend `buildSnapshotRow` (and its callers)
    to accept a trigger and set it on the row; extend `ForRunningSnapshotCalendars` +
    `makeSnapshotCalendarsUseCase` to accept an optional `{ trigger }` defaulting to "scheduled".
    Keep `assertDefined`/Result discipline — no `!`/`as`. Extend snapshotCalendars.test.ts RED-first:
    default trigger is "scheduled"; passing `{ trigger: "event-move" }` stamps rows accordingly;
    existing snapshot assertions still pass.
  </action>
  <verify>
    <automated>bun run test -- packages/core/src/journal/application/snapshotCalendars.test.ts</automated>
  </verify>
  <acceptance_criteria>
    ForReadingLatestSnapshotTime exported; SnapshotRow.trigger present; use-case defaults to
    "scheduled" and honors an "event-move" arg on every row; prior snapshotCalendars tests green;
    typecheck clean (no non-null assertions).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure domain + port types; no external input crosses here (adapters own the boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-06 | Denial of Service | move-detection false-fire | medium | mitigate | Cooldown ground-truth read via ForReadingLatestSnapshotTime (Pitfall 2) + RTH gate composed in 20-06; pure detector is O(1), no unbounded growth |
| T-20-07 | Tampering | trigger provenance | low | mitigate | trigger is a closed 2-value string set application-side; additive, never mutates existing rows (D-12) |
| T-20-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase (RESEARCH § Package Legitimacy Audit — N/A) |
</threat_model>

<verification>
- `bun run test -- packages/core/src/streaming/domain/spot-move-detector.test.ts` green.
- `bun run test -- packages/core/src/journal/domain/snapshot-cooldown.test.ts` green.
- `bun run test -- packages/core/src/journal/application/snapshotCalendars.test.ts` green.
- `bun run typecheck && bun run lint` clean for packages/core (hexagon purity holds).
</verification>

<success_criteria>
Two pure, property-tested detectors; a new read-port type; and an additive trigger-aware snapshot
use-case — all inside packages/core importing only @morai/shared.
</success_criteria>

<output>
Create `.planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-04-SUMMARY.md` when done.
</output>
