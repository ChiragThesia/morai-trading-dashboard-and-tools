---
phase: 20-stream-watchdog-event-snapshot-strategy-rules
plan: 06
type: execute
wave: 5
depends_on: ["20-05"]
files_modified:
  - apps/server/src/adapters/http/sidecar-sse.ts
  - apps/server/src/adapters/http/sidecar-sse.test.ts
  - apps/server/src/main.ts
  - apps/worker/src/handlers/snapshot-calendars.ts
  - apps/worker/src/handlers/snapshot-calendars.test.ts
autonomous: true
requirements: [SNAP-01]
must_haves:
  truths:
    - "dispatchFrame calls deps.observeSpot(underlyingPrice, ts) after a successful Zod parse whenever underlyingPrice is non-null and > 0, independent of recomputeLiveGreek success."
    - "main.ts wires onSpotObserved: RTH+holiday gate → detectLargeMove over a module-level moveWindow → on trigger, read ForReadingLatestSnapshotTime → isWithinCooldown check → jobBoss.send('snapshot-calendars', {trigger:'event-move'}, {singletonKey:'event-move'})."
    - "A DB read error in the cooldown check skips firing (fail-safe) and never crashes the tick path."
    - "The worker snapshot-calendars handler Zod-parses an optional trigger job-payload field and passes it to the use-case; empty payload defaults to 'scheduled'."
  artifacts:
    - apps/server/src/adapters/http/sidecar-sse.ts (observeSpot hook + ConnectToSidecarStreamDeps field)
    - apps/server/src/main.ts (onSpotObserved wiring + module-level moveWindow state)
    - apps/worker/src/handlers/snapshot-calendars.ts (optional trigger payload passthrough)
  key_links:
    - "observeSpot feeds detectLargeMove (20-04); the cooldown read uses the Postgres ForReadingLatestSnapshotTime (20-05); the enqueue reuses main.ts's existing jobBoss client (main.ts ~line 216)."
    - "The event-move enqueue carries {trigger:'event-move'} → the worker stamps the row → provenance column (20-05) records it."
  prohibitions:
    - "MUST NOT create a new PgBoss client or an HTTP hop to the worker — reuse the existing enqueue-only jobBoss (main.ts ~216)."
    - "MUST NOT use an in-memory lastFiredAt as the sole cooldown guard — the DB MAX(time) read is the ground truth (Pitfall 2)."
    - "observeSpot MUST fire even when recomputeLiveGreek fails (a bad option contract does not mean the spot is bad)."
    - "The onSpotObserved path MUST NOT throw into the tick loop — enqueue is fire-and-forget with .catch (Pitfall: never crash the relay)."
    - "MUST NOT remove or bypass the worker handler's existing RTH+holiday gate (D-15)."
---

<objective>
SNAP-01 wiring: connect the already-arriving SPX spot tick to the ad-hoc snapshot enqueue. Add the
`observeSpot` hook in `sidecar-sse.ts`'s `dispatchFrame`, wire `onSpotObserved` in `main.ts`
(RTH gate → `detectLargeMove` → DB cooldown → `jobBoss.send` with `trigger:'event-move'`), and teach
the worker handler to accept and persist an optional `trigger` payload field.

Purpose: Detection runs server-side / headless (D-04) — a journaling feature must not depend on a
browser tab being open. The SPX spot already arrives on every option tick at
`RawOptionTick.underlyingPrice` and is currently discarded — this is the only place it exists without
a new data source (Pattern 2). The enqueue reuses the server's existing idle `jobBoss` client; the
existing RTH+holiday-gated worker job does the actual capture unchanged.
Output: server-side headless detector firing supplemental snapshots, cooldown-correct across
processes, provenance-stamped.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-RESEARCH.md
@.planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-PATTERNS.md
@apps/server/src/adapters/http/sidecar-sse.ts
@apps/server/src/main.ts
@apps/worker/src/handlers/snapshot-calendars.ts
</context>

## Artifacts this plan produces

| Symbol | File | Kind |
|--------|------|------|
| `observeSpot` hook + `ConnectToSidecarStreamDeps.observeSpot` | `apps/server/src/adapters/http/sidecar-sse.ts` | ingest hook |
| `onSpotObserved` wiring + `moveWindow` state | `apps/server/src/main.ts` | composition root |
| optional `trigger` payload passthrough | `apps/worker/src/handlers/snapshot-calendars.ts` | job handler |

<tasks>

<task type="execute" tdd="true">
  <name>Task 1: observeSpot hook in dispatchFrame (Pattern 2)</name>
  <files>apps/server/src/adapters/http/sidecar-sse.ts, apps/server/src/adapters/http/sidecar-sse.test.ts</files>
  <read_first>
    - apps/server/src/adapters/http/sidecar-sse.ts — `ConnectToSidecarStreamDeps`, the `sidecarTickSchema.safeParse` success branch inside `dispatchFrame`, and the existing `recomputeLiveGreek` call site; `RawOptionTick.underlyingPrice` + `.ts` fields.
    - .planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-PATTERNS.md § sidecar-sse.ts.
  </read_first>
  <behavior>
    - Add optional `readonly observeSpot?: (spot: number, ts: string) => void` to ConnectToSidecarStreamDeps.
    - After a successful tick parse, if `underlyingPrice !== null && underlyingPrice > 0`, call `deps.observeSpot?.(underlyingPrice, ts)` — alongside, not replacing, recomputeLiveGreek.
    - observeSpot fires even when recomputeLiveGreek fails/returns an error (independent of recompute outcome).
  </behavior>
  <action>
    Extend `ConnectToSidecarStreamDeps` with the optional `observeSpot` field (JSDoc per RESEARCH:
    fires on every valid underlying tick, independent of recompute success). Add the guarded call in
    dispatchFrame right after the Zod-parse success branch. Extend sidecar-sse.test.ts RED-first:
    a valid tick with underlyingPrice>0 invokes observeSpot with (price, ts); a tick whose recompute
    fails still invokes observeSpot; a null/<=0 underlyingPrice does not.
  </action>
  <verify>
    <automated>bun run test -- apps/server/src/adapters/http/sidecar-sse.test.ts</automated>
  </verify>
  <acceptance_criteria>
    observeSpot invoked on valid ticks incl. recompute-failure ticks; skipped on null/<=0 price;
    recomputeLiveGreek behavior unchanged.
  </acceptance_criteria>
</task>

<task type="execute" tdd="true">
  <name>Task 2: Worker handler accepts optional trigger payload (D-12/D-15)</name>
  <files>apps/worker/src/handlers/snapshot-calendars.ts, apps/worker/src/handlers/snapshot-calendars.test.ts</files>
  <read_first>
    - apps/worker/src/handlers/snapshot-calendars.ts — the thin-adapter shape: array-guard → RTH+holiday self-check (`isWithinRth`/`isNyseHoliday`) → call use-case → map Result → fire-and-forget `boss.send("compute-analytics", …, {singletonKey})`.
    - .planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-PATTERNS.md § snapshot-calendars.ts (job handler).
  </read_first>
  <behavior>
    - The handler Zod-parses the job payload for an optional `trigger` field ("scheduled" | "event-move"); parse failure/absent ⇒ defaults to "scheduled".
    - The resolved trigger is passed into the snapshotCalendars use-case call `{ trigger }`.
    - The existing RTH+holiday gate and the compute-analytics chain enqueue are unchanged (D-15).
  </behavior>
  <action>
    Add a small Zod schema for the optional job payload (`{ trigger?: enum(['scheduled','event-move']) }`),
    parse-don't-cast; default to "scheduled". Pass `{ trigger }` to `snapshotCalendarsUseCase(...)`.
    Do NOT touch the RTH+holiday gate or the compute-analytics fan-out. Extend the handler test
    RED-first: an `{trigger:'event-move'}` payload reaches the use-case as event-move; an empty
    payload defaults to scheduled; off-hours still no-ops.
  </action>
  <verify>
    <automated>bun run test -- apps/worker/src/handlers/snapshot-calendars.test.ts</automated>
  </verify>
  <acceptance_criteria>
    event-move payload propagates to the use-case; empty defaults to scheduled; RTH gate + analytics
    chain unchanged.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: main.ts wire onSpotObserved → detect → cooldown → jobBoss.send</name>
  <files>apps/server/src/main.ts</files>
  <read_first>
    - apps/server/src/main.ts — the `jobBoss` enqueue-only client (~line 216), the `connectToSidecarStream(config.SIDECAR_URL, {...})` wiring (~line 335), and how `isWithinRth`/`isNyseHoliday` (already available via @morai/core) are used elsewhere.
    - .planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-RESEARCH.md § Pattern 2 (the onSpotObserved reference implementation) + Pitfall 2.
    - .planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-PATTERNS.md § main.ts + § Fire-and-forget pg-boss enqueue.
  </read_first>
  <action>
    Compose (composition-root wiring — the pure logic already lives in 20-04): declare a module-level
    mutable `moveWindow: ReadonlyArray<SpotSample> = []` (same pattern as stream-fan-out.ts's tick
    buffer). Build a `readLatestSnapshotTime` from the Postgres ForReadingLatestSnapshotTime impl
    (20-05). Define `onSpotObserved(spot, tsIso)`: parse `const now = new Date(tsIso)`; RTH+holiday
    gate (`isWithinRth(now) && !isNyseHoliday(now)`, else return — D-15); call `detectLargeMove(moveWindow,
    {ts: now.getTime(), price: spot}, MOVE_WINDOW_MS, MOVE_THRESHOLD_PCT)`; assign `moveWindow =
    nextWindow`; if not triggered return; read latest snapshot time (on err return — fail-safe skip,
    never crash the tick path); if `isWithinCooldown(now, lastValue, SNAPSHOT_COOLDOWN_MS)` return;
    else `void jobBoss.send("snapshot-calendars", { trigger: "event-move" }, { singletonKey:
    "event-move" }).catch((e: unknown) => console.warn("snapshot-calendars: event-move enqueue
    failed", e))`. Pass `observeSpot: onSpotObserved` into the existing `connectToSidecarStream`
    deps. Reuse the existing jobBoss — do NOT create a new client. (This task is composition-root
    wiring; behavior is covered by the pure-fn tests in 20-04 and the handler test in Task 2 — no new
    unit test required for the glue, per tdd.md composition-root exemption.)
  </action>
  <verify>
    <automated>bun run typecheck && rg -n "onSpotObserved|moveWindow|event-move|isWithinCooldown|detectLargeMove" apps/server/src/main.ts</automated>
  </verify>
  <acceptance_criteria>
    main.ts wires observeSpot→onSpotObserved with RTH gate + detectLargeMove + DB cooldown +
    fire-and-forget jobBoss.send({trigger:'event-move'}); reuses the existing jobBoss; typecheck
    clean; no new PgBoss client.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sidecar→server (tick ingest) | Untrusted sidecar frames parsed before observeSpot |
| server→queue (ad-hoc enqueue) | Server enqueues a snapshot job into pg-boss |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-10 | Denial of Service | snapshot-enqueue flood | high | mitigate | RTH gate (D-15) + DB MAX(time) cooldown (D-06) make off-hours/within-cooldown fires no-ops by construction; singletonKey guards same-process double-fire (RESEARCH § Security Domain) |
| T-20-06 | Denial of Service | detector crashing tick relay | medium | mitigate | Cooldown read error ⇒ skip firing (fail-safe); enqueue is fire-and-forget with .catch — never throws into dispatchFrame |
| T-20-07 | Tampering | provenance stamp | low | mitigate | trigger:'event-move' set at enqueue + parsed by handler; closed enum |
| T-20-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase (RESEARCH § Package Legitimacy Audit — N/A) |
</threat_model>

<verification>
- `bun run test -- apps/server/src/adapters/http/sidecar-sse.test.ts` green.
- `bun run test -- apps/worker/src/handlers/snapshot-calendars.test.ts` green.
- `bun run typecheck && bun run lint` clean for apps/server + apps/worker.
- Manual (SNAP-01 ship UAT): during RTH, an injected/real >1% SPX move enqueues exactly one
  supplemental snapshot; a second move within 15min is cooldown-suppressed; off-hours no-op.
</verification>

<success_criteria>
A large SPX move on the live stream triggers exactly one supplemental, provenance-stamped journal
snapshot via the existing job, cooldown-correct across processes, without a new dependency or a
duplicated 30-min cadence.
</success_criteria>

<output>
Create `.planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-06-SUMMARY.md` when done.
This SUMMARY marks the SNAP-01 feature complete and ready for its own deploy + UAT cycle (D-18).
</output>
