---
phase: 20-stream-watchdog-event-snapshot-strategy-rules
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - apps/server/src/adapters/http/stream.routes.ts
  - apps/server/src/adapters/http/stream.routes.test.ts
  - docs/architecture/streaming-fanout.md
autonomous: true
requirements: [WATCH-01]
must_haves:
  truths:
    - "Both GET /api/stream ping-emit call sites write event:'ping' with data = the JSON of streamPingEvent.parse({ isRth })."
    - "isRth on the ping equals isWithinRth(now) && !isNyseHoliday(now) — the SAME predicate the snapshot job's gate uses, never a second RTH implementation."
    - "The 30s ping interval (stream.sleep) is unchanged (Pitfall 5)."
  artifacts:
    - apps/server/src/adapters/http/stream.routes.ts (ping payload on both duplicated handlers)
    - docs/architecture/streaming-fanout.md (ping-now-carries-isRth note)
  key_links:
    - "streamPingEvent (from 20-01) is imported from @morai/contracts and parsed before writeSSE — the server emits the exact shape the client (20-03) parses."
    - "isWithinRth + isNyseHoliday imported from @morai/core — same import path apps/worker/src/handlers/snapshot-calendars.ts already uses."
  prohibitions:
    - "MUST NOT change the stream.sleep(30_000) ping interval (Pitfall 5 — ping cadence and stall threshold are orthogonal)."
    - "MUST NOT write a second weekday/hour RTH check (Don't-Hand-Roll #1) — reuse @morai/core exports."
    - "MUST NOT update only one of the two duplicated ping-emit sites — both are kept in sync per the file's own comment."
---

<objective>
WATCH-01 server side: make the SSE heartbeat carry the server-authoritative RTH truth (D-03).
Replace the empty `data: ""` on BOTH duplicated `event:"ping"` writes in stream.routes.ts with the
JSON of `streamPingEvent.parse({ isRth: isWithinRth(now) && !isNyseHoliday(now) })`.

Purpose: The client can no longer need a local RTH clock — the server that already knows market
state pushes it in. Reusing @morai/core's `isWithinRth`/`isNyseHoliday` guarantees the badge's
"market open" truth and the snapshot job's "market open" truth can never disagree.
Output: heartbeat payload additively upgraded; streaming-fanout doc note.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-RESEARCH.md
@.planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-PATTERNS.md
@apps/server/src/adapters/http/stream.routes.ts
@apps/server/src/adapters/http/stream.routes.test.ts
@apps/worker/src/handlers/snapshot-calendars.ts
</context>

## Artifacts this plan produces

| Symbol | File | Kind |
|--------|------|------|
| ping SSE payload `{isRth}` (both handlers) | `apps/server/src/adapters/http/stream.routes.ts` | modified emitter |
| "ping carries isRth" note | `docs/architecture/streaming-fanout.md` | doc update |

<tasks>

<task type="execute">
  <name>Task 1: Emit isRth on both ping call sites (D-03)</name>
  <files>apps/server/src/adapters/http/stream.routes.ts, apps/server/src/adapters/http/stream.routes.test.ts</files>
  <read_first>
    - apps/server/src/adapters/http/stream.routes.ts — locate BOTH `writeSSE({ event: "ping", data: "" })` sites (the JWT-inside handler and makeStreamSseRouter's outside-JWT handler; the file's own comment says they are "kept in sync manually"). Note the existing `stream.sleep(30_000)` loop.
    - .planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-PATTERNS.md § stream.routes.ts (exact replacement snippet) and § RESEARCH Pitfall 5.
    - apps/worker/src/handlers/snapshot-calendars.ts — confirm the `import { isWithinRth, isNyseHoliday } from "@morai/core"` path to reuse.
  </read_first>
  <action>
    Import `streamPingEvent` from @morai/contracts and `isWithinRth`, `isNyseHoliday` from
    @morai/core. In BOTH ping-emit sites, compute `const now = new Date()` and replace `data: ""`
    with `data: JSON.stringify(streamPingEvent.parse({ isRth: isWithinRth(now) && !isNyseHoliday(now) }))`.
    Leave the `stream.sleep(30_000)` interval and every other line of both handlers unchanged
    (Pitfall 5). Do not add a second RTH check. Extend stream.routes.test.ts: assert an emitted
    ping frame's data parses via streamPingEvent and that isRth reflects a mocked/injected clock
    (open-market clock ⇒ true; weekend/holiday clock ⇒ false) — follow the existing test's SSE-frame
    capture pattern; write the assertion RED-first where practical.
  </action>
  <verify>
    <automated>bun run test -- apps/server/src/adapters/http/stream.routes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Both ping sites emit streamPingEvent JSON; isRth true under an open-market clock and false under
    a weekend/holiday clock; ping interval still 30s; existing stream.routes tests green.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Document the heartbeat contract change (docs-before-architecture)</name>
  <files>docs/architecture/streaming-fanout.md</files>
  <read_first>
    - docs/architecture/streaming-fanout.md — find the section describing the SSE event set (ticks/reconcile/ping) to amend.
    - .claude/rules/workflow.md § Docs Before Code, and .claude/rules/docs.md (Hemingway style, no line-number refs).
  </read_first>
  <action>
    Add a short note (Hemingway style, 2-4 sentences) to the streaming fan-out doc recording that
    the `ping` heartbeat now carries `{ isRth: boolean }` (schema `streamPingEvent` in
    @morai/contracts), server-computed via `isWithinRth`/`isNyseHoliday`, consumed client-side to
    drive the 3-state stream badge (WATCH-01, D-03). Note it is additive — prior consumers ignored
    the empty ping and are unaffected. Do not restate the schema; reference it by name.
  </action>
  <verify>
    <automated>rg -n "streamPingEvent|isRth" docs/architecture/streaming-fanout.md</automated>
  </verify>
  <acceptance_criteria>
    streaming-fanout.md references the additive isRth ping payload by schema name; no line-number
    references introduced.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server→client (SSE) | Ping frame now carries a server-computed boolean |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-03 | Tampering | RTH computation drift | medium | mitigate | Reuse @morai/core isWithinRth/isNyseHoliday (single source) — no second RTH check (Don't-Hand-Roll #1) |
| T-20-01 | Information Disclosure | ping payload | low | mitigate | Emit only isRth via streamPingEvent.parse; no other state batched |
| T-20-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase (RESEARCH § Package Legitimacy Audit — N/A) |
</threat_model>

<verification>
- `bun run test -- apps/server/src/adapters/http/stream.routes.test.ts` green.
- `bun run typecheck && bun run lint` clean for apps/server.
- Manual (UAT, deferred to WATCH-01 ship): browser badge reflects true RTH state off-hours.
</verification>

<success_criteria>
Both ping handlers emit `{isRth}` computed from the shared core predicate; ping interval unchanged;
doc updated; no regression in stream.routes tests.
</success_criteria>

<output>
Create `.planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-02-SUMMARY.md` when done.
</output>
