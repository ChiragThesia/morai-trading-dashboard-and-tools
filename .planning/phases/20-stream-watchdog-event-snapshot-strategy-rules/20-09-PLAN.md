---
phase: 20-stream-watchdog-event-snapshot-strategy-rules
plan: 09
type: execute
wave: 7
depends_on: ["20-07", "20-08"]
files_modified:
  - packages/core/src/journal/application/ports.ts
  - packages/core/src/journal/application/getCalendarEventsWithRules.ts
  - packages/core/src/journal/application/getCalendarEventsWithRules.test.ts
  - packages/core/src/journal/application/setRuleTags.ts
  - packages/core/src/journal/application/setRuleTags.test.ts
autonomous: true
requirements: [RULE-01]
must_haves:
  truths:
    - "ForReadingAnnotations (read-one + read-many-by-hash) and ForWritingAnnotations (upsert) driven-port types exist in ports.ts."
    - "getCalendarEventsWithRules reads a calendar's events (existing ForReadingCalendarEvents) + their annotations, joins on fillIdsHash, and returns each event with its tags + otherNote (empty when unannotated)."
    - "An annotation whose fillIdsHash no longer matches any current event is logged and orphaned (not returned, not deleted) — D-09."
    - "setRuleTags validates the tags against the event's type enum (via the resolver) and the OTHER-requires-note rule before upserting; invalid input returns a validation-error Result, never persists."
  artifacts:
    - packages/core/src/journal/application/ports.ts (ForReadingAnnotations + ForWritingAnnotations)
    - packages/core/src/journal/application/getCalendarEventsWithRules.ts (read use-case)
    - packages/core/src/journal/application/setRuleTags.ts (write use-case)
  key_links:
    - "The read use-case consumes the EXISTING-but-unwired ForReadingCalendarEvents (RESEARCH: no read surface exists today) + the new ForReadingAnnotations (20-08)."
    - "The write use-case validates against the rule-tags resolver (20-07) keyed by the event's CalendarEventType."
  prohibitions:
    - "MUST NOT evaluate rules or infer which rule 'should' fire — the write use-case only records the tags the caller supplied (recording layer, not a DSL)."
    - "setRuleTags MUST reject an OTHER without a note and cross-type tags at the use-case boundary (defense-in-depth beyond the contract refine, D-21/D-07)."
    - "A hash-orphaned annotation MUST NOT be deleted — log and orphan (D-09)."
    - "These use-cases MUST return Result<T,E>; no throw-for-control-flow in core."
---

<objective>
RULE-01 application layer: the read use-case that finally exposes calendar_events joined with their
rule annotations (RESEARCH's key scope finding — NO read surface for calendar_events exists in this
app today), and the write use-case that validates + upserts rule tags. Plus the two new
driven-port types.

Purpose: The Journal UI cannot currently know whether a trade has a CLOSE event or how many ROLL
events it has — this read path is new work, not an assumed one-liner (Pitfall 4). The write use-case
enforces the recording-layer discipline (validate the supplied tags against the event type + the
OTHER-note rule; never infer/evaluate).
Output: ForReadingAnnotations / ForWritingAnnotations ports; getCalendarEventsWithRules + setRuleTags
use-cases.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-RESEARCH.md
@.planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-PATTERNS.md
@packages/core/src/journal/application/ports.ts
@packages/core/src/journal/application/snapshotCalendars.ts
@packages/core/src/journal/domain/calendar-event.ts
@packages/core/src/journal/domain/rule-tags.ts
</context>

## Artifacts this plan produces

| Symbol | File | Kind |
|--------|------|------|
| `ForReadingAnnotations` / `ForWritingAnnotations` | `packages/core/src/journal/application/ports.ts` | driven-port types |
| `makeGetCalendarEventsWithRulesUseCase` | `packages/core/src/journal/application/getCalendarEventsWithRules.ts` | read use-case |
| `makeSetRuleTagsUseCase` | `packages/core/src/journal/application/setRuleTags.ts` | write use-case |

<tasks>

<task type="execute" tdd="true">
  <name>Task 1: Annotation ports + getCalendarEventsWithRules read use-case (D-09/D-10)</name>
  <files>packages/core/src/journal/application/ports.ts, packages/core/src/journal/application/getCalendarEventsWithRules.ts, packages/core/src/journal/application/getCalendarEventsWithRules.test.ts</files>
  <read_first>
    - packages/core/src/journal/application/ports.ts — the existing ForReadingCalendarEvents type (already present, currently unwired) + the ForVerbingNoun port idiom.
    - packages/core/src/journal/application/snapshotCalendars.ts — the thin-use-case-over-ports, Result-returning factory pattern to mirror.
    - packages/core/src/journal/domain/calendar-event.ts — CalendarEvent fields (fillIdsHash, eventType, eventedAt) used in the join.
    - .planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-RESEARCH.md § Pattern 3 (missing read surface, log-and-orphan) + § Architectural Responsibility Map.
  </read_first>
  <behavior>
    - Add ForReadingAnnotations (read-one by hash, read-many by hash set) + ForWritingAnnotations (upsert) port types.
    - getCalendarEventsWithRules(calendarId): reads events (ForReadingCalendarEvents) + their annotations (ForReadingAnnotations read-many over the events' hashes); returns each event paired with tags (default []) + otherNote (default null).
    - Events with no annotation return empty tags; an annotation whose hash matches no current event is logged (console.warn) and omitted from the result, never deleted (D-09 orphan policy).
    - Returns Result; propagates StorageError from either port.
  </behavior>
  <action>
    Add the two port types to ports.ts (JSDoc: ForWritingAnnotations upsert by fillIdsHash;
    ForReadingAnnotations read-one + read-many; both internal driven ports feeding the RULE-01 read
    route). Implement makeGetCalendarEventsWithRulesUseCase as a factory over
    {readCalendarEvents, readAnnotations}: fetch events, collect their fillIdsHash set, fetch
    annotations, left-join in memory, default tags/otherNote, log-and-omit orphans. Return Result.
    Write RED tests first using in-memory port doubles: event with annotation returns its tags;
    event without annotation returns []; an orphan annotation (hash matching no event) is warned +
    omitted; StorageError propagates. Run, confirm failure, implement.
  </action>
  <verify>
    <automated>bun run test -- packages/core/src/journal/application/getCalendarEventsWithRules.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Read use-case joins events + annotations on fillIdsHash; unannotated events return []; orphan
    annotations are warned and omitted (not deleted); errors propagate as Result.
  </acceptance_criteria>
</task>

<task type="execute" tdd="true">
  <name>Task 2: setRuleTags write use-case (D-07/D-14/D-21)</name>
  <files>packages/core/src/journal/application/setRuleTags.ts, packages/core/src/journal/application/setRuleTags.test.ts</files>
  <read_first>
    - packages/core/src/journal/domain/rule-tags.ts — the event-type→enum resolver (20-07) to validate tags against.
    - packages/core/src/journal/domain/calendar-event.ts — CalendarEventType to look up the event's type.
    - .planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-RESEARCH.md § Pattern 3 + § Security Domain (input validation).
  </read_first>
  <behavior>
    - setRuleTags({calendarId, fillIdsHash, tags, otherNote}): looks up the target event (via ForReadingCalendarEvents) to determine its CalendarEventType; validates every tag against that type's enum (resolver); enforces OTHER-requires-note (D-21).
    - On any validation failure returns a validation-error Result and does NOT upsert.
    - On success upserts via ForWritingAnnotations and returns the saved annotation.
    - Unknown fillIdsHash (no matching event) returns a not-found/validation Result, never a blind write.
  </behavior>
  <action>
    Implement makeSetRuleTagsUseCase as a factory over {readCalendarEvents, writeAnnotations}.
    Resolve the event's type, pick the enum via the resolver, validate the supplied tags + the
    OTHER-note rule (defense-in-depth beyond the contract refine). Only on full validation upsert the
    annotation. Return Result with a validation-error kind for bad input / unknown hash. No rule
    evaluation — record only what was supplied. Write RED tests first: valid enter tags upsert;
    cross-type tag rejected; OTHER-without-note rejected (no write); unknown hash rejected; StorageError
    propagates. Run, confirm failure, implement.
  </action>
  <verify>
    <automated>bun run test -- packages/core/src/journal/application/setRuleTags.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Valid tags upsert; cross-type + OTHER-without-note + unknown-hash all rejected without a write;
    success returns the saved annotation; Result-typed throughout; no evaluation logic.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| use-case input (from route/MCP) | Rule-tag writes validated at the application layer, not only at the edge |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-11 | Tampering | setRuleTags input | high | mitigate | Use-case re-validates tags vs event-type enum + OTHER-note (defense-in-depth beyond the contract refine); rejects before persist |
| T-20-14 | Elevation of Privilege | write to arbitrary fillIdsHash | medium | mitigate | Use-case looks up the event first; unknown/mismatched hash rejected, never blind-written |
| T-20-12 | Tampering | scope creep to a DSL | medium | mitigate | Use-cases record only supplied tags; no evaluation/inference (REQUIREMENTS Out of Scope) |
| T-20-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase (RESEARCH § Package Legitimacy Audit — N/A) |
</threat_model>

<verification>
- `bun run test -- packages/core/src/journal/application/getCalendarEventsWithRules.test.ts` green.
- `bun run test -- packages/core/src/journal/application/setRuleTags.test.ts` green.
- `bun run typecheck && bun run lint` clean for packages/core (hexagon purity holds).
</verification>

<success_criteria>
The previously-missing calendar-events read path exists (events + annotations), and rule-tag writes
are validated + upserted with recording-layer discipline — both Result-typed, both with in-memory
port tests.
</success_criteria>

<output>
Create `.planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-09-SUMMARY.md` when done.
</output>
