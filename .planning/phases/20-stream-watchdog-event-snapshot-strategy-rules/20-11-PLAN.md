---
phase: 20-stream-watchdog-event-snapshot-strategy-rules
plan: 11
type: execute
wave: 9
depends_on: ["20-10"]
files_modified:
  - apps/web/src/hooks/useRuleTags.ts
  - apps/web/src/hooks/useRuleTags.test.ts
  - apps/web/src/screens/Journal.tsx
  - apps/web/src/screens/Journal.test.tsx
autonomous: true
requirements: [RULE-01]
must_haves:
  truths:
    - "useRuleTags fetches the combined events+annotations payload for a calendar and exposes a save mutation (PUT) with a retry path."
    - "The Notes panel shows a rule-tag control ABOVE the existing free-text textarea (textarea unchanged): an ENTER section always, an EXIT section only once the trade has a CLOSE event, and one ROLL section per ROLL event."
    - "Tags are multi-select toggle chips (Phase-21 Button variant=toggle tone=violet size=xs); OTHER reveals a required inline note before the set can save (D-21)."
    - "A chip's active state flips only AFTER the save call resolves (no optimistic-then-revert); on failure the save-error copy + Retry render inline."
    - "Trade-list rows show one neutral read-view pill (border-line2 text-dim) when the trade has >=1 recorded tag, comma-joined + truncated (D-22)."
  artifacts:
    - apps/web/src/hooks/useRuleTags.ts (fetch + save + retry)
    - apps/web/src/screens/Journal.tsx (rule control in Notes panel + read-view pill)
  key_links:
    - "useRuleTags calls GET/PUT /api/journal/.../rules (20-10) using the journal-rules contracts (20-07); Journal.tsx renders per-event-type sections keyed by CalendarEventType."
  prohibitions:
    - "MUST NOT modify the Journal 3-column grid, panel set, snapshot table, lifecycle chart, or any Overview/Analyzer surface (20-UI-SPEC Scope)."
    - "MUST NOT touch the existing free-text thesis textarea (D-10 supplement, unchanged)."
    - "MUST NOT flip a chip's active state before the save resolves (no lying UI, 20-UI-SPEC)."
    - "The read-view pill MUST be neutral (border-line2 text-dim), NOT violet — violet stays reserved for the live editing control's active state (20-UI-SPEC Color)."
    - "MUST NOT add any SNAP-01 UI (no snapshot-triggered toast/banner/badge — SNAP-01 is headless)."
    - "MUST NOT introduce a new npm dependency, shadcn component, hex value, spacing, or type size (20-UI-SPEC frozen scales)."
---

<objective>
RULE-01 UI: the rule-tag capture control in the Journal Notes panel + the read-view pill in the
trade list (D-10/D-22). One new hook (fetch + save), one restyled panel section, one extra list
pill — all reusing the Phase-21 Button primitive and the frozen MORAI tokens per 20-UI-SPEC.

Purpose: Let the user record which enter/exit/roll rule fired per trade, editable anytime, and scan
"which rule fired" across trades at a glance — without inventing any new visual grammar.
Output: useRuleTags hook + Journal rule control + read-view pill.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-UI-SPEC.md
@.planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-RESEARCH.md
@apps/web/src/screens/Journal.tsx
@apps/web/src/screens/Journal.test.tsx
@apps/web/src/hooks/usePicker.ts
@apps/web/src/components/system/Button.tsx
@packages/contracts/src/journal-rules.ts
</context>

## Artifacts this plan produces

| Symbol | File | Kind |
|--------|------|------|
| `useRuleTags` (fetch + save + retry) | `apps/web/src/hooks/useRuleTags.ts` | data hook |
| rule-tag control + read-view pill | `apps/web/src/screens/Journal.tsx` | UI |

<tasks>

<task type="execute" tdd="true">
  <name>Task 1: useRuleTags hook (fetch + save + retry)</name>
  <files>apps/web/src/hooks/useRuleTags.ts, apps/web/src/hooks/useRuleTags.test.ts</files>
  <read_first>
    - apps/web/src/hooks/usePicker.ts — the existing query-hook shape (fetch → safeParse contract → null/error handling) + the Phase-19 picker-error retry convention to mirror.
    - packages/contracts/src/journal-rules.ts — getEventsWithRulesResponse + setRuleTagsRequest/response schemas to parse against.
    - .planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-UI-SPEC.md § RULE-01 adapter surface (no optimistic revert; retry on failure).
  </read_first>
  <behavior>
    - Fetches GET /api/journal/:calendarId/rules, safeParses getEventsWithRulesResponse; exposes events+tags for render.
    - Exposes a save(fillIdsHash, tags, otherNote?) mutation calling PUT, safeParsing setRuleTagsResponse; success updates local state, failure sets an error flag for that section (drives the inline error + Retry).
    - No optimistic state — the local tag state reflects only server-confirmed saves.
  </behavior>
  <action>
    Create useRuleTags mirroring usePicker's fetch/parse/error idiom. Implement fetch + a save
    mutation with per-target error state and a retry that resubmits the same payload. Parse both
    responses via the journal-rules contracts (drop/flag on parse failure). Write RED tests first
    (msw or the project's existing hook-test harness): fetch returns events+tags; save success
    updates state; save failure surfaces an error + retry re-issues the PUT. Run, confirm failure,
    implement.
  </action>
  <verify>
    <automated>bun run test -- apps/web/src/hooks/useRuleTags.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Hook fetches + parses the combined payload; save is non-optimistic (state changes only on
    success); failure exposes an error + working retry.
  </acceptance_criteria>
</task>

<task type="execute" tdd="true">
  <name>Task 2: Journal rule-tag control + read-view pill (D-07/D-10/D-22, 20-UI-SPEC)</name>
  <files>apps/web/src/screens/Journal.tsx, apps/web/src/screens/Journal.test.tsx</files>
  <read_first>
    - apps/web/src/screens/Journal.tsx — the Notes "thesis · review" Panel (PanelHeading + free-text textarea), the trade-list map that renders the history/entry-exit/OPEN pills, and the SectionLabel usage.
    - apps/web/src/components/system/Button.tsx — variant="toggle" tone="violet" size="xs" (active-fill behavior) for the chips.
    - .planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-UI-SPEC.md § RULE-01 component contract (section rendering rules, OTHER inline input, read-view pill grammar) + § Copywriting (chip labels, EXIT-before-close line, error copy).
  </read_first>
  <behavior>
    - Inside the Notes panel, ABOVE the untouched textarea: one row per applicable event type. ENTER always renders; EXIT renders its chip row only once the trade has a CLOSE event (else the muted "Available at close." line); ROLL renders once per ROLL event with its own timestamp label.
    - Each row is flex-wrapped toggle chips (one per enum value for that event type via Button variant=toggle) with human-readable title-case labels; OTHER is last; activating OTHER reveals a required inline single-line note; deactivating hides + discards it.
    - Chips are editable regardless of open/closed (no read-only lock). A chip's active state flips only after save resolves; on failure the "Couldn't save rule tags." copy + Retry render inline under that section.
    - Trade-list rows: append one neutral pill (rounded-[3px] border-line2 text-dim px-[5px] text-[8px]) with the comma-joined tag labels (truncated with …) only when the trade has >=1 recorded tag; nothing rendered when there are none.
  </behavior>
  <action>
    Render the rule-tag control in the Notes panel per the UI-SPEC anatomy (SectionLabel +
    Button toggle chips + conditional OTHER input), keyed off the event types from useRuleTags.
    Wire chip toggles + OTHER-note to useRuleTags.save (non-optimistic; inline error + Retry on
    failure). Add the neutral read-view pill to the trade-list row (same grammar as the existing
    History badge; NOT violet). Do NOT touch the textarea, the 3-column grid, the snapshot table, or
    any other surface. Reuse only existing tokens/sizes/Button primitive — zero new deps/hex/size.
    Write RED tests first: ENTER always renders; EXIT hidden→"Available at close." before a CLOSE,
    chips after; ROLL section per roll event; OTHER requires a note before save; read-view pill
    appears only with >=1 tag and is neutral-toned. Run, confirm failure, implement.
  </action>
  <verify>
    <automated>bun run test -- apps/web/src/screens/Journal.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    Rule control renders per-event-type sections with the correct conditional rendering; OTHER
    requires a note; saves are non-optimistic with inline error+Retry; neutral read-view pill shows
    only with >=1 tag; no other Journal surface changed; no new dependency/hex/size introduced.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→server (rule-tag save) | UI submits tag sets; server (20-10) is the enforcing boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-17 | Spoofing | UI showing tags as saved when they aren't | medium | mitigate | Non-optimistic — chip active state flips only after the save PUT resolves; failure shows error + Retry (20-UI-SPEC honesty principle) |
| T-20-11 | Tampering | OTHER free-text | high | mitigate | Required-note enforced client-side AND server-side (20-07/20-09); bounded length at the contract |
| T-20-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase (RESEARCH § Package Legitimacy Audit — N/A) |
</threat_model>

<verification>
- `bun run test -- apps/web/src/hooks/useRuleTags.test.ts` green.
- `bun run test -- apps/web/src/screens/Journal.test.tsx` green.
- `bun run test` (web project) green; `bun run typecheck && bun run lint` clean.
- Manual (RULE-01 ship UAT, chrome-devtools MCP): set ENTER/EXIT/ROLL tags in the Notes panel,
  reload, confirm persistence + inline read-view pill in the trade timeline; OTHER requires a note.
</verification>

<success_criteria>
The user can record + edit enter/exit/roll rule tags per trade in the Journal (OTHER requires a
note), and scan recorded rules across trades via a neutral read-view pill — reusing the Phase-21
Button and frozen tokens, with no other surface changed.
</success_criteria>

<output>
Create `.planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-11-SUMMARY.md` when done.
This SUMMARY marks the RULE-01 feature complete and ready for its own deploy + UAT cycle (D-18).
</output>
