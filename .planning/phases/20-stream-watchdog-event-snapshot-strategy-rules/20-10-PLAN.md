---
phase: 20-stream-watchdog-event-snapshot-strategy-rules
plan: 10
type: execute
wave: 8
depends_on: ["20-09"]
files_modified:
  - apps/server/src/adapters/http/journal-rules.routes.ts
  - apps/server/src/adapters/http/journal-rules.routes.test.ts
  - apps/server/src/main.ts
  - apps/server/src/adapters/mcp/tools.ts
  - apps/server/src/adapters/mcp/tools.test.ts
  - apps/server/src/adapters/mcp/server.ts
autonomous: true
requirements: [RULE-01]
must_haves:
  truths:
    - "GET /api/journal/:calendarId/rules returns the combined events+annotations payload (getEventsWithRulesResponse) via the read use-case."
    - "PUT /api/journal/events/:hash/rules validates setRuleTagsRequest (incl. OTHER-requires-note) and calls the write use-case, mapping Result to 200/400/404/500."
    - "Both routes are mounted inside the same authReadGroup JWT middleware as the other journal-adjacent routes (Security V4) — no unauthenticated rule-tag access."
    - "MCP tools get_rule_tags (read) and set_rule_tags (write) are registered, share the journal-rules contract schemas, and safeParse args at the boundary."
  artifacts:
    - apps/server/src/adapters/http/journal-rules.routes.ts (GET + PUT rule routes)
    - apps/server/src/adapters/mcp/tools.ts (get_rule_tags + set_rule_tags)
    - apps/server/src/main.ts (route mount inside apiRouter / authReadGroup)
  key_links:
    - "Routes + MCP tools share the journal-rules Zod schemas (20-07) and call the use-cases (20-09) — MCP-02 convention (same contract both surfaces)."
    - "The apiRouter chain in main.ts (mounted under authReadGroup at ~line 285) is where these routes attach for JWT auth."
  prohibitions:
    - "MUST NOT mount the rule routes outside authReadGroup (Security V4 — never expose rule-tag write unauthenticated)."
    - "Adapters MUST stay thin: Zod-parse input → call use-case → map Result → respond; zero business logic in the route/tool (architecture-boundaries #3)."
    - "MUST NOT put the read/write behind two separate round trips where the combined read payload suffices (D-13 minimal surface)."
    - "MCP tools MUST NOT throw on invalid args — safeParse and return an error content payload (existing tool precedent)."
---

<objective>
RULE-01 adapter surface (D-13): the HTTP routes and MCP tools that expose the read/write use-cases —
shipped together in the same PR. GET the combined events+annotations payload, PUT a validated
rule-tag set; get_rule_tags / set_rule_tags MCP tools mirror the same contracts so Claude Code can
set/query rule tags.

Purpose: This is the surface RESEARCH flags as entirely missing today — nothing exposes
OPEN/CLOSE/ROLL events (or annotations) to the browser or MCP. Mounting inside authReadGroup keeps
it JWT-gated like every other journal route (Security V4).
Output: journal-rules routes + MCP tools, wired into the composition root.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-RESEARCH.md
@.planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-PATTERNS.md
@apps/server/src/adapters/http/calendar.routes.ts
@apps/server/src/adapters/http/journal.routes.ts
@apps/server/src/adapters/mcp/tools.ts
@apps/server/src/adapters/mcp/server.ts
@apps/server/src/main.ts
@packages/contracts/src/journal-rules.ts
</context>

## Artifacts this plan produces

| Symbol | File | Kind |
|--------|------|------|
| `journalRulesRoutes` (GET + PUT) | `apps/server/src/adapters/http/journal-rules.routes.ts` | Hono router |
| route mount in apiRouter | `apps/server/src/main.ts` | composition root |
| `registerGetRuleTagsTool` / `registerSetRuleTagsTool` | `apps/server/src/adapters/mcp/tools.ts` | MCP tools |

<tasks>

<task type="execute" tdd="true">
  <name>Task 1: journal-rules HTTP routes + mount inside authReadGroup (D-13, Security V4)</name>
  <files>apps/server/src/adapters/http/journal-rules.routes.ts, apps/server/src/adapters/http/journal-rules.routes.test.ts, apps/server/src/main.ts</files>
  <read_first>
    - apps/server/src/adapters/http/calendar.routes.ts — the factory-returning-Hono-router pattern: zValidator("json", schema) → c.req.valid → use-case → branch on result.ok → map result.error.kind to status → c.json(contract.parse(...), status).
    - apps/server/src/adapters/http/journal.routes.ts — the existing journalRoutes(getJournal) signature/mount to sit beside.
    - apps/server/src/main.ts — the apiRouter chain (.route("/", …)) mounted under authReadGroup (~line 285); add the new router into that chain, NOT outside it.
    - .planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-PATTERNS.md § journal-rules.routes.ts.
  </read_first>
  <behavior>
    - GET /api/journal/:calendarId/rules → 200 with getEventsWithRulesResponse; 404 when the calendar is unknown; 500 on StorageError.
    - PUT /api/journal/events/:hash/rules with a setRuleTagsRequest body → 200 with the saved annotation; 400 on validation-error (incl. OTHER-without-note); 404 on unknown hash; 500 on StorageError.
    - Both routes require a valid JWT (mounted in authReadGroup) — an unauthenticated request is rejected by the group middleware.
  </behavior>
  <action>
    Create journalRulesRoutes(getEventsWithRules, setRuleTags) as a Hono router factory mirroring
    calendar.routes.ts exactly (zValidator on the PUT body using setRuleTagsRequest; parse-don't-cast
    path params). Map use-case Result kinds to HTTP status. Respond with the journal-rules contract
    schemas (.parse before c.json). Mount it into the apiRouter chain in main.ts (inside
    authReadGroup — same group as calendar/journal routes). Write RED tests first (Hono app test
    harness like calendar.routes.test.ts): GET returns combined payload / 404; PUT persists / 400 on
    OTHER-without-note / 404 unknown hash; assert the routes are under the authenticated group. Run,
    confirm failure, implement.
  </action>
  <verify>
    <automated>bun run test -- apps/server/src/adapters/http/journal-rules.routes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    GET/PUT behave per the status map incl. 400 on OTHER-without-note; routes mount inside
    authReadGroup (JWT-gated); thin adapter (no business logic); contracts parsed on the way out.
  </acceptance_criteria>
</task>

<task type="execute" tdd="true">
  <name>Task 2: get_rule_tags + set_rule_tags MCP tools (D-13, MCP-02)</name>
  <files>apps/server/src/adapters/mcp/tools.ts, apps/server/src/adapters/mcp/tools.test.ts, apps/server/src/adapters/mcp/server.ts</files>
  <read_first>
    - apps/server/src/adapters/mcp/tools.ts — registerListCalendarsTool / registerGetJournalTool: server.registerTool(name, {title, description, inputSchema}, handler) + safeParse args + reuse the SAME contract schema as the HTTP route + return {content:[{type:"text", text: JSON.stringify(payload)}]}.
    - apps/server/src/adapters/mcp/server.ts — where tools are registered in the per-request closure (see how getPicker/getJournal are threaded in).
    - .planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-PATTERNS.md § tools.ts (MCP tools).
  </read_first>
  <behavior>
    - get_rule_tags (inputSchema {calendarId}) returns the combined events+annotations payload (same schema as the GET route).
    - set_rule_tags (inputSchema {calendarId, fillIdsHash, tags[], otherNote?}) validates + upserts via the write use-case (same schema as the PUT route), returns the saved annotation; OTHER-without-note is rejected with an error content payload, never a throw.
    - Both safeParse args at the boundary; invalid args yield an error content payload.
  </behavior>
  <action>
    Add registerGetRuleTagsTool + registerSetRuleTagsTool mirroring the existing tool factories,
    reusing the journal-rules contract schemas (MCP-02 — same schema both surfaces). Thread them into
    server.ts's registration closure alongside the existing journal/picker tools (add the use-case
    deps to makeMcpRouter/registration as those precedents do). safeParse args; never throw. Write
    RED tests first (mirror the strongest existing tool test — a real McpServer + InMemoryTransport
    client invocation): get_rule_tags returns the payload; set_rule_tags upserts; OTHER-without-note
    returns an error payload. Run, confirm failure, implement.
  </action>
  <verify>
    <automated>bun run test -- apps/server/src/adapters/mcp/tools.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Both MCP tools registered and invoked via a real transport; share the journal-rules schemas;
    OTHER-without-note rejected as an error payload (no throw); args safeParsed.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→server (HTTP rule routes) | Untrusted JWT-authed requests write rule tags |
| MCP client→server (tools) | Bearer-authed MCP tool calls read/write rule tags |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-15 | Elevation of Privilege | unauthenticated rule write | high | mitigate | Routes mounted inside authReadGroup JWT middleware (Security V4); MCP behind existing bearer auth |
| T-20-11 | Tampering | request body | high | mitigate | zValidator(setRuleTagsRequest) at the route + MCP safeParse; OTHER-requires-note + bounds enforced (Security V5) |
| T-20-16 | Information Disclosure | cross-calendar tag read via guessed hash | low | accept | fill_ids_hash is a 64-char SHA-256 (unguessable) + authReadGroup; single-user app (defense-in-depth note, RESEARCH § Security Domain) |
| T-20-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase (RESEARCH § Package Legitimacy Audit — N/A) |
</threat_model>

<verification>
- `bun run test -- apps/server/src/adapters/http/journal-rules.routes.test.ts` green.
- `bun run test -- apps/server/src/adapters/mcp/tools.test.ts` green.
- `bun run typecheck && bun run lint` clean for apps/server; MCP integration suite (mcp.test.ts) green.
</verification>

<success_criteria>
JWT-gated GET/PUT rule routes + MCP read/write tools, sharing one contract, calling the use-cases as
thin adapters — the full RULE-01 backend surface shipped in one PR (D-13).
</success_criteria>

<output>
Create `.planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-10-SUMMARY.md` when done.
</output>
