---
phase: 20-stream-watchdog-event-snapshot-strategy-rules
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/stream-events.ts
  - packages/contracts/src/stream-events.test.ts
  - apps/web/src/lib/deriveStreamStatus.ts
  - apps/web/src/lib/deriveStreamStatus.test.ts
autonomous: true
requirements: [WATCH-01]
must_haves:
  truths:
    - "A ping frame carrying {isRth:boolean} round-trips through streamPingEvent.parse; a malformed frame fails safeParse (caller drops it)."
    - "deriveStreamStatus returns 'quiet' whenever isRth===false regardless of tick recency."
    - "deriveStreamStatus returns 'stalled' only when isRth===true AND msSinceLastTickOrConnect >= stallThresholdMs; never before the threshold."
    - "deriveStreamStatus returns 'connecting' when isRth===null (no ping yet) or during RTH before the first tick within the grace window."
  artifacts:
    - packages/contracts/src/stream-events.ts (streamPingEvent schema + StreamPingEvent type)
    - apps/web/src/lib/deriveStreamStatus.ts (deriveStreamStatus fn + DerivedStatus type)
  key_links:
    - "streamPingEvent is the single source of the wire shape both the server emitter (20-02) and the client ping listener (20-03) parse against."
    - "deriveStreamStatus is the single derivation the hook interval (20-03) and badge (20-03) read — no status logic duplicated elsewhere."
  prohibitions:
    - "deriveStreamStatus MUST NOT call Date.now() internally — caller passes msSinceLastTickOrConnect."
    - "streamPingEvent MUST carry only isRth (no PII, no account data, no batching of other state)."
    - "deriveStreamStatus MUST NOT return a 4th enum value; CONNECTING is a derived label condition, not a state-machine member (D-01)."
---

<objective>
WATCH-01 foundation: the two pure, test-first primitives every other WATCH-01 plan builds on —
the additive `streamPingEvent` stream-contract schema (D-03) and the framework-free
`deriveStreamStatus` state-derivation function (D-01/D-02/D-11, Pattern 1).

Purpose: Extract the badge's LIVE/QUIET/CONNECTING/STALLED logic as a pure function BEFORE it is
wired into React, so it is unit- and property-testable in isolation (per tdd.md — do not test it
embedded in the hook). Lock the wire shape of the heartbeat once, so server (20-02) and client
(20-03) can be built in parallel against the same contract.
Output: `streamPingEvent`/`StreamPingEvent` in `@morai/contracts`; `deriveStreamStatus`/
`DerivedStatus` in `apps/web/src/lib`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-RESEARCH.md
@.planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-PATTERNS.md
@.planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-UI-SPEC.md
@.planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-VALIDATION.md
@packages/contracts/src/stream-events.ts
@packages/contracts/src/stream-events.test.ts
@apps/web/src/components/LiveStatusBadge.tsx
</context>

## Artifacts this plan produces

| Symbol | File | Kind |
|--------|------|------|
| `streamPingEvent` (Zod) + `StreamPingEvent` (type) | `packages/contracts/src/stream-events.ts` | additive contract schema |
| `deriveStreamStatus` (fn) + `DerivedStatus` (type) | `apps/web/src/lib/deriveStreamStatus.ts` | pure derivation |

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: streamPingEvent contract schema (D-03)</name>
  <files>packages/contracts/src/stream-events.ts, packages/contracts/src/stream-events.test.ts</files>
  <read_first>
    - packages/contracts/src/stream-events.ts — mirror the existing `streamLiveGreekEvent`/`streamReconcileEvent` z.object shape + `z.infer` type-export idiom in this same file (PATTERNS: exact analog).
    - packages/contracts/src/stream-events.test.ts — existing round-trip test style to extend.
  </read_first>
  <behavior>
    - `streamPingEvent.parse({ isRth: true })` → `{ isRth: true }`; `.parse({ isRth: false })` → `{ isRth: false }`.
    - `streamPingEvent.safeParse({})` → `success: false` (isRth required).
    - `streamPingEvent.safeParse({ isRth: "yes" })` → `success: false` (boolean only).
    - `streamPingEvent.safeParse({ isRth: true, extra: 1 })` → success (extra keys stripped, additive-safe) — assert the parsed value equals `{ isRth: true }`.
  </behavior>
  <action>
    Add an additive exported `streamPingEvent = z.object({ isRth: z.boolean() })` and
    `export type StreamPingEvent = z.infer<typeof streamPingEvent>` to stream-events.ts,
    following the file's existing schema/type-export pairs verbatim. Do NOT modify or renumber the
    existing event schemas. isRth is the ONLY field (Security V5 / threat T-20-01: keep the ping a
    minimal boolean channel, never batch other state into it). Write the RED test first in
    stream-events.test.ts, run it, confirm it fails for the assertion reason, then implement.
  </action>
  <verify>
    <automated>bun run test -- packages/contracts/src/stream-events.test.ts</automated>
  </verify>
  <acceptance_criteria>
    streamPingEvent round-trips {isRth} both true/false; missing/non-boolean isRth rejected;
    extra keys stripped; existing stream-events tests still green.
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: deriveStreamStatus pure function (D-01/D-02/D-11, Pattern 1)</name>
  <files>apps/web/src/lib/deriveStreamStatus.ts, apps/web/src/lib/deriveStreamStatus.test.ts</files>
  <read_first>
    - .planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-RESEARCH.md § Pattern 1 (the exact derivation logic + rationale) and § Common Pitfalls Pitfall 1.
    - .planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-PATTERNS.md § deriveStreamStatus.ts (analog = rth-window.ts purity idiom).
    - packages/core/src/journal/domain/rth-window.ts — the "caller passes now, no Date.now() inside, single clear return" idiom to mirror.
  </read_first>
  <behavior>
    - Input object: `{ hasReceivedFirstTick: boolean; msSinceLastTickOrConnect: number; isRth: boolean | null; stallThresholdMs: number }`; return `DerivedStatus = "live" | "quiet" | "connecting" | "stalled"`.
    - isRth===false → "quiet" (dominates; property: for ALL other inputs, isRth===false ⇒ "quiet").
    - isRth===null → "connecting" (no ping received yet).
    - isRth===true AND msSinceLastTickOrConnect < stallThresholdMs AND hasReceivedFirstTick → "live".
    - isRth===true AND msSinceLastTickOrConnect < stallThresholdMs AND NOT hasReceivedFirstTick → "connecting".
    - isRth===true AND msSinceLastTickOrConnect >= stallThresholdMs → "stalled" (boundary: exactly == threshold IS stalled).
    - Property (fast-check): with isRth===true and hasReceivedFirstTick===true, status is monotonic in msSinceLastTickOrConnect — once "stalled" it never returns to "live" as elapsed grows.
  </behavior>
  <action>
    Create the pure `deriveStreamStatus` in apps/web/src/lib (NOT under hooks/, so it imports no
    React and is unit-testable). Implement exactly the branch order in RESEARCH Pattern 1: quiet
    (isRth===false) wins first, then connecting (isRth===null), then the elapsed-vs-threshold check.
    Export `DerivedStatus`. No imports beyond types. Write RED tests first (example cases for every
    branch above + two fast-check properties: quiet-dominates and stall-monotonic-in-elapsed), run,
    confirm failure, then implement.
  </action>
  <verify>
    <automated>bun run test -- apps/web/src/lib/deriveStreamStatus.test.ts</automated>
  </verify>
  <acceptance_criteria>
    All branch example cases pass; quiet-dominates and stall-monotonic fast-check properties pass;
    threshold boundary (== threshold ⇒ stalled) asserted; function contains no Date.now() and no
    React import.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server→client (SSE ping frame) | Server-emitted heartbeat payload parsed defensively client-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Information Disclosure | streamPingEvent payload | low | mitigate | Schema carries only `isRth:boolean` — no PII/account data; resist adding fields (Security V5 note, RESEARCH § Security Domain) |
| T-20-02 | Tampering | client parse of ping frame | low | mitigate | Client parses via streamPingEvent.safeParse and drops malformed frames (wired in 20-03); never casts raw JSON |
| T-20-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase (RESEARCH § Package Legitimacy Audit — N/A); nothing to verify |
</threat_model>

<verification>
- `bun run test -- packages/contracts/src/stream-events.test.ts` green.
- `bun run test -- apps/web/src/lib/deriveStreamStatus.test.ts` green.
- `bun run typecheck && bun run lint` clean for the two changed packages.
</verification>

<success_criteria>
streamPingEvent and deriveStreamStatus exist, are exported, and are covered by passing unit +
fast-check tests. No React import leaks into deriveStreamStatus. No existing contract test regresses.
</success_criteria>

<output>
Create `.planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-01-SUMMARY.md` when done.
</output>
