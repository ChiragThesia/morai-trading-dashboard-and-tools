---
phase: 20-stream-watchdog-event-snapshot-strategy-rules
plan: 05
type: execute
wave: 4
depends_on: ["20-04"]
files_modified:
  - packages/adapters/src/postgres/migrations/0016_snapshot_trigger.sql
  - packages/adapters/src/postgres/schema.ts
  - packages/adapters/src/postgres/repos/calendar-snapshots.ts
  - packages/adapters/src/memory/calendar-snapshots.ts
  - packages/adapters/src/__contract__/calendar-snapshots.contract.ts
  - docs/architecture/data-model.md
autonomous: true
requirements: [SNAP-01]
user_setup:
  - service: supabase-postgres
    why: "Migration 0016 (additive nullable trigger column) must be applied to the live DB during the SNAP-01 deploy cycle (D-18)."
    dashboard_config:
      - task: "Run `bun run migrate` against the live DATABASE_URL to apply 0016 (verify locally via testcontainers first; note: local migrate validates all worker env incl SIDECAR_URL — see project memory)"
        location: "SNAP-01 deploy step, after this plan's PR merges"
must_haves:
  truths:
    - "Migration 0016 adds a nullable trigger column to calendar_snapshots additively (existing rows unaffected)."
    - "persistSnapshot writes row.trigger; reading a row returns its trigger (default 'scheduled')."
    - "The Postgres ForReadingLatestSnapshotTime impl returns MAX(time) as Date, or null when the table is empty, never throws."
    - "The memory twin implements ForReadingLatestSnapshotTime with identical semantics; the contract test asserts parity."
  artifacts:
    - packages/adapters/src/postgres/migrations/0016_snapshot_trigger.sql (additive nullable trigger)
    - packages/adapters/src/postgres/schema.ts (calendarSnapshots.trigger column)
    - packages/adapters/src/postgres/repos/calendar-snapshots.ts (persist trigger + MAX(time) read)
    - packages/adapters/src/memory/calendar-snapshots.ts (twin: trigger + latest-time read)
    - packages/adapters/src/__contract__/calendar-snapshots.contract.ts (trigger + MAX(time) parity)
  key_links:
    - "SnapshotRow.trigger (20-04) is the field mapped here; ForReadingLatestSnapshotTime (20-04) is the port implemented here and consumed by the detector (20-06)."
    - "MAX(time) is an index-only scan — time is the leading column of the composite PK (time, calendar_id)."
  prohibitions:
    - "Migration 0016 MUST be purely additive — no NOT NULL without default, no rewrite of existing rows (D-12)."
    - "MUST NOT change the composite-PK / onConflictDoNothing idempotency of persistSnapshot."
    - "Drizzle MUST stay confined to packages/adapters (architecture-boundaries #4)."
    - "MUST NOT plan a drizzle-kit push — this repo applies numbered SQL migrations via bun run migrate."
---

<objective>
SNAP-01 adapters: land the provenance column and the cooldown ground-truth read. Numbered migration
0016 adds a nullable `trigger` to `calendar_snapshots` (D-12); the Postgres + memory
`calendar-snapshots` repos map `trigger` and implement `ForReadingLatestSnapshotTime`
(`SELECT MAX(time)`); the contract-parity test covers both the trigger round-trip and the
cold-start null read.

Purpose: The cooldown must reconcile snapshots written by two separate OS processes (server
detector, worker cron) — only Postgres is shared ground truth (Pitfall 2). MAX(time) is cheap
because `time` leads the composite PK.
Output: migration + schema + repo + twin + contract test + data-model doc note.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-RESEARCH.md
@.planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-PATTERNS.md
@packages/adapters/src/postgres/schema.ts
@packages/adapters/src/postgres/repos/calendar-snapshots.ts
@packages/adapters/src/memory/calendar-snapshots.ts
@packages/adapters/src/__contract__/calendar-snapshots.contract.ts
@packages/adapters/src/postgres/migrations/0015_picker_snapshot.sql
</context>

## Artifacts this plan produces

| Symbol | File | Kind |
|--------|------|------|
| `0016_snapshot_trigger.sql` | `packages/adapters/src/postgres/migrations/` | numbered migration |
| `calendarSnapshots.trigger` | `packages/adapters/src/postgres/schema.ts` | Drizzle column |
| persist trigger + `ForReadingLatestSnapshotTime` impl | `packages/adapters/src/postgres/repos/calendar-snapshots.ts` | repo |
| memory twin (trigger + latest-time) | `packages/adapters/src/memory/calendar-snapshots.ts` | twin |
| trigger + MAX(time) parity assertions | `packages/adapters/src/__contract__/calendar-snapshots.contract.ts` | contract test |

<tasks>

<task type="execute">
  <name>Task 1: Migration 0016 + Drizzle trigger column (D-12) + data-model doc</name>
  <files>packages/adapters/src/postgres/migrations/0016_snapshot_trigger.sql, packages/adapters/src/postgres/schema.ts, docs/architecture/data-model.md</files>
  <read_first>
    - packages/adapters/src/postgres/migrations/0015_picker_snapshot.sql — the numbered-migration format/style to mirror; confirm 0016 is the next free number.
    - packages/adapters/src/postgres/schema.ts — the `calendarSnapshots` pgTable definition + column conventions (varchar/text/timestamp, enableRLS).
    - docs/architecture/data-model.md — the calendar_snapshots section to amend.
    - .claude/rules/workflow.md § Docs Before Code.
  </read_first>
  <action>
    Create `0016_snapshot_trigger.sql`: `ALTER TABLE calendar_snapshots ADD COLUMN trigger text;`
    (nullable, additive — NO default at DB level, NO backfill; existing rows stay NULL and read as
    "scheduled" at the application layer). Add a SQL comment citing D-12 (provenance marker;
    'scheduled' default vs 'event-move'). Mirror the column in schema.ts as
    `trigger: text("trigger")` on `calendarSnapshots`. Add a Hemingway-style note to data-model.md
    recording the additive trigger/provenance column and its two values (scheduled | event-move),
    referencing SNAP-01/D-12 — docs updated with the schema change per workflow rule.
  </action>
  <verify>
    <automated>rg -n "trigger" packages/adapters/src/postgres/migrations/0016_snapshot_trigger.sql packages/adapters/src/postgres/schema.ts && rg -n "event-move|provenance|trigger" docs/architecture/data-model.md</automated>
  </verify>
  <acceptance_criteria>
    0016 migration exists, additive nullable trigger column; schema.ts mirrors it; data-model.md
    documents the column; no NOT-NULL/backfill in the migration.
  </acceptance_criteria>
</task>

<task type="execute" tdd="true">
  <name>Task 2: persistSnapshot trigger mapping + ForReadingLatestSnapshotTime impls</name>
  <files>packages/adapters/src/postgres/repos/calendar-snapshots.ts, packages/adapters/src/memory/calendar-snapshots.ts, packages/adapters/src/__contract__/calendar-snapshots.contract.ts</files>
  <read_first>
    - packages/adapters/src/postgres/repos/calendar-snapshots.ts — existing `persistSnapshot` row mapping + `onConflictDoNothing` composite-PK idempotency; how it constructs Drizzle inserts/selects.
    - packages/adapters/src/memory/calendar-snapshots.ts — the in-memory store shape.
    - packages/adapters/src/__contract__/calendar-snapshots.contract.ts — the memory-vs-Postgres parity harness (describe.each over both impls, testcontainers Postgres).
    - .planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-RESEARCH.md § Pattern 2 (MAX(time) rationale — index-only scan on leading PK column).
  </read_first>
  <behavior>
    - persistSnapshot writes row.trigger; a persisted row read back carries its trigger; a legacy/NULL trigger reads as "scheduled".
    - The Postgres ForReadingLatestSnapshotTime runs `SELECT MAX(time) FROM calendar_snapshots`, returns ok(Date) when rows exist, ok(null) on an empty table (cold start), err(StorageError) only on a real DB error — never throws.
    - The memory twin returns the max `time` across stored rows, or null when empty.
    - Contract test asserts BOTH impls agree on: trigger round-trip (scheduled + event-move) and latest-time (null on empty, max after inserts).
  </behavior>
  <action>
    Map `trigger` in persistSnapshot (default "scheduled" when the row/legacy value is absent).
    Implement `ForReadingLatestSnapshotTime` in the Postgres repo via `SELECT MAX(time)` (parse the
    nullable result to `Date | null` explicitly — never `!`) returning `Result<Date | null,
    StorageError>`; implement the same in the memory twin (architecture-boundaries #8: twin in the
    same PR). Extend the contract test to cover trigger round-trip + latest-time parity (null on
    cold start, correct max after inserts) across both impls. Write the contract assertions RED
    first, run against testcontainers, confirm failure, implement.
  </action>
  <verify>
    <automated>bun run test -- packages/adapters/src/__contract__/calendar-snapshots.contract.ts</automated>
  </verify>
  <acceptance_criteria>
    trigger round-trips (scheduled + event-move) in both impls; MAX(time) returns null on cold start
    and the correct max after inserts, never throws; composite-PK idempotency unchanged; memory twin
    parity green under testcontainers.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter→database (Postgres) | Snapshot writes/reads; migration alters shared schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-08 | Tampering | migration 0016 | medium | mitigate | Purely additive nullable column; no rewrite/backfill; verified via testcontainers before live apply (D-12) |
| T-20-09 | Information Disclosure | MAX(time) read | low | accept | Returns only a timestamp; no row content leaked; internal driven port (no HTTP/MCP surface) |
| T-20-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase (RESEARCH § Package Legitimacy Audit — N/A) |
</threat_model>

<verification>
- `bun run test -- packages/adapters/src/__contract__/calendar-snapshots.contract.ts` green (testcontainers).
- `bun run typecheck && bun run lint` clean for packages/adapters.
- Live: `bun run migrate` applies 0016 during SNAP-01 deploy (user_setup) — verify column present.
</verification>

<success_criteria>
Additive trigger column live-ready via numbered migration; both repos map trigger and implement the
MAX(time) cooldown read with proven memory/Postgres parity.
</success_criteria>

<output>
Create `.planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-05-SUMMARY.md` when done.
</output>
