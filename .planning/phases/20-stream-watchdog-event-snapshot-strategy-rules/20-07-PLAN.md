---
phase: 20-stream-watchdog-event-snapshot-strategy-rules
plan: 07
type: execute
wave: 6
depends_on: ["20-06"]
files_modified:
  - packages/core/src/journal/domain/rule-tags.ts
  - packages/core/src/journal/domain/rule-tags.test.ts
  - packages/contracts/src/journal-rules.ts
  - packages/contracts/src/journal-rules.test.ts
autonomous: false
requirements: [RULE-01]
must_haves:
  truths:
    - "Three event-keyed Zod enums exist: enterRuleTag, exitRuleTag, rollRuleTag (each includes 'other'), keyed to CalendarEventType OPEN→enter / CLOSE→exit / ROLL→roll (D-07)."
    - "A helper resolves the correct enum for a given CalendarEventType (exhaustive switch)."
    - "The set-rule-tags request schema is list-shaped (tags[]) with an optional otherNote, and a .refine that REQUIRES a non-empty otherNote iff 'other' is among tags (D-21); listed values without a note are accepted."
    - "The get-events-with-rules response schema carries the event list plus per-event recorded tags + otherNote."
  artifacts:
    - packages/core/src/journal/domain/rule-tags.ts (enterRuleTag/exitRuleTag/rollRuleTag + resolver)
    - packages/contracts/src/journal-rules.ts (setRuleTagsRequest/response + getEventsWithRulesResponse)
  key_links:
    - "The contract enums import/derive from rule-tags.ts so the DB-boundary vocabulary and the HTTP/MCP vocabulary can never diverge."
    - "CalendarEventType (packages/core/src/journal/domain/calendar-event.ts) is the discriminant that selects which enum applies."
  prohibitions:
    - "MUST NOT encode any rule-evaluation logic — these are recording enums only, NOT a DSL (RULE-01 boundary; REQUIREMENTS Out of Scope)."
    - "MUST NOT put the OTHER-requires-note refinement on the DB column — it is a contract-level .refine (D-21)."
    - "MUST NOT lock the enum values without the user-trim decision checkpoint below (D-08)."
    - "The ruleTags array MUST be bounded (.max) and otherNote bounded (.max) — never an unbounded client string (Security V5)."
---

<objective>
RULE-01 vocabulary: the three event-keyed recording enums (D-07) and the list-shaped request/response
contracts (D-14) with the OTHER-requires-note refinement (D-21). Values are seeded from the KB
(D-08) and TRIMMED by the user via a decision checkpoint before they are locked into code + DB.

Purpose: This is a thin recording layer, not a DSL — the enums exist only to structure "which rule
fired," reusing the picker's own scoring vocabulary for ENTER so the user records the same
dimensions they scored candidates on. Locking the wire+DB vocabulary once lets the table (20-08),
use-cases (20-09), adapter surface (20-10), and UI (20-11) all agree.
Output: rule-tags domain enums + resolver; journal-rules contracts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-RESEARCH.md
@.planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-PATTERNS.md
@.planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-UI-SPEC.md
@packages/core/src/journal/domain/calendar-event.ts
@packages/contracts/src/stream-events.ts
</context>

## Artifacts this plan produces

| Symbol | File | Kind |
|--------|------|------|
| `enterRuleTag` / `exitRuleTag` / `rollRuleTag` + resolver | `packages/core/src/journal/domain/rule-tags.ts` | recording enums |
| `setRuleTagsRequest` / `setRuleTagsResponse` / `getEventsWithRulesResponse` | `packages/contracts/src/journal-rules.ts` | contracts |

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: Lock the ENTER/EXIT/ROLL rule-tag enum values (D-08)</name>
  <action>
    Present the seeded enum lists below to the user and await their trim/approval BEFORE Task 1
    writes the enums. This is a recording taxonomy that becomes a Zod enum + text[] DB column — do
    not lock it without the user's confirmation (D-08 "user trims before lock"). Carry the confirmed
    values into Task 1 and record them in the SUMMARY.
  </action>
  <decision>Final ENTER / EXIT / ROLL rule-tag enum values to lock (D-08: user trims before lock)</decision>
  <context>
    These strings become a Zod enum + a text[] DB column consumed by the route, MCP tool, and
    Journal UI. Changing them later means a code + data change. RESEARCH grounds the seeds in the
    KB (ENTER mirrors the 8 scoreCalendarCandidates criteria; EXIT/ROLL from trade_management.md /
    calendar_spread.md). `thesis-invalidated` is flagged [ASSUMED] (Assumption A1) — confirm it
    earns a slot distinct from `other`. Human-readable chip labels are title-case forms of these
    (20-UI-SPEC Copywriting).
  </context>
  <options>
    <option id="accept-seeds">
      <name>Lock the seeded values as-is</name>
      <pros>Zero further decisions; KB-grounded; reuses picker vocabulary for ENTER.</pros>
      <cons>Carries `thesis-invalidated` [ASSUMED]; may include a value you'd rather drop.</cons>
    </option>
    <option id="trim">
      <name>Trim / rename before lock</name>
      <pros>Exactly the taxonomy you want; drops any value that doesn't earn its slot.</pros>
      <cons>Requires you to specify the final lists.</cons>
    </option>
  </options>
  <resume-signal>
    Reply "accept-seeds" to lock: ENTER = iv-skew-favorable · term-structure-edge · event-window-play
    · gex-fit · other; EXIT = profit-target · max-loss · time-stop · thesis-invalidated · other;
    ROLL = defend-tested-side · roll-for-duration · other. Or provide the trimmed lists.
  </resume-signal>
</task>

<task type="execute" tdd="true">
  <name>Task 1: rule-tags domain enums + event-type resolver (D-07)</name>
  <files>packages/core/src/journal/domain/rule-tags.ts, packages/core/src/journal/domain/rule-tags.test.ts</files>
  <read_first>
    - packages/core/src/journal/domain/calendar-event.ts — `CalendarEventType` (OPEN/CLOSE/ROLL) discriminant + the file's Zod-boundary domain-module idiom.
    - .planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-PATTERNS.md § rule-tags.ts.
    - .planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-RESEARCH.md § Pattern 3 (enum seed grounding).
  </read_first>
  <behavior>
    - `enterRuleTag`, `exitRuleTag`, `rollRuleTag` are `z.enum([...])` with the values LOCKED by the decision checkpoint (each includes 'other').
    - A resolver `ruleTagEnumForEventType(eventType)` returns the matching enum via an exhaustive switch over CalendarEventType (OPEN→enter, CLOSE→exit, ROLL→roll).
    - Parsing a valid value for its event type succeeds; a cross-type value (e.g. exit value under ENTER) fails.
  </behavior>
  <action>
    Create rule-tags.ts with the three enums (values per the checkpoint outcome) and the
    event-type→enum resolver (exhaustive switch, no default fall-through — switch-exhaustiveness).
    Pure domain, imports only zod/@morai/shared + CalendarEventType. Write RED tests first: each
    enum accepts its own values incl 'other' and rejects a foreign value; resolver maps each
    CalendarEventType correctly. Run, confirm failure, implement.
  </action>
  <verify>
    <automated>bun run test -- packages/core/src/journal/domain/rule-tags.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Three event-keyed enums (each with 'other'); resolver maps OPEN/CLOSE/ROLL exhaustively;
    cross-type values rejected; no evaluation logic present.
  </acceptance_criteria>
</task>

<task type="execute" tdd="true">
  <name>Task 2: journal-rules request/response contracts (D-14/D-21)</name>
  <files>packages/contracts/src/journal-rules.ts, packages/contracts/src/journal-rules.test.ts</files>
  <read_first>
    - packages/contracts/src/stream-events.ts — the contracts-package schema/type-export idiom.
    - .planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-UI-SPEC.md § RULE-01 component contract (list-shaped, OTHER inline required, read-view fields).
    - .planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-RESEARCH.md § Pattern 3 (missing read surface → combined events+annotations response) + § Security Domain (bounds).
  </read_first>
  <behavior>
    - `setRuleTagsRequest`: `{ tags: z.array(<ruleTag>).max(N), otherNote?: z.string().max(280) }`, with `.refine` requiring a non-empty otherNote iff tags includes 'other' (D-21); listed values without a note pass. The tag element type is the per-event-type enum (route resolves which by the event's type).
    - `getEventsWithRulesResponse`: list-shaped — an array of events (id, eventType, eventedAt ISO, fillIdsHash, and the primary leg info the UI needs) each with its recorded `tags: string[]` + `otherNote: string | null`.
    - `setRuleTagsResponse`: the saved annotation (fillIdsHash, tags, otherNote, updatedAt ISO).
  </behavior>
  <action>
    Create journal-rules.ts contracts. Import the rule-tag enums from @morai/core (or re-export the
    value set) so the vocabulary is single-sourced. Bound `tags` with `.max` and `otherNote` with
    `.max(280)` (Security V5 / T-20-11). Implement the D-21 `.refine` on the request. Define the
    combined get-events-with-rules response (events + per-event tags/otherNote) — this is the read
    surface RESEARCH flags as MISSING today. Write RED tests first: OTHER-without-note rejected;
    listed-value-without-note accepted; OTHER-with-note accepted; oversized note/array rejected;
    response round-trips. Run, confirm failure, implement.
  </action>
  <verify>
    <automated>bun run test -- packages/contracts/src/journal-rules.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Request is list-shaped + bounded; OTHER-requires-note refine enforced (D-21); combined
    events+annotations response schema defined; enum vocabulary single-sourced from @morai/core.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→server (rule-tag write) | Contract shapes validate untrusted tag arrays + free-text before persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-11 | Tampering | ruleTags array / otherNote | high | mitigate | z.array(z.enum).max(N) for tags; z.string().max(280) for otherNote — never an unbounded/arbitrary client string (Security V5) |
| T-20-12 | Tampering | scope creep into a DSL | medium | mitigate | Enums are recording-only; no evaluation/condition logic (REQUIREMENTS Out of Scope; RULE-01 boundary) |
| T-20-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase (RESEARCH § Package Legitimacy Audit — N/A) |
</threat_model>

<verification>
- `bun run test -- packages/core/src/journal/domain/rule-tags.test.ts` green.
- `bun run test -- packages/contracts/src/journal-rules.test.ts` green.
- `bun run typecheck && bun run lint` clean for packages/core + packages/contracts.
</verification>

<success_criteria>
Locked, user-trimmed, event-keyed recording enums and a bounded, list-shaped, OTHER-requires-note
contract pair — the single-sourced vocabulary for the rest of RULE-01.
</success_criteria>

<output>
Create `.planning/phases/20-stream-watchdog-event-snapshot-strategy-rules/20-07-SUMMARY.md` when done.
Record the final locked enum values (from the decision checkpoint) in the SUMMARY.
</output>
