---
phase: 34-tos-parity-scenario-model
plan: 05
type: tdd
wave: 3
depends_on: [34-02, 34-04]
files_modified:
  - apps/web/src/lib/resolve-carry.ts
  - apps/web/src/lib/resolve-carry.test.ts
  - apps/web/src/screens/Overview.tsx
  - apps/web/src/screens/Overview.test.tsx
  - .planning/phases/34-tos-parity-scenario-model-close-the-t-0-gap-to-tos-analyze-f/34-UAT.md
autonomous: true
requirements: [TOSP-01, TOSP-02, TOSP-03, TOSP-04]
user_setup: []
must_haves:
  truths:
    - "resolveCarry looks up the per-expiry {rate, divYield} from the GEX snapshot's impliedCarry and degrades to DEFAULT_RATE/DEFAULT_DIV when the snapshot, the field, or the expiry entry is absent"
    - "The live-book payoff hero prices each calendar leg with its settlement-aware fractional DTE and its own resolved carry — buildCalendarPosition sets frontDteExact/backDteExact and the per-leg carry fields on every live AnalyzerPosition"
    - "The whole workspace suite, typecheck, and lint are green with the fractional-DTE + implied-carry changes integrated"
    - "The phase records the researched smile-IV decision (DO NOT BUILD, D-12) and leaves an RTH before/after BE-gap measurement hook for the orchestrator/user-driven UAT"
  artifacts:
    - apps/web/src/lib/resolve-carry.ts
    - apps/web/src/screens/Overview.tsx
    - .planning/phases/34-tos-parity-scenario-model-close-the-t-0-gap-to-tos-analyze-f/34-UAT.md
  key_links:
    - "resolveCarry reads the impliedCarry field shipped by plan 34-04 via useGex; buildCalendarPosition wires its output plus dteExact (plan 34-02) into the optional AnalyzerPosition fields the kernel (plan 34-02) resolves"
    - "front and back leg each look up their OWN expiry's carry, so a calendar's two legs price with two different (r, q) pairs when the chain implies them"
    - "when GEX has no impliedCarry (cold start / degraded), every leg falls back to DEFAULT_RATE/DEFAULT_DIV — byte-identical to today's behavior (Overview keeps DEFAULT for the ScenarioParams floor)"
---

<objective>
Integrate the two levers into the live-book payoff hero and close the phase: a small `resolve-carry.ts`
turns the GEX snapshot's per-expiry `impliedCarry` into a `{rate, divYield}` pair (degrading to the flat
defaults), `Overview.tsx`'s `buildCalendarPosition` sets the settlement-aware fractional DTE and per-leg
carry on every live calendar, and the phase records the researched smile-IV decision plus an RTH parity
measurement hook. Then prove the whole workspace is green.

Purpose: plans 34-01..34-04 built and tested every piece in isolation (settlement DTE, the kernel's
optional inputs, the parity solver, the server-side carry field). This plan is the wiring that makes the
live book actually price with exact time-to-settlement and parity-implied carry — the change the user
sees against TOS — while keeping the picker/pasted paths and the cold-start/degraded path byte-identical.
The smile-IV third item resolves to DO NOT BUILD (D-12): it is recorded, not implemented, with the
measured residual left as the follow-up trigger.

Output: `resolveCarry` + test; `buildCalendarPosition` wiring + a focused transform test; `34-UAT.md`
(smile decision record + before/after BE-gap table); full-suite + typecheck + lint gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-tos-parity-scenario-model-close-the-t-0-gap-to-tos-analyze-f/34-CONTEXT.md
@.planning/phases/34-tos-parity-scenario-model-close-the-t-0-gap-to-tos-analyze-f/34-RESEARCH.md
@apps/web/src/screens/Overview.tsx
@apps/web/src/lib/pair-calendars.ts
@apps/web/src/lib/scenario-engine.ts
@apps/web/src/hooks/useGex.ts
@packages/contracts/src/gex.ts
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: resolve-carry.ts — per-expiry {rate, divYield} lookup with DEFAULT fallback (RED→GREEN)</name>
  <files>apps/web/src/lib/resolve-carry.ts, apps/web/src/lib/resolve-carry.test.ts</files>
  <read_first>
    - packages/contracts/src/gex.ts — the `impliedCarry` field shape shipped by plan 34-04
      ([{expiration, rate, divYield}] | null on GexSnapshotResponse).
    - 34-RESEARCH.md "Code Examples → Per-calendar carry resolution" (the resolveCarry sketch and the
      DEFAULT_RATE/DEFAULT_DIV degrade targets). NOTE the delivery refinement (plan 34-04): the server
      now emits BOTH rate and divYield per expiry, so resolveCarry is a pure lookup — it does NOT
      interpolate FRED client-side (that would risk desyncing r from the q it was solved against).
    - apps/web/src/screens/Overview.tsx lines ~80-82 — the existing DEFAULT_RATE (0.045) / DEFAULT_DIV
      (0.013) constants that resolveCarry must degrade to (and that Overview will import from here).
  </read_first>
  <behavior>
    - resolveCarry(gex: GexSnapshotResponse | undefined, expiration: string): { rate: number; divYield: number }.
    - Hit: gex.impliedCarry has an entry for `expiration` → returns that {rate, divYield}.
    - Miss/degrade → { rate: DEFAULT_RATE, divYield: DEFAULT_DIV } when gex is undefined, impliedCarry is
      null, or no entry matches the expiration.
    - resolve-carry.ts exports DEFAULT_RATE and DEFAULT_DIV as the single source of the degrade targets.
    - No `any`/`as`/`!`; total function (always returns a pair).
  </behavior>
  <action>
    STEP 1 (RED): write resolve-carry.test.ts FIRST — a hit case (entry present → its {rate, divYield}),
    and the three degrade cases (gex undefined, impliedCarry null, expiration not found → DEFAULTs). RUN,
    confirm RED. STEP 2 (GREEN): implement resolve-carry.ts exporting `resolveCarry` + `DEFAULT_RATE`/
    `DEFAULT_DIV`, a pure lookup with the fallback. Run GREEN.
  </action>
  <verify>
    <automated>cd apps/web && bunx vitest run src/lib/resolve-carry.test.ts && cd ../.. && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - source: resolve-carry.ts exports `resolveCarry(gex, expiration)` + DEFAULT_RATE/DEFAULT_DIV; total, no `any`/`as`/`!`.
    - behavior: hit returns the per-expiry pair; gex undefined / impliedCarry null / no-match → DEFAULTs.
    - test-command: `cd apps/web && bunx vitest run src/lib/resolve-carry.test.ts` green; `bun run typecheck` clean.
  </acceptance_criteria>
  <done>resolveCarry maps the GEX impliedCarry field to a per-expiry carry pair, degrading to the flat defaults; pure and tested.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Overview buildCalendarPosition — wire fractional DTE + per-leg carry onto the live book (RED→GREEN)</name>
  <files>apps/web/src/screens/Overview.tsx, apps/web/src/screens/Overview.test.tsx</files>
  <read_first>
    - apps/web/src/screens/Overview.tsx — `buildCalendarPosition` (sets the AnalyzerPosition; add the new
      fields here), its call site in the `calendarPositions` useMemo (~line 865), the `useGex()` data
      (~line 794) that must reach buildCalendarPosition, the `now`/`new Date()` argument, and the local
      DEFAULT_RATE/DEFAULT_DIV (replace with the imports from resolve-carry.ts). Leave `resolveLeg`/
      `resolveLegIv` (IV calibration) and the ScenarioParams carry UNCHANGED — the re-pricing carry
      flows via the AnalyzerPosition fields only (RESEARCH scope; the calibration path is untouched).
    - apps/web/src/lib/pair-calendars.ts — `dteExact(occSymbol, now)` (plan 34-02).
    - apps/web/src/lib/resolve-carry.ts — `resolveCarry` + DEFAULT_RATE/DEFAULT_DIV (task 1).
    - packages/shared — `parseOccSymbol` for each leg's expiry date; format it to YYYY-MM-DD (UTC) to
      match the chain's `expiration` key used by impliedCarry.
    - 34-RESEARCH.md Pitfall 4 (picker/pasted construction sites are a DIFFERENT path — they do NOT get
      these fields this phase, by design).
  </read_first>
  <behavior>
    - buildCalendarPosition gains a gex param (GexSnapshotResponse | undefined) threaded from useGex.
    - For each live calendar it sets on the AnalyzerPosition: frontDteExact = dteExact(cal.front.occSymbol,
      now), backDteExact = dteExact(cal.back.occSymbol, now); and per-leg carry from resolveCarry(gex,
      frontExpiryYmd) / resolveCarry(gex, backExpiryYmd) → frontRate/frontDivYield/backRate/backDivYield.
    - Expiry keys derive from parseOccSymbol(leg.occSymbol).expiry formatted YYYY-MM-DD (UTC); a parse
      failure leaves that leg's exact/carry fields at the resolveCarry default (never throws).
    - Display DTE (frontDte/backDte = cal.dteFront/dteBack) and everything else in the position are
      unchanged (integer badges stay — CONTEXT "integer display can stay").
    - Transform test: given a stub calendar + a stub gex whose impliedCarry covers both expiries, the
      built AnalyzerPosition carries the four carry fields (matching the stub) and both exact-DTE fields
      (fractional, ≠ the integer dte); given gex undefined, the carry fields equal DEFAULT_RATE/DEFAULT_DIV.
  </behavior>
  <action>
    STEP 1 (RED): add a focused transform test in Overview.test.tsx (export buildCalendarPosition if it is
    currently module-private — it is a pure data transform, exporting it is the least-invasive way to test
    it per .claude/rules/tdd.md "UI data transforms"). Assert the built position carries the fractional
    frontDteExact/backDteExact and the per-leg carry from a stub gex, and falls back to DEFAULTs when gex
    is undefined. RUN — confirm RED (fields not set / param absent). STEP 2 (GREEN): thread gex into
    buildCalendarPosition at its call site; compute the front/back expiry YYYY-MM-DD via parseOccSymbol;
    set the six optional fields via dteExact + resolveCarry; replace Overview's local DEFAULT_RATE/
    DEFAULT_DIV with the imports from resolve-carry.ts (keep them as the ScenarioParams floor and for the
    unchanged resolveLeg/netGreeksForLegs call sites). Do NOT touch resolveLegIv, the ScenarioParams
    construction, or the picker/pasted paths. Run GREEN + the full Overview suite. No `any`/`as`/`!`.
  </action>
  <verify>
    <automated>cd apps/web && bunx vitest run src/screens/Overview.test.tsx && cd ../.. && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - source: buildCalendarPosition sets frontDteExact/backDteExact + per-leg carry from dteExact + resolveCarry; gex threaded from useGex; resolveLegIv/ScenarioParams/picker paths untouched; no `any`/`as`/`!`.
    - behavior: built live position carries fractional DTE + stubbed per-leg carry; gex-undefined falls back to DEFAULTs.
    - source: Overview imports DEFAULT_RATE/DEFAULT_DIV from resolve-carry.ts (single source).
    - test-command: `cd apps/web && bunx vitest run src/screens/Overview.test.tsx` green; `bun run typecheck` clean.
  </acceptance_criteria>
  <done>The live-book payoff hero prices each leg with settlement-aware fractional DTE and its own parity-implied carry, degrading to defaults; display integers and out-of-scope paths unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Record smile-IV decision + RTH BE-gap measurement hook; full-suite gate</name>
  <files>.planning/phases/34-tos-parity-scenario-model-close-the-t-0-gap-to-tos-analyze-f/34-UAT.md</files>
  <read_first>
    - 34-CONTEXT.md item 3 (smile-aware IV = RESEARCHED DECISION) and the "Hard requirements" UAT gate
      (RTH, live marks, BE-today within a few points of TOS; record before/after).
    - 34-RESEARCH.md Summary + Open Questions #2 + D-12 + State of the Art (smile-aware IV = DO NOT BUILD:
      TOS's default "Individual Implied Volatility" already matches this single-strike-per-calendar book).
    - .planning/phases/33-*/ for the SUMMARY/doc tone; this is a phase artifact under .planning/, not a
      docs/architecture change (no docs-before-code trigger — no new bounded context/table/tech swap that
      docs/architecture must record; the additive GEX field is documented by its Zod contract).
  </read_first>
  <action>
    Create 34-UAT.md with two sections. SECTION 1 "Researched deferral — smile-aware scenario IV (item 3,
    D-12)": record that it is DO NOT BUILD, with the one-paragraph rationale (TOS's default/recommended
    "Individual Implied Volatility" mode holds each series' own IV fixed as spot moves — exactly what the
    engine already does for a single-strike-per-calendar book; smile interpolation would only matter for
    multi-strike books) and the follow-up TRIGGER (revisit only if the UAT-measured residual is
    demonstrably vol-attributable). SECTION 2 "RTH BE-today parity measurement (UAT gate)": a table with
    columns Book | BE-today TOS (low/high) | BE-today ours BEFORE | BE-today ours AFTER | gap AFTER
    (points), pre-filled with the CONTEXT baseline (TOS 7413.21 / 7690.62 vs ours 7421 / 7673 = 8 and 18
    points BEFORE) and empty AFTER cells for the orchestrator/user to fill during the live-marks RTH
    comparison. State explicitly that running the UAT is orchestrator/user-driven (via /gsd-verify-work
    34), not an executor step, and that AH comparisons are informative-only. Then run the full workspace
    gate to prove the integrated phase is green.
  </action>
  <verify>
    <automated>bun run test && bun run typecheck && bun run lint</automated>
  </verify>
  <acceptance_criteria>
    - docs: 34-UAT.md records smile-IV = DO NOT BUILD (D-12) with rationale + revisit trigger (TOSP-03), and an RTH before/after BE-gap table seeded with the CONTEXT baseline and empty AFTER cells (TOSP-04).
    - test-command: `bun run test` (full workspace) green; `bun run typecheck` and `bun run lint` clean.
  </acceptance_criteria>
  <done>Smile-IV decision recorded and the RTH parity measurement hook is in place; the whole integrated phase is green (test + typecheck + lint).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GET /api/analytics/gex → browser (useGex) | The impliedCarry field crosses into the client via the existing authenticated read; consumed read-only, no new input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-05 | Tampering | resolveCarry over a malformed/absent impliedCarry | low | mitigate | resolveCarry is total — a missing snapshot, null field, or unmatched expiry returns DEFAULT_RATE/DEFAULT_DIV; the contract is Zod-parsed by useGex before it reaches resolveCarry, so a malformed field never enters as an unvalidated shape |
| T-34-SC | Tampering | package installs | low | accept | No package-manager installs this phase — no supply-chain surface |
</threat_model>

<verification>
- `cd apps/web && bunx vitest run src/lib/resolve-carry.test.ts src/screens/Overview.test.tsx` green.
- Built live AnalyzerPosition carries fractional frontDteExact/backDteExact + per-leg carry; gex-undefined degrades to DEFAULTs.
- 34-UAT.md records the smile-IV DO-NOT-BUILD decision (D-12) and the RTH before/after BE-gap table.
- `bun run test` (full workspace) + `bun run typecheck` + `bun run lint` all green.
</verification>

<success_criteria>
- The live-book T+0 line prices with settlement-aware fractional DTE and parity-implied per-leg carry; picker/pasted/cold-start paths byte-identical.
- The researched smile-IV decision is recorded (DO NOT BUILD, D-12) with a measured-residual revisit trigger.
- The RTH BE-today parity measurement hook is seeded for the orchestrator/user-driven UAT gate.
- The whole workspace suite, typecheck, and lint are green.
</success_criteria>

<output>
Create `.planning/phases/34-tos-parity-scenario-model-close-the-t-0-gap-to-tos-analyze-f/34-05-SUMMARY.md` when done
</output>
