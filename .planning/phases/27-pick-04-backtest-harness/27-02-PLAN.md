---
phase: 27-pick-04-backtest-harness
plan: 02
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - packages/core/src/picker/index.ts
  - packages/core/src/exits/index.ts
  - packages/core/src/journal/index.ts
  - packages/core/src/index.ts
  - packages/core/src/picker/domain/scoring.ts
  - packages/core/src/picker/domain/scoring.test.ts
  - packages/core/src/journal/application/snapshotCalendars.ts
  - packages/core/src/journal/application/snapshotCalendars.test.ts
  - packages/core/src/backtest/reuse-exports.test.ts
autonomous: true
requirements: [BT-01, BT-04]

must_haves:
  truths:
    - "Every live domain function the backtest must reuse (selectCandidates, haircutFill, scoreCalendarCandidates, evaluateExit, the rule/exit registries, realizedVol, rankAndCapCandidates) is importable from @morai/core"
    - "scoreCalendarCandidates accepts an optional per-rule weights override; omitting it reproduces today's live score byte-identically; setting a rule's weight to 0 zeroes ONLY that rule's contribution (the ablation seam)"
    - "computeLegPairMetrics is an exported pure function producing the same leg-pair metrics buildSnapshotRow already computes, so a hypothetical (never-traded) candidate can be priced without a Calendar row"
  artifacts:
    - "packages/core/src/backtest/reuse-exports.test.ts — a reachability guard importing every reused symbol from @morai/core"
    - "ScoringParams.weights?: Partial<Record<BreakdownCriterion, number>> in scoring.ts"
    - "computeLegPairMetrics exported from journal, threaded to @morai/core"
  key_links:
    - "scoreOne substitutes params.weights?.X ?? WEIGHT_X for all 9 weights in BOTH the breakdown[] push and the rawScore sum — no live call site passes weights, so behavior is unchanged"
    - "buildSnapshotRow is refactored to call computeLegPairMetrics — the live snapshot writer's output is unchanged (snapshotCalendars tests stay green)"
---

<objective>
Build the reuse seams the backtest depends on, entirely inside `packages/core`, with zero live-behavior change. Three additive moves: (1) thread the picker + exit domain functions the harness must reuse through the package barrels so they are reachable from `@morai/core` (the "export, never reimplement" lock); (2) add the ablation seam — an optional per-rule `weights` override on `ScoringParams`, defaulting per-criterion to the existing `rules.ts` constants; (3) extract `computeLegPairMetrics` out of `snapshotCalendars.ts`'s private `buildSnapshotRow` so a hypothetical candidate's leg-pair metrics can be computed without a `Calendar` row.

Every change is additive or optional-parameter — no existing call site changes, so `scoring.test.ts`, `rules.test.ts`, `computePickerSnapshot.test.ts`, and `snapshotCalendars.test.ts` must all stay green (the regression gate that proves live behavior is untouched).

Purpose: satisfy BT-01's "SAME pure functions" lock and provide BT-04's ablation seam without a rewrite.
Output: barrel exports + `ScoringParams.weights?` + `computeLegPairMetrics`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-pick-04-backtest-harness/27-CONTEXT.md
@.planning/phases/27-pick-04-backtest-harness/27-RESEARCH.md

# The exact symbols + seams to touch (read the Required Additive Exports table + Patterns 2 & 5 in RESEARCH)
@packages/core/src/picker/index.ts
@packages/core/src/exits/index.ts
@packages/core/src/picker/domain/scoring.ts
@packages/core/src/journal/application/snapshotCalendars.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread the reused picker + exit symbols through the barrels to @morai/core</name>
  <files>packages/core/src/picker/index.ts, packages/core/src/exits/index.ts, packages/core/src/journal/index.ts, packages/core/src/index.ts, packages/core/src/backtest/reuse-exports.test.ts</files>
  <read_first>
    - .planning/phases/27-pick-04-backtest-harness/27-RESEARCH.md → "Required Additive Exports (Wave 0 prerequisite)" table (the authoritative symbol/source/thread-to list)
    - packages/core/src/picker/index.ts and packages/core/src/exits/index.ts (the existing barrel export style)
  </read_first>
  <action>
    Thread every symbol in RESEARCH's Required Additive Exports table up to @morai/core, following the existing barrel style. From picker: selectCandidates + SelectCandidatesParams + SelectCandidatesResult + GateDrops, haircutFill, scoreCalendarCandidates + ScoringParams, RULE_SET_METADATA + RuleMetadata, the types (RawCandidate, ScoredCandidate, BreakdownEntry, BreakdownCriterion, ContextEntry, ExitPlan), realizedVol, rankAndCapCandidates + PICKER_TOP_N — via picker/index.ts → core/index.ts. From exits: evaluateExit (the pure fn) + the exit-rules constants (EXIT_RULE_METADATA, EXIT_PRECEDENCE, TAKE_RUNGS, STOP_RUNGS, TERM_INVERSION_MIN/DISARM, GAMMA_* constants, EVT_BLACKOUT_DAYS, ROLL_* constants) — via exits/index.ts → core/index.ts (the exits barrel currently only re-threads the use-case factories, not the pure fn/constants). Add a reachability guard packages/core/src/backtest/reuse-exports.test.ts that imports each symbol from @morai/core and asserts it is defined (a function/constant of the expected typeof) — this is the cheap regression that catches a missed barrel thread before 03/05 need it. Pure barrel wiring only — no logic. (journal/index.ts is listed for Task 3's computeLegPairMetrics thread; leave it for Task 3.)
  </action>
  <verify>
    <automated>bun run vitest run packages/core/src/backtest/reuse-exports.test.ts && bun run typecheck</automated>
  </verify>
  <done>Every reused symbol imports cleanly from @morai/core and the reachability guard passes; typecheck clean; no live call site changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ablation seam — optional per-rule weights override on ScoringParams</name>
  <files>packages/core/src/picker/domain/scoring.ts, packages/core/src/picker/domain/scoring.test.ts</files>
  <read_first>
    - packages/core/src/picker/domain/scoring.ts (scoreOne — the 9 WEIGHT_* constants in BOTH the breakdown[] push and the rawScore sum; ScoringParams currently has no weights field)
    - .planning/phases/27-pick-04-backtest-harness/27-RESEARCH.md → "Pattern 2: Ablation seam" (the exact ?? substitution)
  </read_first>
  <behavior>
    - Omitting weights entirely (every live call site) reproduces the exact same ScoredCandidate.score and breakdown[] as before the change — a regression fixture over a representative RawCandidate asserts byte-identical output.
    - Passing weights: { slope: 0 } zeroes ONLY the slope criterion's contribution to rawScore and its breakdown weight, leaving all other criteria's contributions unchanged, and lowers (or holds) the total score.
    - Passing weights for a criterion not in the map falls back to that criterion's rules.ts constant (per-criterion ?? fallback, not all-or-nothing).
  </behavior>
  <action>
    Add readonly weights?: Partial<Record<BreakdownCriterion, number>> to ScoringParams with a doc comment naming it the PICK-04 ablation seam. In scoreOne, substitute params.weights?.slope ?? WEIGHT_SLOPE (and the analogous ?? for the other 8 weight constants) in BOTH the breakdown[] entries' weight field and the rawScore sum — nowhere else. Do NOT change any live call site (computePickerSnapshot.ts passes no weights, so its behavior is byte-identical). RED first: add the regression fixture + the slope-ablation test to scoring.test.ts, run, confirm the ablation test fails for the right reason (no weights field yet); then GREEN. Keep the existing scoring.test.ts / rules.test.ts / computePickerSnapshot.test.ts assertions untouched and green (the live-behavior-unchanged proof).
  </action>
  <verify>
    <automated>bun run vitest run packages/core/src/picker</automated>
  </verify>
  <done>scoreCalendarCandidates accepts an optional weights override; omitting it is byte-identical to today; weights:{slope:0} zeroes only slope; every pre-existing picker test stays green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extract computeLegPairMetrics from buildSnapshotRow</name>
  <files>packages/core/src/journal/application/snapshotCalendars.ts, packages/core/src/journal/application/snapshotCalendars.test.ts, packages/core/src/journal/index.ts, packages/core/src/index.ts</files>
  <read_first>
    - packages/core/src/journal/application/snapshotCalendars.ts (the private buildSnapshotRow — the leg-pair metrics formula: netMark, NaN-propagation, netGreek, termSlope, dteFront/dteBack, spot, source)
    - .planning/phases/27-pick-04-backtest-harness/27-RESEARCH.md → "Pattern 5: Hypothetical-position MarketContext assembly" (the exact extraction signature + which fields stay in buildSnapshotRow)
  </read_first>
  <behavior>
    - computeLegPairMetrics(now, front, back, qty, frontExpiry, backExpiry) returns the leg-pair metric fields (everything buildSnapshotRow computes EXCEPT calendarId, pnlOpen, trigger) with identical NaN-stamp propagation.
    - buildSnapshotRow, refactored to call computeLegPairMetrics and then add calendarId/pnlOpen/trigger, produces a SnapshotRow byte-identical to before the extraction — the existing snapshotCalendars tests are the parity proof and stay green.
  </behavior>
  <action>
    Extract the pure leg-pair-metrics computation out of buildSnapshotRow into an exported computeLegPairMetrics(now, front: LegSnapshot | null, back: LegSnapshot | null, qty, frontExpiry, backExpiry) returning Omit<SnapshotRow, "calendarId" | "pnlOpen" | "trigger"> — the exact body currently inside buildSnapshotRow minus the pnlOpen/calendarId/trigger fields (which depend on a Calendar object a hypothetical candidate lacks). Refactor buildSnapshotRow to call computeLegPairMetrics(now, front, back, cal.qty, cal.frontExpiry, cal.backExpiry) then attach calendarId := cal.id, pnlOpen := computeSnapshotPnl(...) over the returned netMark, and trigger — behavior byte-identical. Thread computeLegPairMetrics through journal/index.ts → core/index.ts. RED first: add a parity test asserting buildSnapshotRow's output is unchanged AND computeLegPairMetrics is directly callable with literal LegSnapshots; then GREEN. Keep every existing snapshotCalendars.test.ts assertion green.
  </action>
  <verify>
    <automated>bun run vitest run packages/core/src/journal/application/snapshotCalendars.test.ts</automated>
  </verify>
  <done>computeLegPairMetrics is exported and reachable from @morai/core; buildSnapshotRow's output is unchanged; snapshotCalendars tests stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backtest reuse ↔ live picker/exit domain | the harness reuses the same pure functions; a seam that changes live behavior would silently corrupt live picks |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-03 | Tampering | scoring.ts ablation seam | high | mitigate | weights is optional with per-criterion ?? fallback to the rules.ts constant; NO live call site passes it; byte-identical regression fixture + the untouched scoring/rules/computePickerSnapshot suites prove live behavior unchanged |
| T-27-04 | Tampering | buildSnapshotRow extraction | medium | mitigate | pure refactor-extract; the existing snapshotCalendars tests are the parity gate — a changed SnapshotRow fails them |
| T-27-SC | Tampering | package installs | high | accept | zero new packages this plan |
</threat_model>

<verification>
- `bun run vitest run packages/core/src/picker packages/core/src/journal/application/snapshotCalendars.test.ts packages/core/src/backtest/reuse-exports.test.ts` all green.
- `bun run test` (full suite) green — the additive-export/optional-param changes touch shared live-path files and must not regress rules.test.ts / scoring.test.ts / computePickerSnapshot.test.ts / evaluate-exit.test.ts / computeExitAdvice.test.ts.
- `bun run typecheck` and `bun run lint` clean.
</verification>

<success_criteria>
- Every reused live function is importable from @morai/core.
- The ablation seam works and omitting it is byte-identical to today's live scoring.
- computeLegPairMetrics is extracted with zero change to the live snapshot writer's output.
</success_criteria>

<output>
Create `.planning/phases/27-pick-04-backtest-harness/27-02-SUMMARY.md` when done.
</output>
