---
phase: 27-pick-04-backtest-harness
plan: 06
type: execute
wave: 6
depends_on: [27-05]
files_modified:
  - packages/core/src/backtest/application/runBacktest.ts
  - packages/core/src/backtest/application/runBacktest.test.ts
  - packages/core/src/backtest/application/ports.test.ts
  - packages/core/src/backtest/index.ts
  - apps/worker/src/backtest.ts
  - apps/worker/src/backtest.test.ts
autonomous: true
requirements: [BT-04, BT-05]

must_haves:
  truths:
    - "runBacktest reduces the three replay paths into one BacktestReport where every headline number is stamped with n= + date range + coverage %, includes per-rule directional attribution + leave-one-rule-out ablation + a bootstrap CI, and flags the late-BSM and economic-events leakage caveats"
    - "The operator can run bun run apps/worker/src/backtest.ts --from 2026-06-12 --to <date>, connecting with DATABASE_URL only, and get a persisted report + a console summary"
    - "A --from earlier than 2026-06-12 (before the replayable corpus) is rejected"
    - "No file or port anywhere in the backtest module tree writes weights or the rule registries — the harness's only write is backtest_runs (BT-05)"
  artifacts:
    - "packages/core/src/backtest/application/runBacktest.ts — orchestrator + report assembly + single persist"
    - "apps/worker/src/backtest.ts — DATABASE_URL-only CLI composition root"
    - "packages/core/src/backtest/application/ports.test.ts — the BT-05 no-write-path structural guard"
  key_links:
    - "runBacktest calls the 04 kernel fns (directionalAttribution, ablationDelta, bootstrapCi, coveragePercent) over the 05 replay outcomes, then persists ONCE via ForPersistingBacktestRun"
    - "the CLI parses DATABASE_URL only (NOT bootWorkerConfig — the backtest does zero brokerage I/O) and Zod-validates argv (YYYY-MM-DD round-trip, UUID --calendar)"
    - "the BT-05 guard scans the backtest source tree via import.meta.glob (never node:fs — forbidden in packages/core, per the 26-04 lesson)"
---

<objective>
Close the harness: the `runBacktest` orchestrator that reduces the three replay paths into a single honest `BacktestReport` (every number stamped, attribution + ablation + bootstrap CI + coverage, caveats flagged) and persists it once; the `apps/worker/src/backtest.ts` operator CLI (DATABASE_URL-only, Zod argv, `import.meta.main`); and the BT-05 structural guard proving no path in the backtest tree can write weights or rule registries.

This is where the phase goal becomes runnable: an operator replays stored history and reads directional evidence — never a weight the machine tuned.

Purpose: BT-04's persisted, stamped report + BT-05's never-writes-weights guarantee, behind a runnable CLI.
Output: runBacktest.ts, apps/worker/src/backtest.ts, ports.test.ts (BT-05 guard).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-pick-04-backtest-harness/27-CONTEXT.md
@.planning/phases/27-pick-04-backtest-harness/27-RESEARCH.md

# CLI + guard precedents (read the CLI env-bootstrap finding + the EXIT-10 import.meta.glob guard lesson)
@apps/worker/src/backfill-transactions.ts
@apps/worker/src/fix-pnl-reingest.ts
@packages/core/src/exits/application/computeExitAdvice.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: runBacktest orchestrator — report assembly + single persist</name>
  <files>packages/core/src/backtest/application/runBacktest.ts, packages/core/src/backtest/application/runBacktest.test.ts, packages/core/src/backtest/index.ts</files>
  <read_first>
    - .planning/phases/27-pick-04-backtest-harness/27-RESEARCH.md → System Architecture Diagram (the runBacktest reduce → report → persist flow)
    - packages/core/src/backtest/domain/types.ts (the BacktestReport shape from 27-01)
    - .planning/phases/27-pick-04-backtest-harness/27-CONTEXT.md → "Report content (BT-04)" (sign+n attribution, ablation rank/outcome deltas, bootstrap CI on every headline metric, every number n=+date-range+coverage%)
  </read_first>
  <behavior>
    - runBacktest(deps, params) runs the three replay paths (05) over the in-range cohorts, reduces the outcomes with the 04 kernel fns (directionalAttribution per rule, ablationDelta per leave-one-out rule, bootstrapCi on each headline metric, coveragePercent per day), and assembles a BacktestReport where EVERY headline number carries { n, dateRange, coveragePct }.
    - The report includes: the leakage-oracle result (reproduced/mismatched/registry-drift counts), the 13-trade direction+magnitude reproductions, per-rule directional attribution (sign+n), the leave-one-rule-out ablation table, and P&L reported as a mid→haircut RANGE (never a bare mid).
    - The report explicitly flags the two residual caveats (late-solved-BSM optimism; economic-events reflect the current calendar, no discoveredAt column).
    - runBacktest persists the report EXACTLY ONCE via ForPersistingBacktestRun and never calls any weight/rule write path.
  </behavior>
  <action>
    Implement makeRunBacktestUseCase(deps) → the driver port. Orchestrate: enumerate cohorts (readPickerSnapshotsInRange), run replayPickerCohort/replayExitsForCalendar/replayHypotheticalEntry (05), feed outcomes to the 04 kernel fns, and assemble the BacktestReport with every number stamped n=+dateRange+coveragePct and both caveats present. Persist once via deps.persistBacktestRun. Unit test with the in-memory twins + the memory backtest-runs repo (no testcontainers needed here — the replay fns are covered in 05): assert the report shape, that every headline metric carries n/dateRange/coveragePct, that attribution is sign+n, that both caveats are present, that P&L is a range, and that persist is called exactly once. RED → GREEN.
  </action>
  <verify>
    <automated>bun run vitest run packages/core/src/backtest/application/runBacktest.test.ts</automated>
  </verify>
  <done>runBacktest assembles a fully-stamped report (n/dateRange/coverage on every headline number, attribution + ablation + CI + caveats) and persists it exactly once.</done>
</task>

<task type="auto">
  <name>Task 2: apps/worker/src/backtest.ts operator CLI (DATABASE_URL-only, Zod argv)</name>
  <files>apps/worker/src/backtest.ts, apps/worker/src/backtest.test.ts</files>
  <read_first>
    - apps/worker/src/backfill-transactions.ts (the CLI composition-root shape + the YYYY-MM-DD Zod regex + round-trip date guard for --from/--to)
    - .planning/phases/27-pick-04-backtest-harness/27-RESEARCH.md → "CLI env bootstrap — DATABASE_URL only, NOT bootWorkerConfig()" code block (the one deliberate deviation + why)
    - packages/adapters/src/postgres/db.ts (makeDb signature)
  </read_first>
  <action>
    Write apps/worker/src/backtest.ts as a composition root mirroring backfill-transactions.ts EXCEPT env parsing: a minimal z.object({ DATABASE_URL: z.string().url() }).parse(process.env) — NOT bootWorkerConfig (the backtest does zero brokerage I/O; forcing four Schwab secrets is friction CONTEXT explicitly guards against). Zod-parse argv: --from/--to via the same YYYY-MM-DD regex + round-trip-date guard backfill-transactions uses, --calendar validated UUID-shaped (optional), --report-only (optional). Reject a --from earlier than 2026-06-12 (the documented start of the replayable leg_observations corpus) with a loud error (self-inflicted-DoS guard). Under import.meta.main: parse env+argv, makeDb(DATABASE_URL), compose the backtest-owned repos (backtest-chain, backtest-history, calendar-snapshots full-history, backtest-runs) + the reused journal ports (listCalendars, readCalendarEvents), call makeRunBacktestUseCase, and console.warn a human-readable summary (n=, date range, coverage %, oracle reproduced/mismatched, attribution signs, ablation deltas). No pg-boss, no sidecar, no Schwab adapters. This is composition-root glue (exempt from TDD per tdd.md); the argv/env parsing gets a small unit test (rejects bad dates + --from<2026-06-12; accepts a valid range).
  </action>
  <verify>
    <automated>bun run vitest run apps/worker/src/backtest.test.ts && bun run typecheck</automated>
  </verify>
  <done>The CLI parses DATABASE_URL only + Zod-validated argv, rejects --from before 2026-06-12 and malformed dates, composes the backtest repos, runs the use-case, and prints a stamped summary; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 3: BT-05 no-write-path structural guard</name>
  <files>packages/core/src/backtest/application/ports.test.ts</files>
  <read_first>
    - packages/core/src/exits/application/computeExitAdvice.test.ts (the EXIT-10 import.meta.glob static-scan guard — the precedent; node:fs is forbidden in packages/core, per the 26-04 lesson)
    - .planning/phases/27-pick-04-backtest-harness/27-RESEARCH.md → BT-05 test-map row (no ForWriting*Rules-shaped port anywhere in the backtest tree)
  </read_first>
  <action>
    Add packages/core/src/backtest/application/ports.test.ts: a static structural guard scanning the whole backtest module source tree via import.meta.glob (with the same minimal local ImportMeta.glob ambient augmentation the exits EXIT-10 guard uses — never node:fs, which the architecture-boundaries lint blocks in packages/core incl. test files). Assert that no driven port whose name matches the write-rule-weights shape (a ForWriting…Rules / ForPersisting…RuleWeights pattern) exists anywhere under packages/core/src/backtest/, and that no backtest source imports a write path into the picker/exit rule registries (rules.ts / exit-rules.ts). Also assert the backtest_runs repo surface exposes no update/delete method (re-affirming 27-01's append-only property from the port side). This is the BT-05 guarantee as an executable test.
  </action>
  <verify>
    <automated>bun run vitest run packages/core/src/backtest/application/ports.test.ts</automated>
  </verify>
  <done>The guard passes: no weight/rule write port or import exists anywhere in the backtest tree; the harness's only write capability is backtest_runs (append-only).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator argv/env → CLI | untrusted date range + calendar id + connection string cross into the tool |
| backtest evidence → rule registry | the harness must NEVER be able to promote a weight; a human reads the evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-13 | Elevation of Privilege (weight-write creep) | backtest module tree | high | mitigate | BT-05 structural guard (import.meta.glob scan) asserts no ForWriting*Rules port + no import into rules.ts/exit-rules.ts; the only write is append-only backtest_runs |
| T-27-14 | Tampering (input validation) | CLI argv/env | medium | mitigate | Zod-parse DATABASE_URL + argv; YYYY-MM-DD round-trip guard; UUID --calendar; reject --from before the corpus start |
| T-27-15 | Denial of Service (self-inflicted, wide range) | CLI replay | low | mitigate | reject --from < 2026-06-12; stream per-cohort (never load the whole range into memory) |
| T-27-16 | Repudiation (false precision at n=13) | runBacktest report | high | mitigate | every headline number stamped n=+date-range+coverage%; bootstrap CI + directional-only framing per the report's own honesty rule |
| T-27-SC | Tampering | package installs | high | accept | zero new packages |
</threat_model>

<verification>
- `bun run vitest run packages/core/src/backtest/application/runBacktest.test.ts apps/worker/src/backtest.test.ts packages/core/src/backtest/application/ports.test.ts` all green.
- `bun run test` (full suite) green; `bun run typecheck` / `bun run lint` clean.
- The CLI runs under import.meta.main with DATABASE_URL only (no Schwab secrets required).
- Migration 0021 applied to the target DB (`bun run migrate`) before a real run; the CLI is a worker artifact — push repo so the worker image carries it, no job-behavior change (deploy note, 27-CONTEXT.md).
</verification>

<success_criteria>
- The operator can run the CLI and get a persisted, fully-stamped report + console summary (BT-04).
- No path in the backtest tree can write weights or rule registries (BT-05).
- Every headline report number carries n= + date range + coverage %; the late-BSM and events caveats are flagged.
</success_criteria>

<output>
Create `.planning/phases/27-pick-04-backtest-harness/27-06-SUMMARY.md` when done.
</output>
