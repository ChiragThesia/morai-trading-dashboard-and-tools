---
phase: 27-pick-04-backtest-harness
plan: 03
type: execute
wave: 3
depends_on: [27-02]
files_modified:
  - packages/adapters/src/postgres/repos/backtest-chain.ts
  - packages/adapters/src/postgres/repos/backtest-chain.contract.test.ts
  - packages/adapters/src/postgres/repos/backtest-history.ts
  - packages/adapters/src/postgres/repos/backtest-history.contract.test.ts
  - packages/adapters/src/postgres/repos/calendar-snapshots.ts
  - packages/adapters/src/postgres/repos/calendar-snapshots.contract.test.ts
  - packages/adapters/src/memory/backtest-chain.ts
  - packages/adapters/src/memory/backtest-history.ts
  - packages/adapters/src/memory/calendar-snapshots.ts
  - packages/adapters/src/__contract__/backtest-chain.contract.ts
  - packages/adapters/src/__contract__/backtest-history.contract.ts
autonomous: true
requirements: [BT-01, BT-02, BT-03]

must_haves:
  truths:
    - "The as-of-T chain read returns the newest cohort at or before T and a future-dated leg_observations row NEVER changes a past-T read's result (BT-01 no-lookahead — the required check)"
    - "The as-of-T RV20 read returns only daily closes at or before T (no future close leaks into a trailing-window stat)"
    - "readFullSnapshotHistoryForCalendar returns schwab_chain-sourced rows (regression guard for the mapSnapshotRow drop bug) for every calendar regardless of status"
    - "readPickerSnapshotsInRange enumerates the picker_snapshot.observed_at cohorts in [from,to] — the canonical 'a live cycle actually ran' ledger for the replay walk-forward"
  artifacts:
    - "packages/adapters/src/postgres/repos/backtest-chain.ts — as-of-T union/dedup full-column chain read + memory twin"
    - "packages/adapters/src/postgres/repos/backtest-history.ts — readDailySpotClosesAsOf + readPickerSnapshotsInRange + memory twin"
    - "readFullSnapshotHistoryForCalendar added to calendar-snapshots.ts with a source-inclusive mapper"
  key_links:
    - "the as-of-T chain query adds exactly one predicate — lte(legObservations.time, asOfT) — to the existing picker-chain two-step latest-cohort pattern"
    - "readFullSnapshotHistoryForCalendar reuses readLatestSnapshotPerOpenCalendar's source-inclusive mapping (never mapSnapshotRow's source!=='cboe' → null drop)"
---

<objective>
Build the three point-in-time reads the replay engine needs — none exist today because every current chain/history read resolves the LATEST state relative to wall-clock now. All three implement driven ports declared in 27-01's `backtest/application/ports.ts` (plus one additive method on the existing calendar-snapshots repo), each with an in-memory twin and a testcontainers contract test.

The load-bearing test here is BT-01's own required check: a future-dated `leg_observations` row must NOT change a past-T read's result. That single lookahead-refusal test is the mechanical proof that the harness reads history as history.

Purpose: give the replay engine a chain slice as-of-T, an RV20 close series as-of-T, the cohort ledger (`picker_snapshot.observed_at` in range), and a source-inclusive full snapshot history (dodging the `mapSnapshotRow` schwab_chain drop bug).
Output: backtest-chain.ts, backtest-history.ts, readFullSnapshotHistoryForCalendar — repos + twins + contract tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-pick-04-backtest-harness/27-CONTEXT.md
@.planning/phases/27-pick-04-backtest-harness/27-RESEARCH.md

# The exact existing read patterns to generalize (read the "New reads needed" table + as-of-T query pattern in RESEARCH)
@packages/adapters/src/postgres/repos/picker-chain.ts
@packages/adapters/src/postgres/repos/picker-history.ts
@packages/adapters/src/postgres/repos/calendar-snapshots.ts
@packages/adapters/src/postgres/repos/exit-verdicts.contract.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: As-of-T union/dedup chain read (BT-01 no-lookahead)</name>
  <files>packages/adapters/src/postgres/repos/backtest-chain.ts, packages/adapters/src/postgres/repos/backtest-chain.contract.test.ts, packages/adapters/src/memory/backtest-chain.ts, packages/adapters/src/__contract__/backtest-chain.contract.ts</files>
  <read_first>
    - packages/adapters/src/postgres/repos/picker-chain.ts (readChainForPicker — the two-step MAX(time WHERE bsm_iv NOT NULL) + 10-min lookback + selectDistinctOn union/dedup pattern to generalize)
    - packages/adapters/src/postgres/gex-snapshot.repo.ts (readLegObsForGex — the per-contract-latest deduped dual-source union semantics)
    - .planning/phases/27-pick-04-backtest-harness/27-RESEARCH.md → "As-of-T chain query pattern" code block (the single added predicate + the full column set to SELECT)
  </read_first>
  <behavior>
    - Given leg_observations rows at times T1 < T2 (both ≤ asOfT), readChainAsOf(asOfT) resolves the cohort at T2 (newest at-or-before T), with the same per-30-min-slot union + per-contract-newest-wins dedup the live readers use.
    - Seeding a NEW row dated AFTER asOfT and re-running readChainAsOf(asOfT) returns the IDENTICAL result — the future row is invisible (BT-01 no-lookahead: this is the required test).
    - When no rows exist at or before asOfT, returns ok([]).
    - Each returned quote carries the FULL column set (bid, ask, mark, bsmIv, bsmDelta, bsmGamma, bsmTheta, bsmVega, openInterest, underlyingPrice, source, time) so one read serves both candidate-universe generation and hypothetical exit-context assembly.
  </behavior>
  <action>
    Implement backtest-chain.ts's makePostgresBacktestChainRepo satisfying ForReadingChainAsOf (declared in 27-01's ports.ts). Generalize readChainForPicker's two-step query by adding exactly ONE predicate — lte(legObservations.time, asOfT) — to the MAX(time WHERE bsm_iv IS NOT NULL) step, then the identical windowStart = latestTime - LOOKBACK_MS + selectDistinctOn union/dedup, but SELECT the full column set named in the behavior block (not just the picker's bid/ask/OI/iv subset). Use Drizzle parameterized builders throughout (never raw interpolation). Add the memory twin and the shared __contract__ spec. RED first: write the no-lookahead contract test (seed T1<T2≤asOfT, assert cohort=T2; then seed a future row, assert the result is byte-identical), run against the missing repo, confirm failure for the right reason; then GREEN. Commit at green.
  </action>
  <verify>
    <automated>bun run vitest run packages/adapters/src/postgres/repos/backtest-chain.contract.test.ts</automated>
  </verify>
  <done>readChainAsOf resolves the newest at-or-before-T cohort with live union/dedup semantics; a future-dated row never changes a past-T read; the full column set is returned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: As-of-T RV20 closes + picker-snapshot cohort ledger</name>
  <files>packages/adapters/src/postgres/repos/backtest-history.ts, packages/adapters/src/postgres/repos/backtest-history.contract.test.ts, packages/adapters/src/memory/backtest-history.ts, packages/adapters/src/__contract__/backtest-history.contract.ts</files>
  <read_first>
    - packages/adapters/src/postgres/repos/picker-history.ts (readDailySpotCloses — "last N distinct days" anchored to data; the read to bound by asOfT)
    - packages/adapters/src/postgres/repos/picker-snapshot.ts (the picker_snapshot row shape + pickerSnapshotResponse validate-on-read boundary)
    - .planning/phases/27-pick-04-backtest-harness/27-RESEARCH.md → "New reads needed" table (RV20 asOfT gap = the vector Pitfall 2 names for vrp; cohort enumeration via observed_at)
  </read_first>
  <behavior>
    - readDailySpotClosesAsOf(nDays, asOfT) returns at most nDays distinct daily closes, ALL dated at or before asOfT — a close dated after asOfT is never included (the point-in-time RV20 input; feeding realizedVol over it reproduces the live trailing-window stat).
    - readPickerSnapshotsInRange(from, to) returns every stored picker_snapshot row whose observed_at is in [from, to], validated through pickerSnapshotResponse on read (a corrupt blob surfaces StorageError, never a loosely-typed object).
    - Both degrade to ok([]) when no rows match.
  </behavior>
  <action>
    Implement backtest-history.ts's makePostgresBacktestHistoryRepo satisfying ForReadingDailySpotClosesAsOf and ForReadingPickerSnapshotsInRange (declared in 27-01's ports.ts). readDailySpotClosesAsOf bounds picker-history's "last N distinct days" logic with an lte(..., asOfT) predicate. readPickerSnapshotsInRange selects picker_snapshot WHERE observed_at BETWEEN from AND to, ordered ASC, mapping each row's snapshot blob through pickerSnapshotResponse.parse at the read boundary (mirror picker-snapshot.ts's existing T-19-10 validate-on-read). Drizzle parameterized builders only. Add the memory twin + shared __contract__ spec. RED first: contract test asserts a future-dated close is excluded and the in-range cohorts are returned in observed_at order; then GREEN.
  </action>
  <verify>
    <automated>bun run vitest run packages/adapters/src/postgres/repos/backtest-history.contract.test.ts</automated>
  </verify>
  <done>readDailySpotClosesAsOf excludes future closes; readPickerSnapshotsInRange returns the in-range cohort ledger validated on read; both have a passing twin + contract test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Source-inclusive full snapshot history (BT-03 schwab_chain regression guard)</name>
  <files>packages/adapters/src/postgres/repos/calendar-snapshots.ts, packages/adapters/src/postgres/repos/calendar-snapshots.contract.test.ts, packages/adapters/src/memory/calendar-snapshots.ts</files>
  <read_first>
    - packages/adapters/src/postgres/repos/calendar-snapshots.ts (mapSnapshotRow's source!=="cboe" → null drop [~line 416]; readLatestSnapshotPerOpenCalendar's source-inclusive mapping [~line 326] — the fix to generalize)
    - .planning/phases/27-pick-04-backtest-harness/27-RESEARCH.md → "Pattern 4: readJournal's silent source-drop" (the exact new-method sketch)
  </read_first>
  <behavior>
    - readFullSnapshotHistoryForCalendar(calendarId) returns EVERY calendar_snapshots row for that calendar ordered by time ASC, regardless of calendars.status (closed included) and regardless of source — a schwab_chain-sourced row is present in the result (the regression guard for mapSnapshotRow's drop bug).
    - A calendar with no snapshot rows returns ok([]) (not null, not an error).
  </behavior>
  <action>
    Add readFullSnapshotHistoryForCalendar as a new method on makePostgresCalendarSnapshotsRepo satisfying ForReadingFullSnapshotHistoryForCalendar (declared in 27-01's ports.ts) — a plain SELECT * FROM calendar_snapshots WHERE calendar_id = $1 ORDER BY time ASC, with NO status join and NO source filter, mapped through a source-inclusive mapper (mirror readLatestSnapshotPerOpenCalendar's "schwab_chain stays schwab_chain, everything else → cboe, never drop the row" mapping — NOT mapSnapshotRow, which returns null and drops schwab_chain rows). Mirror the method into the memory twin. RED first: add a contract test case seeding a calendar with BOTH a cboe and a schwab_chain snapshot row and asserting BOTH appear in ASC order (the schwab_chain-inclusion regression) — extend the existing calendar-snapshots.contract.test.ts, no new file; run, confirm failure for the right reason; then GREEN.
  </action>
  <verify>
    <automated>bun run vitest run packages/adapters/src/postgres/repos/calendar-snapshots.contract.test.ts</automated>
  </verify>
  <done>readFullSnapshotHistoryForCalendar returns all rows (both sources, any status) ASC; a schwab_chain row is never dropped; the memory twin matches.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI-supplied asOfT / calendarId → SQL reads | operator-supplied date range + id cross into query builders |
| stored history → point-in-time replay | reading "today's" table must reconstruct "as of T", not leak the future |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-05 | Tampering (lookahead leakage) | backtest-chain / backtest-history reads | high | mitigate | every read carries an lte(time, asOfT) bound; the BT-01 no-lookahead contract test (future row invisible) is the proof; RV20 trailing window is point-in-time |
| T-27-06 | Information Disclosure (silent data loss) | full snapshot history read | high | mitigate | source-inclusive mapper (never mapSnapshotRow's source-drop) — regression test asserts schwab_chain rows survive |
| T-27-07 | Tampering (SQL injection) | asOfT / calendarId predicates | medium | mitigate | Drizzle parameterized builders (and(gte,lte), eq) throughout — never raw template interpolation, uniform with every existing repo |
</threat_model>

<verification>
- `bun run vitest run packages/adapters/src/postgres/repos/backtest-chain.contract.test.ts packages/adapters/src/postgres/repos/backtest-history.contract.test.ts packages/adapters/src/postgres/repos/calendar-snapshots.contract.test.ts` all green (testcontainers, real Postgres).
- `bun run typecheck` / `bun run lint` clean.
- `bun run test` green — calendar-snapshots.ts is a live-path file; its existing readers must not regress.
</verification>

<success_criteria>
- A future-dated leg_observations row never changes a past-T chain read (BT-01).
- RV20 as-of-T excludes future closes; the cohort ledger enumerates in-range picker_snapshot cohorts.
- schwab_chain snapshot rows are returned by the full-history read (no silent drop).
</success_criteria>

<output>
Create `.planning/phases/27-pick-04-backtest-harness/27-03-SUMMARY.md` when done.
</output>
