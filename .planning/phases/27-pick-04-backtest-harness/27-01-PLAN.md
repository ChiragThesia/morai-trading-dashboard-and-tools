---
phase: 27-pick-04-backtest-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture/backtest-harness.md
  - docs/architecture/data-model.md
  - docs/TOPIC-MAP.md
  - packages/adapters/src/postgres/schema.ts
  - packages/adapters/src/postgres/migrations/0021_backtest_runs.sql
  - packages/adapters/src/__contract__/backtest-runs.contract.ts
  - packages/adapters/src/postgres/repos/backtest-runs.ts
  - packages/adapters/src/postgres/repos/backtest-runs.contract.test.ts
  - packages/adapters/src/memory/backtest-runs.ts
  - packages/adapters/src/memory/backtest-runs.contract.test.ts
  - packages/core/src/backtest/index.ts
  - packages/core/src/backtest/application/ports.ts
  - packages/core/src/backtest/domain/types.ts
  - packages/core/src/index.ts
autonomous: true
requirements: [BT-04, BT-05]

must_haves:
  truths:
    - "docs/architecture/backtest-harness.md exists and describes the three replay paths, the point-in-time discipline, the n=13 honesty rule, and the never-writes-weights boundary BEFORE any backtest code is written (docs-first, per D-04 workflow rule)"
    - "A backtest run's JSONB report persists append-only to backtest_runs and a second run never overwrites the first"
    - "The backtest_runs repo exposes only a persist (INSERT) capability — no update, no delete, no weight-write path (BT-05)"
  artifacts:
    - "packages/core/src/backtest/application/ports.ts — every driven port the harness needs, declared once (persist + the read ports 03/05 implement)"
    - "packages/core/src/backtest/domain/types.ts — BacktestReport and replay-result shapes"
    - "packages/adapters/src/postgres/migrations/0021_backtest_runs.sql — drizzle-generated, enableRLS"
    - "packages/adapters/src/postgres/repos/backtest-runs.ts + packages/adapters/src/memory/backtest-runs.ts (twin)"
  key_links:
    - "core/src/index.ts re-exports the backtest barrel so apps/worker and the adapters can reach the ports + report type"
    - "backtest_runs.report column stores the whole BacktestReport as one validated JSONB blob (mirrors picker_snapshot/exit_verdicts D-06 append-history convention)"
---

<objective>
Lay the storage + module foundation for the PICK-04 backtest harness: the docs-first architecture note, the append-only `backtest_runs` table (migration 0021), its INSERT-only repo + in-memory twin + shared contract test, and the `packages/core/src/backtest/` bounded-context skeleton (all driven ports + the report/result domain types) threaded through the core barrel.

This is the first plan of a strictly sequential phase (per 27-CONTEXT.md). It owns every shared scaffold the later plans import, so nothing downstream has to invent a port shape or a report type.

Purpose: give the replay engine (05), the readers (03), and the CLI/report (06) a stable persistence contract and a single place the harness's evidence lands — never a weight-write path (BT-05).
Output: architecture doc + data-model row, migration 0021, backtest_runs repo/twin/contract, backtest module skeleton.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-pick-04-backtest-harness/27-CONTEXT.md
@.planning/phases/27-pick-04-backtest-harness/27-RESEARCH.md

# Persistence + append-history precedents (read before writing the table + repo)
@packages/adapters/src/postgres/schema.ts
@packages/adapters/src/postgres/repos/exit-verdicts.ts
@packages/adapters/src/memory/exit-verdicts.ts
@packages/adapters/src/__contract__/exit-verdicts.contract.ts
@docs/architecture/data-model.md
@docs/TOPIC-MAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Docs-first — backtest-harness architecture note + data-model row + TOPIC-MAP</name>
  <files>docs/architecture/backtest-harness.md, docs/architecture/data-model.md, docs/TOPIC-MAP.md</files>
  <read_first>
    - docs/architecture/data-model.md (find the picker_snapshot / exit_verdicts append-history table rows to mirror the entry style)
    - docs/TOPIC-MAP.md (the documentation index this new doc must be registered in)
    - .planning/phases/27-pick-04-backtest-harness/27-RESEARCH.md (System Architecture Diagram + Recommended Project Structure — the source of the doc's content)
  </read_first>
  <action>
    Write docs/architecture/backtest-harness.md (Hemingway style, one concept, 20-250 lines) as the source-of-truth for this phase, per the D-04 docs-before-architecture rule. Cover: the operator-CLI form (apps/worker/src/backtest.ts, DATABASE_URL-only, not a pg-boss job); the three replay paths (leakage-oracle score reproduction [BT-02], the 13 real trades' exit mechanics [BT-03], full-universe hypothetical entry+exit for attribution/ablation [BT-04]); the point-in-time discipline (observedAt ≤ T; distributional stats trailing-window only); the reuse rule (replay through the UNTOUCHED live selectCandidates/scoreCalendarCandidates/evaluateExit — export, never reimplement); the honesty rule (every number stamped n= + date range + coverage %, bootstrap CI shown, n=13 refutation-only); the two documented residual caveats (late-solved-BSM optimism, economic-events have no discoveredAt column); and the hard boundary — the harness NEVER writes weights or rule registries, its only write is backtest_runs (BT-05). Add a data-model.md row for backtest_runs mirroring the picker_snapshot / exit_verdicts append-history entries (columns: id, created_at, params JSONB, report JSONB; append-only; enableRLS). Register the new doc in docs/TOPIC-MAP.md.
  </action>
  <verify>
    <automated>test -f docs/architecture/backtest-harness.md && grep -q "backtest_runs" docs/architecture/data-model.md && grep -q "backtest-harness" docs/TOPIC-MAP.md && echo DOCS_OK</automated>
  </verify>
  <done>The architecture doc, data-model row, and TOPIC-MAP entry all exist; the doc states the three replay paths, the point-in-time discipline, the n=13 honesty rule, and the never-writes-weights boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 0021 backtest_runs table + backtest module skeleton (ports + report types)</name>
  <files>packages/adapters/src/postgres/schema.ts, packages/adapters/src/postgres/migrations/0021_backtest_runs.sql, packages/core/src/backtest/application/ports.ts, packages/core/src/backtest/domain/types.ts, packages/core/src/backtest/index.ts, packages/core/src/index.ts</files>
  <read_first>
    - packages/adapters/src/postgres/schema.ts (the picker_snapshot [~line 487] + exit_verdicts [~line 518] append-history table definitions — mirror their jsonb + .enableRLS() shape)
    - packages/core/src/exits/application/ports.ts (the ForVerbingNoun driven-port declaration style + the driver-port-in-ports convention)
    - drizzle.config.ts (schema → migrations out dir; the generate command source of truth)
  </read_first>
  <action>
    Add a backtest_runs pgTable to schema.ts mirroring the picker_snapshot / exit_verdicts append-history convention: id uuid primaryKey (defaultRandom), created_at timestamptz notNull defaultNow, params jsonb notNull ($type Record string unknown), report jsonb notNull ($type Record string unknown), chained with .enableRLS(). Generate the migration with drizzle-kit (drizzle.config.ts points schema→packages/adapters/src/postgres/migrations/) producing 0021_backtest_runs.sql — do NOT hand-write the SQL; run the generator and commit its output. Create the packages/core/src/backtest/ bounded-context skeleton: application/ports.ts declaring every driven port the harness needs as fine-grained ForVerbingNoun function types — ForPersistingBacktestRun (report → Result void StorageError), plus the read ports 03 will implement (ForReadingChainAsOf(asOfT: Date), ForReadingDailySpotClosesAsOf(nDays: number, asOfT: Date), ForReadingPickerSnapshotsInRange(from: Date, to: Date), ForReadingFullSnapshotHistoryForCalendar(calendarId: string)); domain/types.ts with the BacktestReport shape and the replay-result shapes (a CohortMismatch/ReplayMismatch union incl. a registry-drift variant, a DirectionalAttributionRow, an AblationRow, a CoverageDay, a TradeReproduction row) — pure structural types, readonly fields, no imports beyond @morai/shared Result. Add index.ts as the barrel and re-export it from packages/core/src/index.ts. Keep core pure (imports @morai/shared only). Do not implement any read port here — declaration only.
  </action>
  <verify>
    <automated>ls packages/adapters/src/postgres/migrations/0021_backtest_runs.sql && bun run typecheck && echo SKELETON_OK</automated>
  </verify>
  <done>Migration 0021 exists (generated, enableRLS); backtest module skeleton declares all driven ports + the BacktestReport/result types; typecheck is clean; the backtest barrel is reachable from @morai/core.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: backtest_runs INSERT-only repo + memory twin + append-only contract test</name>
  <files>packages/adapters/src/__contract__/backtest-runs.contract.ts, packages/adapters/src/postgres/repos/backtest-runs.ts, packages/adapters/src/postgres/repos/backtest-runs.contract.test.ts, packages/adapters/src/memory/backtest-runs.ts, packages/adapters/src/memory/backtest-runs.contract.test.ts</files>
  <read_first>
    - packages/adapters/src/__contract__/exit-verdicts.contract.ts (the shared contract-spec pattern run against both impls)
    - packages/adapters/src/postgres/repos/exit-verdicts.ts (the INSERT-only append-history repo shape + JSONB validate-on-write/read boundary)
    - packages/adapters/src/memory/exit-verdicts.ts (the in-memory twin shape)
  </read_first>
  <behavior>
    - Persisting a report inserts one backtest_runs row and returns ok(void).
    - Two persists (distinct auto-generated ids) both survive — the second never overwrites/updates the first (append-only): after two persists the table holds two rows.
    - The repo surface exposes ONLY persist — no update, no delete, no weight-write method (BT-05 storage discipline: assert the repo's key set contains persist and nothing that mutates rules/weights).
    - The postgres and memory impls satisfy the identical shared contract spec.
  </behavior>
  <action>
    Author the shared contract spec (packages/adapters/src/__contract__/backtest-runs.contract.ts) covering the behaviors above, run against BOTH the postgres repo (testcontainers, real Postgres — SQL never mocked, per tdd.md) and the memory twin. Implement makePostgresBacktestRunsRepo as an INSERT-only repo: values id/params/report, no onConflictDoUpdate, no update/delete methods; validate the report blob through the same kind of parse boundary exit-verdicts uses (a report Zod schema or the BacktestReport validator) so a malformed blob is rejected before insert. Implement makeMemoryBacktestRunsRepo as the twin (array push, same ForPersistingBacktestRun signature). RED first: write the contract test, run it against the missing repo, confirm module-not-found/assertion failure for the right reason; then GREEN. Commit at green only.
  </action>
  <verify>
    <automated>bun run vitest run packages/adapters/src/postgres/repos/backtest-runs.contract.test.ts packages/adapters/src/memory/backtest-runs.contract.test.ts</automated>
  </verify>
  <done>Both contract-test files pass; a second persist never overwrites the first; the repo exposes only an append (persist) capability; the twin and postgres impl share one contract spec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI → Postgres | the only write the harness performs lands in backtest_runs |
| stored report blob → DB row | dollar P&L aggregates persist as JSONB |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-01 | Elevation of Privilege | backtest_runs repo | high | mitigate | INSERT-only repo — no update/delete/weight-write method exists; append-only contract test proves it (BT-05) |
| T-27-02 | Information Disclosure | backtest_runs table | low | mitigate | .enableRLS() on the new table, uniform with every existing schema.ts table; not new sensitive data (same figures already live in calendars/calendar_events) |
| T-27-SC | Tampering | npm/pip/cargo installs | high | accept | This phase installs ZERO new packages (fast-check/testcontainers/drizzle-kit already present at HEAD) — Package Legitimacy Gate skipped per its own trigger; no [ASSUMED]/[SUS]/[SLOP] packages to verify |
</threat_model>

<verification>
- `bun run typecheck` clean; `bun run lint` clean.
- `bun run vitest run packages/adapters/src/postgres/repos/backtest-runs.contract.test.ts packages/adapters/src/memory/backtest-runs.contract.test.ts` green.
- Docs artifacts exist (backtest-harness.md, data-model row, TOPIC-MAP entry).
- Migration 0021 is a drizzle-generated file, not hand-written.
</verification>

<success_criteria>
- The architecture doc exists before any harness code (docs-first honored).
- backtest_runs is append-only with RLS; a second run never overwrites the first.
- The backtest module skeleton declares all driven ports + the report/result types, reachable from @morai/core, typecheck clean.
</success_criteria>

<output>
Create `.planning/phases/27-pick-04-backtest-harness/27-01-SUMMARY.md` when done.
</output>
