---
phase: 27-pick-04-backtest-harness
plan: 04
type: execute
wave: 4
depends_on: [27-03]
files_modified:
  - packages/core/src/backtest/domain/directional-attribution.ts
  - packages/core/src/backtest/domain/directional-attribution.test.ts
  - packages/core/src/backtest/domain/ablation-delta.ts
  - packages/core/src/backtest/domain/ablation-delta.test.ts
  - packages/core/src/backtest/domain/bootstrap-ci.ts
  - packages/core/src/backtest/domain/bootstrap-ci.test.ts
  - packages/core/src/backtest/domain/coverage.ts
  - packages/core/src/backtest/domain/coverage.test.ts
  - packages/core/src/backtest/index.ts
autonomous: true
requirements: [BT-04]

must_haves:
  truths:
    - "Directional attribution reports a sign + n (high-scoring beat low-scoring: yes/no/insufficient), NEVER a correlation coefficient"
    - "Leave-one-rule-out ablation reports the rank/outcome delta from zeroing a rule; zeroing a rule with a positive raw contribution never IMPROVES a candidate's rank"
    - "Bootstrap CI is seeded (re-running over identical replay data reproduces an identical interval) and its width is honest at small n — constant array degenerates to a point, n=1 degenerates, low ≤ high always"
    - "Coverage reports replayed-of-total cohorts per day and excludes gap cohorts (spot 0/NaN) — no gap cohort is ever counted as replayed"
  artifacts:
    - "packages/core/src/backtest/domain/{directional-attribution,ablation-delta,bootstrap-ci,coverage}.ts — pure, ~15-30 lines each, no I/O"
    - "a fast-check property test per kernel fn (constant arrays, n=1, ties, empty)"
  key_links:
    - "every kernel fn is pure and reused by 06's runBacktest report assembly — no I/O, no Date.now(), deterministic"
    - "bootstrap-ci reuses a small quantile() helper (percentileRank doesn't invert); attribution reuses the median-split, never Pearson"
---

<objective>
Build the pure numeric report kernel — the four functions 06's `runBacktest` reduces raw replay outcomes into a persisted report with. All live in `packages/core/src/backtest/domain/`, pure, ~15-30 lines each, no I/O, and each proven with a fast-check property test over the edge cases 27-CONTEXT.md names (constant arrays, n=1, ties, empty).

These functions encode the phase's honesty constraints as code: directional attribution is a median-split SIGN test (never a coefficient — the CONTEXT lock), ablation is a rank-delta (never a weight-fit), the CI is a seeded bootstrap (a parametric interval is dishonest at n=13), and coverage makes the thin real-data footprint visible.

Purpose: BT-04's per-rule directional attribution + leave-one-out ablation + bootstrap-CI + coverage, as reusable pure functions.
Output: directional-attribution.ts, ablation-delta.ts, bootstrap-ci.ts, coverage.ts + fast-check tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-pick-04-backtest-harness/27-CONTEXT.md
@.planning/phases/27-pick-04-backtest-harness/27-RESEARCH.md

# Numeric-invariant precedent + the CI code sketch (read the Bootstrap CI kernel + fast-check invariants in RESEARCH)
@packages/core/src/picker/domain/realized-vol.ts
@packages/shared/src/percentile-rank.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Directional attribution (median-split sign test) + ablation-delta</name>
  <files>packages/core/src/backtest/domain/directional-attribution.ts, packages/core/src/backtest/domain/directional-attribution.test.ts, packages/core/src/backtest/domain/ablation-delta.ts, packages/core/src/backtest/domain/ablation-delta.test.ts</files>
  <read_first>
    - .planning/phases/27-pick-04-backtest-harness/27-RESEARCH.md → Anti-Patterns ("sign + n, never a coefficient" — median-split, not Pearson) + the BT-04 test map rows for attribution + ablation
    - packages/core/src/backtest/domain/types.ts (the DirectionalAttributionRow + AblationRow shapes declared in 27-01)
  </read_first>
  <behavior>
    - directionalAttribution over paired (metric, outcome) samples splits at the metric MEDIAN and returns a verdict of "yes" (high half's outcomes beat low half's), "no", or "insufficient" (n below a small floor, e.g. <4), plus n — never a numeric coefficient.
    - A constant metric array (no split possible) → "insufficient". A known positive split → "yes"; a known negative split → "no". Ties at the median are handled deterministically (documented split rule).
    - ablationDelta compares a candidate's rank under baseline scoring vs under a rule's weight zeroed and returns the rank delta; zeroing a rule whose raw contribution to that candidate was positive never yields an improved (lower-numbered) rank.
  </behavior>
  <action>
    Implement directionalAttribution as a pure median-split sign test returning { verdict: "yes"|"no"|"insufficient", n } — reject the Pearson-coefficient shape outright (the CONTEXT lock). Implement ablationDelta as a pure per-cohort rank-delta between a baseline scored list and an ablated scored list (the ablated list is produced upstream by scoreCalendarCandidates with a weights override — this fn only diffs ranks, it does not re-score). Add fast-check property tests: attribution over constant arrays, n<4, and a constructed positive/negative split; ablation asserting the "zeroing a positive-contribution rule never improves rank" invariant over randomized inputs. RED → GREEN, commit at green.
  </action>
  <verify>
    <automated>bun run vitest run packages/core/src/backtest/domain/directional-attribution.test.ts packages/core/src/backtest/domain/ablation-delta.test.ts</automated>
  </verify>
  <done>Attribution returns sign+n (yes/no/insufficient), never a coefficient; ablation rank-delta holds the positive-contribution invariant; both fast-check suites green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seeded bootstrap CI (+ quantile helper) + coverage</name>
  <files>packages/core/src/backtest/domain/bootstrap-ci.ts, packages/core/src/backtest/domain/bootstrap-ci.test.ts, packages/core/src/backtest/domain/coverage.ts, packages/core/src/backtest/domain/coverage.test.ts, packages/core/src/backtest/index.ts</files>
  <read_first>
    - .planning/phases/27-pick-04-backtest-harness/27-RESEARCH.md → "Bootstrap CI kernel (BT-04)" code block (the seeded mulberry-style PRNG + quantile helper + the exact fast-check invariants)
    - .planning/phases/27-pick-04-backtest-harness/27-RESEARCH.md → Common Pitfalls: gap-row poisoning (coverage = replayed-of-total, gap cohorts excluded)
  </read_first>
  <behavior>
    - bootstrapCi(samples, seed, iterations, confidence) resamples with a SEEDED PRNG and returns { low, high, n }: same seed + same samples → identical interval (determinism); a constant samples array → low === high === that constant; samples.length === 1 → degenerate point CI at that value; low ≤ high always; empty samples → { low: NaN, high: NaN, n: 0 }.
    - coveragePercent over per-day cohort counts returns replayed-of-total per day and overall, EXCLUDING gap cohorts (a cohort flagged spot 0/NaN counts toward total-skipped, never toward replayed).
  </behavior>
  <action>
    Implement bootstrapCi with the seeded PRNG + quantile() helper from RESEARCH's code block (the seeding is what keeps an append-only audit tool reproducible between runs). Implement coveragePercent as a pure reducer over a list of per-cohort {day, isGap} (or equivalent) records → per-day { replayed, total, coveragePct } + an overall figure, gap cohorts excluded from replayed. Thread all four domain fns through backtest/index.ts. Add fast-check property tests: the four bootstrap invariants (constant → degenerate, n=1 → point, low≤high, seed-determinism) and a coverage test asserting gap cohorts never count as replayed and the percentage is total-minus-gap over total. RED → GREEN.
  </action>
  <verify>
    <automated>bun run vitest run packages/core/src/backtest/domain/bootstrap-ci.test.ts packages/core/src/backtest/domain/coverage.test.ts</automated>
  </verify>
  <done>bootstrapCi is deterministic and degenerates correctly at small n; coveragePercent excludes gap cohorts; both fast-check suites green; all four kernel fns exported from the backtest barrel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| replay outcomes → reported statistic | a leaky/overclaiming statistic converts a thin dataset into false confidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-08 | Repudiation (false precision at n=13) | attribution / CI kernel | high | mitigate | attribution is sign+n only (never a coefficient); CI is a seeded bootstrap whose enormous width at n=13 IS the honesty signal; coverage surfaces the real footprint |
| T-27-09 | Tampering (nondeterministic report) | bootstrap-ci | low | mitigate | seeded PRNG → re-running over identical replay data reproduces an identical interval, so an append-only audit tool never shows spurious "numbers changed" churn |
| T-27-SC | Tampering | package installs | high | accept | zero new packages (fast-check already present) |
</threat_model>

<verification>
- `bun run vitest run packages/core/src/backtest/domain` all green (fast-check invariants incl. constant arrays, n=1, ties, empty, determinism).
- `bun run typecheck` / `bun run lint` clean.
</verification>

<success_criteria>
- The four kernel fns are pure, exported, and each has a passing fast-check property test.
- Attribution never returns a coefficient; ablation holds its rank invariant; the CI is seeded/degenerate-correct; coverage excludes gap cohorts.
</success_criteria>

<output>
Create `.planning/phases/27-pick-04-backtest-harness/27-04-SUMMARY.md` when done.
</output>
