---
phase: 09-reconciliation-invariant-and-status-endpoint
plan: 02
type: tdd
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/morai/ledger/reconciliation.py
  - tests/ledger/test_reconciliation.py
autonomous: true
requirements: [RECON-01, RECON-03]
estimate:
  tokens: 105000
  raw_tokens: 105000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Over a closed window whose derived cash flows and broker cash agree to the cent, `reconcile_window` returns `passed` — the sum of realised P&L equals the broker's cash delta, net of transfers (RECON-01, criterion 1)."
    - "A deliberately seeded discrepancy of exactly one cent returns `failed` — the check has teeth, which is the whole point of criterion 2's seeded case and this phase's own anti-vacuous-pass control (D9-07)."
    - "A parametrized sweep at differences of exactly zero, plus one cent, minus one cent and a large amount returns `passed` for the zero case alone, so no epsilon and no constant-returning implementation can satisfy the suite."
    - "A seeded fault in the comparison seam makes the exact-match case fail, proving the assertion is sensitive to a real corruption and not merely passing when the pipeline happens to be right — the same claim `tests/ledger/test_pairing_seeded_faults.py` already makes for the oracle."
    - "A failed result names its window as a `trading_day` date and carries both sides plus the signed difference, so how far off and in which direction is answerable without re-running anything (RECON-03, criterion 3, D9-13)."
    - "All three verdicts are exercised, and `indeterminate` is reached through each of its five distinct causes with a distinguishable reason on every one (D9-08, D9-09, D9-11)."
    - "An unrecognised `transaction_type` — one in neither the allow-list nor the named-exclusion set — yields `indeterminate`, never a silent inclusion in the cash delta and never a silent exclusion from it (D9-09)."
    - "An in-window SETTLEMENT whose money fields are null yields `indeterminate`, not `failed` — a documented gap that fires on every expiry must not be reported as a ledger error (D9-11, D7-07)."
    - "A transaction at Eastern midnight belongs to the later trading day and one microsecond earlier belongs to the earlier one, so two adjacent windows share no instant and nothing is counted twice (D9-01, boundary and adjacency probe rows)."
    - "A trading day with no broker activity is never a candidate window, and a set with only one observed day closes nothing (D9-02, empty and ordering probe rows)."
    - "Re-running the shell over an unchanged closed window writes no row; a window whose inputs changed after closing writes a new row marked as a reopening rather than modifying the old one (D9-03)."
    - "The 13-calendar oracle stays byte-identical and green: `git diff --exit-code` over `pairing.py`, `oracle_seed.py`, `test_oracle_gate.py` and `salvage/oracle-fixtures.md` exits 0 (D9-05, D5-04, Pitfall 1)."
  artifacts:
    - tests/ledger/test_reconciliation.py
  key_links:
    - "`tests/ledger/test_reconciliation.py` → `ledger/reconciliation.py::reconcile_window`, called directly with hand-built `EventRecord` and `BrokerCashRecord` values and no `AsyncSession` — the pure half of `D9-12`'s one-function-two-callers rule."
    - "`tests/ledger/test_reconciliation.py` → `ledger/reconciliation.py::run_reconciliation`, exercised on a live session for the reopening and no-op cases only."
    - "`ledger/reconciliation.py::reconcile_window` → `trading_day_for`, the sole window-membership predicate every edge case in this plan tests."
  prohibitions:
    - "An unanswerable check must never report `passed`."
    - "A commission or cash amount must never be fabricated when unknown."
    - "The 13-calendar oracle's fee-free fields must never be modified to make reconciliation pass."
    - "A tolerance, a rounding step or a normalisation must never sit between the two sides of the comparison."
    - "No symbol plan 09-01 exported may be renamed or removed — plan 09-03 imports this module in the same wave."
---

<objective>
Phase goal (ROADMAP, verbatim): "The core value is enforced and queryable — realised P&L equals the
broker's cash delta, checked every ingest cycle."

Plan 09-01 wired the invariant into production and proved a verdict reaches disk. This plan proves
the verdict is worth reading. It exercises all three states, all five `indeterminate` causes, and
the window arithmetic at its edges — and it carries this phase's own anti-vacuous-pass control: a
deliberately seeded one-cent discrepancy that must fail, alongside a seeded fault in the comparison
seam that must make the passing case fail.

Purpose: this run has produced seven defects and every one was a test that passed while proving
nothing, including a property test written with the exact input that broke the code excluded from
its own strategy. A reconciliation that cannot fail on a corrupted input is worse than no check at
all, because it teaches the reader that green means correct. Criterion 2 names the one-cent case
explicitly for that reason.

Output: `tests/ledger/test_reconciliation.py`, and whatever corrections to
`src/morai/ledger/reconciliation.py` those tests expose.
</objective>

<artifacts_produced>
Symbols, files and database objects this plan creates or changes.

| Kind | Name | Location |
|---|---|---|
| test file | `tests/ledger/test_reconciliation.py` | NEW |
| test | `test_a_window_that_agrees_to_the_cent_passes` | `tests/ledger/test_reconciliation.py` |
| test | `test_a_seeded_one_cent_discrepancy_fails` | `tests/ledger/test_reconciliation.py` |
| test | `test_verdict_across_a_discrepancy_sweep` (parametrized) | `tests/ledger/test_reconciliation.py` |
| test | `test_a_seeded_fault_in_the_comparison_makes_the_passing_case_fail` | `tests/ledger/test_reconciliation.py` |
| test | `test_failure_names_the_window_and_both_sides` | `tests/ledger/test_reconciliation.py` |
| test | `test_indeterminate_on_an_unrecognised_transaction_type` | `tests/ledger/test_reconciliation.py` |
| test | `test_indeterminate_on_a_missing_commission` | `tests/ledger/test_reconciliation.py` |
| test | `test_indeterminate_on_a_missing_cash_amount` | `tests/ledger/test_reconciliation.py` |
| test | `test_indeterminate_on_an_unpriced_settlement` | `tests/ledger/test_reconciliation.py` |
| test | `test_indeterminate_on_a_gapped_event_amount` | `tests/ledger/test_reconciliation.py` |
| test | `test_indeterminate_publishes_no_numbers` | `tests/ledger/test_reconciliation.py` |
| test | `test_an_excluded_transfer_type_is_not_counted_and_is_not_indeterminate` | `tests/ledger/test_reconciliation.py` |
| test | `test_eastern_midnight_belongs_to_exactly_one_window` (parametrized) | `tests/ledger/test_reconciliation.py` |
| test | `test_adjacent_windows_share_no_instant` | `tests/ledger/test_reconciliation.py` |
| test | `test_a_single_observed_day_closes_nothing` | `tests/ledger/test_reconciliation.py` |
| test | `test_closed_days_are_every_day_before_the_newest` | `tests/ledger/test_reconciliation.py` |
| test | `test_a_day_with_no_activity_is_never_a_candidate_window` | `tests/ledger/test_reconciliation.py` |
| test | `test_an_unchanged_closed_window_writes_no_second_row` | `tests/ledger/test_reconciliation.py` |
| test | `test_late_data_writes_a_new_row_marked_as_a_reopening` | `tests/ledger/test_reconciliation.py` |
| helper | `_event(...)`, `_cash(...)` — local `EventRecord`/`BrokerCashRecord` builders | `tests/ledger/test_reconciliation.py` |
| corrections | any defect these tests expose | `src/morai/ledger/reconciliation.py` |
</artifacts_produced>

<flagged_assumptions>
This plan owns six of the ten probe rows the spec-less fallback surfaced. See `09-01-PLAN.md`'s
`<flagged_assumptions>` for the whole table; the rows owned here are:

| Requirement | Probe category | Owned by |
|---|---|---|
| RECON-01 | unclassified | Task 1 — exact match passes, one cent fails, and the sweep proves no epsilon |
| RECON-02 | adjacency | Task 3 — two adjacent windows share no instant |
| RECON-02 | empty | Task 3 — a day with no activity is never a candidate; one observed day closes nothing |
| RECON-02 | ordering | Task 3 — days out of order, and a reopened window arriving after a later one closed |
| RECON-02 | precision | Task 1 — exact `Decimal` at four decimal places, with no rounding step anywhere |
| RECON-03 | (partly) | Task 1 — a failure names its window and carries both sides |

Assumptions this plan acts on:

- **The fixtures are hand-built, not vendor-shaped.** Every `BrokerCashRecord` in this file is
  constructed directly, so nothing here depends on Assumption A1's unverified Schwab key names. That
  is deliberate: the arithmetic must be provable today, and the field names are owed to the first
  live payload (`09-VALIDATION.md`, Manual-Only).
- **A green first run is not automatically a vacuous pass.** Plan 09-01 authored `reconcile_window`
  without seeing this file's fixtures, so a case that is green on its first run is genuine evidence
  — but only when the paired cases in the same sweep disagree in verdict, which is exactly what the
  sweep and the seeded fault are for. Record the observed first-run output in the SUMMARY either
  way; do not paraphrase it.
</flagged_assumptions>

<execution_context>
@/Users/chiragpersonalmac/Desktop/morai-trading-dashboard-and-tools/.claude/gsd-core/workflows/execute-plan.md
@/Users/chiragpersonalmac/Desktop/morai-trading-dashboard-and-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-reconciliation-invariant-and-status-endpoint/09-CONTEXT.md
@.planning/phases/09-reconciliation-invariant-and-status-endpoint/09-PATTERNS.md
@.planning/phases/09-reconciliation-invariant-and-status-endpoint/09-VALIDATION.md
@.planning/phases/09-reconciliation-invariant-and-status-endpoint/09-01-SUMMARY.md
@.claude/rules/workflow.md
</context>

<parallelism_note>
This plan runs in wave 2 alongside plan 09-03. Their `files_modified` sets are disjoint: this plan
owns `src/morai/ledger/reconciliation.py` and `tests/ledger/test_reconciliation.py`; 09-03 owns the
`src/morai/api/` and `tests/api/` files. `V093`: a git worktree isolates the filesystem, not
Postgres. Two agents sharing one database truncate each other's tables mid-test. Run this plan with
its own database — `createdb morai_agent_<id>`, `DATABASE_URL` pointed at it, `uv run alembic upgrade
head` before the first test.

Plan 09-03 imports `ReconciliationVerdict` and `IndeterminateReason` from this module in the same
wave. Correcting a body in `reconciliation.py` is in scope; renaming or removing an exported symbol
is not.
</parallelism_note>

<local_test_invocation>
Run every verify command with this environment. Postgres 18 runs natively via Homebrew; the full
suite is ~13s.

```
export DATABASE_URL="postgresql://morai:morai@localhost:5432/morai"
export MORAI_APP_DB_PASSWORD="localdevpassword"
export MORAI_MASTER_KEY="bW9yYWktbG9jYWwtZGV2LWtleS1ub3QtYS1zZWNyZXQ="
export MORAI_ENV_FILE=""
uv run pytest -q
bash tools/gate.sh
```

Baseline entering this phase: **587 passed, gate exit 0.** Any drop is a regression.

**Never push to CI to find out whether a test passes.**

**Take the cheapest honest red.** `tests/ledger/test_reconciliation.py` does not exist; writing a
case that imports a symbol and asserts on its output is a real red the moment either is wrong, and
it costs nothing. Never build temporary scaffolding to manufacture a more satisfying failure — that
exact instruction cost this project four hours and three throwaway commits in Phase 2. The seeded
fault in Task 1 is not scaffolding: it is a shipped test, following the convention
`tests/ledger/test_pairing_seeded_faults.py` already established for exactly this claim.

**Time-box.** Past roughly thirty minutes on one task with nothing committed, stop and report the
blocker.
</local_test_invocation>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: The invariant holds to the cent, and one cent breaks it</name>
  <precondition>Plan 09-01 is committed: `src/morai/ledger/reconciliation.py` exports `reconcile_window`, `ReconciliationResult` and `ReconciliationVerdict`, and `uv run alembic current` reports `0016`.</precondition>
  <files>tests/ledger/test_reconciliation.py, src/morai/ledger/reconciliation.py</files>
  <read_first>
    - `src/morai/ledger/reconciliation.py` — the whole module as plan 09-01 wrote it: `reconcile_window`'s signature and its docstring's statement of the arithmetic and sign convention, `ReconciliationResult`'s fields, `ReconciliationVerdict`, `BrokerCashRecord`, `trading_day_for`, `CASH_TRANSACTION_TYPES`.
    - `src/morai/ledger/events.py` — `EventRecord`'s exact field list, so the hand-built fixtures construct it correctly.
    - `src/morai/ledger/pairing.py` — `_signed_leg_amount`'s docstring for the sign convention the fixtures must respect (an OPEN buy is positive, a CLOSE sell is positive) and `EventType`'s four member strings. Read it; do not modify it.
    - `tests/ledger/test_pairing_seeded_faults.py` — the whole file. It is this project's own convention for proving an assertion has teeth: patch the one seam every contribution routes through, assert the check raises, and state in the docstring what the suite proves and what it does not.
    - `tests/ledger/test_pairing_pure.py` — the pure-test shape and the no-database-marker convention for a function that needs nothing but data.
    - `tests/test_decimal_canary.py` and `tests/test_money_roundtrip.py` — the existing proofs that a `Decimal` survives its round trip exactly, so this plan cites them rather than re-proving them.
    - `.planning/phases/05-fill-pairing-and-the-oracle-gate/05-CONTEXT.md` — decision `D5-04`, for why the left side of this comparison is fee-free and must stay that way.
  </read_first>
  <behavior>
    - Test 1: one OPEN event at a debit, one CLOSE event at a credit, and two allow-listed broker transactions whose net amounts and commissions make the identity exact — `reconcile_window` returns `passed`, `signed_difference_usd` is zero, and `reason` is `None`.
    - Test 2: the same fixture with the broker's net amount moved by exactly one cent — `reconcile_window` returns `failed`, and `signed_difference_usd` is exactly one cent with the sign that says which side is larger.
    - Test 3, parametrized over differences of exactly `0`, `+0.01`, `-0.01` and `+1000.00`: `passed` for the zero case and `failed` for the other three, with `signed_difference_usd` equal to the seeded difference in every case. No epsilon and no constant-returning implementation can satisfy this set.
    - Test 4: with the comparison seam patched to a corrupted form, the exact-match fixture from Test 1 no longer returns `passed` — the assertion is sensitive to a real corruption, not merely passing when the pipeline happens to be right.
    - Test 5: a failed result carries the `trading_day` the failure belongs to, the window bounds, and all three of `realised_pnl_usd`, `commissions_usd` and `cash_delta_usd` — the next question is answerable from the result alone.
    - Test 6: four-decimal amounts survive the arithmetic digit-for-digit; a difference of `0.0001` still returns `failed`.
    - Test 7: a ROLL event contributes its close credit minus its open debit, and a window containing one ROLL reconciles exactly.
  </behavior>
  <action>
Create `tests/ledger/test_reconciliation.py`. Write the seven behaviours above as tests before
changing a line of `reconciliation.py`, and run them. Record the first run's real output — the
command and its actual result — in this plan's SUMMARY, unparaphrased. A green first run is genuine
evidence here only because plan 09-01 authored the implementation without seeing these fixtures, and
only when Test 3's sweep and Test 4's seeded fault both ran; a green run without those two is a
vacuous pass and must be treated as one.

Carry no database marker on this file's cases. `reconcile_window` takes no session and reads no
clock, so every case in this task is a pure unit run — the same reason `tests/ledger/
test_pairing_pure.py` carries none. Task 3 adds the only db-marked cases in the file, following
`tests/ledger/test_oracle_gate.py`'s own marker-mixing convention.

Write two local builders, `_event(...)` and `_cash(...)`, that construct an `EventRecord` and a
`BrokerCashRecord` with sensible defaults and keyword overrides, so each case states only what it is
about. Build every money value from a string, never from a numeric literal that has already lost
precision before it reaches the fixture.

For Test 1, pick amounts that make the identity exact by construction and write the derivation as a
comment above the fixture, so a later reader can check the arithmetic by eye rather than trusting
that it balances: the opening debit, the closing credit, the two commissions, and the two broker net
amounts, with the sum shown. Respect `_signed_leg_amount`'s sign convention — an OPEN's
`open_debit_usd` is positive cash out and a CLOSE's `close_credit_usd` is positive cash in — and a
fee-inclusive broker net amount is negative when cash left the account.

For Test 2, derive the one-cent case from Test 1's fixture with a single field changed, so the two
differ by exactly the seeded cent and nothing else. This is criterion 2's own named case and this
phase's anti-vacuous-pass control: assert the verdict is `failed` and assert the signed difference
equals the seeded cent exactly, not merely that it is non-zero.

For Test 3, parametrize the same fixture over the four differences with `pytest.param(...,
id=...)` so a failing case reports by its difference rather than by index — the same convention the
oracle suite uses to report by order id. The parametrized set is the structural proof that no
tolerance sits in the comparison: a tolerance large enough to pass the one-cent case would fail the
zero case's own exactness assertion or the one-cent case's verdict, and there is no single constant
that satisfies all four.

For Test 4, follow `tests/ledger/test_pairing_seeded_faults.py`'s convention exactly. Patch the one
seam every amount routes through with `monkeypatch`, assert the previously-passing fixture no longer
returns `passed`, and write a docstring stating what the case proves — that the comparison is
sensitive to a real corruption — and what it does not: it does not prove the corruption cannot occur
in production, which is a different claim. This is a shipped test, not scaffolding: it stays in the
suite and runs on every gate.

For Test 6, use amounts carrying four decimal places, matching `NUMERIC(14, 4)`'s own scale and the
four-decimal prices this project's fixtures already use. Cite `tests/test_decimal_canary.py` and
`tests/test_money_roundtrip.py` in the docstring rather than re-proving the encrypted round trip;
this task's claim is about the arithmetic, not the storage.

If any case exposes a defect in `reconcile_window`, fix `reconciliation.py` rather than weakening
the case, and say so in the SUMMARY with the defect named. Do not rename or remove any symbol plan
09-01 exported: plan 09-03 imports this module in the same wave. Do not add a tolerance, a rounding
step or a normalisation between the two sides — `D9-07` is explicit that any of them defeats
criterion 2 outright, which is the concrete reason this path is `Decimal` end to end.
  </action>
  <verify>
    <automated>uv run pytest tests/ledger/test_reconciliation.py -x -q -k "passes or discrepancy or sweep or seeded_fault or names_the_window or decimal or roll"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/ledger/test_reconciliation.py -x -q` exits 0.
    - `uv run pytest tests/ledger/test_reconciliation.py -m "not db" -q` collects and passes every case written in this task, proving they need no Postgres.
    - The sweep collects exactly 4 parametrized cases and `uv run pytest tests/ledger/test_reconciliation.py -k sweep -q` reports 4 passed.
    - `uv run python -c "import ast; t=ast.parse(open('src/morai/ledger/reconciliation.py').read()); bad=[n.attr for n in ast.walk(t) if isinstance(n, ast.Attribute) and n.attr in {'quantize','normalize','to_integral_value'}]; assert not bad, bad"` exits 0 — an AST check for a rounding step in the comparison path, immune to prose and docstrings.
    - `uv run python -c "import ast; t=ast.parse(open('src/morai/ledger/reconciliation.py').read()); banned={n.id for n in ast.walk(t) if isinstance(n, ast.Name)} & {'float','abs','round'}; assert not banned, banned"` exits 0.
    - `git diff --exit-code src/morai/ledger/pairing.py tests/ledger/oracle_seed.py tests/ledger/test_oracle_gate.py salvage/oracle-fixtures.md` exits 0, and `git diff --stat tests/ledger/test_oracle_gate.py` reports no change.
    - `uv run pytest tests/ledger/test_oracle_gate.py -q` exits 0.
    - The SUMMARY records the first run's actual command and output for this file, unparaphrased.
    - `bash tools/gate.sh` exits 0 and the full suite count is at least the 587 baseline.
  </acceptance_criteria>
  <done>The invariant is proven exact on a hand-derived fixture, a seeded one-cent discrepancy fails it, a four-point sweep rules out any tolerance, and a seeded fault proves the assertion has teeth.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: All three verdicts, and every cause of the third</name>
  <files>tests/ledger/test_reconciliation.py, src/morai/ledger/reconciliation.py</files>
  <read_first>
    - `src/morai/ledger/reconciliation.py` — `IndeterminateReason`'s five members, `CASH_TRANSACTION_TYPES`, `EXCLUDED_TRANSACTION_TYPES`, and the documented order in which `reconcile_window` walks its indeterminate causes.
    - `tests/ledger/test_reconciliation.py` — the `_event`/`_cash` builders Task 1 wrote; reuse them rather than writing a second pair.
    - `src/morai/ledger/settlements.py` — `DerivedSettlement`'s docstring on why a SETTLEMENT carries no money fields at all (`D7-07`), which is the input this task's unpriced-settlement case models.
    - `src/morai/ledger/pairing.py` — `_net_amount`'s docstring: a partial gap makes the whole net amount a gap, which is the shape the gapped-event case models. Read it; do not modify it.
    - `src/morai/ingest/snapshot_runs.py` — the fixed-vocabulary-never-free-text discipline for a classification enum (`NN-20`, `NN-34`).
    - `.venv/lib/python3.13/site-packages/schwab/client/base.py` lines 343-359 — the 15-member `TransactionType` enum, so the excluded-transfer case uses a real vendor value and the unrecognised case uses one that is genuinely not a member.
  </read_first>
  <behavior>
    - Test 1: a window containing a transaction whose `transaction_type` is in neither the allow-list nor the named-exclusion set returns `indeterminate` with reason `unrecognised_transaction_type`.
    - Test 2: an allow-listed transaction whose `commission_usd` is `None` returns `indeterminate` with reason `commission_unavailable`.
    - Test 3: an allow-listed transaction whose `net_amount_usd` is `None` returns `indeterminate` with reason `cash_amount_unavailable`.
    - Test 4: a window containing a SETTLEMENT event whose money fields are `None` returns `indeterminate` with reason `settlement_unpriced` — never `failed`.
    - Test 5: an OPEN whose `open_debit_usd` is `None`, a CLOSE whose `close_credit_usd` is `None`, and a ROLL missing either, each return `indeterminate` with reason `event_amount_unavailable`.
    - Test 6: every `indeterminate` result carries all four money fields as `None` — an unanswerable check publishes no numbers, not partial ones.
    - Test 7: a window containing a real excluded transfer type from the vendor's own enum is `passed` when the rest balances, and the transfer's amount is not in the cash delta — "net of transfers" is exclusion, not indeterminacy.
    - Test 8: the union of `CASH_TRANSACTION_TYPES` and `EXCLUDED_TRANSACTION_TYPES` is exactly the 15 members of the vendor's installed `TransactionType` enum, and the two sets are disjoint.
    - Test 9: a window with two simultaneous indeterminate causes returns the same reason on every run, matching the documented order.
  </behavior>
  <action>
Extend `tests/ledger/test_reconciliation.py` with the nine behaviours above, written before any
change to `reconciliation.py`. Reuse Task 1's `_event` and `_cash` builders; a second pair of
builders is drift waiting to happen.

`D9-08` is what this task exists to prove, and it is a claim in two directions. Collapsing
`indeterminate` into `passed` hides a broken window. Collapsing it into `failed` cries wolf on every
expiry and trains the reader to ignore the alarm. Every case here must assert the exact reason, not
merely that the verdict is not `passed` — the reasons exist so the causes are distinguishable, and a
test that only checks the verdict leaves that promise unproven.

For Test 4, state in the docstring that this is `D9-11` and that it is the case most likely to be
"fixed" wrongly by a later reader: a SETTLEMENT's money fields are null by `D7-07`'s own deliberate
decision, pricing them needs Phase 8's captured market data applied to expiries, and that work is
explicitly out of this phase's scope. Reporting it as a failure would fire on every expiry.

For Test 7, use a transaction type that is a real member of the vendor's own enum and a real
transfer — a deposit or a wire — and assert two things: the verdict is `passed`, and the cash delta
equals the sum of the allow-listed amounts alone. `RECON-01` says "net of transfers", and this case
is what proves the carve-out is a subtraction from membership rather than a reason to give up on the
window.

For Test 8, import the two sets from `reconciliation.py` and compare their union against a literal
tuple of the fifteen strings, with the source path and line range named in the test docstring. This
is the case that fails loudly the day a sixteenth type appears in a live payload, which is exactly
what `D9-09`'s allow-list exists to surface — and it fails at the boundary rather than by silently
folding an unknown type into the sum.

For Test 9, build a window carrying both an unrecognised type and a missing commission, and assert
the reason matches the first cause in `reconcile_window`'s documented order. A stable answer is what
makes an `indeterminate` window actionable; two runs disagreeing about why would be worse than
either answer alone.

If any case exposes a defect, fix `reconciliation.py` and name the defect in the SUMMARY. Do not
widen either transaction-type set to make a case pass — a type that belongs in neither set is
`D9-09`'s whole subject.
  </action>
  <verify>
    <automated>uv run pytest tests/ledger/test_reconciliation.py -x -q -k "indeterminate or excluded or transaction_type"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/ledger/test_reconciliation.py -x -q` exits 0.
    - `uv run pytest tests/ledger/test_reconciliation.py -k indeterminate -q` reports at least 6 passed.
    - `uv run python -c "from morai.ledger.reconciliation import IndeterminateReason; import pathlib; src=pathlib.Path('tests/ledger/test_reconciliation.py').read_text(); missing=[m.value for m in IndeterminateReason if m.value not in src]; assert not missing, missing"` exits 0 — every one of the five reasons is named in the suite, so none can be added to the enum and left untested.
    - `uv run python -c "from morai.ledger.reconciliation import CASH_TRANSACTION_TYPES as A, EXCLUDED_TRANSACTION_TYPES as B; assert not (A & B); assert len(A | B) == 15"` exits 0.
    - All three members of `ReconciliationVerdict` appear as an asserted outcome somewhere in `tests/ledger/test_reconciliation.py`: `uv run python -c "from morai.ledger.reconciliation import ReconciliationVerdict as V; import pathlib; src=pathlib.Path('tests/ledger/test_reconciliation.py').read_text(); missing=[m.name for m in V if m.name not in src]; assert not missing, missing"` exits 0.
    - `git diff --exit-code src/morai/ledger/pairing.py tests/ledger/oracle_seed.py tests/ledger/test_oracle_gate.py` exits 0.
    - `bash tools/gate.sh` exits 0.
  </acceptance_criteria>
  <done>All three verdicts are exercised, each of the five indeterminate causes has its own case asserting its own reason, and the allow-list is proven to cover the vendor's whole enum with nothing overlapping and nothing missing.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: The window at its edges — midnight, adjacency, emptiness, and a reopening</name>
  <files>tests/ledger/test_reconciliation.py, src/morai/ledger/reconciliation.py</files>
  <read_first>
    - `src/morai/ledger/reconciliation.py` — `trading_day_for`, `window_bounds`, `closed_trading_days`, and `run_reconciliation`'s change-detection branch as plan 09-01 wrote it.
    - `src/morai/ingest/reconciliation_runs.py` — `record_reconciliation_run`, `read_latest_run_for_trading_day`, and `ReconciliationRunRecord`'s fields.
    - `src/morai/ledger/settlements.py` — `_EASTERN` and the comment on why a fixed UTC offset is wrong roughly half the year; the daylight-saving cases in this task are what make that concrete.
    - `docs/learnings/LAWS.md` entry `L048` — the half-open-window bug: an observation just before an anchor was invisible because a periodic trigger and a fixed-cadence source disagreed about when a boundary had passed. `D9-02` cites it directly.
    - `tests/ledger/test_oracle_gate.py` — the marker-mixing convention for a file whose cases are mostly pure with a few needing Postgres.
    - `tests/ledger/conftest.py` — `provisioned_users`, `clean_ledger_tables`, `app_db_session`, `superuser_db_session`.
    - `tests/ingest/conftest.py` — `clean_reconciliation_tables` from plan 09-01, and the `__all__` re-export convention pytest resolves fixtures through.
    - `src/morai/ledger/events.py` — `insert_events` and `EventWrite`, the real write path the db-marked cases seed through.
    - `src/morai/ingest/broker_transactions.py` — `insert_broker_transactions` and `BrokerTransactionWrite`, likewise. Never a test-only fast path (`D3-14`).
  </read_first>
  <behavior>
    - Test 1, parametrized: an instant one microsecond before Eastern midnight and an instant at Eastern midnight yield different dates from `trading_day_for`, and each belongs to exactly one window. Cover a date inside daylight saving and one outside it, so a fixed offset could not satisfy both.
    - Test 2: two adjacent trading days reconciled from the same input lists share no transaction and no event — the sum of the two windows' counted items equals the count of items falling on those two days, with no item in both.
    - Test 3: `closed_trading_days` over records spanning one day only returns an empty tuple.
    - Test 4: `closed_trading_days` over records spanning three days returns the two earlier days in ascending order and never the newest.
    - Test 5: `closed_trading_days` over records whose dates arrive out of chronological order returns the same result as the sorted input — closure depends on the observed set, not on arrival order.
    - Test 6: a trading day with no records is absent from `closed_trading_days` and is never reconciled, so a weekend or a holiday needs no calendar.
    - Test 7: `window_bounds` returns Eastern midnight and the next Eastern midnight, and `window_end` is strictly greater than `window_start` across a daylight-saving transition in both directions.
    - Test 8 (db): calling `run_reconciliation` twice with unchanged inputs leaves the `reconciliation_runs` row count unchanged after the first call.
    - Test 9 (db): inserting a late broker transaction into an already-closed window and calling `run_reconciliation` again writes one new row for that same `trading_day` with `is_reopening` true, and leaves the earlier row untouched.
  </behavior>
  <action>
Extend `tests/ledger/test_reconciliation.py` with the nine behaviours above, written first. Tests 1
through 7 are pure and carry no marker. Tests 8 and 9 need Postgres and carry `pytest.mark.db` at
the case level, following `tests/ledger/test_oracle_gate.py`'s own marker-mixing convention rather
than promoting the whole module to a db file.

For Test 1, pick one date in July and one in January so the Eastern offset differs between them, and
build each instant as an aware UTC datetime converted through the module's own constant. State in
the docstring that `trading_day_for` is the sole membership predicate in the whole module, that
nothing compares an instant against `window_start` or `window_end`, and that this is what makes a
half-open boundary structurally impossible rather than a comparison a later caller could get
backwards. Cite `L048` by number: that bug was exactly a periodic trigger and a fixed-cadence source
disagreeing about when a boundary had passed.

For Test 2, assert the disjointness positively — compute the two windows' counted items and assert
their intersection is empty and their union is the full set for those two days. An assertion that
each window has the right count would pass even if one item were counted in both.

For Tests 3 through 6, drive `closed_trading_days` directly with hand-built `BrokerCashRecord`
lists. Test 6 is the empty probe row and it is also the argument for adding no dependency: a day
with no broker activity never enters the observed set, so no window ever has to know whether that
day was a market holiday. Say so in the docstring.

For Tests 8 and 9, seed through the real write paths — `insert_events` and
`insert_broker_transactions` — never a direct table insert (`D3-14`'s discipline: a test-only fast
path proves the wrong thing). Set `app.current_user_id` on the app session before any read or write
of a protected table. Depend on `clean_reconciliation_tables` so neither case leaks rows into the
other.

Test 9 is `D9-03` and it is the ordering probe row. Assert three things separately: exactly one new
row appeared, its `is_reopening` is true, and the original row's own values are unchanged. The third
assertion is what proves this is a new finding rather than an edit — a closed window whose inputs
later changed means the broker restated, and silently absorbing that would make the ledger agree
with a history that no longer exists.

If any case exposes a defect, fix `reconciliation.py` and name it in the SUMMARY. Do not add a
market-calendar dependency to make a case pass; `D9-02` makes closure data-driven for exactly this
reason, and `09-CONTEXT.md` leaves the calendar to discretion only on the condition that no new
dependency is added for it.
  </action>
  <verify>
    <automated>uv run pytest tests/ledger/test_reconciliation.py -x -q -k "midnight or adjacent or closes_nothing or closed_days or no_activity or window_bounds or second_row or reopening"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/ledger/test_reconciliation.py -x -q` exits 0.
    - `uv run pytest tests/ledger/test_reconciliation.py -m "not db" -q` passes, and `uv run pytest tests/ledger/test_reconciliation.py -m db -q` reports at least 2 passed — the file mixes markers as intended.
    - `SELECT count(*) FROM reconciliation_runs WHERE is_reopening` returns 1 after Test 9, and `SELECT count(DISTINCT trading_day) FROM reconciliation_runs` returns 1 for that case's window — the reopening is a second row for the same window, not a second window.
    - The midnight case is parametrized over at least 4 instants spanning both a summer and a winter date: `uv run pytest tests/ledger/test_reconciliation.py -k midnight -q` reports at least 4 passed.
    - `uv run python -c "import ast; t=ast.parse(open('src/morai/ledger/reconciliation.py').read()); assert not [n for n in ast.walk(t) if isinstance(n, ast.Call) and isinstance(n.func, ast.Attribute) and n.func.attr in {'utcnow','now','today'}]"` exits 0 — no clock read inside the module, `D9-12`'s purity contract.
    - `git diff --exit-code pyproject.toml uv.lock` exits 0 — no calendar dependency was added.
    - `git diff --exit-code src/morai/ledger/pairing.py tests/ledger/oracle_seed.py tests/ledger/test_oracle_gate.py` exits 0.
    - `bash tools/gate.sh` exits 0 and the full suite count is at least the 587 baseline plus this plan's own new cases.
  </acceptance_criteria>
  <done>The window is proven to have exactly one owner for every instant across both daylight-saving states, an inactive day is proven never to become a window, and a late-arriving restatement is proven to land as a new marked row rather than an edit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture → `reconcile_window` | This plan's fixtures are the only untrusted input in its scope; the module's real vendor boundary is plan 09-01's. |
| One user's RLS context → `reconciliation_runs` | Tasks 3's db-marked cases write through the app role and must not depend on a superuser session. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-07 | Repudiation (false assurance) | a reconciliation test that passes while proving nothing | high | mitigate | Task 1's four-point discrepancy sweep, which no epsilon and no constant-returning implementation can satisfy, plus the seeded-fault case following `tests/ledger/test_pairing_seeded_faults.py`'s convention, plus the enum-coverage assertions that fail if a verdict or a reason is ever added and left untested. |
| T-09-08 | Repudiation | an `indeterminate` window whose cause cannot be told apart from another's | high | mitigate | Task 2 asserts the exact reason in every case, and the coverage check fails if any member of `IndeterminateReason` is absent from the suite. Task 2's Test 9 pins the answer as stable when two causes are present. |
| T-09-09 | Tampering | the 13-calendar oracle altered to make reconciliation pass | high | mitigate | Every task's acceptance criteria run `git diff --exit-code` over `pairing.py`, `oracle_seed.py`, `test_oracle_gate.py` and `salvage/oracle-fixtures.md`, and Task 1 re-runs the oracle gate. `D5-04`/`D9-05` are restated in `reconcile_window`'s own docstring so a later reader meets them at the code. |
| T-09-10 | Information Disclosure | a db-marked case reading another user's rows | medium | mitigate | Tasks 3's cases set `app.current_user_id` before touching a protected table and seed through the real write paths on the app session; plan 09-01 Task 2 already proved the policy itself. |
| T-09-11 | Denial of Service | a hidden rounding step letting a real discrepancy pass unnoticed | medium | mitigate | The AST check for a rounding, normalisation or absolute-value call in `reconciliation.py`, which is immune to prose and docstrings. |
| T-09-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs nothing; `09-RESEARCH.md` § Package Legitimacy Audit records "Not applicable". Asserted by `git diff --exit-code pyproject.toml uv.lock` exiting 0, which doubles as the no-calendar-dependency gate. |
</threat_model>

<verification>
- `uv run pytest tests/ledger/test_reconciliation.py -q` exits 0.
- `uv run pytest tests/ledger/test_oracle_gate.py -q` exits 0 and `git diff --stat tests/ledger/test_oracle_gate.py` reports no change.
- All three `ReconciliationVerdict` members and all five `IndeterminateReason` members are named in the suite, checked programmatically rather than by eye.
- `bash tools/gate.sh` exits 0 with the suite count above the 587 baseline.
</verification>

<success_criteria>
- Exact agreement passes; a seeded one-cent discrepancy fails; a four-point sweep rules out any tolerance.
- A seeded fault in the comparison seam makes the passing case fail.
- A failure names its `trading_day` and carries both sides plus the signed difference.
- Each of the five `indeterminate` causes has its own case asserting its own reason.
- Every instant belongs to exactly one window, in both daylight-saving states.
- An unchanged closed window writes no second row; a restated one writes a new row marked as a reopening.
</success_criteria>

<output>
Create `.planning/phases/09-reconciliation-invariant-and-status-endpoint/09-02-SUMMARY.md` when done.
</output>
