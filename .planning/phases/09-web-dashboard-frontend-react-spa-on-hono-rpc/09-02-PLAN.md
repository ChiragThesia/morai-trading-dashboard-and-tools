---
phase: 09-web-dashboard-frontend-react-spa-on-hono-rpc
plan: 02
type: tdd
wave: 2
depends_on: ["09-01"]
files_modified:
  - packages/quant/package.json
  - packages/quant/tsconfig.json
  - packages/quant/vitest.config.ts
  - packages/quant/src/bsm.ts
  - packages/quant/src/index.ts
  - packages/quant/src/bsm.test.ts
  - packages/core/src/journal/domain/bsm.ts
  - packages/core/package.json
  - packages/core/tsconfig.json
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "packages/quant exports bsmPrice/bsmGreeks/bsmVega/BsmGreeks as a pure zero-dependency leaf"
    - "The quant leaf produces output identical to the original core bsm.ts for the same inputs (parity)"
    - "All existing core BSM/IV/GEX/snapshot tests still pass after the move (re-export shim works)"
    - "bun run typecheck and bun run lint stay green across the workspace"
  artifacts:
    - path: "packages/quant/src/bsm.ts"
      provides: "Relocated pure BSM kernel"
      exports: ["bsmPrice", "bsmGreeks", "bsmVega", "BsmGreeks"]
    - path: "packages/quant/src/index.ts"
      provides: "Quant leaf barrel"
      contains: "bsmPrice"
    - path: "packages/quant/src/bsm.test.ts"
      provides: "Parity + fast-check property suite"
      contains: "bsmPrice"
    - path: "packages/core/src/journal/domain/bsm.ts"
      provides: "Re-export shim to @morai/quant (keeps core call sites working)"
      contains: "@morai/quant"
  key_links:
    - from: "packages/core/src/journal/domain/bsm.ts"
      to: "packages/quant"
      via: "re-export from @morai/quant"
      pattern: "@morai/quant"
    - from: "packages/core/src/analytics/domain/gex.ts"
      to: "packages/core/src/journal/domain/bsm.ts"
      via: "existing relative import resolves through the shim"
      pattern: "bsmGreeks"
---

<objective>
Extract the pure BSM kernel DOWN to a new `packages/quant` pure-leaf package (D-01), proving
output parity with the original `packages/core/src/journal/domain/bsm.ts`, then convert that core
file into a thin re-export shim so every existing core call site keeps working unchanged. Both
`core` and (later) `web` import `@morai/quant`; no dependency arrow reverses. This is TDD: the
ported parity + fast-check suite is the RED baseline, the relocated kernel turns it GREEN.

Purpose: One shared kernel guarantees the Analyzer's client-side live P&L (Plan 10) and the
server-computed Positions/Journal P&L agree to the float — a second hand-rolled browser copy would
risk visible divergence. The leaf must be BELOW both core and web so neither boundary is violated.
Output: `packages/quant` leaf with the kernel + tests; core bsm.ts as a re-export shim.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-PATTERNS.md
@.planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-RESEARCH.md
@.claude/rules/tdd.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/typescript.md
@packages/core/src/journal/domain/bsm.ts
@packages/core/src/journal/domain/bsm.test.ts
@packages/shared/package.json
@packages/shared/tsconfig.json
@packages/core/tsconfig.json
</context>

<artifacts_this_phase_produces>
New package + exported symbols this plan creates (exclude from drift verification):
- New package `@morai/quant` (`packages/quant/`).
- Exported functions: `bsmPrice`, `bsmGreeks`, `bsmVega` from `@morai/quant`.
- Exported type: `BsmGreeks` from `@morai/quant`.
- New files: `packages/quant/package.json`, `packages/quant/tsconfig.json`,
  `packages/quant/vitest.config.ts`, `packages/quant/src/bsm.ts`, `packages/quant/src/index.ts`,
  `packages/quant/src/bsm.test.ts`.
- Modified: `packages/core/src/journal/domain/bsm.ts` becomes a re-export shim; `packages/core/
  package.json` gains `"@morai/quant": "workspace:*"`; `packages/core/tsconfig.json` gains the
  `{ "path": "../quant" }` reference.
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Scaffold quant leaf + port BSM parity suite (RED)</name>
  <files>packages/quant/package.json, packages/quant/tsconfig.json, packages/quant/vitest.config.ts, packages/quant/src/bsm.test.ts</files>
  <read_first>
    - packages/shared/package.json (exact pure-leaf package.json shape to copy — name/module/main/types/private/devDependencies)
    - packages/shared/tsconfig.json (canonical pure-leaf tsconfig: composite, outDir, rootDir, no references)
    - packages/core/vitest.config.ts (vitest config analog — defineConfig + workspace aliases pattern)
    - packages/core/src/journal/domain/bsm.test.ts (the FULL parity suite to port — fixtures, TOL=1e-4, fast-check properties with Math.fround() bounds + numRuns:1000)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-PATTERNS.md ("packages/quant/*" exact analog copies; "Vitest fast-check property tests" pattern)
  </read_first>
  <behavior>
    - bsmPrice/bsmGreeks/bsmVega in the quant leaf match the original core values to TOL=1e-4 across the three calibration fixtures
    - fast-check round-trip + monotonicity properties hold across numRuns:1000 with Math.fround() float bounds
    - Parity assertion: for a grid of (S,K,T,sigma,r,q,type) inputs, quant output === a captured snapshot of the original core kernel's output (the test is the oracle that locks parity)
  </behavior>
  <action>
    Create `packages/quant/package.json` by copying `packages/shared/package.json` and renaming
    `@morai/shared` → `@morai/quant` (zero runtime dependencies — no `"dependencies"` key;
    devDependencies = `@types/bun` + `typescript` only). Create `packages/quant/tsconfig.json` as the
    canonical pure-leaf tsconfig (composite:true, outDir:dist, rootDir:src, include:["src"], exclude
    fixtures + `*.test.ts`; NO references — pure leaf). Create `packages/quant/vitest.config.ts`
    mirroring `packages/core/vitest.config.ts` (defineConfig from vitest/config; globals:false). Port
    `packages/core/src/journal/domain/bsm.test.ts` verbatim to `packages/quant/src/bsm.test.ts`,
    keeping imports as `./bsm.ts`, the same fixtures, the same `TOL = 1e-4`, and the same fast-check
    properties (Math.fround() bounds mandatory, numRuns:1000). This test will FAIL initially because
    `packages/quant/src/bsm.ts` does not exist yet — that is the RED state. Run the quant project to
    confirm RED is an unresolved-import / missing-SUT failure, then commit the RED scaffold.
  </action>
  <verify>
    <automated>cd packages/quant && bun x vitest run 2>&1 | grep -qiE 'fail|cannot find|error' && echo "RED confirmed"</automated>
  </verify>
  <acceptance_criteria>
    - `packages/quant/package.json` name is `@morai/quant` with no `"dependencies"` key
    - `packages/quant/tsconfig.json` has `"composite": true` and NO `"references"` array
    - `vitest run --project packages/quant` FAILS (RED) on the missing `./bsm.ts` SUT, not on a test-syntax error
    - The ported test file imports `bsmPrice, bsmGreeks, bsmVega` and `type BsmGreeks` from `./bsm.ts`
  </acceptance_criteria>
  <done>The quant leaf is scaffolded and the ported BSM parity + fast-check suite fails RED on the missing kernel.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Relocate the kernel (GREEN) + barrel</name>
  <files>packages/quant/src/bsm.ts, packages/quant/src/index.ts</files>
  <read_first>
    - packages/core/src/journal/domain/bsm.ts (the FULL kernel to relocate verbatim — header JSDoc, private ncdf/npdf, exported bsmPrice/bsmGreeks/bsmVega + BsmGreeks type)
    - packages/shared/src/index.ts (barrel pattern: comment line + `export type {}` / `export {}` split required by verbatimModuleSyntax, .ts extensions explicit)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-PATTERNS.md ("packages/quant/src/bsm.ts" and "src/index.ts" exact target shapes)
  </read_first>
  <action>
    Copy `packages/core/src/journal/domain/bsm.ts` VERBATIM into `packages/quant/src/bsm.ts` —
    keep the full header JSDoc (A&S reference, D-04/D-12 display-convention table), the unexported
    `ncdf`/`npdf` helpers, and the exported `bsmPrice`/`bsmGreeks`/`bsmVega` + `BsmGreeks` type. The
    file is already zero-import, no `any`/`as`/`!` — preserve that. Create
    `packages/quant/src/index.ts` as the barrel: a one-line purpose comment, then
    `export type { BsmGreeks } from "./bsm.ts";` and `export { bsmPrice, bsmGreeks, bsmVega } from
    "./bsm.ts";` (the type/value split is required by `verbatimModuleSyntax: true`). Run the quant
    project — the parity + fast-check suite must turn GREEN. Commit at green.
  </action>
  <verify>
    <automated>vitest run --project packages/quant</automated>
  </verify>
  <acceptance_criteria>
    - `vitest run --project packages/quant` exits 0 (GREEN) with the full ported parity + fast-check suite passing
    - `packages/quant/src/bsm.ts` has zero import statements (pure leaf)
    - `packages/quant/src/index.ts` exports `bsmPrice`, `bsmGreeks`, `bsmVega`, and type `BsmGreeks`
    - `grep -c 'any\|as \| as$' packages/quant/src/bsm.ts` shows no forbidden assertions introduced
  </acceptance_criteria>
  <done>The relocated kernel passes the parity + property suite GREEN; the barrel exports the four symbols.</done>
</task>

<task type="auto">
  <name>Task 3: Convert core bsm.ts to a re-export shim + wire workspace references</name>
  <files>packages/core/src/journal/domain/bsm.ts, packages/core/package.json, packages/core/tsconfig.json</files>
  <read_first>
    - packages/core/src/journal/domain/bsm.ts (current source-of-truth file to replace with a shim)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-PATTERNS.md ("packages/core/src/journal/domain/bsm.ts (modify existing — add re-export)" — the shim pattern that keeps all call sites working; "packages/core/tsconfig.json (modify — add quant reference)")
    - packages/core/package.json (where to add the workspace dep)
    - packages/core/src/analytics/domain/gex.ts (a call site that imports `bsmGreeks` via the relative `../../journal/domain/bsm.ts` path — confirm the shim keeps it working)
    - packages/core/src/journal/domain/iv-inversion.ts (call site importing `bsmPrice, bsmVega` from `./bsm.ts`)
  </read_first>
  <action>
    Replace the body of `packages/core/src/journal/domain/bsm.ts` with a thin re-export shim:
    `export type { BsmGreeks } from "@morai/quant";` and `export { bsmPrice, bsmGreeks, bsmVega }
    from "@morai/quant";` plus a one-line comment that `@morai/quant` now owns this kernel. This
    keeps the existing relative-path call sites (`analytics/domain/gex.ts`, `journal/domain/
    iv-inversion.ts`, `journal/application/computeBsmGreeks.ts`, `journal/index.ts`, and the
    `bsm.test.ts`) working with zero changes — they keep importing from `./bsm.ts` / `../domain/
    bsm.ts`, which now forwards to the leaf. Add `"@morai/quant": "workspace:*"` to
    `packages/core/package.json` dependencies. Add `{ "path": "../quant" }` to the `references` array
    in `packages/core/tsconfig.json`. Run `bun install` to link the workspace dep, then run the FULL
    workspace test + typecheck + lint. The pre-existing core BSM/IV/GEX/snapshot suites are the
    regression guard — they must all still pass through the shim. Do not delete `packages/core/src/
    journal/domain/bsm.test.ts` (it now exercises the shim re-export end to end).
  </action>
  <verify>
    <automated>bun install >/dev/null 2>&1; bun run typecheck && bun run lint && bun run test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '@morai/quant' packages/core/src/journal/domain/bsm.ts` ≥ 1 (shim forwards to the leaf)
    - `packages/core/src/journal/domain/bsm.ts` contains no function bodies (only re-exports)
    - `grep -q '"@morai/quant": "workspace:\*"' packages/core/package.json`
    - `packages/core/tsconfig.json` references include `{ "path": "../quant" }`
    - `bun run typecheck` clean, `bun run lint` exits 0 (no boundary violation — core → quant is now allowed per Plan 01), `bun run test` green across the workspace
  </acceptance_criteria>
  <done>core bsm.ts is a re-export shim, the workspace dep + tsconfig reference are wired, and the entire workspace suite + typecheck + lint stay green.</done>
</task>

</tasks>

<verification>
- `vitest run --project packages/quant` green (parity + fast-check)
- `bun run typecheck && bun run lint && bun run test` green across the workspace (regression through the shim)
- `grep` confirms core bsm.ts forwards to `@morai/quant` and the workspace dep + tsconfig reference exist
- No `any`/`as`/`!` introduced; no `eslint-disable` for boundaries
</verification>

<success_criteria>
`@morai/quant` is a pure leaf exporting the BSM kernel with proven parity to the original; core
imports it through a zero-change shim; the whole workspace stays green. The kernel is now available
for the Analyzer's client-side re-pricing (Plan 10) and any client-side per-position greeks
(Plan 06) without violating the hexagon.
</success_criteria>

<output>
Create `.planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-02-SUMMARY.md` when done
</output>
