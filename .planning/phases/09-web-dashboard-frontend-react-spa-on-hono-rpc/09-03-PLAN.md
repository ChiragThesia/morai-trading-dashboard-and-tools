---
phase: 09-web-dashboard-frontend-react-spa-on-hono-rpc
plan: 03
type: execute
wave: 3
depends_on: ["09-01"]
files_modified:
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/web/vite.config.ts
  - apps/web/vitest.config.ts
  - apps/web/vite-env.d.ts
  - apps/web/index.html
  - apps/web/components.json
  - apps/web/src/main.tsx
  - apps/web/src/index.css
  - apps/web/src/lib/queryClient.ts
  - apps/web/src/lib/supabase.ts
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "apps/web builds with Vite + React + Tailwind v4 (@theme tokens) + shadcn initialized"
    - "TanStack Query provider and Supabase Auth client singletons exist and are wired into main.tsx"
    - "apps/web/vitest.config.ts runs jsdom + React Testing Library and is auto-discovered by the root workspace"
    - "bun run typecheck and bun run lint pass for the new apps/web workspace"
  artifacts:
    - path: "apps/web/package.json"
      provides: "Web workspace deps (react, tanstack, hono parity, supabase, tailwind, charts)"
      contains: "@tanstack/react-query"
    - path: "apps/web/src/index.css"
      provides: "Tailwind v4 @theme Morai palette tokens + shadcn bridge"
      contains: "@theme"
    - path: "apps/web/src/lib/queryClient.ts"
      provides: "QueryClient singleton with retry/staleTime defaults"
      contains: "QueryClient"
    - path: "apps/web/src/lib/supabase.ts"
      provides: "Supabase createClient singleton"
      contains: "createClient"
    - path: "apps/web/vitest.config.ts"
      provides: "jsdom test config for web"
      contains: "jsdom"
  key_links:
    - from: "apps/web/src/main.tsx"
      to: "apps/web/src/lib/queryClient.ts"
      via: "QueryClientProvider client={queryClient}"
      pattern: "QueryClientProvider"
    - from: "apps/web/src/index.css"
      to: "apps/web/vite.config.ts"
      via: "@tailwindcss/vite plugin processes @theme"
      pattern: "tailwindcss"
---

<objective>
Scaffold the `apps/web` workspace: Vite + React 19 + Tailwind v4 (`@theme` palette from the locked
UI-SPEC) + `shadcn init` + the locked component set + a TanStack Query provider singleton + a
Supabase Auth client singleton + a jsdom vitest config with React Testing Library. This is the
foundation of the D-02 vertical thin-slice — it stops at the provider/client level; the auth gate,
RPC client, and Overview screen land in Plans 04-05.

Purpose: De-risk the new-to-the-project stack (Vite, Tailwind v4 `@theme`, shadcn v4 bridge, chart
peer-deps) in one scaffold before any screen is built. RESEARCH flags Tailwind-v4+shadcn and chart
peer-deps as LOW confidence — this plan verifies them by getting a clean build + typecheck.
Output: A buildable, lint-clean, test-ready `apps/web` shell with no screens yet.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-RESEARCH.md
@.planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-PATTERNS.md
@.planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-UI-SPEC.md
@.claude/rules/typescript.md
@.claude/rules/architecture-boundaries.md
@apps/server/package.json
@apps/server/tsconfig.json
@packages/core/vitest.config.ts
</context>

<artifacts_this_phase_produces>
New files/symbols this plan creates (exclude from drift verification):
- New `apps/web` workspace: `package.json`, `tsconfig.json`, `vite.config.ts`, `vitest.config.ts`,
  `vite-env.d.ts`, `index.html`, `components.json` (shadcn), `src/main.tsx`, `src/index.css`,
  plus `src/components/ui/*` written by `shadcn add` (card, tabs, slider, toggle-group, dialog,
  input, textarea, badge, tooltip, skeleton, separator, button).
- `queryClient` singleton export in `apps/web/src/lib/queryClient.ts`.
- `supabase` client singleton export in `apps/web/src/lib/supabase.ts`.
- Tailwind `@theme` Morai palette tokens in `apps/web/src/index.css`.
- New deps in `apps/web/package.json`: react, react-dom, @tanstack/react-query, hono (parity
  version), @supabase/supabase-js, zod, tailwindcss, @tailwindcss/vite, @visx/* set, uplot,
  uplot-react, echarts, echarts-for-react, lucide-react; dev: @testing-library/react,
  @testing-library/user-event, jsdom, @vitejs/plugin-react, vite, typescript, @types/react,
  @types/react-dom.
</artifacts_this_phase_produces>

<user_setup>
  - service: supabase
    why: "SPA auth client needs the project URL and anon key at runtime (browser)"
    env_vars:
      - name: VITE_SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> API -> Project URL (same project as Phase 8 backend)"
      - name: VITE_SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> anon/public key"
      - name: VITE_API_BASE_URL
        source: "Railway API base URL from Phase 8 deploy (must be https://); used by the RPC client in Plan 04"
</user_setup>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React + Tailwind v4 build + workspace wiring</name>
  <files>apps/web/package.json, apps/web/tsconfig.json, apps/web/vite.config.ts, apps/web/vite-env.d.ts, apps/web/index.html, apps/web/src/main.tsx, apps/web/src/index.css</files>
  <read_first>
    - apps/server/package.json (workspace package.json analog — name/module/private/scripts/workspace-dep shape; the EXACT hono version to match for hc<AppType>() parity)
    - apps/server/tsconfig.json (tsconfig analog — composite/outDir/rootDir/references shape)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-PATTERNS.md ("apps/web/package.json", "apps/web/tsconfig.json", "apps/web/vitest.config.ts" exact target shapes incl. references list and `types: []`)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-RESEARCH.md (Pattern 1 Tailwind v4 @theme CSS + vite.config; Pitfall 5 VITE_ prefix + vite-env.d.ts; Open Question 4 dev proxy)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-UI-SPEC.md ("Color" token table — the exact hex values for the @theme block; "Typography" fonts Space Grotesk + JetBrains Mono via Google Fonts <link>)
  </read_first>
  <action>
    Create `apps/web/package.json` modeled on `apps/server/package.json`: name `@morai/web`,
    version `0.0.1`, `module` `src/main.tsx`, private, scripts `dev`/`build`/`typecheck`. Add
    dependencies per RESEARCH Standard Stack: react, react-dom, @tanstack/react-query,
    `hono` pinned to the SAME version range as `apps/server` (hc<AppType>() parity — mismatched hono
    breaks type inference), @supabase/supabase-js, zod, the @visx/* set, uplot, uplot-react, echarts,
    echarts-for-react, lucide-react; devDependencies: tailwindcss, @tailwindcss/vite,
    @vitejs/plugin-react, vite, typescript, @types/react, @types/react-dom, @testing-library/react,
    @testing-library/user-event, jsdom. Workspace deps `@morai/contracts` and `@morai/quant` as
    `workspace:*`. Create `apps/web/tsconfig.json` per PATTERNS (composite, `types: []` to drop bun
    globals, references to `../../packages/contracts`, `../../packages/shared`, `../../packages/quant`,
    `../server`). Create `apps/web/vite.config.ts` with `react()` + `tailwindcss()` plugins, `@`→`/src`
    alias, and a dev `server.proxy` forwarding `/api` to `process.env.VITE_API_BASE_URL`
    (changeOrigin:true) to bypass CORS in dev. Create `apps/web/vite-env.d.ts` typing the three
    `VITE_*` env vars on `ImportMetaEnv`. Create `apps/web/index.html` with the root div + Google
    Fonts `<link>` for Space Grotesk + JetBrains Mono. Create `apps/web/src/index.css`: `@import
    "tailwindcss";` then the `@theme` block mapping the LOCKED UI-SPEC palette hexes to `--color-*`
    tokens (bg/panel/panel2/raise/line/line2/faint/txt/muted/dim/up/down/violet/amber/blue/cyan +
    dark tints), the `@theme inline` shadcn bridge, a `:root` block with shadcn base vars, and the
    `body` radial-gradient background + mono font + tabular-nums. Create a minimal
    `apps/web/src/main.tsx` that mounts a placeholder root (real App lands Plan 04). Run `bun install`
    then `bun run build` (vite build) from apps/web and confirm a clean production build.
  </action>
  <verify>
    <automated>bun install >/dev/null 2>&1; cd apps/web && bun run build && test -d dist && echo "BUILD OK"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/package.json` hono version range matches `apps/server/package.json` exactly
    - `apps/web/package.json` lists `@tanstack/react-query`, `@supabase/supabase-js`, `@tailwindcss/vite`, the `@visx/*` set, `uplot`, `echarts`, `echarts-for-react`, and `@morai/quant`/`@morai/contracts` as `workspace:*`
    - `apps/web/src/index.css` contains an `@theme` block with the locked palette tokens (e.g. `--color-violet: #a78bfa`)
    - `apps/web/tsconfig.json` has `"types": []` and references `../server` + `../../packages/quant`
    - `cd apps/web && bun run build` exits 0 and produces `apps/web/dist`
  </acceptance_criteria>
  <done>apps/web builds cleanly with Vite + React + Tailwind v4 @theme; workspace deps + tsconfig references are wired.</done>
</task>

<task type="auto">
  <name>Task 2: shadcn init + locked components + Supabase/QueryClient singletons</name>
  <files>apps/web/components.json, apps/web/src/lib/queryClient.ts, apps/web/src/lib/supabase.ts, apps/web/src/main.tsx</files>
  <read_first>
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-RESEARCH.md (Pattern 2 shadcn init + add; Pattern 4 QueryClient defaults; Pattern 5 supabase createClient; Pitfall 2 @theme inline; Pitfall 3 tailwindcss-animate v4)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-UI-SPEC.md ("Design System" shadcn Gate — dark theme, CSS variables, the exact 12-component add list, no third-party registries)
    - apps/web/src/index.css (the @theme + :root written in Task 1 — shadcn vars override targets)
  </read_first>
  <action>
    Run `npx shadcn@latest init` inside `apps/web` (dark style, CSS variables yes) producing
    `components.json`; then `npx shadcn@latest add card tabs slider toggle-group dialog input
    textarea badge tooltip skeleton separator button` (the LOCKED list — shadcn official registry
    only, no third-party). After init, reconcile any shadcn-generated `:root`/`@theme` against the
    Task 1 Morai palette so `bg-background`/`bg-card`/`border-border` resolve to the locked tokens via
    the `@theme inline` bridge (Pitfall 2). If `npx shadcn init` adds `tailwindcss-animate`, verify the
    Dialog/Skeleton animations still work under Tailwind v4 or replace with native `animate-*`
    (Pitfall 3). Create `apps/web/src/lib/queryClient.ts` exporting a single `queryClient =
    new QueryClient({...})` with `retry: 3`, exponential `retryDelay` capped at 30s,
    `refetchOnWindowFocus: true`, `staleTime: 20_000` (RESEARCH Pattern 4). Create
    `apps/web/src/lib/supabase.ts` exporting a single `supabase = createClient(VITE_SUPABASE_URL,
    VITE_SUPABASE_ANON_KEY)` (the only permitted `as string` on `import.meta.env.*` per typescript
    rule exception). Update `apps/web/src/main.tsx` to wrap the placeholder root in
    `QueryClientProvider client={queryClient}`. No `any`/`!`; only `console.warn`/`console.error`.
    Run `bun run typecheck && bun run lint && bun run build`.
  </action>
  <verify>
    <automated>cd apps/web && bun run typecheck && bun run build && test -d src/components/ui && grep -q 'QueryClientProvider' src/main.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/components.json` exists (shadcn initialized)
    - `apps/web/src/components/ui/` contains the 12 locked components (card, tabs, slider, toggle-group, dialog, input, textarea, badge, tooltip, skeleton, separator, button)
    - `apps/web/src/lib/queryClient.ts` exports a `QueryClient` with `retry: 3` and `refetchOnWindowFocus: true`
    - `apps/web/src/lib/supabase.ts` exports `supabase` via `createClient` reading the two `VITE_SUPABASE_*` vars
    - `apps/web/src/main.tsx` wraps the root in `QueryClientProvider`
    - `bun run typecheck`, `bun run lint`, `bun run build` all exit 0 from apps/web
  </acceptance_criteria>
  <done>shadcn is initialized with the 12 locked components on the Morai palette; the QueryClient and Supabase singletons exist and main.tsx provides the query client.</done>
</task>

<task type="auto">
  <name>Task 3: jsdom vitest config + React Testing Library smoke test</name>
  <files>apps/web/vitest.config.ts, apps/web/src/main.tsx</files>
  <read_first>
    - packages/core/vitest.config.ts (vitest config analog — defineConfig + workspace aliases)
    - apps/web/vite.config.ts (the react plugin + @ alias from Task 1 — reuse in vitest)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-VALIDATION.md ("Wave 0 Requirements" — jsdom env + react plugin + RTL dev deps; the quick-run command)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-PATTERNS.md ("Root vitest workspace auto-discovery" — the apps/*/vitest.config.ts glob already matches)
  </read_first>
  <action>
    Create `apps/web/vitest.config.ts` using `defineConfig` from vitest/config with the React plugin,
    `test.environment: "jsdom"`, `test.globals: false`, and resolve aliases for `@morai/contracts`,
    `@morai/quant`, `@morai/shared`, and `@`→`/src` (Vite/Vitest needs explicit aliases because
    workspace packages expose `module` not `exports`). Add a tiny RTL smoke test (e.g.
    `apps/web/src/main.smoke.test.tsx`) that renders a trivial element through `@testing-library/
    react` and asserts it is in the document — this proves the jsdom + RTL pipeline works (it is a
    harness check, not a product behavior). Confirm the root `vitest.config.ts` glob
    `apps/*/vitest.config.ts` auto-discovers the new project (no root change needed). Run the web
    project and the full workspace suite.
  </action>
  <verify>
    <automated>vitest run --project apps/web && bun run test</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/vitest.config.ts` sets `environment: "jsdom"` and registers the react plugin + workspace aliases
    - `vitest run --project apps/web` exits 0 and runs at least one RTL smoke test
    - `bun run test` (root workspace) discovers and runs the apps/web project (no root config edit required)
    - `bun run typecheck && bun run lint` remain green
  </acceptance_criteria>
  <done>apps/web has a jsdom + RTL vitest config auto-discovered by the workspace, and a passing smoke test proves the test pipeline.</done>
</task>

</tasks>

<verification>
- `cd apps/web && bun run build` exits 0 (Vite + Tailwind v4 + shadcn all resolve)
- `vitest run --project apps/web` green (jsdom + RTL pipeline works)
- `bun run typecheck && bun run lint && bun run test` green across the workspace
- shadcn 12-component set present; QueryClient + Supabase singletons exported and wired
</verification>

<success_criteria>
A buildable, lint-clean, test-ready `apps/web` shell exists with the Tailwind v4 Morai palette, the
shadcn component set, a TanStack Query provider, a Supabase Auth client, and a jsdom test harness —
the foundation the thin-slice (Plans 04-05) builds on. The LOW-confidence stack pieces (Tailwind v4
+ shadcn bridge, chart peer-deps) are verified by a clean build.
</success_criteria>

<output>
Create `.planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-03-SUMMARY.md` when done
</output>
