---
phase: 09-web-dashboard-frontend-react-spa-on-hono-rpc
plan: 06
type: execute
wave: 6
depends_on: ["09-02", "09-05"]
files_modified:
  - apps/web/src/screens/Positions.tsx
  - apps/web/src/components/charts/GreekStrips.tsx
  - apps/web/src/components/charts/GreekStrips.test.tsx
  - apps/web/src/components/AttributionWaterfall.tsx
  - apps/web/src/components/AttributionWaterfall.test.tsx
  - apps/web/src/components/LevelBar.tsx
  - apps/web/src/lib/position-greeks.ts
  - apps/web/src/lib/position-greeks.test.ts
  - apps/web/src/screens/Positions.test.tsx
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "POSITIONS-01 is resolved: confirmed whether GET /api/positions carries computed greeks; if not, greeks are computed client-side via @morai/quant (the shared kernel — not a fake/cache)"
    - "The Positions screen renders open positions, the per-leg greeks table, greeks-vs-spot strips, and the attribution waterfall from live data"
    - "Per-position greeks computed in the browser agree with the shared kernel the server uses (cross-screen consistency, D-01)"
  artifacts:
    - path: "apps/web/src/screens/Positions.tsx"
      provides: "Positions screen per UI-SPEC"
      contains: "Why it's moving"
    - path: "apps/web/src/lib/position-greeks.ts"
      provides: "Client-side per-position greeks via @morai/quant"
      contains: "@morai/quant"
    - path: "apps/web/src/components/charts/GreekStrips.tsx"
      provides: "uPlot 4-panel synced greek strips"
      contains: "uplot"
    - path: "apps/web/src/components/AttributionWaterfall.tsx"
      provides: "Greek-attribution waterfall (reused on Analyzer)"
      contains: "residual"
  key_links:
    - from: "apps/web/src/lib/position-greeks.ts"
      to: "packages/quant"
      via: "imports bsmGreeks from @morai/quant"
      pattern: "bsmGreeks"
    - from: "apps/web/src/screens/Positions.tsx"
      to: "apps/web/src/hooks/usePositions.ts"
      via: "reads live positions"
      pattern: "usePositions"
---

<objective>
Build the Positions screen and resolve POSITIONS-01. `GET /api/positions` returns
`brokerPosition[]` with NO computed greeks (occSymbol/putCall/qty/averagePrice/marketValue/
underlyingSymbol only). Per D-03 (live-only, fix at source — never fake/cache in the frontend): the
Positions/greeks-vs-spot/attribution UI needs per-position delta/gamma/theta/vega. Because the GEX
endpoint already re-prices the chain server-side and the trader's own positions are the SAME BSM
math, the correct path is to compute per-position greeks CLIENT-SIDE using the shared `@morai/quant`
kernel (D-01) — this is the one shared kernel guaranteeing the numbers match the server, NOT a
hand-rolled divergent copy. First task confirms the endpoint; if a backend greeks field was added in
Phase 8, use it instead.

Purpose: Deliver the Positions deep-dive (anchor: the greek-attribution waterfall) with correct,
consistent greeks. Establish GreekStrips, AttributionWaterfall, and LevelBar components reused by the
Analyzer (Plan 10).
Output: Positions screen + position-greeks lib + GreekStrips/AttributionWaterfall/LevelBar.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-UI-SPEC.md
@.planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-RESEARCH.md
@.planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-PATTERNS.md
@.claude/rules/typescript.md
@.claude/rules/tdd.md
@packages/contracts/src/brokerage.ts
@mockups/positions-v1.html
</context>

<artifacts_this_phase_produces>
New files/symbols this plan creates (exclude from drift verification):
- `apps/web/src/screens/Positions.tsx` — `Positions` screen.
- `apps/web/src/lib/position-greeks.ts` — `computePositionGreeks` (uses `@morai/quant` `bsmGreeks`).
- `apps/web/src/components/charts/GreekStrips.tsx` — `GreekStrips` (uPlot 4-panel synced).
- `apps/web/src/components/AttributionWaterfall.tsx` — `AttributionWaterfall` (reused on Analyzer).
- `apps/web/src/components/LevelBar.tsx` — `LevelBar` (call wall / flip / strike / spot / put wall).
- Tests: `apps/web/src/lib/position-greeks.test.ts`, `apps/web/src/screens/Positions.test.tsx`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Resolve POSITIONS-01 + client-side per-position greeks via @morai/quant (TDD)</name>
  <files>apps/web/src/lib/position-greeks.ts, apps/web/src/lib/position-greeks.test.ts</files>
  <read_first>
    - packages/contracts/src/brokerage.ts (CONFIRM the current brokerPosition fields — occSymbol, putCall, longQty, shortQty, averagePrice, marketValue, underlyingSymbol; verify whether any greeks field was added in Phase 8)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-RESEARCH.md (Open Question 1 POSITIONS-01 recommendation: confirm contract; if greeks missing, thin client-side computation via @morai/quant using positions + chain)
    - packages/quant/src/bsm.ts (the bsmGreeks signature: S, K, T, sigma, r, q, type — the kernel to call)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-UI-SPEC.md ("Positions screen" per-leg greeks table columns Leg/Mark/Δ/Γ/Θ/d/Vega/IV)
    - packages/contracts/src/gex.ts (gex.spot is the live spot input for greeks; chain IV source)
  </read_first>
  <action>
    FIRST confirm POSITIONS-01 by reading `packages/contracts/src/brokerage.ts`: record in the SUMMARY
    whether `brokerPosition` carries computed greeks. Current state (research-confirmed): it does NOT.
    Decision tree per D-03: (a) if Phase 8 added a greeks field, consume it directly and skip
    client-side compute; (b) if absent — the documented case — compute per-position greeks client-side
    via the shared `@morai/quant` kernel (this is the cross-screen-consistency use of D-01, NOT a
    fake). Write `apps/web/src/lib/position-greeks.test.ts` FIRST (RED): assert that
    `computePositionGreeks(position, spot, iv, rate)` returns delta/gamma/theta/vega equal to a direct
    `bsmGreeks(...)` call for the same parsed inputs (parity with the kernel), and a fast-check
    property that scaling qty scales delta linearly. Run RED. Then write
    `apps/web/src/lib/position-greeks.ts`: parse the OCC strike/type/expiry from `occSymbol` (reuse the
    shared OCC parser from `@morai/shared` if available — do NOT hand-roll), derive T from expiry,
    call `bsmGreeks` from `@morai/quant`, scale by net qty (longQty − shortQty). No `any`/`as`/`!`;
    parse, don't cast. If the backend DOES carry greeks (case a), this lib still exists as the Analyzer's
    re-pricing helper but reads the field for Positions. Run GREEN. Commit at green.
  </action>
  <verify>
    <automated>vitest run --project apps/web -t "position-greeks" && cd apps/web && bun run typecheck && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the POSITIONS-01 finding (greeks present or absent in brokerPosition)
    - `position-greeks.test.ts` asserts kernel parity (computePositionGreeks === bsmGreeks for same inputs) + a qty-scaling property; both pass
    - `apps/web/src/lib/position-greeks.ts` imports `bsmGreeks` from `@morai/quant` (the shared kernel — not a re-implementation)
    - No `as`/`any`/`!`; OCC parsing reuses the shared parser, not a new hand-rolled one
    - `vitest run --project apps/web -t "position-greeks"` exits 0
  </acceptance_criteria>
  <done>POSITIONS-01 is resolved and per-position greeks are computed via the shared @morai/quant kernel with proven parity (D-01/D-03).</done>
</task>

<task type="auto">
  <name>Task 2: GreekStrips (uPlot) + AttributionWaterfall + LevelBar components + isolated smoke tests</name>
  <files>apps/web/src/components/charts/GreekStrips.tsx, apps/web/src/components/charts/GreekStrips.test.tsx, apps/web/src/components/AttributionWaterfall.tsx, apps/web/src/components/AttributionWaterfall.test.tsx, apps/web/src/components/LevelBar.tsx</files>
  <read_first>
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-RESEARCH.md (Pattern 8 uPlot greek strips with cursor.sync.key across 4 panels + uplot-react memoized options; Pitfall 8 import uPlot.min.css)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-UI-SPEC.md ("Positions screen" greeks-vs-spot strips + strike vertical line; "Analyzer screen" greek strips colors Δ=blue/Γ=cyan/Θ=amber/Vega=teal + zero line + spot line; "Attribution waterfall" Positions 5-item variant spot/theta/vega-front/vega-back/residual + track/fill spec; "Strike structure" LevelBar)
    - apps/web/src/lib/position-greeks.ts (the greeks source for the strips)
    - apps/web/src/components/Overview.test.tsx OR apps/web/src/screens/Overview.test.tsx (the RTL render pattern from Plan 05 to mirror)
    - mockups/positions-v1.html (greek strips + waterfall + level bar visual reference)
    - NOTE: uPlot draws to a `<canvas>`; under jsdom `HTMLCanvasElement.prototype.getContext` returns null and uPlot may throw on mount. The GreekStrips smoke test must provide a minimal canvas getContext stub (or mock `uplot-react` to a passthrough) so the wrapper mounts without throwing — assert the panel labels render, not the canvas pixels. AttributionWaterfall + LevelBar are pure DOM (no canvas) and need no stub.
  </read_first>
  <action>
    Create `apps/web/src/components/charts/GreekStrips.tsx`: a 4-panel synced uPlot small-multiple
    (Net Δ / Net Γ / Net Θ/d / Net Vega vs spot) using `uplot-react` with memoized options, a shared
    `cursor.sync.key` across all four, the locked curve colors (Δ `#5b9cf6`, Γ `#22d3ee`, Θ `#f0b429`,
    Vega `#26a69a`), zero line `#283342`, spot vertical `#5b9cf6` 45% opacity, and a dot at current
    spot; import `uplot/dist/uPlot.min.css` (Pitfall 8). Accept an optional strike vertical line prop
    (Positions adds `#46556a` dashed). Create `apps/web/src/components/AttributionWaterfall.tsx`:
    label | center-anchored track bar | signed value, fills extending left/right from the 50% midpoint
    proportional to magnitude, per-row colors per the spec (spot=blue, theta=amber, vega-front=coral,
    vega-back=teal, residual=dim); support both the Positions 5-item set and the Analyzer combined set
    via props. Create `apps/web/src/components/LevelBar.tsx`: the call wall / γ flip / your strike /
    spot / put wall level bar with color-coded markers. All chart math from the position-greeks lib /
    live data — no seed data. No `any`/`!`. Keep these reusable (Analyzer Plan 10 imports all three).

    Add isolated RTL smoke-render tests (vitest + jsdom). `GreekStrips.test.tsx`: with the canvas
    getContext stub (or a passthrough mock of `uplot-react`), render `<GreekStrips>` with mock
    greek-curve data and assert the four panel labels (Net Δ / Net Γ / Net Θ/d / Net Vega) are in the
    DOM and the component mounts without throwing. `AttributionWaterfall.test.tsx`: render with mock
    rows and assert a known row label (e.g. "residual") is in the DOM and the component mounts without
    throwing, for both the 5-item and combined variants. These are the Positions visual anchor and the
    Analyzer reuse target, so isolated coverage matters. `bun run typecheck && bun run lint`.
  </action>
  <verify>
    <automated>vitest run --project apps/web -t "GreekStrips" && vitest run --project apps/web -t "AttributionWaterfall" && cd apps/web && bun run typecheck && bun run lint && grep -q "uPlot.min.css" src/components/charts/GreekStrips.tsx && grep -q "residual" src/components/AttributionWaterfall.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `GreekStrips.tsx` uses `uplot-react`, a shared `cursor.sync.key`, the locked curve colors, and imports `uPlot.min.css`
    - `GreekStrips.test.tsx` renders the component under jsdom (with the canvas stub / uplot-react mock) and asserts the four panel labels are present + no throw on mount
    - `AttributionWaterfall.tsx` renders center-anchored fills with the per-row colors and supports both the 5-item and combined variants
    - `AttributionWaterfall.test.tsx` renders both variants and asserts a known row label present + no throw on mount
    - `LevelBar.tsx` renders call wall / flip / strike / spot / put wall markers
    - `vitest run --project apps/web -t "GreekStrips"` and `-t "AttributionWaterfall"` both exit 0; `bun run typecheck` + `bun run lint` green; no seed data
  </acceptance_criteria>
  <done>The uPlot greek strips, attribution waterfall, and level bar render from live/computed data, are covered by isolated RTL smoke tests, and are reusable by the Analyzer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Positions screen assembled from live data</name>
  <files>apps/web/src/screens/Positions.tsx, apps/web/src/screens/Positions.test.tsx</files>
  <read_first>
    - apps/web/src/hooks/usePositions.ts (live positions source)
    - apps/web/src/lib/position-greeks.ts (per-position greeks)
    - apps/web/src/components/charts/GreekStrips.tsx, AttributionWaterfall.tsx, LevelBar.tsx (Task 2 components)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-UI-SPEC.md ("Positions screen" Row1/Row2 spans + "Visual Anchor" = the attribution waterfall; "Copywriting Contract" Positions headings/badges/KPI labels + the "Why it's moving" body text; "Empty / loading / error states")
    - mockups/positions-v1.html (full screen layout)
  </read_first>
  <behavior>
    - Positions renders the locked "No open positions…" empty state when usePositions is empty (D-04)
    - With positions: renders the open-positions list, the 5-item "Why it's moving" attribution waterfall, the Position KPI card (Mark/Debit/Unreal/DTE) + per-leg greeks table, the greeks-vs-spot strips, and the strike-vs-structure level bar
    - Selecting a position drives the deep-dive (waterfall + KPI card + strips reflect the selection)
  </behavior>
  <action>
    Write `apps/web/src/screens/Positions.test.tsx` FIRST (RED): empty positions → assert the locked
    "No open positions…" copy; one position → assert the "Why it's moving" waterfall, the Position KPI
    labels (Mark · Debit · Unreal · DTE), and the per-leg greeks columns render. Run RED. Then build
    `apps/web/src/screens/Positions.tsx` to the UI-SPEC: Row 1 (Open positions span-3 list /
    Why-it's-moving span-5 5-item AttributionWaterfall + total row + the locked callout body /
    Position card span-4: 4-KPI grid + per-leg greeks table from position-greeks); Row 2 (Greeks-vs-
    spot span-8 GreekStrips with the strike line / Strike-vs-structure span-4 LevelBar + distances +
    callout). All headings/badges/KPI labels use the EXACT locked copy. Live-only; empty/loading/error
    use the locked states (skeletons; region error never crashes). Wire row-click selection. No
    `any`/`!`. Run GREEN + typecheck + lint. Commit at green.
  </action>
  <verify>
    <automated>vitest run --project apps/web -t "Positions" && cd apps/web && bun run typecheck && bun run lint</automated>
  </verify>
  <acceptance_criteria>
    - `Positions.test.tsx` asserts the empty state AND the populated deep-dive (waterfall + KPI labels + per-leg greeks); all pass
    - `vitest run --project apps/web -t "Positions"` exits 0
    - Headings/badges/KPI labels match the UI-SPEC Positions copy exactly
    - Positions reads live `usePositions()` + computed greeks (no seed data); `bun run typecheck` + `bun run lint` green
  </acceptance_criteria>
  <done>The Positions screen renders the live deep-dive with correct greeks and the locked copy/empty state, proven by passing tests.</done>
</task>

</tasks>

<verification>
- `vitest run --project apps/web -t "position-greeks"` and `-t "Positions"` green
- `bun run typecheck && bun run lint && bun run test` green
- POSITIONS-01 finding recorded; greeks via @morai/quant with kernel parity proven
- Reusable GreekStrips/AttributionWaterfall/LevelBar in place for the Analyzer
</verification>

<success_criteria>
The Positions screen renders the live deep-dive (anchor: the attribution waterfall) with per-position
greeks computed via the one shared @morai/quant kernel — guaranteeing they match the server-computed
P&L. POSITIONS-01 is resolved per D-03 (no fake/cache), and the strip/waterfall/level-bar components
are ready for the Analyzer.
</success_criteria>

<output>
Create `.planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-06-SUMMARY.md` when done
</output>
