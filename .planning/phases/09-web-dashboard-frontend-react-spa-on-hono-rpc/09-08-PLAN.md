---
phase: 09-web-dashboard-frontend-react-spa-on-hono-rpc
plan: 08
type: execute
wave: 6
depends_on: ["09-05"]
files_modified:
  - apps/web/src/screens/Market.tsx
  - apps/web/src/components/charts/GammaProfile.tsx
  - apps/web/src/components/charts/GexBars.tsx
  - apps/web/src/components/charts/GexBars.test.tsx
  - apps/web/src/components/charts/GexByExpiry.tsx
  - apps/web/src/lib/gex-regime.ts
  - apps/web/src/lib/gex-regime.test.ts
  - apps/web/src/screens/Market.test.tsx
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "The Market screen renders the net dealer gamma profile (visx), GEX-by-strike and GEX-by-expiry bars (ECharts), key levels, and the regime strip from the live GEX snapshot"
    - "The Charm/Vanna and intraday-delta-flow coming-soon stubs render as badged dashed-border placeholders, never errors, never omitted"
    - "The regime strip correctly classifies AMPLIFY (negative γ) vs DAMPEN (positive γ) from the snapshot"
  artifacts:
    - path: "apps/web/src/screens/Market.tsx"
      provides: "Market screen per UI-SPEC"
      contains: "Net dealer gamma profile"
    - path: "apps/web/src/components/charts/GexBars.tsx"
      provides: "ECharts GEX/OI/Volume by-strike bars"
      contains: "echarts"
    - path: "apps/web/src/components/charts/GammaProfile.tsx"
      provides: "visx net-gamma profile (compact + full)"
      contains: "AreaClosed"
    - path: "apps/web/src/lib/gex-regime.ts"
      provides: "AMPLIFY/DAMPEN regime classification"
      contains: "AMPLIFY"
  key_links:
    - from: "apps/web/src/screens/Market.tsx"
      to: "apps/web/src/hooks/useGex.ts"
      via: "all Market charts read the live GEX snapshot"
      pattern: "useGex"
    - from: "apps/web/src/screens/Market.tsx"
      to: "apps/web/src/components/stubs/ComingSoon.tsx"
      via: "Charm/Vanna + intraday-flow stubs"
      pattern: "ComingSoon"
---

<objective>
Build the Market screen: the Net Dealer Gamma Profile (visx, the screen anchor), GEX-by-strike bars
with the GEX/OI/Volume toggle and GEX-by-expiry bars (both Apache ECharts), the key-levels table, the
regime strip (AMPLIFY/DAMPEN), and the TWO coming-soon stubs (Charm/Vanna, intraday delta-flow) — all
from the live Phase-8 GEX snapshot via the typed RPC client. GEX is server-computed (D-01: the heavy
full-chain GEX stays server-side); the frontend reads `useGex()` and never recomputes it.

Purpose: The Market screen surfaces the dealer-positioning market structure. It exercises the ECharts
integration and ships two of the three locked coming-soon stubs (the third, economic-calendar, ships
on Overview in Plan 05). Reuse the GammaProfile + GexBars on the Analyzer right panel (Plan 10).
Output: Market screen + GammaProfile/GexBars/GexByExpiry chart components + gex-regime classifier.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-UI-SPEC.md
@.planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-RESEARCH.md
@.planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-PATTERNS.md
@.claude/rules/typescript.md
@.claude/rules/tdd.md
@packages/contracts/src/gex.ts
@mockups/market-v1.html
@mockups/gex-snapshot.json
</context>

<artifacts_this_phase_produces>
New files/symbols this plan creates (exclude from drift verification):
- `apps/web/src/screens/Market.tsx` — `Market` screen.
- `apps/web/src/components/charts/GammaProfile.tsx` — `GammaProfile` (visx, compact + full).
- `apps/web/src/components/charts/GexBars.tsx` — `GexBars` (ECharts, GEX/OI/Volume toggle).
- `apps/web/src/components/charts/GexByExpiry.tsx` — `GexByExpiry` (ECharts vertical bars).
- `apps/web/src/lib/gex-regime.ts` — `classifyRegime` (AMPLIFY/DAMPEN from netGammaAtSpot).
- Tests: `apps/web/src/lib/gex-regime.test.ts`, `apps/web/src/screens/Market.test.tsx`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GEX regime classifier (TDD) + GammaProfile (visx)</name>
  <files>apps/web/src/lib/gex-regime.ts, apps/web/src/lib/gex-regime.test.ts, apps/web/src/components/charts/GammaProfile.tsx</files>
  <read_first>
    - packages/contracts/src/gex.ts (gexSnapshotResponse: spot, flip, callWall, putWall, netGammaAtSpot, profile[{spot,gamma}], strikes[{k,gex,coi,poi,vol}], byExpiry[{date,gex}])
    - mockups/gex-snapshot.json (sample payload + reference values: γ-flip ≈ 7488, net ≈ −$47B/1%, walls 7400/7600)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-UI-SPEC.md ("Market screen" regime strip AMPLIFY (coral, negative γ) vs DAMPEN (teal, positive γ); "Net dealer gamma" profile spec teal/coral fills above/below zero + flip + spot lines)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-RESEARCH.md (Pattern 7 visx AreaClosed/LinePath/LinearGradient gradient fills above/below zero)
  </read_first>
  <behavior>
    - classifyRegime(netGammaAtSpot) returns "AMPLIFY" when net gamma at spot < 0, "DAMPEN" when ≥ 0
    - The classifier is pure (no I/O) and drives the regime strip + the GEX note text
  </behavior>
  <action>
    Write `apps/web/src/lib/gex-regime.test.ts` FIRST (RED): assert `classifyRegime` returns "AMPLIFY"
    for a negative `netGammaAtSpot` and "DAMPEN" for a non-negative value, using the mockup reference
    (net ≈ −$47B → AMPLIFY). Run RED. Then write `apps/web/src/lib/gex-regime.ts` with the pure
    `classifyRegime` function. Create `apps/web/src/components/charts/GammaProfile.tsx`: a visx net-
    gamma profile curve (`AreaClosed` + `LinePath` + `LinearGradient`) reading `profile[{spot,gamma}]`,
    with teal fill above the zero line and coral below, an amber γ-flip vertical line at `flip` and a
    blue spot vertical line at `spot`. Support a `compact` prop (300×130 for the Analyzer right panel)
    and a full size (720×230 for Market) via dimension props. No `any`/`!`. Run GREEN + typecheck.
    Commit at green.
  </action>
  <verify>
    <automated>vitest run --project apps/web -t "gex-regime" && cd apps/web && bun run typecheck && grep -q 'AMPLIFY' src/lib/gex-regime.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `gex-regime.test.ts` asserts AMPLIFY for negative net gamma and DAMPEN for non-negative; both pass
    - `gex-regime.ts` exports a pure `classifyRegime`
    - `GammaProfile.tsx` uses visx `AreaClosed`/`LinePath`/`LinearGradient` with teal/coral fills + flip + spot lines and a `compact` size prop
    - `vitest run --project apps/web -t "gex-regime"` exits 0; `bun run typecheck` green
  </acceptance_criteria>
  <done>The regime classifier is proven by tests and the visx gamma profile renders compact + full sizes from the live snapshot.</done>
</task>

<task type="auto">
  <name>Task 2: ECharts GEX-by-strike bars (GEX/OI/Volume toggle) + GEX-by-expiry bars + isolated smoke tests</name>
  <files>apps/web/src/components/charts/GexBars.tsx, apps/web/src/components/charts/GexBars.test.tsx, apps/web/src/components/charts/GexByExpiry.tsx</files>
  <read_first>
    - packages/contracts/src/gex.ts (strikes[{k,gex,coi,poi,vol}] for the toggle; byExpiry[{date,gex}])
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-RESEARCH.md (Pattern 9 echarts-for-react ReactECharts, peer dep echarts, transparent background, auto-resize; Pitfall 8 N/A here)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-UI-SPEC.md ("Market screen" GEX by strike horizontal bars + GEX/OI/Volume toggle (GEX teal-right/coral-left; OI call-teal/put-coral; Volume blue) + wall/spot dashed reference lines; "GEX by expiry" vertical bars all coral + date labels + value labels; "Copywriting Contract" Market GEX badges + toggle options)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-VALIDATION.md (RTL render-only convention; assert option/data shape, not canvas output)
    - mockups/market-v1.html (bar chart visual reference)
    - NOTE: ECharts renders to a `<canvas>`; under jsdom `getContext` is null and `echarts-for-react` may throw on init. The GexBars smoke test must mock `echarts-for-react` to a passthrough stub (or stub canvas getContext) so the wrapper mounts without throwing — assert the ToggleGroup options render, not the canvas pixels. The shadcn ToggleGroup itself is pure DOM and needs no stub.
  </read_first>
  <action>
    Create `apps/web/src/components/charts/GexBars.tsx`: a `ReactECharts` horizontal bar chart over
    `strikes[]` with a shadcn `ToggleGroup` for GEX / OI wall / Volume (GEX: teal bars right / coral
    left of center using `gex`; OI: call teal/put coral using `coi`/`poi`; Volume: blue bars from left
    using `vol`), with put-wall / call-wall / spot horizontal dashed reference lines drawn over the
    bars. Transparent background; let echarts-for-react own resize/dispose (do NOT hand-roll the
    lifecycle). Create `apps/web/src/components/charts/GexByExpiry.tsx`: a `ReactECharts` vertical bar
    chart over `byExpiry[{date,gex}]` (all-coral bars per spec, date labels on the x-axis, value
    labels on bars). Parse the snapshot data through the gex contract upstream (in useGex) — never
    cast here. No `any`/`!`.

    Add an isolated RTL smoke-render test (vitest + jsdom). `GexBars.test.tsx`: with the
    `echarts-for-react` passthrough mock (or canvas getContext stub), render `<GexBars>` with mock
    `strikes[]` and assert the GEX / OI wall / Volume ToggleGroup options are in the DOM and the
    ECharts wrapper mounts without error; optionally assert that toggling to OI re-shapes the data fed
    to the chart wrapper. `bun run typecheck && lint`.
  </action>
  <verify>
    <automated>vitest run --project apps/web -t "GexBars" && cd apps/web && bun run typecheck && bun run lint && grep -qi "echarts" src/components/charts/GexBars.tsx && grep -qi "echarts" src/components/charts/GexByExpiry.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `GexBars.tsx` uses `echarts-for-react` with a GEX/OI/Volume ToggleGroup and wall/spot reference lines
    - `GexBars.test.tsx` renders the component under jsdom (with the echarts-for-react mock / canvas stub) and asserts the ToggleGroup options render + the wrapper mounts without throwing
    - `GexByExpiry.tsx` uses `echarts-for-react` vertical bars over `byExpiry`
    - Neither component hand-rolls the ECharts init/resize/dispose lifecycle
    - `vitest run --project apps/web -t "GexBars"` exits 0; `bun run typecheck` + `bun run lint` green
  </acceptance_criteria>
  <done>The ECharts GEX-by-strike bars (with the 3-way toggle, covered by an isolated RTL smoke test) and GEX-by-expiry bars render from the live snapshot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Market screen assembled + two coming-soon stubs</name>
  <files>apps/web/src/screens/Market.tsx, apps/web/src/screens/Market.test.tsx</files>
  <read_first>
    - apps/web/src/hooks/useGex.ts (the live snapshot source)
    - apps/web/src/lib/gex-regime.ts (regime classification)
    - apps/web/src/components/charts/GammaProfile.tsx, GexBars.tsx, GexByExpiry.tsx (Task 1/2 components)
    - apps/web/src/components/stubs/ComingSoon.tsx (the reusable stub from Plan 05)
    - apps/web/src/components/LevelBar.tsx (key-levels — reuse from Plan 06 if available; otherwise a key-levels table)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-UI-SPEC.md ("Market screen" regime strip + grid spans; "Coming-Soon Stubs Contract" Charm/Vanna + intraday-flow exact badge/title/body; "Copywriting Contract" Market headings/badges + Charm/Vanna + Intraday-flow stub copy; "Empty / loading / error states" the "GEX data unavailable…" copy)
    - mockups/market-v1.html (full screen layout)
  </read_first>
  <behavior>
    - Market renders the gamma profile, GEX-by-strike bars, GEX-by-expiry bars, key levels, and the regime strip from the live GEX snapshot
    - The Charm/Vanna stub renders with badge "○ next" and the intraday-flow stub with badge "○ needs denser snapshots" — both dashed-border placeholders, never errors, never omitted
    - When GEX data is unavailable, the locked "GEX data unavailable…" copy renders instead of a crash
  </behavior>
  <action>
    Write `apps/web/src/screens/Market.test.tsx` FIRST (RED): mock `useGex` with the sample snapshot →
    assert the "Net dealer gamma profile" heading + the regime strip + the GEX bars regions render;
    assert BOTH the Charm/Vanna ("○ next") and intraday-flow ("○ needs denser snapshots") ComingSoon
    stubs render with their badges; mock `useGex` with no data → assert the locked "GEX data
    unavailable…" copy renders (no crash). Run RED. Then build `apps/web/src/screens/Market.tsx` to the
    UI-SPEC: the regime strip (4 chips: SPX spot / net γ /1% blood-dark when negative / γ flip / regime
    AMPLIFY-or-DAMPEN via classifyRegime), then the 12-col grid — Net dealer gamma profile (span-7
    full GammaProfile + callout), GEX by strike (span-5 GexBars), Key levels (span-4 table/LevelBar),
    GEX by expiry (span-4 GexByExpiry), Charm/Vanna (span-4 ComingSoon "○ next"), Intraday flow (span-4
    ComingSoon "○ needs denser snapshots"). All headings/badges/stub copy use the EXACT locked strings.
    Live-only; empty/loading/error use the locked states. No `any`/`!`. Run GREEN + typecheck + lint.
    Commit at green.
  </action>
  <verify>
    <automated>vitest run --project apps/web -t "Market" && cd apps/web && bun run typecheck && bun run lint</automated>
  </verify>
  <acceptance_criteria>
    - `Market.test.tsx` asserts the profile + regime strip + GEX bars render, BOTH coming-soon stubs render with their badges, AND the "GEX data unavailable…" empty state; all pass
    - `vitest run --project apps/web -t "Market"` exits 0
    - The Charm/Vanna ("○ next") and intraday-flow ("○ needs denser snapshots") stubs are never omitted and never render as errors
    - Market headings/badges/stub copy match the UI-SPEC exactly; GEX is read from `useGex` (never recomputed in the browser); `bun run typecheck` + `bun run lint` green
  </acceptance_criteria>
  <done>The Market screen renders the live GEX market structure plus the two coming-soon stubs with the locked copy, proven by passing tests.</done>
</task>

</tasks>

<verification>
- `vitest run --project apps/web -t "gex-regime"` and `-t "Market"` green
- `bun run typecheck && bun run lint && bun run test` green
- Charm/Vanna + intraday-flow stubs render as badged placeholders (never errors/omitted)
- GEX is read server-side via useGex, never recomputed in the browser (D-01)
</verification>

<success_criteria>
The Market screen surfaces the live dealer-gamma market structure (anchor: the net-gamma profile)
from the Phase-8 GEX snapshot, ships two of the three locked coming-soon stubs, and never recomputes
the heavy GEX in the browser (D-01). The GammaProfile + GexBars are ready for the Analyzer right
panel.
</success_criteria>

<output>
Create `.planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-08-SUMMARY.md` when done
</output>
