---
phase: 09-web-dashboard-frontend-react-spa-on-hono-rpc
plan: 05
type: execute
wave: 5
depends_on: ["09-04"]
files_modified:
  - apps/web/src/components/Shell.tsx
  - apps/web/src/hooks/useGex.ts
  - apps/web/src/hooks/usePositions.ts
  - apps/web/src/screens/Overview.tsx
  - apps/web/src/components/charts/EquityCurve.tsx
  - apps/web/src/components/charts/MiniLine.tsx
  - apps/web/src/components/stubs/ComingSoon.tsx
  - apps/web/src/App.tsx
  - apps/web/vercel.json
  - apps/web/src/screens/Overview.test.tsx
autonomous: false
requirements: [UI-01, UI-02]
must_haves:
  truths:
    - "The Shell renders the sticky header (MORAI brand + 5 nav tabs) and the live market strip polling GEX + positions"
    - "The Overview screen renders the 12-col card grid with real API data and the locked empty state when there are no positions"
    - "The economic-calendar coming-soon stub renders as a badged dashed-border placeholder, never an error"
    - "apps/web is deployed to Vercel and all-thin-slice screens render live over the typed RPC client (UI-01)"
  artifacts:
    - path: "apps/web/src/components/Shell.tsx"
      provides: "Sticky header + nav + market strip + banner host"
      contains: "MOR"
    - path: "apps/web/src/screens/Overview.tsx"
      provides: "Overview 12-col grid wired to live data"
      contains: "Open positions"
    - path: "apps/web/src/components/stubs/ComingSoon.tsx"
      provides: "Reusable badged coming-soon stub"
      contains: "needs feed"
    - path: "apps/web/vercel.json"
      provides: "SPA rewrites for Vercel"
      contains: "rewrites"
  key_links:
    - from: "apps/web/src/components/Shell.tsx"
      to: "apps/web/src/hooks/useGex.ts"
      via: "market strip reads GEX poll for net γ / γ flip"
      pattern: "useGex"
    - from: "apps/web/src/screens/Overview.tsx"
      to: "apps/web/src/hooks/usePositions.ts"
      via: "Open positions card reads positions poll"
      pattern: "usePositions"
---

<objective>
Complete the D-02 vertical thin-slice: build the layout Shell (sticky header with MORAI brand + the
five nav tabs + the live market strip) and the Overview screen wired to REAL API data via the typed
RPC client, ship the reusable coming-soon stub (economic-calendar instance on Overview), then deploy
`apps/web` to Vercel and confirm the slice is green and live. Only after this proves the full stack
end-to-end do the other four screens fan out (Plans 06-10).

Purpose: De-risk the entire stack on the simplest screen before parallelizing (D-02). Live-only per
D-03/D-04 — no seed/sample data; empty states use the LOCKED copy. This is the first production
deploy of the frontend and forces the CORS `WEB_ORIGIN` round-trip in the right order (Pitfall 4).
Output: Shell, Overview, gex/positions hooks, equity-curve + mini-chart + ComingSoon components, a
vercel.json, and a live Vercel deployment.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-UI-SPEC.md
@.planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-RESEARCH.md
@.planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-PATTERNS.md
@.claude/rules/typescript.md
@mockups/overview-v1.html
@packages/contracts/src/gex.ts
@packages/contracts/src/brokerage.ts
</context>

<artifacts_this_phase_produces>
New files/symbols this plan creates (exclude from drift verification):
- `apps/web/src/components/Shell.tsx` — `Shell` (sticky header + nav tabs + market strip + banner host).
- `apps/web/src/hooks/useGex.ts` — `useGex()` (30s poll of GET /api/gex, parsed via gex contract).
- `apps/web/src/hooks/usePositions.ts` — `usePositions()` (30s poll of GET /api/positions, parsed).
- `apps/web/src/screens/Overview.tsx` — `Overview` screen.
- `apps/web/src/components/charts/EquityCurve.tsx` — visx equity curve.
- `apps/web/src/components/charts/MiniLine.tsx` — visx term/skew mini line (reused later).
- `apps/web/src/components/stubs/ComingSoon.tsx` — reusable `ComingSoon` stub.
- `apps/web/vercel.json` — SPA rewrites.
- Test: `apps/web/src/screens/Overview.test.tsx`.
- Routing: a screen switcher (React Router or useState) introduced in Shell/App for the 5 nav tabs.
</artifacts_this_phase_produces>

<user_setup>
  - service: vercel
    why: "Host the apps/web SPA; first deploy is required to learn the deployment URL for CORS"
    dashboard_config:
      - task: "Create/Link a Vercel project with Root Directory = apps/web, Framework = Vite, Build = bun run build, Output = dist"
        location: "Vercel Dashboard -> Add New Project -> Import the repo -> set Root Directory to apps/web"
      - task: "Set VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY, VITE_API_BASE_URL (https Railway URL) as Vercel env vars"
        location: "Vercel Dashboard -> Project -> Settings -> Environment Variables"
      - task: "After first deploy, set WEB_ORIGIN on the Railway API to the Vercel deployment origin so CORS preflight passes"
        location: "Railway Dashboard -> morai server service -> Variables -> WEB_ORIGIN"
</user_setup>

<tasks>

<task type="auto">
  <name>Task 1: gex + positions hooks and the layout Shell (header + nav + market strip)</name>
  <files>apps/web/src/hooks/useGex.ts, apps/web/src/hooks/usePositions.ts, apps/web/src/components/Shell.tsx, apps/web/src/App.tsx</files>
  <read_first>
    - apps/web/src/hooks/useStatus.ts (the hook pattern to mirror: useQuery + 401 short-circuit + contract parse)
    - packages/contracts/src/gex.ts (gexSnapshotResponse shape — spot, flip, callWall, putWall, netGammaAtSpot, profile, strikes, byExpiry — parse with this)
    - packages/contracts/src/brokerage.ts (positionsResponse / brokerPosition shape — note: NO computed greeks; this surfaces again in Plan 06)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-UI-SPEC.md ("Global — all screens" sticky header + market strip spec; "Copywriting Contract" global nav-tab order + market-strip labels SPX / net γ /1% / γ flip / book P&L; "Auto-Poll Intervals" 30s)
    - mockups/overview-v1.html (header + market-strip visual reference)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-RESEARCH.md (Open Question 3 React Router vs useState switcher — choose for 5-tab nav)
  </read_first>
  <action>
    Create `apps/web/src/hooks/useGex.ts` (`useGex()`, key `["gex"]`, 30s poll, parse via the gex
    contract) and `apps/web/src/hooks/usePositions.ts` (`usePositions()`, key `["positions"]`, 30s
    poll, parse via `positionsResponse`) — both mirroring the `useStatus` pattern (401 short-circuit,
    parse-don't-cast). Create `apps/web/src/components/Shell.tsx`: the sticky header (height ~48px,
    z-50, frosted-glass gradient) with the MOR**AI** logotype (violet "AI"), the five nav tabs in the
    LOCKED order (Overview · Analyzer · Positions · Journal · Market) with the locked active/inactive
    styling, and the right-aligned live market strip — SPX spot (blue, from gex.spot), net γ /1%
    (teal/coral + blood-dark bg when negative, from gex.netGammaAtSpot), γ flip (amber, from
    gex.flip), book P&L (sign-colored, summed from positions). Implement nav via a lightweight screen
    switcher (React Router v7 per RESEARCH Open Question 3, OR a typed `useState<ScreenName>` switch —
    pick one and keep it consistent; React Router enables deep-linking + Vercel SPA rewrites). The
    Shell renders the active screen in its content area and always mounts `<AuthExpiredBanner>`; when
    the banner is visible the body gets `padding-bottom: 38px`. Wire `App.tsx` so the authenticated
    branch renders `<Shell>`. Use @theme palette tokens. No `any`/`!`. `bun run typecheck && lint`.
  </action>
  <verify>
    <automated>cd apps/web && bun run typecheck && bun run lint && grep -q 'useGex' src/components/Shell.tsx && grep -q 'Overview' src/components/Shell.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `useGex.ts` and `usePositions.ts` poll at 30_000ms and parse through their contracts (no `as`)
    - `Shell.tsx` renders the MORAI brand, the five nav tabs in the exact locked order, and the market strip with SPX / net γ /1% / γ flip / book P&L labels
    - The market strip reads live `useGex()` + `usePositions()` (no hardcoded values)
    - A screen switcher routes between the five tab names; `<AuthExpiredBanner>` is mounted in the Shell
    - `bun run typecheck` + `bun run lint` green
  </acceptance_criteria>
  <done>The Shell renders the sticky header, nav, and live market strip; gex/positions hooks poll real data; the active screen mounts in the Shell.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Overview screen (12-col grid, live data, locked empty state) + ComingSoon stub</name>
  <files>apps/web/src/screens/Overview.tsx, apps/web/src/components/charts/EquityCurve.tsx, apps/web/src/components/charts/MiniLine.tsx, apps/web/src/components/stubs/ComingSoon.tsx, apps/web/src/screens/Overview.test.tsx</files>
  <read_first>
    - apps/web/src/hooks/usePositions.ts, apps/web/src/hooks/useStatus.ts (data sources for Open positions / Net greeks / System health cards)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-UI-SPEC.md ("Overview screen" Row A/B/C layout + spans; "Copywriting Contract" Overview headings/badges + the Catalysts coming-soon stub copy + Data-range note; "Empty / loading / error states" the locked "No open positions…" string; "Coming-Soon Stubs Contract" dashed-border visual spec + economic-calendar row)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-RESEARCH.md (Pattern 7 visx area/line for the equity curve + minis)
    - mockups/overview-v1.html (12-col grid + mini charts reference)
  </read_first>
  <behavior>
    - Overview renders the locked "No open positions…" empty-state copy when usePositions returns an empty array (no seed/sample data — D-04)
    - Overview renders the positions table + net greeks + P&L cards from live data when positions exist
    - The economic-calendar ComingSoon stub renders with its badge "○ needs feed" and dashed border, never as an error or omission
  </behavior>
  <action>
    Create `apps/web/src/components/stubs/ComingSoon.tsx`: a reusable badged placeholder (props:
    badge, title, body) per the Coming-Soon Stubs Contract — dashed border `#27313f`, radius 8px,
    `md` padding, centered flex column, `label` token title/body. Create
    `apps/web/src/components/charts/EquityCurve.tsx` (visx `AreaClosed`+`LinePath`, coral line when
    cumulative negative) and `apps/web/src/components/charts/MiniLine.tsx` (small visx line for term/
    skew minis, reused by later screens). Write `apps/web/src/screens/Overview.test.tsx` FIRST (RED):
    mock `usePositions` empty → assert the locked "No open positions…" copy renders; mock it with one
    position → assert the position row + net row render; assert the economic-calendar ComingSoon stub
    renders its "○ needs feed" badge. Run RED. Then build `apps/web/src/screens/Overview.tsx` to the
    UI-SPEC Overview layout: Row A (Open positions span-7 table with the locked columns + Net greeks
    span-2 + P&L span-3 with EquityCurve), Row B (Market regime / Your strike / Volatility minis), Row
    C (Catalysts = economic-calendar ComingSoon stub with the locked copy / System health job rows
    from useStatus / Recent activity). Include the locked Data-range note ("Data from 2026-06-12
    forward…"). All data from live hooks; empty/loading/error use the LOCKED copy (skeletons not
    spinners; region error never crashes the page). No seed data. Run the test GREEN + typecheck +
    lint. Commit at green.
  </action>
  <verify>
    <automated>vitest run --project apps/web -t "Overview" && cd apps/web && bun run typecheck && bun run lint</automated>
  </verify>
  <acceptance_criteria>
    - `Overview.test.tsx` asserts the empty-state copy on no positions AND live rows when positions exist AND the economic-calendar stub badge; all pass
    - `vitest run --project apps/web -t "Overview"` exits 0
    - The "No open positions…" and "Data from 2026-06-12 forward…" strings match the UI-SPEC exactly
    - `ComingSoon.tsx` renders a dashed-border badged placeholder (never throws)
    - No sample/demo/seed position data exists in Overview (D-04); `bun run typecheck` + `bun run lint` green
  </acceptance_criteria>
  <done>Overview renders the 12-col grid from live data with the locked empty state and the economic-calendar coming-soon stub, proven by passing tests.</done>
</task>

<task type="auto">
  <name>Task 3: vercel.json + deploy + CORS wiring</name>
  <files>apps/web/vercel.json</files>
  <read_first>
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-RESEARCH.md (Pattern 10 vercel.json SPA rewrites in apps/web; Vercel project settings; VITE_ env vars; CORS WEB_ORIGIN; Pitfall 4 deploy-first-then-set-WEB_ORIGIN ordering)
    - apps/web/vite.config.ts (build output dir = dist)
  </read_first>
  <action>
    Create `apps/web/vercel.json` with the `$schema` + SPA rewrite `{ source: "/(.*)", destination:
    "/index.html" }` (in apps/web, NOT repo root). Do NOT put any `VITE_*` secret in vercel.json
    (dashboard env vars only). Deploy via the Vercel CLI (or trigger the dashboard build) with Root
    Directory `apps/web`, Framework Vite, Build `bun run build`, Output `dist`. After the first deploy
    produces a URL, set `WEB_ORIGIN` on the Railway API to that Vercel origin so the CORS preflight
    passes (Pitfall 4 — deploy first, then set origin). If the Vercel/Railway CLI returns an auth
    error, that becomes a dynamic auth checkpoint (authenticate then retry). Confirm the build command
    succeeds in CI/Vercel before the human-verify checkpoint.
  </action>
  <verify>
    <automated>cd apps/web && test -f vercel.json && grep -q 'rewrites' vercel.json && bun run build && test -d dist && echo "DEPLOY-READY"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/vercel.json` contains an SPA rewrite to `/index.html` and NO `VITE_*` secrets
    - `cd apps/web && bun run build` exits 0 (Vercel build command succeeds)
    - A Vercel deployment URL exists and `WEB_ORIGIN` on Railway is set to that origin (recorded in the SUMMARY)
  </acceptance_criteria>
  <done>vercel.json is in apps/web, the build succeeds, the SPA is deployed, and Railway WEB_ORIGIN points at the Vercel origin.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify the live Vercel thin-slice</name>
  <files>apps/web (deployed)</files>
  <what-built>
    The D-02 thin-slice end-to-end: apps/web deployed to Vercel rendering the Shell (sticky header,
    nav, live market strip), the Login gate (Supabase Auth), the Overview screen wired to live API
    data over the typed RPC client, the economic-calendar coming-soon stub, and the AUTH_EXPIRED
    banner. CORS WEB_ORIGIN is set so live API calls succeed from the Vercel origin.
  </what-built>
  <action>
    Pause for human verification of the live Vercel deployment. The executor presents the deployment
    URL and the how-to-verify steps; the human confirms the thin-slice renders correctly before the
    fan-out (Plans 06-10) proceeds. This is a blocking human gate — do not auto-advance.
  </action>
  <how-to-verify>
    1. Open the Vercel deployment URL in a browser.
    2. Confirm the Login screen renders (centered card, MORAI brand). Sign in with the trader's Supabase credentials.
    3. After login, confirm the Overview screen renders the sticky header + the five nav tabs + the live market strip (SPX spot, net γ /1%, γ flip, book P&L populated from the API — not blank/zero).
    4. Confirm the Open Positions card shows REAL positions OR the exact "No open positions…" empty state (no demo/sample rows).
    5. Confirm the Catalysts card shows the economic-calendar coming-soon stub (dashed border, "○ needs feed") — not an error.
    6. Open DevTools Network: confirm `/api/*` calls go to the Railway origin over https and return 200 (no CORS errors).
    7. (If the Schwab token is expired) confirm the AUTH_EXPIRED banner appears at the bottom with the locked copy.
  </how-to-verify>
  <verify>
    <human-check>Human confirms the live thin-slice (Login → Overview over real API on Vercel) renders per the how-to-verify steps with no CORS errors.</human-check>
  </verify>
  <resume-signal>Type "approved" if the live thin-slice renders correctly, or describe any issues (blank market strip, CORS error, wrong copy, missing stub).</resume-signal>
  <done>The human approves the live Vercel thin-slice: Login, Overview, market strip, coming-soon stub, and banner all render correctly over the real API.</done>
</task>

</tasks>

<verification>
- `vitest run --project apps/web -t "Overview"` green (empty state + live rows + stub)
- `bun run typecheck && bun run lint && bun run test` green across the workspace
- `apps/web/vercel.json` present with SPA rewrites; build succeeds
- Human-verify checkpoint confirms the live Vercel thin-slice over real API (UI-01)
</verification>

<success_criteria>
The vertical thin-slice is green AND live: Login → Overview renders real data over the typed RPC
client on Vercel, the market strip polls live GEX/positions, the coming-soon stub and AUTH_EXPIRED
banner behave per the locked spec. The full stack is proven end-to-end, unblocking the fan-out
(Plans 06-10).
</success_criteria>

<output>
Create `.planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-05-SUMMARY.md` when done
</output>
