---
phase: 09-web-dashboard-frontend-react-spa-on-hono-rpc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture/stack-decisions.md
  - docs/architecture/monorepo-layout.md
  - eslint.config.js
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "stack-decisions.md names visx/uPlot/ECharts for charting, not Recharts (D-05)"
    - "stack-decisions.md records the packages/quant pure-leaf BSM extraction (D-01)"
    - "monorepo-layout.md shows packages/quant leaf with core→quant and web→quant edges"
    - "eslint.config.js declares a quant boundary element and allows core/apps to import it"
  artifacts:
    - path: "docs/architecture/stack-decisions.md"
      provides: "Reconciled charting decision + new quant-leaf decision"
      contains: "visx"
    - path: "docs/architecture/monorepo-layout.md"
      provides: "Updated dependency graph with quant leaf"
      contains: "quant"
    - path: "eslint.config.js"
      provides: "quant boundary element + apps/web typed-lint wiring"
      contains: "quant"
  key_links:
    - from: "eslint.config.js"
      to: "packages/quant/src"
      via: "boundaries/elements quant pattern"
      pattern: "packages/quant/src"
---

<objective>
Docs-before-code: reconcile the charting stack decision (D-05) and record the new
`packages/quant` pure-leaf BSM extraction (D-01) in the architecture source-of-truth docs,
then update the ESLint boundary config to allow the new `quant` element and to lint the
coming `apps/web` workspace. This is a HARD PREDECESSOR for the kernel move (Plan 02) and all
chart code: per `workflow.md` "Docs Before Code", architecture and boundary changes update
docs first; per `architecture-boundaries.md`, adding a new package without a boundary element
makes the dependency law block the import before docs exist.

Purpose: Satisfy the project's blocking docs-before-code rule so the kernel extraction and the
chart libraries have a documented, lint-enforceable home before any code lands.
Output: Updated stack-decisions.md, monorepo-layout.md, eslint.config.js.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-CONTEXT.md
@.planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-RESEARCH.md
@.planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-PATTERNS.md
@.claude/rules/workflow.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/docs.md
</context>

<artifacts_this_phase_produces>
This plan introduces no code symbols. It produces documentation + config changes that the
rest of Phase 9 depends on:
- `docs/architecture/stack-decisions.md` — new decision (BSM kernel `packages/quant` leaf,
  call it D21 or the next free number) + reconciled D3 charting (visx/uPlot/ECharts).
- `docs/architecture/monorepo-layout.md` — `packages/quant/` node + `core → quant`,
  `web → quant` edges + `apps/web` row.
- `eslint.config.js` — `quant` boundary element, `quant` in the allow lists for `core` and
  `apps`, `apps/web/tsconfig.json` + `packages/quant/tsconfig.json` in the resolver `project`
  arrays, and `.tsx` added to the typed-lint `files` glob.

Downstream convergence note: no new exported functions/components here.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile charting decision + record quant-leaf decision in stack-decisions.md</name>
  <files>docs/architecture/stack-decisions.md</files>
  <read_first>
    - docs/architecture/stack-decisions.md (the file being edited — read D3 charting entry, D4 RPC, D19 Vercel host; find the highest existing D-number to append the new decision)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-CONTEXT.md (D-01 kernel extraction rationale, D-05 Recharts drift)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-RESEARCH.md ("Architecture Patterns" Pattern 6 dependency arrows before/after; "Docs Updates" table)
  </read_first>
  <action>
    Edit `docs/architecture/stack-decisions.md`. (1) Reconcile the D3 charting decision (D-05):
    replace the Recharts naming with the LOCKED assignment visx (`@visx/shape`/`gradient`/`event`/
    `scale`/`axis`/`group`/`tooltip`) for payoff / net-gamma profile / equity curve / term+skew
    minis, uPlot (`uplot` + `uplot-react`) for greek strips, Apache ECharts (`echarts` +
    `echarts-for-react`) for GEX bars / P&L heatmap / GEX-by-expiry. State the swap reason: the
    UI-SPEC locked these three after mockup iteration; Recharts cannot do the synced greek-strip
    small-multiples or the gradient-fill payoff z-order. (2) Add a new decision entry (next free
    D-number) recording the `packages/quant` pure-leaf BSM kernel extraction per D-01: the pure
    `bsmPrice`/`bsmGreeks`/`bsmVega` kernel moves DOWN from `packages/core/src/journal/domain/bsm.ts`
    to a new zero-dependency `packages/quant` leaf imported by BOTH `core` and `web`; this reverses
    no dependency arrow (`core → quant` ✓, `web → quant` ✓) and the forbidden `web → core` edge is
    never introduced. Note the swap cost (call sites + tsconfig references + eslint boundary) and the
    rationale (one kernel = cross-screen P&L consistency between client live re-pricing and
    server-computed P&L). Keep Hemingway style per docs.md. Do NOT restate config-file contents.
    Cite D-01 and D-05 in the entry prose.
  </action>
  <verify>
    <automated>grep -q 'visx' docs/architecture/stack-decisions.md && grep -q 'quant' docs/architecture/stack-decisions.md && ! grep -iq 'Recharts' <(grep -v 'rejected\|instead of\|not Recharts\|superseded' docs/architecture/stack-decisions.md) && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'visx' docs/architecture/stack-decisions.md` ≥ 1 AND `grep -c 'uPlot\|uplot' docs/architecture/stack-decisions.md` ≥ 1 AND `grep -c 'ECharts\|echarts' docs/architecture/stack-decisions.md` ≥ 1
    - The charting decision no longer presents Recharts as the active choice (Recharts may appear only as the superseded/rejected option, marked as such)
    - A decision entry naming `packages/quant` (or `@morai/quant`) as a pure leaf for the BSM kernel exists, referencing D-01
    - File still passes any docs lint already wired (no broken markdown links)
  </acceptance_criteria>
  <done>stack-decisions.md names visx+uPlot+ECharts as the active charting choice and records the quant-leaf BSM extraction; Recharts is no longer the active decision.</done>
</task>

<task type="auto">
  <name>Task 2: Add quant leaf to monorepo-layout.md dependency graph</name>
  <files>docs/architecture/monorepo-layout.md</files>
  <read_first>
    - docs/architecture/monorepo-layout.md (the file being edited — read the workspace dependency graph, the layout table, and the apps table; find where `web → contracts → shared` and "web never imports core" are stated)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-PATTERNS.md ("Docs Updates" table — exact edges to add)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-RESEARCH.md (Pattern 6 dependency arrows after extraction)
  </read_first>
  <action>
    Edit `docs/architecture/monorepo-layout.md`. Add `packages/quant/` as a new pure-leaf node in
    the workspace dependency graph and the layout/packages table, described as "BSM kernel — pure
    math leaf, zero runtime deps". Add the dependency edges `core → quant` and `web → quant`. Add an
    `apps/web` row to the apps table (React SPA on Vercel, may import `contracts`, `shared`, `quant`,
    and `apps/server` type-only for `AppType`). Preserve the existing `web → contracts → shared`
    statement and the "web never imports core/adapters" rule (the quant leaf is BELOW both core and
    web, so web reaching quant does not reach core). Cite D-01. Hemingway style.
  </action>
  <verify>
    <automated>grep -q 'quant' docs/architecture/monorepo-layout.md && grep -q 'apps/web' docs/architecture/monorepo-layout.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'quant' docs/architecture/monorepo-layout.md` ≥ 2 (node + at least one edge)
    - `grep -c 'apps/web' docs/architecture/monorepo-layout.md` ≥ 1
    - The "web never imports core" rule is still present (not deleted)
    - Edges `core → quant` and `web → quant` are both described
  </acceptance_criteria>
  <done>monorepo-layout.md shows the quant leaf, the two new edges, and an apps/web row; the web-never-imports-core rule is intact.</done>
</task>

<task type="auto">
  <name>Task 3: Add quant boundary element + apps/web lint wiring to eslint.config.js</name>
  <files>eslint.config.js</files>
  <read_first>
    - eslint.config.js (the file being edited — read `boundaries/elements` block (~line 43), the `from`/`allow` rules (~lines 58-69), the resolver `project` arrays (~lines 32-39 and 104-111), and the typed-lint `files` glob (~line 100))
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-PATTERNS.md ("eslint.config.js updates" — exact element + allow-list diffs)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-RESEARCH.md (Pattern 6 step 6 — boundary element + allow list)
  </read_first>
  <action>
    Edit `eslint.config.js`. (1) Add a new boundary element `{ type: "quant", pattern:
    "**/packages/quant/src/**", mode: "full" }` to `boundaries/elements`. (2) Add a self-allow rule
    `{ from: "quant", allow: ["quant"] }` and add `"quant"` to the `allow` list for the `core` and
    `apps` element types (so `core → quant` and `apps → quant` are permitted; the leaf imports
    nothing). (3) Add `"packages/quant/tsconfig.json"` and `"apps/web/tsconfig.json"` to BOTH
    resolver `project` arrays (the import-resolver settings and the typed-lint `parserOptions.project`
    list). (4) Extend the typed-lint `files` glob from `["packages/**/*.ts", "apps/**/*.ts"]` to also
    match `.tsx` (add `"packages/**/*.tsx"`, `"apps/**/*.tsx"`) so React components in `apps/web` are
    type-aware-linted. Keep `**/*.test.ts`/`**/*.test.tsx` and `__fixtures__` ignores consistent.
    Do NOT add any `eslint-disable` for boundary rules (forbidden by architecture-boundaries.md).
    NOTE: `apps/web/tsconfig.json` and `packages/quant/tsconfig.json` do not exist yet — they are
    created in Plans 02/03. Adding them to the resolver list now is intentional (docs/config-before-
    code); a `bun run lint` over the existing tree must still pass because no `.tsx`/quant files exist
    yet to resolve. Verify lint stays green on the current tree after the edit.
  </action>
  <verify>
    <automated>node -e "1" && grep -q 'packages/quant/src' eslint.config.js && grep -q 'apps/web/tsconfig.json' eslint.config.js && bun run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'type: "quant"' eslint.config.js` = 1
    - `grep -c '"quant"' eslint.config.js` ≥ 3 (element type + core allow + apps allow + self-allow)
    - `grep -c 'apps/web/tsconfig.json' eslint.config.js` ≥ 1 AND `grep -c 'packages/quant/tsconfig.json' eslint.config.js` ≥ 1
    - The typed-lint `files` glob includes a `.tsx` pattern
    - `bun run lint` exits 0 over the current tree (no quant/web files exist yet, so no new violations)
  </acceptance_criteria>
  <done>eslint.config.js declares the quant element, allows core/apps to import it, registers the future tsconfig paths, lints .tsx, and `bun run lint` is green.</done>
</task>

</tasks>

<verification>
- `grep` confirms visx/uPlot/ECharts present and Recharts demoted in stack-decisions.md
- `grep` confirms quant leaf + apps/web in monorepo-layout.md
- `bun run lint` exits 0 with the new quant element and apps/web wiring registered
- No `eslint-disable` added for any boundary rule
</verification>

<success_criteria>
Docs-before-code gate satisfied: the charting reconciliation (D-05) and the quant-leaf decision
(D-01) are recorded in the architecture source of truth, and the boundary config is ready for the
kernel move (Plan 02) and apps/web (Plan 03) without any later docs backfill.
</success_criteria>

<output>
Create `.planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-01-SUMMARY.md` when done
</output>
