---
phase: 09-web-dashboard-frontend-react-spa-on-hono-rpc
plan: 10
type: execute
wave: 7
depends_on: ["09-06", "09-08", "09-09"]
files_modified:
  - apps/web/src/screens/Analyzer.tsx
  - apps/web/src/lib/scenario-engine.ts
  - apps/web/src/lib/scenario-engine.test.ts
  - apps/web/src/components/charts/PayoffChart.tsx
  - apps/web/src/components/charts/PnlHeatmap.tsx
  - apps/web/src/components/RollSimulator.tsx
  - apps/web/src/screens/Analyzer.test.tsx
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "The Analyzer re-prices payoff curves, greek strips, and the P&L heatmap client-side on slider drag using @morai/quant — no API round-trip (D-01)"
    - "The Analyzer's live P&L preview uses the same shared kernel as the server-computed Positions/Journal P&L, so they agree for the same calendar"
    - "The 3-column cockpit renders the payoff chart with the full locked z-order, the roll simulator overlay, the right-panel GEX/attribution, and the live position list"
    - "Positions can be added via TOS paste (parseTosOrder) or blank; live positions are protected from removal"
  artifacts:
    - path: "apps/web/src/lib/scenario-engine.ts"
      provides: "Client-side scenario re-pricing (payoff/greeks/heatmap) over @morai/quant"
      contains: "@morai/quant"
    - path: "apps/web/src/components/charts/PayoffChart.tsx"
      provides: "visx payoff chart with the locked z-order"
      contains: "LinePath"
    - path: "apps/web/src/components/RollSimulator.tsx"
      provides: "Roll simulator (amber overlay)"
      contains: "Roll"
    - path: "apps/web/src/screens/Analyzer.tsx"
      provides: "Analyzer 3-column cockpit per UI-SPEC"
      contains: "Scenario"
  key_links:
    - from: "apps/web/src/lib/scenario-engine.ts"
      to: "packages/quant"
      via: "imports bsmPrice/bsmGreeks from @morai/quant for live re-pricing"
      pattern: "bsm(Price|Greeks)"
    - from: "apps/web/src/screens/Analyzer.tsx"
      to: "apps/web/src/lib/tos-parser.ts"
      via: "add-from-paste uses parseTosOrder"
      pattern: "parseTosOrder"
---

<objective>
Build the Analyzer — the hardest screen, sequenced LAST (D-02). The 3-column dense cockpit (236px |
1fr | 320px): left controls (position list with TOS paste / blank add + scenario sliders + roll
simulator), center charts (the visx payoff chart with the full locked z-order, the uPlot greek
strips, the ECharts P&L heatmap), and right analytics (GEX bars + net-gamma profile + key levels +
book greeks + attribution waterfall). All scenario math re-prices CLIENT-SIDE on slider drag via the
shared `@morai/quant` kernel (D-01) — no API round-trip — porting the proven sub-1ms playground-v3
engine. The heavy full-chain GEX still comes from the server (`useGex`), never recomputed.

Purpose: One shared kernel guarantees the Analyzer's live P&L preview agrees with the server-computed
Positions/Journal P&L for the same calendar (D-01 cross-screen consistency). Anchor: the payoff chart.
Output: Analyzer screen + scenario-engine + PayoffChart + PnlHeatmap + RollSimulator.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-UI-SPEC.md
@.planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-RESEARCH.md
@.planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-PATTERNS.md
@.claude/rules/typescript.md
@.claude/rules/tdd.md
@packages/quant/src/bsm.ts
@mockups/playground-v3.html
@mockups/analyzer-v1.html
</context>

<artifacts_this_phase_produces>
New files/symbols this plan creates (exclude from drift verification):
- `apps/web/src/lib/scenario-engine.ts` — `repriceScenario` (payoff curve + greek strips + heatmap
  cells) and `rollScenario` over `@morai/quant`.
- `apps/web/src/components/charts/PayoffChart.tsx` — `PayoffChart` (visx, full z-order + crosshair).
- `apps/web/src/components/charts/PnlHeatmap.tsx` — `PnlHeatmap` (ECharts spot×date grid).
- `apps/web/src/components/RollSimulator.tsx` — `RollSimulator` (amber overlay controls).
- `apps/web/src/screens/Analyzer.tsx` — `Analyzer` screen.
- Tests: `apps/web/src/lib/scenario-engine.test.ts`, `apps/web/src/screens/Analyzer.test.tsx`.
- Reuses (no new symbol): GreekStrips/AttributionWaterfall/LevelBar (Plan 06), GammaProfile/GexBars
  (Plan 08), position-greeks (Plan 06), parseTosOrder/impliedFlatIv (Plan 09).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Client-side scenario engine over @morai/quant (TDD) + kernel-parity assertion</name>
  <files>apps/web/src/lib/scenario-engine.ts, apps/web/src/lib/scenario-engine.test.ts</files>
  <read_first>
    - packages/quant/src/bsm.ts (bsmPrice/bsmGreeks signatures — the re-pricing kernel)
    - apps/web/src/lib/position-greeks.ts (the Plan-06 per-position greeks helper to reuse for consistency)
    - mockups/playground-v3.html (the reference scenario engine: payoff over a spot grid, greek strips, P&L heatmap cells, roll math — the oracle to port; γ-flip ≈ 7488 / net ≈ −$47B reference)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-UI-SPEC.md ("Analyzer screen" payoff curve set + greek strips + heatmap columns (T+0/+5/+10/+15/+20/+30d) × ±7 strikes; "TOS Calendar Paste Parser Contract"; scenario slider ranges spot 6900–7900/days 0→front-expiry/IV −8..+8)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-CONTEXT.md (D-01: one kernel = the Analyzer live P&L and the server P&L must agree — the parity requirement)
  </read_first>
  <behavior>
    - repriceScenario(positions, {spot, daysForward, ivShift}) returns the payoff curve over the spot grid, the four greek strips, and the heatmap cells — all computed via @morai/quant
    - Kernel-parity: for a single position at the live spot/0-days/0-IV, repriceScenario's per-position greeks equal a direct bsmGreeks call AND equal the Plan-06 position-greeks output (cross-screen consistency, D-01)
    - rollScenario applies the front-roll (+7/+14/+21d) and strike-offset (−100/same/+100) producing the amber overlay curve
    - Heatmap cell P&L is symmetric and monotonic as documented (fast-check property)
  </behavior>
  <action>
    Write `apps/web/src/lib/scenario-engine.test.ts` FIRST (RED): (a) a kernel-parity test — for a
    single calendar at live spot, 0 days, 0 IV-shift, assert `repriceScenario` per-position greeks ===
    a direct `bsmGreeks(...)` call AND === the Plan-06 `computePositionGreeks` output (this is the D-01
    cross-screen-consistency guarantee — the Analyzer must agree with Positions/server); (b) a payoff-
    shape example (calendar payoff peaks near the strike); (c) a fast-check property over the heatmap
    cells (numRuns:1000, Math.fround() bounds) asserting symmetry/monotonicity as documented; (d) a
    roll-overlay example. Run RED. Then write `apps/web/src/lib/scenario-engine.ts`: `repriceScenario`
    and `rollScenario` porting the playground-v3 math, calling `bsmPrice`/`bsmGreeks` from
    `@morai/quant` over the spot grid / days-forward / IV-shift, reusing the Plan-06 position-greeks
    helper for per-position greeks so there is a single code path. Pure functions, no DOM. No
    `any`/`as`/`!`. Run GREEN + typecheck + lint. Commit at green.
  </action>
  <verify>
    <automated>vitest run --project apps/web -t "scenario-engine" && cd apps/web && bun run typecheck && grep -q '@morai/quant' src/lib/scenario-engine.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `scenario-engine.test.ts` includes the kernel-parity test (repriceScenario === bsmGreeks === Plan-06 helper), a payoff example, a heatmap fast-check property (numRuns:1000), and a roll example; all pass
    - `scenario-engine.ts` imports `bsmPrice`/`bsmGreeks` from `@morai/quant` and reuses the Plan-06 position-greeks helper (single code path — no second kernel copy)
    - `vitest run --project apps/web -t "scenario-engine"` exits 0; `bun run typecheck` green
  </acceptance_criteria>
  <done>The client-side scenario engine re-prices over the shared kernel with proven parity to bsmGreeks and the Positions helper (D-01 consistency).</done>
</task>

<task type="auto">
  <name>Task 2: PayoffChart (full z-order + crosshair) + PnlHeatmap + RollSimulator</name>
  <files>apps/web/src/components/charts/PayoffChart.tsx, apps/web/src/components/charts/PnlHeatmap.tsx, apps/web/src/components/RollSimulator.tsx</files>
  <read_first>
    - apps/web/src/lib/scenario-engine.ts (the curves/cells/roll the components render)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-UI-SPEC.md ("Analyzer screen" payoff chart: the 9-layer z-order (profit zone → T+0 curve gradient → dated fan → expiration tent → roll overlay → GEX walls → expiration BE → T+0 BE → spot line+dot), the TOS-stable y-axis + "fit Y", the crosshair fixed tooltip (P&L/SPX/dist-to-walls); the P&L heatmap columns/rows/diverging colors/step toggle; the Roll simulator segments + amber status copy; "Copywriting Contract" Analyzer legend + roll + heatmap labels)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-RESEARCH.md (Pattern 7 visx AreaClosed/LinePath/LinearGradient/localPoint crosshair; Pattern 9 ECharts heatmap)
    - mockups/analyzer-v1.html (payoff z-order + heatmap + roll visual reference)
  </read_first>
  <action>
    Create `apps/web/src/components/charts/PayoffChart.tsx`: a visx SVG (1000×470 logical,
    preserveAspectRatio none) rendering the curves bottom-to-top in the EXACT locked z-order
    (1 profit-zone teal fill, 2 T+0 curve violet `#a78bfa` 2.6px with teal-above/coral-below gradient,
    3 dated fan curves when toggled, 4 expiration tent dashed when toggled, 5 amber roll overlay when
    active, 6 GEX wall lines when toggled, 7 expiration BE lines, 8 T+0 BE lines, 9 blue spot vertical
    + dot). Implement the TOS-stable y-axis (computed once from the expiration profile on position-set
    change; "fit Y" resets on demand) and the crosshair (gray line on pointermove via `localPoint`) +
    the FIXED HTML tooltip showing P&L (sign-colored) / SPX / distance to put wall / distance to call
    wall. Create `apps/web/src/components/charts/PnlHeatmap.tsx`: a `ReactECharts` spot×date grid
    (columns T+0/+5/+10/+15/+20/+30d × ±7 strikes at the selected step) with the diverging teal↔coral
    symmetric colors + compact cell labels + the step ToggleGroup (10s/25s/50s/100s, default 50s).
    Create `apps/web/src/components/RollSimulator.tsx`: amber segment controls (front +7/+14/+21d,
    strike −100/same/+100) that drive `rollScenario`, with the locked active/inactive status copy and
    the "on {position name}" badge. No `any`/`!`. `bun run typecheck && lint`.
  </action>
  <verify>
    <automated>cd apps/web && bun run typecheck && bun run lint && grep -q "localPoint" src/components/charts/PayoffChart.tsx && grep -qi "echarts" src/components/charts/PnlHeatmap.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `PayoffChart.tsx` renders the full 9-layer z-order, a TOS-stable y-axis with a "fit Y" reset, and a crosshair fixed tooltip with P&L/SPX/wall distances
    - `PnlHeatmap.tsx` uses echarts-for-react with the diverging colors + step toggle (default 50s)
    - `RollSimulator.tsx` drives `rollScenario` with the locked segment options + status copy
    - `bun run typecheck` + `bun run lint` green
  </acceptance_criteria>
  <done>The payoff chart (full z-order + crosshair), the P&L heatmap, and the roll simulator render per the locked spec over the scenario engine.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Analyzer 3-column cockpit assembled with live re-pricing</name>
  <files>apps/web/src/screens/Analyzer.tsx, apps/web/src/screens/Analyzer.test.tsx</files>
  <read_first>
    - apps/web/src/lib/scenario-engine.ts (live re-pricing), apps/web/src/lib/tos-parser.ts (add-from-paste)
    - apps/web/src/components/charts/PayoffChart.tsx, PnlHeatmap.tsx, RollSimulator.tsx (Task 2)
    - apps/web/src/components/charts/GreekStrips.tsx, AttributionWaterfall.tsx, LevelBar.tsx (Plan 06 reuse)
    - apps/web/src/components/charts/GammaProfile.tsx, GexBars.tsx (Plan 08 reuse), apps/web/src/hooks/useGex.ts, usePositions.ts
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-UI-SPEC.md ("Analyzer screen" 3-column cockpit (236px|1fr|320px); left Positions panel (live=🔒 protected from removal, non-live=× removable, checkbox combine, paste TOS / blank add) + Scenario panel (3 sliders + Reset) + Roll simulator; center payoff + greek strips + heatmap; right What's-moving (regime strip + gamma profile + key levels + GEX bars) + Book greeks + attribution; "Copywriting Contract" all Analyzer copy incl. parse success/error strings + live-position-protected tooltip; "Empty / loading / error states" remove-position destructive copy)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-RESEARCH.md (slider debounce/throttle discretion)
    - mockups/analyzer-v1.html (full cockpit layout)
  </read_first>
  <behavior>
    - Moving the spot/days/IV sliders re-prices the payoff/greek-strips/heatmap client-side (no network call fires for re-pricing)
    - "+ add from paste" with a valid TOS string adds a non-live position and shows the locked success message; an invalid string shows the locked parse-error copy
    - Live positions show the 🔒 protected indicator and cannot be removed; non-live positions can be removed
    - The Reset button returns spot/days/IV to defaults and clears the roll + y-lock
  </behavior>
  <action>
    Write `apps/web/src/screens/Analyzer.test.tsx` FIRST (RED): (a) render with one live position →
    assert the payoff chart region + greek strips render; (b) simulate a spot-slider change → assert
    the scenario re-prices (e.g. the combined P&L readout changes) and assert NO RPC/network call fires
    for re-pricing (the scenario engine is local); (c) "+ add from paste" with the canonical TOS string
    → assert the locked success message + a new non-live row; with garbage → assert the locked parse-
    error copy; (d) assert a live position shows the protected indicator and offers no × remove. Run
    RED. Then build `apps/web/src/screens/Analyzer.tsx` to the UI-SPEC cockpit: left column (Positions
    panel seeded from live `usePositions()` — live rows protected, checkbox-combine, paste via
    `parseTosOrder` / blank-at-nearest-25 add; Scenario panel with the 3 locked sliders + Reset; Roll
    simulator), center column (PayoffChart + the combined P&L `display`-token readout + GreekStrips +
    PnlHeatmap, all fed by `repriceScenario` on slider state — debounce/throttle per discretion), right
    column (What's-moving: regime strip + GammaProfile compact + LevelBar + GexBars from `useGex`; Book
    greeks table; AttributionWaterfall combined-book variant). Heavy GEX from `useGex` (never
    recomputed). The remove-position destructive dialog uses the locked copy. All copy EXACT. No
    seed/demo positions (D-04 — the mockup `ex` labels do not ship). No `any`/`!`. Run GREEN +
    typecheck + lint + the FULL workspace suite. Commit at green.
  </action>
  <verify>
    <automated>vitest run --project apps/web -t "Analyzer" && cd apps/web && bun run typecheck && bun run lint && bun run test</automated>
  </verify>
  <acceptance_criteria>
    - `Analyzer.test.tsx` asserts: charts render, a slider change re-prices locally with NO network call, paste-success + paste-error use the locked copy, and live positions are protected from removal; all pass
    - `vitest run --project apps/web -t "Analyzer"` exits 0
    - Re-pricing uses `repriceScenario` (client-side @morai/quant) — no API round-trip for scenario math (D-01); GEX comes from `useGex` (server)
    - No seed/demo positions ship (D-04); all Analyzer copy matches the UI-SPEC exactly
    - `bun run typecheck`, `bun run lint`, `bun run test` all green
  </acceptance_criteria>
  <done>The Analyzer cockpit re-prices live via the shared kernel, supports TOS-paste/blank/live-protected positions with the locked copy, and reuses the GEX/strip/waterfall components — proven by passing tests.</done>
</task>

</tasks>

<verification>
- `vitest run --project apps/web -t "scenario-engine"` and `-t "Analyzer"` green
- `bun run typecheck && bun run lint && bun run test` green across the whole workspace
- Slider re-pricing fires no network call (client-side @morai/quant); kernel parity with Positions proven
- Payoff z-order, roll overlay, heatmap, and all copy match the locked UI-SPEC
</verification>

<success_criteria>
The Analyzer — the hardest screen, sequenced last — re-prices payoff/greeks/heatmap live in the
browser via the one shared @morai/quant kernel (D-01), guaranteeing its P&L preview matches the
server-computed Positions/Journal P&L, supports TOS-paste / blank / live-protected positions, and
reuses the GEX/strip/waterfall components. With this, all five locked screens are live.
</success_criteria>

<output>
Create `.planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-10-SUMMARY.md` when done
</output>
