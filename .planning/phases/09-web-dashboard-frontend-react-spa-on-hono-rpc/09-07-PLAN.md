---
phase: 09-web-dashboard-frontend-react-spa-on-hono-rpc
plan: 07
type: execute
wave: 6
depends_on: ["09-05"]
files_modified:
  - apps/web/src/screens/Journal.tsx
  - apps/web/src/hooks/useJournal.ts
  - apps/web/src/hooks/useRebuildJournal.ts
  - apps/web/src/components/LifecycleChart.tsx
  - apps/web/src/components/RebuildButton.tsx
  - apps/web/src/lib/journal-history.ts
  - apps/web/src/lib/journal-history.test.ts
  - apps/web/src/screens/Journal.test.tsx
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "The Journal screen renders 30-min snapshots for a trade (lifecycle chart + scrubber + snapshot table)"
    - "Trades older than the Jun-12 chain-history start render an entry/exit-only badge and a no-day-by-day stub, never an error or blank screen (JOURNAL-01)"
    - "The per-calendar rebuild button triggers POST /api/jobs/rebuild-journal/trigger with calendarId, behind a confirmation dialog (REBUILD-01)"
  artifacts:
    - path: "apps/web/src/screens/Journal.tsx"
      provides: "Journal 3-column screen per UI-SPEC"
      contains: "Lifecycle"
    - path: "apps/web/src/lib/journal-history.ts"
      provides: "Pre-Jun-12 classification (history vs entry/exit-only)"
      contains: "2026-06-12"
    - path: "apps/web/src/components/RebuildButton.tsx"
      provides: "Rebuild trigger + confirmation dialog (REBUILD-01)"
      contains: "rebuild-journal"
    - path: "apps/web/src/hooks/useJournal.ts"
      provides: "Journal snapshots poll (60s)"
      contains: "journalResponse"
  key_links:
    - from: "apps/web/src/components/RebuildButton.tsx"
      to: "apps/web/src/hooks/useRebuildJournal.ts"
      via: "POST /api/jobs/rebuild-journal/trigger with calendarId"
      pattern: "rebuild-journal"
    - from: "apps/web/src/screens/Journal.tsx"
      to: "apps/web/src/lib/journal-history.ts"
      via: "classifies each trade as history vs entry/exit-only"
      pattern: "journal-history"
---

<objective>
Build the Journal screen: the trade list, the lifecycle chart (Running P&L / Price & spot / Greeks
tabs) with a scrubber over 30-min snapshots, the snapshot table + notes, the pre-Jun-12 graceful UX
(JOURNAL-01), and the per-calendar rebuild button (REBUILD-01) behind a confirmation dialog. Live
data via the typed RPC client; trades before the Jun-12 chain-history start show entry/exit-only,
never an error.

Purpose: The Journal answers the project's core value ("how did price and greeks move over the life
of this trade?"). JOURNAL-01 and REBUILD-01 are Phase-9-owned UI-SPEC action items. Anchor: the
lifecycle chart.
Output: Journal screen + useJournal/useRebuildJournal hooks + LifecycleChart + RebuildButton +
journal-history classifier.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-UI-SPEC.md
@.planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-RESEARCH.md
@.planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-PATTERNS.md
@.claude/rules/typescript.md
@.claude/rules/tdd.md
@packages/contracts/src/journal.ts
@packages/contracts/src/jobs.ts
@mockups/journal-v1.html
</context>

<artifacts_this_phase_produces>
New files/symbols this plan creates (exclude from drift verification):
- `apps/web/src/screens/Journal.tsx` — `Journal` screen.
- `apps/web/src/hooks/useJournal.ts` — `useJournal(calendarId)` (60s poll of GET /api/journal/:id).
- `apps/web/src/hooks/useRebuildJournal.ts` — `useRebuildJournal()` mutation (POST rebuild trigger).
- `apps/web/src/components/LifecycleChart.tsx` — `LifecycleChart` (visx, 3 tabs + scrubber).
- `apps/web/src/components/RebuildButton.tsx` — `RebuildButton` + confirm dialog (REBUILD-01).
- `apps/web/src/lib/journal-history.ts` — `classifyTradeHistory` (history vs entry/exit-only, Jun-12).
- Tests: `apps/web/src/lib/journal-history.test.ts`, `apps/web/src/screens/Journal.test.tsx`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pre-Jun-12 history classifier (JOURNAL-01) + journal/rebuild hooks (TDD)</name>
  <files>apps/web/src/lib/journal-history.ts, apps/web/src/lib/journal-history.test.ts, apps/web/src/hooks/useJournal.ts, apps/web/src/hooks/useRebuildJournal.ts</files>
  <read_first>
    - packages/contracts/src/journal.ts (journalResponse / snapshotResponse shape — parse with this)
    - packages/contracts/src/jobs.ts (triggerJobBodyFor("rebuild-journal") ⇒ calendarId REQUIRED; the route is POST /api/jobs/rebuild-journal/trigger under the bearer-guarded /api/jobs group)
    - apps/web/src/hooks/useStatus.ts (hook pattern: useQuery + 401 short-circuit + contract parse)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-UI-SPEC.md ("Journal screen" history vs entry/exit badge; "Backend Data Gaps" JOURNAL-01 "Data from 2026-06-12 forward" + REBUILD-01 rebuild route; "Copywriting Contract" Journal history/entry-exit/OPEN badges + pre-history note; Auto-Poll 60s)
  </read_first>
  <behavior>
    - classifyTradeHistory(trade) returns "history" when the trade's lifecycle overlaps the Jun-12-forward chain window, else "entry-exit-only" (pre Jun-12)
    - An OPEN trade with chain data classifies as history; a fully pre-Jun-12 closed trade classifies as entry/exit-only
    - useJournal parses through journalResponse; useRebuildJournal sends calendarId in the body
  </behavior>
  <action>
    Write `apps/web/src/lib/journal-history.test.ts` FIRST (RED): assert `classifyTradeHistory`
    returns "history" for a trade with snapshots on/after 2026-06-12, "entry-exit-only" for a trade
    whose entire lifecycle is before 2026-06-12, and handles an OPEN trade (history if any chain data
    exists). Run RED. Then write `apps/web/src/lib/journal-history.ts` with the `2026-06-12`
    chain-history-start constant and the pure `classifyTradeHistory` function (no I/O). Create
    `apps/web/src/hooks/useJournal.ts` (`useJournal(calendarId)`, key `["journal", calendarId]`, 60s
    poll, parse via `journalResponse`, 401 short-circuit) and `apps/web/src/hooks/useRebuildJournal.ts`
    (a `useMutation` POSTing to `/api/jobs/rebuild-journal/trigger` with `{ calendarId }` — note the
    route is under the bearer-guarded jobs group; the Supabase Bearer header from rpc.ts applies;
    invalidate the journal query on success). No `any`/`as`/`!`; parse, don't cast. Run GREEN +
    typecheck. Commit at green.
  </action>
  <verify>
    <automated>vitest run --project apps/web -t "journal-history" && cd apps/web && bun run typecheck && grep -q '2026-06-12' src/lib/journal-history.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `journal-history.test.ts` asserts history / entry-exit-only / OPEN cases; all pass
    - `journal-history.ts` defines the `2026-06-12` start and a pure classifier (no I/O)
    - `useJournal.ts` polls at 60_000ms and parses via `journalResponse`
    - `useRebuildJournal.ts` POSTs `{ calendarId }` to the rebuild trigger route
    - `vitest run --project apps/web -t "journal-history"` exits 0; `bun run typecheck` green
  </acceptance_criteria>
  <done>The Jun-12 classifier and the journal/rebuild hooks exist and parse through contracts, proven by passing tests (JOURNAL-01 foundation).</done>
</task>

<task type="auto">
  <name>Task 2: LifecycleChart (3 tabs + scrubber) + RebuildButton (confirmation dialog, REBUILD-01)</name>
  <files>apps/web/src/components/LifecycleChart.tsx, apps/web/src/components/RebuildButton.tsx</files>
  <read_first>
    - apps/web/src/hooks/useRebuildJournal.ts (the mutation the button triggers)
    - apps/web/src/components/charts/MiniLine.tsx (visx line analog from Plan 05 — reuse the area/line pattern)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-UI-SPEC.md ("Journal screen" center column: 3 mode tabs Running P&L (violet curve+area) / Price & spot (blue) / Greeks (teal vega or amber theta), day-separator dashed lines, the scrubber range input tracking a highlighted point; the destructive "Rebuild journal …" confirmation dialog copy; "Copywriting Contract" Journal lifecycle-tab labels)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-RESEARCH.md (Pattern 7 visx area/line; shadcn Dialog for the confirm)
    - mockups/journal-v1.html (lifecycle chart + scrubber visual reference)
  </read_first>
  <action>
    Create `apps/web/src/components/LifecycleChart.tsx`: a visx chart with shadcn `Tabs` for the three
    locked modes (Running P&L = violet line + area fill; Price & spot = blue line; Greeks = teal vega /
    amber theta), day-separator dashed vertical lines `#27313f` with date labels, and a scrubber (range
    input) below that highlights the selected snapshot point on the curve and surfaces its timestamp +
    values. Accept `snapshots` + the active-tab/scrubber state via props. Create
    `apps/web/src/components/RebuildButton.tsx`: a button that opens a shadcn `Dialog` with the LOCKED
    destructive confirmation copy ("Rebuild journal for \"{calendarId}\"? This overwrites all snapshot
    history. [Rebuild] [Cancel]"); on [Rebuild] it calls the `useRebuildJournal` mutation with the
    selected calendarId; on [Cancel] it dismisses without firing. No `any`/`!`. `bun run typecheck &&
    lint`.
  </action>
  <verify>
    <automated>cd apps/web && bun run typecheck && bun run lint && grep -q "Running P&L" src/components/LifecycleChart.tsx && grep -qi "overwrites all snapshot history" src/components/RebuildButton.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `LifecycleChart.tsx` renders the three locked tab labels and a scrubber tracking a highlighted point
    - `RebuildButton.tsx` shows the locked rebuild confirmation copy and only fires the mutation on confirm
    - `bun run typecheck` + `bun run lint` green
  </acceptance_criteria>
  <done>The lifecycle chart with 3 tabs + scrubber and the rebuild button with a confirmation dialog render per the locked spec.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Journal screen assembled with pre-Jun-12 graceful UX</name>
  <files>apps/web/src/screens/Journal.tsx, apps/web/src/screens/Journal.test.tsx</files>
  <read_first>
    - apps/web/src/hooks/useJournal.ts (snapshots source)
    - apps/web/src/lib/journal-history.ts (history vs entry/exit-only classification)
    - apps/web/src/components/LifecycleChart.tsx, RebuildButton.tsx (Task 2 components)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-UI-SPEC.md ("Journal screen" 3-column layout (trade list / lifecycle / snapshot+notes); pre-history trades show "no day-by-day (pre Jun-12)" + a dashed stub instead of the chart; "Copywriting Contract" Journal headings/badges + Notes placeholder; "Empty / loading / error states" the locked "No journal history yet…" copy)
    - mockups/journal-v1.html (full screen layout)
  </read_first>
  <behavior>
    - A pre-Jun-12 trade renders the entry/exit-only badge and the "no day-by-day (pre Jun-12)" dashed stub (NOT an error, NOT a blank screen) — JOURNAL-01
    - A history-eligible trade renders the lifecycle chart + scrubber + snapshot table
    - Empty journal renders the locked "No journal history yet…" copy
    - The rebuild button is present and wired to the selected calendar
  </behavior>
  <action>
    Write `apps/web/src/screens/Journal.test.tsx` FIRST (RED): a pre-Jun-12 trade → assert the
    entry/exit-only badge + the "no day-by-day (pre Jun-12)" stub renders and NO chart error is thrown;
    a history trade → assert the lifecycle chart region + snapshot table render; empty journal →
    assert the locked "No journal history yet…" copy; assert the RebuildButton is present. Run RED.
    Then build `apps/web/src/screens/Journal.tsx` to the UI-SPEC 3-column layout: left trade list
    (newest-open-first, history/entry-exit/OPEN badges via the classifier), center lifecycle
    (trade header + 3 KPIs + LifecycleChart for history trades OR the dashed pre-history stub for
    entry/exit-only trades + the RebuildButton), right snapshot table + the "Why it moved" callout +
    the Notes textarea (locked placeholder). All headings/badges use the EXACT locked copy. Live-only;
    empty/loading/error use the locked states. No `any`/`!`. Run GREEN + typecheck + lint. Commit at
    green.
  </action>
  <verify>
    <automated>vitest run --project apps/web -t "Journal" && cd apps/web && bun run typecheck && bun run lint</automated>
  </verify>
  <acceptance_criteria>
    - `Journal.test.tsx` asserts the pre-Jun-12 graceful stub (no error/blank), the history lifecycle render, the empty "No journal history yet…" copy, and the RebuildButton presence; all pass
    - `vitest run --project apps/web -t "Journal"` exits 0
    - Pre-Jun-12 trades never throw / never blank (JOURNAL-01); headings/badges match the UI-SPEC exactly
    - `bun run typecheck` + `bun run lint` green; no seed data
  </acceptance_criteria>
  <done>The Journal screen renders the lifecycle for history trades, the graceful pre-Jun-12 stub for older trades, the locked empty state, and the rebuild button — proven by passing tests (JOURNAL-01 + REBUILD-01).</done>
</task>

</tasks>

<verification>
- `vitest run --project apps/web -t "journal-history"` and `-t "Journal"` green
- `bun run typecheck && bun run lint && bun run test` green
- Pre-Jun-12 trades render the graceful stub (JOURNAL-01); rebuild button fires only on confirm (REBUILD-01)
</verification>

<success_criteria>
The Journal screen answers the core value over live 30-min snapshots, handles pre-Jun-12 trades
gracefully (JOURNAL-01), and lets the trader trigger a per-calendar rebuild behind a confirmation
dialog (REBUILD-01) — all with the locked copy and no seed data.
</success_criteria>

<output>
Create `.planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-07-SUMMARY.md` when done
</output>
