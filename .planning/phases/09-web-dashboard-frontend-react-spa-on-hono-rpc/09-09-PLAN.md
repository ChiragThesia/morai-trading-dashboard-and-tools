---
phase: 09-web-dashboard-frontend-react-spa-on-hono-rpc
plan: 09
type: tdd
wave: 7
depends_on: ["09-02"]
files_modified:
  - apps/web/src/lib/tos-parser.ts
  - apps/web/src/lib/tos-parser.test.ts
  - apps/web/src/lib/iv-bisection.ts
  - apps/web/src/lib/iv-bisection.test.ts
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "parseTosOrder applies the 9 locked parse rules and returns a typed calendar struct or null on failure"
    - "Implied-IV bisection finds the flat IV such that BSM(back) − BSM(front) ≈ debit using the @morai/quant kernel"
    - "Parse → IV → re-price round-trips: BSM at the implied IV reproduces the input debit within tolerance"
  artifacts:
    - path: "apps/web/src/lib/tos-parser.ts"
      provides: "TOS calendar paste parser (9 rules)"
      exports: ["parseTosOrder"]
    - path: "apps/web/src/lib/iv-bisection.ts"
      provides: "Implied-IV bisection over the BSM kernel"
      contains: "@morai/quant"
    - path: "apps/web/src/lib/tos-parser.test.ts"
      provides: "9-rule unit suite + round-trip property"
      contains: "parseTosOrder"
  key_links:
    - from: "apps/web/src/lib/iv-bisection.ts"
      to: "packages/quant"
      via: "imports bsmPrice from @morai/quant for the bisection target"
      pattern: "bsmPrice"
    - from: "apps/web/src/lib/tos-parser.ts"
      to: "apps/web/src/lib/iv-bisection.ts"
      via: "Rule 8 implies a flat IV via bisection"
      pattern: "iv-bisection|bisect"
---

<objective>
TDD the TOS calendar paste parser (9 locked rules from the UI-SPEC TOS Parser Contract) and the
implied-IV bisection that backs Rule 8, both ported from `mockups/playground-v3.html` (the reference
implementation that proves sub-1ms in-browser re-pricing). The IV bisection prices over the shared
`@morai/quant` kernel (D-01) so the implied IV — and every downstream Analyzer re-price — matches the
server. This is a dedicated TDD plan (string→struct I/O + a numerical algorithm with defined
tolerance); the Analyzer screen consumes these in Plan 10.

Purpose: Deterministic, well-tested parsing + IV inversion is the foundation of the Analyzer's "paste
a TOS order → analyze a scenario" flow. The parser is pure TS over a plain string (no DOM) — a clean
V5 input-validation boundary.
Output: tos-parser.ts (parseTosOrder) + iv-bisection.ts with unit + fast-check round-trip suites.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-UI-SPEC.md
@.planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-RESEARCH.md
@.planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-PATTERNS.md
@.claude/rules/tdd.md
@.claude/rules/typescript.md
@packages/quant/src/bsm.ts
@mockups/playground-v3.html
</context>

<artifacts_this_phase_produces>
New files/symbols this plan creates (exclude from drift verification):
- `apps/web/src/lib/tos-parser.ts` — `parseTosOrder(text, today, spot, rate)` → `ParsedCalendar | null`
  + the `ParsedCalendar` type.
- `apps/web/src/lib/iv-bisection.ts` — `impliedFlatIv(...)` bisection over `@morai/quant` `bsmPrice`.
- Tests: `apps/web/src/lib/tos-parser.test.ts`, `apps/web/src/lib/iv-bisection.test.ts`.
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Implied-IV bisection over @morai/quant (RED→GREEN)</name>
  <files>apps/web/src/lib/iv-bisection.ts, apps/web/src/lib/iv-bisection.test.ts</files>
  <read_first>
    - packages/quant/src/bsm.ts (the `bsmPrice(S,K,T,sigma,r,q,type)` signature to bisect over)
    - mockups/playground-v3.html (the reference IV-bisection implementation — the math oracle to port)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-UI-SPEC.md ("TOS Calendar Paste Parser Contract" Rule 8: bisect to find iv such that BSM(back, iv) − BSM(front, iv) ≈ debit at current spot; default IV 15% when no debit)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-PATTERNS.md ("Vitest fast-check property tests" Math.fround() bounds + numRuns:1000; bsm.test.ts note about the IV-bisection round-trip property)
  </read_first>
  <behavior>
    - impliedFlatIv finds an iv in a bounded range such that (BSM(back, iv) − BSM(front, iv)) is within tolerance of the target debit at the current spot
    - Round-trip: re-pricing the calendar at the returned iv reproduces the target debit within tolerance (fast-check, numRuns:1000, Math.fround() bounds)
    - Returns a sensible default (15%) or null per the contract when the debit is unprovided / unbracketable
  </behavior>
  <action>
    Write `apps/web/src/lib/iv-bisection.test.ts` FIRST (RED): an example test that for a known
    (front,back,strike,spot,rate,debit) the returned iv re-prices the calendar spread to ≈ debit
    within tolerance; plus a fast-check round-trip property (generate iv in a bounded range with
    Math.fround() bounds, price the spread to get a synthetic debit, bisect back, assert the recovered
    iv re-prices to the same debit) at numRuns:1000. Run RED. Then write `apps/web/src/lib/iv-bisection.ts`:
    `impliedFlatIv` doing a bounded bisection over `bsmPrice` from `@morai/quant` for both legs of the
    calendar (same flat iv on front + back), returning the iv at convergence; handle the no-debit
    default (15%) and the unbracketable case per the contract. No `any`/`as`/`!`. Run GREEN. Commit at
    green.
  </action>
  <verify>
    <automated>vitest run --project apps/web -t "iv-bisection" && cd apps/web && bun run typecheck && grep -q '@morai/quant' src/lib/iv-bisection.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `iv-bisection.test.ts` includes an example test AND a fast-check round-trip property at numRuns:1000 with Math.fround() bounds; both pass
    - `iv-bisection.ts` imports `bsmPrice` from `@morai/quant` (the shared kernel — no re-implementation)
    - The bisection converges to within the documented tolerance and handles the no-debit default
    - `vitest run --project apps/web -t "iv-bisection"` exits 0; `bun run typecheck` green
  </acceptance_criteria>
  <done>Implied-IV bisection over the shared kernel is proven by an example + a round-trip property test.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: TOS parser — 9 rules (RED→GREEN)</name>
  <files>apps/web/src/lib/tos-parser.ts, apps/web/src/lib/tos-parser.test.ts</files>
  <read_first>
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-UI-SPEC.md ("TOS Calendar Paste Parser Contract" — the 9 numbered rules, the sample input string, success/failure behavior, the locked error copy)
    - mockups/playground-v3.html (the reference parser implementation — the oracle for tokenization)
    - apps/web/src/lib/iv-bisection.ts (Rule 8 uses impliedFlatIv from Task 1)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-RESEARCH.md ("TOS Parser" code example — ParsedCalendar shape; the parser belongs in apps/web/src/lib, web-only)
    - .planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-VALIDATION.md (parseTosOrder unit + round-trip rows)
  </read_first>
  <behavior>
    - parseTosOrder applies all 9 rules: BUY/SELL+qty (abs, min 1), PUT/CALL (default P), strike (last 3–5 digit before PUT/CALL), debit (after @, optional), two dates (day + 3-letter month + 2-digit year, sorted → front/back), DTE validation (front>0, back>front), underlying (after CALENDAR, default SPX), Rule-8 implied flat IV via impliedFlatIv, call+put both supported
    - Returns a typed ParsedCalendar on success; returns null when fewer than 2 expiries / no strike / no PUT-or-CALL or DTE invalid
    - The canonical sample "BUY +1 CALENDAR SPX 100 (Weeklys) 30 NOV 26/20 NOV 26 [AM] 7550 PUT @5.80 LMT GTC" parses to strike 7550, type P, qty 1, front 20 NOV / back 30 NOV, debit 5.80, underlying SPX
  </behavior>
  <action>
    Write `apps/web/src/lib/tos-parser.test.ts` FIRST (RED): one test PER rule (qty extraction incl.
    bare N and minimum 1; PUT/CALL incl. default-P; strike extraction; optional debit; two-date scan +
    ascending sort into front/back; DTE rejection when front≤0 or back≤front; underlying extraction +
    SPX default; Rule-8 IV via bisection; call+put both); plus the canonical sample assertion; plus a
    round-trip property (parse a generated order → re-price at the implied IV → ≈ the input debit) at
    numRuns:1000; plus failure cases returning null (one expiry, no strike, no PUT/CALL). Run RED.
    Then write `apps/web/src/lib/tos-parser.ts`: the `ParsedCalendar` type + `parseTosOrder(text,
    today, spot, rate)` implementing the 9 rules with regex tokenization (port the playground-v3
    logic), computing front/back DTE from `today`, and calling `impliedFlatIv` for Rule 8. Operate on
    a plain string only — never touch the DOM, never `eval`. No `any`/`as`/`!`; return `null` (not a
    thrown error) on parse failure. Run GREEN + typecheck + lint. Commit at green.
  </action>
  <verify>
    <automated>vitest run --project apps/web -t "parseTosOrder" && cd apps/web && bun run typecheck && bun run lint</automated>
  </verify>
  <acceptance_criteria>
    - `tos-parser.test.ts` has a test per rule + the canonical sample + a round-trip property (numRuns:1000) + null-on-failure cases; all pass
    - `parseTosOrder("BUY +1 CALENDAR SPX 100 (Weeklys) 30 NOV 26/20 NOV 26 [AM] 7550 PUT @5.80 LMT GTC", …)` returns `{ strike: 7550, type: "P", qty: 1, … }` with front=20 NOV, back=30 NOV, debit 5.80
    - Failure inputs return `null` (never throw); the parser touches no DOM and uses no `eval`
    - `vitest run --project apps/web -t "parseTosOrder"` exits 0; `bun run typecheck` + `bun run lint` green
  </acceptance_criteria>
  <done>The 9-rule TOS parser is proven by per-rule tests, the canonical sample, a round-trip property, and null-on-failure cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user paste string → parser | Free-text TOS order pasted by the user, parsed in pure TS |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Tampering (V5 Input Validation) | tos-parser.ts | mitigate | Parser operates on a plain string only — no DOM write, no `eval`, no `dangerouslySetInnerHTML`. Parsed output renders via React JSX (auto-escaped). Failure returns `null`, never a thrown exception that could leak internals |
| T-09-07 | Denial of Service | iv-bisection.ts | mitigate | Bisection is bounded (fixed max iterations + bounded iv range) so a crafted debit cannot cause an unbounded loop |
</threat_model>

<verification>
- `vitest run --project apps/web -t "iv-bisection"` and `-t "parseTosOrder"` green (units + round-trip properties)
- `bun run typecheck && bun run lint && bun run test` green
- Parser is pure (no DOM/eval); IV bisection imports the shared @morai/quant kernel
</verification>

<success_criteria>
The TOS parser (9 locked rules) and the implied-IV bisection are deterministic and well-tested over
the shared kernel, so the Analyzer's paste flow produces an IV — and downstream re-prices — that
match the server. The V5 input boundary is modeled at ASVS L1.
</success_criteria>

<output>
Create `.planning/phases/09-web-dashboard-frontend-react-spa-on-hono-rpc/09-09-SUMMARY.md` when done
</output>
