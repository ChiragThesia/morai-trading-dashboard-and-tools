---
phase: 12-streaming-ts-fan-out
plan: 03
type: tdd
wave: 3
depends_on: ["12-02"]
files_modified:
  - apps/sidecar/stream_proxy.py
  - apps/sidecar/tests/test_stream_proxy.py
  - apps/sidecar/positions_proxy.py
  - apps/sidecar/tests/test_positions_proxy.py
  - apps/sidecar/main.py
autonomous: true
requirements: [STRM-05]
must_haves:
  truths:
    - "GET /sidecar/events returns a text/event-stream that drains streamer.event_queue, framing each event as a data: block, and emits an event: ping keep-alive on idle (Pattern 5)."
    - "The SSE generator stops when await request.is_disconnected() is true (Pitfall 10 — awaited)."
    - "GET /sidecar/positions returns the current trader-client positions with an asOf field that ends in Z (chain_proxy.py lesson) and a 503 {error:AUTH_EXPIRED} when trader_client is absent (Pattern 6)."
    - "main.py includes both routers and launches start_streamer as an asyncio task at the same point as the keep-alive task — after the advisory lock is held and clients are initialized (Pattern 1)."
  artifacts:
    - path: "apps/sidecar/stream_proxy.py"
      provides: "GET /sidecar/events StreamingResponse draining streamer.event_queue with ping keep-alive"
      contains: "/sidecar/events"
    - path: "apps/sidecar/positions_proxy.py"
      provides: "GET /sidecar/positions reconcile endpoint (trader client, Z-suffixed asOf, 503 AUTH_EXPIRED guard)"
      contains: "/sidecar/positions"
    - path: "apps/sidecar/main.py"
      provides: "router includes + start_streamer task launch inside the lock/init lifecycle"
      contains: "start_streamer"
  key_links:
    - from: "apps/sidecar/stream_proxy.py"
      to: "apps/sidecar/streamer.py event_queue"
      via: "async drain of the shared asyncio.Queue"
      pattern: "event_queue"
    - from: "apps/sidecar/main.py"
      to: "apps/sidecar/streamer.py start_streamer"
      via: "asyncio.create_task(start_streamer(app)) after lock + client init"
      pattern: "start_streamer"
  prohibitions:
    - "MUST NOT expose /sidecar/events or /sidecar/positions on a public domain — internal Railway private-net only (GW-05)."
    - "MUST NOT emit any timestamp as +00:00 — use the utc_now_z helper so Zod .datetime() accepts it (Pitfall 5)."
    - "MUST NOT log str(exc) or token/account values — log type(exc).__name__ only (token_store V6)."
    - "MUST NOT launch start_streamer before app.state.has_lock is True (login-after-lock, Pattern 1)."
    - "MUST NOT call request.is_disconnected without awaiting it (Pitfall 10)."
---

<objective>
Expose the sidecar's stream to the TS server and wire the streamer into the app lifecycle. Two
new FastAPI routes — GET /sidecar/events (an internal SSE endpoint draining the streamer's
event queue) and GET /sidecar/positions (the cold-start/reconnect reconcile source, STRM-05) —
plus the main.py changes that include both routers and launch the streamer background task at
the correct point in the advisory-lock lifecycle.

Purpose: STRM-05 reconcile endpoint + the sidecar→server SSE transport that the server fan-out
(12-05) consumes; the streamer (12-02) is started exactly once, after the lock.
Output: stream_proxy.py, positions_proxy.py, main.py wiring + pytest suites.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-streaming-ts-fan-out/12-RESEARCH.md
@.planning/phases/12-streaming-ts-fan-out/12-PATTERNS.md
@apps/sidecar/main.py
@apps/sidecar/chain_proxy.py
</context>

<artifacts_this_phase_produces>
This plan creates:
- `GET /sidecar/events` (SSE) in `apps/sidecar/stream_proxy.py`
- `GET /sidecar/positions` (REST reconcile) in `apps/sidecar/positions_proxy.py`
- `main.py` router includes + `asyncio.create_task(start_streamer(app))` launch
- Consumed by 12-05 (`sidecar-sse.ts` reads /sidecar/events; the reconcile route fetches /sidecar/positions).
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: GET /sidecar/events internal SSE endpoint (drains event_queue, ping keep-alive)</name>
  <files>apps/sidecar/stream_proxy.py, apps/sidecar/tests/test_stream_proxy.py</files>
  <read_first>
    - 12-RESEARCH.md Pattern 5 (StreamingResponse generator + is_disconnected + 25s ping) + Pitfall 10 (await is_disconnected) + Assumption A4 (ping keepalive)
    - 12-PATTERNS.md stream_proxy.py section (analog: chain_proxy.py module header/router/logger; StreamingResponse instead of JSONResponse)
    - apps/sidecar/streamer.py (event_queue — the shared asyncio.Queue this endpoint drains)
    - apps/sidecar/chain_proxy.py (router/logger module shape + error-logging discipline)
  </read_first>
  <behavior>
    - GET /sidecar/events returns StreamingResponse(media_type="text/event-stream").
    - The generator drains streamer.event_queue and yields each event as a "data: <json>\n\n" block.
    - On asyncio.TimeoutError (idle ~25s) it yields an "event: ping\ndata: \n\n" keep-alive (A4).
    - When await request.is_disconnected() is true, the generator breaks and logs at INFO (Pitfall 10).
    - Test drives it by pre-seeding event_queue and asserting the first data block contains the event JSON; assert the ping path on a forced timeout; assert clean stop on a faked disconnected request.
  </behavior>
  <action>
    Implement stream_proxy.py mirroring chain_proxy.py's module header (APIRouter, module logger). The route
    returns a StreamingResponse whose async generator loops: check await request.is_disconnected() (break +
    INFO log when true); else await asyncio.wait_for(event_queue.get(), timeout=25.0); on an event yield the
    data block, on TimeoutError yield the ping. Import event_queue from streamer.py. Keep error logging to
    type(exc).__name__ only. Tests inject a fake Request with an awaitable is_disconnected and pre-seed the
    queue (no live stream). This endpoint is internal (GW-05) — add a module docstring noting private-net only.
  </action>
  <verify>
    <automated>cd apps/sidecar && .venv/bin/python -m pytest tests/test_stream_proxy.py -x</automated>
  </verify>
  <acceptance_criteria>
    - RED first, then GREEN.
    - Seeded event drains as a data: block; idle path yields event: ping; disconnected request stops the generator (awaited is_disconnected).
    - Module docstring marks the endpoint internal/private-net only.
  </acceptance_criteria>
  <done>GET /sidecar/events streams queued events with ping keep-alive and awaited disconnect detection; pytest green.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GET /sidecar/positions reconcile endpoint (STRM-05, Z-suffix asOf, AUTH_EXPIRED guard)</name>
  <files>apps/sidecar/positions_proxy.py, apps/sidecar/tests/test_positions_proxy.py</files>
  <read_first>
    - 12-RESEARCH.md Pattern 6 (positions_proxy.py — PositionItem/PositionsResponse Pydantic models + 503 guard) + Code Examples utc_now_z
    - 12-PATTERNS.md positions_proxy.py section (EXACT analog: chain_proxy.py — Pydantic response model, auth-expired 503, trader_client substitution)
    - apps/sidecar/chain_proxy.py (full route shape, 503 AUTH_EXPIRED guard, exception logging)
    - packages/contracts/src/brokerage.ts (positions response field names: occSymbol, longQty, shortQty, averagePrice, marketValue, underlyingSymbol — match these so the server reconcile maps cleanly)
  </read_first>
  <behavior>
    - GET /sidecar/positions with a present trader_client returns PositionsResponse { positions:[{occSymbol, longQty, shortQty, marketValue?, underlyingSymbol}], asOf } where asOf ends in "Z".
    - With trader_client absent (None on app.state) it returns 503 JSONResponse {error:"AUTH_EXPIRED"} (Pattern 6).
    - A raised exception from the trader positions call is caught, logged as type(exc).__name__, and returns 503 AUTH_EXPIRED (chain_proxy.py discipline).
    - Field names mirror the brokerage.ts positions contract so 12-05 can map to ReconciledPosition / streamReconcileEvent.
  </behavior>
  <action>
    Implement positions_proxy.py as a near-copy of chain_proxy.py's REST proxy: Pydantic PositionItem +
    PositionsResponse models, the trader_client null guard returning 503 AUTH_EXPIRED, the try/except around
    the trader positions call (log type(exc).__name__ only), and asOf = utc_now_z(). Substitute trader_client
    for market_client and the positions endpoint for the chain call. Tests inject a fake app.state.trader_client
    returning canned positions and assert shape + Z-suffix; a second test asserts the 503 path with trader_client=None.
  </action>
  <verify>
    <automated>cd apps/sidecar && .venv/bin/python -m pytest tests/test_positions_proxy.py -x</automated>
  </verify>
  <acceptance_criteria>
    - RED first, then GREEN.
    - Present client → PositionsResponse with asOf ending in "Z" and brokerage-contract field names.
    - Absent/erroring client → 503 {error:AUTH_EXPIRED}; only type(exc).__name__ logged.
  </acceptance_criteria>
  <done>GET /sidecar/positions returns Z-stamped current positions or a 503 AUTH_EXPIRED guard; pytest green.</done>
</task>

<task type="execute">
  <name>Task 3: Wire routers + launch the streamer task in main.py (login-after-lock)</name>
  <files>apps/sidecar/main.py, apps/sidecar/tests/test_stream_proxy.py</files>
  <read_first>
    - apps/sidecar/main.py (_acquire_lock_and_init / _trader_token_keepalive launch point ~lines 196-227; include_router calls ~lines 322-323; lifespan)
    - 12-PATTERNS.md streamer.py section ("streamer task created at the same point as keepalive_task, after lock acquisition and client init")
    - apps/sidecar/streamer.py (start_streamer signature + event_queue)
  </read_first>
  <action>
    Glue only (no business logic): import the two routers and include them next to health/chain routers
    (app.include_router(stream_router); app.include_router(positions_router)). In _acquire_lock_and_init,
    after _init_schwab_clients and alongside keepalive_task = asyncio.create_task(_trader_token_keepalive(app)),
    add streamer_task = asyncio.create_task(start_streamer(app)); cancel + await it in the same finally/shutdown
    block as keepalive_task (CancelledError-clean). The streamer must launch only inside this post-lock path so
    login() never runs before app.state.has_lock. Add a TestClient assertion that both new routes are mounted
    (e.g. /sidecar/events + /sidecar/positions resolve, not 404) under the no-lifespan test harness.
  </action>
  <verify>
    <automated>cd apps/sidecar && .venv/bin/python -m pytest tests/ -x</automated>
  </verify>
  <acceptance_criteria>
    - Both routers included; routes resolve (not 404) under the existing TestClient harness.
    - start_streamer launched only in the post-lock init path, cancelled cleanly on shutdown alongside keepalive.
    - Full sidecar pytest suite green.
  </acceptance_criteria>
  <done>main.py mounts both new routes and launches the streamer task after the lock; full pytest suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → sidecar (internal SSE/REST) | TS server reads /sidecar/events + /sidecar/positions over the Railway private net |
| trader API → sidecar | Positions data fetched with the trader token (kept fresh by Phase 11 keep-alive) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-01 | Information Disclosure | /sidecar/events + /sidecar/positions exposure | mitigate | Internal Railway private-net only (GW-05) — no public domain; documented in module docstrings |
| T-12-03-02 | Information Disclosure | error logging on the reconcile path | mitigate | Log type(exc).__name__ only; never str(exc), token, or account values (token_store V6) |
| T-12-03-03 | Tampering | malformed timestamps to the TS Zod boundary | mitigate | utc_now_z on asOf and every emitted ts (Pitfall 5) so Zod .datetime() accepts the payload |
| T-12-03-04 | Elevation of Privilege | streamer launched before lock | mitigate | start_streamer task created only in the post-lock init path (login-after-lock, Pattern 1) |
| T-12-SC | Tampering | pip installs | accept | Zero new PyPI packages — StreamingResponse + existing FastAPI only (RESEARCH audit) |
</threat_model>

<verification>
- `cd apps/sidecar && .venv/bin/python -m pytest tests/ -x` green.
- Grep gate: no +00:00 timestamp emission; no str(exc)/token/account logging.
- Manual/RTH (UAT — VALIDATION.md / STRM-05): after a sidecar restart, /sidecar/positions returns current positions used to seed the first reconcile SSE event.
</verification>

<success_criteria>
- GET /sidecar/events streams queued events with ping keep-alive and awaited disconnect detection.
- GET /sidecar/positions returns Z-stamped current positions or a 503 AUTH_EXPIRED guard (STRM-05 source).
- main.py mounts both routes and launches the streamer task once, after the lock.
</success_criteria>

<output>
Create `.planning/phases/12-streaming-ts-fan-out/12-03-SUMMARY.md` when done.
</output>
