---
phase: 35.1-mobile-overview-complete-redesign
plan: 03
type: execute
wave: 3
depends_on: [35.1-01, 35.1-02]
files_modified:
  - apps/web/src/components/PositionCard.tsx
  - apps/web/src/components/PositionCard.test.tsx
  - apps/web/src/screens/Overview.tsx
  - apps/web/src/screens/overview-mobile/OverviewMobile.tsx
  - apps/web/src/screens/Overview.test.tsx
autonomous: true
requirements: [MOBILE-09]
must_haves:
  truths:
    - "Card row 1 reads label+verdict left / unreal P&L right as the 16px sign-colored focal number (D-07)"
    - "Card row 2 is one muted meta line: expiry · DTE · net val (D-07)"
    - "Expand shows the greeks grid always, plus VerdictDetailBody when a verdict exists (desktop parity, D-07)"
    - "One mono footer line under the card list: Net {usd} · {signed unreal} · {n}/{m} included, reacting to checkbox toggles (D-11)"
    - "Exit-advisor state branches render under the list with copy verbatim from the contract"
  artifacts:
    - "apps/web/src/components/PositionCard.tsx re-hierarchied + verdictObservedAt prop"
    - "OverviewMobile positions section: heading row, card list, footer, exitsBody"
  key_links:
    - "Card expand/include reuse the model's selectedRowKey/excluded state — no second mechanism (D-07)"
    - "Footer total computed via resolveLivePositionRow over included legs — the same lib fn as the desktop Net row (D-11)"
    - "Expand stays UN-gated by verdict (catch #23 — greeks are mobile's only greeks surface)"
---

<objective>
Re-hierarchy `PositionCard` (D-07: focal unreal P&L, meta line, verdict detail on
expand) and build the mobile positions section (heading + card list + footer total +
exit-advisor states) into OverviewMobile (D-11).

Purpose: cards were "the best part of 35" but flat — this makes each card answer
"which position, what verdict, how much" in one glance.
Output: positions section complete on mobile; PositionCard is mobile-only so desktop
cannot regress (D-07).

Closes validation claims: J10, J11; extends J5 (hero→chart→cards order).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/35.1-mobile-overview-complete-redesign-dedicated-mobile-only-comp/35.1-UI-SPEC.md
@.planning/phases/35.1-mobile-overview-complete-redesign-dedicated-mobile-only-comp/35.1-VALIDATION.md
@apps/web/src/components/PositionCard.tsx
@apps/web/src/components/PositionCard.test.tsx
@apps/web/src/screens/overview-mobile/OverviewMobile.tsx
@apps/web/src/screens/overview-mobile/useOverviewModel.ts
@apps/web/src/screens/Overview.tsx
@apps/web/src/screens/Overview.test.tsx
@apps/web/src/screens/HeldPositionsPanel.tsx
@.planning/phases/35.1-mobile-overview-complete-redesign-dedicated-mobile-only-comp/35.1-02-SUMMARY.md
</context>

<repo_law>
- TDD red→green per .claude/rules/tdd.md: RED run output shown per task, commit at green
  only (test + impl in one conventional commit).
- Explicit commit paths only — never `git add -A`.
- No `any`/`as`/`!`; match existing style; copy classes/copy VERBATIM from
  35.1-UI-SPEC.md §5 (PositionCard), §6 (Positions section), §Copywriting Contract.
</repo_law>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PositionCard re-hierarchy + verdictObservedAt (D-07) — J10</name>
  <files>apps/web/src/components/PositionCard.tsx, apps/web/src/components/PositionCard.test.tsx, apps/web/src/screens/Overview.tsx</files>
  <behavior>
    Update PositionCard.test.tsx to the D-07 contract, RED first (some existing tests are
    MIGRATED to the new hierarchy — expected churn, keep every behavior covered):
    - J10a row 1: with a live-resolvable fixture, the unreal value renders in an element
      whose className contains `font-mono text-base font-bold tabular-nums` plus the
      signClass color; when unreal is null it renders `—` with `text-dim`. The old
      "Net val"/"Unreal" Stat labels are GONE (queryByText("Net val") null).
    - J10b row 2: one meta line containing `{expiry.line1} · {expiry.line2} · {usd(netVal)}`
      with classes `font-mono text-[10px] text-dim truncate`.
    - J10c card surface: className contains `bg-raise/30` and `ring-1 ring-line`
      (card-not-table-row cue); `opacity-40` when `included={false}` (unchanged).
    - J10d expand + verdict: `expanded` + `verdict={VERDICT}` +
      `verdictObservedAt="2026-07-11T14:30:00Z"` → greeks grid AND the VerdictDetailBody
      content render (assert on a VerdictDetailBody-owned element/text, e.g. the rule/
      metric line); `expanded` with `verdict={null}` → greeks grid only.
    - J10e unchanged behaviors re-asserted: checkbox fires onToggleIncluded without
      onSelect; expand button aria-expanded flips; IV n/a badge iff ivNa; VerdictChip
      renders in row 1 when verdict present.
  </behavior>
  <action>
    Edit PositionCard.tsx in place per UI-SPEC §5 (PositionCard is mobile-only — sole
    consumer is the card list — so restyling cannot regress desktop, D-07):
    - Props: add `readonly verdictObservedAt: string | null` (for VerdictDetailBody's
      as-of). All existing props unchanged.
    - Card surface: `rounded-lg bg-raise/30 p-3 ring-1 ring-line transition-opacity`
      (+ `opacity-40` when excluded — unchanged).
    - Structure: checkbox label (unchanged 44px hit area + stopPropagation split via
      sibling layout) + expand `<button aria-expanded>` filling the rest.
    - Row 1 `flex items-center justify-between gap-2`: left
      `flex min-w-0 items-center gap-1.5` → label (`font-display text-sm font-bold text-txt`),
      IV n/a Badge (unchanged), VerdictChip (unchanged component); right → unreal:
      `font-mono text-base font-bold tabular-nums` + `signClass(unreal)` (`text-dim` `—`
      when null).
    - Row 2 `mt-1 font-mono text-[10px] text-dim truncate`:
      `{row.expiry.line1} · {row.expiry.line2} · {usd(netVal)}`.
    - REMOVE the old two-Stat Net val/Unreal row (net val moved to row 2 meta; unreal to
      row 1 focal).
    - Expanded `mt-2 border-t border-line/40 pt-2`: existing 4-col greeks grid, then
      `verdict !== null && <VerdictDetailBody row={verdict} observedAt={verdictObservedAt ?? ""} />`
      wrapped in `mt-2`. Import VerdictDetailBody from ../screens/HeldPositionsPanel.tsx.
    - Expand stays UN-gated by verdict (catch #23).
    - Call-site fix (typecheck): the desktop PositionsTable card list in Overview.tsx
      still renders PositionCard (it is display-none at every width the desktop tree
      mounts and is deleted in plan 04) — thread the existing `verdictObservedAt` prop
      PositionsTable already receives into it. That one prop addition is the ONLY
      Overview.tsx line this plan touches.
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/components/PositionCard.test.tsx && bunx vitest run apps/web/src/screens/Overview.test.tsx && bun run typecheck</automated>
  </verify>
  <done>
    J10 green after shown RED; desktop guard (J2) + full Overview suite still green.
    Commit: `feat(35.1-03): PositionCard re-hierarchy — focal unreal + verdict detail (D-07)`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mobile positions section — heading, card list, footer total, exitsBody (D-11) — J11</name>
  <files>apps/web/src/screens/overview-mobile/OverviewMobile.tsx, apps/web/src/screens/Overview.test.tsx</files>
  <behavior>
    Extend the mobile describe in Overview.test.tsx, RED first (default jsdom render):
    - Section heading: SectionLabel text `Positions`, the LiveStatusBadge, and the
      `Exit rules ▸` DialogTrigger render when an exits snapshot is set via
      setExitsReturn (reuse the file's existing helper + fixtures).
    - Card list: with CAL_FRONT/CAL_BACK + CAL2 fixtures, one `position-card-*` per
      calendar row; tapping a card body reveals the greeks grid (expand un-gated —
      no verdict needed); tapping again collapses (handleSelectRow toggle semantics).
    - J11: footer line shows `2/2 included`; unchecking one card's checkbox → `1/2
      included` AND the unchecked card gains `opacity-40` AND
      `latestPayoffChartProps().positionSetSignature` reflects included=false (the same
      lifted state drives chart + footer).
    - Footer format: matches `Net {usd(netVal)} · {signedUsd(unreal)} · {n}/{m} included`
      with `—` for null unreal; hidden when rows.length is 0.
    - Empty state: with `setPositions([])`, the exact copy
      `No open positions. Register a calendar via the API or paste a TOS order in the Analyzer.`
      renders and no footer renders.
    - Exit states: error branch renders `Couldn't load exit verdicts.` + Retry button
      (fires refetch); cold-start renders `Exit advisor warming up`; unlinked verdicts
      render the HeldPositionsPanel `Unlinked verdicts` title (reuse desktop tests'
      fixtures — same branches, mobile tree).
    - J5 extension: `mobile-payoff` precedes the first position card in DOM order.
    - No `<table>` anywhere: `queryByRole("table")` null in the default render.
  </behavior>
  <action>
    Build the positions section inside OverviewMobile per UI-SPEC §6, after
    MobileRiskPanel, inside a `px-4` section:
    - Heading row `flex items-center justify-between gap-2 mb-2`: SectionLabel
      `Positions` + `LiveStatusBadge` (spread the model's live-badge props exactly as
      desktop passes them) + the existing `Exit rules ▸` Dialog trigger with the SAME
      classes as the desktop trigger (copy from Overview.tsx) opening ExitRulesPanel —
      rendered only when the exits snapshot is non-null (desktop parity).
    - Card list `flex flex-col gap-2`: map the model's rows (buildRows over positions —
      reuse the exported helper via the model, never re-derive) → PositionCard with
      spot/liveGreeks/ivNa/verdict/marketSession from the model,
      `expanded={selectedRowKey === r.key}` (UN-gated by verdict),
      `onSelect={handleSelectRow}`, `included={!excluded.has(r.key)}`,
      `onToggleIncluded={handleToggleExcluded}`, and
      `verdictObservedAt={exitsSnapshot?.observedAt ?? null}`.
    - Footer (rows.length > 0): `mt-2 font-mono text-[11px] text-muted-foreground tabular-nums`
      rendering `Net {usd(total.netVal)} · {signedUsd(total.unreal)} · {included}/{rows.length} included`
      (signClass on the unreal segment, `—` when null), where total =
      `resolveLivePositionRow` over the included rows' legs — a view-level call to the
      shared lib fn, same as the desktop Net row (D-11). Memoize like the desktop table
      does.
    - Below the footer: the exitsBody — re-render the same five state branches
      (loading / error+Retry / warming-up / empty / Unlinked verdicts) with copy VERBATIM
      from the Copywriting Contract (view duplication sanctioned, strings identical to
      Overview.tsx's exitsBody; the model exposes the exits slice). Keep the desktop
      testids (held-positions-loading etc.) so fixtures assert uniformly.
    - No `<table>`, no ChipRail, no MetricChip in this file.
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/screens/Overview.test.tsx && bun run test && bun run typecheck && bun run lint</automated>
  </verify>
  <done>
    J11 + section behaviors green after shown RED; full suite green; J2 desktop guard
    untouched. Commit: `feat(35.1-03): mobile positions section — cards + footer total + exit states (D-11)`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Re-composition of already-rendered position/verdict data; no new input parsing, no new network surface, zero new dependencies |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35.1-05 | Repudiation/Integrity | Footer total drifting from chart inclusion state (two sources of truth) | low | mitigate | D-11/J11: footer computed from the SAME lifted excluded-set + resolveLivePositionRow the chart uses; test asserts one checkbox click moves both |
| T-35.1-06 | Tampering | Supply chain (npm installs) | low | accept | Zero new dependencies — no install tasks exist |
</threat_model>

<verification>
- `bun run test` + typecheck + lint green.
- Desktop tripwire (this plan touches Overview.tsx — one prop-threading line): J2
  structural guard green; `git diff apps/web/src/screens/Overview.tsx` shows ONLY the
  verdictObservedAt prop addition on the card-list call site.
- Rollback note: revert the two commits; PositionCard's old layout returns and the
  mobile tree falls back to the plan-02 state. No shared-state or data-layer surface.
</verification>

<success_criteria>
- J10, J11 green; J5 order hero→chart→cards asserted.
- PositionCard has exactly one new prop (verdictObservedAt); all pre-existing card
  behaviors (checkbox split, aria-expanded, dim, IV n/a) still covered by tests.
- Positions copy is character-identical to the Copywriting Contract table.
</success_criteria>

<output>
Create `.planning/phases/35.1-mobile-overview-complete-redesign-dedicated-mobile-only-comp/35.1-03-SUMMARY.md` when done (include RED outputs).
</output>
