---
phase: 35.1-mobile-overview-complete-redesign
plan: 04
subsystem: ui
tags: [react, vitest, mobile, tdd, market-section, deletion-refactor]

# Dependency graph
requires:
  - phase: 35.1-mobile-overview-complete-redesign
    provides: useOverviewModel + Overview switch (plan 01), MobileHero + MobileRiskPanel (plan 02), positions section + PositionCard re-hierarchy (plan 03)
provides:
  - MobileMarketSection (D-08) — key levels (exact GexRail rows) + gamma/greeks/macro Stat grids + MarketRail reused verbatim as a closed disclosure; NO GammaProfile/GexBars anywhere in the mobile tree
  - MarketRail summary copy `Regime · COT · health` (D-14); open-attribute mechanism (D-09/catch #24) untouched
  - Desktop Overview cleaned of Phase-35 responsive dead branches (D-12) — pill-header priority row + secondary ChipRail, positions-card-list twin, payoff bleed wrapper all deleted
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Mobile market data = headline stat grids, never shrunken desktop charts (D-08)"
    - "A component that only mounts >=1024px carries NO lg: responsive splits — the JS branch replaces the CSS branch (D-12)"

key-files:
  created:
    - apps/web/src/screens/overview-mobile/MobileMarketSection.tsx
    - apps/web/src/screens/overview-mobile/MobileMarketSection.test.tsx
  modified:
    - apps/web/src/screens/MarketRail.tsx
    - apps/web/src/screens/MarketRail.test.tsx
    - apps/web/src/screens/overview-mobile/OverviewMobile.tsx
    - apps/web/src/screens/Overview.tsx
    - apps/web/src/screens/Overview.test.tsx

key-decisions:
  - "MobileMarketSection root carries data-testid=mobile-market (spec named none; follows the mobile-* convention) for the J5 order chain"
  - "gex-undefined branch renders the unavailable copy as a plain <p> line (spec says 'line', not the GexRail Panel) — macro + net-book-greeks grids still render per spec §7 note"
  - "Payoff bleed wrapper dropped entirely (plan allowed keep-or-drop) — a classless wrapper div contradicts the deletion phase's point; zero pixel effect either way"
  - "J1's `market-rail absent in mobile tree` assertion updated to `present but closed` — the rail is mobile-legal by design as of D-08 block 6"

patterns-established:
  - "rg-based no-import gates match PROSE too — never name banned components in comments/test titles under the gated directory"

requirements-completed: [MOBILE-10]

coverage:
  - id: J12
    description: "MarketRail summary reads 'Regime · COT · health'; details lacks open under jsdom default, carries it under the desktop stub"
    requirement: MOBILE-10
    verification:
      - kind: unit
        ref: "apps/web/src/screens/MarketRail.test.tsx (7 tests)"
        status: pass
    human_judgment: false
  - id: J13
    description: "Mobile market section — key-level rows + gamma/greeks/macro Stat grids; no GammaProfile/GexBars leakage; gex-undefined copy; closed rail with D-14 summary"
    requirement: MOBILE-10
    verification:
      - kind: unit
        ref: "apps/web/src/screens/overview-mobile/MobileMarketSection.test.tsx (9 tests)"
        status: pass
    human_judgment: false
  - id: J5
    description: "Complete mobile DOM order — hero precedes chart precedes first card precedes market section (compareDocumentPosition chain)"
    requirement: MOBILE-10
    verification:
      - kind: unit
        ref: "apps/web/src/screens/Overview.test.tsx (85 tests)"
        status: pass
    human_judgment: false
  - id: J15
    description: "Phase integration gate — full suite + typecheck + lint green after the D-12 deletion"
    requirement: MOBILE-10
    verification:
      - kind: integration
        ref: "bun run test (3316 tests, 299 files) + bun run typecheck + bun run lint"
        status: pass
    human_judgment: false
  - id: C1-C11
    description: "chrome-devtools checks (390x844 + 320px + 1440px pixel-identity + resize swap)"
    requirement: MOBILE-10
    verification:
      - kind: manual
        ref: "PENDING-ORCHESTRATOR — executor has no browser tools; see C1-C11 section below"
        status: pending
    human_judgment: true

# Metrics
duration: 16min
completed: 2026-07-11
status: complete
---

# Phase 35.1 Plan 04: Market Section + Desktop Cleanup Summary

**The mobile MARKET section now answers "where are the walls, what's dealer gamma, what's macro doing" with headline stat grids instead of desktop charts shrunk to axis soup — key levels reuse the exact GexRail rows, gamma/greeks/macro sit in 2-col Stat grids, and the regime/COT/health rail folds into a closed native disclosure — while the desktop Overview sheds every Phase-35 responsive dead branch it can no longer reach (D-12)**

## Performance

- **Duration:** ~16 min
- **Started:** 2026-07-11T18:01:12Z
- **Completed:** 2026-07-11T18:17:30Z
- **Tasks:** 3
- **Files modified:** 7 (2 created)

## Accomplishments
- MarketRail summary copy → `Regime · COT · health` (D-14); the `open={isDesktop || undefined}` mechanism (catch #24 law) is byte-untouched, and the summary stays `lg:hidden` so the change has zero desktop surface
- `MobileMarketSection` (D-08, UI-SPEC §7): SectionLabel `Market` → Key levels (exact GexRail row markup off `keyLevelsFor`) → Gamma grid (`net γ /1%` regime-colored, `0DTE γ` sign-colored) → Net book greeks 2x2 (verbatim GexRail) → Macro grid (VVIX/Fed funds/10y−2y/COT lev, chip formatting, `—` nulls) → `<MarketRail />` closed; `gex === undefined` renders `GEX data unavailable — run fetch-chain to populate.` instead of the key-levels + gamma blocks
- Composed as OverviewMobile's last section; J5 order chain complete (hero → chart → first card → market)
- D-12 deletion: PillHeader priority row + secondary ChipRail gone (full row de-gated to plain flex-wrap, wrapper unconditionally `sticky top-0 z-10 ... backdrop-blur`); positions-card-list twin + PositionCard import gone (table de-gated to plain `table`); payoff bleed wrapper gone; 67 insertions / 188 deletions
- Zero `GammaProfile`/`GexBars`/`ChipRail`/`MetricChip` usage in the mobile tree; zero `lg:hidden` arms left in Overview.tsx

## Task Commits

1. **Task 1: MarketRail summary copy (D-14) — J12** — `bb4e436` (feat) — RED: 1 failed / 6 passed shown, then 7/7
2. **Task 2: MobileMarketSection + compose (D-08) — J13, J5 complete** — `1bb649e` (feat) — RED: import-failure (new module) for the component file + 1 failed / 87 passed for the J5 composition assertion, then 9/9 + 88/88 + rg no-import gate
3. **Task 3: D-12 desktop dead-branch cleanup + integration gate** — `aabcd17` (refactor) — deletion refactor (sanctioned exception): suite green both sides (3316/3316), typecheck + lint clean

**Pre-D-12 baseline commit (C10 1440px diff anchor): `1bb649e`** — the orchestrator must screenshot the desktop Overview at 1440px on `1bb649e` and on `aabcd17` (or HEAD) and confirm pixel-identity.

## RED Run Outputs (per task)

**Task 1 (MarketRail):**
```
TestingLibraryElementError: Unable to find an element with the text: Regime · COT · health
Tests  1 failed | 6 passed (7)
```

**Task 2 (MobileMarketSection — new module, import-failure RED; then composition RED):**
```
Test Files  1 failed (1) — Failed to resolve import "./MobileMarketSection.tsx" (module not yet created)
…
Overview.test.tsx: Unable to find [data-testid="mobile-market"]
Tests  1 failed | 87 passed (88)
```

**Task 3:** no RED (deletion refactor, plan-sanctioned exception) — guard = migrated desktop suite green on both sides of the deletion.

## Files Created/Modified
- `apps/web/src/screens/overview-mobile/MobileMarketSection.tsx` — new, UI-SPEC §7 verbatim blocks; imports keyLevelsFor/fmtGammaCompact from the shared model (D-02), Stat/SectionLabel from system, MarketRail verbatim
- `apps/web/src/screens/overview-mobile/MobileMarketSection.test.tsx` — new, 9 tests (J13a-d + AMPLIFY color + null-macro dashes)
- `apps/web/src/screens/MarketRail.tsx` — ONE text node: summary `Market` → `Regime · COT · health`
- `apps/web/src/screens/MarketRail.test.tsx` — +J12a exact-string assertion
- `apps/web/src/screens/overview-mobile/OverviewMobile.tsx` — MobileMarketSection composed as the last section (8 model slices threaded)
- `apps/web/src/screens/Overview.tsx` — D-12 deletions (see accomplishments); imports pruned (PositionCard, ChipRail)
- `apps/web/src/screens/Overview.test.tsx` — J5-complete added; J1 rail assertion updated; Phase-35 dual-render + PillHeader-mobile + bleed assertions replaced with D-12 absence tripwires (85 tests)

## Decisions Made
- `data-testid="mobile-market"` on the section root (spec named none; mobile-* convention)
- gex-unavailable copy rendered as a plain line, not the GexRail Panel (spec §7 wording: "line"); macro + greeks grids still render
- Bleed wrapper div dropped entirely rather than kept classless — honest deletion, zero pixel delta
- Task 2's `0DTE γ` null color uses the full PillHeader ternary (null → text-muted-foreground) — "the PillHeader convention" per plan

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] J1 assertion contradicted the D-08 composition**
- **Found during:** Task 2
- **Issue:** `Overview.test.tsx` J1 asserted `market-rail` does NOT mount in the mobile tree — written when the rail was desktop-only; D-08 block 6 intentionally mounts it inside MobileMarketSection
- **Fix:** J1 now asserts the rail is present but CLOSED (no `open` attribute) in the mobile tree; pill-header/table absence assertions unchanged
- **Files modified:** apps/web/src/screens/Overview.test.tsx
- **Commit:** 1bb649e

**2. [Rule 3 - Blocking] rg no-import gate matched prose**
- **Found during:** Task 2 verify
- **Issue:** `! rg -q 'GammaProfile|GexBars' overview-mobile/` failed on my own doc comment + a test title naming the banned components
- **Fix:** reworded prose ("desktop GEX chart components", "strike-chart mode tab"); no code change
- **Files modified:** MobileMarketSection.tsx, MobileMarketSection.test.tsx
- **Commit:** 1bb649e

## C1–C11: PENDING-ORCHESTRATOR

Executor ran without browser tools. All automated gates are green; the chrome-devtools checklist (UI-SPEC §chrome-devtools manual) is queued for the orchestrator:

| # | Check | Status |
|---|-------|--------|
| C1 | 390x844 first screen = hero + control row + chart | PENDING-ORCHESTRATOR |
| C2 | body.scrollWidth === window.innerWidth everywhere | PENDING-ORCHESTRATOR |
| C3 | chart edge-to-edge, ~300px tall, BE axis legible | PENDING-ORCHESTRATOR |
| C4 | control row one line at 390px AND 320px, controls >=44px | PENDING-ORCHESTRATOR |
| C5 | freshness caption truncates, never wraps (320px) | PENDING-ORCHESTRATOR |
| C6 | cards: focal unreal, tap-expand, checkbox independent, excluded dims | PENDING-ORCHESTRATOR |
| C7 | market section grids legible; NO shrunken charts; `▸ Regime · COT · health` starts closed, opens on tap | PENDING-ORCHESTRATOR |
| C8 | ⋯ dialog thumb-usable, toggles affect chart | PENDING-ORCHESTRATOR |
| C9 | chart tap crosshair responds (log follow-up if flaky) | PENDING-ORCHESTRATOR |
| C10 | 1440px pixel-identical: baseline `1bb649e` vs post-cleanup `aabcd17`, and vs pre-phase | PENDING-ORCHESTRATOR |
| C11 | resize across 1024px swaps trees without crash | PENDING-ORCHESTRATOR |

C12 (user phone check on morai.wtf) = end-of-phase UAT via /gsd-verify-work.

## Desktop Regression Tripwires (automated portion)

- Frozen files zero diff across phase 35.1 (`git diff 60d0f73~1 HEAD --stat`): PayoffControls.tsx, ChipRail.tsx, Button.tsx, GammaProfile.tsx, GexBars.tsx, Analyzer.tsx — clean. PayoffChart.tsx diff = exactly the plan-01 sanctioned D-06 additive props (showBePills/aspectRatio), verified by inspection
- Desktop-stub suite (J2 + migrated tests) green: all-10-chip PillHeader, 3-col grid, table, GexRail 4 panels, legend, freshness chips
- `Analyzer 2.tsx` / analyzer bleed wrapper untouched

## Known Stubs

None — the plan-03 stubs (MobileMarketSection placeholder, doomed desktop card list) are both resolved by this plan.

## Threat Flags

None — no new endpoints, auth paths, file access, or schema surface. T-35.1-07 mitigated (suite green both sides of the deletion; C10 pixel diff queued for orchestrator); T-35.1-08 accepted (zero new dependencies).

## Issues Encountered
None beyond the two auto-fixed deviations above.

## User Setup Required
None.

## Next Phase Readiness
- Phase 35.1 code-complete: all 4 plans executed; mobile Overview = hero → chart/controls → positions → market, desktop path clean
- Remaining phase gate: orchestrator C1–C11 pass, then C12 user phone UAT via /gsd-verify-work

## Self-Check: PASSED

- All 7 created/modified files + SUMMARY — FOUND
- Commits `bb4e436`, `1bb649e`, `aabcd17` — FOUND in git log
