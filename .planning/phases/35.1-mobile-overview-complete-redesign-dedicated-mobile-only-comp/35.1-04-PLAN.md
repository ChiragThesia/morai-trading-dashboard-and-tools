---
phase: 35.1-mobile-overview-complete-redesign
plan: 04
type: execute
wave: 4
depends_on: [35.1-02, 35.1-03]
files_modified:
  - apps/web/src/screens/MarketRail.tsx
  - apps/web/src/screens/MarketRail.test.tsx
  - apps/web/src/screens/overview-mobile/MobileMarketSection.tsx
  - apps/web/src/screens/overview-mobile/MobileMarketSection.test.tsx
  - apps/web/src/screens/overview-mobile/OverviewMobile.tsx
  - apps/web/src/screens/Overview.tsx
  - apps/web/src/screens/Overview.test.tsx
autonomous: true
requirements: [MOBILE-10]
must_haves:
  truths:
    - "Mobile MARKET section = key levels + gamma/greeks/macro stat grids + closed Regime·COT·health disclosure — NO shrunken GammaProfile/GexBars charts (D-08)"
    - "MarketRail summary reads 'Regime · COT · health' and starts closed on mobile via the real open attribute (D-09/D-14)"
    - "Desktop dead branches (pill-header priority row, secondary ChipRail, positions card list, chart bleed wrapper) are removed with 1440px pixel-identity (D-12)"
    - "Full suite + typecheck + lint green; 390x844 and 320px checks pass with no horizontal scroll (C1-C11)"
  artifacts:
    - "apps/web/src/screens/overview-mobile/MobileMarketSection.tsx + test"
    - "MarketRail.tsx summary copy updated (D-14)"
    - "Overview.tsx desktop path cleaned of unreachable lg:hidden branches (D-12)"
  key_links:
    - "MobileMarketSection consumes keyLevelsFor/fmtGammaCompact from the shared model module — never re-implemented (D-02)"
    - "MarketRail reused verbatim inside the mobile market section; open driven by useIsDesktop's real attribute (D-09)"
    - "D-12 cleanup gated by the 1440px before/after screenshot tripwire (C10)"
---

<objective>
Finish the mobile Overview: purpose-built `MobileMarketSection` (key levels + stat
grids + collapsed rail disclosure, D-08), the MarketRail summary copy (D-14), then the
D-12 desktop dead-branch cleanup and the phase integration gate (full suite, tripwires,
chrome-devtools C1-C11).

Purpose: the MARKET section is where "desktop panels shrunk to axis soup" failed
hardest — headline numbers replace the charts on mobile; then delete the Phase 35
responsive branches the dedicated tree made unreachable.
Output: phase-complete mobile Overview + cleaned desktop path, gated.

Closes validation claims: J12, J13, completes J5, J15; runs C1-C11 (C12 = user phone
check, the end-of-phase UAT bar).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/35.1-mobile-overview-complete-redesign-dedicated-mobile-only-comp/35.1-UI-SPEC.md
@.planning/phases/35.1-mobile-overview-complete-redesign-dedicated-mobile-only-comp/35.1-VALIDATION.md
@.planning/phases/35.1-mobile-overview-complete-redesign-dedicated-mobile-only-comp/35.1-CONTEXT.md
@apps/web/src/screens/MarketRail.tsx
@apps/web/src/screens/MarketRail.test.tsx
@apps/web/src/screens/overview-mobile/OverviewMobile.tsx
@apps/web/src/screens/overview-mobile/useOverviewModel.ts
@apps/web/src/screens/Overview.tsx
@apps/web/src/screens/Overview.test.tsx
@.planning/phases/35.1-mobile-overview-complete-redesign-dedicated-mobile-only-comp/35.1-03-SUMMARY.md
</context>

<repo_law>
- TDD red→green per .claude/rules/tdd.md: RED run output shown per task, commit at green
  only. D-12 cleanup is a deletion refactor — guard is the migrated desktop suite +
  the 1440px screenshot tripwire, green on both sides.
- Explicit commit paths only — never `git add -A`.
- No `any`/`as`/`!`; copy strings/classes VERBATIM from 35.1-UI-SPEC.md §7 (market
  section), §8 (cleanup), D-14, §Copywriting Contract.
</repo_law>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MarketRail summary copy (D-14) — J12</name>
  <files>apps/web/src/screens/MarketRail.tsx, apps/web/src/screens/MarketRail.test.tsx</files>
  <behavior>
    Extend MarketRail.test.tsx, RED first:
    - J12a: the summary element's text is `Regime · COT · health` (getByText exact).
    - J12b (already covered — keep green): `market-rail` details lacks the `open`
      attribute under jsdom default; carries it under the desktop matchMedia stub.
  </behavior>
  <action>
    Change ONLY the `<summary>` text node in MarketRail.tsx from `Market` to
    `Regime · COT · health` (D-14 — the rail now sits inside a section already headed
    "MARKET"). The summary keeps `lg:hidden`, so this has zero desktop surface; the
    `open={isDesktop || undefined}` mechanism is untouched (D-09).
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/screens/MarketRail.test.tsx</automated>
  </verify>
  <done>
    J12 green after shown RED. Commit: `feat(35.1-04): MarketRail summary copy — Regime · COT · health (D-14)`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MobileMarketSection + compose (D-08) — J13, J5 complete</name>
  <files>apps/web/src/screens/overview-mobile/MobileMarketSection.tsx, apps/web/src/screens/overview-mobile/MobileMarketSection.test.tsx, apps/web/src/screens/overview-mobile/OverviewMobile.tsx, apps/web/src/screens/Overview.test.tsx</files>
  <behavior>
    MobileMarketSection.test.tsx (new file), RED first. Mock the hooks MarketRail pulls
    (useStatus/useRegimeBoard/usePicker/useMacro/useCot — copy MarketRail.test.tsx's
    vi.hoisted mock pattern) so the section renders standalone; pass a full
    GexSnapshotEntry fixture (copy Overview.test.tsx's GEX_FIXTURE shape).
    - J13a: with gex present → SectionLabel `Market`; `Key levels` block rendering the
      keyLevelsFor rows (Call Wall/γ flip/Spot/Put Wall values from the fixture);
      `Gamma` grid with `net γ /1%` = fmtGammaCompact(netGammaAtSpot) and `0DTE γ`
      (`—` when zeroDte null); `Net book greeks` 2x2 with the railGreeks fixture;
      `Macro` grid VVIX/Fed funds/10y−2y/COT lev with `—` for nulls.
    - J13b: NO desktop chart leakage — `queryByText("Dealer γ profile")` null,
      `queryByText("GEX by strike")` null, `queryByTestId("toggle-gex")` null (GexBars'
      mode tab testid).
    - J13c: `gex === undefined` → the exact copy
      `GEX data unavailable — run fetch-chain to populate.` renders instead of the
      key-levels + gamma blocks (macro/greeks grids per spec §7 note).
    - J13d: the MarketRail `<details>` renders inside the section without the `open`
      attribute (jsdom default = closed, D-09) and its summary reads
      `Regime · COT · health`.
    - In Overview.test.tsx (mobile describe): J5 complete — hero precedes chart precedes
      first card precedes the market section root (compareDocumentPosition chain).
  </behavior>
  <action>
    Create MobileMarketSection per UI-SPEC §7. Props:
    `{ gex, railGreeks, zeroDte, regime, vvix, dff, curveSlope, cotLev }` (types from
    the model). Root `<section className="flex flex-col gap-3 px-4">` (add a
    data-testid, e.g. mobile-market, for the order assertion). Blocks in order:
    1. SectionLabel `Market`.
    2. Key levels: `SectionLabel tone="dim"` `Key levels` + the EXACT desktop rows —
       map `keyLevelsFor(gex)` with the same row classes GexRail uses (copy from
       Overview.tsx GexRail's key-levels block verbatim). When `gex === undefined`,
       render the existing `GEX data unavailable — run fetch-chain to populate.` line
       instead of blocks 2-3.
    3. Gamma: `SectionLabel tone="dim"` `Gamma` + `grid grid-cols-2 gap-2`:
       Stat label `net γ /1%` value `fmtGammaCompact(gex.netGammaAtSpot)` with
       valueClassName `text-down` when regime === "AMPLIFY" else `text-up`; Stat label
       `0DTE γ` value `zeroDte !== null ? fmtGammaCompact(zeroDte) : "—"` sign-colored
       (negative → text-down, else text-up — the PillHeader convention).
    4. Net book greeks: the existing 2x2 Stat grid VERBATIM from GexRail (same labels
       Net Δ/Net Γ/Net Θ/d/Net Vega, same signed/signedUsd/signClass calls) under
       `SectionLabel tone="dim"` `Net book greeks`.
    5. Macro: `SectionLabel tone="dim"` `Macro` + `grid grid-cols-2 gap-2` Stats:
       VVIX (`.toFixed(1)`), Fed funds (`{dff.toFixed(2)}%`), 10y−2y (signed
       `.toFixed(2)` with signClass), COT lev (`signed(cotLev, 0)` with signClass) —
       `—` for nulls, the same formatting the chips used (copy expressions from
       PillHeader).
    6. `<MarketRail />` reused verbatim (its own details/useIsDesktop; false here →
       closed, user-toggled).
    Import keyLevelsFor/fmtGammaCompact from ./useOverviewModel.ts; Stat/SectionLabel
    from ../../components/system/index.tsx. Do NOT import GammaProfile or GexBars.
    Then compose `<MobileMarketSection …modelSlices />` as the last section in
    OverviewMobile (after positions) and add the J5 order assertion.
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/screens/overview-mobile/MobileMarketSection.test.tsx && bunx vitest run apps/web/src/screens/Overview.test.tsx && ! rg -q 'GammaProfile|GexBars' apps/web/src/screens/overview-mobile/</automated>
  </verify>
  <done>
    J13 + J5 green after shown RED; no chart-component import anywhere under
    overview-mobile/. Commit: `feat(35.1-04): MobileMarketSection — key levels + stat grids + closed rail (D-08)`
  </done>
</task>

<!-- planner-discipline-allow: GammaProfile -->
<!-- planner-discipline-allow: GexBars -->

<task type="auto">
  <name>Task 3: D-12 desktop dead-branch cleanup + phase integration gate</name>
  <files>apps/web/src/screens/Overview.tsx, apps/web/src/screens/Overview.test.tsx</files>
  <action>
    Deletion refactor per UI-SPEC §8 — every removed branch is `display:none` at every
    width the desktop tree now mounts (>=1024px), so 1440px must be pixel-identical.
    BEFORE editing: capture the 1440px baseline screenshot (chrome-devtools MCP against
    the local dev server, standing permission) of the Overview at desktop width.
    Remove from Overview.tsx:
    1. PillHeader: the `pill-header-priority` row and the secondary mobile rail of
       MetricChips below it. PillHeader keeps ONLY the full chip row — which then drops
       its `hidden lg:flex ...` prefixes to a plain flex-wrap row, and the wrapper's
       `static lg:sticky lg:top-0 lg:z-10` collapses to `sticky top-0 z-10` (the
       component now only ever mounts at >=1024px). Keep `lg:backdrop-blur` semantics by
       making it unconditional `backdrop-blur`.
    2. PositionsTable: the `positions-card-list` block and the PositionCard import; the
       table drops its `hidden ... lg:table` pairing to a plain `w-full border-collapse
       font-mono text-[11px] tabular-nums table` class list. The verdictObservedAt
       card-list threading from plan 03 goes with it.
    3. The payoff-chart bleed wrapper: remove its `-mx-3 lg:mx-0` classes (keep or drop
       the div per whichever leaves the smaller diff; if kept, it carries no layout
       classes).
    4. Remove imports Overview.tsx no longer uses after 1-3 (PositionCard, and the
       mobile-rail chip primitives if now unreferenced — MetricChip stays if the full
       row still uses it, which it does).
    5. Update Overview.test.tsx: delete/adjust any Phase-35-era assertions that
       referenced the removed testids (`pill-header-priority`, `positions-card-list`,
       `payoff-chart-bleed`) — these DOM branches no longer exist by design (D-12).
       No other assertion may be weakened.
    Then run the phase integration gate (see verify) including the AFTER screenshot at
    1440px and a pixel comparison against the baseline (C10).
  </action>
  <verify>
    <automated>bun run test && bun run typecheck && bun run lint && bunx vitest run apps/web/src/screens/Overview.test.tsx</automated>
    <human-check>
      chrome-devtools pass (agent-run per standing permission, then user):
      C1 390x844: first screen = hero + one control row + chart, chart visible without
      scroll. C2: document.body.scrollWidth === window.innerWidth everywhere. C3: chart
      SVG edge-to-edge, ~300px tall, BE axis numbers legible. C4: control row one line at
      390px AND 320px, all controls >=44px tall. C5: freshness caption truncates, never
      wraps (320px). C6: cards — unreal focal, tap-expand, checkbox independent,
      excluded dims. C7: market section — grids legible, NO shrunken Dealer-γ/GEX-by-
      strike charts, `▸ Regime · COT · health` starts closed, opens on tap. C8: ⋯ dialog
      thumb-usable, toggles visibly affect the chart. C9: chart tap crosshair responds
      (log follow-up if flaky — internals frozen). C10: 1440px screenshot pixel-identical
      to the pre-cleanup baseline AND to the pre-phase baseline. C11: resize across
      1024px swaps trees without crash (state reset accepted, D-02).
      C12 (end-of-phase UAT, /gsd-verify-work): user phone check on morai.wtf — reads as
      a designed mobile app screen. The only bar that matters.
    </human-check>
  </verify>
  <done>
    Full suite + typecheck + lint green (J15); C10 pixel-identity holds; C1-C11 recorded
    in the SUMMARY with screenshots. Commit: `refactor(35.1-04): remove Phase-35 responsive dead branches from desktop Overview (D-12)`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Presentation composition + deletion of unreachable branches; no new input parsing, no new network surface, zero new dependencies |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35.1-07 | DoS | D-12 deletion accidentally removing a branch the desktop tree still reaches | low | mitigate | Migrated desktop suite (J2 + all stubbed tests) green after deletion + C10 1440px pixel-identity before/after |
| T-35.1-08 | Tampering | Supply chain (npm installs) | low | accept | Zero new dependencies — no install tasks exist |
</threat_model>

<verification>
- `bun run test` + typecheck + lint green (J15 phase gate).
- Desktop tripwires (35.1-UI-SPEC §Desktop Regression Tripwires) all pass: desktop-stub
  render shows all-10-chip PillHeader, 3-column grid, expanded MarketRail, full table,
  PayoffControls ChipRail, legend, BE pills, freshness chips, GexRail 4 panels;
  PayoffControls.tsx/ChipRail.tsx/Button.tsx/chart internals have zero diff this phase
  (`git diff --stat` per file).
- Rollback note: Task 3 is a pure deletion commit — `git revert` restores the Phase 35
  branches wholesale; Tasks 1-2 revert independently (mobile-only surface).
</verification>

<success_criteria>
- J12, J13, J5, J15 green; C1-C11 executed and recorded; C12 queued for user UAT.
- Zero ChipRail/MetricChip/GammaProfile/GexBars usage in the mobile tree (D-04/D-08).
- Overview.tsx contains no `lg:hidden` mobile arms in the desktop path (D-12).
</success_criteria>

<output>
Create `.planning/phases/35.1-mobile-overview-complete-redesign-dedicated-mobile-only-comp/35.1-04-SUMMARY.md` when done (include RED outputs + C1-C11 evidence).
</output>
