---
phase: 35.1-mobile-overview-complete-redesign
plan: 03
subsystem: ui
tags: [react, vitest, mobile, tdd, positions]

# Dependency graph
requires:
  - phase: 35.1-mobile-overview-complete-redesign
    provides: useOverviewModel + Overview switch (plan 01), MobileHero + MobileRiskPanel composed into OverviewMobile (plan 02)
provides:
  - PositionCard re-hierarchy (D-07) — focal 16px sign-colored unreal in row 1, one muted meta line (expiry · DTE · net val) in row 2, bg-raise/30 + ring-line card surface, expand = greeks grid ALWAYS + VerdictDetailBody when a verdict exists, new verdictObservedAt prop
  - OverviewMobile positions section (D-11) — heading row (SectionLabel + LiveStatusBadge + Exit rules ▸ Dialog), card list off the model's rows, mono footer total via resolveLivePositionRow, exitsBody five state branches (copy verbatim)
affects: [35.1-04]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Footer/chart/card inclusion off ONE lifted excluded set + resolveLivePositionRow — a single checkbox click provably moves all three (T-35.1-05 mitigation test)"
    - "exitsBody view duplication sanctioned (D-02): identical copy + testids in both trees, state from the shared model's exits slice"

key-files:
  created: []
  modified:
    - apps/web/src/components/PositionCard.tsx
    - apps/web/src/components/PositionCard.test.tsx
    - apps/web/src/screens/Overview.tsx
    - apps/web/src/screens/overview-mobile/OverviewMobile.tsx
    - apps/web/src/screens/Overview.test.tsx

key-decisions:
  - "Footer element carries data-testid=mobile-positions-footer (spec named no testid; matches the mobile-hero/mobile-payoff convention) so J11 asserts the exact string"
  - "exitsBody wrapped in a mt-3 div inside the positions section — the desktop parent used gap-3; branch JSX/copy/testids stay byte-identical"
  - "Expanded card wrapper split: outer mt-2 border-t pt-2, inner grid grid-cols-4 — so VerdictDetailBody sits below the greeks grid inside one bordered expand block (UI-SPEC §5)"

patterns-established:
  - "Card focal number: font-mono text-base font-bold tabular-nums + signClass, — with text-dim on null — the D-07 hierarchy for any future mobile card"

requirements-completed: [MOBILE-09]

coverage:
  - id: J10
    description: "PositionCard hierarchy — focal unreal + sign class, meta line, bg-raise/30 surface, expand greeks+VerdictDetailBody / greeks-only, checkbox/aria-expanded/IV n/a/VerdictChip re-asserted"
    requirement: MOBILE-09
    verification:
      - kind: unit
        ref: "apps/web/src/components/PositionCard.test.tsx (13 tests)"
        status: pass
    human_judgment: false
  - id: J11
    description: "Mobile positions section — heading row, card list (expand un-gated, catch #23), footer n/m included reacting to checkbox + chart signature, exact footer format, empty state, exit error/cold-start/unlinked branches, J5 chart→cards order, no <table>"
    requirement: MOBILE-09
    verification:
      - kind: unit
        ref: "apps/web/src/screens/Overview.test.tsx#Overview branch — D-01/D-10 (35.1) (87/87 file)"
        status: pass
      - kind: integration
        ref: "bun run test (3308 tests) + bun run typecheck + bun run lint"
        status: pass
    human_judgment: false

# Metrics
duration: 8min
completed: 2026-07-11
status: complete
---

# Phase 35.1 Plan 03: Positions Section Summary

**Mobile position cards now answer "which position, what verdict, how much" in one glance — focal sign-colored unreal P&L right of the label+verdict, one muted meta line, tap-expand greeks + verdict detail — over a card list with a live footer total (`Net · unreal · n/m included`) and the desktop's exit-advisor state branches, all off the one lifted inclusion state the chart uses**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-07-11T17:48:44Z
- **Completed:** 2026-07-11T17:56:27Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- `PositionCard` (D-07): row 1 = label + IV-n/a badge + VerdictChip left, unreal right in `font-mono text-base font-bold tabular-nums` + `signClass` (`—` `text-dim` on null); row 2 = `{expiry.line1} · {expiry.line2} · {usd(netVal)}` muted mono truncate; card surface `bg-raise/30 ring-1 ring-line`; old two-Stat Net val/Unreal row removed; expand (still verdict-UN-gated, catch #23) renders the 4-col greeks grid plus `VerdictDetailBody` in `mt-2` when a verdict exists (new `verdictObservedAt` prop feeds its as-of dot)
- Overview.tsx touched on exactly ONE line — threading `verdictObservedAt` into the (display:none, plan-04-doomed) desktop card-list call site for typecheck — verified via `git diff` before commit
- OverviewMobile positions section (D-11): heading row (SectionLabel `Positions` + `LiveStatusBadge {...liveBadgeProps}` + the desktop-classed `Exit rules ▸` DialogTrigger, snapshot-gated), card list mapping the model's `rows` with `expanded={selectedRowKey === r.key}`, footer `Net {usd} · {signed unreal} · {n}/{m} included` computed via `resolveLivePositionRow` over included legs (the desktop Net-row fn, memoized), and the five exitsBody branches with verbatim copy + testids
- T-35.1-05 mitigation proven by test: ONE checkbox click moves the footer count, dims the card, and moves the chart's `positionSetSignature` — no second inclusion mechanism

## Task Commits

1. **Task 1: PositionCard re-hierarchy + verdictObservedAt (D-07) — J10** — `ef1fa04` (feat) — RED: 6 failed / 7 passed shown, then 13/13 + Overview 78/78 + typecheck
2. **Task 2: Mobile positions section (D-11) — J11** — `a62b03a` (feat) — RED: 9 failed / 78 passed shown, then 87/87 file, 3308 workspace, typecheck + lint clean

## RED Run Outputs (per task)

**Task 1 (PositionCard):**
```
FAIL … TestingLibraryElementError: Unable to find an element with the text: +$XXX /
Unable to find [data-testid=held-position-rule-cal-hold] / class assertions on bg-raise/30
Tests  6 failed | 7 passed (13)
```

**Task 2 (positions section):**
```
FAIL … 9 tests — Unable to find [data-testid="mobile-positions-footer"] /
"Positions" heading / position-card-* in the mobile tree
Tests  9 failed | 78 passed (87)
```

## Files Created/Modified
- `apps/web/src/components/PositionCard.tsx` — D-07 re-hierarchy, UI-SPEC §5 verbatim classes; +verdictObservedAt prop; VerdictDetailBody import
- `apps/web/src/components/PositionCard.test.tsx` — migrated to the D-07 contract (13 tests; J10a-e, every pre-existing behavior re-covered)
- `apps/web/src/screens/Overview.tsx` — ONE line: verdictObservedAt threaded at the desktop card-list call site
- `apps/web/src/screens/overview-mobile/OverviewMobile.tsx` — positions section per UI-SPEC §6 (heading/list/footer/exitsBody)
- `apps/web/src/screens/Overview.test.tsx` — 9 new tests in the 35.1 mobile describe (J11, J5 extension, exit branches, empty state, no-table)

## Decisions Made
- Footer gets `data-testid="mobile-positions-footer"` (spec named none; follows the mobile-* testid convention) so the exact-format assertion is stable
- exitsBody branches sit in a `mt-3` wrapper inside the section (desktop spacing came from parent `gap-3`); branch markup/copy/testids byte-identical to Overview.tsx's
- Expanded block split into outer border wrapper + inner grid so VerdictDetailBody renders inside the same bordered expand region below the greeks (UI-SPEC §5 reading)

## Deviations from Plan

None - plan executed exactly as written.

## Known Stubs

| Stub | File | Reason |
|---|---|---|
| MobileMarketSection placeholder comment | `apps/web/src/screens/overview-mobile/OverviewMobile.tsx` | Intentional per plan slicing — lands in plan 04 |
| Desktop PositionsTable card list (display:none) | `apps/web/src/screens/Overview.tsx` | Unreachable-visible at every desktop width; deleted in plan 04 (D-12) |

## Threat Flags

None — no new endpoints, auth paths, file access, or schema surface. T-35.1-05 (footer/chart drift) mitigated per plan; T-35.1-06 accepted (zero new dependencies).

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Plan 04 (MobileMarketSection + D-12 desktop dead-branch cleanup) unblocked: the positions section composes below MobileRiskPanel; the desktop card list + its lg:hidden pairing are now the only PositionCard desktop remnants to delete
- J5 order hero→chart→cards asserted; market section slots below the positions section

## Self-Check: PASSED

- All 5 modified files + SUMMARY — FOUND
- Commits `ef1fa04`, `a62b03a` — FOUND in git log

---
*Phase: 35.1-mobile-overview-complete-redesign*
*Completed: 2026-07-11*
