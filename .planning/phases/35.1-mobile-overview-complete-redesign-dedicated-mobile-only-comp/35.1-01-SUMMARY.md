---
phase: 35.1-mobile-overview-complete-redesign
plan: 01
subsystem: ui
tags: [react, vitest, matchMedia, useSyncExternalStore, recharts, mobile]

# Dependency graph
requires:
  - phase: 35-mobile-redesign
    provides: useIsDesktop hook (matchMedia 1024px, jsdom-safe false), PositionCard, matchMedia stub pattern (MarketRail.test.tsx)
provides:
  - useOverviewModel() — the shared Overview model hook (ALL state/derivation, single useLiveStream consumer)
  - Overview root switch — useIsDesktop() ? OverviewDesktop : OverviewMobile (only ONE tree mounts)
  - OverviewMobile skeleton root (overview-mobile-root) for plans 02-04
  - PayoffChart additive props showBePills / aspectRatio with byte-identical defaults
  - Overview.test.tsx desktop describes migrated to stubDesktopMatchMedia() (D-10 byte-identity guard)
affects: [35.1-02, 35.1-03, 35.1-04]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Shared model hook: view trees (desktop/mobile) both consume useOverviewModel(); view code may duplicate, logic never (D-02)"
    - "Root-level viewport branch via useIsDesktop — exactly one tree mounts (D-01)"
    - "Additive chart props default to today's exact behavior (undefined-sentinel-free style computed above return)"

key-files:
  created:
    - apps/web/src/screens/overview-mobile/useOverviewModel.ts
    - apps/web/src/screens/overview-mobile/OverviewMobile.tsx
  modified:
    - apps/web/src/screens/Overview.tsx
    - apps/web/src/screens/Overview.test.tsx
    - apps/web/src/components/charts/PayoffChart.tsx
    - apps/web/src/components/charts/PayoffChart.test.tsx

key-decisions:
  - "OverviewDesktop destructures the model to the pre-extraction local names so the desktop JSX is byte-identical (zero JSX churn beyond excludedFromT0.count → excludedFromT0Count)"
  - "aspectRatio default renders the literal `${SVG_W} / ${SVG_H}` template string ('1000 / 470'), never String(SVG_W/SVG_H) — byte-identical default proven by the pre-existing container-sizing test"
  - "exits slice exposes dataIsUndefined (raw exitsData === undefined) so the loading branch stays distinct from the settled cold-start (snapshot === null)"
  - "Pure-function describes (formatExpiryCell, buildCalendarPosition) not stubbed — they render nothing, so they assert no desktop-tree output"

patterns-established:
  - "stubDesktopMatchMedia(): one shared helper per test file, installed in beforeEach + Reflect.deleteProperty(window, 'matchMedia') in afterEach"

requirements-completed: [MOBILE-07]

coverage:
  - id: D1
    description: "PayoffChart showBePills/aspectRatio additive props — defaults byte-identical (J8/J9)"
    requirement: MOBILE-07
    verification:
      - kind: unit
        ref: "apps/web/src/components/charts/PayoffChart.test.tsx#PayoffChart — D-06 additive mobile props (35.1)"
        status: pass
    human_judgment: false
  - id: D2
    description: "useOverviewModel extraction — behavior-preserving (Overview.test.tsx 70/70 with ZERO test edits)"
    requirement: MOBILE-07
    verification:
      - kind: unit
        ref: "apps/web/src/screens/Overview.test.tsx (pre-existing suite, unmodified at Task 2 commit 67abe50)"
        status: pass
    human_judgment: false
  - id: D3
    description: "Overview useIsDesktop switch + mobile skeleton root + desktop-test matchMedia migration (J1/J2/J14/J15)"
    requirement: MOBILE-07
    verification:
      - kind: unit
        ref: "apps/web/src/screens/Overview.test.tsx#Overview branch — D-01/D-10 (35.1)"
        status: pass
      - kind: integration
        ref: "bun run test (296 files / 3273 tests) + bun run typecheck + bun run lint"
        status: pass
    human_judgment: false

# Metrics
duration: 17min
completed: 2026-07-11
status: complete
---

# Phase 35.1 Plan 01: Structural Foundation Summary

**Overview now branches at the root via useIsDesktop into OverviewDesktop (byte-identical) or the OverviewMobile skeleton, with all state extracted into a shared useOverviewModel hook and PayoffChart gaining showBePills/aspectRatio props whose defaults render exactly today's output**

## Performance

- **Duration:** ~17 min
- **Started:** 2026-07-11T17:11:46Z
- **Completed:** 2026-07-11T17:28:30Z
- **Tasks:** 3
- **Files modified:** 6 (2 created)

## Accomplishments
- `useOverviewModel()` owns ALL Overview state/derivation (data hooks, single useLiveStream consumer, verdict join, calendar builds, scenario/domain, toggles, date control, hover, freshness, railGreeks) — plus new mobile slices `regime`/`zeroDte`/`macroValues` as one-line shared-lib calls
- `Overview` is the thin UI-SPEC §1 switch; `OverviewDesktop` is the old body renamed in-file with JSX untouched; `OverviewMobile` skeleton renders `overview-mobile-root` and calls the model so the single-live-stream-consumer invariant holds from day one
- PayoffChart `showBePills` (gates ONLY the be-pills row; axis BE labels untouched) and `aspectRatio` (default stays the literal `1000 / 470` string) — Analyzer + desktop Overview call sites pass neither prop (asserted)
- Every pre-existing Overview desktop describe migrated to `stubDesktopMatchMedia()` in the SAME commit as the switch — zero assertions weakened or deleted

## Task Commits

1. **Task 1: PayoffChart additive props (D-06)** — `d4ea74f` (feat) — RED: 2 assertion failures shown, then 53/53
2. **Task 2: Extract useOverviewModel (D-02)** — `67abe50` (refactor) — guard: Overview.test.tsx 70/70 with zero test edits, typecheck clean
3. **Task 3: useIsDesktop switch + test migration (D-01/D-10)** — `9cbed5c` (feat) — RED: J1 assertion failure shown, then 73/73 file / 3273 workspace green, typecheck + lint clean

## RED Run Outputs (per task)

**Task 1 (D-06 props):**
```
FAIL … J8 off: showBePills={false} … expect(screen.queryByTestId("be-pills")).toBeNull()
FAIL … J9 override: AssertionError: expected '1000 / 470' to be '1.3'
Tests  2 failed | 51 passed (53)
```

**Task 3 (branch):**
```
FAIL … J1 (root): TestingLibraryElementError: Unable to find an element by: [data-testid="overview-mobile-root"]
Tests  1 failed | 2 passed | 70 skipped (73)
```
(J2/J14 pass pre-switch by design — they are the byte-identity guards.)

## Files Created/Modified
- `apps/web/src/screens/overview-mobile/useOverviewModel.ts` — shared model hook + exported helpers (`buildCalendarPosition`, `formatExpiryCell`, `buildRows`, `keyLevelsFor`, `fmtGammaCompact`, `latestMacroValue`, `netGreeksForLegs`, `NetGreeks`)
- `apps/web/src/screens/overview-mobile/OverviewMobile.tsx` — mobile tree root skeleton (sections land in plans 02-04)
- `apps/web/src/screens/Overview.tsx` — thin switch + OverviewDesktop (renamed in-file); keeps PillHeader/PositionsTable/GexRail view components; re-exports `formatExpiryCell`/`buildCalendarPosition`
- `apps/web/src/screens/Overview.test.tsx` — `stubDesktopMatchMedia()` helper + migration of 9 render-based describes; new `Overview branch — D-01/D-10 (35.1)` describe (J1/J2/J14)
- `apps/web/src/components/charts/PayoffChart.tsx` — two additive props, defaults byte-identical; chart internals frozen
- `apps/web/src/components/charts/PayoffChart.test.tsx` — new `D-06 additive mobile props (35.1)` describe (4 tests)

## Decisions Made
- Destructured the model to the pre-extraction local names inside OverviewDesktop so the JSX diff is one identifier (`excludedFromT0.count` → `excludedFromT0Count`) — byte-identity by construction
- `liveBadgeProps` bundles the LiveStatusBadge inputs under the badge's own prop names; OverviewDesktop re-destructures to the old locals
- Pure-function describes (`formatExpiryCell (OVW-03)`, `buildCalendarPosition (34-05)`) were not stubbed — they never render, so they assert no desktop-tree output (the plan's qualifier "describes that assert desktop-tree output" excludes them)

## Deviations from Plan

None - plan executed exactly as written.

## Known Stubs

| Stub | File | Reason |
|---|---|---|
| Empty `overview-mobile-root` div | `apps/web/src/screens/overview-mobile/OverviewMobile.tsx` | Intentional per plan — the skeleton lands the branch seam; MobileHero/MobileRiskPanel/positions/market sections land in plans 02-04 |

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Plans 02-04 can wire model slices by name (`regime`, `zeroDte`, `macroValues`, `exits`, `freshness`, `hover`, etc.) into MobileHero / MobileRiskPanel / positions / MobileMarketSection
- Desktop ≥1024px provably unchanged: J2 structural guard + full pre-existing suite under the stub
- MOBILE-07 completes with plan 04 (D-12 desktop cleanup); this plan delivered its structural half

## Self-Check: PASSED

- `apps/web/src/screens/overview-mobile/useOverviewModel.ts` — FOUND
- `apps/web/src/screens/overview-mobile/OverviewMobile.tsx` — FOUND
- Commits `d4ea74f`, `67abe50`, `9cbed5c` — FOUND in git log

---
*Phase: 35.1-mobile-overview-complete-redesign*
*Completed: 2026-07-11*
