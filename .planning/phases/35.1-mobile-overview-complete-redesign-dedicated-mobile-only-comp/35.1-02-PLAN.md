---
phase: 35.1-mobile-overview-complete-redesign
plan: 02
type: execute
wave: 2
depends_on: [35.1-01]
files_modified:
  - apps/web/src/screens/overview-mobile/MobileHero.tsx
  - apps/web/src/screens/overview-mobile/MobileHero.test.tsx
  - apps/web/src/screens/overview-mobile/MobileRiskPanel.tsx
  - apps/web/src/screens/overview-mobile/MobileRiskPanel.test.tsx
  - apps/web/src/screens/overview-mobile/OverviewMobile.tsx
  - apps/web/src/screens/Overview.test.tsx
autonomous: true
requirements: [MOBILE-08]
must_haves:
  truths:
    - "Mobile first screen leads with BOOK P&L as a 32px sign-colored mono number with SPX/VIX/regime context inline (D-03)"
    - "Exactly ONE control row sits between hero and chart; Fan/Walls/Profit-zone + Today live behind the ⋯ Dialog (D-05)"
    - "The mobile chart mounts full-bleed with showBePills=false, aspectRatio=1.3, highlightedPositionId=null (D-06/D-13)"
    - "Freshness reduces to one 9px caption line below the chart with a single worst-of dot (D-05)"
    - "No ChipRail/MetricChip mounts anywhere in the mobile tree (D-04)"
  artifacts:
    - "apps/web/src/screens/overview-mobile/MobileHero.tsx + test"
    - "apps/web/src/screens/overview-mobile/MobileRiskPanel.tsx + test"
    - "OverviewMobile.tsx composing hero + risk panel from the model"
  key_links:
    - "MobileRiskPanel toggles/dateControl are the SAME model state the desktop uses — no second toggle store (D-05)"
    - "MobileHero.bookPnl = bookUnrealizedPnl via the model — no new P&L path (D-03)"
    - "PayoffChart internals untouched — mobile differences enter only via the D-06 props"
---

<objective>
Build the mobile first screen: `MobileHero` (BOOK P&L focal number + context line, D-03)
and `MobileRiskPanel` (one-line control row, `⋯` overflow Dialog, full-bleed PayoffChart,
single freshness caption — D-05/D-06/D-09/D-13), composed into the OverviewMobile root
from plan 01.

Purpose: this is the screen that failed the user phone check twice — hero-first, chart
with one row of chrome, per the approved UI-SPEC.
Output: mobile hero + risk panel live below 1024px; desktop untouched.

Closes validation claims: J3, J4, J6, J7, and completes J1 (mobile-hero present by
default) + the hero→chart half of J5.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/35.1-mobile-overview-complete-redesign-dedicated-mobile-only-comp/35.1-UI-SPEC.md
@.planning/phases/35.1-mobile-overview-complete-redesign-dedicated-mobile-only-comp/35.1-VALIDATION.md
@apps/web/src/screens/overview-mobile/useOverviewModel.ts
@apps/web/src/screens/overview-mobile/OverviewMobile.tsx
@apps/web/src/screens/Overview.test.tsx
@apps/web/src/components/charts/PayoffControls.tsx
@apps/web/src/components/system/Button.tsx
@.planning/phases/35.1-mobile-overview-complete-redesign-dedicated-mobile-only-comp/35.1-01-SUMMARY.md
</context>

<repo_law>
- TDD red→green per .claude/rules/tdd.md: RED run output shown per task, commit at green
  only (test + impl in one conventional commit).
- Explicit commit paths only — never `git add -A`.
- No `any`/`as`/`!`; no IIFE in JSX; compute values above the return.
- Copy strings, class lists, and props VERBATIM from 35.1-UI-SPEC.md §2 (MobileHero),
  §3 (MobileRiskPanel), §Copywriting Contract, §Typography, §Spacing Scale. The spec's
  JSX sketches are the contract — do not restyle or paraphrase.
</repo_law>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MobileHero (D-03) — J3/J4</name>
  <files>apps/web/src/screens/overview-mobile/MobileHero.tsx, apps/web/src/screens/overview-mobile/MobileHero.test.tsx</files>
  <behavior>
    MobileHero.test.tsx (new file), RED first — pure presentational component, no mocks
    needed beyond props:
    - J3a: `bookPnl={1204} hasPositions` → `mobile-hero-value` text is `signedUsd(1204, 0)`
      output ("+$1,204") and className contains `text-up`.
    - J3b: negative bookPnl → className contains `text-down`.
    - J3c: `hasPositions={false}` → value renders `—`, className contains `text-txt` and
      neither sign class.
    - J4a: `spot={6842.1} vix={14.32} regime="DAMPEN"` → context line contains
      `SPX 6842.1`, `VIX 14.32`, and a `γ DAMPEN` segment with `text-up` (AMPLIFY → `text-down`).
    - J4b: `spot={null}` / `vix={null}` → their segments render `—`; `regime={null}` →
      the regime segment (including its `·` separator) is absent entirely.
  </behavior>
  <action>
    Create MobileHero per UI-SPEC §2 — props
    `{ bookPnl: number; hasPositions: boolean; spot: number | null; vix: number | null; regime: GexRegime | null }`
    and the spec's JSX verbatim: `<section data-testid="mobile-hero" className="px-4 pt-4">`,
    label span with the exact tracked-label classes, value div
    `data-testid="mobile-hero-value"` with `mt-1 font-mono text-[32px] font-bold tabular-nums leading-none`
    + `hasPositions ? signClass(bookPnl) : "text-txt"`, value
    `hasPositions ? signedUsd(bookPnl, 0) : "—"`, context line
    `mt-1.5 font-mono text-[11px] text-muted-foreground tabular-nums` with SPX value in
    `text-blue`, `·` separators in `text-dim`, VIX plain, and the regime segment
    `className={regime === "AMPLIFY" ? "text-down" : "text-up"}` rendered only when
    `regime !== null`. Label copy: `Book P&L` (uppercased by the class). Formatting:
    `spot.toFixed(1)`, `vix.toFixed(2)`, `—` fallbacks per the Copywriting Contract.
    Import GexRegime type from ../../lib/gex-regime.ts, signedUsd/signClass from
    ../../lib/position-format.ts, cn from @/lib/utils. No loading spinner (D-03 states).
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/screens/overview-mobile/MobileHero.test.tsx</automated>
  </verify>
  <done>
    All J3/J4 assertions green after a shown RED run.
    Commit: `feat(35.1-02): MobileHero — hero-first BOOK P&L (D-03)`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MobileRiskPanel — control row, ⋯ Dialog, full-bleed chart, freshness caption (D-05/D-06/D-09/D-13) — J6/J7</name>
  <files>apps/web/src/screens/overview-mobile/MobileRiskPanel.tsx, apps/web/src/screens/overview-mobile/MobileRiskPanel.test.tsx</files>
  <behavior>
    MobileRiskPanel.test.tsx (new file), RED first. The component is fully controlled —
    tests pass a props fixture (scenario with a small curve, payoffDomain, spot, gex null
    or a walls object, toggles, vi.fn handlers, dateControl fixture, bounds, freshness
    fixture). Render the real PayoffChart (jsdom-safe; existing chart tests prove this).
    - J6a: clicking aria-label "Previous day" calls `stepDate(-1)`; "Next day" calls
      `stepDate(1)`.
    - J6b: when `dateControl.dateInputValue !== bounds.minIso` the date input className
      contains `ring-1 ring-violet`; when equal it does not (projected-state signal,
      UI-SPEC Open Question 2).
    - J6c: the `@ exp` button has `aria-pressed` mirroring `toggles.showExpiration` and
      click calls `onToggle("showExpiration")`.
    - J7a: clicking the `⋯` button (aria-label "More chart options") opens the Dialog
      containing the heading `Chart` and buttons `Today`, `Fan`, `Walls`, `Profit zone`.
    - J7b: `Today` is disabled when `dateInputValue === bounds.minIso`; when projected,
      clicking it calls `resetDate`.
    - J7c: clicking `Fan` calls `onToggle("showFan")`; `Walls` → "showWalls";
      `Profit zone` → "showProfitZone"; each carries aria-pressed from `toggles`.
    - D-06/D-13 wiring: the mounted chart section `mobile-payoff` exists; freshness
      caption `mobile-freshness` renders one line with the dot span classed `bg-up` when
      BOTH gexFresh and markFresh, else `bg-amber`.
  </behavior>
  <action>
    Create MobileRiskPanel per UI-SPEC §3. Props: the chart slice of the model —
    `scenario`, `payoffDomain`, `spot`, `gex` (GexSnapshotEntry | undefined), `toggles`,
    `onToggle`, `dateControl` (PayoffDateControl), `bounds`, `positionSetSignature`,
    `excludedFromT0Count`, `freshness` ({ gexFresh, gexAsOf, gexAgeMs, markFresh,
    markAsOf, markAgeMs }). Structure, classes and copy verbatim from the spec:
    - Control row `flex items-center gap-2 px-4`: left cluster `flex items-center gap-1`
      with Button size="touch" `‹` (aria-label `Previous day`), native date input
      (min-h-11, SAME classes as PayoffControls' input — copy them — PLUS
      `ring-1 ring-violet` only when `dateControl.dateInputValue !== bounds.minIso`;
      bounds.minIso IS today's ISO by the usePayoffDateControl/computeProjectionBounds
      contract, which keeps the comparison dependency-free), Button size="touch" `›`
      (aria-label `Next day`). Right cluster `ml-auto flex items-center gap-2`: Button
      size="touch" variant="toggle" active={toggles.showExpiration} aria-pressed label
      `@ exp` calling onToggle("showExpiration"), then Button size="touch" `⋯`
      (aria-label `More chart options`) as DialogTrigger.
    - `⋯` Dialog (shadcn Dialog, D-09 portal state): `DialogContent className="max-w-xs"`,
      DialogTitle rendered as SectionLabel-styled `Chart` (a11y: keep a DialogTitle so
      the dialog is labelled), then `flex flex-col gap-2` of full-width size="touch"
      buttons: `Today` (variant="secondary", disabled when already today, calls
      dateControl.resetDate) then Fan / Walls / Profit zone as variant="toggle" with
      active/aria-pressed from `toggles` calling onToggle with showFan/showWalls/
      showProfitZone. Same state object as desktop — no local toggle state.
    - Chart: `<div data-testid="mobile-payoff" className="mt-2 w-full">` containing
      PayoffChart with the spec's exact prop list: todayCurve/expirationCurve from
      scenario, fanCurves=[], rollCurve=null, gex mapped to {callWall,putWall,flip} (null
      when undefined — same mapping as desktop), domain, spot, toggles, fitY=false,
      onFitYConsumed=noop (module-level or useCallback noop), positionSetSignature,
      baseExpirationCurve=scenario.expirationCurve, excludedFromT0Count,
      highlightedPositionId={null} (D-13 — no highlight props at all beyond this),
      showBePills={false} and aspectRatio={1.3} (D-06). The section owns px-0 (the row
      and caption own px-4) so the SVG spans edge to edge.
    - Freshness caption verbatim from §3: `data-testid="mobile-freshness"`,
      `flex items-center gap-1.5 px-4 mt-1.5 font-mono text-[9px] text-dim`, dot span
      `size-1.5 shrink-0 rounded-full` + `bg-up` iff gexFresh && markFresh else
      `bg-amber`, text span `truncate` with
      `GEX {gexAsOf} · {relAge(gexAgeMs)} · mark {markAsOf} · {relAge(markAgeMs)}` where
      each relAge segment renders only when its age is non-null (desktop's guards).
      relAge imports from ../Market.tsx.
    - PayoffControls.tsx is UNTOUCHED (desktop Overview + Analyzer keep it).
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/screens/overview-mobile/MobileRiskPanel.test.tsx && ! rg -q 'ChipRail|MetricChip' apps/web/src/screens/overview-mobile/MobileRiskPanel.tsx</automated>
  </verify>
  <done>
    J6/J7 unit assertions green after shown RED; no ChipRail/MetricChip import in the
    mobile tree (D-04). Commit: `feat(35.1-02): MobileRiskPanel — one-row chart chrome + ⋯ dialog (D-05/D-06/D-13)`
  </done>
</task>

<!-- planner-discipline-allow: ChipRail -->
<!-- planner-discipline-allow: MetricChip -->

<task type="auto" tdd="true">
  <name>Task 3: Compose hero + risk panel into OverviewMobile (J1 complete, J5 partial)</name>
  <files>apps/web/src/screens/overview-mobile/OverviewMobile.tsx, apps/web/src/screens/Overview.test.tsx</files>
  <behavior>
    Extend the plan-01 "Overview branch" mobile describe in Overview.test.tsx, RED first
    (default jsdom render = mobile tree; the file's existing hook mocks apply):
    - J1 complete: `getByTestId("mobile-hero")` present by default; `pill-header` still
      absent.
    - J5 (partial): `mobile-hero` precedes `mobile-payoff` in DOM order via
      `compareDocumentPosition` (DOCUMENT_POSITION_FOLLOWING).
    - Hero wiring: with the CAL_FRONT/CAL_BACK positions fixture, `mobile-hero-value`
      shows the signedUsd of `bookUnrealizedPnl` over the fixture (compute expected via
      the imported lib fn — no hardcoded magic value).
    - J7 shared-state proof: open `⋯`, click `Fan` → `latestPayoffChartProps().toggles.showFan`
      is true (the SAME toggles object drives the chart — no second store).
    - J6 integration: click "Next day" → `latestPayoffChartProps().domain`/curves
      re-render (assert the PayoffChart mock was called again with the input value
      advanced one day — assert via the date input's value attribute changing).
  </behavior>
  <action>
    Wire OverviewMobile per UI-SPEC §Screen Composition items 2-3: inside the plan-01
    root render `<MobileHero bookPnl={m.bookPnl} hasPositions={positions.length > 0} spot={...} vix={...} regime={...} />`
    (spot from gex — pass null when gex undefined, per the Copywriting Contract's
    per-segment `—` rule; vix from macroValues; regime from the model) followed by
    `<MobileRiskPanel …chartSlice />` passing the model slices from plan 01's hook.
    Keep the root classes from plan 01. No other sections yet (positions = plan 03,
    market = plan 04).
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/screens/Overview.test.tsx && bun run test && bun run typecheck && bun run lint</automated>
  </verify>
  <done>
    Mobile describe green after shown RED; full suite green (J15 per-wave gate); desktop
    guard test (J2) still green — this plan never touched Overview.tsx or
    PayoffChart.tsx. Commit: `feat(35.1-02): compose MobileHero + MobileRiskPanel into OverviewMobile`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | New presentational components over already-rendered data; no new input parsing, no new network surface, zero new dependencies |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35.1-03 | Information Disclosure | MobileHero P&L on a phone lock-screen-adjacent surface | low | accept | Same book P&L the desktop chip already renders; auth gate (existing) unchanged |
| T-35.1-04 | Tampering | Supply chain (npm installs) | low | accept | Zero new dependencies — no install tasks exist |
</threat_model>

<verification>
- `bun run test` + typecheck + lint green.
- Desktop tripwire: J2 structural guard green (files this plan owns are all
  mobile-tree-only; Overview.tsx, PayoffChart.tsx, PayoffControls.tsx have zero diff —
  verify with `git diff --stat` showing only the 6 files_modified).
- Rollback note: all-new files except OverviewMobile.tsx composition + test extension;
  reverting the plan's commits restores the plan-01 skeleton with no desktop surface.
</verification>

<success_criteria>
- J1, J3, J4, J6, J7 green; J5 hero→chart ordering asserted.
- Acceptance geometry inputs in place for the phase gate: hero, ONE control row, chart,
  caption — nothing else between Shell header and positions.
- No ChipRail/MetricChip/PayoffControls mounted in the mobile tree (D-04/D-05).
</success_criteria>

<output>
Create `.planning/phases/35.1-mobile-overview-complete-redesign-dedicated-mobile-only-comp/35.1-02-SUMMARY.md` when done (include RED outputs).
</output>
