---
phase: 35.1-mobile-overview-complete-redesign
plan: 02
subsystem: ui
tags: [react, vitest, mobile, recharts, dialog, shadcn]

# Dependency graph
requires:
  - phase: 35.1-mobile-overview-complete-redesign
    provides: useOverviewModel (plan 01), Overview useIsDesktop switch, OverviewMobile skeleton root, PayoffChart showBePills/aspectRatio props
provides:
  - MobileHero — 32px sign-colored BOOK P&L hero + SPX/VIX/γ regime context line (D-03)
  - MobileRiskPanel — ONE control row (‹ date › + @ exp + ⋯ Dialog), full-bleed PayoffChart (showBePills=false, aspectRatio=1.3, highlightedPositionId=null), single worst-of freshness caption (D-05/D-06/D-09/D-13)
  - OverviewMobile composition — hero-first mobile root wired to the shared model (J1 complete, J5 hero→chart half)
affects: [35.1-03, 35.1-04]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Overflow controls behind a shadcn Dialog (portal state, catch #24 compliant) driving the SAME lifted toggle state the desktop uses — no second store"
    - "buttonClass() on DialogTrigger when the trigger can't route through <Button> directly"
    - "Worst-of freshness dot: one caption line, bg-up iff BOTH channels fresh else bg-amber"

key-files:
  created:
    - apps/web/src/screens/overview-mobile/MobileHero.tsx
    - apps/web/src/screens/overview-mobile/MobileHero.test.tsx
    - apps/web/src/screens/overview-mobile/MobileRiskPanel.tsx
    - apps/web/src/screens/overview-mobile/MobileRiskPanel.test.tsx
  modified:
    - apps/web/src/screens/overview-mobile/OverviewMobile.tsx
    - apps/web/src/screens/Overview.test.tsx

key-decisions:
  - "MobileRiskPanel types scenario as the payoffCurve/expirationCurve slice (structural Pick) — the model's full ScenarioResult passes; tests stay a 3-point fixture"
  - "Hero spot = gex?.spot ?? null, NOT the model's 5800 pricing fallback — the Copywriting Contract's per-segment — rule beats the engine default"
  - "Date input keeps PayoffControls' data-testid=date-picker-input — only one tree ever mounts, so desktop/mobile tests never collide on it"
  - "gex fixture in MobileRiskPanel tests is undefined (walls mapping is the same desktop expression); wall-present rendering is covered by PayoffChart's own suite"

patterns-established:
  - "Projected-date signal: ring-1 ring-violet on the native date input iff dateInputValue !== bounds.minIso (bounds.minIso IS today's ISO by contract)"

requirements-completed: [MOBILE-08]

coverage:
  - id: D1
    description: "MobileHero — sign-colored hero value, context segments, — fallbacks, regime omission (J3/J4)"
    requirement: MOBILE-08
    verification:
      - kind: unit
        ref: "apps/web/src/screens/overview-mobile/MobileHero.test.tsx (7 tests)"
        status: pass
    human_judgment: false
  - id: D2
    description: "MobileRiskPanel — control row handlers, projected ring, ⋯ Dialog contents/disabled/toggle wiring, chart section, worst-of caption (J6/J7)"
    requirement: MOBILE-08
    verification:
      - kind: unit
        ref: "apps/web/src/screens/overview-mobile/MobileRiskPanel.test.tsx (8 tests)"
        status: pass
      - kind: static
        ref: "! rg -q 'ChipRail|MetricChip' MobileRiskPanel.tsx (D-04 gate)"
        status: pass
    human_judgment: false
  - id: D3
    description: "OverviewMobile composition — J1 complete, J5 hero→chart order, hero P&L via bookUnrealizedPnl, J7 shared-state proof, J6 date-step integration"
    requirement: MOBILE-08
    verification:
      - kind: unit
        ref: "apps/web/src/screens/Overview.test.tsx#Overview branch — D-01/D-10 (35.1) (78/78 file)"
        status: pass
      - kind: integration
        ref: "bun run test (3293 tests) + bun run typecheck + bun run lint"
        status: pass
    human_judgment: false

# Metrics
duration: 14min
completed: 2026-07-11
status: complete
---

# Phase 35.1 Plan 02: Mobile Hero + Risk Panel Summary

**Mobile first screen now leads with a 32px sign-colored BOOK P&L hero over a full-bleed payoff chart with exactly one control row — Fan/Walls/Profit-zone/Today behind a ⋯ Dialog driving the same toggle state the desktop uses — and a single worst-of freshness caption**

## Performance

- **Duration:** ~14 min
- **Started:** 2026-07-11T17:33:28Z
- **Completed:** 2026-07-11T17:47:00Z
- **Tasks:** 3
- **Files modified:** 6 (4 created)

## Accomplishments
- `MobileHero` (D-03): `signedUsd(bookPnl, 0)` in 32px mono bold with `signClass` sign color (`—` uncolored when no positions); context line `SPX · VIX · γ {regime}` with per-segment `—` fallbacks and the regime segment (plus its separator) omitted entirely when gex is absent
- `MobileRiskPanel` (D-05/D-06/D-09/D-13): ONE control row — `‹ [date] ›` (violet ring iff projected) + right-aligned `@ exp` toggle + `⋯` opening a shadcn Dialog with Today (disabled at today) / Fan / Walls / Profit zone, all calling the shared `handleToggle`/`resetDate`; full-bleed PayoffChart mounted with `showBePills={false}`, `aspectRatio={1.3}`, `highlightedPositionId={null}`; single 9px worst-of freshness caption below
- `OverviewMobile` composes both from the plan-01 model — J1 complete (mobile-hero by default, pill-header absent), J5 hero→chart DOM order proven, hero P&L = `bookUnrealizedPnl` through the model (no second P&L path), toggle clicks in the Dialog provably reach the chart's props (no second store)
- No ChipRail/MetricChip anywhere in the mobile tree (D-04, grep-gated); PayoffControls.tsx, Overview.tsx, PayoffChart.tsx untouched (J2 desktop guard green)

## Task Commits

1. **Task 1: MobileHero (D-03) — J3/J4** — `0bea532` (feat) — RED: 7 assertion failures shown, then 7/7
2. **Task 2: MobileRiskPanel (D-05/D-06/D-09/D-13) — J6/J7** — `c8f145c` (feat) — RED: 8 assertion failures shown, then 8/8 + D-04 grep gate
3. **Task 3: Compose into OverviewMobile (J1/J5/J6/J7)** — `e6a36c6` (feat) — RED: 5 new tests failed / 73 passed, then 78/78 file, 3293 workspace, typecheck + lint clean

## RED Run Outputs (per task)

**Task 1 (MobileHero):**
```
FAIL … 7 tests — TestingLibraryElementError: Unable to find [data-testid="mobile-hero-value"] /
AssertionError: expected [] to have a length of 1 but got +0
Tests  7 failed (7)
```

**Task 2 (MobileRiskPanel):**
```
FAIL … 8 tests — TestingLibraryElementError against the stub section
Tests  8 failed (8)
```

**Task 3 (composition):**
```
FAIL … J1/J5/hero-wiring/J7/J6 — Unable to find [data-testid="mobile-hero"] / [data-testid="date-picker-input"]
Tests  5 failed | 73 passed (78)
```

## Files Created/Modified
- `apps/web/src/screens/overview-mobile/MobileHero.tsx` — hero block, UI-SPEC §2 verbatim classes/copy
- `apps/web/src/screens/overview-mobile/MobileHero.test.tsx` — J3a-c, J4a-b (7 tests)
- `apps/web/src/screens/overview-mobile/MobileRiskPanel.tsx` — control row + ⋯ Dialog + full-bleed chart + freshness caption, UI-SPEC §3 verbatim
- `apps/web/src/screens/overview-mobile/MobileRiskPanel.test.tsx` — J6a-c, J7a-c + freshness dot (8 tests, real PayoffChart rendered)
- `apps/web/src/screens/overview-mobile/OverviewMobile.tsx` — hero-first composition off the shared model
- `apps/web/src/screens/Overview.test.tsx` — 5 new tests in the `Overview branch — D-01/D-10 (35.1)` describe

## Decisions Made
- Scenario prop typed as the two-curve slice (structural typing) so tests pass a 3-point fixture while the model's full `ScenarioResult` flows through unchanged
- Hero spot reads `gex?.spot ?? null` — never the model's 5800 engine fallback (a fabricated "SPX 5800.0" hero would violate the Copywriting Contract's honesty rule)
- Mobile date input reuses `data-testid="date-picker-input"` (desktop parity); safe because D-01 guarantees only one tree mounts per render

## Deviations from Plan

None - plan executed exactly as written.

## Known Stubs

| Stub | File | Reason |
|---|---|---|
| Positions + market sections placeholder comment | `apps/web/src/screens/overview-mobile/OverviewMobile.tsx` | Intentional per plan slicing — positions cards land in plan 03, MobileMarketSection in plan 04 |

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Plan 03 (positions cards) composes below `<MobileRiskPanel />` in OverviewMobile; model slices (`rows`, `excluded`, `selectedRowKey`, `verdictByRowKey`, `exits`, `liveBadgeProps`) are already returned by the hook
- Plan 04 (market section + D-12 desktop cleanup) unblocked; acceptance geometry inputs (hero → ONE row → chart → caption) are in place for the phase gate

## Self-Check: PASSED

- All 5 mobile-tree files — FOUND
- Commits `0bea532`, `c8f145c`, `e6a36c6` — FOUND in git log

---
*Phase: 35.1-mobile-overview-complete-redesign*
*Completed: 2026-07-11*
