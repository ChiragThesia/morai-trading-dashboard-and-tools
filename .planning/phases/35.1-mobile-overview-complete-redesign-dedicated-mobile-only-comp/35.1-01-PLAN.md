---
phase: 35.1-mobile-overview-complete-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/charts/PayoffChart.tsx
  - apps/web/src/components/charts/PayoffChart.test.tsx
  - apps/web/src/screens/Overview.tsx
  - apps/web/src/screens/Overview.test.tsx
  - apps/web/src/screens/overview-mobile/useOverviewModel.ts
  - apps/web/src/screens/overview-mobile/OverviewMobile.tsx
autonomous: true
requirements: [MOBILE-07]
must_haves:
  truths:
    - "At <1024px (jsdom default) Overview renders the mobile tree root; the desktop pill-header/table/GEX rail do not mount"
    - "At >=1024px (matchMedia stub) Overview renders today's desktop tree with identical structure — table, pill-header, MarketRail, GexRail all present"
    - "PayoffChart with no new props renders exactly as today (be-pills present, aspect-ratio 1000/470) — Analyzer and desktop Overview unaffected"
    - "Every pre-existing Overview desktop test passes under the matchMedia stub — the byte-identity guard"
  artifacts:
    - "apps/web/src/screens/overview-mobile/useOverviewModel.ts — shared model hook + exported helpers"
    - "apps/web/src/screens/overview-mobile/OverviewMobile.tsx — mobile tree root (skeleton this plan)"
    - "apps/web/src/screens/Overview.tsx — thin useIsDesktop switch + OverviewDesktop (renamed in-file)"
  key_links:
    - "Overview() -> useIsDesktop() -> OverviewDesktop | OverviewMobile (D-01: only ONE tree mounts, one useLiveStream consumer)"
    - "Both trees -> useOverviewModel() (D-02: logic never duplicated)"
    - "PayoffChart showBePills/aspectRatio default to today's exact behavior (D-06)"
---

<objective>
Land the structural foundation for the dedicated mobile Overview tree: extract all
Overview state/derivation into `useOverviewModel()`, turn `Overview` into a
`useIsDesktop()` switch (D-01/D-02), add the two additive PayoffChart props (D-06),
and migrate the existing Overview desktop tests to the matchMedia stub in the same
change that lands the switch (D-10). This is the byte-identity-guard plan — desktop
regression protection lands WITH the switch, not after.

Purpose: everything in plans 02-04 hangs off this branch + model. Desktop >=1024px must
be provably unchanged before any mobile UI is built.
Output: switch + model hook + skeleton mobile root + chart props, full suite green.

Closes validation claims: J1 (partial — root-level branch; mobile-hero lands in plan 02),
J2, J8, J9, J14, J15.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/35.1-mobile-overview-complete-redesign-dedicated-mobile-only-comp/35.1-CONTEXT.md
@.planning/phases/35.1-mobile-overview-complete-redesign-dedicated-mobile-only-comp/35.1-UI-SPEC.md
@.planning/phases/35.1-mobile-overview-complete-redesign-dedicated-mobile-only-comp/35.1-VALIDATION.md
@apps/web/src/screens/Overview.tsx
@apps/web/src/screens/Overview.test.tsx
@apps/web/src/components/charts/PayoffChart.tsx
@apps/web/src/components/charts/PayoffChart.test.tsx
@apps/web/src/hooks/useIsDesktop.ts
@apps/web/src/screens/MarketRail.test.tsx
</context>

<repo_law>
- TDD red→green per .claude/rules/tdd.md: write the failing test, RUN it, show the
  failure output (fails on the assertion, not an import error), then implement, RUN
  green, show output. Commit only at green — test + implementation land together in a
  single conventional commit so the red→green pairing is visible in the diff.
- Commit with EXPLICIT paths only (the repo has unrelated dirty files). Never `git add -A`
  or `git add .`.
- No `any`, no `as` (except `as const`), no `!`. Match existing file style exactly.
- Copy strings/classes/props VERBATIM from 35.1-UI-SPEC.md — never paraphrase a design
  decision. Cite the D-ID in code comments where the spec does.
</repo_law>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PayoffChart additive props — showBePills + aspectRatio (D-06)</name>
  <files>apps/web/src/components/charts/PayoffChart.tsx, apps/web/src/components/charts/PayoffChart.test.tsx</files>
  <behavior>
    New describe "PayoffChart — D-06 additive mobile props (35.1)" in PayoffChart.test.tsx,
    written RED first (use the file's existing curve fixtures that produce zero crossings):
    - Test 1 (J8 default): rendering with neither new prop → `screen.getByTestId("be-pills")`
      present (crossings exist in fixture) — locks the default.
    - Test 2 (J8 off): `showBePills={false}` → `queryByTestId("be-pills")` is null AND
      `be-axis-label-t0` / `be-axis-label-exp` testids STILL render (BE-on-axis labels are
      unconditional internals; only the pill row above the plot is gated).
    - Test 3 (J9 default): the ChartContainer root (`container.querySelector('[data-slot="chart"]')`)
      has inline style aspect-ratio equal to today's `1000 / 470` string.
    - Test 4 (J9 override): `aspectRatio={1.3}` → that element's style aspect-ratio is "1.3".
  </behavior>
  <action>
    Add two optional props to `PayoffChartProps` per UI-SPEC §4, defaults preserving
    today's behavior exactly so desktop Overview + Analyzer call sites need zero edits:
    - `showBePills?: boolean` defaulting to `true`. When false, skip rendering the
      breakeven pill row block (the one carrying `data-testid="be-pills"`). Nothing else
      is gated by it — the axis BE labels (rendered by PayoffChartGrid) and the in-chart
      BE marker bars are untouched.
    - `aspectRatio?: number` defaulting to `SVG_W / SVG_H` (the existing module
      constants). Feed it into the existing ChartContainer style as
      `aspectRatio: String(aspectRatio)`-equivalent (today the style is the literal
      template `${SVG_W} / ${SVG_H}` — keep the DEFAULT rendering that exact string so
      the default is byte-identical; when the prop is passed, render the numeric value).
      Implementation note: default the prop to `undefined` sentinel-free by making the
      style expression `aspectRatio === undefined ? `${SVG_W} / ${SVG_H}` : String(aspectRatio)`
      computed above the return — no IIFE in JSX, no behavioral drift at defaults.
    Nothing else in the file changes: z-order, marks, grid, tooltip, scales, y-domain
    lock all frozen (CONTEXT lock: chart internals untouched).
    Do NOT touch apps/web/src/screens/Analyzer.tsx or PayoffControls.tsx.
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/components/charts/PayoffChart.test.tsx && bunx vitest run apps/web/src/components/charts/PayoffChart.resize.test.tsx && ! rg -q 'showBePills|aspectRatio' apps/web/src/screens/Analyzer.tsx</automated>
  </verify>
  <done>
    All 4 new tests green after a shown RED run; every pre-existing PayoffChart test green
    unmodified; Analyzer.tsx contains neither prop name (J14 half — the Overview half is
    asserted in Task 3's desktop-stub test). Commit: `feat(35.1-01): PayoffChart showBePills + aspectRatio additive props`
  </done>
</task>

<!-- planner-discipline-allow: showBePills -->
<!-- planner-discipline-allow: aspectRatio -->

<task type="auto">
  <name>Task 2: Extract useOverviewModel — behavior-preserving refactor (D-02)</name>
  <files>apps/web/src/screens/overview-mobile/useOverviewModel.ts, apps/web/src/screens/Overview.tsx</files>
  <action>
    Pure refactor — no RED step; the guard is the existing 1,400-line Overview.test.tsx
    passing UNMODIFIED (per .claude/rules/tdd.md, refactors run with green on both sides).

    Create `apps/web/src/screens/overview-mobile/useOverviewModel.ts`:
    - Move from Overview.tsx into this file: the module helpers `resolveLeg`,
      `legExpiryKey`, `buildCalendarPosition`, `formatExpiryCell`, `formatExpiryDate`,
      `buildRows`, `keyLevelsFor`, `fmtGammaCompact`, `latestMacroValue`,
      `netGreeksForLegs`, and the constants `DEFAULT_IV` / `LIVE_MARK_FRESH_MS` and the
      `NetGreeks` type. Export `buildCalendarPosition`, `formatExpiryCell`, `buildRows`,
      `keyLevelsFor`, `fmtGammaCompact`, `latestMacroValue`, `netGreeksForLegs` (mobile
      tree + desktop both consume; never re-implemented — D-02).
    - Export `useOverviewModel()` containing ALL non-trivial state/derivation currently
      inline in `Overview()`: data hooks (usePositions/useGex/useCot/useMacro/useExits),
      useLiveStream (single consumer — only one tree mounts, D-01), verdict join maps
      (verdictByRowKey, rowLabels, unlinkedVerdicts), calendars + calendarBuild +
      calendarPositions + ivNaByRowKey, excludedCalendars state + toggle handler, today +
      bounds + dateControl, toggles state + handleToggle, payoffDomain, scenario,
      positionSetSignature, noop, railGreeks, bookPnl, cotLev, hover/selected row state +
      handlers + highlightedScenario, excludedFromT0, all freshness derivation (gexTs/
      gexAgeMs/gexFresh/gexAsOf, markAgeMs/markFresh/markAsOf), and — new, for the mobile
      tree — `regime` (classifyRegime over gex.netGammaAtSpot, null when gex undefined),
      `zeroDte` (zeroDteGex), and `macroValues: { vix, vvix, dff, curveSlope }` via
      latestMacroValue (these are one-line calls to existing shared lib fns, so no logic
      duplication; desktop PillHeader keeps its own identical internal derivation
      untouched — byte-identity wins). Return shape per UI-SPEC §1 (names indicative —
      keep them close to the spec sketch so plans 02-04 can wire slices by name). Also
      expose `exitsBodyInputs`: `{ snapshot, isPending, isError, refetch }` plus the raw
      `exitsData`-undefined flag the loading branch needs. Follow existing typing style:
      explicit return type interface, readonly fields, no any/as/!.
    - Move the relevant imports; Overview.tsx keeps `relAge`/`GEX_FRESH_MS` usage via the
      model's freshness slice (GEX_FRESH_MS import moves to the model file).

    Rework `Overview.tsx`:
    - `Overview()` body starts with `const m = useOverviewModel();` and the JSX consumes
      model fields. JSX output must be IDENTICAL — same elements, classes, testids, order.
      PillHeader / PositionsTable / GexRail component definitions stay in Overview.tsx
      untouched (they are desktop view code); they now receive values from the model where
      Overview passed locals before.
    - Re-export moved symbols so existing test imports keep resolving (the
      EDGE_ARROW_LANE_Y precedent in PayoffChart.tsx): Overview.tsx keeps
      `export { formatExpiryCell, buildCalendarPosition } from "./overview-mobile/useOverviewModel.ts";`
      (Overview.test.tsx imports both from "./Overview.tsx").
    - Do NOT add the useIsDesktop branch yet — this task is extraction only; Overview
      still renders the (desktop) tree unconditionally so the existing suite passes
      without any matchMedia stub.
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/screens/Overview.test.tsx && bun run typecheck && bunx vitest run apps/web/src/screens/Market.test.tsx</automated>
  </verify>
  <done>
    Overview.test.tsx passes with ZERO test-file edits (behavior-preservation proof);
    typecheck clean. Commit: `refactor(35.1-01): extract useOverviewModel shared model hook (D-02)`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Overview useIsDesktop switch + migrate desktop tests to matchMedia stub (D-01/D-10)</name>
  <files>apps/web/src/screens/Overview.tsx, apps/web/src/screens/overview-mobile/OverviewMobile.tsx, apps/web/src/screens/Overview.test.tsx</files>
  <behavior>
    New describe "Overview branch — D-01/D-10 (35.1)" in Overview.test.tsx, RED first:
    - Test 1 (J1, root level): default jsdom render (NO matchMedia stub) →
      `getByTestId("overview-mobile-root")` present; `queryByTestId("pill-header")`,
      `queryByRole("table")`, `queryByTestId("market-rail")` all null. (mobile-hero
      arrives in plan 02 — this plan guards the branch at root level.)
    - Test 2 (J2 byte-identity guard): with the desktop matchMedia stub (copy the exact
      Object.defineProperty pattern from MarketRail.test.tsx's desktop describe,
      matches === `query === "(min-width: 1024px)"`, afterEach
      `Reflect.deleteProperty(window, "matchMedia")`) → `pill-header` present,
      `overview-mobile-root` absent, positions `<table>` renders (with the POS fixture),
      `overview-center-column` + `overview-gex-column` + `market-rail` present, and
      GexRail structural content present ("Key levels", "Net book greeks" headings).
    - Test 3 (J14 Overview half): under the desktop stub, `latestPayoffChartProps()`
      reports both new chart props undefined — the desktop call site passes neither.
  </behavior>
  <action>
    GREEN, all in ONE commit with the test migration (a split commit would leave the
    suite red — forbidden):
    1. Create `apps/web/src/screens/overview-mobile/OverviewMobile.tsx` — skeleton:
       calls `useOverviewModel()` and renders the mobile root per UI-SPEC §Screen
       Composition: `<div data-testid="overview-mobile-root" className="flex flex-col gap-6 pb-10">`
       with a body comment noting sections land in plans 02-04. No horizontal padding on
       the root (sections own px-4 later). It must reference the model result (e.g.
       renders nothing visible yet beyond the root) so the single-live-stream-consumer
       invariant holds from day one.
    2. Overview.tsx: rename today's component body to `function OverviewDesktop()`
       (module-private, same file, JSX untouched), and make the exported `Overview` the
       thin switch VERBATIM from UI-SPEC §1: `useIsDesktop()` → `<OverviewDesktop />`
       : `<OverviewMobile />`. Import useIsDesktop from ../hooks/useIsDesktop.ts and
       OverviewMobile from ./overview-mobile/OverviewMobile.tsx.
    3. Migrate the existing Overview.test.tsx desktop tests to the stub (D-10, expected
       churn not regression): add ONE shared helper (e.g. `stubDesktopMatchMedia()`)
       using the MarketRail.test.tsx pattern; install it in `beforeEach` of every
       pre-existing describe block that asserts desktop-tree output (table rows,
       pill-header chips, GEX rail, PayoffControls, verdict column, live-cell classes,
       date-picker/toggle chips — i.e. all pre-existing describes in this file), with
       matching afterEach cleanup. Do not weaken or delete any pre-existing assertion —
       only wrap them in the stubbed environment.
  </action>
  <verify>
    <automated>bunx vitest run apps/web/src/screens/Overview.test.tsx && bun run test && bun run typecheck && bun run lint</automated>
  </verify>
  <done>
    RED output shown for the 3 new tests, then the FULL workspace suite green (J15),
    typecheck + lint clean. Every pre-existing Overview desktop assertion passes under
    the stub. Commit: `feat(35.1-01): Overview useIsDesktop switch + desktop-test matchMedia migration (D-01/D-10)`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Presentation-only refactor + branch; no new input parsing, no new network surface, zero new dependencies |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35.1-01 | DoS | Overview branch mounting BOTH trees (double EventSource/live-stream consumer) | low | mitigate | D-01 single-branch switch (one tree mounts); J1/J2 assert the other tree is absent from the DOM |
| T-35.1-02 | Tampering | Supply chain (npm installs) | low | accept | Zero new dependencies this phase (locked constraint) — no install tasks exist |
</threat_model>

<verification>
- `bun run test` full workspace green (J15), `bun run typecheck`, `bun run lint` clean.
- Desktop tripwire (this plan touches Overview.tsx + PayoffChart.tsx): the J2 structural
  guard test passes, and `bunx vitest run apps/web/src/screens/Analyzer.test.tsx` green
  (Analyzer call site untouched by the chart props).
- Rollback note: all three commits are additive/refactor on 6 files; `git revert` of the
  Task 3 commit alone restores the unconditional desktop render (the switch is a
  single-file seam) — no data or migration surface.
</verification>

<success_criteria>
- J1 (root), J2, J8, J9, J14, J15 demonstrably green via the commands above.
- Overview.tsx exports the same public symbols as before (Overview, formatExpiryCell,
  buildCalendarPosition) — no external import breaks.
- useOverviewModel is the ONLY place Overview state/derivation lives; OverviewDesktop and
  OverviewMobile both call it.
</success_criteria>

<output>
Create `.planning/phases/35.1-mobile-overview-complete-redesign-dedicated-mobile-only-comp/35.1-01-SUMMARY.md` when done (include the RED run outputs per task).
</output>
