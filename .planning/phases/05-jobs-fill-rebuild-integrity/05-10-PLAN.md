---
phase: 05-jobs-fill-rebuild-integrity
plan: 10
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/core/src/brokerage/application/refreshToken.ts
  - packages/core/src/brokerage/application/refreshToken.test.ts
  - packages/adapters/src/postgres/repos/job-runs.ts
  - packages/adapters/src/postgres/repos/job-runs.contract.test.ts
  - packages/adapters/src/memory/job-queue.ts
  - packages/adapters/src/memory/job-queue.test.ts
  - packages/contracts/src/jobs.ts
  - packages/contracts/src/jobs.test.ts
  - apps/server/src/adapters/http/jobs.routes.ts
  - apps/server/src/adapters/http/jobs.routes.test.ts
  - apps/worker/src/main.ts
autonomous: true
gap_closure: true
requirements: [JOB-01, MCP-02]
must_haves:
  truths:
    - "Transient OAuth network/parse errors return a retryable error, not auth-expired"
    - "readJobRuns reports lastSuccessAt and lastErrorAt independently per job"
    - "Memory job-queue twin returns ok(null) on a dedup hit (mirrors pg-boss singletonKey)"
    - "A rebuild-journal trigger without calendarId is rejected 400 at the boundary, not enqueued"
  artifacts:
    - path: "packages/core/src/brokerage/application/refreshToken.ts"
      provides: "CR-02 transient vs terminal OAuth error mapping"
      contains: "invalid_grant"
    - path: "packages/adapters/src/postgres/repos/job-runs.ts"
      provides: "CR-03 independent last-success / last-error per job"
      contains: "FILTER"
    - path: "packages/contracts/src/jobs.ts"
      provides: "WR-04 rebuild-journal ⇒ calendarId required refinement"
      contains: "rebuild-journal"
  key_links:
    - from: "apps/server/src/adapters/http/jobs.routes.ts"
      to: "packages/contracts/src/jobs.ts"
      via: "triggerJobPayload refinement"
      pattern: "triggerJobPayload"
---

<objective>
Close the four criticals/warnings that are independent of the fills data path: CR-02 (token
refresh error mapping), CR-03 (job-runs independent success/error), WR-04/C4 (rebuild requires
calendarId at the boundary), WR-05/C5 (memory twin dedup return), plus the C6 info cleanups.

This plan shares ZERO files with 05-09, so both run in Wave 1 in parallel.

Purpose: a transient 04:00 ET DNS blip must not present as "re-auth required" (CR-02); the
status surface must be able to show "last succeeded at X but now failing" (CR-03); an empty-body
rebuild trigger must not flood the queue (WR-04); the in-memory twin must match production
dedup semantics (WR-05).
Output: corrected error mapping, corrected SQL, a boundary refinement, and an honest twin.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-jobs-fill-rebuild-integrity/05-GAPS.md
@.planning/phases/05-jobs-fill-rebuild-integrity/05-REVIEW.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/tdd.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-02 token refresh error mapping + CR-03 independent job-runs — RED→GREEN</name>
  <read_first>
    - packages/core/src/brokerage/application/refreshToken.ts (lines 94-114 — the collapse to auth-expired)
    - packages/core/src/brokerage/application/refreshToken.test.ts (existing cases)
    - packages/adapters/src/postgres/repos/job-runs.ts (full — DISTINCT ON query)
    - packages/adapters/src/postgres/repos/orphan-fills.contract.test.ts (testcontainer harness pattern: inject("dbUrl"), skipIf, TRUNCATE)
  </read_first>
  <behavior>
    - refreshToken: when OAuth error code is `invalid_grant` or `invalid_client` → err({kind:"auth-expired", appId}).
      When code is `network` or `parse` → err({kind:"storage-error", message:`${appId}: ${code}`})
      (retryable; pg-boss retries; status does NOT claim expiry). An unexpected throw → storage-error.
    - job-runs: for a job with one completed run at T1 then a failed run at T2 (T2>T1),
      readJobRuns returns lastSuccessAt=T1 AND lastErrorAt=T2 AND lastError=<failed output message>.
      For a job with only completed runs, lastErrorAt=null, lastError=null. Empty/absent pgboss schema → ok({}).
  </behavior>
  <action>
    RED: add a refreshToken test asserting `network` and `parse` codes map to storage-error
    (NOT auth-expired) while `invalid_grant`/`invalid_client` still map to auth-expired. Add a
    job-runs CONTRACT test (`job-runs.contract.test.ts`, testcontainers) that seeds pgboss.job
    with a completed-then-failed sequence for one job and asserts both timestamps populate
    independently. Run; confirm RED.
    GREEN: in `refreshToken.ts` replace the catch-all `auth-expired` at the network/parse branch
    (line ~107-108) with the storage-error mapping; keep the terminal branch unchanged. In
    `job-runs.ts` replace the single `DISTINCT ON (name)` query with two aggregates per name:
    `MAX(completed_on) FILTER (WHERE state='completed')` and
    `MAX(completed_on) FILTER (WHERE state='failed')`, plus a correlated subselect (or a
    DISTINCT ON failed-only subquery joined on name) for the failed run's output → lastError.
    Preserve the Pitfall-6 empty-schema guard (return ok({}) when pgboss missing). Commit at green.
  </action>
  <verify>
    <automated>bunx vitest run refreshToken && cd packages/adapters && bunx vitest run job-runs.contract</automated>
  </verify>
  <acceptance_criteria>
    - refreshToken.test asserts network/parse → storage-error and invalid_grant/invalid_client → auth-expired; all green.
    - `rg -c "FILTER" packages/adapters/src/postgres/repos/job-runs.ts` ≥ 2.
    - job-runs.contract.test (testcontainers) proves independent lastSuccessAt + lastErrorAt for one job; green (skips cleanly if Docker absent).
    - No `as`/`!`/`any` introduced; `bun run lint` exits 0.
  </acceptance_criteria>
  <done>CR-02 + CR-03 closed: transient refresh failures retry; status can show last-success and last-error together.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-05 twin dedup return + WR-04 rebuild calendarId boundary + C6 info cleanups — RED→GREEN</name>
  <read_first>
    - packages/adapters/src/memory/job-queue.ts (lines 44-52 — returns existing.jobId on dedup hit)
    - packages/adapters/src/pgboss/job-queue.ts (lines 30-47 — returns ok(null) on collision)
    - packages/contracts/src/jobs.ts (full — triggerJobPayload optional calendarId)
    - apps/server/src/adapters/http/jobs.routes.ts (full — route validation)
    - packages/core/src/journal/application/enqueueJob.ts (buildDedupeKey null-key branch)
    - apps/worker/src/main.ts (lines 165-169 — void pgBossJobQueue dead wiring; IN-01)
  </read_first>
  <behavior>
    - memory job-queue: a second enqueue with the SAME non-null dedupeKey returns ok(null)
      (NOT ok(existingJobId)). First enqueue returns ok(jobId:string). Null dedupeKey → always ok(jobId).
    - contracts: triggerJobPayload refined so `name === "rebuild-journal"` ⇒ calendarId required.
      A rebuild trigger with no calendarId fails parse (drives 400). sync-fills/refresh-tokens/
      compute-bsm-greeks remain calendarId-optional.
    - jobs.routes: invalid param/body returns 400 (zValidator default); a rebuild-journal POST
      with empty body returns 400 and never calls enqueueJob.
    - C6: remove the dead `void pgBossJobQueue` binding in worker main.ts (IN-01) — either wire
      it or delete the construction; do not leave a void-suppressed unused binding.
  </behavior>
  <action>
    RED: add a memory job-queue test asserting ok(null) on dedup hit. Add a contracts test
    (`jobs.test.ts`) asserting rebuild-journal without calendarId fails and with calendarId
    passes; other jobs pass without it. Add a jobs.routes test asserting 400 + enqueueJob NOT
    called for a rebuild trigger lacking calendarId. Run; confirm RED.
    GREEN: in `memory/job-queue.ts` return `ok(null)` on the dedup-hit branch (mirror pg-boss).
    In `contracts/src/jobs.ts` add a `.superRefine`/`.refine` on `triggerJobPayload` (or a
    discriminated refinement keyed on the route name) enforcing rebuild-journal ⇒ calendarId;
    keep `TriggerJobPayload`/`triggerJobResponse` exports stable. If the refinement needs the
    job name, thread `name` into the validated shape at the route boundary in `jobs.routes.ts`
    so the refinement can run (route still returns 400 on failure). Remove the dead
    `void pgBossJobQueue` (IN-01). Commit at green.
  </action>
  <verify>
    <automated>bunx vitest run job-queue jobs && bunx vitest run jobs.routes && rg -q "void pgBossJobQueue" apps/worker/src/main.ts; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - memory job-queue twin returns ok(null) on dedup hit (test asserts `value === null`).
    - contracts test: rebuild-journal w/o calendarId → parse failure; w/ calendarId → success; other jobs unaffected.
    - jobs.routes test: rebuild trigger w/o calendarId → 400, enqueueJob spy not called.
    - `rg "void pgBossJobQueue" apps/worker/src/main.ts` returns nothing.
    - `bun run typecheck && bun run lint` exit 0.
  </acceptance_criteria>
  <done>WR-04/WR-05 closed; the twin matches production dedup; empty rebuild triggers are rejected; dead wiring removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/jobs/:name/trigger | untrusted trigger body; calendarId must be validated |
| Schwab OAuth → refresh use-case | transient vs terminal failures must not be conflated |
| memory twin ↔ pg-boss | twin must match the real port contract (architecture-boundaries §8) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10-01 | Denial of Service | rebuild trigger without calendarId | mitigate | contract refinement rejects (400) un-keyed rebuilds → no queue flood (WR-04) |
| T-05-10-02 | Repudiation | job-runs masking last error/success | mitigate | independent FILTER aggregates surface both states (CR-03) |
| T-05-10-03 | Spoofing | transient blip presented as auth-expired | mitigate | network/parse → retryable storage-error, never auth-expired (CR-02) |
| T-05-10-SC | Tampering | npm/pip/cargo installs | accept | no package installs (RESEARCH §Package Legitimacy Audit: zero new packages) |
</threat_model>

<verification>
- `bunx vitest run refreshToken job-queue jobs jobs.routes` green.
- `cd packages/adapters && bunx vitest run job-runs.contract` green (or clean skip without Docker).
- `bun run typecheck && bun run lint` exit 0.
</verification>

<success_criteria>
CR-02, CR-03, WR-04/C4, WR-05/C5, IN-01 closed. All independent of the fills data path.
</success_criteria>

<output>
Create `.planning/phases/05-jobs-fill-rebuild-integrity/05-10-SUMMARY.md` when done
</output>
