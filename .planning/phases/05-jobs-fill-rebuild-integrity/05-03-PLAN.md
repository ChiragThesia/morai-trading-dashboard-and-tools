---
phase: 05-jobs-fill-rebuild-integrity
plan: 03
type: tdd
wave: 2
depends_on: ["05-01"]
autonomous: true
requirements: [JRNL-01]
files_modified:
  - packages/core/src/journal/domain/fill-pairing.ts
  - packages/core/src/journal/domain/fill-pairing.test.ts

must_haves:
  truths:
    - "classifyFill maps every (side, positionEffect) pair to exactly one of OPEN/CLOSE/UNKNOWN (D-02)"
    - "aggregatePartialFills groups by (calendarId, legOccSymbol, orderId), sums qty, and computes qty-weighted average price (D-04)"
    - "computePnl returns |closeCredit| − openDebit − totalFees (D-08/D-09)"
    - "detectRoll is true only for same-calendar, same-strike/type, different-expiry close+open pairs (D-03)"
    - "hashFillIds is deterministic and order-independent over the fill-id set"
  artifacts:
    - path: "packages/core/src/journal/domain/fill-pairing.ts"
      provides: "Pure domain functions: classifyFill, aggregatePartialFills, computePnl, detectRoll, hashFillIds"
      exports: ["classifyFill", "aggregatePartialFills", "computePnl", "detectRoll", "hashFillIds"]
      min_lines: 40
    - path: "packages/core/src/journal/domain/fill-pairing.test.ts"
      provides: "Example + fast-check property tests proving D-02/D-03/D-04/D-08/D-09"
      min_lines: 60
  key_links:
    - from: "packages/core/src/journal/domain/fill-pairing.ts"
      to: "node:crypto"
      via: "hashFillIds uses createHash('sha256')"
      pattern: "createHash"
---

<objective>
TDD the pure fill-pairing domain — the algorithmic heart of JRNL-01 — with no I/O. Implement
`classifyFill` (D-02), `aggregatePartialFills` (D-04), `computePnl` (D-08/D-09), `detectRoll`
(D-03), and `hashFillIds` (idempotency key), driving each from the failing tests stubbed in
plan 05-01.

Purpose: These five functions are property-testable (`expect(fn(input)).toBe(output)`), so
they get a dedicated TDD plan. They are pure (Result/number/boolean/string returns, no ports),
so they run fast with no Docker and can develop in parallel with the migration (05-02).

Output: A green `fill-pairing.ts` consumed by the sync-fills + rebuild-journal use-cases in
later waves.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-jobs-fill-rebuild-integrity/05-CONTEXT.md
@.planning/phases/05-jobs-fill-rebuild-integrity/05-RESEARCH.md
@.planning/phases/05-jobs-fill-rebuild-integrity/05-PATTERNS.md
@packages/core/src/journal/domain/calendar-event.ts
@packages/core/src/journal/domain/bsm.ts
@packages/core/src/journal/domain/bsm.test.ts
</context>

<artifacts_this_phase_produces>
- `classifyFill(side, positionEffect)` → "OPEN" | "CLOSE" | "UNKNOWN"
- `aggregatePartialFills(fills)` → AggregatedFill[] (qty-weighted avg, summed commission/fees)
- `computePnl(openDebit, closeCredit, totalFees)` → number
- `detectRoll(closing, opening)` → boolean
- `hashFillIds(ids)` → SHA-256 hex string (idempotency key for calendar_events.fillIdsHash)
</artifacts_this_phase_produces>

<feature>
  <name>Fill-pairing pure domain functions (D-02/D-03/D-04/D-08/D-09)</name>
  <files>packages/core/src/journal/domain/fill-pairing.ts, packages/core/src/journal/domain/fill-pairing.test.ts</files>
  <read_first>
    - packages/core/src/journal/domain/fill-pairing.ts (signatures from 05-01 — throwing stubs to implement)
    - packages/core/src/journal/domain/fill-pairing.test.ts (the RED tests from 05-01 to satisfy)
    - packages/core/src/journal/domain/calendar-event.ts (RawFill, AggregatedFill types)
    - packages/core/src/journal/domain/bsm.ts (analog — pure numeric domain module, no adapter imports)
    - packages/core/src/journal/domain/bsm.test.ts (analog — fast-check property test conventions)
    - .planning/phases/05-jobs-fill-rebuild-integrity/05-RESEARCH.md (§Fill→Event Pairing Logic, Pattern 4 ROLL, Open Question 1 hash impl)
  </read_first>
  <behavior>
    RED tests (extend the 05-01 stubs until they assert real expectations, run, confirm fail):
    - classifyFill completeness (D-02): ("buy","OPENING")→OPEN; ("sell","CLOSING")→CLOSE;
      ("buy","CLOSING")→CLOSE; ("sell","OPENING")→OPEN; (any,"UNKNOWN")→UNKNOWN. Property:
      every (side, positionEffect) pair maps to one of the three values, never throws.
    - aggregatePartialFills (D-04): two fills same (calendarId, legOccSymbol, orderId) →
      one group; sumQty == sum of input qtys; avgPrice == sum(qty*price)/sumQty;
      totalCommission == sum(commission ?? 0); totalFees == sum(fees ?? 0). Property:
      sumQty round-trip equals the total input qty across all groups.
    - computePnl (D-08/D-09): computePnl(openDebit, closeCredit, totalFees) ==
      Math.abs(closeCredit) − openDebit − totalFees. Property: monotonic — increasing
      |closeCredit| strictly increases P&L; increasing totalFees strictly decreases P&L.
    - detectRoll (D-03): true when closing.calendarId==opening.calendarId AND same
      underlying/strike/type (from legOccSymbol parse or AggregatedFill fields) AND
      different expiry AND same orderId (or within ROLL window per RESEARCH Open Question 3);
      false otherwise (different calendar, same expiry, different strike).
    - hashFillIds: deterministic (same set → same hex), order-independent (sorts ids before
      hashing), 64-char hex output (SHA-256). Property: hashFillIds([a,b]) == hashFillIds([b,a]).
  </behavior>
  <implementation>
    GREEN — minimal implementation to pass:
    - classifyFill: switch/branch over side+positionEffect, exhaustive, returns the literal.
    - aggregatePartialFills: group via a Map keyed on `${calendarId}|${legOccSymbol}|${orderId}`,
      reduce qty/price/commission/fees; avgPrice = weightedSum/sumQty. No `any`, no `!` —
      use assertDefined where Map.get is involved or branch on undefined.
    - computePnl: pure arithmetic `Math.abs(closeCredit) - openDebit - totalFees`.
    - detectRoll: boolean expression over the AggregatedFill fields per Pattern 4. Start with
      orderId-match only (RESEARCH Open Question 3 recommendation); document the ROLL_WINDOW_MS
      extension as a comment, do not implement the time window unless a test demands it.
    - hashFillIds: `import { createHash } from "node:crypto"`; sort ids; join with a separator;
      `createHash("sha256").update(joined).digest("hex")`. (RESEARCH Open Question 1 — node:crypto
      preferred over crypto.subtle for sync simplicity; node:crypto is allowed in core as it is a
      pure runtime builtin, not a framework/adapter.)
    REFACTOR only if obvious; keep functions small.
  </implementation>
  <verify>
    <automated>cd packages/core && bun run test src/journal/domain/fill-pairing.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test src/journal/domain/fill-pairing.test.ts` passes (GREEN)
    - `rg -c "createHash" packages/core/src/journal/domain/fill-pairing.ts` returns >= 1
    - No `any`/`as`/`!` introduced: `rg -nE "\bas\b|: any|!\." packages/core/src/journal/domain/fill-pairing.ts` returns no production-code violations
    - fast-check property tests present for computePnl monotonicity and aggregatePartialFills qty round-trip
    - git log shows a `test(05-03)` commit before the `feat(05-03)` commit (RED before GREEN)
  </acceptance_criteria>
  <done>fill-pairing.ts is green: all five functions implemented, property + example tests pass, no type-safety violations, RED→GREEN commit sequence present.</done>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure domain functions; no I/O, no external input, no DB. fillIdsHash is computed over already-trusted internal fill UUIDs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Tampering | hashFillIds idempotency key | mitigate | Deterministic, order-independent SHA-256 ensures re-running sync-fills on the same fill set yields the same hash → the calendar_events UNIQUE constraint blocks duplicate-event injection (SC4 idempotency) |
| T-05-07 | Repudiation | classifyFill UNKNOWN path | mitigate | classifyFill never silently misclassifies — UNKNOWN is an explicit return that the sync-fills use-case (05-07) routes to the calendar.openedAt/closedAt cross-check or orphan parking, never dropping the fill (D-05) |
</threat_model>

<verification>
- `cd packages/core && bun run test src/journal/domain/fill-pairing.test.ts` green
- RED→GREEN commit sequence present in git log for 05-03
</verification>

<success_criteria>
- All five fill-pairing functions implemented and property/example tested
- computePnl and aggregatePartialFills have fast-check property tests
- No `any`/`as`/`!` in production code
</success_criteria>

<output>
Create `.planning/phases/05-jobs-fill-rebuild-integrity/05-03-SUMMARY.md` with RED/GREEN/REFACTOR notes + commit list.
</output>
