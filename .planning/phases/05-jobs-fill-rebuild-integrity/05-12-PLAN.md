---
phase: 05-jobs-fill-rebuild-integrity
plan: 12
type: tdd
wave: 2
depends_on: ["05-09"]
files_modified:
  - packages/adapters/src/postgres/repos/fills.ts
  - packages/adapters/src/postgres/repos/fills.contract.test.ts
  - packages/adapters/src/memory/fills.ts
  - packages/adapters/src/__contract__/fills.contract.ts
  - packages/adapters/src/memory/fills.contract.test.ts
  - packages/adapters/src/index.ts
  - packages/core/src/journal/application/syncTransactions.ts
  - packages/core/src/journal/application/syncTransactions.test.ts
  - packages/core/src/journal/index.ts
  - packages/core/src/index.ts
autonomous: true
gap_closure: true
requirements: [JRNL-01]
must_haves:
  truths:
    - "A Postgres fills repo reads unprocessed fills (all + per-calendar), reads calendar legs by OCC, recomputes calendar amounts, and writes fills — all SQL contract-tested"
    - "The fills table is populated from Schwab BrokerTransaction legs (A4 source), idempotent on fill id"
    - "readUnprocessedFills excludes fills already reflected in calendar_events or orphan_fills"
    - "recomputeCalendarAmounts writes openNetDebit/closeNetCredit from calendar_events (not just clears)"
  artifacts:
    - path: "packages/adapters/src/postgres/repos/fills.ts"
      provides: "A1 ForReadingUnprocessedFills(+ForCalendar), ForReadingCalendarLegs, ForResetting/RecomputingCalendarAmounts, ForWritingFills"
      contains: "ForReadingCalendarLegs"
    - path: "packages/core/src/journal/application/syncTransactions.ts"
      provides: "A4 use-case mapping BrokerTransaction[] → RawFill[] → ForWritingFills"
      contains: "ForFetchingTransactions"
    - path: "packages/adapters/src/memory/fills.ts"
      provides: "in-memory twin for all fills ports (architecture-boundaries §8)"
      contains: "ForReadingUnprocessedFills"
  key_links:
    - from: "packages/adapters/src/postgres/repos/fills.ts"
      to: "packages/adapters/src/postgres/schema.ts"
      via: "fills/calendars/calendarEvents/orphanFills tables"
      pattern: "from \"../schema"
    - from: "packages/core/src/journal/application/syncTransactions.ts"
      to: "packages/core/src/brokerage/application/ports.ts"
      via: "ForFetchingTransactions + BrokerTransaction"
      pattern: "ForFetchingTransactions"
---

<objective>
Build the real data path so SC4/SC5 are verifiable against actual fills, not stubs. Implement
the fills repo (A1) with SQL contract tests, the calendar-amounts recompute port (A3), and the
fills SOURCE (A4) that populates the `fills` table from the existing Phase-4 Schwab transactions
adapter. Depends only on 05-09's port contracts; shares no files with 05-11 → parallel in Wave 2.

Purpose: the GAPS locked decision is a full vertical slice — sync-fills and rebuild-journal must
work end-to-end. Today there is NO fills repo and NO fills source; the worker runs against stubs.
This plan removes both stubs' reason to exist.
Output: a Postgres fills repo + in-memory twin (contract-tested), and a sync-transactions
use-case feeding the fills table.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-jobs-fill-rebuild-integrity/05-GAPS.md
@.planning/phases/05-jobs-fill-rebuild-integrity/05-RESEARCH.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/tdd.md
@packages/core/src/journal/application/ports.ts
@packages/adapters/src/postgres/schema.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A1 fills repo (Postgres + memory twin) + A3 recompute amounts — RED→GREEN (testcontainers)</name>
  <read_first>
    - packages/adapters/src/postgres/repos/calendar-events.ts (analog repo: imports, Db, ok/err wrapping)
    - packages/adapters/src/postgres/repos/calendars.ts (lines 306-345 — getOpenCalendarLegs: formatOccSymbol from strike/expiry/optionType — the pattern for readCalendarLegs)
    - packages/adapters/src/memory/calendar-events.ts (in-memory twin structure + seed/count helpers)
    - packages/adapters/src/postgres/repos/orphan-fills.contract.test.ts (testcontainer harness: inject("dbUrl"), skipIf, TRUNCATE, makeDb)
    - packages/adapters/src/postgres/schema.ts (lines 163-176 fills, 223-270 calendar_events + orphan_fills, calendars)
    - packages/core/src/journal/application/ports.ts (all fills ports + ForRecomputingCalendarAmounts)
  </read_first>
  <behavior>
    - makePostgresFillsRepo(db) implements: ForReadingUnprocessedFills (all fills whose id is NOT in
      any calendar_events.fill_ids_hash component AND NOT in orphan_fills; since fill_ids_hash is a
      hash, "unprocessed" = fills not present in orphan_fills AND not covered by an existing event for
      their leg — define the exclusion concretely: a fill is processed if its id is in orphan_fills OR
      its leg already has a calendar_events row whose fill set includes it; simplest correct rule:
      exclude fills present in orphan_fills, and let calendar_events idempotency via fill_ids_hash
      absorb re-emission — document the chosen rule in a comment), ForReadingUnprocessedFillsForCalendar
      (same filtered to the calendar's leg OCC symbols), ForReadingCalendarLegs (given an OCC symbol,
      return all (calendarId, legOccSymbol, positionEffect) where the calendar has that symbol as a leg
      — derive leg OCC via formatOccSymbol over front+back expiry like getOpenCalendarLegs; positionEffect
      from calendar status: open→OPENING, closed→CLOSING, else UNKNOWN), ForResettingCalendarAmounts
      (UPDATE calendars SET open_net_debit/close_net_credit NULL), ForRecomputingCalendarAmounts
      (A3: SUM calendar_events.net_amount by sign into open_net_debit/close_net_credit for the calendar
      and UPDATE), ForWritingFills (INSERT fills onConflictDoNothing on id PK).
    - makeMemoryFillsRepo() mirrors every method with Maps + seed helpers.
    - Contract test (shared in __contract__/fills.contract.ts, run against BOTH Postgres + memory):
      seed fills+calendar+event → readUnprocessedFills excludes parked/processed; readCalendarLegs
      returns the right calendar; recomputeCalendarAmounts writes non-null totals matching summed events;
      writeFills is idempotent on id.
  </behavior>
  <action>
    RED: write `__contract__/fills.contract.ts` exporting `runFillsContractTests(makeRepo, getSeed)`
    with the cases above; wire `postgres/repos/fills.contract.test.ts` (testcontainers + runMigrations,
    skipIf no dbUrl, TRUNCATE fills/calendar_events/orphan_fills/calendars between tests) and
    `memory/fills.contract.test.ts`. Run; confirm RED (repos do not exist).
    GREEN: implement `postgres/repos/fills.ts` and `memory/fills.ts` per the behavior; reuse the
    formatOccSymbol leg-derivation from calendars.ts for readCalendarLegs; wrap every Postgres method
    in the StorageError try/catch pattern. Export both from `packages/adapters/src/index.ts`. Commit at green.
  </action>
  <verify>
    <automated>cd packages/adapters && bunx vitest run fills.contract</automated>
  </verify>
  <acceptance_criteria>
    - `fills.contract.test.ts` (Postgres, testcontainers) and `memory/fills.contract.test.ts` both green (Postgres skips cleanly without Docker).
    - `rg -c "ForReadingCalendarLegs" packages/adapters/src/postgres/repos/fills.ts` ≥ 1.
    - recomputeCalendarAmounts proven to write non-null open_net_debit/close_net_credit derived from calendar_events (contract assertion).
    - writeFills idempotent on id (insert same fill twice → 1 row).
    - `bun run lint` exits 0 (Drizzle confined to adapters; no `as`/`!`/`any`).
  </acceptance_criteria>
  <done>A1 + A3 closed: a real, SQL-tested fills repo with calendar-amounts recompute exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: A4 fills source — sync-transactions use-case (BrokerTransaction → fills) — RED→GREEN</name>
  <read_first>
    - packages/core/src/brokerage/application/ports.ts (lines 82-138 — BrokerTransaction shape + ForFetchingTransactions)
    - packages/adapters/src/schwab/trader/transactions-adapter.ts (maps Schwab tx → BrokerTransaction with legs[].occSymbol/qty/price/positionEffect)
    - packages/core/src/journal/domain/calendar-event.ts (RawFill shape — the write target)
    - packages/core/src/journal/application/syncFills.ts (analog use-case factory shape)
    - packages/core/src/journal/application/ports.ts (ForWritingFills from 05-09)
  </read_first>
  <behavior>
    - makeSyncTransactionsUseCase(deps) where deps = { fetchTransactions: ForFetchingTransactions,
      writeFills: ForWritingFills, accountHash/from/to providers or args, newId: NewId, now: () => Date }.
      It fetches BrokerTransaction[] for the window, flattens each tx's legs into RawFill rows
      (id = deps.newId() or a deterministic id; orderId = String(tx.orderId ?? activityId);
      occSymbol = leg.occSymbol; side derived from positionEffect+amount sign — OPENING buy / CLOSING sell
      is a reasonable default, OR carry the real side if available; qty/price from the leg; filledAt
      from tradeDate; commission/fees null unless present), and writes them via writeFills (idempotent).
    - AUTH_EXPIRED from fetchTransactions → return ok(undefined) (degrade, do not abort the worker),
      mirroring the Phase-4 degradation pattern; FetchError → err mapped to StorageError-compatible result.
    - Idempotency: re-running the same window writes no duplicate fills (writeFills onConflictDoNothing on id).
      To make ids stable across re-runs, derive the fill id deterministically from
      (activityId + legIndex) rather than random — document this so re-runs dedupe.
  </behavior>
  <action>
    RED: write `syncTransactions.test.ts` with an in-memory fetchTransactions returning two txs
    (one OPENING, one CLOSING) and an in-memory writeFills capturing rows; assert legs flatten to the
    right RawFill rows, side/positionEffect map correctly, and a second run writes no new rows
    (deterministic ids). Add an AUTH_EXPIRED case asserting ok(undefined) + no writes. Run; confirm RED.
    GREEN: implement `syncTransactions.ts`; export from journal/core barrels. Decide the deterministic
    id scheme (e.g. a stable string from activityId+legIndex) and document it. Commit at green.
  </action>
  <verify>
    <automated>bunx vitest run syncTransactions && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - syncTransactions.test proves BrokerTransaction legs flatten to RawFill rows with correct occSymbol/qty/price/side.
    - Second run over the same window writes zero new fills (deterministic ids + idempotent writeFills).
    - AUTH_EXPIRED → ok(undefined), no writes (asserted).
    - `makeSyncTransactionsUseCase` exported from `@morai/core`.
    - `bun run typecheck && bun run lint` exit 0.
  </acceptance_criteria>
  <done>A4 closed: the fills table has a real source; sync-fills now has genuine input to pair.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schwab transactions → fills | untrusted broker payload (already Zod-parsed in the adapter); flattening must not invent data |
| fills repo SQL ↔ DB | unprocessed-fills exclusion rule must be correct or events double-emit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12-01 | Tampering | fills source idempotency | mitigate | deterministic fill ids + onConflictDoNothing → re-runs add no duplicates |
| T-05-12-02 | Information Disclosure | broker payload → fills | mitigate | adapter already Zod-parses; use-case maps only typed BrokerTransaction fields; no raw token/secret carried |
| T-05-12-03 | Tampering | calendar amounts left NULL post-rebuild | mitigate | ForRecomputingCalendarAmounts writes totals from events (A3/WR-08) |
| T-05-12-SC | Tampering | npm/pip/cargo installs | accept | no package installs (RESEARCH §Package Legitimacy Audit: zero new packages) |
</threat_model>

<verification>
- `cd packages/adapters && bunx vitest run fills.contract` green (memory always; Postgres with Docker).
- `bunx vitest run syncTransactions` green.
- `bun run typecheck && bun run lint` exit 0.
</verification>

<success_criteria>
A1 (fills repo), A3 (recompute amounts), A4 (fills source) closed and contract-tested. The data
path is real and ready for wiring (05-13).
</success_criteria>

<output>
Create `.planning/phases/05-jobs-fill-rebuild-integrity/05-12-SUMMARY.md` when done
</output>
