---
phase: 05-jobs-fill-rebuild-integrity
plan: 11
type: tdd
wave: 2
depends_on: ["05-09"]
files_modified:
  - packages/core/src/journal/application/syncFills.ts
  - packages/core/src/journal/application/syncFills.test.ts
autonomous: true
gap_closure: true
requirements: [JRNL-01]
must_haves:
  truths:
    - "On CLOSE/ROLL, syncFills looks up the prior OPEN event for the leg and computes realizedPnl = closeCredit − originalOpenDebit − feesOnClose"
    - "UNKNOWN aggregates park EACH underlying raw fill individually with its real side and filledAt; no synthesized non-UUID fillId"
    - "syncFills mints event ids and fill-id hashes via injected ports (newId, hashFillIds) — no node crypto import in core"
    - "A calendar-scoped sync variant re-pairs ONLY the target calendar's fills (CR-04)"
  artifacts:
    - path: "packages/core/src/journal/application/syncFills.ts"
      provides: "Corrected sync-fills use-case consuming the fixed domain + injected ports"
      contains: "readCalendarEvents"
  key_links:
    - from: "packages/core/src/journal/application/syncFills.ts"
      to: "packages/core/src/journal/domain/fill-pairing.ts"
      via: "import computeRealizedPnl, detectRoll, aggregatePartialFills"
      pattern: "from \"../domain/fill-pairing"
---

<objective>
Rewrite the sync-fills use-case to consume the corrected domain (05-09) and the injected ports,
closing B1 (realized-P&L lookup), B5 (orphan parking), C1 (injected id/hasher), and A2
(calendar-scoped sync for CR-04). Pure core; depends only on 05-09's stable contracts.

Purpose: the use-case is where realized P&L is actually assembled — it must read the prior OPEN
event to get the original open debit, park UNKNOWN fills correctly, and offer a calendar-scoped
mode so rebuild-journal re-pairs exactly one calendar.
Output: a correct, pure, fully-tested sync-fills use-case ready for real wiring (05-13).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-jobs-fill-rebuild-integrity/05-GAPS.md
@.planning/phases/05-jobs-fill-rebuild-integrity/05-REVIEW.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/tdd.md
@packages/core/src/journal/application/ports.ts
@packages/core/src/journal/domain/fill-pairing.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: B1 realized-P&L lookup + C1 injected id/hasher + B5 orphan parking — RED→GREEN</name>
  <read_first>
    - packages/core/src/journal/application/syncFills.ts (full — current buggy use-case)
    - packages/core/src/journal/application/syncFills.test.ts (existing in-memory-twin tests)
    - packages/core/src/journal/application/ports.ts (ForReadingCalendarEvents, NewId, HashFillIds, etc.)
    - packages/core/src/journal/domain/fill-pairing.ts (computeRealizedPnl, detectRoll, aggregatePartialFills after 05-09)
  </read_first>
  <behavior>
    - SyncFillsDeps gains: `readCalendarEvents: ForReadingCalendarEvents` (read prior OPEN events for a
      calendar to find originalOpenDebit), `newId: NewId`, `hashFillIds: HashFillIds`. Remove the
      `randomUUID`/`crypto` import from syncFills.ts — ids come from `deps.newId()`.
    - CLOSE realized P&L: find the matching prior OPEN event for the same leg (legOccSymbol) on the
      calendar; originalOpenDebit = that OPEN event's netAmount (positive debit). realizedPnl =
      |closeCredit| − originalOpenDebit − feesOnClose. If no prior OPEN event exists, realizedPnl = null
      (do NOT report a wrong number — REVIEW WR-01 fix-of-last-resort).
      Example: OPEN event netAmount=300 for leg X; later CLOSE of leg X with closeCredit=500, feesOnClose=2
      → realizedPnl=198.
    - ROLL realized P&L: realizedPnl is computed on the CLOSED (old) leg only:
      |closeCredit_oldLeg| − originalOpenDebit_oldLeg − feesOnClose. The NEW leg's debit is netAmount/cost
      basis, NOT subtracted from realizedPnl (locked decision 2).
    - B5 UNKNOWN parking: when an aggregate classifies UNKNOWN, loop over each underlying raw fill and
      park it individually via storeOrphanFill with the fill's REAL side and REAL filledAt (not `now`,
      not hardcoded "buy"); fillId is the real fill UUID — never `agg-unknown-${orderId}`. If a fill has
      no id the aggregate is malformed → return err(StorageError), do not synthesize a PK.
  </behavior>
  <action>
    RED: extend syncFills.test.ts (in-memory twins) with: (a) a CLOSE after an OPEN proving
    realizedPnl uses the OPEN event's netAmount as originalOpenDebit; (b) a CLOSE with no prior OPEN
    → realizedPnl null; (c) a ROLL proving realizedPnl excludes the new leg's debit; (d) an UNKNOWN
    aggregate of 2 raw fills → 2 orphan rows each with real side/filledAt/UUID. Seed prior OPEN
    events through the calendar-events twin's store before the CLOSE. Run; confirm RED.
    GREEN: thread `readCalendarEvents`, `newId`, `hashFillIds` into SyncFillsDeps; replace
    `randomUUID()` with `deps.newId()` and `hashFillIds(...)` (domain import) with `deps.hashFillIds(...)`;
    remove `import { randomUUID } from "crypto"`. Implement the prior-OPEN lookup for CLOSE/ROLL using
    `deps.readCalendarEvents(calendarId)` filtered to OPEN events for the leg. Rewrite the UNKNOWN
    branch to loop `cf.fillIds`/underlying raw fills and park each with real fields. Commit at green.
  </action>
  <verify>
    <automated>bunx vitest run syncFills && grep -v '^ *\*' packages/core/src/journal/application/syncFills.ts | rg -c "from \"crypto\"|node:crypto"; test $? -ne 0 || (grep -v '^ *\*' packages/core/src/journal/application/syncFills.ts | rg -q "from \"crypto\"|node:crypto" && exit 1); bun run lint</automated>
  </verify>
  <acceptance_criteria>
    - syncFills.test proves CLOSE realizedPnl = |closeCredit| − priorOpenNetAmount − feesOnClose, and null when no prior OPEN.
    - syncFills.test proves ROLL realizedPnl excludes the new leg's debit.
    - syncFills.test proves UNKNOWN aggregate of N fills → N orphan rows with real side/filledAt and real UUIDs.
    - `grep -v '^ *\*' packages/core/src/journal/application/syncFills.ts | rg -c "from \"crypto\"|node:crypto"` returns 0.
    - `bun run lint` exits 0 (no crypto import, no `as`/`!`/`any`).
  </acceptance_criteria>
  <done>B1 realized-P&L, B5 orphan parking, and C1 injection closed in the use-case.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: A2 calendar-scoped sync variant (CR-04) — RED→GREEN</name>
  <read_first>
    - packages/core/src/journal/application/syncFills.ts (after Task 1)
    - packages/core/src/journal/application/syncFills.test.ts (after Task 1)
    - packages/core/src/journal/application/ports.ts (ForReadingUnprocessedFillsForCalendar from 05-09)
    - packages/core/src/journal/application/rebuildJournal.ts (consumes syncFillsForCalendar)
  </read_first>
  <behavior>
    - A calendar-scoped sync (either `makeSyncFillsForCalendarUseCase(deps)` returning
      `(calendarId) => Promise<Result<void, StorageError>>`, or an optional `calendarId` arg on the
      existing use-case) reads fills via `readUnprocessedFillsForCalendar(calendarId)` and pairs ONLY
      that calendar's fills. The full-sweep use-case (no calendarId) keeps reading all unprocessed fills.
    - Given fills for calendar A and calendar B, the scoped sync for A emits events for A only and
      touches no B events (CR-04: delete scope and sync scope must agree).
  </behavior>
  <action>
    RED: add a syncFills.test case with fills spanning calendars A and B; assert the scoped variant
    for A produces only A's events (B untouched). Run; confirm RED.
    GREEN: add `ForReadingUnprocessedFillsForCalendar` to SyncFillsDeps (or a sibling factory) and a
    scoped entry point; the scoped path uses the calendar-filtered fills reader. Keep the full-sweep
    path intact. Export the scoped driver type so 05-13 can wire it as `syncFillsForCalendar`. Commit
    at green.
  </action>
  <verify>
    <automated>bunx vitest run syncFills && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - syncFills.test proves the scoped sync for calendar A emits only A's events when B's fills are also unprocessed.
    - A scoped driver type `(calendarId: string) => Promise<Result<void, StorageError>>` is exported for 05-13.
    - `bun run typecheck` exits 0.
  </acceptance_criteria>
  <done>A2/CR-04 closed: rebuild can re-pair exactly one calendar via the scoped sync.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fills → events | mis-classified or mis-priced events corrupt the trade ledger |
| core ↔ adapters | ids/hashes injected; core stays pure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11-01 | Tampering | realized P&L assembly | mitigate | prior-OPEN lookup pins originalOpenDebit; null rather than wrong number when absent |
| T-05-11-02 | Repudiation | UNKNOWN orphan parking | mitigate | each raw fill parked individually with real audit fields; no synthesized PK (B5) |
| T-05-11-03 | Tampering | cross-calendar event bleed on rebuild | mitigate | calendar-scoped sync (A2) keeps delete + sync scopes aligned (CR-04) |
| T-05-11-SC | Tampering | npm/pip/cargo installs | accept | no package installs (RESEARCH §Package Legitimacy Audit: zero new packages) |
</threat_model>

<verification>
- `bunx vitest run syncFills` green.
- `grep -v '^ *\*' packages/core/src/journal/application/syncFills.ts | rg -c "crypto"` returns 0.
- `bun run typecheck && bun run lint` exit 0.
</verification>

<success_criteria>
B1, B5, C1 (use-case half), A2/CR-04 closed in pure core. Real wiring + reconciliation follow in 05-13.
</success_criteria>

<output>
Create `.planning/phases/05-jobs-fill-rebuild-integrity/05-11-SUMMARY.md` when done
</output>
