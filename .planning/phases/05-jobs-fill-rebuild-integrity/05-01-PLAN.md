---
phase: 05-jobs-fill-rebuild-integrity
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [JOB-01, JRNL-01]
files_modified:
  - docs/architecture/data-model.md
  - docs/architecture/jobs.md
  - packages/adapters/src/postgres/schema.ts
  - packages/core/src/journal/domain/calendar-event.ts
  - packages/core/src/journal/domain/fill-pairing.ts
  - packages/core/src/journal/domain/fill-pairing.test.ts
  - packages/core/src/journal/application/ports.ts
  - packages/core/src/journal/application/syncFills.test.ts
  - packages/core/src/journal/application/rebuildJournal.test.ts
  - packages/core/src/brokerage/application/refreshTokens.test.ts
  - packages/adapters/src/__contract__/calendar-events.contract.ts
  - packages/adapters/src/__contract__/orphan-fills.contract.ts
  - apps/worker/src/handlers/sync-fills.test.ts
  - apps/worker/src/handlers/refresh-tokens.test.ts
  - apps/worker/src/handlers/rebuild-journal.test.ts

must_haves:
  truths:
    - "docs/architecture/data-model.md documents the calendar_events + orphan_fills tables and the entry_thesis column before any schema code lands"
    - "docs/architecture/jobs.md documents sync-fills, refresh-tokens, rebuild-journal job specs before implementation"
    - "schema.ts declares calendarEvents, orphanFills tables, calendarEventTypeEnum, and entryThesis on calendars"
    - "Every Wave 0 failing-test stub file exists and fails for the right reason (assertion, not import error)"
  artifacts:
    - path: "docs/architecture/data-model.md"
      provides: "calendar_events + orphan_fills + entry_thesis documentation (docs-before-architecture rule)"
      contains: "calendar_events"
    - path: "packages/adapters/src/postgres/schema.ts"
      provides: "calendarEvents, orphanFills tables + calendarEventTypeEnum + calendars.entryThesis"
      contains: "calendarEvents"
    - path: "packages/core/src/journal/domain/calendar-event.ts"
      provides: "CalendarEvent domain ADT + CalendarEventType union (OPEN/CLOSE/ROLL)"
      contains: "CalendarEventType"
    - path: "packages/core/src/journal/application/ports.ts"
      provides: "New driven port type declarations for events/orphans/fills/jobs"
      contains: "ForStoringCalendarEvent"
    - path: "packages/core/src/journal/domain/fill-pairing.test.ts"
      provides: "Failing Wave 0 stubs for classifyFill/aggregatePartialFills/computePnl/detectRoll/hashFillIds"
      min_lines: 30
  key_links:
    - from: "packages/adapters/src/postgres/schema.ts"
      to: "docs/architecture/data-model.md"
      via: "table shapes match the documented design"
      pattern: "calendarEvents"
---

<objective>
Lay the typed + documented foundation for Phase 5 in one Wave-1 plan: update the
architecture docs FIRST (docs-before-architecture rule), then add the new Drizzle table
declarations to `schema.ts`, declare the new domain types and driven ports, and create
every Wave 0 failing-test stub so downstream TDD plans have a RED baseline to drive.

Purpose: The strict project rules require docs to precede schema changes, and TDD requires
failing tests to exist before production code. This plan delivers both, plus the type/port
surface every later slice imports — eliminating the scavenger-hunt anti-pattern.

Output: Updated `data-model.md` + `jobs.md`; extended `schema.ts`; `calendar-event.ts`
domain ADT; extended `journal/application/ports.ts`; and 8 failing Wave-0 test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-jobs-fill-rebuild-integrity/05-CONTEXT.md
@.planning/phases/05-jobs-fill-rebuild-integrity/05-RESEARCH.md
@.planning/phases/05-jobs-fill-rebuild-integrity/05-PATTERNS.md
@docs/architecture/data-model.md
@docs/architecture/jobs.md
@packages/adapters/src/postgres/schema.ts
@packages/core/src/journal/application/ports.ts
@packages/core/src/brokerage/application/ports.ts
@packages/core/src/brokerage/application/refreshToken.ts
</context>

<artifacts_this_phase_produces>
This plan introduces the following NEW symbols (consumed by later Phase 5 plans):

- `calendar_events` table (columns: id, calendar_id, event_type, evented_at, fill_ids_hash UNIQUE, leg_occ_symbol, rolled_from_occ_symbol, qty, avg_price, net_amount, realized_pnl, leg_breakdown, entry_thesis, created_at) + `calendarEventTypeEnum` (OPEN/CLOSE/ROLL)
- `orphan_fills` table (columns: fill_id PK, occ_symbol, side, qty, price, filled_at, reason, created_at)
- `calendars.entry_thesis` nullable column (D-07 entry-thesis hook)
- `CalendarEvent` domain ADT + `CalendarEventType` literal union in `calendar-event.ts`
- Function signatures (no bodies yet) in `fill-pairing.ts`: `classifyFill`, `aggregatePartialFills`, `computePnl`, `detectRoll`, `hashFillIds`
- New ports in `journal/application/ports.ts`: `ForStoringCalendarEvent`, `ForReadingCalendarEvents`, `ForDeletingCalendarEvents`, `ForReadingUnprocessedFills`, `ForReadingCalendarLegs`, `ForStoringOrphanFill`, `ForResettingCalendarAmounts`, `ForEnqueueingJob`
- New Zod schemas (declared here, parsed in later handlers): `syncFillsPayload`, `rebuildJournalPayload`, `triggerJobPayload`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Docs-first — document calendar_events, orphan_fills, entry_thesis, and the three new jobs</name>
  <read_first>
    - docs/architecture/data-model.md (the file being modified — source of truth for tables)
    - docs/architecture/jobs.md (the file being modified — job catalog + dedupe/RTH rules)
    - .planning/phases/05-jobs-fill-rebuild-integrity/05-RESEARCH.md (§calendar_events Table Design, §schedule.ts Extraction, §refresh-tokens Handler)
    - .planning/phases/05-jobs-fill-rebuild-integrity/05-CONTEXT.md (D-06, D-07, D-08, D-09, D-11..D-15)
  </read_first>
  <action>
    Per the docs-before-architecture rule (workflow.md), update the architecture docs BEFORE
    touching schema.ts. In docs/architecture/data-model.md add a `calendar_events` table
    section (the L1 trade-ledger layer per D-06, distinct from calendar_snapshots) documenting
    columns calendar_id (FK calendars.id), event_type (OPEN|CLOSE|ROLL per D-03 first-class
    roll), evented_at, fill_ids_hash (SHA-256 hex, UNIQUE, the idempotency key per D-11),
    leg_occ_symbol, rolled_from_occ_symbol (nullable, set only on ROLL per D-03), qty,
    avg_price (qty-weighted per D-04), net_amount (open debit positive / close credit negative
    per D-08, includes commission+fees), realized_pnl (close credit − open debit − fees per
    D-09, populated on CLOSE/ROLL), leg_breakdown (per-leg JSON breakdown, hard requirement per
    D-09), entry_thesis (D-07 hook). Add an `orphan_fills` table section (D-05 — unmatched fills
    parked, never dropped) with columns fill_id PK, occ_symbol, side, qty, price, filled_at,
    reason, created_at. Document that `calendars` gains a nullable `entry_thesis` column (D-07).
    In docs/architecture/jobs.md, add catalog entries for sync-fills (every 10 min RTH, dedupe
    `sync-fills:{windowStart}`), refresh-tokens (04:00 ET daily, NO RTH gate, per-app independence
    per D-13, proactive 7-day expiry warning per D-14), and rebuild-journal (on-demand only, no
    schedule, dedupe `rebuild-journal:{calendarId}` per D-10). Note that snapshot-calendars stays
    chain-triggered (no cron). Prose follows Hemingway style.
  </action>
  <verify>
    <automated>rg -q "calendar_events" docs/architecture/data-model.md && rg -q "orphan_fills" docs/architecture/data-model.md && rg -q "entry_thesis" docs/architecture/data-model.md && rg -q "refresh-tokens" docs/architecture/jobs.md && rg -q "sync-fills" docs/architecture/jobs.md && rg -q "rebuild-journal" docs/architecture/jobs.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "calendar_events" docs/architecture/data-model.md` returns >= 1
    - `rg -c "orphan_fills" docs/architecture/data-model.md` returns >= 1
    - `rg -c "entry_thesis" docs/architecture/data-model.md` returns >= 1
    - docs/architecture/jobs.md mentions sync-fills, refresh-tokens, and rebuild-journal
    - docs note: snapshot-calendars has NO cron (chain-triggered); refresh-tokens has NO RTH gate
  </acceptance_criteria>
  <done>data-model.md and jobs.md document the new tables, the entry_thesis column, and the three new jobs, matching the schema declared in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Extend schema.ts with calendarEvents + orphanFills tables, event-type enum, and calendars.entryThesis</name>
  <read_first>
    - packages/adapters/src/postgres/schema.ts (the file being modified — existing pgTable/pgEnum conventions, calendars table lines 48-64, fills table lines 157-168)
    - .planning/phases/05-jobs-fill-rebuild-integrity/05-PATTERNS.md (§schema.ts extended — exact column declarations)
    - docs/architecture/data-model.md (just-updated source of truth — Task 1 output)
  </read_first>
  <action>
    Add `calendarEventTypeEnum = pgEnum("calendar_event_type", ["OPEN","CLOSE","ROLL"])`
    (additive — no existing enum touched). Declare `calendarEvents` pgTable "calendar_events":
    id uuid PK defaultRandom; calendarId uuid("calendar_id") notNull (FK calendars.id);
    eventType calendarEventTypeEnum("event_type") notNull; eventedAt timestamp("evented_at",
    {withTimezone:true}) notNull; fillIdsHash varchar("fill_ids_hash",{length:64}) notNull
    .unique() (exactly 64 hex for SHA-256 per RESEARCH Pitfall 7); legOccSymbol
    varchar("leg_occ_symbol",{length:32}) notNull; rolledFromOccSymbol
    varchar("rolled_from_occ_symbol",{length:32}) (nullable, D-03); qty integer notNull;
    avgPrice numeric("avg_price") notNull; netAmount numeric("net_amount") notNull; realizedPnl
    numeric("realized_pnl") (nullable); legBreakdown text("leg_breakdown") (JSON string, D-09);
    entryThesis text("entry_thesis") (D-07); createdAt timestamp("created_at",
    {withTimezone:true}) notNull defaultNow. Chain `.enableRLS()` to match every existing table.
    Declare `orphanFills` pgTable "orphan_fills": fillId uuid("fill_id") .primaryKey(); occSymbol
    varchar(32) notNull; side varchar(4) notNull; qty integer notNull; price numeric notNull;
    filledAt timestamp({withTimezone:true}) notNull; reason text notNull; createdAt timestamp
    defaultNow notNull; `.enableRLS()`. Add nullable `entryThesis: text("entry_thesis")` column
    to the existing `calendars` table (D-07 — nullable, no default, non-destructive). Do NOT
    generate or apply a migration here — that is the [BLOCKING] task in plan 05-02.
  </action>
  <verify>
    <automated>cd packages/adapters && bunx tsc --noEmit -p tsconfig.json 2>&1 | rg -v "\.test\.ts" | rg -c "error TS" | rg -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "calendarEvents" packages/adapters/src/postgres/schema.ts` returns >= 1
    - `rg -c "orphanFills" packages/adapters/src/postgres/schema.ts` returns >= 1
    - `rg -c 'calendar_event_type' packages/adapters/src/postgres/schema.ts` returns >= 1
    - `rg -c 'entry_thesis' packages/adapters/src/postgres/schema.ts` returns >= 1 (on calendars)
    - fillIdsHash declared with `.unique()` and length 64
    - `packages/adapters` typecheck passes (0 non-test errors)
  </acceptance_criteria>
  <done>schema.ts declares calendarEvents + orphanFills + the event-type enum + calendars.entryThesis; package typechecks. No migration applied yet (deferred to 05-02 [BLOCKING]).</done>
</task>

<task type="auto">
  <name>Task 3: Declare CalendarEvent domain ADT, new journal ports, fill-pairing signatures, and 8 failing Wave-0 test stubs</name>
  <read_first>
    - packages/core/src/journal/application/ports.ts (the file being modified — ForVerbingNoun convention, StorageError, Calendar type)
    - packages/core/src/journal/domain/bsm.ts (analog for fill-pairing.ts pure-function module shape)
    - packages/core/src/journal/domain/bsm.test.ts (analog for fast-check property test structure)
    - packages/core/src/journal/application/snapshotCalendars.ts (analog for use-case Deps/factory shape that syncFills.test.ts will assert against)
    - packages/adapters/src/__contract__/calendar-snapshots.contract.ts (analog for the new contract test exports)
    - apps/worker/src/handlers/compute-bsm-greeks.test.ts (analog for handler test structure)
    - .planning/phases/05-jobs-fill-rebuild-integrity/05-PATTERNS.md (§fill-pairing.ts signatures, §ports list, §Wave 0 list in 05-RESEARCH.md)
  </read_first>
  <action>
    Create `packages/core/src/journal/domain/calendar-event.ts`: export
    `CalendarEventType = "OPEN" | "CLOSE" | "ROLL"` literal union and a readonly `CalendarEvent`
    domain type mirroring the calendar_events columns (id, calendarId, eventType, eventedAt,
    fillIdsHash, legOccSymbol, rolledFromOccSymbol nullable, qty, avgPrice, netAmount, realizedPnl
    nullable, legBreakdown nullable, entryThesis nullable). Also export readonly `RawFill` and
    `AggregatedFill` types (RawFill: id, orderId, occSymbol, side "buy"|"sell", qty, price,
    filledAt, commission nullable, fees nullable; AggregatedFill: calendarId, legOccSymbol,
    orderId, sumQty, avgPrice, totalCommission, totalFees, positionEffect, fillIds[]).
    Create `packages/core/src/journal/domain/fill-pairing.ts` with EXPORTED SIGNATURES ONLY
    (throw `new Error("not implemented")` bodies so the type surface compiles but tests fail RED):
    `classifyFill(side, positionEffect): "OPEN"|"CLOSE"|"UNKNOWN"`;
    `aggregatePartialFills(fills: ReadonlyArray<RawFill>): ReadonlyArray<AggregatedFill>`;
    `computePnl(openDebit: number, closeCredit: number, totalFees: number): number`;
    `detectRoll(closing: AggregatedFill, opening: AggregatedFill): boolean`;
    `hashFillIds(ids: ReadonlyArray<string>): string` (will use `node:crypto` createHash sha256).
    Extend `journal/application/ports.ts` with: `ForStoringCalendarEvent`, `ForReadingCalendarEvents`,
    `ForDeletingCalendarEvents`, `ForReadingUnprocessedFills`, `ForReadingCalendarLegs`,
    `ForStoringOrphanFill`, `ForResettingCalendarAmounts`, `ForEnqueueingJob` (all
    `ForVerbingNoun` function types returning `Promise<Result<..., StorageError>>` per the
    existing convention; ForEnqueueingJob returns `Promise<Result<string, StorageError>>` — the
    jobId). Then create these 8 FAILING test stub files (each must fail on an assertion, not an
    import error — import the real signatures so the files compile):
    `journal/domain/fill-pairing.test.ts` (example + fast-check property tests for classifyFill
    completeness, aggregatePartialFills qty round-trip, computePnl monotonicity, detectRoll,
    hashFillIds determinism), `journal/application/syncFills.test.ts`,
    `journal/application/rebuildJournal.test.ts`, `brokerage/application/refreshTokens.test.ts`,
    `apps/worker/src/handlers/sync-fills.test.ts`, `apps/worker/src/handlers/refresh-tokens.test.ts`,
    `apps/worker/src/handlers/rebuild-journal.test.ts`, plus the two contract-test export modules
    `packages/adapters/src/__contract__/calendar-events.contract.ts` and
    `packages/adapters/src/__contract__/orphan-fills.contract.ts` (exporting
    `runCalendarEventsContractTests` / `runOrphanFillsContractTests` describe blocks, analogous to
    calendar-snapshots.contract.ts). Each stub asserts the behavior its later plan must satisfy.
    NO fenced production logic — signatures + throwing stubs only.
  </action>
  <verify>
    <automated>cd packages/core && bun run test src/journal/domain/fill-pairing.test.ts 2>&1 | rg -qiE "fail|not implemented"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "CalendarEventType" packages/core/src/journal/domain/calendar-event.ts` returns >= 1
    - `rg -c "ForStoringCalendarEvent" packages/core/src/journal/application/ports.ts` returns >= 1
    - `rg -c "ForEnqueueingJob" packages/core/src/journal/application/ports.ts` returns >= 1
    - All 5 fill-pairing signatures exported: `rg -c "export function (classifyFill|aggregatePartialFills|computePnl|detectRoll|hashFillIds)" packages/core/src/journal/domain/fill-pairing.ts` returns 5
    - `bun run test src/journal/domain/fill-pairing.test.ts` FAILS (RED) on assertions / "not implemented", not on import errors
    - All 8 Wave-0 stub files exist (fill-pairing.test, syncFills.test, rebuildJournal.test, refreshTokens.test, 3 handler tests, 2 contract modules)
  </acceptance_criteria>
  <done>Domain ADT + new ports + fill-pairing signatures compile; all 8 Wave-0 test stubs exist and fail RED for the right reason, giving downstream TDD plans a baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none crossed in this plan) | This plan is docs + type declarations + failing test stubs. No runtime data flow, no external input parsed, no DB writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | schema.ts fill_ids_hash column width | mitigate | varchar(64) is exactly SHA-256 hex width (RESEARCH Pitfall 7); UNIQUE constraint declared so duplicate-event injection is impossible at the DB layer (enforced when migration applies in 05-02) |
| T-05-02 | Information Disclosure | docs prose | accept | Docs describe token-refresh + fill flows abstractly; no secrets, tokens, or account numbers appear in documentation |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase — RESEARCH Package Legitimacy Audit confirms all deps pre-existing in workspace; no [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
- docs/architecture/data-model.md + jobs.md updated (docs-before-architecture rule satisfied)
- `cd packages/adapters && bunx tsc --noEmit` passes (schema compiles)
- `cd packages/core && bun run test src/journal/domain/fill-pairing.test.ts` fails RED for the right reason
- All 8 Wave-0 stub files present
</verification>

<success_criteria>
- Architecture docs document calendar_events, orphan_fills, entry_thesis, and the 3 new jobs
- schema.ts declares the two new tables + enum + calendars.entryThesis; package typechecks
- CalendarEvent ADT, 8 new ports, and 5 fill-pairing signatures exist and compile
- 8 Wave-0 failing test stubs exist and fail on assertions (TDD RED baseline established)
</success_criteria>

<output>
Create `.planning/phases/05-jobs-fill-rebuild-integrity/05-01-SUMMARY.md` when done.
</output>
