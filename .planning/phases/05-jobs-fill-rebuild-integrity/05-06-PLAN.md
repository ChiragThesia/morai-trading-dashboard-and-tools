---
phase: 05-jobs-fill-rebuild-integrity
plan: 06
type: tdd
wave: 4
depends_on: ["05-02", "05-04"]
autonomous: true
requirements: [JOB-03]
files_modified:
  - packages/adapters/src/postgres/repos/leg-observations.ts
  - packages/adapters/src/__contract__/leg-observations.contract.ts
  - packages/adapters/src/postgres/repos/leg-observations.bsm-drain.contract.test.ts

must_haves:
  truths:
    - "After compute-bsm-greeks drains, SELECT count(*) FROM leg_observations WHERE bsm_iv IS NULL AND mark IS NOT NULL returns 0 (SC3)"
    - "The pending-BSM scan returns every row with bsm_iv IS NULL AND mark IS NOT NULL and no others"
    - "Re-running the drain is idempotent — already-computed rows are not recomputed and no duplicate rows appear"
  artifacts:
    - path: "packages/adapters/src/postgres/repos/leg-observations.bsm-drain.contract.test.ts"
      provides: "testcontainers integration test asserting SC3 (zero pending rows after drain)"
      min_lines: 40
    - path: "packages/adapters/src/__contract__/leg-observations.contract.ts"
      provides: "Extended contract covering the SC3 drain-to-zero assertion"
      contains: "bsm_iv"
  key_links:
    - from: "packages/adapters/src/postgres/repos/leg-observations.ts"
      to: "leg_obs_pending_bsm_idx partial index"
      via: "pending-BSM scan query (bsm_iv IS NULL AND mark IS NOT NULL)"
      pattern: "bsm_iv IS NULL"
---

<objective>
Deliver the compute-bsm-greeks backfill-drain verification slice (JOB-03 / SC3): assert that
after the existing `compute-bsm-greeks` use-case runs, zero `leg_observations` rows remain with
`bsm_iv IS NULL AND mark IS NOT NULL`, and that the drain is idempotent on re-run (D-15).

Purpose: The compute logic was built in Phase 2 and the job is registered in 05-04's schedule.ts.
What is MISSING is the SC3 assertion: a testcontainers integration test proving the drain reaches
zero pending rows and the partial-index scan + upsert are idempotent. This plan closes that
verification gap against the live-schema DB (migration applied in 05-02).

Output: A green SC3 contract test; any pending-scan / upsert repo fixes needed to make it pass.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-jobs-fill-rebuild-integrity/05-CONTEXT.md
@.planning/phases/05-jobs-fill-rebuild-integrity/05-RESEARCH.md
@.planning/phases/05-jobs-fill-rebuild-integrity/05-PATTERNS.md
@packages/adapters/src/postgres/repos/leg-observations.ts
@packages/adapters/src/__contract__/leg-observations.contract.ts
@packages/adapters/src/postgres/schema.ts
@apps/worker/src/handlers/compute-bsm-greeks.ts
</context>

<artifacts_this_phase_produces>
- SC3 drain-to-zero assertion: `leg-observations.bsm-drain.contract.test.ts` (testcontainers)
- Extended `leg-observations.contract.ts` covering the pending-scan completeness + idempotent-upsert contract
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD the SC3 drain-to-zero + idempotent-upsert integration contract (testcontainers)</name>
  <read_first>
    - packages/adapters/src/postgres/repos/leg-observations.ts (the repo under test — pending-scan + upsertComputedGreeks methods)
    - packages/adapters/src/__contract__/leg-observations.contract.ts (the contract being extended)
    - packages/adapters/src/postgres/schema.ts (leg_observations table + leg_obs_pending_bsm_idx partial index, lines 103-138)
    - apps/worker/src/handlers/compute-bsm-greeks.ts (the drain handler — what the test exercises end-to-end)
    - packages/adapters/src/postgres/repos/calendar-snapshots.contract.test.ts (analog — testcontainers + runMigrations harness)
    - .planning/phases/05-jobs-fill-rebuild-integrity/05-RESEARCH.md (§Validation Architecture JOB-03 row, D-15)
  </read_first>
  <behavior>
    - RED first (testcontainers, real Postgres via runMigrations): seed leg_observations with N rows
      where bsm_iv IS NULL AND mark IS NOT NULL plus M rows already computed (bsm_iv NOT NULL) plus
      K rows with mark NULL (must be skipped). Run the compute-drain use-case. Assert
      `SELECT count(*) FROM leg_observations WHERE bsm_iv IS NULL AND mark IS NOT NULL` == 0 (SC3).
      Assert the M already-computed rows are unchanged (not recomputed). Assert the K mark-NULL rows
      remain bsm_iv NULL (correctly skipped). Re-run the drain: assert still 0 pending and no
      duplicate rows (idempotent — D-15). The pending-scan returns exactly the N rows, no others.
  </behavior>
  <action>
    Create `packages/adapters/src/postgres/repos/leg-observations.bsm-drain.contract.test.ts`
    using the testcontainers + runMigrations harness from the calendar-snapshots contract test.
    Extend `packages/adapters/src/__contract__/leg-observations.contract.ts` with a
    `runBsmDrainContract` section asserting: pending-scan completeness (only bsm_iv IS NULL AND
    mark IS NOT NULL rows), drain-to-zero (SC3 count == 0 after run), already-computed rows
    untouched, mark-NULL rows skipped, and idempotent re-run (no duplicates, still zero pending).
    Exercise the actual compute-bsm-greeks use-case path (the existing pending-scan + upsert repo
    methods). If the existing pending-scan or upsert has a defect that prevents drain-to-zero
    (e.g. the partial index predicate or batch chunking), fix it in leg-observations.ts as the
    GREEN step — root-cause only, no band-aids. Do NOT modify the BSM math (Phase 2, already
    calibrated). This is a real-row assertion that depends on the 05-02 migration being applied.
  </action>
  <verify>
    <automated>cd packages/adapters && bun run test src/postgres/repos/leg-observations.bsm-drain.contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test src/postgres/repos/leg-observations.bsm-drain.contract.test.ts` passes (GREEN, testcontainers)
    - The test asserts `SELECT count(*) FROM leg_observations WHERE bsm_iv IS NULL AND mark IS NOT NULL` returns 0 after the drain (SC3)
    - The test asserts mark-NULL rows are skipped (remain bsm_iv NULL)
    - The test re-runs the drain and asserts still 0 pending + no duplicate rows (D-15 idempotency)
    - git log shows test(05-06) before any feat/fix(05-06)
  </acceptance_criteria>
  <done>SC3 is proven by a passing testcontainers contract: the drain empties the pending set, skips mark-NULL rows, leaves computed rows untouched, and is idempotent on re-run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compute-drain → leg_observations (Postgres) | The drain reads pending observations and upserts computed greeks; data is internal (already-persisted CBOE/Schwab chain rows), not fresh external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-15 | Tampering | duplicate-row injection on re-run | mitigate | Upsert on the (time, contract) composite PK + partial-index scan means a re-run recomputes nothing new and inserts no duplicates; the idempotency assertion in the contract test proves it (D-15) |
| T-05-16 | Repudiation | silently-skipped pending rows | mitigate | The pending-scan completeness assertion proves every bsm_iv-NULL/mark-not-NULL row is processed; mark-NULL rows are deliberately and verifiably skipped, not lost |
</threat_model>

<verification>
- `cd packages/adapters && bun run test src/postgres/repos/leg-observations.bsm-drain.contract.test.ts` green
- SC3 assertion (count == 0) present and passing
- RED→GREEN commit sequence for 05-06
</verification>

<success_criteria>
- After compute-bsm-greeks drains, zero rows with bsm_iv IS NULL AND mark IS NOT NULL (SC3)
- Drain is idempotent on re-run (D-15)
- mark-NULL rows correctly skipped; computed rows untouched
</success_criteria>

<output>
Create `.planning/phases/05-jobs-fill-rebuild-integrity/05-06-SUMMARY.md` with RED/GREEN/REFACTOR notes + commit list.
</output>
