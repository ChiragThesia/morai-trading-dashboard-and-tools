---
phase: 05-jobs-fill-rebuild-integrity
plan: 09
type: tdd
wave: 1
depends_on: []
files_modified:
  - docs/architecture/data-model.md
  - docs/architecture/jobs.md
  - packages/core/src/journal/application/ports.ts
  - packages/core/src/journal/domain/fill-pairing.ts
  - packages/core/src/journal/domain/fill-pairing.test.ts
autonomous: true
gap_closure: true
requirements: [JRNL-01]
must_haves:
  truths:
    - "Realized P&L on a CLOSE/ROLL equals closeCredit − originalOpenDebit − feesOnClose (the new leg's premium on a ROLL is cost basis, not realized P&L)"
    - "detectRoll only returns true for same root + strike + type + DIFFERENT expiry"
    - "aggregatePartialFills treats sumQty <= 0 as an error and never emits avgPrice 0"
    - "packages/core imports only @morai/shared — no node crypto bare specifier in fill-pairing.ts"
  artifacts:
    - path: "docs/architecture/data-model.md"
      provides: "Updated D-08/D-09 realized-P&L definition (read original open debit at close)"
      contains: "originalOpenDebit"
    - path: "packages/core/src/journal/application/ports.ts"
      provides: "C1 injected newId + hasher port types; A3 recompute port (interface anchor for downstream plans)"
      contains: "ForRecomputingCalendarAmounts"
    - path: "packages/core/src/journal/domain/fill-pairing.ts"
      provides: "Corrected computePnl, detectRoll, aggregatePartialFills, classifyFill; no crypto import"
      contains: "computeRealizedPnl"
  key_links:
    - from: "packages/core/src/journal/domain/fill-pairing.ts"
      to: "@morai/shared"
      via: "import (only)"
      pattern: "from \"@morai/shared\""
---

<objective>
Close the domain-correctness and boundary gaps that make SC4 economically wrong, and define
the port contracts the rest of the gap round consumes. This is the INTERFACE-ANCHOR plan:
it owns all changes to `packages/core/src/journal/application/ports.ts` so Waves 2-3 build
against a stable contract.

Covers: docs-first D-08/D-09 redefinition (locked decision 2), B1 realized-P&L formula,
B2 detectRoll, B3 aggregatePartialFills, B4 classifyFill/side, C1 boundary (remove
core→node:crypto), and declares the A1/A3 port contracts (no implementations here).

Purpose: SC4's realized P&L must reflect the CLOSED leg's economics, not the new leg's cost
basis. The hexagon must stay pure (CLAUDE.md non-negotiable #1).
Output: corrected pure-domain functions, updated architecture docs, and the full port surface
for the fills data path.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-jobs-fill-rebuild-integrity/05-GAPS.md
@.planning/phases/05-jobs-fill-rebuild-integrity/05-REVIEW.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/tdd.md
@.claude/rules/workflow.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Docs-first D-08/D-09 realized-P&L redefinition + declare data-path port contracts</name>
  <read_first>
    - docs/architecture/data-model.md (lines 100-150 — calendar_events realized_pnl section)
    - docs/architecture/jobs.md (lines 41-101 — sync-fills + rebuild-journal sections)
    - packages/core/src/journal/application/ports.ts (full — existing fills/calendar-event ports)
    - packages/core/src/journal/domain/calendar-event.ts (CalendarEvent, RawFill, AggregatedFill)
  </read_first>
  <action>
    Per workflow.md "Docs Before Code": update docs FIRST, then declare port types.

    DOCS (locked decision 2):
    - In `docs/architecture/data-model.md` `### calendar_events` section, rewrite the
      `realized_pnl` line and add a short paragraph defining realized P&L as
      `realizedPnl = closeCredit − originalOpenDebit − feesOnClose`, where `originalOpenDebit`
      is the debit recorded on the prior OPEN event for the leg being closed. State explicitly
      that on a ROLL the NEW leg's premium is cost basis (`net_amount`), NOT `realized_pnl`.
      Use the literal token `originalOpenDebit` so the must_have grep matches.
    - In `docs/architecture/jobs.md` `## sync-fills` step 6, replace
      `closeCredit − openDebit − totalFees` with the corrected definition referencing the prior
      OPEN event lookup. Add a `## sync-transactions` subsection note that the `fills` table is
      populated from Schwab transactions (the A4 source landing in plan 05-12), so sync-fills
      has real input. Keep Hemingway style (docs.md rule).

    PORTS (interface anchor — these types are consumed by 05-11/05-12/05-13; no impl here):
    In `packages/core/src/journal/application/ports.ts`:
    - Add `ForWritingFills` = `(fills: ReadonlyArray<RawFill>) => Promise<Result<void, StorageError>>`
      (A4 fills-source writer; idempotent on fill id PK at the adapter).
    - Add `ForRecomputingCalendarAmounts` =
      `(calendarId: string) => Promise<Result<void, StorageError>>` (A3 — recompute and WRITE
      `openNetDebit`/`closeNetCredit` from rebuilt calendar_events; distinct from the existing
      `ForResettingCalendarAmounts` which only clears).
    - Add a `ForReadingUnprocessedFillsForCalendar` =
      `(calendarId: string) => Promise<Result<ReadonlyArray<RawFill>, StorageError>>` (A2 —
      calendar-scoped variant so rebuild re-pairs ONLY the target calendar; CR-04).
    - Add port-injection function-type aliases for C1:
      `type NewId = () => string;` and `type HashFillIds = (ids: ReadonlyArray<string>) => string;`
      Export all. Do NOT change the existing `ForReadingUnprocessedFills`,
      `ForReadingCalendarLegs`, `ForResettingCalendarAmounts`, `ForReadingCalendarEvents` —
      they stay (the realized-P&L lookup in 05-11 reuses `ForReadingCalendarEvents`).
    Reference each addition with its work-item tag in a comment (A2/A3/A4/C1).
  </action>
  <verify>
    <automated>rg -q "originalOpenDebit" docs/architecture/data-model.md && rg -q "ForRecomputingCalendarAmounts|ForWritingFills|ForReadingUnprocessedFillsForCalendar" packages/core/src/journal/application/ports.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `docs/architecture/data-model.md` contains the token `originalOpenDebit` and the ROLL
      cost-basis clarification.
    - `docs/architecture/jobs.md` sync-fills step 6 no longer reads `closeCredit − openDebit − totalFees`.
    - `ports.ts` exports `ForWritingFills`, `ForRecomputingCalendarAmounts`,
      `ForReadingUnprocessedFillsForCalendar`, `NewId`, `HashFillIds`.
    - `bun run typecheck` exits 0.
  </acceptance_criteria>
  <done>Docs define the corrected P&L model; all data-path port contracts exist for downstream plans.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix fill-pairing domain (B1 P&L, B2 detectRoll, B3 aggregate, B4 classify, C1 boundary) — RED→GREEN</name>
  <read_first>
    - packages/core/src/journal/domain/fill-pairing.ts (full — current buggy impl)
    - packages/core/src/journal/domain/fill-pairing.test.ts (existing tests that codify the bugs)
    - packages/core/src/journal/domain/bsm.test.ts (fast-check property-test pattern; Math.fround bounds)
    - packages/core/src/journal/application/ports.ts (NewId, HashFillIds added in Task 1)
  </read_first>
  <behavior>
    - computeRealizedPnl(closeCredit, originalOpenDebit, feesOnClose) === closeCredit − originalOpenDebit − feesOnClose.
      Example: closeCredit=500, originalOpenDebit=300, feesOnClose=2 → realizedPnl=198.
      ROLL example: close front for credit 500 (orig open debit 300, close fees 2) AND open back for debit 450
      → realizedPnl=198 (the 450 is cost basis, NOT subtracted from realizedPnl).
    - detectRoll(closing, opening) returns true ONLY when calendarId equal, orderId equal,
      same root+strike+type, and DIFFERENT expiry. Same expiry → false. Different strike/type/root → false.
    - aggregatePartialFills: a group with sumQty <= 0 yields an error sentinel (Result.err or a
      thrown-free flag the use-case maps to orphan), never avgPrice 0. Remove the internal
      re-grouping and placeholder fields (`calendarId: ""`, hardcoded `positionEffect: "UNKNOWN"`);
      accept a pre-bucketed single group whose calendarId/positionEffect the caller supplies.
    - classifyFill: either validate real `fill.side` against positionEffect per the documented
      matrix, or drop the dead `side` param entirely. Pick one and make the test assert it
      (no dead parameter remains).
    - fill-pairing.ts imports ONLY from @morai/shared and intra-domain types — no `crypto`.
      hashFillIds becomes INJECTABLE via the `HashFillIds` port type (Task 1); the use-case
      calls `deps.hashFillIds(...)` (see 05-11 T1) and the adapter supplies the sha256
      implementation (05-13). Do NOT move hashFillIds wholesale into the adapter — the
      injectable path is required so 05-11/05-12/05-13 stay consistent. The pure-domain file
      must not `import { createHash } from "crypto"`.
  </behavior>
  <action>
    RED: extend `fill-pairing.test.ts` with the behaviors above (example + fast-check property
    tests for the P&L formula: monotonic in closeCredit, decreasing in originalOpenDebit/fees).
    Update the existing tests that assert the OLD buggy formula to the corrected one (these are
    the "stale tests codifying bugs" — VERIFICATION superseded_note). Run; confirm RED for the
    right reason. GREEN: rename/replace `computePnl` with `computeRealizedPnl(closeCredit,
    originalOpenDebit, feesOnClose)`; parse OCC structure inside `detectRoll` (carry root/strike/
    type/expiry on `AggregatedFill` or parse `legOccSymbol`) and require different expiry; rewrite
    `aggregatePartialFills` to operate on one pre-bucketed group and error on `sumQty <= 0`;
    resolve `classifyFill` side handling; remove the `crypto` import by making hashFillIds
    INJECTABLE via the `HashFillIds` port (per C1) — the use-case will pass `deps.hashFillIds`,
    the adapter supplies sha256 in 05-13 (do NOT relocate it into the adapter here). Keep
    `hashFillIds`'s pure algorithm available as the reference the adapter implements (sorted
    ids, ':'-join, sha256 hex, 64 chars). Commit at green only (tdd.md).
  </action>
  <verify>
    <automated>bunx vitest run fill-pairing && rg -L "from \"crypto\"|from 'crypto'|node:crypto" packages/core/src/journal/domain/fill-pairing.ts; test $? -ne 0 && bun run lint</automated>
  </verify>
  <acceptance_criteria>
    - `bunx vitest run fill-pairing` passes (example + property tests).
    - `grep -v '^ *\*' packages/core/src/journal/domain/fill-pairing.ts | rg -c "from \"crypto\"|node:crypto"` returns 0 (no crypto import in the pure file).
    - No reference to the old `computePnl(openDebit, closeCredit, totalFees)` signature remains.
    - `detectRoll` returns false for same-expiry different-OCC inputs (asserted in test).
    - `bun run lint` exits 0 (no boundary violation, no dead `side` param flagged).
  </acceptance_criteria>
  <done>SC4's pure-domain economics are correct and the hexagon is pure; downstream use-case (05-11) consumes corrected functions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs ↔ code | docs are source of truth; code that contradicts is a bug (workflow.md) |
| core ↔ adapters | core must import only @morai/shared (CLAUDE.md #1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09-01 | Tampering | fill-pairing P&L math | mitigate | fast-check property tests pin the formula; example test with exact numbers |
| T-05-09-02 | Elevation | core→node:crypto bare specifier | mitigate | remove import; hasher injected (C1); lint boundary rule re-enforced |
| T-05-09-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this plan (RESEARCH §Package Legitimacy Audit: zero new packages) |
</threat_model>

<verification>
- `bunx vitest run fill-pairing` green.
- `bun run typecheck && bun run lint` exit 0.
- `rg "originalOpenDebit" docs/architecture/data-model.md` matches.
</verification>

<success_criteria>
B1-B4 + C1 closed at the domain layer; D-08/D-09 docs corrected; all data-path port contracts
declared. SC4 economics correct in pure code (use-case wiring follows in 05-11).
</success_criteria>

<output>
Create `.planning/phases/05-jobs-fill-rebuild-integrity/05-09-SUMMARY.md` when done
</output>
