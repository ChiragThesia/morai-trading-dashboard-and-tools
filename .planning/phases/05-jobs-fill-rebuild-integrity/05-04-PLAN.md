---
phase: 05-jobs-fill-rebuild-integrity
plan: 04
type: tdd
wave: 3
depends_on: ["05-02"]
autonomous: true
requirements: [JOB-01]
files_modified:
  - packages/core/src/journal/application/enqueueJob.ts
  - packages/core/src/journal/application/enqueueJob.test.ts
  - packages/core/src/journal/domain/dedupe-key.ts
  - packages/core/src/journal/domain/dedupe-key.test.ts
  - packages/adapters/src/pgboss/job-queue.ts
  - packages/adapters/src/memory/job-queue.ts
  - packages/adapters/src/postgres/repos/job-runs.ts
  - apps/worker/src/schedule.ts
  - apps/worker/src/schedule.test.ts
  - apps/worker/src/main.ts

must_haves:
  truths:
    - "All seven jobs are registered in apps/worker/src/schedule.ts (createQueue for 7, schedule cron for 5, work handler for 7)"
    - "GET /api/status lastJobRuns surfaces all seven tracked job names (TRACKED_JOBS extended)"
    - "scheduledDedupeKey returns a deterministic {jobName}:{windowStart} key; the same window produces the same key (JOB-01 idempotency)"
    - "JobQueue port enqueues via pg-boss singletonKey; the in-memory twin has identical semantics"
    - "snapshot-calendars has NO cron schedule (chain-triggered only); rebuild-journal has NO cron (on-demand only)"
  artifacts:
    - path: "apps/worker/src/schedule.ts"
      provides: "registerAllJobs(boss, handlers) — extracted from main.ts; all 7 queues"
      exports: ["registerAllJobs"]
      min_lines: 40
    - path: "packages/adapters/src/pgboss/job-queue.ts"
      provides: "pg-boss adapter implementing ForEnqueueingJob with singletonKey dedupe"
      min_lines: 20
    - path: "packages/adapters/src/memory/job-queue.ts"
      provides: "In-memory JobQueue twin (architecture-boundaries §8)"
      min_lines: 20
    - path: "packages/core/src/journal/domain/dedupe-key.ts"
      provides: "scheduledDedupeKey + rebuildDedupeKey pure functions"
      exports: ["scheduledDedupeKey", "rebuildDedupeKey"]
  key_links:
    - from: "apps/worker/src/main.ts"
      to: "apps/worker/src/schedule.ts"
      via: "import { registerAllJobs } and call it; inline queue/schedule/work blocks removed"
      pattern: "registerAllJobs"
    - from: "packages/adapters/src/postgres/repos/job-runs.ts"
      to: "GET /api/status lastJobRuns"
      via: "TRACKED_JOBS extended to 7 names + WHERE name IN (...) updated"
      pattern: "sync-fills"
---

<objective>
Deliver the jobs backbone vertical slice (JOB-01 / SC1): a `JobQueue` port + pg-boss adapter +
in-memory twin, a `dedupe-key` domain module, an extracted `schedule.ts` registering all seven
jobs behind deterministic dedupe keys, the `job-runs.ts` `TRACKED_JOBS` extension so
`/api/status` lastJobRuns surfaces every job, and the `main.ts` rewire to call `registerAllJobs`.

Purpose: This is the first end-to-end user-observable slice — after it, `GET /api/status`
lastJobRuns lists all seven jobs and duplicate enqueues within a window are idempotent (no
duplicate rows). It owns the shared composition root (schedule.ts + main.ts) so later
handler-adding plans only edit their own files plus a small main.ts wiring line in their own wave.

Output: A queryable seven-job status surface; the JobQueue port that trigger_job (05-08) and
sync-fills (05-07) enqueue through.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-jobs-fill-rebuild-integrity/05-CONTEXT.md
@.planning/phases/05-jobs-fill-rebuild-integrity/05-RESEARCH.md
@.planning/phases/05-jobs-fill-rebuild-integrity/05-PATTERNS.md
@apps/worker/src/main.ts
@apps/worker/src/handlers/compute-bsm-greeks.ts
@apps/worker/src/handlers/fetch-schwab-chain.ts
@packages/adapters/src/postgres/repos/job-runs.ts
@packages/core/src/journal/application/ports.ts
@docs/architecture/jobs.md
</context>

<artifacts_this_phase_produces>
- `ForEnqueueingJob` implementations: `makePgBossJobQueue` (adapter) + `makeMemoryJobQueue` (twin)
- `apps/worker/src/schedule.ts` exporting `registerAllJobs(boss, handlers)` — all 7 queues, 5 crons
- `dedupe-key.ts`: `scheduledDedupeKey(jobName, now, windowMinutes)` + `rebuildDedupeKey(calendarId)`
- `makeEnqueueJobUseCase` (returns ForEnqueueingJob) — used by trigger_job in 05-08
- `job-runs.ts` TRACKED_JOBS extended to 7 names (fetch-cboe-chain → fetch-schwab-chain rename per RESEARCH State of the Art)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD dedupe-key domain + JobQueue port, pg-boss adapter, and in-memory twin</name>
  <read_first>
    - packages/core/src/journal/application/ports.ts (ForEnqueueingJob declared in 05-01)
    - apps/worker/src/main.ts (existing boss.send + singletonKey usage — the pg-boss API surface to wrap)
    - packages/adapters/src/memory/calendar-snapshots.ts (analog — in-memory twin structure + seed/getAll helpers)
    - .planning/phases/05-jobs-fill-rebuild-integrity/05-RESEARCH.md (Pattern 1 dedupe keys, Pitfall 1 singletonKey vs singletonSeconds)
    - .planning/phases/05-jobs-fill-rebuild-integrity/05-PATTERNS.md (§Port naming, §onConflictDoNothing)
  </read_first>
  <behavior>
    - dedupe-key (pure, RED first): scheduledDedupeKey("sync-fills", now, 10) ==
      "sync-fills:" + windowStart.toISOString() where windowStart floors now to the 10-min
      boundary. Same now within a window → identical key. rebuildDedupeKey(id) == "rebuild-journal:"+id.
      Property: two times in the same window produce the same key; adjacent windows differ.
    - JobQueue twin (RED first): makeMemoryJobQueue().enqueue(name, payload, dedupeKey) records
      the enqueue; a second enqueue with the SAME dedupeKey is a no-op (returns the existing
      jobId, no duplicate) — mirrors pg-boss singletonKey semantics. Returns Result<string, StorageError>.
  </behavior>
  <action>
    Create `packages/core/src/journal/domain/dedupe-key.ts` with `scheduledDedupeKey(jobName: string,
    now: Date, windowMinutes: number): string` (floor now to the window boundary, format as
    `${jobName}:${windowStart.toISOString()}` per RESEARCH Pattern 1) and
    `rebuildDedupeKey(calendarId: string): string`. Create
    `packages/adapters/src/pgboss/job-queue.ts` exporting `makePgBossJobQueue(boss): ForEnqueueingJob`
    that calls `boss.send(name, payload, { singletonKey: dedupeKey })` (singletonKey, NEVER
    singletonSeconds — Pitfall 1) and returns `ok(jobId)` or wraps errors as StorageError. Create
    `packages/adapters/src/memory/job-queue.ts` exporting `makeMemoryJobQueue(): ForEnqueueingJob`
    backed by a `Map<dedupeKey, jobId>` so a repeated dedupeKey is a no-op (returns the existing
    jobId); expose `getAll()` for test assertions. Both implement the identical port signature.
  </action>
  <verify>
    <automated>cd packages/core && bun run test src/journal/domain/dedupe-key.test.ts && cd ../adapters && bunx tsc --noEmit -p tsconfig.json 2>&1 | rg -v "\.test\.ts" | rg -c "error TS" | rg -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test src/journal/domain/dedupe-key.test.ts` passes (GREEN)
    - `rg -c "singletonKey" packages/adapters/src/pgboss/job-queue.ts` returns >= 1
    - `rg -c "singletonSeconds" packages/adapters/src/pgboss/job-queue.ts` returns 0 (Pitfall 1)
    - memory twin treats repeated dedupeKey as a no-op (test asserts getAll() length unchanged on re-enqueue)
    - `packages/adapters` typecheck passes (0 non-test errors)
    - git log shows test(05-04) before feat(05-04)
  </acceptance_criteria>
  <done>dedupe-key module green; pg-boss adapter + in-memory twin implement ForEnqueueingJob with singletonKey dedupe and identical semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: makeEnqueueJobUseCase + extend job-runs TRACKED_JOBS to seven jobs</name>
  <read_first>
    - packages/adapters/src/postgres/repos/job-runs.ts (the file being modified — TRACKED_JOBS line 8, WHERE name IN line 85)
    - packages/core/src/journal/application/snapshotCalendars.ts (analog — use-case factory shape)
    - .planning/phases/05-jobs-fill-rebuild-integrity/05-PATTERNS.md (§job-runs.ts modified — the exact 7-name list, fetch-cboe-chain → fetch-schwab-chain rename)
    - .planning/phases/05-jobs-fill-rebuild-integrity/05-RESEARCH.md (§lastJobRuns Extension, State of the Art deprecation note)
  </read_first>
  <behavior>
    - enqueueJob use-case (RED first): makeEnqueueJobUseCase({ jobQueue, now }) returns
      ForEnqueueingJob; given a triggerable job name + payload it builds the dedupe key (scheduled
      key for scheduled jobs via scheduledDedupeKey, rebuildDedupeKey for rebuild-journal) and
      delegates to the jobQueue port; returns the jobId. Test with the in-memory twin: a repeated
      enqueue in the same window returns the same jobId (no duplicate).
  </behavior>
  <action>
    Create `packages/core/src/journal/application/enqueueJob.ts` exporting `makeEnqueueJobUseCase`
    that composes the dedupe-key domain fns with the `ForEnqueueingJob` jobQueue port (the use-case
    chooses the right dedupe key per job name — rebuildDedupeKey when name is "rebuild-journal",
    scheduledDedupeKey otherwise). In `packages/adapters/src/postgres/repos/job-runs.ts` replace
    the `TRACKED_JOBS` constant with the seven-name list: fetch-schwab-chain (RENAMED from
    fetch-cboe-chain per RESEARCH State of the Art), fetch-rates, compute-bsm-greeks,
    snapshot-calendars, sync-fills, refresh-tokens, rebuild-journal. Update the inline SQL
    `WHERE name IN (...)` on the existing query to the same seven names. Do not change the
    DISTINCT ON query shape — it already returns the most-recent row per job.
  </action>
  <verify>
    <automated>cd packages/core && bun run test src/journal/application/enqueueJob.test.ts && rg -q "sync-fills" packages/adapters/src/postgres/repos/job-runs.ts && rg -q "refresh-tokens" packages/adapters/src/postgres/repos/job-runs.ts && rg -q "fetch-schwab-chain" packages/adapters/src/postgres/repos/job-runs.ts && test "$(rg -c 'fetch-cboe-chain' packages/adapters/src/postgres/repos/job-runs.ts || echo 0)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test src/journal/application/enqueueJob.test.ts` passes (GREEN)
    - TRACKED_JOBS contains all 7 names: fetch-schwab-chain, fetch-rates, compute-bsm-greeks, snapshot-calendars, sync-fills, refresh-tokens, rebuild-journal
    - `rg -c "fetch-cboe-chain" packages/adapters/src/postgres/repos/job-runs.ts` returns 0 (renamed)
    - The inline SQL `WHERE name IN (...)` lists the same 7 names
    - git log shows test(05-04) before feat(05-04)
  </acceptance_criteria>
  <done>enqueueJob use-case green; job-runs TRACKED_JOBS + SQL extended to all seven jobs so /api/status lastJobRuns surfaces them.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extract schedule.ts (registerAllJobs) and rewire main.ts</name>
  <read_first>
    - apps/worker/src/main.ts (the file being modified — inline createQueue/schedule/work blocks to extract)
    - apps/worker/src/schedule.test.ts (RED stub — assert 7 createQueue calls, 5 schedule calls, snapshot-calendars + rebuild-journal have no cron)
    - .planning/phases/05-jobs-fill-rebuild-integrity/05-PATTERNS.md (§schedule.ts — registerAllJobs signature, createQueue→schedule→work order, cron strings)
    - .planning/phases/05-jobs-fill-rebuild-integrity/05-RESEARCH.md (§schedule.ts Extraction, Pitfall 2 snapshot-calendars not re-scheduled, Anti-Patterns)
  </read_first>
  <behavior>
    - schedule.test (RED first): registerAllJobs(fakeBoss, fakeHandlers) calls createQueue for all
      7 queue names; schedule for exactly 5 (fetch-schwab-chain, fetch-rates, compute-bsm-greeks,
      sync-fills, refresh-tokens) — NOT snapshot-calendars (chain-triggered, Pitfall 2) and NOT
      rebuild-journal (on-demand). sync-fills cron is "*/10 9-16 * * 1-5" tz America/New_York;
      refresh-tokens cron is "0 4 * * *" tz America/New_York. createQueue precedes schedule/work.
      Use a fake boss recording calls (no real pg-boss / Docker).
  </behavior>
  <action>
    Create `apps/worker/src/schedule.ts` exporting `registerAllJobs(boss: PgBoss, handlers:
    AllHandlers): Promise<void>` that runs createQueue → schedule → work in that order, extracted
    from main.ts. createQueue for all 7: fetch-schwab-chain, fetch-rates, compute-bsm-greeks,
    snapshot-calendars, sync-fills, refresh-tokens, rebuild-journal. schedule for 5 only
    (sync-fills "*/10 9-16 * * 1-5"; refresh-tokens "0 4 * * *"; the 3 existing crons preserved
    from main.ts). Add the comment `// snapshot-calendars: NO schedule — chain-triggered only by
    compute-bsm-greeks (D-03 / Pitfall 2)` and `// rebuild-journal: NO schedule — on-demand via
    trigger_job`. work() registers all 7 handlers (the handlers struct includes syncFills,
    refreshTokens, rebuildJournal entries; their factories are wired in their own plans' main.ts
    edits — for this plan, wire the existing 4 handlers + accept the 3 new ones as optional/typed
    slots so schedule.ts compiles). Rewire `main.ts`: import and call `registerAllJobs`; delete
    the inline createQueue/schedule/work blocks. Compose `makePgBossJobQueue(boss)` in main.ts so
    the enqueue path is live.
  </action>
  <verify>
    <automated>cd apps/worker && bun run test src/schedule.test.ts && rg -q "registerAllJobs" apps/worker/src/main.ts && rg -q "snapshot-calendars: NO schedule" apps/worker/src/schedule.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test src/schedule.test.ts` passes (GREEN) — asserts 7 createQueue, 5 schedule
    - schedule.ts does NOT call boss.schedule for snapshot-calendars or rebuild-journal
    - main.ts imports registerAllJobs and no longer contains inline boss.createQueue blocks for the original jobs
    - `rg -c "makePgBossJobQueue" apps/worker/src/main.ts` returns >= 1
    - git log shows test(05-04) before feat(05-04) for the schedule extraction
  </acceptance_criteria>
  <done>schedule.ts registers all 7 jobs (5 cron, snapshot-calendars + rebuild-journal cronless); main.ts delegates to registerAllJobs and composes the pg-boss JobQueue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scheduler → pg-boss → Postgres | Job enqueues cross into the queue; dedupe keys are internally generated, not user-supplied here (user-supplied trigger_job comes in 05-08) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Tampering | duplicate job enqueue | mitigate | pg-boss singletonKey with deterministic scheduledDedupeKey guarantees at-most-one job per window (RESEARCH Pitfall 1); in-memory twin enforces the same in tests |
| T-05-09 | Denial of Service | snapshot-calendars double-scheduling | mitigate | schedule.ts explicitly omits a cron for snapshot-calendars (chain-triggered) and rebuild-journal (on-demand) — asserted by schedule.test (RESEARCH Pitfall 2) |
| T-05-10 | Information Disclosure | job payload logging | accept | Scheduled job payloads here are null/calendarId only — no secrets; refresh-tokens payload is empty (token handling is plan 05-05's boundary) |
</threat_model>

<verification>
- `cd packages/core && bun run test src/journal/domain/dedupe-key.test.ts src/journal/application/enqueueJob.test.ts` green
- `cd apps/worker && bun run test src/schedule.test.ts` green
- `cd packages/adapters && bunx tsc --noEmit` passes
- job-runs TRACKED_JOBS lists all 7 jobs; fetch-cboe-chain removed
</verification>

<success_criteria>
- JobQueue port + pg-boss adapter + in-memory twin with singletonKey dedupe (JOB-01)
- schedule.ts registers all 7 jobs; snapshot-calendars + rebuild-journal cronless
- /api/status lastJobRuns surfaces all 7 jobs (TRACKED_JOBS extended) — SC1
- main.ts delegates scheduling to registerAllJobs
</success_criteria>

<output>
Create `.planning/phases/05-jobs-fill-rebuild-integrity/05-04-SUMMARY.md` with RED/GREEN/REFACTOR notes + commit list.
</output>
