---
phase: 05-jobs-fill-rebuild-integrity
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
autonomous: false
requirements: [JRNL-01]
files_modified:
  - packages/adapters/src/postgres/migrations/0004_calendar_events.sql
  - packages/adapters/src/postgres/migrations/meta/_journal.json

must_haves:
  truths:
    - "bunx drizzle-kit generate emits a SQL migration creating calendar_events, orphan_fills, the calendar_event_type enum, and the calendars.entry_thesis column"
    - "bun run migrate applies the migration to the live DATABASE_URL; calendar_events, orphan_fills exist and calendars has an entry_thesis column"
    - "A second bun run migrate run is a no-op (idempotent)"
  artifacts:
    - path: "packages/adapters/src/postgres/migrations/0004_calendar_events.sql"
      provides: "Generated DDL for the two new tables, the enum, and the calendars ALTER"
      contains: "calendar_events"
  key_links:
    - from: "packages/adapters/src/postgres/migrations/0004_calendar_events.sql"
      to: "packages/adapters/src/postgres/schema.ts"
      via: "drizzle-kit generate diff against schema declarations from 05-01"
      pattern: "calendar_events"
---

<objective>
[BLOCKING] Generate and apply the Drizzle migration for the schema changes declared in plan
05-01, so the live Supabase database actually has the `calendar_events` and `orphan_fills`
tables, the `calendar_event_type` enum, and the `calendars.entry_thesis` column BEFORE any
plan verifies against real rows (SC3 / SC4 / SC5 read live data).

Purpose: `tsc` and the test build pass against types inferred from schema.ts even when the
live DB lacks the table — a false-positive verification state. This task closes that gap. The
phase CANNOT pass verification without it.

Output: `0004_calendar_events.sql` migration file (reviewed + committed) and the migration
applied to the live database.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-jobs-fill-rebuild-integrity/05-RESEARCH.md
@packages/adapters/src/postgres/schema.ts
@apps/worker/src/migrate.ts
@drizzle.config.ts
</context>

<artifacts_this_phase_produces>
- `packages/adapters/src/postgres/migrations/0004_calendar_events.sql` — DDL creating `calendar_events`, `orphan_fills`, `calendar_event_type` enum, and `ALTER TABLE calendars ADD COLUMN entry_thesis text`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Generate the Drizzle migration and review the emitted DDL</name>
  <read_first>
    - drizzle.config.ts (generate config — schema path + migrations output dir)
    - packages/adapters/src/postgres/schema.ts (the schema diffed against — 05-01 declarations)
    - packages/adapters/src/postgres/migrations/meta/_journal.json (the migration journal that gets appended)
    - .planning/phases/05-jobs-fill-rebuild-integrity/05-RESEARCH.md (§Migration Flow, §Destructive-change caveat)
  </read_first>
  <action>
    From `packages/adapters/`, run `bunx drizzle-kit generate`. It diffs schema.ts against the
    existing migration journal and emits `0004_*.sql` in
    `packages/adapters/src/postgres/migrations/`. Rename the generated file to
    `0004_calendar_events.sql` if drizzle-kit assigns a random adjective name (follow the Phase 4
    precedent where `0003_famous_azazel` was renamed to `0003_broker_tokens` and the journal tag
    updated to match). Review the emitted SQL: it MUST contain `CREATE TYPE ... calendar_event_type`,
    `CREATE TABLE ... calendar_events`, `CREATE TABLE ... orphan_fills`, the UNIQUE constraint on
    `fill_ids_hash`, and `ALTER TABLE "calendars" ADD COLUMN "entry_thesis"`. All changes are
    additive (new tables + new nullable column + new enum) — confirm NO destructive statements
    (no DROP/RENAME) appear. If drizzle-kit emits an interactive prompt for a column rename/drop,
    STOP — that indicates an unexpected destructive diff; do not auto-confirm.
  </action>
  <verify>
    <automated>rg -q "calendar_events" packages/adapters/src/postgres/migrations/0004_calendar_events.sql && rg -q "orphan_fills" packages/adapters/src/postgres/migrations/0004_calendar_events.sql && rg -q "entry_thesis" packages/adapters/src/postgres/migrations/0004_calendar_events.sql && ! rg -qi "DROP TABLE|DROP COLUMN|RENAME" packages/adapters/src/postgres/migrations/0004_calendar_events.sql</automated>
  </verify>
  <acceptance_criteria>
    - `0004_calendar_events.sql` exists and contains CREATE TABLE statements for calendar_events and orphan_fills
    - The migration contains `CREATE TYPE` for calendar_event_type and `ALTER TABLE "calendars" ADD COLUMN "entry_thesis"`
    - The UNIQUE constraint on fill_ids_hash is present
    - NO destructive statements (DROP/RENAME) in the file
    - meta/_journal.json has a new entry referencing the 0004 migration
  </acceptance_criteria>
  <done>0004_calendar_events.sql generated, reviewed, additive-only, and journal updated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Apply the migration to the live database</name>
  <what-built>
    `bun run migrate` (apps/worker/src/migrate.ts → runMigrations over DATABASE_URL) was run to
    apply 0004_calendar_events.sql to the live Supabase Postgres. This adds calendar_events,
    orphan_fills, the calendar_event_type enum, and calendars.entry_thesis to the live DB. Note:
    the Phase 4 STATE.md flagged a pending broker_tokens migration — confirm the migrator is
    pointed at the correct live DATABASE_URL and that earlier migrations are applied first.
  </what-built>
  <how-to-verify>
    1. Run `bun run migrate` from the repo root. Expect it to apply 0004 and log success.
    2. Run `bun run migrate` a SECOND time — expect a no-op (no errors, no re-apply).
    3. Confirm the tables exist (psql or Supabase SQL editor):
       `SELECT to_regclass('public.calendar_events'), to_regclass('public.orphan_fills');`
       — both must be non-null.
    4. Confirm the column: `SELECT column_name FROM information_schema.columns WHERE table_name='calendars' AND column_name='entry_thesis';` — must return one row.
    5. Confirm the enum: `SELECT 1 FROM pg_type WHERE typname='calendar_event_type';` — must return one row.
  </how-to-verify>
  <acceptance_criteria>
    - `to_regclass('public.calendar_events')` IS NOT NULL
    - `to_regclass('public.orphan_fills')` IS NOT NULL
    - information_schema shows calendars.entry_thesis exists
    - pg_type contains calendar_event_type
    - Second `bun run migrate` is a clean no-op
  </acceptance_criteria>
  <resume-signal>Type "applied" once the five live-DB checks pass, or describe the error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migrator → live Postgres | DDL applied to the production Supabase DB; DATABASE_URL (direct connection, not pooler) carries the credential |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | DDL migration content | mitigate | Migration reviewed for additive-only changes before apply; verify command asserts no DROP/RENAME; idempotent re-run check confirms no double-apply |
| T-05-04 | Denial of Service | live DB during ALTER | accept | `ALTER TABLE calendars ADD COLUMN entry_thesis` is a nullable add (metadata-only in Postgres, no table rewrite); new-table creates lock nothing existing |
| T-05-05 | Information Disclosure | DATABASE_URL credential | mitigate | DATABASE_URL read only in the composition root / migrator from env; never logged; not committed (carried Phase 1 DATA-04 pattern) |
</threat_model>

<verification>
- `bunx drizzle-kit generate` produced an additive 0004 migration
- `bun run migrate` applied cleanly; second run is a no-op
- Live DB has calendar_events, orphan_fills, calendar_event_type, calendars.entry_thesis
</verification>

<success_criteria>
- 0004_calendar_events.sql committed (additive-only)
- Live database has the two new tables, the enum, and the entry_thesis column
- Migration is idempotent (second run no-op)
</success_criteria>

<output>
Create `.planning/phases/05-jobs-fill-rebuild-integrity/05-02-SUMMARY.md` when done.
</output>
