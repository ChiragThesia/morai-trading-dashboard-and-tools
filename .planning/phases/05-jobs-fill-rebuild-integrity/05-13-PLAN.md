---
phase: 05-jobs-fill-rebuild-integrity
plan: 13
type: tdd
wave: 3
depends_on: ["05-10", "05-11", "05-12"]
files_modified:
  - apps/worker/src/main.ts
  - apps/worker/src/schedule.ts
  - apps/worker/src/handlers/sync-transactions.ts
  - apps/worker/src/handlers/sync-transactions.test.ts
  - packages/core/src/journal/application/rebuildJournal.ts
  - packages/core/src/journal/application/rebuildJournal.test.ts
autonomous: true
gap_closure: true
requirements: [JRNL-01, JOB-01]
must_haves:
  truths:
    - "The worker composition root wires the REAL fills repo + sync-transactions source — no readUnprocessedFills/readCalendarLegs/resetCalendarAmounts/syncFillsForCalendar stubs remain"
    - "rebuild-journal deletes events, recomputes calendar amounts from the rebuilt events, and re-pairs ONLY the target calendar via the scoped sync"
    - "After a rebuild, calendars.openNetDebit/closeNetCredit are non-null and match the summed calendar_events"
    - "An end-to-end test exercises fills → sync-fills → events → rebuild against the real repo (SC4 + SC5)"
  artifacts:
    - path: "apps/worker/src/main.ts"
      provides: "A5 real wiring of fills repo + sync-transactions + scoped rebuild"
      contains: "makePostgresFillsRepo"
    - path: "packages/core/src/journal/application/rebuildJournal.ts"
      provides: "WR-08 recompute-amounts reconciliation step"
      contains: "recomputeCalendarAmounts"
  key_links:
    - from: "apps/worker/src/main.ts"
      to: "packages/adapters/src/postgres/repos/fills.ts"
      via: "makePostgresFillsRepo wiring into syncFills + rebuildJournal"
      pattern: "makePostgresFillsRepo"
    - from: "packages/core/src/journal/application/rebuildJournal.ts"
      to: "ForRecomputingCalendarAmounts"
      via: "final reconciliation step"
      pattern: "recomputeCalendarAmounts"
---

<objective>
Wire the real data path end-to-end (A5) and add the rebuild reconciliation step (WR-08), then
prove SC4 + SC5 against the real repo. Depends on the corrected use-case (05-11), the real
fills repo + source (05-12), and the worker main.ts IN-01 cleanup (05-10).

Purpose: this is the plan that turns "passes against stubs" into "verified against real data".
It deletes the worker stubs, wires `makePostgresFillsRepo`, injects `newId`/`hashFillIds`, adds
a `sync-transactions` job to populate fills, and makes rebuild recompute the calendar aggregates
so SC5 reconciliation actually holds.
Output: a composition root with no fills stubs, a reconciling rebuild, and an end-to-end test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-jobs-fill-rebuild-integrity/05-GAPS.md
@.planning/phases/05-jobs-fill-rebuild-integrity/05-REVIEW.md
@.planning/phases/05-jobs-fill-rebuild-integrity/05-VERIFICATION.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/tdd.md
@apps/worker/src/main.ts
@packages/core/src/journal/application/rebuildJournal.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WR-08 rebuild reconciliation — recompute calendar amounts + scoped re-pair — RED→GREEN</name>
  <read_first>
    - packages/core/src/journal/application/rebuildJournal.ts (full — current 3-step shape)
    - packages/core/src/journal/application/rebuildJournal.test.ts (existing cases)
    - packages/core/src/journal/application/ports.ts (ForRecomputingCalendarAmounts, ForResettingCalendarAmounts)
    - packages/core/src/journal/application/syncFills.ts (scoped sync driver type from 05-11)
  </read_first>
  <behavior>
    - RebuildJournalDeps gains `recomputeCalendarAmounts: ForRecomputingCalendarAmounts`.
    - rebuildJournal steps become: (1) deleteCalendarEvents(calendarId); (2) resetCalendarAmounts(calendarId)
      → NULL; (3) syncFillsForCalendar(calendarId) (the SCOPED sync from 05-11, NOT the full sweep);
      (4) recomputeCalendarAmounts(calendarId) → writes openNetDebit/closeNetCredit from the rebuilt events.
      Any step's err short-circuits and propagates.
    - After rebuild, the calendar's amounts are non-null and equal the summed calendar_events (SC5 reconciliation).
  </behavior>
  <action>
    RED: extend rebuildJournal.test.ts (in-memory twins) to assert the new 4-step order: a spy/sequence
    proving recomputeCalendarAmounts is called AFTER syncFillsForCalendar, and that final amounts are
    non-null and match the events the scoped sync produced. Assert syncFillsForCalendar (scoped) is used,
    not a full sweep. Run; confirm RED.
    GREEN: add `recomputeCalendarAmounts` to RebuildJournalDeps and append step 4; ensure step 3 uses the
    scoped driver type from 05-11. Commit at green.
  </action>
  <verify>
    <automated>bunx vitest run rebuildJournal && rg -q "recomputeCalendarAmounts" packages/core/src/journal/application/rebuildJournal.ts</automated>
  </verify>
  <acceptance_criteria>
    - rebuildJournal.test asserts call order: delete → reset → scoped sync → recompute.
    - rebuildJournal.test asserts post-rebuild amounts non-null and equal to summed events.
    - `rg -c "recomputeCalendarAmounts" packages/core/src/journal/application/rebuildJournal.ts` ≥ 1.
    - `bun run typecheck && bun run lint` exit 0.
  </acceptance_criteria>
  <done>WR-08 closed in the use-case: rebuild repopulates calendar aggregates; SC5 reconciliation can hold.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: A5 real wiring + sync-transactions job + end-to-end SC4/SC5 — RED→GREEN</name>
  <read_first>
    - apps/worker/src/main.ts (lines 165-289 — stubs at 204-208, 266-267; registerAllJobs)
    - apps/worker/src/schedule.ts (queue/schedule/work registration; add sync-transactions)
    - apps/worker/src/handlers/sync-fills.ts (handler analog for the new sync-transactions handler)
    - packages/adapters/src/postgres/repos/fills.ts (the real repo from 05-12)
    - packages/core/src/journal/application/syncTransactions.ts (the source use-case from 05-12)
    - packages/adapters/src/index.ts (exports for makePostgresFillsRepo)
  </read_first>
  <behavior>
    - main.ts: construct `const fillsRepo = makePostgresFillsRepo(db);`. Wire syncFillsUseCase with the
      REAL `readUnprocessedFills`, `readCalendarLegs`, `readCalendarEvents` (calendar-events repo),
      `resetCalendarAmounts`, `newId: () => randomUUID()` (adapter supplies node crypto here — adapters
      MAY use node), `hashFillIds` (adapter-supplied hasher). Delete the three async stub literals
      (readUnprocessedFills `[]`, readCalendarLegs `[]`, resetCalendarAmounts no-op).
    - main.ts: wire rebuildJournalUseCase with the REAL deleteCalendarEvents, resetCalendarAmounts,
      recomputeCalendarAmounts, and `syncFillsForCalendar` = the SCOPED sync (05-11) over the real
      per-calendar fills reader. Remove `syncFillsForCalendar: async (_id) => syncFillsUseCase()` and
      `resetCalendarAmounts: async () => ok(undefined)` stubs (CR-04 + WR-08).
    - sync-transactions job: add a `sync-transactions` queue + RTH-gated handler that calls
      makeSyncTransactionsUseCase (05-12) to populate fills before sync-fills runs; register in schedule.ts
      and TRACKED_JOBS is NOT required to change (it is an ingestion job — document if surfaced).
      (If the team prefers, fold the source call into the sync-fills handler instead; pick one and wire it.)
    - End-to-end test (in-memory twins, no Docker): seed BrokerTransaction[] → syncTransactions writes fills
      → syncFills pairs into events with correct realized P&L (SC4) → rebuildJournal re-pairs + recomputes;
      assert events identical and amounts reconcile (SC5).
  </behavior>
  <action>
    RED: write the end-to-end test (worker or core integration using in-memory twins for fills/events/
    orphans) covering the seed→source→pair→rebuild flow asserting SC4 (realized P&L numbers) and SC5
    (post-rebuild amounts match events). Add `sync-transactions.test.ts` for the handler (array-guard,
    RTH gate, delegates to use-case). Run; confirm RED.
    GREEN: edit `apps/worker/src/main.ts` to construct the fills repo and replace ALL fills stubs with
    real ports + injected `newId`/`hashFillIds`; add the sync-transactions handler + schedule wiring;
    update the startup `console.warn` job count/list. Run the full suite. Commit at green.
  </action>
  <verify>
    <automated>rg -q "ok: true as const, value: \[\]|resetCalendarAmounts: async \(_calendarId\) => \(\{ ok: true" apps/worker/src/main.ts; test $? -ne 0 && bunx vitest run sync-transactions && bun run typecheck && bun run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg "value: \[\]|async \(_calendarId\) => \(\{ ok: true as const, value: undefined" apps/worker/src/main.ts` returns nothing (no fills stubs remain).
    - `rg -c "makePostgresFillsRepo" apps/worker/src/main.ts` ≥ 1.
    - The end-to-end test asserts SC4 realized-P&L numbers and SC5 amount reconciliation against the real repo path; green.
    - `sync-transactions` handler test green (RTH gate + delegation).
    - `bun run test` full suite green; `bun run typecheck && bun run lint` exit 0.
  </acceptance_criteria>
  <done>A5 + WR-08 closed end-to-end: no stubs, real fills source + repo, reconciling rebuild; SC4/SC5 verifiable for real.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composition root → repos | wiring errors silently disable the data path (the original gap) |
| adapter → node crypto | adapters MAY use node:crypto; core stays pure (newId/hashFillIds injected here) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13-01 | Tampering | worker fills stubs | mitigate | stubs deleted; real repo wired; grep gate proves absence (A5) |
| T-05-13-02 | Tampering | rebuild leaves amounts NULL | mitigate | recompute step writes totals; end-to-end test asserts reconciliation (WR-08/SC5) |
| T-05-13-03 | Repudiation | sync-transactions ingestion | mitigate | deterministic ids + idempotent writeFills; handler RTH-gated and logged |
| T-05-13-SC | Tampering | npm/pip/cargo installs | accept | no package installs (RESEARCH §Package Legitimacy Audit: zero new packages) |
</threat_model>

<verification>
- `bunx vitest run sync-transactions rebuildJournal` green.
- end-to-end SC4/SC5 test green.
- `rg "value: \[\]" apps/worker/src/main.ts` returns nothing in the fills wiring.
- `bun run test && bun run typecheck && bun run lint` all pass.
</verification>

<success_criteria>
A5 + WR-08 closed. SC4 (correct realized P&L on real fills) and SC5 (post-rebuild reconciliation)
verifiable against the real repo + source, not stubs. Phase 05 gaps closed end-to-end.
</success_criteria>

<output>
Create `.planning/phases/05-jobs-fill-rebuild-integrity/05-13-SUMMARY.md` when done
</output>
