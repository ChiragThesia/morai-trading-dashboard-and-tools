---
phase: 16-deploy-phase-15-image
plan: 03
type: execute
wave: 3
depends_on: [16-02]
files_modified: []
autonomous: false
requirements: [DEPLOY-04]
must_haves:
  truths:
    - "get_status shows db ok and both Schwab apps' auth state; the only errors are the two pre-existing baseline errors, not new deploy regressions"
    - "get_journal returns a fresh 30-min RTH snapshot timestamp"
    - "get_cot returns the latest weekly row"
    - "FRED macro series are present via get_macro"
    - "The web dashboard loads: positions render and GEX charts populate"
    - "The T-24h alert surface is live: refreshExpiresIn key on /api/status AND on the web status surface; AuthExpiredBanner ships in the deployed web bundle"
  artifacts:
    - "Post-deploy smoke-checklist result (MCP/curl) in SUMMARY, diffed against the Plan 01 baseline"
    - "Alert-surface checkpoint-1 confirmation in SUMMARY"
    - "Two scheduled follow-up items recorded: checkpoint-2 (~2026-07-08 live amber banner) and the RTH-bound live-stream check"
  key_links:
    - "Regression judgment compares post-deploy lastJobRuns against the Plan 01 pre-deploy baseline — the two known errors must not be misread as deploy regressions (Pitfall 5)"
    - "The amber banner only renders inside the real T-24h window and cannot be forced (D-02) — its live observation is a scheduled follow-up, not a same-day gate"
---

<objective>
Prove the deploy introduced no regression (DEPLOY-04 criterion 3) and that the T-24h re-auth alert
surface is live (DEPLOY-04 criterion 2, checkpoint 1).

Per D-04, run a manual smoke checklist over the existing MCP tools + a web eyeball — no new
scripted smoke suite. Per RESEARCH Pitfall 5, judge regressions against the Plan 01 pre-deploy
baseline: the two pre-existing errors (`fetch-schwab-chain` AUTH_EXPIRED, `sync-fills` payload
error) predate this deploy and must NOT be flagged as regressions.

Per D-02, the alert surface has two checkpoints. Checkpoint 1 (this plan, same-day): the
`refreshExpiresIn` key is present on both status surfaces and the `AuthExpiredBanner` component
ships in the deployed web bundle. Checkpoint 2 (observing the real amber banner during the actual
~2026-07-08 window) CANNOT be forced — the banner only renders inside the real window — so it is
recorded as a scheduled follow-up UAT item, not a same-day gate. The RTH-bound live-stream check
is likewise a follow-up if the deploy lands outside regular trading hours.

Purpose: Confirm no functional regression and that the alert surface DEPLOY-04 exists for is live.
Output: Smoke result + alert-surface checkpoint-1 confirmation + two recorded follow-ups.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-deploy-phase-15-image/16-CONTEXT.md
@.planning/phases/16-deploy-phase-15-image/16-RESEARCH.md
@.planning/phases/16-deploy-phase-15-image/16-01-SUMMARY.md
@.planning/phases/16-deploy-phase-15-image/16-02-SUMMARY.md
@packages/contracts/src/status.ts
@apps/web/src/components/AuthExpiredBanner.tsx
@docs/operations/schwab-reauth-runbook.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regression smoke checklist (MCP/curl), diffed against baseline (D-04)</name>
  <read_first>
    - .planning/phases/16-deploy-phase-15-image/16-01-SUMMARY.md (the pre-deploy error baseline: the two known job errors to exclude)
    - .planning/phases/16-deploy-phase-15-image/16-CONTEXT.md (D-04 smoke scope + which MCP tool vs curl per check)
    - .planning/phases/16-deploy-phase-15-image/16-RESEARCH.md ("D-04 smoke checklist" + Pitfall 5; stream check is RTH-bound)
  </read_first>
  <action>
    Run the D-04 manual smoke checklist against prod using the existing MCP tools (bearer-authed:
    `get_status`, `get_journal`, `get_cot`, `get_macro`); use `curl .../api/status` for the public
    part where no MCP client is at hand (journal/cot/macro routes need the bearer or a web
    session — exercise those via the MCP tools, not anonymous curl).

    Checks and pass conditions:
    - `get_status`: `db` is `ok`; `tokenFreshness` reports both apps. Compare `lastJobRuns` against
      the Plan 01 baseline — the ONLY tolerated errors are the two pre-existing ones
      (`fetch-schwab-chain` AUTH_EXPIRED, `sync-fills` payload). Any NEW error timestamped after
      DEPLOY_TIME_UTC on a previously-healthy job is a candidate regression — flag it.
    - `get_journal`: returns a fresh 30-min RTH snapshot with a recent timestamp (during RTH; if
      outside RTH, confirm the most recent snapshot is the last expected RTH slot, not stale
      beyond that).
    - `get_cot`: returns the latest weekly COT row.
    - `get_macro`: FRED macro series are present (non-empty).

    Live-stream check is RTH-bound (D-04): the badge + ticking greeks can only be verified during
    an RTH session. If this plan runs outside RTH, record the live-stream verification as a
    scheduled follow-up (next RTH session after deploy) rather than blocking — deploy timing itself
    is unconstrained; only the stream sub-check waits for RTH.

    Record every check result in the SUMMARY, explicitly noting the two baseline errors are
    excluded from the regression judgment, and whether the live-stream check passed now or is
    deferred to the next RTH session.
  </action>
  <verify>
    <automated>curl -s https://server-production-f5ca2.up.railway.app/api/status | jq -e '.db=="ok" and (.tokenFreshness | type=="object")'</automated>
  </verify>
  <acceptance_criteria>
    - `get_status`/curl: `db` is `"ok"`; no NEW post-deploy job error beyond the two documented
      baseline errors (any new one is flagged in SUMMARY as a candidate regression)
    - `get_journal`: returns a 30-min RTH snapshot; its timestamp is the latest expected RTH slot
      (fresh during RTH, last-slot if outside RTH) — recorded in SUMMARY
    - `get_cot`: latest weekly row returned
    - `get_macro`: FRED macro series present (non-empty)
    - Live-stream check result recorded: passed now (if RTH) OR logged as a scheduled follow-up for
      the next RTH session (if outside RTH)
  </acceptance_criteria>
  <done>The D-04 smoke checklist passes with no new regressions beyond the two recorded baseline errors; the RTH-bound stream check is either passed or explicitly deferred as a follow-up.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Web eyeball + T-24h alert-surface checkpoint 1 (D-02)</name>
  <read_first>
    - packages/contracts/src/status.ts (refreshExpiresIn is a REQUIRED key under tokenFreshness.trader / tokenFreshness.market — present even when null)
    - apps/web/src/components/AuthExpiredBanner.tsx (amber banner renders when refreshExpiresIn is non-null OR market AUTH_EXPIRED; red when trader AUTH_EXPIRED)
    - .planning/phases/16-deploy-phase-15-image/16-CONTEXT.md (D-02 two-checkpoint verification; checkpoint 2 is the ~2026-07-08 follow-up)
    - docs/operations/schwab-reauth-runbook.md (the T-24h/expired signals this deploy makes live)
  </read_first>
  <action>
    Drive the operator through the same-day web verification (D-02 checkpoint 1). Have them open the
    prod web app (https://morai.wtf, also morai-web.vercel.app) and confirm the dashboard loads,
    positions render, and GEX charts populate. Then confirm the alert-surface wiring: the web app
    polls `/api/status` and the response carries `tokenFreshness.trader.refreshExpiresIn` and
    `tokenFreshness.market.refreshExpiresIn` keys (value null today is correct — outside the T-24h
    window), and confirm the same keys via `curl .../api/status | jq '.tokenFreshness'`. Record the
    two scheduled follow-ups in the SUMMARY: checkpoint 2 (observe the live amber banner + warn log
    during the real ~2026-07-08 window) and the RTH-bound live-stream check if it was deferred in
    Task 1. Do not attempt to force the amber banner — it only renders inside the real window (D-02).
  </action>
  <what-built>
    Plan 02 deployed the phase-15 build to server + worker and confirmed web at 220719f+. This
    checkpoint has the operator visually confirm, in the live prod web app, (a) no dashboard
    regression and (b) that the T-24h alert surface is wired and live — the same-day half of D-02.
    The amber banner itself only renders inside the real ~2026-07-08 T-24h window and cannot be
    forced (D-02 rejected forcing a near-expiry token state), so its live observation is a
    scheduled follow-up, not part of this gate.
  </what-built>
  <how-to-verify>
    1. Open the prod web app (https://morai.wtf — also reachable at morai-web.vercel.app). Confirm
       the dashboard loads, positions render, and the GEX charts populate (D-04 web eyeball).
    2. Confirm the alert-surface wiring is present in the deployed bundle: with the browser
       DevTools Network tab open, confirm the app polls `/api/status` and that the response JSON
       contains `tokenFreshness.trader.refreshExpiresIn` and `tokenFreshness.market.refreshExpiresIn`
       keys (value `null` today is expected and correct — far from the window). The
       `AuthExpiredBanner` shipping in the bundle is confirmed by web being at 220719f+ (Plan 02)
       plus the status polling being wired — no amber banner should show now, because
       refreshExpiresIn is null (outside the window).
    3. Also confirm the server surface directly: `curl -s
       https://server-production-f5ca2.up.railway.app/api/status | jq '.tokenFreshness'` shows the
       `refreshExpiresIn` key on both apps.
    4. Record two scheduled follow-ups in the SUMMARY (NOT blockers for this phase): (i) checkpoint
       2 — observe the live amber banner + warn log during the real ~2026-07-08 T-24h window while
       running the re-auth runbook anyway; (ii) the RTH-bound live-stream check if it was deferred
       in Task 1.
  </how-to-verify>
  <acceptance_criteria>
    - Operator confirms prod web dashboard loads, positions render, GEX charts populate
    - Operator confirms the web app's `/api/status` poll response carries the
      `tokenFreshness.trader.refreshExpiresIn` and `tokenFreshness.market.refreshExpiresIn` keys
      (value null now, which is correct)
    - `curl .../api/status | jq '.tokenFreshness'` shows `refreshExpiresIn` on both apps
    - SUMMARY records checkpoint 2 (~2026-07-08 live amber-banner observation) as a scheduled
      follow-up UAT item, explicitly NOT a same-day gate
    - SUMMARY records the RTH live-stream check as passed or as a scheduled follow-up
  </acceptance_criteria>
  <resume-signal>Type "verified" once the web dashboard and the refreshExpiresIn wiring are confirmed, or describe any issue</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous client → API routes | only /api/status is public; every other route requires auth (Supabase JWT / MCP bearer) |
| pre-existing prod noise → regression judgment | baseline errors can be misread as deploy regressions (or mask a real one) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-05 | Information Disclosure / Broken Access Control | a protected route reachable unauthenticated after redeploy | high | mitigate | Access-control spot-check (verified in Plan 02 Task 2): /api/status 200 anonymous, a protected route 401 anonymous (V4 ASVS) |
| T-16-06 | Spoofing (of health signal) | pre-existing job errors misread as a deploy regression, or masking a new one | low | accept | Diff post-deploy lastJobRuns against the Plan 01 recorded baseline before judging regression (Task 1, Pitfall 5) |
</threat_model>

<verification>
- get_status: db ok, no new error beyond the two baseline errors.
- get_journal: fresh/latest-slot 30-min RTH snapshot.
- get_cot: latest weekly row. get_macro: FRED series present.
- Web: dashboard loads, positions render, GEX charts populate (operator-confirmed).
- Alert surface: refreshExpiresIn key on /api/status AND on the web status poll; AuthExpiredBanner
  in the deployed bundle (web at 220719f+).
- Follow-ups recorded: checkpoint 2 (~07-08) and RTH stream check (if deferred).
</verification>

<success_criteria>
- DEPLOY-04 criterion 2 (checkpoint 1) met: T-24h alert surface live on both status surfaces.
- DEPLOY-04 criterion 3 met: no regression in live-stream (or deferred RTH follow-up), journal,
  COT, FRED — judged against the pre-deploy baseline.
- Deferred UAT item (checkpoint 2, ~2026-07-08) and any RTH stream follow-up are recorded, not
  silently dropped.
</success_criteria>

## Artifacts this phase produces (Plan 03 scope)

No code symbols — deploy/ops-only phase. Operational artifacts from this plan:
- Post-deploy smoke-checklist result in `16-03-SUMMARY.md` (get_status/get_journal/get_cot/
  get_macro + web eyeball), diffed against the Plan 01 baseline.
- Alert-surface checkpoint-1 confirmation (refreshExpiresIn key on both status surfaces;
  AuthExpiredBanner in the deployed bundle).
- Two scheduled follow-up items recorded for post-phase close: checkpoint 2 (~2026-07-08 live
  amber banner + warn log during the real re-auth window) and the RTH-bound live-stream check if
  deferred.

<output>
Create `.planning/phases/16-deploy-phase-15-image/16-03-SUMMARY.md` when done.
</output>
