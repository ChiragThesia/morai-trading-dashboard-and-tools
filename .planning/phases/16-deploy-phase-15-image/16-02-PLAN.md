---
phase: 16-deploy-phase-15-image
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified: []          # deploy-only phase: no repo source files modified
autonomous: true            # deploy + proof are autonomous ops; no checkpoint
requirements: [DEPLOY-04]
must_haves:
  truths:
    - "Server and worker in prod run a build whose deploy createdAt is AFTER the last phase-15 commit (0c5600f, incl. the refresh-expiry-warner IN-06 fix)"
    - "Both server and worker most-recent deployments report status SUCCESS via `railway up --service`, not SKIPPED"
    - "Web in prod runs commit 220719f or later (all phase-15 web code, incl. AuthExpiredBanner), confirmed by the Vercel dashboard Source panel"
    - "`refreshExpiresIn` key is present on /api/status under tokenFreshness.trader and tokenFreshness.market (even when value is null)"
    - "The worker is running the new image, proven by lastJobRuns advancing past the deploy timestamp"
    - "/api/status returns 200 unauthenticated (healthcheck passes, no restart loop)"
  artifacts:
    - "Fresh SUCCESS deployments for server and worker with createdAt captured in SUMMARY"
    - "Web-commit confirmation (Vercel dashboard sha) recorded in SUMMARY"
    - "Post-deploy build-proof block (key-presence + timestamp correlation) in SUMMARY"
  key_links:
    - "`railway up --service server` / `--service worker` force the deploy — a plain git push SKIPs these services (reconfirmed on 220719f)"
    - "healthcheck GET /api/status (60s, 5 retries) must pass before traffic cutover — a boot-but-unhealthy image never goes live"
    - "worker has no HTTP surface — liveness is proven only by lastJobRuns side-effects, not a healthcheck"
---

<objective>
Bring server and worker in prod up to the phase-15 tip and prove all three services run the
phase-15 build. This closes DEPLOY-04 success criterion 1.

Per D-03, deploy only the stale services: force `railway up --service server` and
`railway up --service worker` from the local working tree (confirmed docs-only ahead in Plan 01),
and verify web via the Vercel dashboard rather than redeploying it (web auto-deployed `220719f`,
which already carries every phase-15 web commit).

Per D-01, the build marker is `refreshExpiresIn` present on /api/status. But that key alone is
NOT sufficient — it was added mid-phase-15 and is already present on the current stale build. The
one real behavior fix in the gap (`0c5600f` / IN-06, correcting the T-24h warn-log copy in
`apps/server/src/adapters/refresh-expiry-warner.ts`) is inside the alert surface DEPLOY-04 cares
about. So the proof pairs key-presence with a deploy-timestamp correlation: the new deployment's
`createdAt` must be after `0c5600f`'s commit time, and `railway up` provides no git sha (Pitfall
1), so timestamp correlation is the proof of record.

Purpose: Put the current build in prod and prove it, not just assert it.
Output: Fresh SUCCESS deploys for server + worker, web confirmed current, build-proof in SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-deploy-phase-15-image/16-CONTEXT.md
@.planning/phases/16-deploy-phase-15-image/16-RESEARCH.md
@.planning/phases/16-deploy-phase-15-image/16-01-SUMMARY.md
@railway.server.toml
@railway.worker.toml
@packages/contracts/src/status.ts
@apps/server/src/adapters/refresh-expiry-warner.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Force-deploy the stale services (server, worker); verify web is current</name>
  <read_first>
    - .planning/phases/16-deploy-phase-15-image/16-01-SUMMARY.md (pre-deploy baseline + target HEAD sha)
    - .planning/phases/16-deploy-phase-15-image/16-RESEARCH.md ("Force-deploy the stale services" + Pitfalls 1, 2; Open Question 1 on web)
    - .planning/phases/16-deploy-phase-15-image/16-CONTEXT.md (D-03: verify shas first, deploy only stale, sidecar excluded)
    - railway.server.toml (healthcheck /api/status, timeout 60, ON_FAILURE ×5), railway.worker.toml (no healthcheck)
  </read_first>
  <action>
    Deploy the two stale Railway services from the current working tree, and confirm web without
    redeploying it. Sidecar is EXCLUDED (redeployed 2026-07-02 during live re-auth; DEPLOY-04
    names server + worker + web only) — never run `railway up` or `railway domain` against
    sidecar.

    Immediately before deploying, capture the deploy start time: `DEPLOY_TIME_UTC=$(date -u
    +%FT%TZ)` and record it in the SUMMARY (needed for the Task 2 timestamp proof). Then force
    each stale service explicitly — never rely on git push (it SKIPs these services, reconfirmed
    on `220719f`): `railway up --service server` then `railway up --service worker`. Order between
    the two is low-stakes (no migration in play — Claude's discretion per D-03); server first is
    fine so its /api/status healthcheck is available sooner for Task 2.

    After each `railway up` reports done, confirm the resulting deployment is live:
    `railway deployment list --service server --json --limit 1` and same for worker, extracting
    `{status, createdAt}` — both must read `status: "SUCCESS"` with `createdAt >= DEPLOY_TIME_UTC`.
    The server healthcheck (GET /api/status, 60s timeout, 5 retries) gates its own cutover, so a
    SUCCESS server deploy also implies /api/status passed.

    Web: do NOT redeploy. Open the Vercel dashboard for the `web` project and read the live
    production deployment's "Source" panel commit sha (the CLI JSON omits it — Open Question 1),
    confirming it is `220719f` or later (i.e. includes all phase-15 web commits, incl.
    `AuthExpiredBanner`). Record the confirmed sha in the SUMMARY. Only if the dashboard shows a
    pre-phase-15 web commit should web be redeployed (push to main / Vercel redeploy) — record
    which path was taken.
  </action>
  <verify>
    <automated>railway deployment list --service server --json --limit 1 | jq -e '.[0].status=="SUCCESS"' && railway deployment list --service worker --json --limit 1 | jq -e '.[0].status=="SUCCESS"' && curl -s -o /dev/null -w "%{http_code}\n" https://server-production-f5ca2.up.railway.app/api/status | grep -q '^200$'</automated>
  </verify>
  <acceptance_criteria>
    - Most-recent `server` deployment: `status: "SUCCESS"`, `createdAt >= DEPLOY_TIME_UTC`
    - Most-recent `worker` deployment: `status: "SUCCESS"`, `createdAt >= DEPLOY_TIME_UTC`
    - `DEPLOY_TIME_UTC` is recorded in the SUMMARY
    - `curl -o /dev/null -w "%{http_code}" .../api/status` returns `200` (healthcheck route stayed
      public — no auth-gate regression that would restart-loop the deploy)
    - Web: Vercel dashboard Source panel confirms the live prod deployment is commit `220719f` or
      later; the confirmed sha and the decision (verify-only vs redeploy) are recorded in SUMMARY
    - Sidecar was not touched (no `railway up`/`railway domain` against sidecar)
  </acceptance_criteria>
  <done>Server and worker report fresh SUCCESS deployments created after DEPLOY_TIME_UTC; web is confirmed at 220719f+ via the Vercel dashboard; sidecar untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Build-proof — key-presence + deploy-timestamp correlation (D-01)</name>
  <read_first>
    - .planning/phases/16-deploy-phase-15-image/16-RESEARCH.md ("Post-deploy proof (D-01 + Pitfall 3)" + Anti-Pattern: refreshExpiresIn key-present is not sufficient)
    - packages/contracts/src/status.ts (refreshExpiresIn lives under tokenFreshness.trader / tokenFreshness.market; a REQUIRED key, null when far from expiry, never omitted)
    - apps/server/src/adapters/refresh-expiry-warner.ts (the IN-06 fix: warns "refresh token EXPIRED ... re-auth now" when refreshExpiresIn===0, not "0s remaining")
  </read_first>
  <action>
    Prove prod runs the phase-15 build, not merely that the marker key exists. Two combined checks
    (D-01 + RESEARCH Pitfall 3):

    Key-presence (necessary): `curl -s https://server-production-f5ca2.up.railway.app/api/status |
    jq '{db, trader_key: (.tokenFreshness.trader | has("refreshExpiresIn")), market_key:
    (.tokenFreshness.market | has("refreshExpiresIn")), trader_val:
    .tokenFreshness.trader.refreshExpiresIn, market_val:
    .tokenFreshness.market.refreshExpiresIn}'`. The `has("refreshExpiresIn")` check must be `true`
    for both apps (the value itself is expected to be `null` today — far from the T-24h window —
    which is correct, not a failure).

    Timestamp correlation (sufficient — the part key-presence cannot prove): confirm the server
    and worker deployments' `createdAt` (captured in Task 1) is after the last phase-15 commit
    `0c5600f` (IN-06, committed 2026-07-02T21:41:25-05:00 = 2026-07-03T02:41:25Z). Since the deploy
    ran today (2026-07-03 afternoon+), `createdAt` is necessarily after that commit — state this
    explicitly in the SUMMARY as the proof that the stale-image gap (which lacked IN-06) is closed.
    `railway up` deploys carry `commitHash: null` (Pitfall 1), so this timestamp correlation — not
    a git sha — is the proof of record.

    Worker liveness (proves the new image is RUNNING, not just built): re-check `curl -s
    .../api/status | jq '.lastJobRuns'` a few minutes after the worker deploy; at least one
    cron/queue job's `lastSuccessAt` should advance past `DEPLOY_TIME_UTC`. A job whose cadence has
    not elapsed yet won't move — do not treat a not-yet-due job as failure; pick a job that should
    have run in the window.

    Access-control spot-check (V4 ASVS, T-16-05): confirm /api/status returns 200 unauthenticated
    AND an authenticated-only route (e.g. an `/api/jobs/*` route) returns 401 unauthenticated —
    proving the redeploy did not reorder middleware and expose a protected route.
  </action>
  <verify>
    <automated>curl -s https://server-production-f5ca2.up.railway.app/api/status | jq -e '(.tokenFreshness.trader | has("refreshExpiresIn")) and (.tokenFreshness.market | has("refreshExpiresIn")) and .db=="ok"'</automated>
  </verify>
  <acceptance_criteria>
    - `jq '.tokenFreshness.trader | has("refreshExpiresIn")'` and the market equivalent both return
      `true` (key present on both apps; value may be `null`)
    - SUMMARY states, with the captured `createdAt` values, that both server and worker deploys
      landed after commit `0c5600f` (2026-07-03T02:41:25Z) — the timestamp proof that IN-06 is now
      in prod
    - At least one `lastJobRuns.<job>.lastSuccessAt` has advanced past `DEPLOY_TIME_UTC` (worker
      liveness), with the chosen job named in the SUMMARY
    - /api/status returns 200 unauthenticated AND a protected route (e.g. `/api/jobs/*`) returns
      401 unauthenticated (access-control not regressed)
  </acceptance_criteria>
  <done>Both the key-presence check and the deploy-timestamp correlation pass; worker liveness confirmed via advancing lastJobRuns; access-control spot-check passes — prod provably runs the phase-15 build (DEPLOY-04 criterion 1).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → Railway/Vercel deploy pipeline | `railway up` uploads the local working tree; a wrong-service or skipped deploy has high blast radius |
| anonymous client → /api/status healthcheck | healthcheck route must stay public or the deploy restart-loops |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-03 | Tampering (indirect) | stale image missing the IN-06 (`0c5600f`) alert-surface fix | high | mitigate | Force `railway up --service` per service; prove deploy createdAt > 0c5600f commit time, not just key-presence (Task 2) |
| T-16-02 | Denial of Service | /api/status accidentally gated behind auth after deploy → healthcheck restart loop | medium | mitigate | Confirm /api/status returns 200 unauthenticated post-deploy (Task 1 + Task 2 access-control spot-check) |
| T-16-04 | Elevation of Privilege | Railway CLI run without `--service` (default: sidecar) | high | mitigate | Explicit `--service server`/`--service worker` on every up/deployment-list command; sidecar never touched |
</threat_model>

<verification>
- server + worker: most-recent deployment `status: "SUCCESS"`, `createdAt >= DEPLOY_TIME_UTC`.
- web: Vercel dashboard Source panel confirms `220719f` or later.
- /api/status: `refreshExpiresIn` key present on both apps; returns 200 unauthenticated; a
  protected route returns 401.
- worker liveness: a `lastJobRuns.<job>.lastSuccessAt` advanced past DEPLOY_TIME_UTC.
- SUMMARY documents the timestamp-correlation proof (deploy after 0c5600f).
</verification>

<success_criteria>
- DEPLOY-04 criterion 1 met: server, worker, and web in prod run the phase-15 build, proven by
  fresh SUCCESS deploys + timestamp correlation (server/worker) and dashboard commit sha (web),
  not merely by a merged commit.
</success_criteria>

## Artifacts this phase produces (Plan 02 scope)

No code symbols — deploy/ops-only phase. Operational artifacts from this plan:
- Fresh SUCCESS Railway deployments for `server` and `worker` (createdAt captured), built from the
  phase-15 tip (local working tree).
- Web-commit confirmation (`220719f` or later) recorded from the Vercel dashboard Source panel.
- Build-proof block in `16-02-SUMMARY.md`: key-presence result + deploy-timestamp correlation
  (deploy after `0c5600f`) + worker-liveness evidence + access-control spot-check result.

<output>
Create `.planning/phases/16-deploy-phase-15-image/16-02-SUMMARY.md` when done.
</output>
