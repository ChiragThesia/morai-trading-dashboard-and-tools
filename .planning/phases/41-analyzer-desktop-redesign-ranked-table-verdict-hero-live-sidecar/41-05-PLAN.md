---
phase: 41
plan: 05
type: execute
wave: 5
depends_on: ["41-01", "41-02", "41-03", "41-04"]
files_modified:
  - .planning/phases/41-analyzer-desktop-redesign-ranked-table-verdict-hero-live-sidecar/41-UAT.md
autonomous: true
requirements: [AUI-01, AUI-02, AUI-03, AUI-04, AUI-05, AUI-06, AUI-07]
must_haves:
  truths:
    - "The whole workspace suite, root typecheck, apps/web's OWN tsc, and lint are all green (catch #29: root typecheck is blind to apps/web's own tsc)"
    - "No exact-broker-value formatter remains in the Analyzer tab files (AUI-04 law); no retired chip/card testid leaks a false-green"
    - "The desktop center/right detail components (PayoffChart, TermStructureChart structure, WhyPanel, EntryExitPlan) still render, driven by the selected row"
    - "The user visually approves the new desktop + mobile Analyzer on morai.wtf: no candidate scroll wall, row click → detail swap instant, verdict hero + groups, trading-precision numbers, live badge, sticky table"
  artifacts:
    - .planning/phases/41-analyzer-desktop-redesign-ranked-table-verdict-hero-live-sidecar/41-UAT.md
  key_links:
    - "full-suite gate → catches any green-suite drift from the table/hero/rounding rewrites before UAT"
---

<objective>
Phase-41 integration gate: prove the whole Analyzer redesign holds together before human verification.
Run the full suite + both typecheck passes + lint, assert the cross-cutting AUI laws by grep (no
exact-value formatter left on the tab; no retired testid false-green), confirm the desktop detail
components still render, and author the UAT script for the end-of-phase visual approval on morai.wtf
(desktop + mobile).

Purpose: a single gate that fails loudly on any drift the per-plan scoped runs missed.
Output: green full gate + 41-UAT.md with the human-verify script.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-analyzer-desktop-redesign-ranked-table-verdict-hero-live-sidecar/41-UI-SPEC.md
@.planning/phases/41-analyzer-desktop-redesign-ranked-table-verdict-hero-live-sidecar/41-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite + dual-typecheck + lint + cross-cutting AUI-law greps</name>
  <files>.planning/phases/41-analyzer-desktop-redesign-ranked-table-verdict-hero-live-sidecar/41-UAT.md</files>
  <read_first>
    RESEARCH §Validation Architecture (full-suite gate command + the "per wave merge" apps/web run),
    project memory catch #29 (root typecheck blind to apps/web's own tsc — run BOTH),
    apps/web/package.json (the app's own `typecheck` script), root package.json (`test`, `lint`).
  </read_first>
  <action>
    Run the phase gate and capture output into 41-UAT.md's automated-results section:
    1. `bun run test` (root workspace — the full Vitest suite; this is the phase-gate command).
    2. `bun run typecheck` (root) AND the apps/web-local tsc (`cd apps/web && bun run typecheck`, or the
       equivalent app-scoped tsc) — BOTH must be clean; the root pass alone has historically missed
       apps/web errors (catch #29).
    3. `bun run lint`.
    4. AUI-04 law grep: `rg -c 'exactAbs' apps/web/src/screens/Analyzer.tsx
       apps/web/src/screens/analyzer-mobile/MobileScorecard.tsx` returns 0 for both (the exact-value
       formatter is fully gone from the tab's source).
       <!-- planner-discipline-allow: exactAbs -->
    5. Green-suite law grep: confirm the retired chip/card DOM testids are no longer emitted by the
       SOURCE components — `rg -n 'scoring-pills|scoring-checklist|candidate-card-' apps/web/src/screens/Analyzer.tsx`
       returns nothing in the desktop rail/scorecard regions (CandidateCard.tsx itself keeps its own
       testids — it is still used by the mobile trees — so scope the grep to Analyzer.tsx, not the whole tree).
    If any step fails, STOP and fix in the owning plan's files (do not paper over with a skip).
  </action>
  <verify>
    <automated>bun run test && bun run typecheck && bun run lint</automated>
  </verify>
  <done>
    Full suite green; root + apps/web tsc clean; lint clean; the exactAbs grep is 0 on both tab files;
    no retired desktop testid remains in Analyzer.tsx. Results captured in 41-UAT.md.
  </done>
</task>

<task type="auto">
  <name>Task 2: Desktop-unchanged tripwire + author the morai.wtf UAT script</name>
  <files>.planning/phases/41-analyzer-desktop-redesign-ranked-table-verdict-hero-live-sidecar/41-UAT.md</files>
  <read_first>
    UI-SPEC Detail Pane Composition ("center/right columns keep their EXISTING components unchanged"),
    41-CONTEXT.md <specifics> (acceptance shape: user visually approves on morai.wtf; no candidate
    scroll wall; row click → detail swap instant; trading precision everywhere), the standing
    chrome-devtools UAT permission in project memory.
  </read_first>
  <action>
    Tripwire: confirm (via the existing "right column" describe block still green) that PayoffChart,
    TermStructureChart, WhyPanel, and EntryExitPlan still render for the selected row and re-wire on a
    new selection — the detail pane's components are UNCHANGED in structure (TermStructureChart got only
    numeric-literal edits). No new test needed if the existing right-column block passes; note it as the
    tripwire in 41-UAT.md.

    Author 41-UAT.md's human-verify script (this is the end-of-phase human_verify gate — see the
    human-check below). Number the steps and give exact URLs/actions/expected outcomes:
    - Desktop (morai.wtf Analyzer tab, ≥1280px): the candidate list is a compact TABLE (no 17-card
      scroll wall); clicking a row instantly swaps the risk-profile chart + term-structure + WHY +
      entry/exit to that candidate with an explicit violet selected row; Score/Debit/Θ headers sort;
      the table scrolls under a sticky header while the center/right panels stay put (no dead columns,
      page height is content-driven); the scorecard is a verdict hero (FAVORABLE/CAUTION/SKIP + score +
      Θ) over Edge/Risk/Fit groups; numbers read at trading precision (whole dollars, +NN.NN vega);
      a LiveStatusBadge shows LIVE/QUIET/STALLED in the Risk-profile header; the term-structure chart is
      taller with clear short/long markers and event labels on the curve.
    - Mobile (morai.wtf Analyzer, <1024px / phone): CandidateCard tap-to-select list (no table);
      scorecard shows the same verdict-word headline + Edge/Risk/Fit stacked groups; rounded numbers;
      a LiveStatusBadge in the chart chrome row.
    - Live check (during RTH if possible): the payoff T+0 marker tracks the live SPX tick while the
      badge reads LIVE; when the stream is quiet/stalled the marker falls back to the snapshot spot and
      the badge reads QUIET/STALLED (never a frozen value shown as live).
  </action>
  <verify>
    <human-check>User (or the operator via standing chrome-devtools permission) walks the 41-UAT.md
    desktop + mobile + live scripts on morai.wtf and confirms each expected outcome; record PASS/FAIL
    per item in 41-UAT.md.</human-check>
  </verify>
  <done>
    Desktop detail components confirmed unchanged (right-column tripwire green); 41-UAT.md carries the
    full desktop + mobile + live human-verify script ready for /gsd-verify-work 41.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| green suite → phase gate | A stale suite passing on retired testids would let a broken redesign ship (green-suite law) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-07 | Repudiation (false-green) | test suite | medium | mitigate | The gate greps that retired testids are gone from source AND the full suite exercises the new table/hero DOM — a false-green from asserting on nonexistent testids is caught (catches #26-#29) |
| T-41-08 | Tampering | apps/web typecheck blind spot | low | mitigate | Run BOTH root typecheck and apps/web's own tsc (catch #29) — a silent apps/web type error can't slip through the root pass |
| T-41-SC | Tampering | npm installs | n/a | accept | Zero package installs this phase |
</threat_model>

<verification>
- `bun run test` (root) green; `bun run typecheck` (root) + apps/web tsc clean; `bun run lint` clean.
- exactAbs grep = 0 on Analyzer.tsx + MobileScorecard.tsx; no retired desktop testid in Analyzer.tsx.
- 41-UAT.md human-verify script authored; right-column tripwire green.
</verification>

<success_criteria>
- Full workspace gate green (suite + dual typecheck + lint).
- AUI-04 + green-suite laws pass by grep.
- Desktop detail components unchanged and still re-wire on selection.
- Human-verify script ready; UAT is the end-of-phase gate (human_verify_mode=end-of-phase).
</success_criteria>

<output>
Create `.planning/phases/41-analyzer-desktop-redesign-ranked-table-verdict-hero-live-sidecar/41-05-SUMMARY.md` when done
</output>
