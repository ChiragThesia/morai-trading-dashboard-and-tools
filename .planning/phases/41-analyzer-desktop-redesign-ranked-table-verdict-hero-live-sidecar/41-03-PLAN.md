---
phase: 41
plan: 03
type: execute
wave: 3
depends_on: ["41-02"]
files_modified:
  - apps/web/src/screens/analyzer-mobile/useAnalyzerModel.ts
  - apps/web/src/screens/analyzer-mobile/useAnalyzerModel.test.ts
  - apps/web/src/components/picker/CandidateCard.tsx
  - apps/web/src/screens/Analyzer.tsx
  - apps/web/src/screens/Analyzer.test.tsx
  - apps/web/src/screens/analyzer-mobile/MobileScorecard.tsx
  - apps/web/src/screens/analyzer-mobile/MobileScorecard.test.tsx
autonomous: true
requirements: [AUI-02, AUI-06]
must_haves:
  truths:
    - "The desktop scorecard is one verdict hero (icon + FAVORABLE/CAUTION/SKIP word + score/100 + Θ headline) over three labeled factor groups (EDGE / RISK / FIT), not an 11-chip flat row"
    - "The verdict word derives from scoreStatus(candidate.score) — no fabricated confidence, existing classifier only (D-02 evidence-honesty)"
    - "Factors group by the LOCKED mapping: EDGE={fwdEdge,slope,vrp}, RISK={eventAdjustment,beVsEm,debitFit}, FIT={gexFit,deltaNeutral,thetaVega}, each row `label ✓/~/✗ NN%` with the existing guard handling"
    - "The calibrating/dropped-quotes/as-of line is a single quiet footer, omitted when empty — never a floating debug row"
    - "A not-scored (pasted) candidate renders ONLY the not-scored note — no verdict word, no groups (catch #23); no selection renders nothing"
    - "Mobile MobileScorecard restructures the flat checklist into the same Edge/Risk/Fit groups, stacked single-column, with the same verdict-word headline"
  artifacts:
    - apps/web/src/screens/analyzer-mobile/useAnalyzerModel.ts
  key_links:
    - "GROUP_OF lookup (useAnalyzerModel) → both desktop hero + mobile scorecard partition the same scoreItems array"
    - "scoreStatus(candidate.score) → verdict word + tier color (both trees)"
    - "formatAsOf(observedAt) (exported from CandidateCard) → hero footer as-of provenance (AUI-07 honesty)"
---

<objective>
AUI-02 (D-02): kill the 11-chip flat scorecard + bare debug line. Desktop gets a verdict hero
(dominant FAVORABLE/CAUTION/SKIP word + score + Θ headline) over three labeled factor groups
(Edge/Risk/Fit) that re-lay-out the already-computed breakdown — no new scoring, no new confidence
math. AUI-06: the mobile MobileScorecard restructures its flat checklist into the same three groups,
stacked for phone width. The verdict word reuses the existing `scoreStatus()` classifier; the footer
carries the `as of {HH:MM} · {source}` provenance that keeps AUI-07's "snapshot-derived score says so"
honest.

Purpose: a scannable, hierarchical verdict replaces the undifferentiated chip wall.
Output: verdict hero (desktop) + grouped scorecard (mobile) sharing one GROUP_OF partition; migrated
scoring tests on both trees.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-analyzer-desktop-redesign-ranked-table-verdict-hero-live-sidecar/41-UI-SPEC.md
@.planning/phases/41-analyzer-desktop-redesign-ranked-table-verdict-hero-live-sidecar/41-RESEARCH.md

# Existing verdict-hero-shaped component to lift + the classifier/labels to reuse:
@apps/web/src/screens/analyzer-mobile/MobileScorecard.tsx
@apps/web/src/screens/analyzer-mobile/useAnalyzerModel.ts
@apps/web/src/screens/Analyzer.tsx
@apps/web/src/components/picker/CandidateCard.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GROUP_OF partition + verdict-word derivation + formatAsOf export (shared)</name>
  <files>
    apps/web/src/screens/analyzer-mobile/useAnalyzerModel.ts,
    apps/web/src/screens/analyzer-mobile/useAnalyzerModel.test.ts,
    apps/web/src/components/picker/CandidateCard.tsx
  </files>
  <read_first>
    UI-SPEC Verdict Hero Contract (verdict-word derivation table + Factor groups mapping — LOCKED),
    useAnalyzerModel.ts:74-98 (scoreStatus + CHIP_LABELS + EXPERIMENTAL_SHORT, all exported already),
    CandidateCard.tsx:80-148 (formatAsOf — currently a private function; make it exported).
  </read_first>
  <behavior>
    - GROUP_OF maps each criterion to its group: fwdEdge/slope/vrp → "EDGE"; eventAdjustment/beVsEm/
      debitFit → "RISK"; gexFit/deltaNeutral/thetaVega → "FIT".
    - verdictWord(score): score≥66.7 → {word:"FAVORABLE", icon:"✓", cls:"text-up"};
      score≥33.3 → {word:"CAUTION", icon:"~", cls:"text-amber"}; else → {word:"SKIP", icon:"✗",
      cls:"text-down"} — derived by reusing scoreStatus(score)'s icon, never a new threshold.
    - formatAsOf(observedAt) is importable from CandidateCard.tsx and returns the same {label, fresh}.
  </behavior>
  <action>
    In useAnalyzerModel.ts export a `GROUP_OF: Record<string, "EDGE" | "RISK" | "FIT">` constant with
    the LOCKED mapping above, and a `verdictWord(score: number)` helper that reuses `scoreStatus(score)`
    to pick icon+tier and maps the icon to the word (✓→FAVORABLE, ~→CAUTION, ✗→SKIP). Keep both pure
    and module-level (no React). In CandidateCard.tsx, add `export` to the existing `formatAsOf`
    function (no behavior change) so the hero footer can reuse it verbatim.

    Tests: RED first, in useAnalyzerModel.test.ts — assert GROUP_OF membership for all nine criteria,
    and verdictWord at the three tier boundaries (e.g. 81→FAVORABLE/text-up, 50→CAUTION/text-amber,
    10→SKIP/text-down). No render needed (pure functions).
  </action>
  <verify>
    <automated>bun run test apps/web/src/screens/analyzer-mobile/useAnalyzerModel.test.ts</automated>
  </verify>
  <done>
    GROUP_OF + verdictWord exported and tested at all boundaries; formatAsOf exported from CandidateCard;
    hook test green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Desktop verdict hero replaces ScoringMethodologyPanel</name>
  <files>apps/web/src/screens/Analyzer.tsx, apps/web/src/screens/Analyzer.test.tsx</files>
  <read_first>
    UI-SPEC Verdict Hero Contract (headline row + factor groups grid + footer + not-scored gate),
    Analyzer.tsx:229-338 (ScoringMethodologyPanel — the chip strip being replaced) and Analyzer.tsx:
    536-543 (the `analyzer-scorecard-wrapper` mount, which STAYS), Analyzer.test.tsx:208-238 +
    809-913 (the scoring-checklist + rule-registry describe blocks to migrate).
  </read_first>
  <behavior>
    - Selected scored candidate renders: headline `{icon} {WORD}   score {Math.round(score)}/100   Θ
      {theta>=0?'+':''}{theta.toFixed(1)}/d` (word at 16px `font-semibold` (600 — NEVER font-bold/700, UI-SPEC rev 2 two-weight cap) tier color; headline score/Θ at 13px `font-semibold` mono; Θ keeps its own sign color
      independent of the verdict tier), then a `grid grid-cols-3 gap-4` of EDGE/RISK/FIT group headers
      each over its 3 factor rows (`label ✓/~/✗ NN%`, guard→n/a), then one quiet footer line
      (calibrating context · gate-drops · `as of {HH:MM} · {source}`), omitted when all parts empty.
    - AH-session badge renders after the headline when marketSession==="after-hours" (existing copy).
    - θ GATE is NOT a separate chip — its value is the headline Θ (never duplicated).
    - Not-scored candidate (breakdown.length===0) → only PASTED_NOT_SCORED_NOTE. candidate===null →
      renders nothing.
    - A criterion absent from breakdown renders nothing in its slot (pre-registry snapshots).
  </behavior>
  <action>
    Replace ScoringMethodologyPanel's chip-strip body with the verdict hero. Reuse the existing
    scoreItems derivation (registry rules → CHIP_LABELS fallback) verbatim, then partition scoreItems by
    `GROUP_OF[item.key]` into the three columns; each factor row reuses the existing per-item lookup
    (`candidate.breakdown.find`, fwdEdge-null guard, `scoreStatus(contribution)`, `Math.round` %). Build
    the headline from `verdictWord(candidate.score)` + the score + the Θ value (Θ sign-colored via
    `theta>=0?'text-up':'text-down'`). Build the footer from the existing calibrating-context join +
    gate-drops line + `formatAsOf(observedAt).label` and the source; omit the footer entirely when it
    would be empty. Keep the `analyzer-scorecard-wrapper` container and its mount site unchanged so the
    DOM-order tripwire test still holds. Thread `observedAt`/`source` into the hero (already on
    snapshot). Retire the chip-strip markup (the MetricChip pills, the `scoring-pills`/
    `scoring-checklist`/`checklist-theta` DOM) — the hero replaces it wholesale.

    Tests: RED first. Migrate the "per-candidate scoring checklist" + "rule-registry-driven checklist"
    describe blocks to assert the hero: the verdict word + tier for the top candidate, the three group
    headers, correct criterion→group membership, the Θ headline, the footer's as-of + gate-drops, the
    AH badge branch, and the not-scored gate (a pasted breakdown-empty candidate shows only the note,
    no verdict word). The suite must exercise the new hero DOM — do not leave a block asserting on the
    retired chip testids.
  </action>
  <verify>
    <automated>bun run test apps/web/src/screens/Analyzer.test.tsx</automated>
  </verify>
  <done>
    Desktop verdict hero renders headline + 3 groups + quiet footer, evidence-honest word from
    scoreStatus; not-scored gate holds; scoring describe blocks migrated and green; whole file green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Mobile MobileScorecard — flat checklist → Edge/Risk/Fit stacked groups</name>
  <files>
    apps/web/src/screens/analyzer-mobile/MobileScorecard.tsx,
    apps/web/src/screens/analyzer-mobile/MobileScorecard.test.tsx
  </files>
  <read_first>
    UI-SPEC Mobile Parity row "Verdict hero grouping (D-02)" (stacked single-column, same GROUP_OF,
    same verdict-word headline), MobileScorecard.tsx:79-183 (the flat checklist to regroup),
    MobileScorecard.test.tsx (existing checklist-row tests = the migration base).
  </read_first>
  <behavior>
    - The scored MobileScorecard shows the verdict-word headline (icon + WORD + score, reusing
      verdictWord) above the checklist, then the same nine factors grouped under EDGE/RISK/FIT — stacked
      one group per row-block (single column, not a 3-col grid — phone width).
    - Same guard handling, same CHIP_LABELS labels, same θ GATE-as-headline retirement, same context/
      gate-drops footer, same not-scored gate (breakdown.length===0 → note only; candidate===null →
      null).
  </behavior>
  <action>
    Restructure MobileScorecard's flat `flex flex-col gap-1` checklist into three stacked group blocks,
    each a group label (SectionLabel tier) over its factors, partitioned by the shared `GROUP_OF` and
    rendered with the SAME per-row derivation the file already has (scoreStatus/CHIP_LABELS/guard). Add
    the verdict-word headline via `verdictWord(candidate.score)` above the groups; fold θ into the
    headline Θ (drop the separate θ GATE row's duplication per the desktop contract, keeping the sign
    color). Preserve the not-scored/no-candidate gates and the context/gate-drops footer rows exactly.
    Import GROUP_OF/verdictWord from useAnalyzerModel.ts (single source — never re-declare the mapping).

    Tests: RED first. Extend MobileScorecard.test.tsx to assert the three group headers, correct
    criterion→group membership mirroring desktop, the verdict-word headline, and that the not-scored
    branch still shows only the note. Keep the existing row-derivation assertions that stay valid.
  </action>
  <verify>
    <automated>bun run test apps/web/src/screens/analyzer-mobile/MobileScorecard.test.tsx</automated>
  </verify>
  <done>
    Mobile scorecard shows verdict-word headline + Edge/Risk/Fit stacked groups sharing GROUP_OF with
    desktop; gates preserved; test green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| snapshot breakdown → verdict hero | The hero re-lays-out already-computed breakdown/score — it must add no new computation and take no live input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-03 | Tampering | verdict hero / factor groups | medium | mitigate | Hero reads candidate.score/breakdown only (snapshot-derived); no `spot`/liveSpot threaded in — grep the hero for `spot` in review (Pitfall 1); footer as-of proves provenance |
| T-41-04 | Spoofing (of confidence) | verdict word | low | mitigate | Word derives strictly from scoreStatus(candidate.score) — no fabricated confidence number, no new threshold (D-02 evidence-honesty); tested at tier boundaries in Task 1 |
| T-41-SC | Tampering | npm installs | n/a | accept | Zero package installs this phase |
</threat_model>

<verification>
- `bun run test apps/web/src/screens/Analyzer.test.tsx apps/web/src/screens/analyzer-mobile` — both trees' scoring suites green against the new hero/grouped DOM.
- `bun run typecheck` — GROUP_OF/verdictWord typed, no any/as/!.
</verification>

<success_criteria>
- Desktop verdict hero: headline + Edge/Risk/Fit groups + quiet footer; word from scoreStatus; θ folded into headline.
- Mobile scorecard grouped identically (stacked); GROUP_OF shared, not duplicated.
- Not-scored + no-selection gates hold on both trees.
- as-of provenance in the footer (AUI-07 honesty).
</success_criteria>

<output>
Create `.planning/phases/41-analyzer-desktop-redesign-ranked-table-verdict-hero-live-sidecar/41-03-SUMMARY.md` when done
</output>
