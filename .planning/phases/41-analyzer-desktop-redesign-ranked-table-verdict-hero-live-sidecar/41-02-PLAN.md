---
phase: 41
plan: 02
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - apps/web/src/screens/Analyzer.tsx
  - apps/web/src/screens/Analyzer.test.tsx
autonomous: true
requirements: [AUI-01, AUI-03]
must_haves:
  truths:
    - "The desktop Suggested-calendars panel renders one <tr> per candidate (score / calendar / debit / Θ/d / event / ⊕), not a 17-card scroll rail"
    - "Clicking anywhere in a row except the ⊕/× cell loads that candidate into the center/right detail panels (same onSelect/selectedId the cards drove)"
    - "The selected row shows an explicit violet tint + left accent bar"
    - "Score/Debit/Θ column headers sort the scored rows, cycling desc→asc→desc, with aria-sort reflecting state; default load is score-desc; pasted rows stay pinned above, unsorted"
    - "The table Panel caps at 70vh and scrolls with a sticky header; the 3,274px dead-column page height is gone (page height is content-driven)"
    - "Pasted-but-unscored rows show a PASTED pill in the score cell and — in the Debit/Θ cells (never a fabricated $0), with a × remove button beside the ⊕"
  artifacts:
    - apps/web/src/screens/Analyzer.tsx
  key_links:
    - "table row onClick → onSelect(candidate) → useAnalyzerModel.selectedId → detail panels (unchanged model)"
    - "⊕ cell onClick stopPropagation → onToggleCombine(candidate) (targets non-selected rows)"
    - "sort useState (local to AnalyzerDesktop) → useMemo over scored candidates → <tbody> row order"
---

<objective>
AUI-01 (D-01) + AUI-03 (D-03): replace the 17-`CandidateCard` scroll rail with one compact sortable
`<table>` inside the existing left-column Panel, and bound that Panel's height so the dead-column /
3,274px-page defect dies with it. Row click drives the exact same `onSelect`/`selectedId` state the
cards drove — zero change to `useAnalyzerModel`. Mobile is untouched (it keeps `CandidateCard`
tap-to-select — AUI-06's resolved "no table on mobile" decision).

Purpose: candidates become scannable and comparable; the center/right detail panels stay put while
the table scrolls.
Output: a native `<table>` rail with sort state + sticky bounded Panel; migrated Analyzer.test.tsx.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-analyzer-desktop-redesign-ranked-table-verdict-hero-live-sidecar/41-UI-SPEC.md
@.planning/phases/41-analyzer-desktop-redesign-ranked-table-verdict-hero-live-sidecar/41-RESEARCH.md

# Table + selection precedent (row click, stopPropagation on the action cell) + selected tint source:
@apps/web/src/screens/Overview.tsx
@apps/web/src/components/picker/CandidateCard.tsx
@apps/web/src/components/system/index.tsx
@apps/web/src/screens/Analyzer.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ranked sortable table + sticky bounded Panel (CandidateRail body)</name>
  <files>apps/web/src/screens/Analyzer.tsx, apps/web/src/screens/Analyzer.test.tsx</files>
  <read_first>
    UI-SPEC Table Contract (columns, row states, sort affordance, bounded height — LOCKED), Overview.tsx
    positions `<table>` (row onClick + `<td onClick={stopPropagation}>` action-cell pattern),
    CandidateCard.tsx:155 (`selected ? "border-violet bg-violet/[0.06]"` tint to port to a <tr>),
    Panel in components/system/index.tsx:34 (verified: `p-3 ring-1 ring-line`, NO overflow/transform —
    `max-h-[70vh] overflow-y-auto` can be added directly, Assumption A1 resolved), Analyzer.tsx:98-207
    (CandidateRail), pickerCandidate fields (name/score/debit/theta/frontEvents/backEvents) in
    packages/contracts/src/picker.ts.
  </read_first>
  <behavior>
    - Renders one row per candidate (pasted rows pinned above scored rows, in paste order).
    - Columns left→right: Score (Math.round, tier color), Calendar (candidate.name verbatim), Debit
      ($Math.round(debit)), Θ/d (`${theta>=0?'+':''}${theta.toFixed(1)}/d`, sign-colored), Event
      (frontEvents[0] ?? backEvents[0] ?? "—", amber ⚡ pill, `+N` when >1 event across both legs), ⊕.
    - Row onClick (anywhere except the ⊕/× cell) calls onSelect(candidate); the ⊕ cell onClick
      stopPropagations and calls onToggleCombine(candidate); ⊕ reflects combinedIds.has(id) via active.
    - Selected row: `bg-violet/[0.06]` + `border-l-2 border-l-violet`.
    - Sort: clicking Score/Debit/Θ header cycles that column desc→asc→desc; only the active column
      carries a ▲/▼ glyph and aria-sort ascending/descending (others aria-sort none); Calendar/Event
      are not sortable; default is score-desc (equals today's sortedCandidates order); pasted rows are
      NEVER re-sorted (always pinned above in paste order).
    - Pasted-unscored row (breakdown.length===0): Score cell = PASTED pill, Debit/Θ cells = "—", the
      action cell carries the × remove button beside ⊕.
    - Empty state ("No candidates in this snapshot") and cold-start/loading/error states render as
      today (inside/around the table body region), copy unchanged.
  </behavior>
  <action>
    Rewrite CandidateRail's populated body from the `flex flex-col gap-2` CandidateCard list into a
    native `<table class="w-full">` with a `<thead>` (`sticky top-0 z-10 bg-panel`) and `<tbody>`.
    Put `max-h-[70vh] overflow-y-auto` on the CandidateRail Panel wrapper (the scroll ancestor the
    sticky thead needs). Add local sort state in AnalyzerDesktop (NOT the shared hook — mobile has no
    table, research OQ2): `useState<{ key: "score" | "debit" | "theta"; dir: "asc" | "desc" }>`
    defaulting to score-desc, plus a `useMemo` that sorts a COPY of the scored candidates by the active
    column/dir. Pass the sorted scored list + the sort state/handler into CandidateRail; pasted rows
    stay a separate pinned group above, unsorted.

    Build each row per the UI-SPEC columns above. Row-value cells use `font-mono tabular-nums
    text-[11px]`; column headers use the 10px tracked-uppercase SectionLabel tier. Selected tint ports
    CandidateCard.tsx:155's violet to the `<tr>` as `bg-violet/[0.06] border-l-2 border-l-violet`.
    The ⊕ cell is `<td onClick={(e) => e.stopPropagation()}>` wrapping a `Button variant="toggle"
    tone="amber" size="xs"` with `aria-label={\`Combine ${candidate.name}\`}` and
    `active={combinedIds.has(candidate.id)}`; the ⊕ column `<th>` carries a visually-hidden "Combine"
    label (sr-only span, Dim-2). Pasted rows add the existing × remove button (title "Remove this
    pasted calendar") beside ⊕. Keep the loading/error/cold-start/zero-filtered branches and their
    testids/copy exactly as today. Keep the `rail-legend` line copy but it may move below the table.

    Detail-pane Combine (UI-SPEC Detail Pane Composition): in the center-column "Risk profile" header
    (where Copy TOS order + the plan-41-01 LiveStatusBadge already sit), add a second Combine button —
    `Button variant="toggle" tone="amber"` calling `handleToggleCombine(selected)` with
    `active={combinedIds.has(selected.id)}` — so the SELECTED candidate can self-toggle into the book
    here, while the per-row ⊕ targets non-selected rows. Renders only when `selected !== null`.

    Tests: RED first. Rewrite the retired "Analyzer — ranked candidate rail (Task 2)" +
    "CandidateRail — zero-candidates empty state" describe blocks to assert on the new table: one
    `<tr>`/row testid per fixture candidate, score-desc default order, row click updates the selection
    (Risk-profile subtitle names the new candidate), a Debit-header click re-orders rows and sets
    aria-sort, the ⊕ cell click toggles combine without changing selection (stopPropagation), the
    pasted-unscored row shows PASTED + "—" cells. Use `stubDesktopMatchMedia` (desktop tree).
    Do NOT assert on any retired `candidate-card-*` testid inside these rail blocks.
  </action>
  <verify>
    <automated>bun run test apps/web/src/screens/Analyzer.test.tsx -t "rail"</automated>
  </verify>
  <done>
    Table renders/ sorts/ selects per the behavior list; sticky bounded Panel in place; the rail-scoped
    Analyzer.test.tsx blocks are migrated to table assertions and pass; no candidate-card testid remains
    in those blocks.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate the card-click test blocks across the rest of Analyzer.test.tsx</name>
  <files>apps/web/src/screens/Analyzer.test.tsx, apps/web/src/screens/Analyzer.tsx</files>
  <read_first>
    Analyzer.test.tsx describe blocks that select via a card click — "payoff center" (lines ~305-387),
    "right column" (~389-431), "pasted calendars" (~433-655), "copy TOS order" (~657-687), "payoff
    controls" (~688-721), "live-data states" (~723-807) — plus RESEARCH Pitfall 5 (test-migration
    undercounted). These select candidates by clicking a card today; they must click a table row now.
  </read_first>
  <behavior>
    - Every test that previously selected a candidate by clicking a `candidate-card-*` element now
      selects by clicking the corresponding table row, and still asserts the same downstream behavior
      (PayoffChart props, right-column re-wire, combined-book summary, copy-out, pasted flows).
    - The suite exercises the NEW table DOM — no test passes merely because it asserts on a retired
      testid that no longer exists (green-suite law, catches #26-#29).
  </behavior>
  <action>
    Sweep the remaining Analyzer.test.tsx desktop describe blocks: replace every card-based selection
    (`getByTestId("candidate-card-…")` click, or `within(card)` action-button lookups) with a
    table-row click / within-row action lookup that targets the new table markup from Task 1. Preserve
    every assertion's INTENT (re-price on new selection, ⊕ Combine sums the book, pasted card × removal
    + re-selection, Copy TOS order, date-projection toggles, the five live-data rail states). Where a
    test looked up the per-card Combine/Copy affordance, retarget it: Combine → the row's ⊕ cell; Copy
    → NOTE Copy moved to the detail-pane header (added in plan 41-03's detail-pane work) — if a Copy
    assertion has no table equivalent yet, keep it pointed at the existing detail-pane `copy-tos-order`
    button (unchanged this plan). Run the WHOLE Analyzer.test.tsx file green.
  </action>
  <verify>
    <automated>bun run test apps/web/src/screens/Analyzer.test.tsx</automated>
  </verify>
  <done>
    All Analyzer.test.tsx desktop blocks select via table rows and pass; no describe block relies on a
    retired card testid; full file green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pasted/scored candidate name → DOM | candidate.name rendered into table cells + `aria-label` (untrusted-ish: parsed from user paste or Zod-validated server response) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-02 | Information Disclosure (XSS) | candidate.name in table rows + `aria-label={\`Combine ${name}\`}` | low | mitigate | React auto-escapes text children and attribute values; no `dangerouslySetInnerHTML`; the name is a Zod-validated string (scored) or client-parsed TOS string (pasted) — no HTML sink introduced |
| T-41-Sort | Tampering | local sort state | low | accept | Sort is client-only UI state, never persisted, never sent to the API; wrong sort is a display-only nuisance, not a data-integrity risk |
| T-41-SC | Tampering | npm installs | n/a | accept | Zero package installs (native `<table>`, no shadcn Table) — no supply-chain surface |
</threat_model>

<verification>
- `bun run test apps/web/src/screens/Analyzer.test.tsx` — whole file green against the new table DOM.
- `bun run typecheck` — sort-state union typed, no any/as/!.
- Sticky/max-h are CSS-only (jsdom can't observe layout) — verified in the phase-gate UAT (plan 41-05).
</verification>

<success_criteria>
- One table row per candidate; row click drives detail panels; selected row visually explicit.
- Score/Debit/Θ sortable with aria-sort; default score-desc; pasted pinned above.
- Table Panel bounded at 70vh with sticky header; page height content-driven.
- Whole Analyzer.test.tsx green exercising the new table (no retired-testid false-green).
</success_criteria>

<output>
Create `.planning/phases/41-analyzer-desktop-redesign-ranked-table-verdict-hero-live-sidecar/41-02-SUMMARY.md` when done
</output>
