---
phase: 41
plan: 04
type: execute
wave: 4
depends_on: ["41-03"]
files_modified:
  - apps/web/src/screens/Analyzer.tsx
  - apps/web/src/screens/Analyzer.test.tsx
  - apps/web/src/screens/analyzer-mobile/MobileScorecard.tsx
  - apps/web/src/screens/analyzer-mobile/MobileScorecard.test.tsx
  - apps/web/src/components/picker/TermStructureChart.tsx
  - apps/web/src/components/picker/TermStructureChart.test.tsx
autonomous: true
requirements: [AUI-04, AUI-05, AUI-06]
must_haves:
  truths:
    - "No raw float like +61.9536112 or $652.2xxx renders anywhere on the Analyzer tab — dollars are whole, vega ≤2dp, theta ≤1dp"
    - "The Analyzer's dollar rounding uses a LOCAL Math.round formatter — NOT position-format.ts's exact-broker-value helpers (which stay the Journal/Overview contract)"
    - "The term-structure chart is taller (H 320), its short/long leg markers read more prominently, and each event line carries an in-chart label matching its below-chart legend chip"
    - "The desktop paste input is a larger, legible target with a clear Analyze affordance"
    - "The rounding fix applies identically on both desktop and mobile (same 5 call sites)"
  artifacts:
    - apps/web/src/components/picker/TermStructureChart.tsx
  key_links:
    - "TermStructureChart is imported unchanged by both Analyzer.tsx and AnalyzerMobile.tsx — one fix, both trees"
---

<objective>
AUI-04 (D-04): round every displayed number on the Analyzer tab to trading precision (dollars whole,
vega ≤2dp, theta ≤1dp) at the DISPLAY layer only — replacing the five `position-format` exact-value
call sites with a local Math.round/.toFixed formatter (reusing the Journal/Overview exact-broker-value
helpers here would silently regress their own locked contract). AUI-05 (D-05): give the term-structure
chart more height, more prominent leg markers, and in-chart event labels tied to the curve kinks; and
enlarge the desktop paste input/Analyze affordance. AUI-06: the rounding fix lands on the mobile
scorecard's two call sites in the same pass.

Purpose: numbers read at trading precision; the term-structure chart and paste flow read clean.
Output: rounded Analyzer numbers (both trees) + a taller/clearer term-structure chart + a bigger paste target.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-analyzer-desktop-redesign-ranked-table-verdict-hero-live-sidecar/41-UI-SPEC.md
@.planning/phases/41-analyzer-desktop-redesign-ranked-table-verdict-hero-live-sidecar/41-RESEARCH.md

@apps/web/src/screens/Analyzer.tsx
@apps/web/src/screens/analyzer-mobile/MobileScorecard.tsx
@apps/web/src/components/picker/TermStructureChart.tsx
@apps/web/src/lib/position-format.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AUI-04 number rounding — local formatter replaces the exact-value call sites</name>
  <files>
    apps/web/src/screens/Analyzer.tsx,
    apps/web/src/screens/Analyzer.test.tsx,
    apps/web/src/screens/analyzer-mobile/MobileScorecard.tsx,
    apps/web/src/screens/analyzer-mobile/MobileScorecard.test.tsx
  </files>
  <read_first>
    UI-SPEC Number Rounding Contract (the field→rule table, and the explicit "do NOT reuse the
    position-format exact-value helpers" note), RESEARCH §Rounding Contract (the exhaustive 5-call-site
    enumeration: 3 in Analyzer.tsx's selected-name subline + combined-book summary, 2 in
    MobileScorecard.tsx's context line + combined-book summary), position-format.ts (the helpers that
    must NOT be reused here).
  <!-- planner-discipline-allow: exactAbs -->
  </read_first>
  <behavior>
    - Desktop selected-name subline: `debit $${Math.round(debit)}` and `vega +${vega.toFixed(2)}`
      (theta already `.toFixed(1)`, unchanged); combined-book summary: `Math.round(bookDebit)` and
      `bookVega.toFixed(2)`.
    - Mobile MobileScorecard context line + combined-book summary: same substitutions.
    - A vega like 61.9536112 renders as +61.95; a debit like 652.2 renders as $652 — no long decimals
      on the tab.
  </behavior>
  <action>
    Replace each exact-value dollar/vega call at the 5 enumerated sites: `exactAbs(X.debit)` →
    `Math.round(X.debit)`, `exactAbs(X.vega)`/`exactAbs(bookVega)` → `X.vega.toFixed(2)`/
    `bookVega.toFixed(2)`, in Analyzer.tsx (3 sites, selected subline + combined-book summary) and
    MobileScorecard.tsx (2 sites). Leave the `theta.toFixed(1)` calls untouched (already compliant).
    Remove the now-unused `exactAbs` import from BOTH files (typecheck fails on an unused import). Do
    NOT introduce a new shared formatter module — inline Math.round/.toFixed(2) at each site (UI-SPEC
    ladder rung 6). Do NOT reuse usd()/signedUsd()/the exact-broker-value helper here.
    <!-- planner-discipline-allow: exactAbs -->

    Tests: RED first. Extend the existing subline/context-line assertions (they already read the
    rendered text via getByTestId("risk-profile-selected-name")-adjacent content / combined-book-summary)
    to assert the ROUNDED values — e.g. the subline contains `$652` and `+61.95` and NOT the long-decimal
    form for the fixture candidate. Add a mobile assertion in MobileScorecard.test.tsx for the same.
  </action>
  <verify>
    <automated>bun run test apps/web/src/screens/Analyzer.test.tsx apps/web/src/screens/analyzer-mobile/MobileScorecard.test.tsx</automated>
  </verify>
  <done>
    All 5 call sites rounded; the exact-value import removed from both files; tests assert whole dollars
    + 2dp vega on both trees; suites green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AUI-05 term-structure chart — taller, prominent leg markers, in-chart event labels</name>
  <files>
    apps/web/src/components/picker/TermStructureChart.tsx,
    apps/web/src/components/picker/TermStructureChart.test.tsx
  </files>
  <read_first>
    UI-SPEC Paste Flow + Term Structure Polish section (H, r, fontSize, opacity, event label — LOCKED
    numeric values), TermStructureChart.tsx:30-32 (W/H), :157 (aspect ratio className), :186-199 (event
    ReferenceLines — currently opacity 0.3, no in-chart label), :237-254 (leg ReferenceDots — r 5.5,
    label fontSize 12), TermStructureChart.test.tsx (extend, don't create).
  </read_first>
  <behavior>
    - Chart height H = 320 (W stays 760); the ChartContainer wrapper aspect ratio becomes 760/320 and
      the LineChart height prop is 320 — the plot has more vertical room.
    - Leg ReferenceDot radius r = 7 (was 5.5); leg label fontSize = 13 (was 12) — "short f"/"long b"
      read more prominently.
    - Each event ReferenceLine gets `label={{ value: <event name>, position: "top", fontSize: 10,
      fill: AMBER }}` (the same name its below-chart legend chip carries) and opacity 0.5 (was 0.3) —
      the eye can trace the in-chart line to its legend chip.
    - The guard-tag / fwd-bracket / axis behavior is otherwise unchanged.
  </behavior>
  <action>
    In TermStructureChart.tsx: change `H` from 230 to 320; update the ChartContainer className aspect
    ratio to `aspect-[760/320]` and the `max-w-[760px]` wrapper stays; the LineChart `height={H}` picks
    up 320 automatically. Change both leg ReferenceDots' `r={5.5}` → `r={7}` and their label
    `fontSize: 12` → `fontSize: 13`. In the events `.map()` that renders ReferenceLines, add a `label`
    prop `{{ value: ev.name, position: "top", fontSize: 10, fill: AMBER }}` and raise `opacity` from
    0.3 to 0.5. Leave the fwd-bracket, GuardTag, axes, legend chips, and caption unchanged.

    Tests: RED first, in TermStructureChart.test.tsx — assert the LineChart renders at height 320 (or
    the aspect-[760/320] wrapper class is present), the leg dots carry r=7, and an event ReferenceLine
    carries an in-chart label with the event's name. Extend the existing suite; keep the guard-case
    (fwdIv null → guard tag, no bracket) test green.
  </action>
  <verify>
    <automated>bun run test apps/web/src/components/picker/TermStructureChart.test.tsx</automated>
  </verify>
  <done>
    Chart height 320, leg dots r=7 / label 13px, event lines labelled + opacity 0.5; test asserts the
    new values; guard-case test still green.
  </done>
</task>

<task type="auto">
  <name>Task 3: AUI-05 desktop paste-input polish (styling-only)</name>
  <files>apps/web/src/screens/Analyzer.tsx</files>
  <read_first>
    UI-SPEC "Paste input (desktop only)" note, Analyzer.tsx:133-145 (CandidateRail paste input +
    Analyze Button; mobile's input is already min-h-11 from Phase 36, unchanged).
  </read_first>
  <action>
    In CandidateRail's paste block: grow the `<input>` from `px-2 py-1 text-[10px]` to
    `px-3 py-2 text-[12px]` for a larger, legible target; grow the "Analyze" Button from its implicit
    `size` to `size="sm"` (it is already `variant="primary"`). No copy change, no new affordance, no
    behavior change — hit-target + legibility only. This is a tdd.md styling-only exemption (no new
    branch), so no RED test; the existing paste tests must stay green unchanged.
  </action>
  <verify>
    <automated>bun run test apps/web/src/screens/Analyzer.test.tsx -t "pasted calendars"</automated>
  </verify>
  <done>
    Desktop paste input is px-3 py-2 text-[12px] with a size="sm" Analyze button; existing paste tests
    still green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored value → displayed number | Rounding must be display-only; the underlying PickerCandidate domain values stay untouched (D-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-05 | Tampering (of data meaning) | number rounding | low | mitigate | Formatting applied only in the render string; no PickerCandidate field is mutated; the API/DB/scoring values are unaffected (D-04) — verified by touching only render expressions, never the model |
| T-41-06 | Information Disclosure | contract regression | low | mitigate | Analyzer uses a LOCAL Math.round formatter, never position-format.ts's usd/signedUsd/exact helpers — keeps the Journal/Overview exact-broker-value contract intact |
| T-41-SC | Tampering | npm installs | n/a | accept | Zero package installs this phase |
</threat_model>

<verification>
- `bun run test apps/web/src/screens/Analyzer.test.tsx apps/web/src/screens/analyzer-mobile/MobileScorecard.test.tsx apps/web/src/components/picker/TermStructureChart.test.tsx` — green.
- `bun run typecheck` — the exact-value import removed cleanly; no unused-import error.
- No long-decimal render remains — asserted by the extended subline/context-line string tests.
</verification>

<success_criteria>
- Dollars whole, vega ≤2dp, theta ≤1dp across desktop + mobile; no raw floats.
- Local formatter (no position-format exact helpers reused).
- Term-structure chart taller (H 320), leg dots r=7, event lines labelled at opacity 0.5.
- Desktop paste input enlarged; existing paste tests green.
</success_criteria>

<output>
Create `.planning/phases/41-analyzer-desktop-redesign-ranked-table-verdict-hero-live-sidecar/41-04-SUMMARY.md` when done
</output>
