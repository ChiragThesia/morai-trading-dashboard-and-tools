---
phase: 11-sidecar-scaffold-auth-migration
plan: 06
type: tdd
wave: 5
depends_on: ["11-03", "11-05"]
files_modified:
  - apps/worker/src/schedule.test.ts
  - apps/worker/src/schedule.ts
  - apps/worker/src/main.ts
  - apps/worker/src/config.ts
autonomous: true
requirements: [GW-03, JRNL-02, GW-02]
user_setup: []

must_haves:
  truths:
    - "schedule.ts no longer creates/schedules/works the refresh-tokens queue, and AllHandlers drops refreshTokens — drops from 6 scheduled crons to 5 (GW-03 sole-writer: TS refresher retired before the sidecar's auto-refresh activates)"
    - "schedule.test.ts asserts exactly 5 scheduled jobs and 9 queues, with NO 'refresh-tokens' entry, and the refresh-tokens cron test is deleted (RED-first per PATTERNS Pattern 4)"
    - "worker main.ts swaps schwabMarketAdapter for makeSidecarChainAdapter as selectChainSource's schwabFetchChain input; cboeFetchChain + readTokenFreshness wiring unchanged (JRNL-02)"
    - "CBOE fallback still activates when the market token freshness is AUTH_EXPIRED — leg_observations continues to gain source='cboe' rows in that case (selectChainSource unchanged; existing test stays green)"
    - "worker config.ts adds SIDECAR_URL (z.string().url()); the retired refresh-tokens OAuth client wiring + handler import are removed from main.ts"
    - "Cross-cutting regression gates stay green after the chain-source switch: SPX OI=0/SPY proxy, CBOE UTC timestamps, GEX put-sign, 65,534-param chunking"
  prohibitions:
    - "MUST NOT leave the refresh-tokens job active alongside the sidecar refresher — no dual-writer window (GW-03 / RESEARCH Pitfall 4); this retirement is the hard-cut, one-release cutover (D-06)"
    - "MUST NOT remove the trader Schwab APP_KEY/SECRET fields from worker config — the trader data path (sync-transactions) stays DIRECT this phase (D-01); only the OAuth-client/refresh wiring and the MARKET chain adapter are retired/swapped"
    - "MUST NOT add a runtime feature-flag for the chain-source toggle — rejected (D-06); CBOE fallback is the continuity safety net"
    - "MUST NOT change selectChainSource itself or packages/contracts — only the worker composition-root wiring changes"
  artifacts:
    - path: "apps/worker/src/schedule.ts"
      provides: "9-queue / 5-cron scheduler with refresh-tokens retired"
      contains: "createQueue"
    - path: "apps/worker/src/main.ts"
      provides: "sidecar chain adapter wired as schwabFetchChain; refresh-tokens OAuth/handler wiring removed"
      contains: "makeSidecarChainAdapter"
    - path: "apps/worker/src/config.ts"
      provides: "SIDECAR_URL added to workerConfigSchema"
      contains: "SIDECAR_URL"
  key_links:
    - from: "apps/worker/src/main.ts"
      to: "packages/adapters/src/sidecar/chain-adapter.ts (11-03)"
      via: "makeSidecarChainAdapter({fetch, sidecarUrl: config.SIDECAR_URL}).fetchChain → selectChainSource.schwabFetchChain"
      pattern: "selectChainSource"
    - from: "apps/worker/src/schedule.ts"
      to: "GET /api/status lastJobRuns"
      via: "retiring the refresh-tokens queue/cron/handler removes it from lastJobRuns (SC2)"
      pattern: "refresh-tokens"
---

<objective>
Execute the GW-03 sole-writer cutover in one release: retire the TS `refresh-tokens` job
(remove it from `schedule.ts`, `AllHandlers`, and the worker's OAuth-client/handler wiring)
and swap the worker's chain source from the direct `schwabMarketAdapter` to the new
`makeSidecarChainAdapter` (11-03) as `selectChainSource`'s `schwabFetchChain` input. TDD
red-first on `schedule.test.ts` (PATTERNS Pattern 4): update the test constants/assertions to
expect 9 queues / 5 crons with no `refresh-tokens`, watch it go RED, then retire from
`schedule.ts` to GREEN.

Purpose: Close the dual-refresher rotating-token race (the milestone's core risk) — exactly
one process (the sidecar) writes `broker_tokens` (GW-03). Re-source the chain through the
sidecar (JRNL-02) while leaving `selectChainSource` + the CBOE fallback untouched so the
journal never stalls during the 7-day re-auth gap. The trader data path stays direct (D-01).

Output: retired `refresh-tokens` job; sidecar chain wired in; `SIDECAR_URL` config; the
schedule test updated; regression gates green.
</objective>

<artifacts_this_phase_produces>
This plan modifies (self-modify, no new files):
- **Scheduler:** `apps/worker/src/schedule.ts` — refresh-tokens queue/cron/handler removed (9 queues, 5 crons, 9 handlers); header comments updated.
- **Scheduler test:** `apps/worker/src/schedule.test.ts` — constants ALL_10_QUEUES→ALL_9_QUEUES, SCHEDULED_6→SCHEDULED_5, refresh-tokens cron test deleted, makeFakeHandlers drops refreshTokens.
- **Composition root:** `apps/worker/src/main.ts` — makeSidecarChainAdapter wired as schwabFetchChain; refresh-tokens OAuth-client construction + handler import removed; the boot log message updated to 9 queues / 5 jobs.
- **Config:** `apps/worker/src/config.ts` — SIDECAR_URL added (trader Schwab fields kept; market chain fields no longer used by the worker may be removed only if unreferenced).
(The sidecar service + SIDECAR_URL env on Railway are set in 11-05's deploy checkpoint.)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-sidecar-scaffold-auth-migration/11-RESEARCH.md
@.planning/phases/11-sidecar-scaffold-auth-migration/11-PATTERNS.md

# Self-modify targets (retire refresh-tokens; swap chain source; add SIDECAR_URL)
@apps/worker/src/schedule.ts
@apps/worker/src/schedule.test.ts
@apps/worker/src/main.ts
@apps/worker/src/config.ts
# The selector whose wiring stays identical (only the schwabFetchChain input changes)
@packages/core/src/brokerage/application/selectChainSource.ts
</context>

<feature>
  <name>GW-03 sole-writer cutover: retire refresh-tokens + swap chain source to the sidecar (one release, D-06)</name>
  <files>
    apps/worker/src/schedule.test.ts,
    apps/worker/src/schedule.ts,
    apps/worker/src/main.ts,
    apps/worker/src/config.ts
  </files>
  <behavior>
    RED-first on schedule.test.ts (PATTERNS Pattern 4):
    - "calls createQueue for all job names" → 9 queues, array excludes "refresh-tokens".
    - "calls schedule for exactly N jobs" → toHaveLength(5); SCHEDULED_5 excludes "refresh-tokens".
    - "refresh-tokens cron is '0 4 * * *'..." → DELETE this entire test block.
    - "calls work() for all queues" → 9 queues.
    - makeFakeHandlers() drops the refreshTokens entry.
    Run the suite → RED (schedule.ts still registers refresh-tokens). Then retire from schedule.ts → GREEN.
    Existing selectChainSource.test.ts (AUTH_EXPIRED → CBOE) stays GREEN unchanged.
  </behavior>
  <read_first>
    - apps/worker/src/schedule.test.ts (lines 45-97, 144-158 — ALL_10_QUEUES, SCHEDULED_6, makeFakeHandlers, the refresh-tokens cron test to delete)
    - apps/worker/src/schedule.ts (the AllHandlers refreshTokens field; createQueue/schedule/work refresh-tokens calls; header comment counts)
    - apps/worker/src/main.ts (lines 25-26, 49, 59, 106-161, 363-396 — schwab adapter/OAuth/refresh wiring to remove; selectChainSource block to repoint; trader wiring at 301-355 STAYS)
    - apps/worker/src/config.ts (workerConfigSchema — add SIDECAR_URL; KEEP SCHWAB_TRADER_* for the direct trader path, D-01)
    - .planning/phases/11-sidecar-scaffold-auth-migration/11-PATTERNS.md (§ schedule.ts / schedule.test.ts / main.ts / config.ts self-modify sections — exact diffs)
  </read_first>
  <implementation>
    Step 1 (RED): edit schedule.test.ts per the behavior list — rename/shrink ALL_10_QUEUES→ALL_9_QUEUES and SCHEDULED_6→SCHEDULED_5 (remove "refresh-tokens"), change toHaveLength(6)→(5), delete the refresh-tokens cron test block, drop refreshTokens from makeFakeHandlers. Run `bun run test apps/worker/src/schedule.test.ts` → confirm RED for the right reason (schedule.ts still registers 10/6).
    Step 2 (GREEN): edit schedule.ts — remove `readonly refreshTokens` from AllHandlers; remove `createQueue("refresh-tokens")`, `schedule("refresh-tokens", "0 4 * * *", ...)`, and `work("refresh-tokens", ...)`; update header comments (10→9 queues, 6→5 crons, 10→9 handlers). Run the test → GREEN.
    Step 3 (chain swap, JRNL-02): in main.ts, remove the makeSchwabChainAdapter import + marketGetAccessToken + schwabMarketAdapter block and the refresh-tokens OAuth-client construction (traderOAuthClient/marketOAuthClient used only for refresh) + makeRefreshTokens* use-cases + the refresh-tokens handler import/wiring; add `import { makeSidecarChainAdapter } from "@morai/adapters"` and `const sidecarAdapter = makeSidecarChainAdapter({ fetch: globalThis.fetch, sidecarUrl: config.SIDECAR_URL })`; in the selectChainSource block change `schwabFetchChain: schwabMarketAdapter.fetchChain` → `schwabFetchChain: sidecarAdapter.fetchChain` (readTokenFreshness + cboeFetchChain unchanged). KEEP the trader/sync-transactions wiring (traderGetAccessToken + makeSchwabTransactionsAdapter) — D-01 direct trader path. If traderOAuthClient is still referenced by the trader refresh path, surface it: the trader on-demand refresh moves to the sidecar as sole writer, so the worker's trader path reads tokens the sidecar keeps fresh (no TS refresh). Update the worker boot log string to "9 queues created, 5 jobs scheduled" and remove refresh-tokens from that list.
    Step 4 (config): add `SIDECAR_URL: z.string().url("SIDECAR_URL must be a valid URL")` to workerConfigSchema. Keep SCHWAB_TRADER_APP_KEY/SECRET (+ callback if the trader path needs it). Remove only config fields that become genuinely unreferenced after the swap (lint's no-unused will flag them) — confirm with a build, do not blind-delete.
    Commit at green only (RED→GREEN→typecheck/lint clean). No any/as/!.
  </implementation>
  <verify>
    <automated>bun run test apps/worker/src/schedule.test.ts && bun run test packages/core/src/brokerage/application/selectChainSource.test.ts && bun run typecheck && bun run lint</automated>
  </verify>
  <acceptance_criteria>
    - schedule.test.ts asserts SCHEDULED == 5 and queues == 9 with no "refresh-tokens"; the refresh-tokens cron test block is gone; suite GREEN.
    - `grep -c 'refresh-tokens' apps/worker/src/schedule.ts` returns 0 (no createQueue/schedule/work/handler for it; filter comment lines: `grep -v '^[[:space:]]*//' apps/worker/src/schedule.ts | grep -c 'refresh-tokens'` == 0).
    - main.ts wires `makeSidecarChainAdapter(...).fetchChain` as selectChainSource's schwabFetchChain; `grep makeSchwabChainAdapter apps/worker/src/main.ts` returns nothing.
    - selectChainSource.test.ts (AUTH_EXPIRED → CBOE) stays GREEN; `bun run typecheck && bun run lint` clean.
    - SIDECAR_URL present in workerConfigSchema; SCHWAB_TRADER_* fields retained (D-01).
  </acceptance_criteria>
  <done>The refresh-tokens job is retired (sole-writer cutover, GW-03), the worker chain source is the sidecar (JRNL-02) with CBOE fallback intact, SIDECAR_URL is configured, and all tests + typecheck + lint are green.</done>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker token writer → broker_tokens | After cutover, the worker no longer writes tokens; the sidecar is sole writer (GW-03) |
| worker → sidecar (chain) | The worker now fetches the chain over the private network via SIDECAR_URL; CBOE fallback unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06-01 | Tampering | dual-writer rotating-token race | mitigate | refresh-tokens retired in the same release the sidecar refresher activates (GW-03 / RESEARCH Pitfall 4); no dual-writer window; verified at deploy by GET /api/status no longer listing refresh-tokens (SC2) |
| T-11-06-02 | Denial of Service | chain stalls if sidecar unreachable | accept/mitigate | CBOE fallback driven by token freshness (AUTH_EXPIRED) keeps the journal writing source='cboe'; transient sidecar errors fail-loud and log (RESEARCH Pitfall 6) — correct behavior, not silent fallback |
| T-11-06-03 | Tampering | regression in chain-source switch | mitigate | Cross-cutting regression gates (SPX OI=0/SPY proxy, CBOE UTC, GEX put-sign, 65,534-param chunking) re-run green after the swap; selectChainSource + persistObservations unchanged |
| T-11-06-04 | Information Disclosure | leftover Schwab OAuth secrets in worker | mitigate | Retired refresh-tokens OAuth-client wiring removed from the worker here; only the direct trader data path keeps SCHWAB_TRADER_* (D-01); no token values logged. The standalone `apps/auth` (`@morai/auth`) setup/refresh CLI is fully retired in 11-07 (D-04, D16 superseded) — the sidecar is the sole dance + refresh owner |
</threat_model>

<verification>
- `bun run test apps/worker/src/schedule.test.ts` green (9 queues / 5 crons, no refresh-tokens).
- `bun run test packages/core/src/brokerage/application/selectChainSource.test.ts` green (CBOE fallback intact).
- `bun run typecheck && bun run lint` clean; `bun run test` full suite green (regression gates: SPX OI/SPY proxy, CBOE UTC timestamps, GEX put-sign, 65,534-param chunking).
- `grep` confirms refresh-tokens fully retired and makeSchwabChainAdapter no longer wired in the worker.
</verification>

<success_criteria>
- SC2 (GW-03): refresh-tokens removed from schedule.ts + TRACKED_JOBS path; GET /api/status no longer lists it; no dual-writer window.
- SC3 (JRNL-02/GW-02): the chain-snapshot job routes through the sidecar; CBOE fallback activates on AUTH_EXPIRED and leg_observations gains source='cboe' rows.
- All cross-cutting regression gates remain green after the switch.
</success_criteria>

<output>
Create `.planning/phases/11-sidecar-scaffold-auth-migration/11-06-SUMMARY.md` when done
</output>
