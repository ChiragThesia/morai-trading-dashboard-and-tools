---
phase: 11-sidecar-scaffold-auth-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture/stack-decisions.md
  - packages/adapters/src/postgres/schema.ts
  - apps/sidecar/pytest.ini
  - apps/sidecar/tests/__init__.py
  - apps/sidecar/tests/test_token_store.py
  - apps/sidecar/tests/test_advisory_lock.py
  - apps/sidecar/tests/test_chain_proxy.py
  - packages/adapters/src/sidecar/chain-adapter.test.ts
autonomous: true
requirements: [GW-01, JRNL-02]
user_setup: []

must_haves:
  truths:
    - "docs/architecture/stack-decisions.md §D22 records the GW-01 relaxation (token_json JSONB column added; dual-write decompose so the TS trader reader stays on discrete columns) WITH rationale, BEFORE the migration lands (per D-02)"
    - "broker_tokens schema gains a nullable token_json JSONB column (additive; no existing column changed; preserves refresh_issued_at semantics)"
    - "Python pytest lane scaffold exists and runs (pytest.ini + tests package) with RED placeholder tests for token round-trip (GW-01), advisory lock (GW-04), chain proxy (GW-02)"
    - "TS sidecar adapter RED test scaffold exists at packages/adapters/src/sidecar/chain-adapter.test.ts and fails on unresolved SUT import (JRNL-02)"
  prohibitions:
    - "MUST NOT apply the migration in this plan — 11-02 owns drizzle generate + bun run migrate (the [BLOCKING] task)"
    - "MUST NOT modify or drop any existing broker_tokens column (access_token, refresh_token, issued_at, refresh_issued_at, expires_at, updated_at, last_refresh_error) — token_json is purely additive"
    - "MUST NOT write any sidecar production .py implementation here — scaffolds are failing tests only (TDD red-first)"
  artifacts:
    - path: "docs/architecture/stack-decisions.md"
      provides: "GW-01 relaxation record (token_json additive column + dual-write rationale)"
      contains: "token_json"
    - path: "packages/adapters/src/postgres/schema.ts"
      provides: "broker_tokens.token_json JSONB column (Drizzle)"
      contains: "token_json"
    - path: "apps/sidecar/pytest.ini"
      provides: "Python/pytest CI lane config (D-07)"
    - path: "apps/sidecar/tests/test_token_store.py"
      provides: "RED scaffold: token round-trip + refresh_issued_at-unchanged (GW-01)"
    - path: "apps/sidecar/tests/test_advisory_lock.py"
      provides: "RED scaffold: two-instance lock failure (GW-04)"
    - path: "apps/sidecar/tests/test_chain_proxy.py"
      provides: "RED scaffold: chain shape + AUTH_EXPIRED 503 (GW-02)"
    - path: "packages/adapters/src/sidecar/chain-adapter.test.ts"
      provides: "RED scaffold: TS Zod parse of /sidecar/chain (JRNL-02)"
  key_links:
    - from: "docs/architecture/stack-decisions.md"
      to: "packages/adapters/src/postgres/schema.ts"
      via: "docs-before-code: the GW-01 relaxation is recorded BEFORE the schema column it authorizes"
      pattern: "token_json"
---

<objective>
Lay the docs + schema + test-scaffold foundation for the sidecar migration. This plan
satisfies the docs-before-code rule for the GW-01 relaxation, adds the additive
`token_json` JSONB column to the Drizzle `broker_tokens` schema (the migration itself is
applied in 11-02), and ships the Wave-0 failing-test scaffolds for both CI lanes (Python
pytest for the sidecar, Vitest for the new TS adapter).

Purpose: Honor the strict docs-before-architecture-change rule (GW-01's "no schema
change" clause is *relaxed* by adding `token_json` — this MUST be recorded first, per
D-02 which pre-authorized the column). Establish the TDD RED baseline so 11-03/11-04/11-05
turn the scaffolds green.

Output: stack-decisions.md §D22 update; `broker_tokens.token_json` schema column; Python
pytest harness + 3 RED test files; 1 TS RED adapter test.
</objective>

<artifacts_this_phase_produces>
This plan creates:
- **Docs:** stack-decisions.md §D22 GW-01-relaxation entry (token_json additive column + dual-write decompose rationale).
- **Schema:** `broker_tokens.token_json` (Drizzle `jsonb`, nullable) — column added to `packages/adapters/src/postgres/schema.ts`.
- **Python test lane:** `apps/sidecar/pytest.ini`, `apps/sidecar/tests/__init__.py`, and RED scaffolds `test_token_store.py`, `test_advisory_lock.py`, `test_chain_proxy.py`.
- **TS test:** RED scaffold `packages/adapters/src/sidecar/chain-adapter.test.ts`.
(Downstream plans add: sidecar Python modules, the TS adapter + twin, migration 0011, FastAPI routes, Dockerfile, Railway service, SIDECAR_URL env var, schedule retirement.)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-sidecar-scaffold-auth-migration/11-CONTEXT.md
@.planning/phases/11-sidecar-scaffold-auth-migration/11-RESEARCH.md
@.planning/phases/11-sidecar-scaffold-auth-migration/11-PATTERNS.md

# Source of truth for the decision record being updated
@docs/architecture/stack-decisions.md
# Schema file the additive column lands in (broker_tokens at line ~206)
@packages/adapters/src/postgres/schema.ts
</context>

<tasks>

<task type="execute">
  <name>Task 1: Record the GW-01 relaxation in stack-decisions.md (docs-before-code)</name>
  <read_first>
    - docs/architecture/stack-decisions.md (§D22 schwab-py sidecar entry from Phase 10 — the area to extend)
    - .planning/phases/11-sidecar-scaffold-auth-migration/11-RESEARCH.md (§ "Token Store Mapping" / Pattern 1 — the D-02 resolution this entry documents)
    - .planning/phases/11-sidecar-scaffold-auth-migration/11-CONTEXT.md (D-02 — pre-authorized "add one token_json blob column (relaxes GW-01)")
  </read_first>
  <action>
    Extend the §D22 schwab-py sidecar entry in docs/architecture/stack-decisions.md to record the GW-01 relaxation per D-02. State, in Hemingway prose: GW-01 was carried-forward locked as "no schema change"; D-02 pre-authorized adding one `token_json` JSONB column to `broker_tokens` as the option that "relaxes GW-01". Record the rationale: schwab-py's `client_from_access_functions` advises storing its token object as an opaque JSON blob ("don't inspect it"), so the sidecar writes the full wrapped blob to `token_json` AND decomposes the inner `access_token`/`refresh_token` into the existing encrypted discrete columns (dual-write) so the TS trader reader stays on discrete columns unchanged (D-01 chain-only — trader stays direct). State the preserved invariant: `refresh_issued_at` anchors the 7-day TTL and is NEVER reset on access-token rotation (Phase 4 P02 rule). Note the column is additive and nullable (NULL until first sidecar OAuth dance seeds it, D-03). Cite D-02 and GW-01 by ID in the prose. Do NOT alter the existing D16/D17/D22 narrative; append/extend only. Per docs.md: if this warrants a TOPIC-MAP touch, none is required (no new doc file — entry lives in the existing stack-decisions.md).
  </action>
  <verify>
    <automated>grep -q 'token_json' docs/architecture/stack-decisions.md && grep -q 'refresh_issued_at' docs/architecture/stack-decisions.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep token_json docs/architecture/stack-decisions.md` matches at least once (the relaxation is recorded).
    - The §D22 area cites GW-01 and D-02 and states the dual-write decompose + the `refresh_issued_at` never-reset invariant.
    - No existing decision line in the file was deleted (git diff shows additions/extensions only).
  </acceptance_criteria>
  <done>The GW-01 relaxation (token_json additive column + dual-write rationale + refresh_issued_at anchor) is recorded in stack-decisions.md §D22 BEFORE any schema/migration change.</done>
</task>

<task type="execute">
  <name>Task 2: Add the additive token_json JSONB column to the Drizzle broker_tokens schema</name>
  <read_first>
    - packages/adapters/src/postgres/schema.ts (lines ~206-226 — the `brokerTokens` pgTable definition; note `jsonb` is the Drizzle import to use, matching gexSnapshots JSONB usage elsewhere in this file)
    - docs/architecture/stack-decisions.md (the §D22 relaxation recorded in Task 1 — schema must match what docs authorized)
  </read_first>
  <action>
    In packages/adapters/src/postgres/schema.ts, add a single nullable column to the `brokerTokens` pgTable: `tokenJson: jsonb("token_json")` (Drizzle `jsonb` from drizzle-orm/pg-core — import it if not already imported in this file). Place it after `lastRefreshError` so the diff is contained. The column is nullable by design (NULL until the sidecar's first OAuth dance seeds it, D-03 / RESEARCH Open Question 2). Add a one-line comment: full schwab-py wrapped token blob (opaque); sidecar is sole writer (GW-03); decomposed access/refresh also kept in the discrete encrypted columns for the TS reader (D-01). Do NOT change any existing column. Do NOT run drizzle-kit generate or migrate here — 11-02 owns that ([BLOCKING] migration). Per typescript.md: no `any`, no `as`.
  </action>
  <verify>
    <automated>grep -q 'token_json' packages/adapters/src/postgres/schema.ts && grep -Eq 'jsonb' packages/adapters/src/postgres/schema.ts && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep token_json packages/adapters/src/postgres/schema.ts` matches (column declared).
    - The column uses Drizzle `jsonb(...)` and is nullable (no `.notNull()`).
    - No existing broker_tokens column declaration was modified or removed (git diff is an addition inside the pgTable object).
  </acceptance_criteria>
  <done>`broker_tokens` schema declares a nullable `token_json` JSONB column; all existing columns are untouched.</done>
</task>

<task type="execute">
  <name>Task 3: Scaffold the Python pytest lane + 3 RED test files, and 1 TS RED adapter test</name>
  <read_first>
    - .planning/phases/11-sidecar-scaffold-auth-migration/11-RESEARCH.md (§ Validation Architecture → "Wave 0 Gaps" list; § Pattern 1 round-trip contract test steps; § Pattern 3 advisory-lock test steps; § Pattern 5 chain shape)
    - packages/adapters/src/memory/chain.contract.test.ts (structural analog for a TS chain test shape)
    - packages/adapters/src/postgres/repos/broker-tokens.contract.test.ts (structural analog for a token round-trip contract test shape)
  </read_first>
  <action>
    Create the Python test lane scaffold under apps/sidecar/: `pytest.ini` (minimal `[pytest]` config: testpaths=tests, asyncio_mode=auto), `tests/__init__.py` (empty package marker), and three RED test files that fail by importing not-yet-existing sidecar modules (TDD red-first):
    (a) `tests/test_token_store.py` — two tests: `test_token_round_trip` (write a sample wrapped blob `{creation_timestamp, token:{access_token, refresh_token, expires_at, token_type, scope}}` via `token_write_func`, read back via `token_read_func`, assert byte-for-byte equal AND the discrete access_token column matches `sample["token"]["access_token"]`); `test_refresh_issued_at_unchanged` (a write does NOT change `refresh_issued_at`). Import from `apps/sidecar/token_store.py` (does not exist yet → RED).
    (b) `tests/test_advisory_lock.py` — `test_second_instance_fails` (hold the lock key 8876543210 on a setup connection, then call `acquire_sidecar_lock` and assert SystemExit + a log/error mentioning "another sidecar instance is running"); `test_first_instance_acquires` (clean acquire returns an open connection). Import from `apps/sidecar/advisory_lock.py` (does not exist yet → RED).
    (c) `tests/test_chain_proxy.py` — `test_chain_shape` (GET /sidecar/chain returns the RawChain-mirroring JSON: root, observedAt, spot, quotes[], source="schwab_chain"); `test_auth_expired` (auth failure → HTTP 503 body `{"error":"AUTH_EXPIRED"}`). Use FastAPI `TestClient`. Import the app from `apps/sidecar/main.py` (does not exist yet → RED).
    Also create the TS RED scaffold `packages/adapters/src/sidecar/chain-adapter.test.ts`: a Vitest test that imports `makeSidecarChainAdapter` from the sibling `./chain-adapter.ts` (does not exist yet → RED on import) and asserts a valid `/sidecar/chain` body Zod-parses into a `RawChain` with `source: "schwab_chain"` and that a 503 `{error:"AUTH_EXPIRED"}` body maps to `err({kind:"fetch-error", message:"AUTH_EXPIRED"})`. Use an injected fake `fetch`.
    Do NOT create any production .py or chain-adapter.ts here — only failing tests. Per tdd.md: tests must fail for the RIGHT reason (unresolved SUT import), not a syntax error.
  </action>
  <verify>
    <automated>test -f apps/sidecar/pytest.ini && test -f apps/sidecar/tests/test_token_store.py && test -f apps/sidecar/tests/test_advisory_lock.py && test -f apps/sidecar/tests/test_chain_proxy.py && test -f packages/adapters/src/sidecar/chain-adapter.test.ts && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All five files exist (pytest.ini + 3 python tests + 1 TS test).
    - `cd apps/sidecar && pytest -q` fails on unresolved imports of token_store/advisory_lock/main (RED for the right reason), once pytest + deps are installed (11-05 ships requirements.txt; if pytest is not yet installed in the harness, the file presence + import targets are the gate here).
    - `bun run test packages/adapters/src/sidecar/chain-adapter.test.ts` fails on the unresolved `./chain-adapter.ts` import (RED).
  </acceptance_criteria>
  <done>Both CI lanes have committed RED scaffolds that pin the GW-01/GW-02/GW-04/JRNL-02 behaviors and will turn green in 11-03/11-04/11-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → schema | The docs-before-code gate: the relaxation must be recorded before the column lands |
| schema → migration (next plan) | Additive column; no data loss path, but a non-additive edit here would risk the live broker_tokens rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Tampering | broker_tokens schema edit | mitigate | Column is strictly additive + nullable; Task 2 acceptance criterion asserts no existing column changed (git diff additions only) |
| T-11-01-02 | Information Disclosure | token_json column documentation | accept | Docs describe the column's *purpose*, never token values; no secret material in docs or schema source |
| T-11-01-03 | Repudiation | docs-before-code ordering | mitigate | Task 1 (docs) is sequenced before Task 2 (schema) within this plan; 11-02 migration depends on this plan |
</threat_model>

<verification>
- `grep token_json docs/architecture/stack-decisions.md` and `grep token_json packages/adapters/src/postgres/schema.ts` both match.
- `bun run typecheck` passes (schema column compiles; Drizzle `jsonb` typed).
- The five scaffold files exist; TS scaffold is RED on the missing `./chain-adapter.ts` import.
- No existing broker_tokens column was modified (git diff review).
</verification>

<success_criteria>
- GW-01 relaxation recorded in stack-decisions.md §D22 before the schema column (docs-before-code).
- `broker_tokens.token_json` nullable JSONB column added to the Drizzle schema (additive only).
- Python pytest lane scaffold + 3 RED tests + 1 TS RED adapter test committed (TDD baseline for the phase).
</success_criteria>

<output>
Create `.planning/phases/11-sidecar-scaffold-auth-migration/11-01-SUMMARY.md` when done
</output>
