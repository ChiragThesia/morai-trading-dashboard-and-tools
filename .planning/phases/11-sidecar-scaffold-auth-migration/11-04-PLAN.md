---
phase: 11-sidecar-scaffold-auth-migration
plan: 04
type: tdd
wave: 3
depends_on: ["11-02"]
files_modified:
  - apps/sidecar/token_store.py
  - apps/sidecar/advisory_lock.py
  - apps/sidecar/tests/test_token_store.py
  - apps/sidecar/tests/test_advisory_lock.py
  - apps/sidecar/tests/conftest.py
autonomous: true
requirements: [GW-01, GW-04]
user_setup: []

must_haves:
  truths:
    - "token_write_func writes the full schwab-py wrapped blob to token_json AND decomposes inner access_token/refresh_token into the existing pgcrypto-encrypted discrete columns (dual-write, D-02)"
    - "A write → read round-trip returns the blob byte-for-byte identical (token_read_func reads token_json and returns the dict schwab-py expects)"
    - "token_write_func NEVER updates refresh_issued_at on access-token rotation — the 7-day TTL anchor is preserved (Phase 4 P02 rule)"
    - "acquire_sidecar_lock uses session-level pg_try_advisory_lock(8876543210) on a dedicated DIRECT-connection (port 5432) cursor; a second instance gets False and raises SystemExit(1) after logging a clear error"
    - "TOKEN_ENCRYPTION_KEY is passed only as a bound %s parameter to pgp_sym_encrypt — never f-string/format-string interpolated, never logged"
    - "The 11-01 RED scaffolds test_token_store.py and test_advisory_lock.py are now GREEN"
  prohibitions:
    - "MUST NOT update refresh_issued_at in token_write_func (resets the 7-day clock — RESEARCH Pitfall 3)"
    - "MUST NOT use DATABASE_POOL_URL / port 6543 for the advisory-lock connection — session-level locks need the direct DATABASE_URL port 5432 (RESEARCH Pitfall 2)"
    - "MUST NOT log token values or interpolate TOKEN_ENCRYPTION_KEY into SQL strings (Schwab tokens never in logs/SQL — project rule; security V6)"
    - "MUST NOT add an explicit pg_advisory_unlock crash path — Postgres releases session locks on connection close (don't-hand-roll)"
  artifacts:
    - path: "apps/sidecar/token_store.py"
      provides: "make_token_callbacks(db_url, app_id, encryption_key) → (token_read_func, token_write_func)"
      min_lines: 30
    - path: "apps/sidecar/advisory_lock.py"
      provides: "acquire_sidecar_lock(direct_db_url) + SIDECAR_LOCK_KEY constant"
      contains: "pg_try_advisory_lock"
    - path: "apps/sidecar/tests/conftest.py"
      provides: "pytest fixtures: test Postgres connection + seeded broker_tokens row"
  key_links:
    - from: "apps/sidecar/token_store.py"
      to: "broker_tokens (token_json + access_token columns)"
      via: "token_write_func dual-writes token_json blob + decomposed encrypted access_token"
      pattern: "token_json"
    - from: "apps/sidecar/advisory_lock.py"
      to: "Postgres direct connection (port 5432)"
      via: "pg_try_advisory_lock over a session-level autocommit connection held for process lifetime"
      pattern: "pg_try_advisory_lock"
---

<objective>
Build the two sidecar core modules whose behavior is pure I/O contract — the token-store
callbacks (GW-01 dual-write) and the advisory-lock guard (GW-04) — TDD red→green over the
scaffolds committed in 11-01. These are the highest-risk seams in the phase: the token
round-trip must not corrupt schwab-py's blob or reset the 7-day TTL, and the lock must use
the direct connection so a second instance genuinely fails.

Purpose: Make the sidecar's auto-refresh the SOLE writer of `broker_tokens` (GW-03) without
breaking the TS trader reader (it keeps reading the discrete `access_token` column, D-01),
and guarantee the single-streamer invariant as a property of the lock, fully testable
without a live Schwab stream (Claude's Discretion — lock-only this phase).

Output: `token_store.py`, `advisory_lock.py`, pytest fixtures; the 11-01 token + lock RED
scaffolds turned green in the Python/pytest CI lane.
</objective>

<artifacts_this_phase_produces>
This plan creates:
- **Python module:** `apps/sidecar/token_store.py` — `make_token_callbacks(db_url, app_id, encryption_key)` returning `(token_read_func, token_write_func)` bound to one app_id row; dual-writes `token_json` + decomposed encrypted `access_token`/`refresh_token`.
- **Python module:** `apps/sidecar/advisory_lock.py` — `acquire_sidecar_lock(direct_db_url)` + the `SIDECAR_LOCK_KEY = 8876543210` constant; session-level non-blocking lock, SystemExit(1) on second instance.
- **Python test fixtures:** `apps/sidecar/tests/conftest.py` (test Postgres connection + seeded broker_tokens row).
(11-05 imports both modules in the FastAPI lifespan.)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-sidecar-scaffold-auth-migration/11-RESEARCH.md
@.planning/phases/11-sidecar-scaffold-auth-migration/11-PATTERNS.md

# Structural analog for token column semantics (no refresh_issued_at reset; null-when-absent)
@packages/adapters/src/memory/broker-tokens.ts
# The discrete-column schema the dual-write decompose targets (broker_tokens ~line 206)
@packages/adapters/src/postgres/schema.ts
</context>

<feature>
  <name>schwab-py token-store callbacks (GW-01 dual-write) + Postgres advisory-lock guard (GW-04)</name>
  <files>
    apps/sidecar/token_store.py,
    apps/sidecar/advisory_lock.py,
    apps/sidecar/tests/test_token_store.py,
    apps/sidecar/tests/test_advisory_lock.py,
    apps/sidecar/tests/conftest.py
  </files>
  <behavior>
    RED → GREEN over the 11-01 scaffolds. Test cases:
    token_store:
    - test_token_round_trip: write sample wrapped blob via token_write_func, read via token_read_func, assert dict equal; assert the decrypted access_token discrete column == sample['token']['access_token'].
    - test_refresh_issued_at_unchanged: seed a row with refresh_issued_at = T0; call token_write_func with a rotated access token; assert the row's refresh_issued_at is still T0 (NOT bumped).
    - test_read_missing_raises: token_read_func on a row with token_json IS NULL raises a clear ValueError ("No token found"/"token not seeded") so the lifespan can degrade gracefully (RESEARCH Open Question 2).
    advisory_lock:
    - test_second_instance_fails: setup connection holds pg_advisory_lock(8876543210); acquire_sidecar_lock(test_db_url) raises SystemExit and logs an error containing "another sidecar instance is running".
    - test_first_instance_acquires: clean acquire returns an open connection; the lock is released when that connection closes (re-acquire succeeds afterward).
  </behavior>
  <read_first>
    - .planning/phases/11-sidecar-scaffold-auth-migration/11-RESEARCH.md (§ Pattern 1 token_store.py full code + token blob shape; § Pattern 3 advisory_lock.py full code; § Pitfall 2/3)
    - .planning/phases/11-sidecar-scaffold-auth-migration/11-PATTERNS.md (§ token_store.py + § advisory_lock.py reference implementations)
    - packages/adapters/src/memory/broker-tokens.ts (lines 54-68 — column semantics: writeTokens never resets refresh_issued_at; readTokens returns null when absent)
    - apps/sidecar/tests/test_token_store.py + apps/sidecar/tests/test_advisory_lock.py (the 11-01 RED scaffolds to extend to the full case list)
  </read_first>
  <implementation>
    token_store.py: implement `make_token_callbacks(db_url, app_id, encryption_key)` returning (token_read_func, token_write_func). token_read_func: `SELECT token_json FROM broker_tokens WHERE app_id = %s` (app_id bound param); raise ValueError when row/token_json is None (graceful-degrade signal). token_write_func(token): extract token['token']['access_token'] and ['refresh_token']; `UPDATE broker_tokens SET token_json = %s, access_token = pgp_sym_encrypt(%s, %s), refresh_token = pgp_sym_encrypt(%s, %s), issued_at = NOW(), expires_at = to_timestamp(%s), updated_at = NOW() WHERE app_id = %s` — encryption_key as a bound %s param (NEVER interpolated), refresh_issued_at deliberately absent from the SET list (Phase 4 P02). All params bound; json.dumps(token) into token_json. Log only app_id + issued_at on write (never token values). advisory_lock.py: `SIDECAR_LOCK_KEY = 8876543210` (documented constant); `acquire_sidecar_lock(direct_db_url)`: psycopg2.connect, conn.autocommit = True (session-level lock needs autocommit), `SELECT pg_try_advisory_lock(%s)`; on False → conn.close(), logging.error("...another sidecar instance is running...Refusing to start..."), raise SystemExit(1); on True → logging.info, return the open conn (caller holds it; lock releases on conn.close()). Docstring: MUST use direct DATABASE_URL port 5432, never DATABASE_POOL_URL. conftest.py: a fixture that connects to a test Postgres (Docker/pytest-postgresql), runs the broker_tokens DDL incl. token_json + pgcrypto extension, and seeds a row. Follow RED→GREEN: confirm scaffolds fail for the right reason (missing module), implement to green, commit at green only. No Schwab credentials needed for any test.
  </implementation>
  <verify>
    <automated>cd apps/sidecar && pytest tests/test_token_store.py tests/test_advisory_lock.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/sidecar && pytest tests/test_token_store.py tests/test_advisory_lock.py -x -q` is GREEN (was RED in 11-01).
    - test_token_round_trip proves byte-for-byte blob equality AND decrypted access_token == sample access_token.
    - test_refresh_issued_at_unchanged proves refresh_issued_at is not bumped on a write.
    - test_second_instance_fails proves SystemExit + log "another sidecar instance is running".
    - `grep -n 'pg_try_advisory_lock' apps/sidecar/advisory_lock.py` matches; `grep -n 'refresh_issued_at' apps/sidecar/token_store.py` returns no SET-clause assignment (only a comment, if any).
  </acceptance_criteria>
  <done>token_store dual-write round-trips losslessly and preserves the refresh_issued_at anchor; the advisory lock genuinely blocks a second instance; both 11-01 RED scaffolds are green.</done>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sidecar → broker_tokens (write) | The sidecar becomes the sole writer of encrypted Schwab tokens (GW-03) |
| sidecar process ↔ Postgres advisory lock | The single-streamer invariant is enforced at the DB lock, not in app logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | Information Disclosure | token value in logs/SQL | mitigate | token_write_func logs only app_id + issued_at; TOKEN_ENCRYPTION_KEY + token values are bound %s params, never interpolated/logged (security V6) |
| T-11-04-02 | Tampering | SQL injection in token write | mitigate | All values (app_id, access_token, encryption_key, expires_at) passed as psycopg2 %s bound parameters — never f-string/format |
| T-11-04-03 | Spoofing | advisory-lock bypass / dual session | mitigate | pg_try_advisory_lock non-blocking + SystemExit(1) on False (no soft fallback); direct connection (port 5432) so PgBouncer can't silently release the lock |
| T-11-04-04 | Tampering | dual-writer rotating-token race | mitigate | The sidecar is sole writer; the TS refresh-tokens job is retired in 11-06 BEFORE this auto-refresh activates at go-live (GW-03 sequence) |
| T-11-04-SC | Tampering | pip installs (schwab-py/psycopg2/etc.) | mitigate | RESEARCH § Package Legitimacy Audit approved all packages; deps pinned in 11-05 requirements.txt |
</threat_model>

<verification>
- `cd apps/sidecar && pytest tests/test_token_store.py tests/test_advisory_lock.py -x -q` green.
- `grep refresh_issued_at apps/sidecar/token_store.py` shows no SET assignment (anchor preserved).
- No token value or encryption key appears in any logging call (manual review of the two modules).
</verification>

<success_criteria>
- The token-store dual-write (GW-01) and advisory-lock guard (GW-04) are implemented and green; the refresh_issued_at anchor and direct-connection lock rules are enforced and tested.
</success_criteria>

<output>
Create `.planning/phases/11-sidecar-scaffold-auth-migration/11-04-SUMMARY.md` when done
</output>
