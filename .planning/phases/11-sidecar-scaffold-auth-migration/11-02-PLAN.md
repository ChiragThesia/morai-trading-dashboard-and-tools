---
phase: 11-sidecar-scaffold-auth-migration
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - packages/adapters/src/postgres/migrations/0011_broker_tokens_token_json.sql
  - packages/adapters/src/postgres/migrations/meta/_journal.json
  - packages/adapters/src/postgres/migrations/meta/0011_snapshot.json
autonomous: false
requirements: [GW-01]
user_setup: []

must_haves:
  truths:
    - "A new numbered migration 0011_broker_tokens_token_json.sql exists and adds only the token_json JSONB column (additive ALTER TABLE) per the established Phase-2..8 numbered-migration convention"
    - "bun run migrate applies the migration against the live Supabase DB over the DIRECT DATABASE_URL (port 5432, not the 6543 transaction pooler) and is idempotent on a second run"
    - "broker_tokens has a token_json column live in the database after migrate (verifiable via information_schema)"
  prohibitions:
    - "MUST NOT use drizzle-kit push against prod — generate a numbered migration and apply via bun run migrate (the established pattern; Phase 2 shipped 0007, Phase 8 shipped 0008-0010)"
    - "MUST NOT use the transaction pooler URL (port 6543 / DATABASE_POOL_URL) for the migration — migrations use the direct DATABASE_URL (port 5432) per stack-decisions §D18 / Phase 1 deploy lesson"
    - "MUST NOT hand-edit existing migration files (0000-0010) or their journal entries — only append 0011"
  artifacts:
    - path: "packages/adapters/src/postgres/migrations/0011_broker_tokens_token_json.sql"
      provides: "additive ALTER TABLE broker_tokens ADD COLUMN token_json JSONB"
      contains: "token_json"
  key_links:
    - from: "packages/adapters/src/postgres/schema.ts"
      to: "packages/adapters/src/postgres/migrations/0011_broker_tokens_token_json.sql"
      via: "drizzle-kit generate diffs schema.ts (11-01 column) → emits the 0011 migration"
      pattern: "token_json"
---

<objective>
[BLOCKING] Generate the numbered Drizzle migration for the `token_json` column added to
the schema in 11-01, then apply it to the live Supabase database via `bun run migrate`.

Purpose: The phase CANNOT pass verification without this. Build and typecheck pass without
the live column (Drizzle types come from schema.ts, not the database), creating a
false-positive verification state — the sidecar's `token_read_func`/`token_write_func`
target a real `token_json` column that must exist in Postgres before 11-04's round-trip
contract test and before go-live.

Output: `0011_broker_tokens_token_json.sql` + journal/meta entries; the column applied live.
</objective>

<artifacts_this_phase_produces>
This plan creates:
- **Migration:** `packages/adapters/src/postgres/migrations/0011_broker_tokens_token_json.sql` (additive `ALTER TABLE broker_tokens ADD COLUMN token_json jsonb`).
- **Migration metadata:** the appended `_journal.json` entry + `0011_snapshot.json` (drizzle-kit generated).
- **Live DB state:** the `token_json` column applied to the Supabase `broker_tokens` table.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-sidecar-scaffold-auth-migration/11-RESEARCH.md

# The schema source the migration is generated from (token_json added in 11-01)
@packages/adapters/src/postgres/schema.ts
# Migration conventions: numbered files, journal, generate-then-rename precedent
@drizzle.config.ts
</context>

<tasks>

<task type="execute">
  <name>Task 1: Generate the numbered 0011 migration from the schema diff</name>
  <read_first>
    - packages/adapters/src/postgres/schema.ts (the token_json column added in 11-01 — drizzle-kit diffs this)
    - drizzle.config.ts (drizzle-kit config — schema path + migrations out dir + dialect)
    - packages/adapters/src/postgres/migrations/0010_gex_wall_numeric.sql (the latest existing migration — confirms 0011 is the next number)
  </read_first>
  <action>
    Run `bunx drizzle-kit generate` (the established generate command for this repo — confirm the exact script in drizzle.config.ts / package.json before running). It diffs schema.ts against the existing snapshots and emits the next-numbered migration. Verify the emitted file is numbered `0011_*` and contains only `ALTER TABLE "broker_tokens" ADD COLUMN "token_json" jsonb;` (additive; no other table touched). Per the Phase 2/4/5 precedent, if drizzle-kit emits a random-word filename, rename it to `0011_broker_tokens_token_json.sql` AND update the corresponding `meta/_journal.json` tag to match the renamed file (Phase 4 P01 / Phase 5 P02 precedent — keep the journal tag in sync). Do NOT touch migrations 0000-0010 or their journal entries. Confirm the diff is exactly one additive column.
  </action>
  <verify>
    <automated>test -f packages/adapters/src/postgres/migrations/0011_broker_tokens_token_json.sql && grep -iq 'add column' packages/adapters/src/postgres/migrations/0011_broker_tokens_token_json.sql && grep -q 'token_json' packages/adapters/src/postgres/migrations/0011_broker_tokens_token_json.sql && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `packages/adapters/src/postgres/migrations/0011_broker_tokens_token_json.sql` exists and is the next sequential number after 0010.
    - The SQL is a single additive `ADD COLUMN ... token_json ... jsonb` against `broker_tokens` — no DROP, no ALTER of any other column/table.
    - `meta/_journal.json` has exactly one new entry whose tag matches the (possibly renamed) 0011 file.
  </acceptance_criteria>
  <done>The 0011 additive migration is generated, named conventionally, and its journal tag matches.</done>
</task>

<task type="execute">
  <name>Task 2: [BLOCKING] Apply the migration to the live database via bun run migrate</name>
  <read_first>
    - apps/worker/src/migrate.ts (the migrate entrypoint `bun run migrate` invokes — confirm it reads the DIRECT DATABASE_URL)
    - .planning/phases/11-sidecar-scaffold-auth-migration/11-RESEARCH.md (§ "Critical: pooler incompatibility" + § Project Constraints "Session-pooler caveat" — migrations use direct DATABASE_URL port 5432)
  </read_first>
  <action>
    Apply the migration to the live Supabase database with `bun run migrate`. This MUST use the DIRECT `DATABASE_URL` (port 5432) — NOT the transaction pooler `DATABASE_POOL_URL` (port 6543); DDL over the transaction pooler is unsafe (stack-decisions §D18 / Phase 1 deploy lesson). The migrator is idempotent (Phase 1 DATA-02 contract): a second `bun run migrate` is a no-op. After applying, confirm the column exists live by querying `information_schema.columns` for table `broker_tokens` column `token_json` (a read-only SELECT — no token values exposed). This task is `autonomous: false`: it mutates production state and may require operator confirmation of the DATABASE_URL/connection before running; if the live DATABASE_URL is not present in the execution environment (prod-deploy-debt: prod DB may be down — see STATE.md Blockers), STOP and surface a checkpoint for the operator to supply the direct connection and run `bun run migrate`, then verify. Do NOT silently skip — the phase verification depends on the live column.
  </action>
  <verify>
    <automated>bun run migrate && echo MIGRATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `bun run migrate` exits 0; a second immediate run is a no-op (idempotent, no error).
    - `SELECT column_name FROM information_schema.columns WHERE table_name='broker_tokens' AND column_name='token_json'` returns one row against the live DB.
    - The connection used was the direct DATABASE_URL (port 5432), confirmed against the migrate entrypoint config.
  </acceptance_criteria>
  <done>The `token_json` column exists in the live Supabase broker_tokens table; the migrator is idempotent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migrate process → live Supabase Postgres | DDL mutates the production broker_tokens table holding encrypted Schwab tokens |
| pooler vs direct connection | The advisory-lock/DDL path requires the direct connection; the pooler silently breaks session semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | Tampering | broker_tokens live DDL | mitigate | Migration is strictly additive (ADD COLUMN, nullable); existing encrypted token columns untouched; idempotent migrator |
| T-11-02-02 | Denial of Service | wrong pooler URL for DDL | mitigate | Enforce direct DATABASE_URL (port 5432) per §D18; task reads migrate.ts to confirm before applying |
| T-11-02-03 | Information Disclosure | verification query on token table | mitigate | Verification SELECTs only information_schema metadata (column existence) — never reads access_token/refresh_token/token_json values |
</threat_model>

<verification>
- `bun run migrate` applies 0011 cleanly; second run is a no-op.
- `information_schema.columns` confirms `broker_tokens.token_json` exists live.
- `bun run typecheck` still passes; no existing migration file changed (git diff shows only the new 0011 files).
</verification>

<success_criteria>
- The additive `token_json` migration is generated (numbered 0011, journal-synced) and applied to the live database over the direct connection.
- 11-04's token round-trip contract test now has a real column to target.
</success_criteria>

<output>
Create `.planning/phases/11-sidecar-scaffold-auth-migration/11-02-SUMMARY.md` when done
</output>
