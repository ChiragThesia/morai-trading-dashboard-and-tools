---
phase: 11-sidecar-scaffold-auth-migration
plan: 05
type: tdd
wave: 4
depends_on: ["11-04"]
files_modified:
  - apps/sidecar/requirements.txt
  - apps/sidecar/config.py
  - apps/sidecar/main.py
  - apps/sidecar/chain_proxy.py
  - apps/sidecar/health.py
  - apps/sidecar/Dockerfile
  - apps/sidecar/tests/test_chain_proxy.py
  - apps/sidecar/tests/test_health.py
  - railway.sidecar.toml
autonomous: false
requirements: [GW-01, GW-02, GW-05]
user_setup:
  - service: railway
    why: "Deploy apps/sidecar as a third Railway service with NO public domain (GW-05 internal-only); set sidecar env vars"
    env_vars:
      - name: SCHWAB_TRADER_APP_KEY
        source: "Schwab Developer Portal -> trader app"
      - name: SCHWAB_TRADER_APP_SECRET
        source: "Schwab Developer Portal -> trader app"
      - name: SCHWAB_MARKET_APP_KEY
        source: "Schwab Developer Portal -> market app"
      - name: SCHWAB_MARKET_APP_SECRET
        source: "Schwab Developer Portal -> market app"
      - name: DATABASE_URL
        source: "Supabase -> direct connection (port 5432) — NOT the 6543 pooler"
      - name: TOKEN_ENCRYPTION_KEY
        source: "Same pgcrypto key as worker/server (>=32 chars)"
    dashboard_config:
      - task: "Create the sidecar service with config-as-code path /railway.sidecar.toml and assign NO public domain (internal private network only)"
        location: "Railway -> new service -> Settings -> Networking (do not generate a public domain)"
      - task: "Add SIDECAR_URL to the server + worker services via Railway reference syntax http://${{sidecar.RAILWAY_PRIVATE_DOMAIN}}:${{sidecar.PORT}}"
        location: "Railway -> server/worker -> Variables"

must_haves:
  truths:
    - "apps/sidecar builds from its own Dockerfile and binds uvicorn to :: (IPv4+IPv6) on $PORT for Railway private networking (RESEARCH Pitfall 5)"
    - "FastAPI lifespan acquires the advisory lock (11-04) BEFORE initializing two client_from_access_functions clients (one per app, trader+market, D-05), and degrades gracefully when token_json is not yet seeded (Open Question 2)"
    - "GET /sidecar/health returns {status, tokenFreshness} read from broker_tokens (no decryption); returns status:'degraded', tokenFreshness:'not_seeded' before the first OAuth dance"
    - "GET /sidecar/chain?root=SPX returns the RawChain-mirroring JSON (source='schwab_chain'); on auth failure returns HTTP 503 {error:'AUTH_EXPIRED'}"
    - "The Python /sidecar/chain contract test pins the response shape to the TS ForFetchingChain/SidecarChainResponseSchema (manual-mirror contract, D-08)"
    - "The sidecar has no public Railway ingress — only apps/server (and the worker for chain proxy) reach it over the private network (GW-05)"
    - "The 11-01 RED scaffold test_chain_proxy.py is now GREEN"
  prohibitions:
    - "MUST NOT bind uvicorn to 0.0.0.0 (IPv4-only) — Railway private net needs :: (RESEARCH Pitfall 5)"
    - "MUST NOT assign the sidecar a public Railway domain (GW-05) — internal DNS only"
    - "MUST NOT open a live Schwab streamer login()/subscription — lock-only this phase (Claude's Discretion; streaming is Phase 12)"
    - "MUST NOT install an unpinned/[SLOP] package — all pip deps pinned per RESEARCH § Package Legitimacy Audit; legitimacy checkpoint precedes install"
    - "MUST NOT read DATABASE_POOL_URL for the advisory-lock connection — config.py exposes the direct DATABASE_URL only (port 5432)"
  artifacts:
    - path: "apps/sidecar/main.py"
      provides: "FastAPI app + lifespan (advisory lock → two schwab-py clients → graceful not-seeded degrade)"
      contains: "lifespan"
    - path: "apps/sidecar/config.py"
      provides: "pydantic-settings SidecarConfig (DATABASE_URL direct, TOKEN_ENCRYPTION_KEY, 4 Schwab fields, PORT)"
    - path: "apps/sidecar/chain_proxy.py"
      provides: "GET /sidecar/chain handler (Pydantic-validated; 503 AUTH_EXPIRED)"
    - path: "apps/sidecar/health.py"
      provides: "GET /sidecar/health handler (tokenFreshness from broker_tokens; not_seeded degrade)"
    - path: "apps/sidecar/Dockerfile"
      provides: "python:3.11-slim image; uvicorn --host :: --port $PORT"
    - path: "apps/sidecar/requirements.txt"
      provides: "pinned deps: schwab-py==1.5.1, fastapi, uvicorn[standard], psycopg2-binary, pydantic, pydantic-settings, pytest, pytest-asyncio, httpx"
    - path: "railway.sidecar.toml"
      provides: "config-as-code for the internal-only sidecar service (Dockerfile build, healthcheck /sidecar/health)"
  key_links:
    - from: "apps/sidecar/main.py"
      to: "apps/sidecar/advisory_lock.py + apps/sidecar/token_store.py (11-04)"
      via: "lifespan calls acquire_sidecar_lock then make_token_callbacks × 2 for client_from_access_functions"
      pattern: "client_from_access_functions"
    - from: "apps/server + apps/worker"
      to: "apps/sidecar (railway.internal)"
      via: "SIDECAR_URL=http://${{sidecar.RAILWAY_PRIVATE_DOMAIN}}:${{sidecar.PORT}} — private network only"
      pattern: "railway.internal"
---

<objective>
Stand up the FastAPI sidecar service: the `lifespan` composition root (advisory lock →
two `client_from_access_functions` clients → graceful not-seeded degrade), the
`/sidecar/health` + `/sidecar/chain` routes, the Dockerfile (bind `::`, `$PORT`), pinned
`requirements.txt`, and the internal-only Railway service. TDD red→green over the
`test_chain_proxy.py` scaffold from 11-01, plus a new `test_health.py` and the
manual-mirror contract test pinning `/sidecar/chain` to the TS `ForFetchingChain` shape.

Purpose: Deliver SC1 (sidecar deploys, `/sidecar/health` returns `{status, tokenFreshness}`
reading `broker_tokens` via the token callbacks), the GW-02 chain slice (`/sidecar/chain`),
and GW-05 (internal-only, no public ingress). The streamer `login()` is deliberately out of
scope — the lock is established but no live subscription opens (Phase 12).

Output: `main.py`, `config.py`, `chain_proxy.py`, `health.py`, `Dockerfile`,
`requirements.txt`, `railway.sidecar.toml`; the 11-01 chain RED scaffold green; the
Python-side contract test pinning the chain shape.
</objective>

<artifacts_this_phase_produces>
This plan creates:
- **FastAPI service:** `apps/sidecar/main.py` (app + lifespan), `config.py` (pydantic-settings), `chain_proxy.py` (GET /sidecar/chain), `health.py` (GET /sidecar/health).
- **Routes:** `/sidecar/health` → {status, tokenFreshness}; `/sidecar/chain?root=SPX` → RawChain-mirroring JSON / 503 AUTH_EXPIRED.
- **Deploy:** `apps/sidecar/Dockerfile` (python:3.11-slim, uvicorn `--host :: --port $PORT`), `apps/sidecar/requirements.txt` (pinned), `railway.sidecar.toml` (internal-only service).
- **Env vars:** sidecar service gets SCHWAB_TRADER/MARKET_APP_KEY/SECRET, DATABASE_URL (direct), TOKEN_ENCRYPTION_KEY; server+worker get SIDECAR_URL.
- **Tests:** `test_health.py` + green `test_chain_proxy.py` + manual-mirror contract test.
(11-06 wires the worker's SIDECAR_URL + chain-source swap and retires refresh-tokens.)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-sidecar-scaffold-auth-migration/11-RESEARCH.md
@.planning/phases/11-sidecar-scaffold-auth-migration/11-PATTERNS.md

# Structural analog: composition-root order (config → infra → adapters → wire)
@apps/worker/src/main.ts
# Structural analog: health route shape (status payload)
@apps/server/src/routes/status.ts
# Railway config-as-code analog (build/deploy block)
@railway.worker.toml
# The two modules the lifespan imports (built in 11-04)
@apps/sidecar/advisory_lock.py
@apps/sidecar/token_store.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: package legitimacy gate before pip install</name>
  <action>BLOCKING human verification. Before installing any sidecar pip dependency, the operator confirms the pinned packages are legitimate per RESEARCH § Package Legitimacy Audit. Not auto-approvable.</action>
  <what-built>Pre-install package legitimacy gate for the sidecar pip dependencies. RESEARCH § Package Legitimacy Audit verified all 6 core packages (schwab-py 1.5.1, fastapi, uvicorn, psycopg2-binary, pydantic, pytest) as OK against authoritative sources; the SUS flags were a PyPI download-count data-gap, not slopsquatting evidence.</what-built>
  <how-to-verify>
    1. Confirm `schwab-py==1.5.1` on https://pypi.org/project/schwab-py/ (repo github.com/alexgolec/schwab-py).
    2. Spot-check `fastapi`, `uvicorn`, `psycopg2-binary`, `pydantic`, `pydantic-settings` exist on pypi.org with the expected maintainers.
    3. Confirm requirements.txt pins schwab-py to exactly 1.5.1 (D22) — no floating version.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the pinned pip install, or name any package to drop/replace.</resume-signal>
</task>

<task type="execute">
  <name>Task 1: requirements.txt + config.py + Dockerfile (service scaffold)</name>
  <read_first>
    - .planning/phases/11-sidecar-scaffold-auth-migration/11-RESEARCH.md (§ Standard Stack install list; § Pattern 4 Dockerfile; § Project Constraints session-pooler caveat)
    - .planning/phases/11-sidecar-scaffold-auth-migration/11-PATTERNS.md (§ config.py pydantic-settings field set; § Dockerfile-per-service)
    - apps/worker/src/config.ts (the field set to mirror; note TOKEN_ENCRYPTION_KEY min-32 rule and "never log config values")
    - railway.worker.toml (the config-as-code analog for the [build]/[deploy] blocks)
  </read_first>
  <action>
    Create apps/sidecar/requirements.txt with PINNED deps: `schwab-py==1.5.1`, `fastapi>=0.115,<1.0`, `uvicorn[standard]>=0.30`, `psycopg2-binary>=2.9`, `pydantic>=2.0`, `pydantic-settings>=2.0`, and dev deps `pytest>=8.0`, `pytest-asyncio>=0.23`, `httpx>=0.27` (per RESEARCH Standard Stack). Create apps/sidecar/config.py: a `pydantic-settings` `BaseSettings` `SidecarConfig` with fields DATABASE_URL: str (direct port 5432 — comment: NEVER the pool URL, advisory lock), TOKEN_ENCRYPTION_KEY: str, SCHWAB_TRADER_APP_KEY/SECRET: str, SCHWAB_MARKET_APP_KEY/SECRET: str, PORT: int = 8000; module-level `config = SidecarConfig()`. Rule: never log config values, only field names on failure (mirror apps/worker/src/config.ts). Create apps/sidecar/Dockerfile: `FROM python:3.11-slim`, WORKDIR /app, COPY requirements.txt, `RUN pip install --no-cache-dir -r requirements.txt`, COPY . ., `CMD ["uvicorn", "main:app", "--host", "::", "--port", "8000"]` (bind :: for Railway private net — RESEARCH Pitfall 5). Do NOT bind 0.0.0.0.
  </action>
  <verify>
    <automated>test -f apps/sidecar/requirements.txt && grep -q 'schwab-py==1.5.1' apps/sidecar/requirements.txt && grep -q 'host.*::' apps/sidecar/Dockerfile && test -f apps/sidecar/config.py && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt pins schwab-py to exactly 1.5.1; all deps pinned/bounded.
    - Dockerfile binds uvicorn to `::` (not 0.0.0.0) and uses python:3.11-slim.
    - config.py is a pydantic-settings BaseSettings exposing DATABASE_URL (direct) + TOKEN_ENCRYPTION_KEY + 4 Schwab fields + PORT; no pool URL field.
  </acceptance_criteria>
  <done>The sidecar's deps, typed config, and Dockerfile exist; the Dockerfile binds :: for Railway private networking.</done>
</task>

<task type="execute" tdd="true">
  <name>Task 2: health.py + chain_proxy.py + main.py lifespan (routes + composition root)</name>
  <read_first>
    - .planning/phases/11-sidecar-scaffold-auth-migration/11-RESEARCH.md (§ Pattern 4 FastAPI lifespan + health; § Pattern 5 chain shape + 503 AUTH_EXPIRED; § Pattern 2 two clients; § Open Question 2 not-seeded degrade)
    - .planning/phases/11-sidecar-scaffold-auth-migration/11-PATTERNS.md (§ chain_proxy.py Pydantic model_validate = Zod safeParse equivalent; § main.py lifespan order)
    - apps/sidecar/advisory_lock.py + apps/sidecar/token_store.py (11-04 — the modules the lifespan imports)
    - apps/sidecar/tests/test_chain_proxy.py (11-01 RED scaffold to turn green)
  </read_first>
  <behavior>
    - test_chain_shape: GET /sidecar/chain returns JSON with root, observedAt, spot, quotes[], source='schwab_chain' matching the ChainResponse Pydantic model (use a stubbed market_client on app.state).
    - test_auth_expired: market_client raises (auth failure) → response is HTTP 503 with body {"error":"AUTH_EXPIRED"}.
    - test_health_ok: with a seeded token_json, GET /sidecar/health → 200 {status:'ok', tokenFreshness:<value>}.
    - test_health_not_seeded: with token_json NULL, GET /sidecar/health → {status:'degraded', tokenFreshness:'not_seeded'} (does not crash).
    - contract test: assert the /sidecar/chain JSON keys + types are exactly the set the TS SidecarChainResponseSchema expects (manual-mirror, D-08) — pin field names/nullability.
  </behavior>
  <action>
    Implement RED→GREEN. health.py: `GET /sidecar/health` reads token freshness from broker_tokens (a non-decrypting SELECT on expires_at/refresh_issued_at/last_refresh_error or token_json presence) and returns {status, tokenFreshness}; when token_json IS NULL return {status:'degraded', tokenFreshness:'not_seeded'}. chain_proxy.py: define Pydantic `ChainQuote` + `ChainResponse` models (source default 'schwab_chain') mirroring the TS shape exactly; `GET /sidecar/chain?root=SPX` calls the market client's option-chain method, maps to ChainResponse via model_validate (the Pydantic = Zod safeParse boundary), and on any auth exception raises HTTPException(503, detail={"error":"AUTH_EXPIRED"}); log the error without token values. main.py: `lifespan` order per RESEARCH Pattern 4 — (1) acquire_sidecar_lock(config.DATABASE_URL) [direct], (2) build trader+market token callbacks via make_token_callbacks (D-05 two clients), (3) init two client_from_access_functions clients (asyncio), catching the not-seeded ValueError and setting app.state degraded + tokenFreshness='not_seeded' instead of crashing (Open Question 2); store market_client on app.state; on shutdown close lock_conn. Do NOT call streamer login()/subscribe — lock-only this phase. Mount the health + chain routers. Confirm scaffolds RED first, implement to green, commit at green.
  </action>
  <verify>
    <automated>cd apps/sidecar && pytest tests/test_chain_proxy.py tests/test_health.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/sidecar && pytest tests/test_chain_proxy.py tests/test_health.py -x -q` is GREEN (chain scaffold was RED in 11-01).
    - test_auth_expired proves 503 {error:'AUTH_EXPIRED'}; test_health_not_seeded proves graceful degrade.
    - The contract test pins the /sidecar/chain key set + nullability to match the TS SidecarChainResponseSchema (11-03).
    - main.py lifespan acquires the lock before client init and opens no live streamer subscription (grep: no login()/subscribe call).
  </acceptance_criteria>
  <done>The FastAPI service exposes /sidecar/health + /sidecar/chain, degrades gracefully when not seeded, holds the advisory lock at startup, and the chain shape is contract-pinned to the TS adapter.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Checkpoint: deploy the sidecar internal-only + seed token via OAuth dance</name>
  <action>BLOCKING human action. The operator creates the Railway sidecar service with NO public domain (GW-05), sets its env vars + SIDECAR_URL on server/worker, runs the one-time fresh OAuth dance (D-03) to seed token_json, and confirms /sidecar/health returns ok and the sidecar is not publicly reachable. No fully-scriptable substitute in this environment.</action>
  <what-built>The sidecar service is built and ready to deploy as a third Railway service. Railway service creation + assigning NO public domain + setting env vars are operator actions with no fully-scriptable substitute in this environment (and the prod DB may be down — prod-deploy-debt, STATE.md Blockers).</what-built>
  <how-to-verify>
    1. Create the Railway `sidecar` service; set config-as-code path to /railway.sidecar.toml (Dockerfile build).
    2. CONFIRM the sidecar has NO public domain (Settings -> Networking -> do not generate a domain) — GW-05 internal-only.
    3. Set the sidecar env vars: SCHWAB_TRADER_APP_KEY/SECRET, SCHWAB_MARKET_APP_KEY/SECRET, DATABASE_URL (direct port 5432), TOKEN_ENCRYPTION_KEY.
    4. Add SIDECAR_URL=http://${{sidecar.RAILWAY_PRIVATE_DOMAIN}}:${{sidecar.PORT}} to BOTH the server and worker services.
    5. Run the one-time fresh OAuth dance (client_from_manual_flow) to seed token_json (the prod token is dead — D-03); confirm GET /sidecar/health (from inside the private network / via server) returns {status:'ok', tokenFreshness:...} and that the sidecar is NOT reachable from the public internet.
  </how-to-verify>
  <resume-signal>Type "approved" once the sidecar is deployed internal-only, env vars are set on all three services, the OAuth dance has seeded token_json, and /sidecar/health returns ok — or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet → sidecar | MUST be closed: GW-05 — the sidecar has no public ingress; only the private Railway network reaches it |
| server/worker → sidecar | The only legitimate callers; over the internal railway.internal hostname |
| sidecar → Schwab API | schwab-py owns the OAuth + REST; tokens never logged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05-01 | Elevation of Privilege | sidecar publicly reachable | mitigate | No public Railway domain assigned (GW-05); private network is the only ingress; verified at the deploy checkpoint |
| T-11-05-02 | Information Disclosure | token/secret in logs or 503 body | mitigate | Health/chain handlers never decrypt or echo token values; 503 body is the fixed {error:'AUTH_EXPIRED'}; config values never logged |
| T-11-05-03 | Spoofing | dual streamer session | mitigate | Lifespan acquires the advisory lock (11-04) before client init; no live login()/subscribe this phase (lock-only) |
| T-11-05-04 | Denial of Service | IPv4-only bind breaks private net | mitigate | uvicorn binds `::` (RESEARCH Pitfall 5); Dockerfile asserted in Task 1 |
| T-11-05-05 | Tampering | hostile chain payload from upstream | mitigate | Pydantic model_validate at the boundary; the TS side independently Zod-parses (defense in depth) |
| T-11-05-SC | Tampering | pip dependency supply chain | mitigate | Blocking human legitimacy checkpoint precedes install; all deps pinned (schwab-py==1.5.1); RESEARCH audit approved |
</threat_model>

<verification>
- `cd apps/sidecar && pytest -x -q` (full sidecar lane) green: token_store + advisory_lock (11-04) + chain_proxy + health + contract test.
- Dockerfile binds `::`; requirements.txt pins schwab-py==1.5.1.
- Deploy checkpoint confirms: no public domain (GW-05), env vars on all three services, /sidecar/health ok after the OAuth dance seeds token_json.
</verification>

<success_criteria>
- SC1: apps/sidecar deploys as a Railway service; /sidecar/health returns {status, tokenFreshness} reading broker_tokens via the token callbacks (GW-01).
- GW-02 chain slice: /sidecar/chain proxies the Schwab chain (source='schwab_chain'); 503 AUTH_EXPIRED on auth failure.
- GW-05: sidecar internal-only, no public ingress.
- The 11-01 chain RED scaffold is green; the chain shape is contract-pinned to the TS adapter.
</success_criteria>

<output>
Create `.planning/phases/11-sidecar-scaffold-auth-migration/11-05-SUMMARY.md` when done
</output>
