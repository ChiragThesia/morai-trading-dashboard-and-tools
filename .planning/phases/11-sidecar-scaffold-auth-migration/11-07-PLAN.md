---
phase: 11-sidecar-scaffold-auth-migration
plan: 07
type: execute
wave: 6
depends_on: ["11-05", "11-06"]
files_modified:
  - apps/auth/
  - tsconfig.json
  - package.json
autonomous: true
requirements: [GW-03]
user_setup: []

must_haves:
  truths:
    - "No process other than the sidecar performs the Schwab OAuth dance or token refresh; `apps/auth` (`@morai/auth`) is removed from the repo and workspace (D-04, D16 superseded) — Schwab auth consolidates in one process"
    - "`apps/auth/` no longer exists; the root `tsconfig.json` project references drop `{ \"path\": \"apps/auth\" }`; `apps/*` workspace glob no longer resolves an auth package"
    - "No source under `apps/` or `packages/` imports `@morai/auth`; the package is gone with no dangling reference (the trader/market data path is unaffected — `apps/auth` was a standalone setup/refresh CLI, not a runtime dependency)"
    - "`bun install` succeeds with no missing-workspace error after removal; full typecheck + workspace test suite stay green"
  prohibitions:
    - "MUST NOT delete `makeSchwabOAuthClient` or any `@morai/adapters` export `apps/auth` consumed — those live in `packages/adapters` and may be referenced elsewhere; only the `apps/auth` app is retired, not the adapter package"
    - "MUST NOT remove `apps/auth` before its sidecar replacement is live — this plan depends_on 11-05 (manual OAuth dance seeds token_json via client_from_manual_flow) and 11-06 (chain-source swap); the sidecar must own the dance first (GW-03, no auth gap)"
    - "MUST NOT touch the prose doc-references to `apps/auth/...` in `apps/worker/src/backfill-transactions.ts` (a comment mirroring a pattern) — that is documentation, not a code dependency; surgical change only"
    - "MUST NOT add a `railway.auth.toml` cleanup task — there is no Railway service for the auth app (verified: only server + worker tomls exist)"
  artifacts:
    - path: "apps/auth/"
      provides: "REMOVED — the retired TS Schwab OAuth setup/refresh CLI (`@morai/auth`); the sidecar (11-05) now owns the dance + refresh (D-04)"
      contains: "(deleted)"
    - path: "tsconfig.json"
      provides: "root tsc project references with apps/auth dropped"
      contains: "references"
  key_links:
    - from: "tsconfig.json"
      to: "apps/* tsconfig project graph"
      via: "removing the apps/auth project reference so `tsc --build` no longer resolves a deleted package"
      pattern: "apps/auth"
    - from: "apps/sidecar (11-05)"
      to: "broker_tokens"
      via: "client_from_manual_flow seeds token_json + schwab-py auto-refresh — the sole replacement for the retired apps/auth setup/refresh (D-04, GW-03)"
      pattern: "client_from_manual_flow"
---

<objective>
Retire the TS `apps/auth` app (`@morai/auth`) — the standalone Schwab OAuth setup/refresh
CLI — now that the sidecar owns the OAuth dance and auto-refresh going forward (D-04, D16
superseded). Delete the `apps/auth/` directory and remove it from the root workspace wiring
(the `tsconfig.json` project reference; the `apps/*` workspace glob), then prove the
workspace still installs, typechecks, and tests green with no dangling `@morai/auth`
reference.

Purpose: Consolidate all Schwab authentication in one process (GW-03 sole-writer; the
milestone's whole point). 11-05 stands up the sidecar and seeds `token_json` via the manual
OAuth dance (`client_from_manual_flow`); 11-06 cuts the worker's refresh-tokens job and
re-sources the chain through the sidecar. With the sidecar live and owning the dance +
refresh, the TS `apps/auth` setup/refresh client has no remaining role and is removed. Phase
15 separately productizes one-click re-auth (AUTH-06) — that stays deferred; THIS plan only
removes the now-dead TS app.

Output: `apps/auth/` deleted; root `tsconfig.json` references updated; workspace install +
typecheck + test green; no `@morai/auth` references remain in `apps`/`packages`.
</objective>

<artifacts_this_phase_produces>
This plan removes (deletion + workspace glue, no new files):
- **Retired app:** `apps/auth/` — the entire `@morai/auth` TS Schwab OAuth setup/refresh/status/doctor CLI is deleted. Its replacement is the sidecar's `client_from_manual_flow` dance + schwab-py auto-refresh (11-05), with the sidecar as sole `broker_tokens` writer (GW-03).
- **Root tsc project graph:** `tsconfig.json` — the `{ "path": "apps/auth" }` project reference is removed so `tsc --build` no longer resolves a deleted package.
- **Workspace manifest:** root `package.json` — the `apps/*` workspace glob auto-drops the deleted package; no explicit `@morai/auth` entry exists to remove (verified). Only edit `package.json` if a build/lint surfaces a dangling reference after deletion.
(The sidecar service that replaces this app is deployed in 11-05's checkpoint; the refresh-tokens job is retired in 11-06.)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-sidecar-scaffold-auth-migration/11-CONTEXT.md

# The app being retired (read to size the removal + confirm no external importers)
@apps/auth/package.json
@apps/auth/src/main.ts
# Root workspace wiring to update
@tsconfig.json
@package.json
</context>

<tasks>

<task type="execute">
  <name>Task 1: Retire apps/auth and remove it from the root workspace wiring (D-04)</name>
  <files>apps/auth/ (deleted), tsconfig.json, package.json</files>
  <read_first>
    - apps/auth/package.json (the `@morai/auth` manifest — confirms it depends ON adapters/core/shared, nothing depends on it)
    - tsconfig.json (root project references — the `{ "path": "apps/auth" }` entry to remove)
    - package.json (root `workspaces` uses the `apps/*` glob — deleting the dir removes it; no explicit auth entry exists)
    - .planning/phases/11-sidecar-scaffold-auth-migration/11-05-PLAN.md (confirms the sidecar `client_from_manual_flow` dance is the live replacement that must precede this removal)
  </read_first>
  <action>
    Retire the TS `apps/auth` app (`@morai/auth`) — its OAuth dance + refresh role is now the
    sidecar's (D-04, D16 superseded). First confirm no live importer: `grep -rn "@morai/auth"
    apps packages` should return only `apps/auth/package.json` itself (the lone prose mention in
    `apps/worker/src/backfill-transactions.ts` is a comment, NOT an import — leave it untouched).
    Then: (1) delete the entire `apps/auth/` directory. (2) In root `tsconfig.json`, remove the
    `{ "path": "apps/auth" }` entry from `references`. (3) Root `package.json` `workspaces` is the
    `apps/*` glob, so the package drops automatically — do NOT add or edit an explicit entry;
    only touch `package.json` if a post-delete build flags a genuinely dangling reference.
    (4) Run `bun install` to re-resolve the workspace. Do NOT delete `makeSchwabOAuthClient` or
    any `@morai/adapters` export — those live in `packages/adapters` and may be used elsewhere;
    only the app is retired, not the adapter package. There is no `railway.auth.toml` to remove
    (verified: only `railway.server.toml` + `railway.worker.toml` exist). This is a deletion +
    workspace-glue change; the proof is a green suite + absence greps, so no RED test is needed.
  </action>
  <verify>
    <automated>test ! -d apps/auth && [ "$(grep -rl '@morai/auth' apps packages 2>/dev/null | wc -l | tr -d ' ')" = "0" ] && ! grep -q 'apps/auth' tsconfig.json && bun install && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `apps/auth/` directory no longer exists (`test ! -d apps/auth`).
    - `@morai/auth` is absent from root `package.json` workspaces (the `apps/*` glob no longer resolves an auth package; no explicit entry to remove).
    - `grep -r "@morai/auth" apps packages` returns nothing.
    - `tsconfig.json` no longer references `apps/auth`.
    - `bun install` succeeds with no missing-workspace / dangling-reference error.
  </acceptance_criteria>
  <done>The `apps/auth` app is deleted, dropped from the root tsc references + workspace, and `bun install` re-resolves cleanly with no `@morai/auth` reference anywhere in `apps`/`packages`.</done>
</task>

<task type="execute">
  <name>Task 2: Prove the workspace is green after the removal</name>
  <files>package.json (root scripts only — no file edits; this task runs typecheck/lint/test)</files>
  <read_first>
    - package.json (root scripts: `typecheck` = `tsc --build --force`, `test` = `vitest run`, `lint` = `eslint .`)
  </read_first>
  <action>
    Run the full proof that the workspace is intact after `apps/auth` is gone: `bun run typecheck`
    (`tsc --build --force` across the remaining project references — must resolve with no missing
    `apps/auth` project), then `bun run lint` (eslint clean — no unresolved import to the deleted
    package), then `bun run test` (the full Vitest workspace suite — green; no test depended on
    `@morai/auth`). If any of the three fails because something still referenced the retired app,
    fix the reference at its root (e.g. a stray project ref or import) — do NOT re-add `apps/auth`.
    Commit only when all three are green.
  </action>
  <verify>
    <automated>bun run typecheck && bun run lint && bun run test</automated>
  </verify>
  <acceptance_criteria>
    - `bun run typecheck` clean (no missing `apps/auth` project reference).
    - `bun run lint` clean (no unresolved `@morai/auth` import).
    - `bun run test` full workspace suite green.
  </acceptance_criteria>
  <done>Typecheck, lint, and the full workspace test suite are all green after the `apps/auth` retirement — the removal left no dangling reference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schwab OAuth dance owner → broker_tokens | After this removal there is exactly one owner of the dance + refresh: the sidecar (GW-03 / D-04). The retired `apps/auth` could mint/refresh tokens independently — removing it closes the second writer path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07-01 | Tampering | second OAuth/refresh path via retired `apps/auth` CLI | mitigate | `apps/auth` deleted so no TS process can run `setup`/`refresh` against `broker_tokens`; the sidecar is the sole dance + refresh owner (D-04, GW-03); proven by absence greps + green suite |
| T-11-07-02 | Denial of Service | removing auth before its replacement is live (auth gap) | mitigate | depends_on 11-05 (sidecar seeds token_json via `client_from_manual_flow`) + 11-06 (chain swap) — the removal runs strictly AFTER the sidecar owns the dance; ordering enforced by the wave/depends_on graph |
| T-11-07-03 | Information Disclosure | leftover OAuth client secrets / token-handling code in the repo | mitigate | the entire `apps/auth` setup/refresh/doctor/status surface is deleted; only the shared `@morai/adapters` OAuth client (which never logs token values) remains, used by the live data paths |
</threat_model>

<verification>
- `test ! -d apps/auth` — the app is gone.
- `grep -r "@morai/auth" apps packages` returns nothing; `grep apps/auth tsconfig.json` returns nothing.
- `bun install` re-resolves the workspace with no error.
- `bun run typecheck && bun run lint && bun run test` all green — no dangling reference to the retired app.
</verification>

<success_criteria>
- D-04 satisfied: the TS `apps/auth` OAuth setup/refresh client is retired (D16 superseded); the sidecar is the sole owner of the Schwab dance + refresh; all Schwab auth consolidates in one process.
- GW-03 reinforced: no second token-writer path remains in the repo.
- The workspace installs, typechecks, lints, and tests green after the removal.
</success_criteria>

<output>
Create `.planning/phases/11-sidecar-scaffold-auth-migration/11-07-SUMMARY.md` when done
</output>
