---
phase: 42-design-system-consolidation-one-reusable-component-system-fo
plan: 05
type: execute
wave: 3
depends_on:
  - 42-01-design-system-consolidation-one-reusable-component-system-fo
  - 42-02-design-system-consolidation-one-reusable-component-system-fo
  - 42-03-design-system-consolidation-one-reusable-component-system-fo
  - 42-04-design-system-consolidation-one-reusable-component-system-fo
files_modified:
  - docs/architecture/design-system.md
  - docs/TOPIC-MAP.md
  - .claude/rules/architecture-boundaries.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "docs/architecture/design-system.md documents DataTable as a system-layer molecule and its Atoms row no longer lists Button (ui/button.tsx is deleted; system/Button is sole)"
    - "docs/TOPIC-MAP.md's Architecture table has a row for design-system.md (it was missing — a pre-existing docs.md-rule violation this phase fixes)"
    - "A relevant rule's 'Where to Look' section links docs/architecture/design-system.md (docs.md cross-reference law)"
    - "Phase gate passes: full web suite green, apps/web tsc at the pre-existing 10-error baseline, and Overview + Analyzer render with no page scroll and indistinguishable table chrome at 1512x860 and 2056x1329"
  artifacts:
    - "docs/architecture/design-system.md (updated, not created — it was a 38-line stub)"
    - "docs/TOPIC-MAP.md (new Architecture-table row)"
    - ".claude/rules/architecture-boundaries.md (Where to Look link added)"
  key_links:
    - "design-system.md now cites index.css @theme + system/ molecules as the single token/component source; TOPIC-MAP indexes it; the rule points to it"
    - "phase gate is the /gsd-verify-work precondition — full suite + tsc baseline + dual-viewport visual parity"
---

<objective>
Close the phase: update the (already-existing) `docs/architecture/design-system.md` to
document the consolidation, add its missing `TOPIC-MAP.md` row, link it from a rule's "Where
to Look", and run the phase-gate verification (full suite + tsc baseline + dual-viewport
visual parity). Migration Manifest step 5's docs half + the phase's success gate.

RESEARCH Pitfall 1 (load-bearing): `docs/architecture/design-system.md` ALREADY EXISTS (a
38-line stub). This is an UPDATE + INDEX, not a create — do NOT overwrite it. And the file is
MISSING from TOPIC-MAP.md's Architecture table (a pre-existing docs.md-rule violation) — adding
that row is in scope regardless of the table/button work.

Purpose: the reference doc reflects the shipped one-DataTable / one-Button reality, and the
whole phase is proven green + visually converged.
Output: 3 doc/rule edits + recorded phase-gate evidence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-design-system-consolidation-one-reusable-component-system-fo/42-UI-SPEC.md
@.planning/phases/42-design-system-consolidation-one-reusable-component-system-fo/42-PATTERNS.md

# The doc being UPDATED (read in full first — it exists):
@docs/architecture/design-system.md
# The docs system rules governing this edit:
@.claude/rules/docs.md
# The system-layer molecules to catalog (Panel/…/DataTable/Button):
@apps/web/src/components/system/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: update design-system.md + TOPIC-MAP row + rule Where-to-Look</name>
  <files>docs/architecture/design-system.md, docs/TOPIC-MAP.md, .claude/rules/architecture-boundaries.md</files>
  <read_first>
    - docs/architecture/design-system.md (FULL 38 lines — the Layers table Atoms/Molecules rows, the Tokens list, "Rules for screens", Reference; match its terse Hemingway tone)
    - docs/TOPIC-MAP.md (Architecture section table — confirm design-system.md has NO row; copy the sibling-row format: path + one-line description)
    - .claude/rules/docs.md ("add cross-references: update TOPIC-MAP; update Where to Look in relevant rules"; "reference config as source of truth, never restate hex")
    - .claude/rules/architecture-boundaries.md (its "Where to Look" section — add the design-system link there, the rule that governs apps/web component layering)
  </read_first>
  <action>
    In docs/architecture/design-system.md (ADDITIVE edits only — do not rewrite the file):
    - Molecules row of the Layers table: add DataTable and Button to the composite list
      (e.g. "Panel, PanelHeading, SectionLabel, Stat, MetricChip, DataTable, Button").
    - Atoms row: drop "Button" from the shadcn-primitives example list (ui/button.tsx is deleted
      this phase; system/Button is the sole button) — a one-line correction, per RESEARCH Pitfall 1.
    - Add ONE short paragraph (matching the doc's terse tone) describing DataTable: the generic
      column-def table primitive (columns = { key, header, align?, mono?, sortable?, width?, render },
      caller-owned sort/selection state, sticky header, per-row testid) that every screen table renders
      through. Cite index.css @theme + components/system as the source — never restate hex values.
    In docs/TOPIC-MAP.md: add one row to the Architecture table for
      docs/architecture/design-system.md with a one-line description, in the same format as its
      sibling rows. Do not restructure the table.
    In .claude/rules/architecture-boundaries.md: add a bullet to its "Where to Look" section linking
      `docs/architecture/design-system.md` (relative path) with a short label (e.g. "the web UI
      design-system layers, tokens, and DataTable/Button primitives"). Keep the rule concise.
  </action>
  <verify>
    <automated>rg -n "DataTable" docs/architecture/design-system.md && rg -n "design-system.md" docs/TOPIC-MAP.md && rg -n "design-system" .claude/rules/architecture-boundaries.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "DataTable" docs/architecture/design-system.md` shows DataTable in the Molecules row + the description paragraph.
    - The Atoms row of design-system.md no longer lists "Button" as a ui/* example (`rg -n "Atoms" docs/architecture/design-system.md` line no longer names Button).
    - `rg -n "design-system.md" docs/TOPIC-MAP.md` returns the new Architecture-table row.
    - `rg -n "design-system" .claude/rules/architecture-boundaries.md` returns the new Where-to-Look link.
    - No hex values are restated in design-system.md's new prose (it cites index.css @theme as source).
  </acceptance_criteria>
  <done>design-system.md documents DataTable + corrected Atoms row; TOPIC-MAP indexes it; architecture-boundaries.md links it; terse tone preserved, no file overwrite.</done>
</task>

<task type="auto">
  <name>Task 2: phase gate — full suite, tsc baseline, dual-viewport visual parity</name>
  <files>docs/architecture/design-system.md</files>
  <read_first>
    - .planning/phases/.../42-VALIDATION.md (Sampling Rate: "Before /gsd-verify-work: full suite green + apps/web tsc at the pre-existing 10-error baseline")
    - .planning/phases/.../42-UI-SPEC.md (Migration Manifest "Visual parity check" — 1512×860 and 2056×1329, Overview + Analyzer desktop, no page scroll, every column/value/behavior present, NOT pixel-identical)
    - MEMORY note "GSD UAT UI via chrome-devtools" (standing permission to drive the browser)
  </read_first>
  <action>
    Run the full-phase gate and record the evidence in this plan's SUMMARY (this task changes no
    production code — it is the phase verification gate; if any check fails, that is a regression to
    fix in the owning plan, not to paper over here):
    1. `bun run test` — full workspace suite green (883+ web tests, per CONTEXT baseline).
    2. `cd apps/web && bunx tsc --noEmit` — at the pre-existing 10-error baseline (catch #29: root
       typecheck is blind to apps/web's own tsc; run apps/web's tsc explicitly).
    3. `cd apps/web && bunx eslint .` (or the repo lint command) — clean.
    4. Visual parity via chrome-devtools (standing permission), run the local web dev server read-only:
       load Overview and Analyzer (desktop trees) at BOTH 1512×860 and 2056×1329. Confirm: (a) NO page
       scroll at either size on either screen; (b) the Overview positions table and the Analyzer
       candidate table are visually indistinguishable in font, density, header chrome, and row hover
       (they now share DataTable); (c) every column/value/behavior is present (live cells flash, IV n/a
       badge, VERDICT chips, checkbox include, sort arrows on the Analyzer, Net-total row). Capture a
       screenshot per screen per size. If chrome-devtools is unavailable, note human_needed with the
       exact steps.
  </action>
  <verify>
    <automated>bun run test && cd apps/web && bunx tsc --noEmit 2>&1 | rg -c "error"</automated>
    <human-check>At 1512×860 and 2056×1329, Overview + Analyzer show no page scroll and the two tables are indistinguishable; all columns/values/behaviors present. Screenshots recorded in SUMMARY.</human-check>
  </verify>
  <acceptance_criteria>
    - `bun run test` full suite green.
    - `cd apps/web && bunx tsc --noEmit` at the pre-existing 10-error baseline (no new errors).
    - Lint clean.
    - Visual parity PASS recorded in SUMMARY: no page scroll at 1512×860 and 2056×1329 on Overview + Analyzer; the two tables indistinguishable; all columns/values/behaviors present (or human_needed noted with steps + screenshots).
  </acceptance_criteria>
  <done>Full suite + tsc baseline + lint green; dual-viewport visual parity confirmed (or human_needed recorded); phase is ready for /gsd-verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation + verification only. No runtime code changes, no input/network/storage/auth surface touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-06 | Information disclosure | design-system doc | low | mitigate | Doc cites index.css @theme as the token source and restates no secrets/hex — knowledge-base is read-only and untouched (docs.md rule). |
| T-42-SC | Tampering | package installs | n/a | accept | Zero package installs this plan. |

ASVS level 1; block-on-high. No high/critical threats — docs + verification only.
</threat_model>

<verification>
- `rg` confirms DataTable in design-system.md, the TOPIC-MAP row, and the rule link.
- `bun run test` green; `cd apps/web && bunx tsc --noEmit` at baseline; lint clean.
- Dual-viewport visual parity PASS (chrome-devtools screenshots or human_needed with steps).
</verification>

<success_criteria>
The design-system doc reflects the shipped one-DataTable / one-Button reality and is indexed +
rule-linked; the whole phase is green (full suite, tsc baseline, lint) and visually converged
(no page scroll, indistinguishable tables at both viewport sizes). Ready for /gsd-verify-work.
</success_criteria>

<output>
Create `.planning/phases/42-design-system-consolidation-one-reusable-component-system-fo/42-05-SUMMARY.md` when done.
</output>
