---
phase: 28-playbook-gates-anti-criteria-sizing
plan: 06
type: execute
wave: 5
depends_on: ["28-03", "28-04", "28-05"]
files_modified:
  - apps/web/src/components/RegimeBoard.tsx
  - apps/web/src/components/RegimeBoard.test.tsx
  - apps/web/src/components/picker/EntryExitPlan.tsx
  - apps/web/src/components/picker/EntryExitPlan.test.tsx
  - apps/web/src/components/picker/CandidateCard.tsx
  - apps/web/src/components/picker/CandidateCard.test.tsx
  - apps/server/src/adapters/mcp/server.ts
autonomous: false
requirements: [PLAY-01, PLAY-03, PLAY-04]

must_haves:
  truths:
    - "The regime board renders the entry-gate state (open/penalty/blocked/blind) with its VIX/ratio/asOf — GATE BLIND is visibly louder (PLAY-01: board shows the gate state)"
    - "The Analyzer entry plan renders the engine-resolved sizing tier + recommended contract count (PLAY-03), read straight from the snapshot, never recomputed"
    - "Event-bucket candidates render in a visually distinct section/label from the primary universe (PLAY-04)"
    - "The get_picker_candidates MCP tool description reflects the new gate/sizing/bucket payload"
  artifacts:
    - apps/web/src/components/RegimeBoard.tsx
    - apps/web/src/components/picker/EntryExitPlan.tsx
    - apps/web/src/components/picker/CandidateCard.tsx
  key_links:
    - "Every rendered value comes from PickerSnapshotResponse.gate/sizing/candidate.bucket — the UI is pure-render, the backend is the sole authority (V1)"
---

<objective>
Surface the new engine state on the two screens that already render the picker/regime data, and
update the MCP tool description — closing PLAY-01's "board shows the gate state", PLAY-03's
recommended-count display, and PLAY-04's distinct event-bucket rendering.

All three are render-only: the RegimeBoard adds a gate chip/tile (open/penalty/blocked, with a
visibly louder GATE BLIND treatment), the Analyzer's EntryExitPlan renders the sizing tier +
contract count straight from the snapshot, and CandidateCard labels event-bucket candidates
distinctly. The get_picker_candidates MCP tool description is updated to mention the gate,
sizing, and bucket fields the contract now carries. A blocking human-verify checkpoint confirms
the proposed [ASSUMED] gate-band and sizing boundaries look right on the live surfaces (the UAT
confirmation CONTEXT.md calls for).

Purpose: Make the gate, sizing, and bucket visible to the user and to Claude Code.
Output: board gate state, Analyzer sizing + bucket rendering, updated MCP surface, UAT sign-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-playbook-gates-anti-criteria-sizing/28-CONTEXT.md
@.planning/phases/28-playbook-gates-anti-criteria-sizing/28-RESEARCH.md

@apps/web/src/components/RegimeBoard.tsx
@apps/web/src/components/picker/EntryExitPlan.tsx
@apps/web/src/components/picker/CandidateCard.tsx
@apps/server/src/adapters/mcp/server.ts
@packages/contracts/src/picker.ts
@docs/architecture/playbook-gates.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RegimeBoard gate chip/tile — render entry-gate state with a loud GATE BLIND</name>
  <files>apps/web/src/components/RegimeBoard.tsx, apps/web/src/components/RegimeBoard.test.tsx</files>
  <read_first>
    - apps/web/src/components/RegimeBoard.tsx — the existing Chip + BAND_CLASSES color-mapping pattern to mirror for the gate state
    - packages/contracts/src/picker.ts — the gate field shape (state/vix/ratio/asOf/brakes) the board renders
    - docs/architecture/playbook-gates.md — the four gate states and what each means for the user
  </read_first>
  <behavior>
    - The board renders a gate chip/tile showing state (open/penalty/blocked/blind), the VIX + VIX/VIX3M ratio, and the asOf date
    - state "blind" renders a visibly louder/distinct treatment than blocked (the never-silent GATE BLIND flag)
    - When a brake is tripped, the chip names it (max-open / cooldown) alongside the gate state
    - All values read from the snapshot's gate field — no client-side band recomputation
  </behavior>
  <action>
    Add a gate chip/tile to RegimeBoard.tsx reusing the existing Chip + band-color pattern. Render
    the gate state, VIX, ratio, and asOf; give "blind" a distinct louder treatment; name any
    tripped brake. Read every value from PickerSnapshotResponse.gate. Extend RegimeBoard.test.tsx
    to cover each of the four states and the brake-named case.
  </action>
  <verify>
    <automated>bun run vitest run apps/web/src/components/RegimeBoard.test.tsx && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The board renders all four gate states with VIX/ratio/asOf; blind is visibly louder
    - A tripped brake is named on the board
    - Values come only from the snapshot gate field; tests + typecheck clean
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Analyzer entry-plan sizing count + distinct event-bucket candidate label</name>
  <files>apps/web/src/components/picker/EntryExitPlan.tsx, apps/web/src/components/picker/EntryExitPlan.test.tsx, apps/web/src/components/picker/CandidateCard.tsx, apps/web/src/components/picker/CandidateCard.test.tsx, apps/server/src/adapters/mcp/server.ts</files>
  <read_first>
    - apps/web/src/components/picker/EntryExitPlan.tsx — the entry-plan panel that renders the engine's exit/entry methodology; where the sizing tier + count line goes
    - apps/web/src/components/picker/CandidateCard.tsx — the candidate row; where the event-bucket label goes
    - packages/contracts/src/picker.ts — the sizing field + the candidate bucket tag
    - apps/server/src/adapters/mcp/server.ts — the get_picker_candidates tool registration/description to update
  </read_first>
  <behavior>
    - EntryExitPlan renders the engine-resolved sizing tier + recommended contract count from the snapshot's sizing field (e.g. VIX 18 → Normal → 2 contracts); no client recomputation
    - CandidateCard renders a distinct label/section for candidates tagged bucket "event-calendar" vs "standard"
    - The get_picker_candidates MCP tool description mentions the gate, sizing, and bucket fields now on the payload
  </behavior>
  <action>
    Render the sizing tier + contract count in EntryExitPlan.tsx from PickerSnapshotResponse.sizing.
    Add a distinct event-calendar label/section in CandidateCard.tsx keyed on the candidate's
    bucket tag. Update the get_picker_candidates tool description in the MCP server to note the new
    gate/sizing/bucket payload fields. Extend the two component test files to cover the sizing line
    and the bucket label.
  </action>
  <verify>
    <automated>bun run vitest run apps/web/src/components/picker/EntryExitPlan.test.tsx apps/web/src/components/picker/CandidateCard.test.tsx && bun run typecheck && bun run lint</automated>
  </verify>
  <acceptance_criteria>
    - The entry plan shows the engine-resolved tier + count from the snapshot (never recomputed)
    - Event-bucket candidates render visually distinct from standard candidates
    - The MCP tool description reflects the new payload; tests + typecheck + lint clean
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify — gate state on the board, sizing count on the Analyzer, [ASSUMED] boundaries</name>
  <action>
    Pause for a human UAT pass on the live surfaces before the phase closes. Present the built
    surfaces and collect either approval or corrected gate-band / sizing-count values (they are
    editable named constants in entry-gate.ts and sizing.ts).
  </action>
  <what-built>
    The regime board now shows the entry-gate state (open/penalty/blocked/blind + VIX/ratio/asOf,
    with a loud GATE BLIND), the Analyzer entry plan shows the engine-resolved sizing tier +
    contract count, and event-bucket candidates render distinctly. The gate-band edges (penalty
    20-25 / block ≥25; ratio 0.90-0.95 / block ≥0.95) and sizing counts (2/2/1/0) are the
    proposed [ASSUMED] defaults CONTEXT.md flagged for UAT confirmation.
  </what-built>
  <how-to-verify>
    1. Run the app locally (server + web) read-only against the latest snapshot, or view morai.wtf.
    2. On the Overview regime board, confirm the entry-gate tile shows the current VIX, VIX/VIX3M
       ratio, asOf date, and state; confirm GATE BLIND (if forced by a stale-macro fixture) reads
       visibly louder than a normal blocked state.
    3. On the Analyzer, confirm the entry plan shows a sizing tier + contract count that matches
       the current VIX tier (e.g. VIX ~18 → Normal → 2 contracts).
    4. Confirm the proposed gate-band edges and the 2/2/1/0 sizing counts match your TOS-tested
       priors, OR tell me the values to change (they are editable named constants).
  </how-to-verify>
  <resume-signal>Type "approved", or give the corrected gate-band / sizing-count values to update</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| snapshot → UI | The browser renders gate/sizing/bucket; it never supplies or overrides them |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-16 | Elevation of Privilege | RegimeBoard / EntryExitPlan render | medium | mitigate | The UI reads gate/sizing/bucket straight from PickerSnapshotResponse and never re-derives or overrides them — the backend stays the sole authority (V1); no client-writable gate input exists |
| T-28-17 | Information Disclosure (silent blind) | GATE BLIND rendering | high | mitigate | The board gives state "blind" a distinct louder treatment so a gate-down state is never invisible to the user (the never-silent requirement) |
</threat_model>

<verification>
- `bun run vitest run apps/web/src/components/RegimeBoard.test.tsx apps/web/src/components/picker/EntryExitPlan.test.tsx apps/web/src/components/picker/CandidateCard.test.tsx` — green
- `bun run typecheck` && `bun run lint` — clean
- Human-verify checkpoint: gate state, sizing count, and bucket label confirmed on the live surface; [ASSUMED] boundaries confirmed or corrected
</verification>

<success_criteria>
- PLAY-01: the regime board reflects the entry-gate state, GATE BLIND included
- PLAY-03: the Analyzer entry plan shows the recommended contract count per VIX tier
- PLAY-04: event-bucket candidates render distinctly
- The proposed [ASSUMED] gate-band and sizing boundaries are UAT-confirmed (or corrected)
</success_criteria>

## Artifacts this phase produces

- `apps/web/src/components/RegimeBoard.tsx` — entry-gate chip/tile with loud GATE BLIND
- `apps/web/src/components/picker/EntryExitPlan.tsx` — sizing tier + contract count line
- `apps/web/src/components/picker/CandidateCard.tsx` — distinct event-bucket label
- `apps/server/src/adapters/mcp/server.ts` — updated get_picker_candidates description

<output>
Create `.planning/phases/28-playbook-gates-anti-criteria-sizing/28-06-SUMMARY.md` when done
</output>
