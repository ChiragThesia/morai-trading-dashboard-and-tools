---
phase: 15-re-auth-smoothing
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/core/src/brokerage/domain/token-freshness.ts
  - packages/core/src/brokerage/domain/token-freshness.test.ts
  - packages/core/src/brokerage/application/ports.ts
  - packages/contracts/src/status.ts
  - packages/contracts/src/status.test.ts
  - apps/server/src/adapters/status-dto.ts
  - apps/server/src/adapters/http/status.routes.test.ts
  - packages/adapters/src/memory/broker-tokens.ts
  - packages/core/src/journal/application/getStatus.test.ts
  - packages/core/src/brokerage/application/selectChainSource.test.ts
  - packages/core/src/brokerage/application/refreshTokens.test.ts
  - apps/worker/src/handlers/fetch-schwab-chain.test.ts
  - apps/web/src/components/AuthExpiredBanner.test.tsx
autonomous: true
requirements: [AUTH-05]
must_haves:
  truths:
    - "refreshExpiresInSeconds(refreshIssuedAt, now) returns null outside the 6-day warn window and a non-negative integer of seconds-until-7d-cutoff inside it"
    - "toAppTokenStatus populates refreshExpiresIn on every app status: null for none_yet, 0 when AUTH_EXPIRED, seconds-remaining when near expiry"
    - "GET /api/status and MCP get_status responses carry refreshExpiresIn per app and pass statusResponse.parse without a Date/undefined leak"
  artifacts:
    - packages/core/src/brokerage/domain/token-freshness.ts
    - packages/core/src/brokerage/application/ports.ts
    - packages/contracts/src/status.ts
    - apps/server/src/adapters/status-dto.ts
  key_links:
    - "toAppTokenStatus is the single freshness choke point both the Postgres repo and the memory twin call — the field propagates through it (no adapter-level duplication)"
    - "status-dto.serializeApp is the single Date-to-wire mapper for both the HTTP route and the MCP get_status tool — refreshExpiresIn passes through it unchanged (MCP-02 by construction)"
---

<objective>
Add a `refreshExpiresIn` field (seconds until the 7-day refresh-token cutoff, non-null only inside the T-24h window) to the token-freshness domain, the `statusResponse` contract, and the status serialization chain. This is the AUTH-05 status-surface backbone: once the field flows through `toAppTokenStatus` and `serializeApp`, it propagates to both `GET /api/status` and MCP `get_status` automatically (D-01), and the amber banner (15-05) and warning log (15-04) consume it.

This is the interface-first plan for AUTH-05. Adding a REQUIRED field to the widely-constructed `AppTokenStatus` domain type is a single atomic, compiler-guided change: the field must be threaded through every literal constructor across core, adapters, contracts, server, web, and worker test fixtures in ONE green commit (architecture-boundaries.md §8 — driven-port changes update the in-memory twin in the same change).

Purpose: SC1 — "when the refresh token is within 24h of its 7-day expiry, GET /api/status includes a non-null refreshExpiresIn field."
Output: `refreshExpiresInSeconds` domain function; `refreshExpiresIn: number | null` on the domain `AppTokenStatus` type and the `appTokenStatus` Zod contract; `serializeApp` passthrough; all downstream constructors + fixtures green.

PROVISIONAL DECISIONS (user was AFK; override at plan review):
- D-01 (Claude-recommended): passive status surfaces only — `refreshExpiresIn` per app on `/api/status`, MCP `get_status` for free; no push adapter.
- Representation (Claude's Discretion, RESEARCH Pitfall 5 / Assumption A1): seconds as a non-negative integer, `null` OUTSIDE the T-24h window (the field IS the alert signal, not a general countdown). Confirm at review.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-re-auth-smoothing/15-CONTEXT.md
@.planning/phases/15-re-auth-smoothing/15-RESEARCH.md
@.planning/phases/15-re-auth-smoothing/15-PATTERNS.md
</context>

<artifacts_this_phase_produces>
New symbols/paths this plan creates (source-grounding exclusion list for plan review):
- `refreshExpiresInSeconds(refreshIssuedAt: Date, now: Date): number | null` — new exported pure function in `packages/core/src/brokerage/domain/token-freshness.ts`.
- `refreshExpiresIn` — new readonly field (`number | null`) on the domain type `AppTokenStatus` in `packages/core/src/brokerage/application/ports.ts`.
- `refreshExpiresIn` — new field on the `appTokenStatus` Zod schema in `packages/contracts/src/status.ts` (`z.number().int().nonnegative().nullable()`).
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: refreshExpiresInSeconds pure domain function (RED→GREEN, isolated)</name>
  <files>packages/core/src/brokerage/domain/token-freshness.ts, packages/core/src/brokerage/domain/token-freshness.test.ts</files>
  <read_first>
    - packages/core/src/brokerage/domain/token-freshness.ts (existing isNearExpiry + SEVEN_DAYS_MS/WARN_THRESHOLD_MS constants — reuse them, do not redefine)
    - packages/core/src/brokerage/domain/token-freshness.test.ts (existing isNearExpiry describe block — mirror its just-under / exactly-at / just-over boundary style)
    - .planning/phases/15-re-auth-smoothing/15-PATTERNS.md (Pattern 1 — the recommended refreshExpiresInSeconds shape)
    - .claude/rules/tdd.md (RED→GREEN discipline), .claude/rules/typescript.md (Result/readonly/no-any)
  </read_first>
  <behavior>
    - refreshExpiresInSeconds(refreshIssuedAt, now) returns null when NOT isNearExpiry (e.g. refreshIssuedAt 5 days ago → null).
    - Returns a non-negative integer of seconds when inside the warn window: at exactly 6 days old → ~86400 seconds (1 day) remaining; boundary values tested just-under (6d − 1min → null) and just-over (6d + 1min → non-null seconds).
    - Returns 0 (not negative, not null) when already past the 7-day cutoff (e.g. 8 days old → isNearExpiry true, SEVEN_DAYS_MS − ageMs is negative → Math.max(0, …) → 0).
    - Pure function: no I/O, no Date.now(); `now` injected. Reuses SEVEN_DAYS_MS and WARN_THRESHOLD_MS already in the file.
  </behavior>
  <action>
    Write the failing boundary tests FIRST in token-freshness.test.ts (a new describe block for refreshExpiresInSeconds), run them and confirm they fail because the function does not yet exist. Then add and export `refreshExpiresInSeconds(refreshIssuedAt: Date, now: Date): number | null` in token-freshness.ts alongside isNearExpiry: return null when isNearExpiry is false; otherwise compute ageMs from now minus refreshIssuedAt and return Math.max(0, Math.round((SEVEN_DAYS_MS - ageMs) / 1000)). Do NOT touch the AppTokenStatus type in this task (that is Task 2) so this task reaches green in isolation with zero downstream typecheck breakage. Reuse the existing module constants; do not introduce a new duration utility (Don't Hand-Roll).
  </action>
  <verify>
    <automated>bun test packages/core/src/brokerage/domain/token-freshness.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - token-freshness.test.ts has a `describe("refreshExpiresInSeconds", …)` block with at least: 5-days-old → null; exactly-6-days → non-null integer; just-under-6d → null; just-over-6d → non-null; 8-days-old (past cutoff) → 0.
    - `refreshExpiresInSeconds` is exported from token-freshness.ts and imported in the test.
    - `bun test packages/core/src/brokerage/domain/token-freshness.test.ts` passes; the RED run was shown failing on the missing symbol before implementation.
    - No new constant introduced — the function references SEVEN_DAYS_MS and WARN_THRESHOLD_MS already defined in the file.
  </acceptance_criteria>
  <done>refreshExpiresInSeconds exists, is unit-tested at the T-24h boundary, and the domain package suite is green with no change to any type.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Thread refreshExpiresIn through the type, contract, DTO, and every constructor (atomic green)</name>
  <files>packages/core/src/brokerage/application/ports.ts, packages/core/src/brokerage/domain/token-freshness.ts, packages/contracts/src/status.ts, packages/contracts/src/status.test.ts, apps/server/src/adapters/status-dto.ts, apps/server/src/adapters/http/status.routes.test.ts, packages/adapters/src/memory/broker-tokens.ts, packages/core/src/journal/application/getStatus.test.ts, packages/core/src/brokerage/application/selectChainSource.test.ts, packages/core/src/brokerage/application/refreshTokens.test.ts, apps/worker/src/handlers/fetch-schwab-chain.test.ts, apps/web/src/components/AuthExpiredBanner.test.tsx</files>
  <read_first>
    - packages/core/src/brokerage/application/ports.ts (AppTokenStatus type — add the field; preserve readonly convention)
    - packages/core/src/brokerage/domain/token-freshness.ts (toAppTokenStatus — 4 return branches to update)
    - packages/contracts/src/status.ts (appTokenStatus schema — MCP-02 single source header comment)
    - apps/server/src/adapters/status-dto.ts (serializeApp — the sole Date→wire mapper for HTTP and MCP)
    - packages/adapters/src/memory/broker-tokens.ts (hand-built none_yet AppTokenStatus literals in readTokenFreshness — several branches)
    - .planning/phases/15-re-auth-smoothing/15-PATTERNS.md (Patterns 1, and the status-dto / ports / contract extension points)
    - .planning/phases/15-re-auth-smoothing/15-RESEARCH.md (Common Pitfall 5 — seconds vs ISO, null-vs-always-populated)
    - .claude/rules/architecture-boundaries.md (§8 in-memory twin same change), .claude/rules/typescript.md (parse-don't-cast; no undefined leak under exactOptionalPropertyTypes)
  </read_first>
  <behavior>
    - toAppTokenStatus returns refreshExpiresIn = null for the null-row (none_yet) branch; for the AUTH_EXPIRED / stale / fresh branches it returns refreshExpiresInSeconds(row.refreshIssuedAt, now) — so an 8-day-old token yields 0, a 6.5-day-old token yields seconds, and a 3-day-old token yields null.
    - The wire response for a near-expiry app carries an integer refreshExpiresIn; for a far-from-expiry app it carries null; statusResponse.parse accepts both and rejects a non-integer or a raw Date.
    - Existing status.routes.test.ts / getStatus.test.ts / contracts status.test.ts / selectChainSource.test.ts / refreshTokens.test.ts / fetch-schwab-chain.test.ts / AuthExpiredBanner.test.tsx fixtures compile and pass with the new field present.
  </behavior>
  <action>
    Add `readonly refreshExpiresIn: number | null` to the domain AppTokenStatus type in ports.ts. In token-freshness.ts toAppTokenStatus, set refreshExpiresIn on all four return objects: null in the none_yet (row === null) branch; `refreshExpiresInSeconds(row.refreshIssuedAt, now)` in the AUTH_EXPIRED, stale, and fresh branches. Add `refreshExpiresIn: z.number().int().nonnegative().nullable()` to the appTokenStatus schema in contracts/status.ts (keep it a required key so the wire shape is stable — a far-from-expiry app sends null, never omits the key). In status-dto.ts serializeApp, add `refreshExpiresIn: app.refreshExpiresIn` (it is already number | null — no Date conversion). Then run the workspace typecheck and full test suite; the compiler and failing fixtures enumerate EVERY remaining AppTokenStatus / appTokenStatus literal that must gain the field — add `refreshExpiresIn` to each: null in none_yet/synthesized literals (memory twin, getStatus.test, selectChainSource.test, refreshTokens.test helper, fetch-schwab-chain.test, AuthExpiredBanner.test makeStatusData) and a realistic value where a near-expiry case is asserted (contracts status.test valid fixtures). Do NOT weaken the field to optional to dodge the ripple (that would let serializeApp emit undefined and break statusResponse.parse). Add one new assertion in status.routes.test.ts proving a near-expiry payload round-trips a non-null integer refreshExpiresIn and a far-from-expiry payload round-trips null through statusResponse.parse. Commit only when workspace typecheck and the full suite are green.
  </action>
  <verify>
    <automated>bun run typecheck && bun run test</automated>
  </verify>
  <acceptance_criteria>
    - `bun run typecheck` is clean across the workspace (core, adapters, contracts, server, web, worker) with no `any`/`as`/`!` added (typescript.md).
    - `bun run test` is fully green.
    - ports.ts AppTokenStatus and contracts/status.ts appTokenStatus both declare `refreshExpiresIn` (domain: number | null; contract: z.number().int().nonnegative().nullable()).
    - toAppTokenStatus none_yet branch yields refreshExpiresIn null; AUTH_EXPIRED branch yields 0 for an >7-day-old row (proven by an extended token-freshness.test.ts assertion or a status.routes.test.ts round-trip).
    - status.routes.test.ts contains a new assertion that refreshExpiresIn survives statusResponse.parse as a non-null integer (near-expiry fixture) and as null (far-from-expiry fixture).
    - serializeApp passes refreshExpiresIn straight through (no `.toISOString()`, no `?? null` masking of an undefined).
  </acceptance_criteria>
  <done>The new field is a first-class part of the domain type, the wire contract, and the serialization chain; every constructor across the workspace supplies it; both HTTP and MCP status responses carry it (MCP-02 by construction).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| core → wire (status-dto → statusResponse.parse) | Domain Date/number values serialized and validated before leaving the server |
| server → browser / MCP client | `refreshExpiresIn` crosses to untrusted consumers via `/api/status` and `get_status` |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Information Disclosure | refreshExpiresIn on the status surface | low | accept | The field is a derived seconds count from refresh_issued_at timestamps already surfaced (expiresAt/refreshIssuedAt); it carries no token material. No new secret exposed. |
| T-15-02 | Tampering | statusResponse boundary | low | mitigate | appTokenStatus adds `z.number().int().nonnegative().nullable()` — parse-don't-cast rejects malformed/negative/float values at the wire boundary (V5 Input Validation). |
| T-15-03 | Tampering | broker_tokens read path | low | accept | This plan reads timestamp columns only (no decryption, no writes); the existing readTokenFreshness bound-param discipline is unchanged. |
| T-15-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: N/A — all libraries already vendored). |
</threat_model>

<verification>
- `bun run typecheck` clean; `bun run test` green (workspace).
- `bun test packages/core/src/brokerage/domain/token-freshness.test.ts` covers the T-24h boundary.
- `bun test apps/server/src/adapters/http/status.routes.test.ts` proves refreshExpiresIn round-trips through statusResponse.parse (non-null integer + null).
- Grep confirms no `console` import added to packages/core and no `any`/`as`/`!` introduced.
</verification>

<success_criteria>
- refreshExpiresIn is present per app on `GET /api/status` and MCP `get_status`, non-null only inside the T-24h window, null otherwise (SC1 status-field half).
- The field is computed once in the domain (toAppTokenStatus) and reused by both adapters — zero duplication.
- Workspace typecheck + full suite green.
</success_criteria>

<output>
Create `.planning/phases/15-re-auth-smoothing/15-01-SUMMARY.md` when done.
</output>
