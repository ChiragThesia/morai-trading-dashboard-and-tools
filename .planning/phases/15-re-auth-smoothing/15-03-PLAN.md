---
phase: 15-re-auth-smoothing
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/jobs.ts
  - packages/contracts/src/jobs.test.ts
  - apps/server/src/adapters/mcp/tools/trigger-job.ts
  - apps/server/src/adapters/http/jobs.routes.test.ts
autonomous: true
requirements: [AUTH-06]
must_haves:
  truths:
    - "TRIGGERABLE_JOBS lists exactly rebuild-journal, sync-fills, compute-bsm-greeks (length 3) — the retired token-refresh job is no longer triggerable"
    - "the trigger_job MCP tool description advertises only the three live jobs"
  artifacts:
    - packages/contracts/src/jobs.ts
    - apps/server/src/adapters/mcp/tools/trigger-job.ts
  key_links:
    - "jobs.routes.test.ts canonical-list assertion enforces the 3-job contract; TRIGGERABLE_JOBS is the single MCP-02 source consumed by both the HTTP route and the MCP tool"
---

<objective>
Fold in the v1.1 milestone-audit debt item (D-04): remove the stale `refresh-tokens` entry from the triggerable-job surface. The token-refresh job was retired in Phase 11 (GW-03 — the Python sidecar is the sole token writer); invoking it via trigger_job today returns a storage-error. Its presence in the triggerable list and the MCP tool description is dead surface that misleads operators and Claude sessions. This pairs with AUTH-06's re-auth story: the old programmatic token-refresh path is gone; the new path is the operator re-auth flow (15-02).

Purpose: D-04 — remove `"refresh-tokens"` from TRIGGERABLE_JOBS and update the trigger_job MCP tool description; keep the two adapter surfaces in sync (MCP-02 single schema source).
Output: 3-entry TRIGGERABLE_JOBS; corrected MCP tool description; updated tests.

PROVISIONAL DECISION (user AFK — override at review): D-04 (fold this cleanup into Phase 15). Requirement mapping: this cleanup completes AUTH-06's token-lifecycle surface (the retired refresh mechanism the operator re-auth flow replaces).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-re-auth-smoothing/15-CONTEXT.md
@.planning/phases/15-re-auth-smoothing/15-PATTERNS.md
</context>

<artifacts_this_phase_produces>
New symbols/paths this plan creates (source-grounding exclusion list for plan review):
- None. This plan only removes one entry from an existing `as const` array and edits an existing description string + test assertions. No new symbols, files, or fields.
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Remove the retired token-refresh job from the triggerable surface (RED-first via the canonical-list test)</name>
  <files>packages/contracts/src/jobs.ts, packages/contracts/src/jobs.test.ts, apps/server/src/adapters/mcp/tools/trigger-job.ts, apps/server/src/adapters/http/jobs.routes.test.ts</files>
  <read_first>
    - packages/contracts/src/jobs.ts (TRIGGERABLE_JOBS array lines 12-17; triggerJobBodyFor; the MCP-02 header comment)
    - apps/server/src/adapters/http/jobs.routes.test.ts (the canonical-list test near lines 134-142 — currently asserts the retired job is present and length 4)
    - packages/contracts/src/jobs.test.ts (near lines 31-32 — a test currently using the retired job name as a "non-rebuild job passes without calendarId" case)
    - apps/server/src/adapters/mcp/tools/trigger-job.ts (the description string near line 28 listing supported jobs)
    - .planning/phases/15-re-auth-smoothing/15-PATTERNS.md (D-04 cleanup section — exact edit targets)
    - .claude/rules/tdd.md (RED->GREEN for a change starts with the failing updated assertion), .claude/rules/architecture-boundaries.md (§9 keep adapter surfaces in sync)
  </read_first>
  <behavior>
    - TRIGGERABLE_JOBS resolves to exactly ["rebuild-journal", "sync-fills", "compute-bsm-greeks"] (length 3).
    - The jobs.routes.test canonical-list test asserts the three live jobs are present and toHaveLength(3).
    - The jobs.test "non-rebuild job passes without calendarId" case uses a still-valid job name and still passes.
    - The trigger_job MCP tool description's "Supported jobs" list names only the three live jobs.
  </behavior>
  <action>
    RED first: update the canonical-list assertion in jobs.routes.test.ts to expect the three live jobs (rebuild-journal, sync-fills, compute-bsm-greeks) and toHaveLength(3), removing the assertion that the retired token-refresh job is contained. Run it and confirm it fails because the array still has 4 entries. Also update the case in jobs.test.ts that currently passes the retired job name into triggerJobBodyFor(...) to instead use a still-valid non-rebuild job name (for example sync-fills) so the "calendarId optional for non-rebuild jobs" intent is preserved. GREEN: remove the retired token-refresh line from the TRIGGERABLE_JOBS array in jobs.ts, and edit the trigger_job MCP tool description in trigger-job.ts so its "Supported jobs" sentence lists only rebuild-journal, sync-fills, compute-bsm-greeks. Do NOT touch job-runs.ts / TRACKED_JOBS or the worker schedule — those legitimately still reference the retired job for reading HISTORICAL run records, which is out of D-04 scope. Run the full suite to confirm green.
  </action>
  <verify>
    <automated>bun test packages/contracts/src/jobs.test.ts apps/server/src/adapters/http/jobs.routes.test.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - jobs.routes.test.ts canonical-list test asserts toHaveLength(3) and toContain for each of rebuild-journal, sync-fills, compute-bsm-greeks; it was shown failing (length 4) before the array edit.
    - TRIGGERABLE_JOBS in jobs.ts has exactly three entries; TriggerableJob union no longer includes the retired token-refresh name.
    - The trigger_job description in trigger-job.ts lists exactly rebuild-journal, sync-fills, compute-bsm-greeks under Supported jobs.
    - jobs.test.ts no longer feeds the retired job name into triggerJobBodyFor; its non-rebuild-optional-calendarId case uses a live job and passes.
    - `bun run typecheck` clean; `bun test` for both files green; job-runs.ts and the worker schedule are untouched (git diff scoped to the four files).
  </acceptance_criteria>
  <done>The retired token-refresh job is gone from the triggerable surface and both adapter surfaces (contract + MCP description) agree; historical job-run reads are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP/HTTP client -> trigger_job enum | z.enum(TRIGGERABLE_JOBS) is the allow-list of job names accepted at both adapters |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-07 | Tampering | trigger_job job-name allow-list | low | mitigate | Removing the retired entry shrinks the accepted allow-list; z.enum(TRIGGERABLE_JOBS) continues to reject any name not in the 3-entry list at both boundaries (T-05-22 preserved). |
| T-15-08 | Information Disclosure | MCP tool description | low | mitigate | The description no longer advertises a job that only returns a storage-error, removing a misleading affordance for agents. |
| T-15-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `bun run typecheck` clean; targeted tests green.
- TRIGGERABLE_JOBS length 3; MCP description lists only the three live jobs.
- job-runs.ts / worker schedule untouched (historical refresh-tokens run records still readable).
</verification>

<success_criteria>
- trigger_job no longer offers the retired token-refresh job at either the HTTP or MCP surface (D-04).
- MCP-02 single-source invariant preserved: one array, both adapters.
</success_criteria>

<output>
Create `.planning/phases/15-re-auth-smoothing/15-03-SUMMARY.md` when done.
</output>
