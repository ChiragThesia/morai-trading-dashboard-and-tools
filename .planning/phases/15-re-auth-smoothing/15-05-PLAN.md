---
phase: 15-re-auth-smoothing
plan: 05
type: tdd
wave: 2
depends_on: ["15-01"]
files_modified:
  - apps/web/src/components/AuthExpiredBanner.tsx
  - apps/web/src/components/AuthExpiredBanner.test.tsx
autonomous: true
requirements: [AUTH-05]
must_haves:
  truths:
    - "the banner shows an amber warning when either app's refreshExpiresIn is non-null and no app is AUTH_EXPIRED"
    - "the banner shows the existing red banner when an app is AUTH_EXPIRED; red takes precedence over amber"
    - "the amber state considers BOTH the trader and market apps (worst-case), not trader-only"
  artifacts:
    - apps/web/src/components/AuthExpiredBanner.tsx
  key_links:
    - "the amber state reads refreshExpiresIn off tokenFreshness from useStatus() -> statusResponse — the contract field added in 15-01"
---

<objective>
Add the amber pre-expiry warning state to the web status banner (AUTH-05, D-03). Today AuthExpiredBanner renders only the red AUTH_EXPIRED banner (or nothing). This plan adds an amber sibling state that surfaces the T-24h window BEFORE expiry — the visible half of "never a silent outage." It extends the existing component in place (D-01: "extend the existing AuthExpiredBanner pattern").

Purpose: SC1 — the web dashboard shows an amber warning banner inside the T-24h window; red at expiry.
Output: an amber state in AuthExpiredBanner.tsx driven by refreshExpiresIn (both apps), red taking precedence; extended tests.

PROVISIONAL DECISIONS — surface at plan review:
- No UI-SPEC (orchestrator provisional decision 1): the only frontend surface is this amber banner; CONTEXT.md D-01 puts banner copy/styling under Claude's Discretion — follow the AuthExpiredBanner precedent (role="alert", fixed-bottom, JetBrains Mono, no dismiss button) with an amber palette. Exact hex is Claude's Discretion.
- Both-apps coverage (orchestrator provisional decision 4; RESEARCH Open Question 2 / Assumption A3): the NEW amber state MUST check both trader and market (surface whichever is closer to expiry). The pre-existing red banner is trader-only; extend red to both apps ONLY if it is genuinely the same one-or-two lines in this same component — otherwise leave red as-is and note the residual red-only gap in the SUMMARY (surgical-changes rule). Do not expand scope further.
- D-02b (runbook link): if practical, the amber copy links/points to the operator re-auth runbook (docs/operations/schwab-reauth-runbook.md) so the operator knows the fix. Optional if it complicates the client build.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-re-auth-smoothing/15-CONTEXT.md
@.planning/phases/15-re-auth-smoothing/15-RESEARCH.md
@.planning/phases/15-re-auth-smoothing/15-PATTERNS.md
</context>

<artifacts_this_phase_produces>
New symbols/paths this plan creates (source-grounding exclusion list for plan review):
- New amber-state branch + markup inside apps/web/src/components/AuthExpiredBanner.tsx (no new component symbol unless a small local helper is extracted).
- Extended fixtures/describe blocks in AuthExpiredBanner.test.tsx (parametrized refreshExpiresIn per app).
- Consumes refreshExpiresIn on the contract StatusResponse (added in 15-01) — not new here.
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Amber pre-expiry banner state (both apps; red precedence) — RED->GREEN</name>
  <files>apps/web/src/components/AuthExpiredBanner.tsx, apps/web/src/components/AuthExpiredBanner.test.tsx</files>
  <read_first>
    - apps/web/src/components/AuthExpiredBanner.tsx (current red-only component: the isExpired gate lines 27-34; the inline-style + role="alert" + no-dismiss markup lines 36-68)
    - apps/web/src/components/AuthExpiredBanner.test.tsx (mock setup; makeStatusData helper — 15-01 added refreshExpiresIn: null to it; extend it to parametrize per-app refreshExpiresIn)
    - apps/web/src/hooks/useStatus.ts (the polled statusResponse the banner consumes; parse-don't-cast)
    - packages/contracts/src/status.ts (appTokenStatus.refreshExpiresIn added in 15-01 — the field driving the amber gate)
    - .planning/phases/15-re-auth-smoothing/15-PATTERNS.md (Pattern 2 — amber as a sibling; boolean-gate shape; a11y/style precedent; Pitfall 3 trader-only gap)
    - .claude/rules/typescript.md (no any/as/!; compute values in variables above return, no IIFE in JSX)
  </read_first>
  <behavior>
    - Renders the amber banner (role="alert") when tokenFreshness is the map form, no app is AUTH_EXPIRED, and at least one app has refreshExpiresIn !== null.
    - Renders the existing red banner when an app is AUTH_EXPIRED (red takes precedence even if the other app is also near expiry).
    - Amber considers both apps: a market-only near-expiry (trader fresh, market refreshExpiresIn non-null) still shows amber.
    - Renders nothing when: data undefined; tokenFreshness "none yet"; both apps fresh with refreshExpiresIn null.
    - No dismiss/close button in either state (queryByRole("button") is null).
  </behavior>
  <action>
    Extend AuthExpiredBanner.tsx: keep the existing isExpired (red) gate, and add an isNearExpiry gate computed over BOTH apps — true when tokenFreshness is the map form, neither app status is AUTH_EXPIRED, and (trader.refreshExpiresIn !== null || market.refreshExpiresIn !== null). Render order: if isExpired return the red banner (unchanged markup/copy — that copy is UI-SPEC-locked); else if isNearExpiry return an amber banner; else return null. The amber banner reuses the red banner's structural precedent — role="alert", fixed-bottom fixed positioning, JetBrains Mono font stack, no dismiss button — with an amber palette (backgroundColor/borderTop/color are Claude's Discretion; pick amber tones distinct from the red #180f10/#ef5350). Amber copy (Claude's Discretion, Hemingway-plain) should say the Schwab auth expires within ~24h and to re-auth soon; if practical, reference the re-auth runbook so the operator knows the fix. Compute both gate booleans in variables above the return (no IIFE in JSX). Only extend the red banner to check market too IF that is the same one-or-two-line change in this component; otherwise leave red trader-only and record the residual gap in the SUMMARY. Write the RED tests first (extend makeStatusData to set per-app refreshExpiresIn; add an amber describe block), confirm failure, then implement to green.
  </action>
  <verify>
    <automated>bun test apps/web/src/components/AuthExpiredBanner.test.tsx && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - AuthExpiredBanner.test.tsx has amber cases asserting: amber renders when a near-expiry non-null and no AUTH_EXPIRED; red renders (precedence) when AUTH_EXPIRED and near-expiry both true; amber renders for market-only near-expiry; null when both apps fresh + refreshExpiresIn null; null on "none yet"; no button in amber.
    - makeStatusData is parametrized to set refreshExpiresIn per app (trader and market independently).
    - The amber and red branches both use role="alert" and render no dismiss button.
    - Existing red-banner tests still pass unchanged (locked copy intact).
    - `bun run typecheck` clean; `bun test` for the component green; no any/as/! and no IIFE in JSX.
    - The RED run was shown failing before implementation.
  </acceptance_criteria>
  <done>The banner surfaces amber inside the T-24h window across both apps and red at expiry, with red taking precedence; the visible AUTH-05 warning exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server /api/status -> browser banner | the banner renders values from the polled, Zod-parsed statusResponse |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-12 | Information Disclosure | amber banner copy | low | mitigate | The banner renders only a human-readable "expires soon" message + optional runbook link; it does not render token material or raw refreshIssuedAt values. |
| T-15-13 | Spoofing/Tampering | status data trust | low | accept | useStatus already parses through statusResponse.parse (parse-don't-cast); the banner consumes validated data only, unchanged by this plan. |
| T-15-14 | Denial of Service (UX) | persistent banner with no dismiss | low | accept | No-dismiss is the intended design (persists until the condition clears); consistent with the red banner precedent. |
| T-15-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `bun test apps/web/src/components/AuthExpiredBanner.test.tsx` green (amber + red precedence + both-apps + no-button).
- `bun run typecheck` clean.
- Manual (optional, human-verify at phase gate): with a near-expiry status fixture, the amber banner is visible and legible; red still shows at AUTH_EXPIRED.
</verification>

<success_criteria>
- The dashboard shows an amber warning banner inside the T-24h window (both apps) and the red banner at expiry, red taking precedence (SC1 visible half, D-01/D-03).
- The component is extended in place following the AuthExpiredBanner precedent; no UI-SPEC required.
</success_criteria>

<output>
Create `.planning/phases/15-re-auth-smoothing/15-05-SUMMARY.md` when done.
</output>
