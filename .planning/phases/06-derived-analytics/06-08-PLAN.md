---
phase: 06-derived-analytics
plan: 08
type: tdd
wave: 2
depends_on: ["06-06"]
gap_closure: true
autonomous: true
requirements: [ANLY-01, ANLY-03, MCP-02]
files_modified:
  - packages/core/src/analytics/domain/percentile-rank.ts
  - packages/core/src/analytics/domain/percentile-rank.test.ts
  - packages/core/src/analytics/domain/percentile-rank.property.test.ts
  - packages/core/src/analytics/application/computeAnalytics.ts
  - packages/core/src/analytics/application/computeAnalytics.test.ts
  - packages/adapters/src/postgres/repos/leg-observations.ts
  - packages/adapters/src/memory/leg-observations.ts
  - packages/adapters/src/__contract__/smile-source.contract.ts
  - packages/contracts/src/analytics.ts
  - packages/contracts/src/analytics.test.ts
  - apps/worker/src/main.ts
  - apps/worker/src/schedule.ts

must_haves:
  truths:
    - "percentileRank returns null for empty history (not 100); the use-case persists rr_rank = null until at least one prior value exists, carried through the skewResponse contract (WR-01 / W1)"
    - "moneyness is populated at the smile read from strike and underlying_price (spot) — K/S — on both adapters; the skew_observations.moneyness column and the contract field carry a real value, not a constant null (WR-03 / W4)"
    - "The stale worker comments are corrected to 9 queues / 6 crons / 9 work handlers (IN-01)"
  artifacts:
    - path: "packages/core/src/analytics/domain/percentile-rank.ts"
      provides: "empty history -> null (number | null return)"
      contains: "null"
    - path: "packages/adapters/src/postgres/repos/leg-observations.ts"
      provides: "moneyness computed from strike / underlying_price"
      contains: "moneyness"
  key_links:
    - from: "packages/core/src/analytics/domain/percentile-rank.ts"
      to: "packages/contracts/src/analytics.ts"
      via: "null rank flows percentileRank -> computeAnalytics rrRank -> skewResponse.rrRank (already nullable)"
      pattern: "rrRank|rr_rank"
    - from: "packages/adapters/src/postgres/repos/leg-observations.ts"
      to: "packages/contracts/src/analytics.ts"
      via: "computed moneyness flows smile read -> skew_observations -> skewSmileResponse.moneyness"
      pattern: "moneyness"
---

<objective>
Close WR-01, WR-03, and IN-01 — the dead-null and stale-comment surfaces that survive the seam fix.
WR-01 (W1): `percentileRank` returns 100 for empty history, so the FIRST-EVER risk-reversal of any
(underlying, expiration) is written at the 100th percentile — economically misleading ("richest skew
ever seen"). WR-03 (W4): `moneyness` is hardcoded `null` end-to-end (both adapters' smile reads, the
skew_observations column, and the contract field) — dead nullable schema surface. IN-01: the worker
comments still say "7 queues / 5 crons / 7 handlers" after the 9-queue/6-cron refactor.

Purpose: honest analytics surfaces — null rank when there is no history (matches R2's never-emit-a-
wrong-number spirit) and a populated moneyness (the smile read has spot available via
`leg_observations.underlying_price`), plus correct operator-facing comments.

Output: `percentileRank -> number | null` with null carried through the use-case + contract; a
computed `moneyness = strike / spot` on both adapter smile reads + contract; corrected worker
comments. Sequenced AFTER 06-06 because it edits the same `computeAnalytics.ts`, `leg-observations.ts`
(both adapters), and `smile-source.contract.ts` that 06-06 rewrites.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-derived-analytics/06-GAPS.md
@.planning/phases/06-derived-analytics/06-REVIEW.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/tdd.md
@.planning/phases/06-derived-analytics/06-06-PLAN.md

@packages/core/src/analytics/domain/percentile-rank.ts
@packages/core/src/analytics/domain/percentile-rank.test.ts
@packages/core/src/analytics/domain/percentile-rank.property.test.ts
@packages/core/src/analytics/application/computeAnalytics.ts
@packages/adapters/src/postgres/repos/leg-observations.ts
@packages/adapters/src/memory/leg-observations.ts
@packages/contracts/src/analytics.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: percentileRank empty history -> null; carry null rank through use-case + contract (WR-01 / W1)</name>
  <files>packages/core/src/analytics/domain/percentile-rank.ts, packages/core/src/analytics/domain/percentile-rank.test.ts, packages/core/src/analytics/domain/percentile-rank.property.test.ts, packages/core/src/analytics/application/computeAnalytics.ts, packages/core/src/analytics/application/computeAnalytics.test.ts, packages/contracts/src/analytics.test.ts</files>
  <read_first>
    - packages/core/src/analytics/domain/percentile-rank.ts (n === 0 -> return 100; change to null, return type -> number | null)
    - packages/core/src/analytics/domain/percentile-rank.test.ts (the "returns 100 for empty history" case to flip; the JSDoc sentinel note to rewrite)
    - packages/core/src/analytics/domain/percentile-rank.property.test.ts (bounds/monotonicity properties assume a number; guard the null case)
    - packages/core/src/analytics/application/computeAnalytics.ts (rrRank = percentileRank(...) — already typed number | null at the row; confirm the null flows; note 06-06 already rewrote the surrounding cycle logic)
    - packages/contracts/src/analytics.ts (skewEntry.rrRank is z.number().nullable() — already supports null; add a test asserting null parses)
  </read_first>
  <behavior>
    - percentileRank(value, []) === null (empty history -> no defined rank). Non-empty history is unchanged: inclusive [0,100] percentile.
    - In computeAnalytics, a (underlying, expiration) with a non-null risk_reversal but NO prior history persists rr_rank = null (not 100). Once at least one prior value exists, rr_rank is the inclusive percentile as before.
    - skewResponse parses an entry with rrRank: null successfully (contract already nullable — assert it).
    - Property tests: bounds/monotonicity hold for non-empty history; the empty-history input now yields null and is handled (skip or branch), not a [0,100] assertion failure.
  </behavior>
  <action>
    Change `percentileRank` to return `number | null` and return `null` when `history.length === 0`
    (replace the `return 100`). Rewrite the JSDoc to state empty history -> null (no defined rank,
    forward-only) and drop the now-wrong "empty -> 100 sentinel" note.

    Update `computeAnalytics.ts`: `rrRank` is already typed `number | null` on the row; assign the
    `percentileRank(...)` result directly (it is now null-or-number). Keep the existing rule: when
    risk_reversal itself is null, rr_rank stays null and the group is excluded from history/rank. Do
    NOT re-introduce a numeric sentinel. (06-06 owns the cycle/stamp rewrite — touch ONLY the rank
    assignment + any type flow here; do not re-edit the cycle resolution.)

    Update tests:
    - percentile-rank.test.ts: flip "returns 100 for empty history" to "returns null for empty history"
      (expect percentileRank(x, []) toBeNull()); keep all non-empty cases.
    - percentile-rank.property.test.ts: for properties over possibly-empty history, branch on null
      (e.g. when history is empty, assert the result is null; otherwise assert the [0,100] / monotonic
      invariant). Keep numRuns and the fc.float fround bounds.
    - computeAnalytics.test.ts: add/adjust a case — non-null RR with EMPTY readRrHistory -> rrRow.rrRank
      is null (was the implicit 100 path). Keep the trailing-percentile case (history present -> numeric).
    - analytics.test.ts (contracts): assert skewResponse.parse([{ ..., rrRank: null }]) succeeds.

    Comment-text discipline: keep comments conceptual; do not inline a literal an acceptance
    negative-grep depends on.
  </action>
  <verify>
    <automated>cd /Users/chiragpersonalmac/Desktop/morai-trading-dashboard-and-tools && bun run test --filter @morai/core -- percentile-rank && bun run test --filter @morai/core -- computeAnalytics</automated>
  </verify>
  <done>RED first: the flipped empty-history test fails on the current return-100 code. GREEN after: empty history -> null, first-ever RR persists rr_rank = null, non-empty history unchanged, contract parses null rrRank, property tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Populate moneyness from strike / spot on both smile reads + contract (WR-03 / W4)</name>
  <files>packages/adapters/src/postgres/repos/leg-observations.ts, packages/adapters/src/memory/leg-observations.ts, packages/adapters/src/__contract__/smile-source.contract.ts, packages/contracts/src/analytics.test.ts</files>
  <read_first>
    - packages/adapters/src/postgres/repos/leg-observations.ts (readSmile — selects strike from contracts; add underlying_price to the select; moneyness currently null. 06-06 rewrote this to a bounded read — build on that.)
    - packages/adapters/src/memory/leg-observations.ts (SeededSmileLeg + readSmile — add a spot/underlyingPrice field to the seed and compute moneyness; 06-06 rewrote the read resolution.)
    - packages/adapters/src/__contract__/smile-source.contract.ts (seedLeg input shape + assertions — extend to seed spot and assert computed moneyness)
    - packages/contracts/src/analytics.ts (skewSmileEntry.moneyness is z.number().nullable() — keep nullable; now usually populated)
    - packages/adapters/src/postgres/schema.ts (skew_observations.moneyness is numeric(), nullable — no migration needed; we populate the existing column)
  </read_first>
  <behavior>
    - moneyness is computed as K/S = (strike / 1000) / spot, where strike is the ×1000 int and spot = underlying_price (points). Example: strike 5400000 (= 5400 pts), spot 5500 -> moneyness ~= 0.9818.
    - When spot (underlying_price) is missing/zero/non-finite, moneyness falls back to null (never Infinity/NaN persisted) — keep the column nullable for this edge.
    - Both adapters compute the SAME moneyness for the same (strike, spot) — proven by the shared smile-source contract suite.
    - The computed moneyness flows through skew_observations and skewSmileResponse.moneyness as a real number (asserted in the contract test, not a constant null).
  </behavior>
  <action>
    Decision (per 06-GAPS W4: "populate OR drop; pick one"): POPULATE — the smile read has spot
    available (`leg_observations.underlying_price`), and dropping a SPEC-named field (R1 "strike/
    moneyness") is scope reduction. No migration is needed: the existing `skew_observations.moneyness`
    numeric column stays nullable and we write a computed value into it.

    Postgres (`leg-observations.ts` readSmile): add `legObservations.underlyingPrice` to the select;
    compute `moneyness = (strike / 1000) / spot` where spot = parseFloat(underlyingPrice). Guard:
    when spot is not a finite positive number, set moneyness = null. Map into the SmileQuote.

    Memory twin (`memory/leg-observations.ts`): add a spot field (e.g. `underlyingPrice: string` or
    number) to SeededSmileLeg, and compute moneyness the same way in readSmile with the same guard.
    Keep SeededSmileLeg backward-compatible for existing seeders by treating a missing spot as null
    moneyness.

    Shared contract (`smile-source.contract.ts`): extend the seedLeg input with an optional spot, add
    a case that seeds spot and asserts the computed moneyness (toBeCloseTo) on both adapters, and a
    case where spot is absent/zero -> moneyness null.

    Contract test (`analytics.test.ts`): assert skewSmileResponse.parse([{ ..., moneyness: <number> }])
    succeeds (it already does via nullable; add a populated-value case to document the now-real field).

    Keep skewSmileEntry.moneyness nullable (the spot-missing edge still yields null) — no contract
    type change, just a real value in the common case. Coordinate with 06-06: that plan deliberately
    left moneyness untouched in the bounded-read rewrite so this task owns the moneyness mapping.
  </action>
  <verify>
    <automated>cd /Users/chiragpersonalmac/Desktop/morai-trading-dashboard-and-tools && bun run test --filter @morai/adapters -- smile-source && bun run test --filter @morai/contracts -- analytics</automated>
  </verify>
  <done>RED first: the new "computes moneyness from spot" contract case fails on the current constant-null mapping (both adapters). GREEN after: moneyness = K/S computed on both adapters, null only when spot is absent/invalid, flows through the contract; no migration required.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fix stale worker queue/cron comments (IN-01)</name>
  <files>apps/worker/src/main.ts, apps/worker/src/schedule.ts</files>
  <read_first>
    - apps/worker/src/main.ts (line ~402 "Register all 7 queues, 5 crons, and 7 work handlers")
    - apps/worker/src/schedule.ts (line ~26 "all 7 job queues", line ~41 "all 7 queues" — the console.warn at main.ts ~417 is already correct: 9 queues / 6 jobs)
  </read_first>
  <action>
    Comment-only fix (no behavior, TDD-exempt per tdd.md/architecture-boundaries §3 — composition
    root wiring). Update the three stale comments to reflect the post-compute-analytics topology:
    - apps/worker/src/main.ts ~402: "Register all 9 queues, 6 crons, and 9 work handlers via registerAllJobs".
    - apps/worker/src/schedule.ts ~26: "Handler type shared by all 9 job queues".
    - apps/worker/src/schedule.ts ~41: "typed handler map for all 9 queues".
    Verify the actual counts against registerAllJobs (9 handlers in the map) and the scheduled crons
    (6) before writing the numbers — match the already-correct console.warn at main.ts. Do not change
    any code, only the comment text.
  </action>
  <verify>
    <automated>cd /Users/chiragpersonalmac/Desktop/morai-trading-dashboard-and-tools && grep -rn "7 queues\|5 crons\|all 7" apps/worker/src/main.ts apps/worker/src/schedule.ts | grep -v '^#' | wc -l | tr -d ' '</automated>
  </verify>
  <done>The verify command returns 0 (no remaining "7 queues" / "5 crons" / "all 7" comments); the worker comments read 9 queues / 6 crons / 9 handlers. typecheck + lint stay clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| smile read -> persisted analytics | computed moneyness must not persist Infinity/NaN when spot is absent |
| first-ever RR -> consumer | rank with no history must not read as a real percentile |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-27 | Information disclosure | misleading 100th-percentile rank for first-ever RR (WR-01) | mitigate | percentileRank empty -> null; use-case persists null rank; contract carries null |
| T-06-28 | Tampering | Infinity/NaN moneyness when spot absent (WR-03) | mitigate | finite-positive spot guard -> null fallback; contract field stays nullable |
| T-06-29 | Repudiation | stale operator comments misstate topology (IN-01) | accept-then-fix | comments corrected to 9 queues / 6 crons; no behavioral surface |
</threat_model>

<verification>
- `bun run test` full workspace green incl. flipped percentile-rank, computeAnalytics rank-null, smile-source moneyness, contract cases.
- Testcontainers ran (smile-source Postgres suite executed against real Postgres 16).
- `bun run typecheck && bun run lint` clean (no any/as/!; core imports only @morai/shared; moneyness math confined to adapters).
- No migration generated (moneyness column already nullable numeric — populated, not altered).
- Worker comment grep returns 0 stale matches.
</verification>

<success_criteria>
- percentileRank empty -> null; first-ever RR persists rr_rank = null; carried through skewResponse (WR-01).
- moneyness = K/S populated on both adapter smile reads + contract; null only when spot absent/invalid; no migration (WR-03).
- Worker comments corrected to 9 queues / 6 crons / 9 handlers (IN-01).
</success_criteria>

<output>
Create `.planning/phases/06-derived-analytics/06-08-SUMMARY.md` when done.

## Artifacts this phase produces
- percentileRank null-on-empty + null rank carry-through (use-case + contract).
- Populated moneyness (K/S) on both smile reads + contract, no migration.
- Corrected worker queue/cron comments.
</output>
