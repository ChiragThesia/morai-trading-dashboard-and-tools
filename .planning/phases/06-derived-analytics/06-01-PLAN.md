---
phase: 06-derived-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture/data-model.md
  - docs/architecture/jobs.md
  - docs/architecture/hexagonal-ddd.md
  - docs/architecture/api-design.md
  - packages/adapters/src/postgres/schema.ts
  - packages/contracts/src/analytics.ts
  - packages/contracts/src/index.ts
  - packages/core/src/analytics/application/ports.ts
  - packages/core/src/analytics/index.ts
  - packages/core/src/index.ts
  - packages/core/src/analytics/domain/risk-reversal.test.ts
  - packages/core/src/analytics/domain/percentile-rank.test.ts
  - packages/core/src/analytics/application/computeAnalytics.test.ts
autonomous: true
requirements: [ANLY-01, ANLY-02, ANLY-03, MCP-02]

must_haves:
  truths:
    - "docs/architecture (data-model, jobs, hexagonal-ddd, api-design) describe the three analytics tables, the compute-analytics chain, the analytics context, and the GET routes BEFORE any implementation exists"
    - "The three analytics tables are defined in schema.ts with append-only, time-leading shape and a UNIQUE key per row grain"
    - "ONE shared @morai/contracts analytics schema set exists (skew, risk-reversal, term-structure) that both the HTTP routes and MCP tools will import (MCP-02)"
    - "The analytics bounded context exists in packages/core with an application/ports.ts importing only @morai/shared"
    - "Three failing-test scaffolds exist (risk-reversal domain, percentile-rank domain, computeAnalytics use-case) that later plans turn green"
  artifacts:
    - path: "packages/adapters/src/postgres/schema.ts"
      provides: "skewObservations, riskReversalObservations, termStructureObservations table definitions"
      contains: "skew_observations"
    - path: "packages/contracts/src/analytics.ts"
      provides: "shared skew/risk-reversal/term-structure Zod response schemas (MCP-02)"
      contains: "riskReversalEntry"
    - path: "packages/core/src/analytics/application/ports.ts"
      provides: "analytics driven ports + domain row types"
      contains: "ForWritingSkewObservations"
  key_links:
    - from: "packages/contracts/src/index.ts"
      to: "packages/contracts/src/analytics.ts"
      via: "barrel re-export of the analytics schemas"
      pattern: "analytics"
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/analytics/index.ts"
      via: "core barrel re-exports the analytics context public surface"
      pattern: "analytics"
---

<objective>
Lay the docs-first + contract + schema + context foundation for Phase 6 analytics. This is the
single foundation plan every later slice depends on. It contains NO production logic — only
documentation (mandatory before architecture changes, CLAUDE.md rule #4), the Drizzle table
definitions, the ONE shared Zod contract that guarantees HTTP↔MCP parity (MCP-02), the new
`analytics` bounded-context skeleton (ports + domain row types), and three failing-test
scaffolds that later plans turn green.

Purpose: Without docs-first the architecture-boundaries/docs rules block the phase. Without the
shared contract, the HTTP route and MCP tool could diverge (the R5 prohibition). Without the
table shapes and ports defined up front, the term-structure (06-04) and skew (06-05) slices
cannot be planned as thin parallel slices.

Output: 4 updated architecture docs, 3 new tables in schema.ts, the shared analytics contract,
the analytics context skeleton, and 3 Wave-0 failing-test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-derived-analytics/06-SPEC.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/docs.md
@.claude/rules/typescript.md
</context>

<artifacts_this_phase_produces>
This section is the canonical list for the whole phase. This plan creates the items marked (06-01).

- **New tables** (06-01 schema.ts; 06-02 migration SQL):
  - `skew_observations` — per-strike smile. Grain/UNIQUE: `(underlying, expiration, strike, snapshot_time)`.
  - `risk_reversal_observations` — 25Δ RR scalar + trailing-window rank. Grain/UNIQUE: `(underlying, expiration, snapshot_time)`.
  - `term_structure_observations` — `back_iv − front_iv` per calendar. Grain/UNIQUE: `(calendar_id, snapshot_time)`.
- **Migration file**: `0007_analytics_observations.sql` (06-02).
- **Contract schemas** (06-01): `skewEntry`/`skewResponse`, `riskReversalEntry`/`riskReversalResponse`, `termStructureEntry`/`termStructureResponse` in `packages/contracts/src/analytics.ts`.
- **Domain** (06-03): `interpolateRiskReversal` (linear-in-delta ±0.25), `percentileRank` (trailing-window, inclusive).
- **Ports** (06-01 declares; 06-04/06-05 implement): `ForReadingSmileSource`, `ForReadingCalendarSnapshotsForCycle`, `ForWritingSkewObservations`, `ForWritingRiskReversalObservations`, `ForWritingTermStructureObservations`, `ForReadingRiskReversalHistory`, `ForReadingSkewSeries`, `ForReadingTermStructureSeries`.
- **Use-cases**: `makeComputeAnalyticsUseCase` (06-04 term-structure half, 06-05 skew/RR half), `makeGetSkewUseCase` + `makeGetTermStructureUseCase` (06-04/06-05).
- **Repos + twins + contract tests** (06-04/06-05): postgres + memory adapters for all three tables.
- **Routes** (06-04/06-05): `GET /api/analytics/term-structure`, `GET /api/analytics/skew`.
- **MCP tools** (06-04/06-05): wire `get_term_structure` + `get_skew` from typed-empty stubs to real data.
- **Job** (06-04 creates, 06-05 extends): `compute-analytics`, chain-triggered after `snapshot-calendars`; registered in `schedule.ts`; added to `TRACKED_JOBS` so it surfaces in `GET /api/status`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Docs-first — flesh out the four architecture docs with the locked Phase 6 design</name>
  <read_first>
    - docs/architecture/data-model.md (the "Analytics tables (added when analytics context lands)" stub at the `skew_observations`/`term_structure_observations` line; the `calendar_events`/`broker_tokens` sections are the precedent format for a new-table doc block)
    - docs/architecture/jobs.md (the `compute-analytics` row already exists in the Job Catalog — flesh it out; mirror the `snapshot-calendars` "chain-triggered only (NO cron)" notation)
    - docs/architecture/hexagonal-ddd.md (the `analytics` context row + the `core/src/analytics/**` cross-context-domain enforcement line; the "Per-Context Directory Shape" block is the precedent)
    - docs/architecture/api-design.md (the `GET /api/analytics/term-structure` and `GET /api/analytics/skew` rows already exist in the Route Surface — flesh out their response shape)
    - .planning/phases/06-derived-analytics/06-SPEC.md (locked design — the authority)
  </read_first>
  <action>
    Update all four docs to describe the locked Phase 6 design BEFORE any code (CLAUDE.md rule #4).
    Hemingway style, follow docs.md (≤250 lines/file, one concept, no code dumps).

    data-model.md: Replace the one-line "Analytics tables (added when analytics context lands)"
    stub with three documented table blocks in the same format as the `calendar_events` block.
    For each table give columns, the time-leading composite shape, and the UNIQUE row grain:
    `skew_observations` UNIQUE `(underlying, expiration, strike, snapshot_time)`;
    `risk_reversal_observations` UNIQUE `(underlying, expiration, snapshot_time)`;
    `term_structure_observations` UNIQUE `(calendar_id, snapshot_time)`. State the two
    non-negotiable invariants in prose: (a) `term_structure_observations.value` MUST equal the
    source `calendar_snapshots.term_slope` (no recompute drift); (b) `risk_reversal` is NULL
    (never fabricated) when the smile cannot bracket ±25Δ, and NULL RR is excluded from rank.
    Document the RR definition `risk_reversal = IV(25Δ put) − IV(25Δ call)`, linear-in-delta
    interpolation, and `rr_rank` = trailing-window (252 same-(underlying,expiration) values, or
    all-available-if-shorter) inclusive percentile, forward-only (no backfill).

    jobs.md: Flesh out the existing `compute-analytics` catalog row: chain-triggered AFTER
    `snapshot-calendars` completes (same chain pattern as compute-bsm-greeks→snapshot-calendars),
    NO cron, idempotent (re-run = 0 new rows via the per-grain UNIQUE keys), reads
    `leg_observations` + `calendar_snapshots`, writes the three analytics tables, surfaced in
    `GET /api/status` lastJobRuns.

    hexagonal-ddd.md: Add an `analytics` per-context directory block (domain: risk-reversal
    interpolation + percentile rank; application: computeAnalytics + getSkew + getTermStructure
    use-cases + ports.ts) following the existing journal block format. Confirm the cross-context
    rule: analytics reads journal/market-data DATA via application ports, never imports another
    context's domain.

    api-design.md: Flesh out the two existing analytics route rows with the response shape:
    each returns a JSON array of `{ time, value, … }` (skew: value=risk_reversal plus rr_rank +
    underlying/expiration; term-structure: value=back_iv−front_iv plus calendarId), queryable by
    underlying/expiration (skew) and calendarId (term-structure), and returns a contract-valid
    EMPTY array (not an error) when no data exists. Note MCP-02: the MCP tool shares the same
    contract schema.

    Do NOT restate tsconfig/eslint config contents (docs.md). Reference source files by path.
  </action>
  <verify>
    <automated>grep -q "skew_observations" docs/architecture/data-model.md && grep -q "risk_reversal_observations" docs/architecture/data-model.md && grep -q "term_structure_observations" docs/architecture/data-model.md && grep -q "term_slope" docs/architecture/data-model.md && grep -q "compute-analytics" docs/architecture/jobs.md && grep -Eq "empty (array|result)|EMPTY array" docs/architecture/api-design.md && echo DOCS_OK</automated>
  </verify>
  <done>All four docs describe the three tables (with UNIQUE grains), the chained idempotent compute-analytics job, the analytics context, and the two GET routes (with the empty-array-not-error rule) — and the two invariants (term_slope equality, null-RR-when-unbracketable) appear in prose. No code written yet.</done>
</task>

<task type="auto">
  <name>Task 2: schema.ts — three append-only, time-leading analytics tables with per-grain UNIQUE keys</name>
  <files>packages/adapters/src/postgres/schema.ts</files>
  <read_first>
    - packages/adapters/src/postgres/schema.ts (the `calendarSnapshots` and `legObservations` blocks are the precedent: numeric columns, time-leading composite primaryKey, .enableRLS(); the `calendarEvents` block shows a UNIQUE column via `.unique()`)
  </read_first>
  <action>
    Add three pgTable definitions after the existing tables, mirroring the existing observation-table
    conventions (numeric for decimals, timestamp withTimezone for times, integer for strike ×1000,
    .enableRLS() on every table, time-leading composite PK as the UNIQUE-per-grain key).

    `skewObservations` ("skew_observations"): columns snapshotTime (timestamptz), underlying
    (varchar 16), expiration (date), strike (integer, ×1000 convention like contracts.strike),
    iv (numeric — from leg_observations.bsm_iv), delta (numeric, nullable — interpolation source),
    moneyness (numeric, nullable). Composite PK `(snapshotTime, underlying, expiration, strike)`
    = the per-grain UNIQUE key (re-run is a no-op).

    `riskReversalObservations` ("risk_reversal_observations"): columns snapshotTime (timestamptz),
    underlying (varchar 16), expiration (date), riskReversal (numeric, NULLABLE — null when ±25Δ
    cannot be bracketed; never fabricated), rrRank (numeric, NULLABLE — null when RR is null or
    no history). Composite PK `(snapshotTime, underlying, expiration)`.

    `termStructureObservations` ("term_structure_observations"): columns snapshotTime (timestamptz),
    calendarId (uuid), value (numeric, NOT NULL — equals calendar_snapshots.term_slope), frontIv
    (numeric), backIv (numeric). Composite PK `(snapshotTime, calendarId)`.

    No new enum needed. Do not use `any`/`as`/`!`. Keep the existing `sql` re-export at the bottom.
  </action>
  <verify>
    <automated>cd /Users/chiragpersonalmac/Desktop/morai-trading-dashboard-and-tools && grep -q 'skew_observations' packages/adapters/src/postgres/schema.ts && grep -q 'risk_reversal_observations' packages/adapters/src/postgres/schema.ts && grep -q 'term_structure_observations' packages/adapters/src/postgres/schema.ts && bunx tsc --noEmit -p packages/adapters/tsconfig.json 2>&1 | tail -5; bun run typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>schema.ts defines skewObservations, riskReversalObservations, termStructureObservations, each with a time-leading composite PK that is the per-grain UNIQUE key, nullable riskReversal/rrRank, and .enableRLS(); typecheck passes. NO migration generated here (06-02 owns drizzle generate + live migrate).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Shared analytics Zod contract (MCP-02) + analytics context ports + 3 failing-test scaffolds</name>
  <files>packages/contracts/src/analytics.ts, packages/contracts/src/index.ts, packages/core/src/analytics/application/ports.ts, packages/core/src/analytics/index.ts, packages/core/src/index.ts, packages/core/src/analytics/domain/risk-reversal.test.ts, packages/core/src/analytics/domain/percentile-rank.test.ts, packages/core/src/analytics/application/computeAnalytics.test.ts</files>
  <read_first>
    - packages/contracts/src/journal.ts (snapshotResponse/journalResponse pattern — z.object + z.array, z.infer types)
    - packages/contracts/src/brokerage.ts (multi-schema file with shared sub-schemas + array wrappers — closest analog for skew+RR+term-structure in one file)
    - packages/contracts/src/index.ts (current analytics export line re-exports the typed-empty stubs — replace it)
    - packages/contracts/src/analytics.ts (the CURRENT typed-empty stubs being replaced)
    - packages/core/src/journal/application/ports.ts (port naming `ForVerbingNoun`, StorageError shape, Result return types, row type definitions)
    - packages/core/src/index.ts (barrel re-export pattern from a context index.ts)
  </read_first>
  <behavior>
    Contract (MCP-02): ONE schema set used by both HTTP route and MCP tool. A one-sided field
    rename must fail `bun run typecheck`. Tests assert the schemas parse a valid entry and accept
    an empty array (the no-data case returns a contract-valid empty array, never an error).
    - skewEntry parses `{ time, underlying, expiration, strike, iv, delta, moneyness }`.
    - riskReversalEntry parses `{ time, underlying, expiration, riskReversal (number|null), rrRank (number|null) }`.
    - termStructureEntry parses `{ time, calendarId, value }`.
    - Each response schema is an array (skewResponse/riskReversalResponse/termStructureResponse) and `.parse([])` succeeds.

    Domain scaffolds (RED — these stay failing until 06-03 implements the functions):
    - risk-reversal.test.ts asserts `interpolateRiskReversal` returns a number for a bracketing
      smile and `null` when ±25Δ cannot be bracketed (worked example seeded; full numerics in 06-03).
    - percentile-rank.test.ts asserts `percentileRank(value, history)` returns the inclusive
      trailing-window percentile (worked example seeded; full numerics in 06-03).
    - computeAnalytics.test.ts asserts `makeComputeAnalyticsUseCase` is callable with stub deps
      (full term-structure + skew behavior in 06-04/06-05).
  </behavior>
  <action>
    Replace the typed-empty `analytics.ts` contract with the real shared schemas. Define
    `skewEntry`, `riskReversalEntry`, `termStructureEntry` (z.object with z.string().datetime()
    for `time`; numeric value fields as z.number(); nullable fields via .nullable()) and array
    wrappers `skewResponse`/`riskReversalResponse`/`termStructureResponse` (z.array(...)). Export
    `z.infer` types. These are the SINGLE source both the HTTP route and MCP tool import (MCP-02).
    Update contracts/src/index.ts to export the new schemas + types (replace the typed-empty line).

    Create the analytics context skeleton (architecture-boundaries §2,§5,§7):
    - analytics/application/ports.ts: import ONLY @morai/shared. Declare the row domain types
      (SkewObservationRow, RiskReversalObservationRow, TermStructureObservationRow, SmileQuote,
      CalendarSnapshotForCycle) and the `ForVerbingNoun` ports listed in
      <artifacts_this_phase_produces> (write ports return Result<void, StorageError>; read ports
      return Result<ReadonlyArray<...>, StorageError>). Define StorageError locally OR import the
      journal application port type per the Phase 4 precedent (cross-context APPLICATION port type
      import is allowed; cross-context DOMAIN import is forbidden).
    - analytics/index.ts: public surface re-exporting ports + (future) use-cases.
    - core/src/index.ts: add `export ... from "./analytics/index.ts"` lines.

    Create three RED test files. Import the not-yet-existing functions; each test must FAIL for
    the RIGHT reason (assertion or unresolved import of the function under test), not a syntax
    error. These are turned green by 06-03 (domain) and 06-04/06-05 (use-case).

    No `any`/`as`/`!`. Use `assertDefined` where narrowing is needed.
  </action>
  <verify>
    <automated>cd /Users/chiragpersonalmac/Desktop/morai-trading-dashboard-and-tools && bun run typecheck 2>&1 | tail -8 && echo "--- RED domain tests (expected to fail) ---" && bun run test -- packages/core/src/analytics 2>&1 | tail -20</automated>
  </verify>
  <done>analytics.ts exports skewEntry/riskReversalEntry/termStructureEntry + array responses (z.infer types), re-exported from the contracts barrel; the analytics context (ports.ts importing only @morai/shared + index.ts) is re-exported from the core barrel; `bun run typecheck` is clean; the three new test files RUN and FAIL for the right reason (functions not yet implemented). The contract typed-empty stubs are gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/code authorship | Internal authoring only; no external input crosses here this plan |
| contracts schema → consumers | The analytics Zod schema is the single trust gate for analytics payloads at both HTTP and MCP edges (defined here, enforced in 06-04/06-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | analytics schema divergence (HTTP vs MCP) | mitigate | ONE shared schema in `packages/contracts/src/analytics.ts`; a one-sided change fails `bun run typecheck` (R5 prohibition); enforced when consumers wire in 06-04/06-05 |
| T-06-02 | Information disclosure | over-wide table columns leaking unintended fields | accept | Tables hold only derived public market metrics (IV, delta, RR, term slope); no PII, no secrets; single-user v1 |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan or phase — all deps (zod, drizzle, vitest, fast-check) already present; no legitimacy gate required |
</threat_model>

<verification>
- `bun run typecheck` clean across the workspace.
- `bun run lint` clean (no boundary violation: analytics/application/ports.ts imports only @morai/shared; contracts imports only zod + shared).
- The three new analytics test files execute and fail for the right reason (RED) — they are NOT skipped.
- Docs grep gate passes (all three table names + compute-analytics + empty-array rule present).
- No migration generated (06-02 owns it); no production logic written (functions still RED).
</verification>

<success_criteria>
- Docs-first complete: data-model, jobs, hexagonal-ddd, api-design all describe the locked design.
- schema.ts has the three tables with per-grain UNIQUE keys; typecheck passes.
- The shared analytics contract exists and is the single source for HTTP + MCP (MCP-02).
- The analytics bounded context skeleton exists with hexagon-pure ports.
- Three RED test scaffolds are in place for later plans to turn green.
</success_criteria>

<output>
Create `.planning/phases/06-derived-analytics/06-01-SUMMARY.md` when done.
</output>
