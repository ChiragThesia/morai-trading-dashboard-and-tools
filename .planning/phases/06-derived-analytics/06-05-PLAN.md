---
phase: 06-derived-analytics
plan: 05
type: execute
wave: 5
depends_on: ["06-03", "06-04"]
files_modified:
  - packages/adapters/src/postgres/repos/skew-observations.ts
  - packages/adapters/src/memory/skew-observations.ts
  - packages/adapters/src/__contract__/skew-observations.contract.ts
  - packages/adapters/src/memory/skew-observations.contract.test.ts
  - packages/adapters/src/postgres/repos/skew-observations.contract.test.ts
  - packages/adapters/src/postgres/repos/risk-reversal-observations.ts
  - packages/adapters/src/memory/risk-reversal-observations.ts
  - packages/adapters/src/__contract__/risk-reversal-observations.contract.ts
  - packages/adapters/src/memory/risk-reversal-observations.contract.test.ts
  - packages/adapters/src/postgres/repos/risk-reversal-observations.contract.test.ts
  - packages/adapters/src/postgres/repos/leg-observations.ts
  - packages/adapters/src/memory/leg-observations.ts
  - packages/adapters/src/__contract__/leg-observations.contract.ts
  - packages/adapters/src/index.ts
  - packages/core/src/analytics/application/computeAnalytics.ts
  - packages/core/src/analytics/application/computeAnalytics.test.ts
  - packages/core/src/analytics/application/getSkew.ts
  - packages/core/src/analytics/application/getSkew.test.ts
  - packages/core/src/analytics/index.ts
  - packages/core/src/index.ts
  - apps/worker/src/main.ts
  - apps/server/src/adapters/http/analytics.routes.ts
  - apps/server/src/adapters/http/analytics.routes.test.ts
  - apps/server/src/adapters/mcp/tools.ts
  - apps/server/src/adapters/mcp/server.ts
  - apps/server/src/main.ts
autonomous: true
requirements: [ANLY-01, ANLY-03, MCP-02]

must_haves:
  truths:
    - "After a snapshot cycle, skew_observations gains one row per (underlying, expiration, strike) in leg_observations for that snapshot time (full smile); a duplicate run adds 0 rows"
    - "risk_reversal_observations gains one row per (underlying, expiration, snapshot_time) with risk_reversal = IV(25Δ put) − IV(25Δ call) interpolated from the smile, or null when ±25Δ cannot be bracketed"
    - "rr_rank is the trailing-window (≤252 prior same-(underlying,expiration) values) inclusive percentile of risk_reversal; null risk_reversal is excluded from rank and yields null rr_rank"
    - "GET /api/analytics/skew returns a contract-valid JSON array of {time, value, …} (value=risk_reversal, with rr_rank + underlying/expiration); empty array (not error) when no data"
    - "MCP get_skew returns the same series as the HTTP route over the shared contract (MCP-02); a one-sided schema change fails bun run typecheck"
  artifacts:
    - path: "packages/adapters/src/postgres/repos/skew-observations.ts"
      provides: "per-strike smile repo (idempotent on the per-grain PK)"
      contains: "onConflictDoNothing"
    - path: "packages/core/src/analytics/application/getSkew.ts"
      provides: "getSkew read use-case over the risk-reversal series"
      exports: ["makeGetSkewUseCase"]
    - path: "apps/server/src/adapters/http/analytics.routes.ts"
      provides: "GET /api/analytics/skew added alongside term-structure"
      contains: "/skew"
  key_links:
    - from: "packages/core/src/analytics/application/computeAnalytics.ts"
      to: "packages/core/src/analytics/domain/risk-reversal.ts"
      via: "interpolateRiskReversal called per (underlying, expiration) smile"
      pattern: "interpolateRiskReversal"
    - from: "packages/core/src/analytics/application/computeAnalytics.ts"
      to: "packages/core/src/analytics/domain/percentile-rank.ts"
      via: "percentileRank over the trailing RR history"
      pattern: "percentileRank"
    - from: "apps/server/src/adapters/http/analytics.routes.ts"
      to: "apps/server/src/adapters/mcp/tools.ts"
      via: "both import skewResponse from @morai/contracts (MCP-02)"
      pattern: "skewResponse"
---

<objective>
The second analytics slice, end-to-end: skew. It writes the full per-strike smile
(`skew_observations`, ANLY-01 R1), derives the headline 25Δ risk-reversal scalar + trailing-window
rank (`risk_reversal_observations`, R2) using the 06-03 domain functions, extends the existing
compute-analytics job/use-case from 06-04, and exposes the series via GET /api/analytics/skew +
MCP get_skew over the shared contract (MCP-02). After this plan a user can read the skew/risk-
reversal series by API and Claude Code — the second new user-facing capability.

MVP slice check: after 06-04 a user could read term-structure but not skew; after THIS plan a user
can run a cycle and read `GET /api/analytics/skew` / MCP `get_skew` with real risk-reversal + rank
data. A true vertical slice on top of the shared job.

Purpose: ANLY-01 (skew smile + risk-reversal + rank) and its ANLY-03/MCP-02 read surface. The
null-RR-when-unbracketable and exclude-null-from-rank rules (R2 prohibition: never fabricate) are
enforced here at the use-case level on top of the 06-03 domain guarantees.

Output: skew + risk-reversal repos/twins/contract tests, a leg_observations smile-source read
port, the skew/RR half of compute-analytics, getSkew use-case, GET /api/analytics/skew + MCP
get_skew.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-derived-analytics/06-SPEC.md
@.claude/rules/architecture-boundaries.md
@.claude/rules/tdd.md
@packages/core/src/analytics/application/ports.ts
@packages/core/src/analytics/application/computeAnalytics.ts
@packages/core/src/analytics/domain/risk-reversal.ts
@packages/core/src/analytics/domain/percentile-rank.ts
@packages/contracts/src/analytics.ts
@packages/adapters/src/postgres/schema.ts
@packages/adapters/src/postgres/repos/leg-observations.ts
@packages/adapters/src/postgres/repos/calendar-events.ts
@packages/adapters/src/memory/calendar-events.ts
@apps/server/src/adapters/http/analytics.routes.ts
@apps/server/src/adapters/mcp/tools.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: skew + risk-reversal repos + twins + contract tests; leg_observations smile-source read port</name>
  <files>packages/adapters/src/postgres/repos/skew-observations.ts, packages/adapters/src/memory/skew-observations.ts, packages/adapters/src/__contract__/skew-observations.contract.ts, packages/adapters/src/memory/skew-observations.contract.test.ts, packages/adapters/src/postgres/repos/skew-observations.contract.test.ts, packages/adapters/src/postgres/repos/risk-reversal-observations.ts, packages/adapters/src/memory/risk-reversal-observations.ts, packages/adapters/src/__contract__/risk-reversal-observations.contract.ts, packages/adapters/src/memory/risk-reversal-observations.contract.test.ts, packages/adapters/src/postgres/repos/risk-reversal-observations.contract.test.ts, packages/adapters/src/postgres/repos/leg-observations.ts, packages/adapters/src/memory/leg-observations.ts, packages/adapters/src/__contract__/leg-observations.contract.ts, packages/adapters/src/index.ts</files>
  <read_first>
    - packages/adapters/src/postgres/repos/calendar-events.ts (idempotent onConflictDoNothing + numeric string boundary precedent)
    - packages/adapters/src/postgres/repos/term-structure-observations.ts (the analog written in 06-04 — mirror its repo+twin+contract structure)
    - packages/adapters/src/__contract__/leg-observations.contract.ts (existing leg-observations contract helper — EXTEND it for the new smile-source read method; do not duplicate)
    - packages/adapters/src/postgres/repos/leg-observations.ts (add a read method returning the per-strike smile {underlying, expiration, strike, bsm_iv→iv, bsm_delta→delta} for a snapshot time)
    - packages/core/src/analytics/application/ports.ts (ForReadingSmileSource, ForWritingSkewObservations, ForWritingRiskReversalObservations, ForReadingRiskReversalHistory, ForReadingSkewSeries, SmileQuote, SkewObservationRow, RiskReversalObservationRow)
  </read_first>
  <behavior>
    skew-observations repo (per-strike smile, PK (snapshot_time, underlying, expiration, strike)):
    - storeSkewObservations(rows): onConflictDoNothing → write N for a fresh snapshot time; re-write
      adds 0 (idempotency — testcontainer count assertion).
    - readSkewSeries({underlying?, expiration?}): ordered by snapshot_time ASC (smile detail —
      optional; the headline read surface uses risk-reversal).

    risk-reversal repo (PK (snapshot_time, underlying, expiration)):
    - storeRiskReversalObservations(rows): onConflictDoNothing; nullable risk_reversal + rr_rank
      round-trip (null stays null through the repo — never coerced to 0).
    - readRiskReversalSeries({underlying?, expiration?}): ordered by snapshot_time ASC; empty array
      when none.
    - readRiskReversalHistory(underlying, expiration, beforeTime, limit=252): the trailing window
      of prior NON-NULL risk_reversal values for that (underlying, expiration), most-recent-first
      or time-ordered (document which), used by computeAnalytics for rank. NULL RR rows excluded.

    leg_observations smile-source read (ForReadingSmileSource): given a snapshot time, return the
    per-(underlying, expiration, strike) smile points using bsm_iv as iv and bsm_delta as delta,
    EXCLUDING NaN-stamped rows (bsm_iv = 'NaN'). Extend the existing leg-observations contract
    helper + twin to cover this method.

    All repos: both memory twin (Map keyed on the grain) and Postgres (testcontainers) pass the
    SAME shared contract suite.
  </behavior>
  <action>
    Implement makePostgres/MemorySkewObservationsRepo and makePostgres/MemoryRiskReversalObservationsRepo
    mirroring the 06-04 term-structure repos (onConflictDoNothing, numeric-string boundary, nullable
    round-trip for RR/rank). Add the ForReadingSmileSource read method to the leg-observations repo
    + twin and extend its existing __contract__ helper (one suite, both adapters). Write shared
    contract helpers for the two new tables and run them from memory + postgres contract.test.ts.
    Export the new repos from packages/adapters/src/index.ts. RED before GREEN; SQL never mocked.
    No `any`/`as`/`!`; nullable RR/rank must survive the round-trip as null (assert in the contract test).
  </action>
  <verify>
    <automated>cd /Users/chiragpersonalmac/Desktop/morai-trading-dashboard-and-tools && bun run typecheck 2>&1 | tail -5 && bun run test -- packages/adapters/src/memory/skew-observations.contract.test.ts packages/adapters/src/memory/risk-reversal-observations.contract.test.ts 2>&1 | tail -18</automated>
  </verify>
  <done>skew + risk-reversal repos pass the shared contract suite on both adapters; idempotent writes (re-run = 0 rows); nullable risk_reversal/rr_rank round-trip as null; the leg_observations smile-source read returns NaN-free per-strike smile points. All exported from the adapters barrel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: extend compute-analytics with the smile + risk-reversal + rank write; getSkew use-case</name>
  <files>packages/core/src/analytics/application/computeAnalytics.ts, packages/core/src/analytics/application/computeAnalytics.test.ts, packages/core/src/analytics/application/getSkew.ts, packages/core/src/analytics/application/getSkew.test.ts, packages/core/src/analytics/index.ts, packages/core/src/index.ts</files>
  <read_first>
    - packages/core/src/analytics/application/computeAnalytics.ts (the term-structure half from 06-04 — EXTEND the same use-case; add deps, keep term-structure behavior intact)
    - packages/core/src/analytics/domain/risk-reversal.ts (interpolateRiskReversal — null when unbracketable)
    - packages/core/src/analytics/domain/percentile-rank.ts (percentileRank — inclusive, window-agnostic)
    - packages/core/src/journal/application/getJournal.ts (thin read-forwarder precedent for getSkew)
    - packages/core/src/analytics/application/ports.ts (ForReadingSmileSource, ForWritingSkewObservations, ForWritingRiskReversalObservations, ForReadingRiskReversalHistory, ForReadingSkewSeries)
  </read_first>
  <behavior>
    Extend makeComputeAnalyticsUseCase (do NOT regress term-structure) with the skew/RR half:
    1. Read the per-strike smile for the cycle's snapshot time (ForReadingSmileSource).
    2. Write the full smile to skew_observations — one row per (underlying, expiration, strike)
       (ANLY-01 R1). N strikes across M expiries → exactly N×M smile rows; idempotent.
    3. Group smile points by (underlying, expiration). For each group:
       a. risk_reversal = interpolateRiskReversal(groupPoints) → number | null (null when ±25Δ
          cannot be bracketed — NEVER fabricated; R2 prohibition).
       b. If risk_reversal is NOT null: history = readRiskReversalHistory(underlying, expiration,
          snapshotTime, 252) (prior NON-NULL RR values, trailing window); rr_rank =
          percentileRank(risk_reversal, history). If risk_reversal IS null: rr_rank = null AND the
          value is excluded from any future rank (it is not written as a rankable number).
       c. Write one risk_reversal_observations row {snapshotTime, underlying, expiration,
          risk_reversal, rr_rank} (idempotent on the grain).
    4. Returns Result<void, StorageError>; propagates errors.

    Tests (in-memory twins + the real 06-03 domain):
    - Smile fixture with N strikes × M expiries at one snapshot time → exactly N×M skew rows;
      re-run → 0 new rows (idempotent).
    - The worked-example smile (from 06-03: rr=0.06) → a risk_reversal_observations row with
      risk_reversal ≈ 0.06.
    - An unbracketable smile (puts shallower than 25Δ) → risk_reversal = null AND rr_rank = null;
      assert NO fabricated number.
    - Seeded RR history fixture → rr_rank equals the expected trailing-window inclusive percentile.
    - term-structure behavior from 06-04 still passes (no regression).

    makeGetSkewUseCase: thin forwarder over ForReadingRiskReversalSeries (the headline read; value
    = risk_reversal, plus rr_rank + underlying/expiration). ok([]) when no data. Optional
    underlying/expiration filter.
  </behavior>
  <action>
    RED: extend computeAnalytics.test.ts with the smile-count, worked-RR, null-RR, and rank
    assertions; add getSkew.test.ts. GREEN: extend the use-case deps (smile read + skew write + RR
    write + RR history read) and implement the grouping/interpolation/rank pipeline using the
    06-03 domain functions; implement getSkew as a thin forwarder. REFACTOR green. Re-export the
    new use-case + driver-port type from analytics/index.ts and core/src/index.ts. No `any`/`as`/`!`;
    injected now(); the use-case calls the domain — it does NOT reimplement interpolation/rank.
  </action>
  <verify>
    <automated>cd /Users/chiragpersonalmac/Desktop/morai-trading-dashboard-and-tools && bun run test -- packages/core/src/analytics/application 2>&1 | tail -18 && bun run typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>computeAnalytics writes the full N×M smile (idempotent), the worked-example RR ≈ 0.06, null RR (and null rank) when unbracketable with no fabricated number, and rr_rank = the expected trailing-window inclusive percentile; term-structure unchanged; getSkew forwards the read; all green and exported.</done>
</task>

<task type="auto">
  <name>Task 3: wire skew into the compute-analytics job + GET /api/analytics/skew + MCP get_skew</name>
  <files>apps/worker/src/main.ts, apps/server/src/adapters/http/analytics.routes.ts, apps/server/src/adapters/http/analytics.routes.test.ts, apps/server/src/adapters/mcp/tools.ts, apps/server/src/adapters/mcp/server.ts, apps/server/src/main.ts</files>
  <read_first>
    - apps/worker/src/main.ts (the compute-analytics composition from 06-04 — add the skew/RR repos + the extended use-case deps; the handler/queue already exist)
    - apps/server/src/adapters/http/analytics.routes.ts (the term-structure route from 06-04 — ADD a GET /analytics/skew route in the same router)
    - apps/server/src/adapters/mcp/tools.ts (registerGetSkewTool is a typed-empty STUB — rewire to call getSkew over skewResponse; precedent: the get_term_structure rewire in 06-04 + registerGetJournalTool)
    - apps/server/src/adapters/mcp/server.ts (thread getSkew use-case into makeMcpRouter, like getJournal/getTermStructure)
    - apps/server/src/main.ts (route mounting + makeMcpRouter args)
    - packages/contracts/src/analytics.ts (skewResponse — the SHARED schema both surfaces import; MCP-02)
  </read_first>
  <action>
    Worker main.ts: build the skew + risk-reversal repos and the leg-observations smile-source
    read; extend the computeAnalytics use-case wiring with those deps (the queue, handler, chain
    trigger, RTH gate, and TRACKED_JOBS entry already exist from 06-04 — do not duplicate them).

    Server:
    - analytics.routes.ts: add GET /analytics/skew → call getSkew use-case (optional
      ?underlying/?expiration) → parse through skewResponse → respond; empty array (not error) when
      no data; flat {error:"internal"} on storage error; serialise Date `time` to ISO before parse.
    - tools.ts: replace registerGetSkewTool's typed-empty body with a real getSkew call parsing
      through the SAME skewResponse schema (MCP-02); contract-valid empty array (never error) when
      no data. Thread getSkew through server.ts makeMcpRouter.
    - main.ts: pass getSkew into makeMcpRouter (route already mounted via analytics.routes from 06-04).
    - analytics.routes.test.ts: extend with a /skew case — skewResponse-valid array with ≥1 entry
      when data present, valid EMPTY array when none. Confirm a one-sided rename of a skewResponse
      field would fail typecheck (MCP-02 invariant — both surfaces import the same inferred type).
  </action>
  <verify>
    <automated>cd /Users/chiragpersonalmac/Desktop/morai-trading-dashboard-and-tools && grep -q '/skew' apps/server/src/adapters/http/analytics.routes.ts && grep -q 'skewResponse' apps/server/src/adapters/mcp/tools.ts && bun run typecheck 2>&1 | tail -5 && bun run lint 2>&1 | tail -5 && bun run test -- apps/server/src/adapters/http/analytics.routes.test.ts 2>&1 | tail -14</automated>
  </verify>
  <done>GET /api/analytics/skew + MCP get_skew both return the same skewResponse-valid series (value=risk_reversal + rr_rank + underlying/expiration), empty array (not error) when no data, over the one shared schema (MCP-02); the compute-analytics job now writes skew + RR alongside term-structure; typecheck + lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /api/analytics/skew | Untrusted query params (underlying, expiration) at the HTTP edge |
| Claude Code → MCP get_skew | Untrusted tool args at the MCP edge |
| leg_observations smile → RR domain → DB | Incomplete smiles (sparse deltas) drive the RR computation written to Postgres |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Tampering | fabricated risk_reversal when ±25Δ cannot be interpolated (R2 prohibition) | mitigate | use-case writes null (from interpolateRiskReversal) and null rr_rank; excluded from history/rank; example + property tests assert no fabricated number |
| T-06-12 | Tampering | HTTP/MCP skew schema divergence (R5 prohibition) | mitigate | both import the single skewResponse from contracts; one-sided rename fails `bun run typecheck` (asserted) |
| T-06-13 | Information disclosure | DB internals leaked on route error | mitigate | flat `{error:"internal"}` body; no stack/SQL exposed |
| T-06-14 | Tampering | malformed underlying/expiration/tool args | mitigate | Zod parse at route + MCP boundary (safeParse, get_journal precedent); invalid → typed/empty response, never throw |
| T-06-15 | Denial of service | unbounded skew series read | mitigate | trailing-window cap (≤252) on rank history; series reads filterable by underlying/expiration; single-user v1 |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No installs — all deps present |
</threat_model>

<verification>
- skew + risk-reversal repos + twins pass the shared contract suite; idempotent (re-run = 0 rows); nullable RR/rank round-trip as null.
- computeAnalytics writes full N×M smile + RR + rank; worked example RR ≈ 0.06; null RR + null rank when unbracketable (no fabricated number); rank = expected trailing percentile.
- GET /api/analytics/skew + MCP get_skew return the same skewResponse-valid series; empty array (not error) on no data; one-sided schema change fails typecheck (MCP-02).
- term-structure behavior unchanged (no regression).
- `bun run typecheck` + `bun run lint` clean.
</verification>

<success_criteria>
- ANLY-01 satisfied: per-strike smile + 25Δ risk-reversal + trailing-window rank, idempotent, null-safe.
- ANLY-03/MCP-02 read surface live for skew over one shared schema.
- A user can run a cycle and read a real skew/risk-reversal series via API and Claude Code.
- All four phase SC (SC1-SC4) are now satisfiable end-to-end across 06-04 + 06-05.
</success_criteria>

<output>
Create `.planning/phases/06-derived-analytics/06-05-SUMMARY.md` when done.
</output>
