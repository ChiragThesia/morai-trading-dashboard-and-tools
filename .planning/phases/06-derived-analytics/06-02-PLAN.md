---
phase: 06-derived-analytics
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - packages/adapters/src/postgres/migrations/0007_analytics_observations.sql
  - packages/adapters/src/postgres/migrations/meta/_journal.json
autonomous: false
requirements: [ANLY-01, ANLY-02, ANLY-03]

must_haves:
  truths:
    - "drizzle-kit generates exactly one new migration for the three analytics tables"
    - "The migration is applied to the live Supabase DB and a second run is a no-op (idempotent)"
    - "All three analytics tables exist in the live DB with their per-grain UNIQUE/PK keys"
  artifacts:
    - path: "packages/adapters/src/postgres/migrations/0007_analytics_observations.sql"
      provides: "CREATE TABLE for the three analytics tables"
      contains: "CREATE TABLE"
  key_links:
    - from: "packages/adapters/src/postgres/migrations/0007_analytics_observations.sql"
      to: "packages/adapters/src/postgres/schema.ts"
      via: "drizzle-kit generate diffs schema.ts → emits SQL"
      pattern: "skew_observations"
---

<objective>
Generate and apply the analytics-tables migration. This is a [BLOCKING] live-DB plan, mirroring
the Phase 3 (03-02) and Phase 5 (05-02) migration precedent: schema.ts is authored in the prior
plan, this plan runs `drizzle-kit generate` then applies the SQL to the live Supabase DB so the
later repo/contract-test slices (06-04, 06-05) have real tables to write against.

Purpose: testcontainer contract tests run the migrations to build their schema; the live DB must
also carry the tables for the forward-only compute-analytics job to write rows from the first
RTH cycle after deploy. No analytics data exists until the tables exist.

Output: `0007_analytics_observations.sql` + journal entry, applied to live Supabase.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-derived-analytics/06-SPEC.md
@packages/adapters/src/postgres/schema.ts
@docs/architecture/data-model.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: drizzle-kit generate the analytics migration</name>
  <files>packages/adapters/src/postgres/migrations/0007_analytics_observations.sql, packages/adapters/src/postgres/migrations/meta/_journal.json</files>
  <read_first>
    - packages/adapters/src/postgres/migrations/0004_calendar_events.sql (precedent for a multi-table CREATE TABLE migration)
    - packages/adapters/src/postgres/schema.ts (the three new tables — the generate source)
    - STATE.md decisions: prior migrations were renamed from drizzle-kit's random slug to a descriptive name; the meta/_journal.json tag MUST be updated to match (Phase 4 P01 / Phase 5 P02 precedent)
  </read_first>
  <action>
    Run the project's drizzle-kit generate command (the same one used in prior migration plans —
    check package.json scripts; do NOT invent flags). It will emit a new migration with a random
    slug. Rename the generated `.sql` file to `0007_analytics_observations.sql` AND update the tag
    in `migrations/meta/_journal.json` to match the renamed file (the runner reads the journal —
    a mismatched tag breaks idempotent replay). Confirm the generated SQL contains three
    `CREATE TABLE` statements (skew_observations, risk_reversal_observations,
    term_structure_observations) with the composite PRIMARY KEY per grain and the nullable
    columns (riskReversal/rrRank/delta/moneyness) as authored in schema.ts. Do NOT hand-edit the
    SQL body beyond the rename — if the SQL is wrong, fix schema.ts (06-01) and regenerate.
  </action>
  <verify>
    <automated>cd /Users/chiragpersonalmac/Desktop/morai-trading-dashboard-and-tools && test -f packages/adapters/src/postgres/migrations/0007_analytics_observations.sql && grep -c 'CREATE TABLE' packages/adapters/src/postgres/migrations/0007_analytics_observations.sql && grep -q '0007_analytics_observations' packages/adapters/src/postgres/migrations/meta/_journal.json && grep -q 'skew_observations' packages/adapters/src/postgres/migrations/0007_analytics_observations.sql && grep -q 'risk_reversal_observations' packages/adapters/src/postgres/migrations/0007_analytics_observations.sql && grep -q 'term_structure_observations' packages/adapters/src/postgres/migrations/0007_analytics_observations.sql</automated>
  </verify>
  <done>0007_analytics_observations.sql exists with three CREATE TABLE statements; meta/_journal.json references the renamed tag; the SQL matches schema.ts (no hand-edits beyond rename).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Apply the analytics migration to the live Supabase database</name>
  <action>
    Apply 0007_analytics_observations.sql to the live Supabase Postgres DB via `bun run migrate`,
    then prove idempotency with a second run, then confirm the three tables exist. This is a
    blocking human gate — DDL on production. Follow the how-to-verify steps below exactly; do not
    proceed until the operator confirms all checks pass.
  </action>
  <what-built>The 0007_analytics_observations.sql migration, ready to apply to the live Supabase Postgres DB.</what-built>
  <how-to-verify>
    This applies DDL to the live production database — a blocking human gate (same as the Phase 3
    03-02 and Phase 4/5 live-migration checkpoints; DDL is migration-safe only over the Supabase
    SESSION pooler port 5432 / direct URL, max:1 — NOT the transaction pooler 6543, per STATE
    decision).

    1. Confirm DATABASE_URL points at the intended Supabase project (session pooler / direct).
    2. Run the project migrate command: `bun run migrate`.
    3. Expected: the runner applies 0007 and reports success (prior migrations already applied = skipped).
    4. Run `bun run migrate` a SECOND time. Expected: no-op, no errors (DATA-02 idempotency).
    5. Verify the three tables exist (psql `\dt` or Supabase table editor): skew_observations,
       risk_reversal_observations, term_structure_observations.
  </how-to-verify>
  <resume-signal>Type "approved" once the three tables exist live and the second migrate run was a clean no-op, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration runner → live Postgres | DDL crosses into the production database; only the operator-approved connection string is used |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | applying DDL to the wrong / transaction-pooler connection | mitigate | Blocking human checkpoint confirms session-pooler/direct URL (max:1) before apply; matches prior live-migration gate |
| T-06-04 | Denial of service | non-idempotent migration breaking boot replay | mitigate | Second `bun run migrate` proven a clean no-op (DATA-02); drizzle journal tag matches renamed file |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs — drizzle-kit already a project dependency |
</threat_model>

<verification>
- `0007_analytics_observations.sql` generated, renamed, journal tag updated.
- `bun run migrate` applies it; a second run is a clean no-op.
- The three tables exist in the live DB (human-verified) with composite PKs.
</verification>

<success_criteria>
- Migration generated from schema.ts (not hand-written) and applied live.
- Idempotent replay proven (second run = no-op).
- Tables ready for the 06-04 / 06-05 repo + contract-test slices.
</success_criteria>

<output>
Create `.planning/phases/06-derived-analytics/06-02-SUMMARY.md` when done.
</output>
