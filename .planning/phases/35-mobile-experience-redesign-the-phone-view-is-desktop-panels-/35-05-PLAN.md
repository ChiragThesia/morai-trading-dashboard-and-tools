---
phase: 35-mobile-experience-redesign
plan: 05
type: execute
wave: 2
depends_on: [35-01]
files_modified:
  - apps/web/src/screens/Analyzer.tsx
  - apps/web/src/screens/Analyzer.test.tsx
  - apps/web/src/screens/Journal.tsx
  - apps/web/src/screens/Journal.test.tsx
autonomous: true
requirements: [MOBILE-02]
user_setup: []
must_haves:
  truths:
    - "Below lg Analyzer stacks in the visual order rail → scorecard → chart+term → right-column, with no horizontal scroll; at lg it is the exact two-level layout of today (scorecard banner full-width above the 300px/1fr/330px grid) with byte-identical DOM"
    - "Analyzer's inner rail/chart/right container is display:contents below lg (promoting its three children to flex-items of the outer column) and lg:grid at desktop — no JSX element is reordered, only order-* (paint) and contents (box-flatten) change"
    - "Below lg Journal flows as a normal single-column document (Trades → Lifecycle → reactive rail) with no clipping; at lg it is the 250px/1fr/290px three-pane layout with independent per-column scroll inside a fixed-height viewport, exactly as today"
  artifacts:
    - apps/web/src/screens/Analyzer.tsx
    - apps/web/src/screens/Journal.tsx
  key_links:
    - "Both screens' chart chrome (PayoffControls) is already fixed by plan 35-01's shared edit — this plan only restructures the page grids and adds full-bleed to Analyzer's payoff Panel (D-09/D-10)"
    - "Journal's outer overflow-hidden + fixed grid is the exact Pitfall-2 clip trap (content clipped invisible on a phone); gating it behind lg: is what makes the two right panes reachable on mobile"
    - "STRAY FILE: apps/web/src/screens/Analyzer 2.tsx (a space in the name) is untracked and must NEVER be touched or imported — the real file is Analyzer.tsx"
---

<objective>
Fix the two screens that have ZERO responsive handling today (RESEARCH Pitfall 2): (1) `Analyzer`'s
inline-pixel `grid-template-columns: 300px 1fr 330px` that forces horizontal overflow / column-crush on
a phone, and (2) `Journal`'s identical fixed grid sitting inside `overflow-hidden`, which actively
CLIPS its two right columns into invisibility below `lg`. Port Overview's proven responsive convention
(D-09): Journal becomes `flex flex-col … lg:grid lg:grid-cols-[…]`; Analyzer uses `display: contents` +
`order-*` on its inner grid to reach across its two-level nesting and produce the mobile visual order
rail → scorecard → chart+term → right — all while keeping the DOM byte-identical and desktop
pixel-identical.

Purpose: these are the "worse than stacked" bugs (CONTEXT framing understated them). The fix is
reuse-not-invention — one responsive convention across all three screens — and it needs the shared
`PayoffControls` chrome fix from 35-01 already in place so both screens' chart strips are consistent.

Output: Analyzer and Journal usable at 390px (no h-scroll, no clipped panes) and byte-identical at
`≥1024px`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-mobile-experience-redesign-the-phone-view-is-desktop-panels-/35-CONTEXT.md
@.planning/phases/35-mobile-experience-redesign-the-phone-view-is-desktop-panels-/35-RESEARCH.md
@.planning/phases/35-mobile-experience-redesign-the-phone-view-is-desktop-panels-/35-UI-SPEC.md
@apps/web/src/screens/Analyzer.tsx
@apps/web/src/screens/Analyzer.test.tsx
@apps/web/src/screens/Journal.tsx
@apps/web/src/screens/Journal.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Analyzer — display:contents + order-* mobile reflow (DOM byte-identical) (RED→GREEN)</name>
  <files>apps/web/src/screens/Analyzer.tsx, apps/web/src/screens/Analyzer.test.tsx</files>
  <read_first>
    - 35-UI-SPEC.md "### 4. Analyzer — mobile stack order" — the exact target: wrap
      `ScoringMethodologyPanel` in `<div className="order-2 lg:order-none">`; change the inner grid
      container from the inline `style={{ gridTemplateColumns: "300px 1fr 330px" }}` grid to `<div
      className="contents lg:grid lg:grid-cols-[300px_minmax(0,1fr)_330px] lg:gap-4">`; wrap `railBody` in
      `order-1 lg:order-none`, the center chart+term column in `order-3 lg:order-none flex min-w-0
      flex-col gap-3`, and `RightColumn` in `order-4 lg:order-none`. The mechanism note: `contents`
      flattens the inner box below lg so rail/center/right become flex-items of the SAME outer column
      `scorecard` sits in, letting `order-*` interleave scorecard between them without moving any JSX.
    - 35-UI-SPEC.md "## Accessibility Notes → Keyboard tab order (Analyzer)" — DOM/tab order stays
      scorecard, rail, chart+term, right at every breakpoint; ScoringMethodologyPanel has zero focusable
      elements so the visual/DOM mismatch displaces zero tab stops (documented, same Pattern-3 class as
      Overview's MarketRail).
    - apps/web/src/screens/Analyzer.tsx return body (the outer `flex flex-col gap-4 bg-bg p-3`,
      `ScoringMethodologyPanel`, then the `<div className="grid gap-4" style={{ gridTemplateColumns:
      "300px 1fr 330px" }}>` with children `railBody` / the center Panel column / `RightColumn`). The
      center Panel's `PayoffControls` is already a ChipRail via plan 35-01 — do not re-wrap it; only add
      the full-bleed `-mx-3 lg:mx-0` to the payoff chart wrapper per D-10, mirroring Overview.
    - CAUTION: NEVER open, edit, or import `apps/web/src/screens/Analyzer 2.tsx` (untracked stray with a
      space in the name) — the real file is `Analyzer.tsx`.
    - apps/web/src/screens/Analyzer.test.tsx — the large existing suite (hook mocks set up); extend it.
  </read_first>
  <behavior>
    - The inner rail/chart/right container's className is `contents lg:grid lg:grid-cols-[300px_minmax(0,1fr)_330px]`
      (assert the class string; the inline `gridTemplateColumns` style is gone — the columns move to the
      arbitrary-value class so they apply only at lg where `grid` is active).
    - `ScoringMethodologyPanel`'s wrapper has `order-2 lg:order-none`; `railBody`'s wrapper has `order-1
      lg:order-none`; `RightColumn`'s wrapper has `order-4 lg:order-none`; the center column has `order-3
      lg:order-none`.
    - No JSX element is reordered vs today (DOM order unchanged) — only wrappers/classes added.
    - The payoff chart wrapper carries `-mx-3 lg:mx-0` (full-bleed below lg).
  </behavior>
  <action>
    STEP 1 (RED): extend Analyzer.test.tsx to assert the inner container has `contents lg:grid` (and NOT
    an inline `gridTemplateColumns` style), the scorecard wrapper has `order-2`, the rail wrapper has
    `order-1`, and the payoff chart wrapper has `-mx-3 lg:mx-0`. RUN it; fails today (inline-style grid,
    no order/contents). Show the RED run.
    STEP 2 (GREEN): apply the UI-SPEC section 4 structure — add the four `order-*` wrappers, convert the
    inner grid to `contents lg:grid lg:grid-cols-[300px_minmax(0,1fr)_330px] lg:gap-4` (drop the inline
    `style`), and add `-mx-3 lg:mx-0` to the payoff chart wrapper. Move NO existing JSX element; change no
    PayoffChart/TermStructureChart prop. No `any`/`as`/`!`. Run GREEN.
  </action>
  <verify>
    <automated>cd apps/web && bunx vitest run src/screens/Analyzer.test.tsx && cd ../.. && bun run typecheck</automated>
  </verify>
  <human-check>
    390px (chrome-devtools): no h-scroll; visual order top-to-bottom is rail → scorecard → chart → term
    structure → right-column; devtools computed style on the inner container is `display: contents` (not
    grid/flex/block), scorecard wrapper `order: 2`, rail wrapper `order: 1`. 1024px+: scorecard banner
    full-width above the 300px/1fr/330px grid, exact original layout.
  </human-check>
  <done>Analyzer reflows rail → scorecard → chart+term → right below lg via display:contents + order-* with DOM byte-identical, full-bleed chart; the 300px/1fr/330px grid is restored at lg; Analyzer suite green, typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Journal — flex-col lg:grid port, un-clip below lg (RED→GREEN)</name>
  <files>apps/web/src/screens/Journal.tsx, apps/web/src/screens/Journal.test.tsx</files>
  <read_first>
    - 35-UI-SPEC.md "### 5. Journal — mobile stack order" — the exact target outer:
      `<div className="flex flex-col gap-3 p-3 lg:grid lg:h-full lg:grid-cols-[250px_minmax(0,1fr)_290px]
      lg:overflow-hidden">` and each of the three column children `flex flex-col gap-3 lg:min-h-0
      lg:overflow-y-auto` — the master-detail order (Trades → Lifecycle → reactive rail) is already the
      left-to-right DOM order, so NO `order-*` is needed; only the responsive mechanics move behind `lg:`.
    - 35-RESEARCH.md Pitfall 2 — Journal's `overflow-hidden` + fixed grid is the clip trap that hides the
      two right panes on a phone; gating `grid`/`h-full`/`overflow-hidden`/per-column `overflow-y-auto`/
      `min-h-0` behind `lg:` lets the page use normal document flow below lg.
    - apps/web/src/screens/Journal.tsx line ~562 — the current outer `<div className="grid h-full
      grid-cols-[250px_1fr_290px] gap-3 overflow-hidden p-3">` and its three `<div className="flex
      min-h-0 flex-col gap-3 overflow-y-auto">` column children (Trades / Lifecycle / reactive rail).
    - apps/web/src/screens/Journal.test.tsx — existing suite (JournalContainer/Journal); extend it.
  </read_first>
  <behavior>
    - The outer container's className is `flex flex-col gap-3 p-3 lg:grid lg:h-full
      lg:grid-cols-[250px_minmax(0,1fr)_290px] lg:overflow-hidden` — i.e. `overflow-hidden`, `grid`,
      `h-full`, and the fixed columns are all `lg:`-gated (assert the container is NOT unconditionally
      `grid ... overflow-hidden`; it starts `flex flex-col`).
    - Each of the three column children is `flex flex-col gap-3 lg:min-h-0 lg:overflow-y-auto` (the
      per-column scroll/min-height gated behind lg).
    - The three columns and all their content (TradeRow list, lifecycle, reactive rail, notes) render in
      the same DOM order as today.
  </behavior>
  <action>
    STEP 1 (RED): extend Journal.test.tsx to assert the outer positions container starts `flex flex-col`
    and gates `grid`/`overflow-hidden` behind `lg:` (not unconditional), and that a column child gates
    `overflow-y-auto` behind `lg:`. RUN it; fails today (unconditional `grid … overflow-hidden`). Show the
    RED run.
    STEP 2 (GREEN): change the outer container to the UI-SPEC section 5 className and each of the three
    column children from `flex min-h-0 flex-col gap-3 overflow-y-auto` to `flex flex-col gap-3 lg:min-h-0
    lg:overflow-y-auto`. Change nothing inside the columns. No `any`/`as`/`!`. Run GREEN.
  </action>
  <verify>
    <automated>cd apps/web && bunx vitest run src/screens/Journal.test.tsx src/screens/JournalContainer.test.tsx && cd ../.. && bun run typecheck && bun run lint</automated>
  </verify>
  <human-check>
    390px (chrome-devtools): no h-scroll, no clipped/invisible content (the two right panes are now
    reachable); Trades → Lifecycle → reactive rail stack in normal document flow (page scrolls, not three
    inner scroll panes); selecting a trade reveals its lifecycle section below. 1024px+: 250px/1fr/290px
    three-pane layout with independent per-column scroll inside a fixed-height viewport, exactly as today.
  </human-check>
  <done>Journal flows as a single un-clipped column below lg and restores the 250px/1fr/290px three-pane scroll layout at lg; Journal + JournalContainer suites green, typecheck and lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Grid/layout re-composition of two existing screens; no new inputs, endpoints, auth, or data-layer surface (RESEARCH Security Domain) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-05 | Denial of Service | Journal overflow-hidden clip | low | mitigate | The clip trap is a usability defect, not a security DoS, but it hides live journal data on mobile; gating `overflow-hidden` behind `lg:` restores visibility, asserted by the un-clip test; no new attack surface is added |
| T-35-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs this phase (RESEARCH Package Legitimacy Audit = N/A, zero new deps) — no supply-chain surface to mitigate |
</threat_model>

<verification>
- `cd apps/web && bunx vitest run src/screens/Analyzer.test.tsx src/screens/Journal.test.tsx src/screens/JournalContainer.test.tsx` all green.
- `bun run typecheck` and `bun run lint` clean.
- Manual (end-of-phase UAT, per 35-06): Analyzer rail→scorecard→chart→term→right at 390px with
  display:contents inner container; Journal un-clipped single-column flow at 390px; both byte-identical
  at 1024px+.
</verification>

<success_criteria>
- Analyzer and Journal have no horizontal scroll and no clipped panes at 390px (CONTEXT "worse than
  stacked" bugs fixed, Pitfall 2).
- Both use the one responsive convention ported from Overview (D-09), DOM byte-identical, desktop
  pixel-identical at ≥1024px.
- The stray `Analyzer 2.tsx` was never touched.
</success_criteria>

<output>
Create `.planning/phases/35-mobile-experience-redesign-the-phone-view-is-desktop-panels-/35-05-SUMMARY.md` when done
</output>
