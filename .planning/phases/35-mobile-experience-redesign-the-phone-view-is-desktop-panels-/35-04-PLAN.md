---
phase: 35-mobile-experience-redesign
plan: 04
type: execute
wave: 3
depends_on: [35-03]
files_modified:
  - apps/web/src/lib/position-format.ts
  - apps/web/src/lib/position-format.test.ts
  - apps/web/src/components/PositionCard.tsx
  - apps/web/src/components/PositionCard.test.tsx
  - apps/web/src/screens/Overview.tsx
  - apps/web/src/screens/Overview.test.tsx
autonomous: true
requirements: [MOBILE-05]
user_setup: []
must_haves:
  truths:
    - "Below lg the positions render as a list of PositionCards fed the same Row[] the desktop table uses; at lg the existing <table> renders and the card list is display:none (removed from the accessibility tree)"
    - "A PositionCard collapsed shows position label (+ IV n/a badge), expiry line1/line2, Net val, Unreal (sign-colored), and the verdict chip; tapping the card body (not the checkbox) expands Δ/Γ/Θ/Vega; the checkbox toggles include/exclude without expanding"
    - "The card's expand state and include/exclude reuse the EXISTING expandedRowKey/onSelectRow and excluded/onToggleExcluded state — no second expand or exclusion mechanism"
    - "The number/format helpers and Row type PositionCard needs live in a shared module (not imported from Overview.tsx) so no Overview→PositionCard→Overview runtime cycle exists"
  artifacts:
    - apps/web/src/lib/position-format.ts
    - apps/web/src/components/PositionCard.tsx
    - apps/web/src/components/PositionCard.test.tsx
  key_links:
    - "PositionCard imports VerdictChip from screens/HeldPositionsPanel.tsx (already the cross-screen source Overview uses; HeldPositionsPanel does not import PositionCard, so no cycle) and resolveLivePositionRow from lib/live-position-greeks.ts"
    - "The dual render lives INSIDE PositionsTable (it already computes rows + owns excluded/verdict/handlers): the existing <table> gets hidden lg:table, a new lg:hidden card list maps the same rows to PositionCard"
    - "hidden lg:table / lg:hidden (display:none) is the ONLY correct hide technique here — sr-only/opacity would announce every position twice to a screen reader (UI-SPEC accessibility strategy)"
---

<objective>
Give the positions table a genuine mobile card render (CONTEXT item 3, D-04/D-05): below `lg` each
position is a `PositionCard` — collapsed showing label + expiry + Net val + Unreal + verdict, tap-to-
expand revealing Δ/Γ/Θ/Vega — fed the SAME `Row[]` the desktop `<table>` renders; at `lg` the existing
table renders and the card list is `display:none`. To avoid an `Overview → PositionCard → Overview`
runtime import cycle, first extract the number-format helpers (`usd`/`signed`/`signedUsd`/`signClass`)
and the `Row`/`ExpiryCell` types — currently local to `Overview.tsx` — into a shared `lib/position-
format.ts` that both files import.

Purpose: this is the last severe mobile bug — Analyzer/Journal clip, but the positions table crushes its
9 columns into horizontal scroll on a phone (CONTEXT item 3). A real second DOM (card), not a CSS
`display:block` table hack, preserves the desktop table's a11y semantics (NN/g, RESEARCH Pattern 1) and
reuses the existing expand/exclude state so there is no second source of truth.

Output: `PositionCard` + a shared format module, and a dual-render positions area that is cards below
`lg` and the untouched table at `lg`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-mobile-experience-redesign-the-phone-view-is-desktop-panels-/35-CONTEXT.md
@.planning/phases/35-mobile-experience-redesign-the-phone-view-is-desktop-panels-/35-RESEARCH.md
@.planning/phases/35-mobile-experience-redesign-the-phone-view-is-desktop-panels-/35-UI-SPEC.md
@apps/web/src/screens/Overview.tsx
@apps/web/src/screens/Overview.test.tsx
@apps/web/src/screens/HeldPositionsPanel.tsx
@apps/web/src/lib/live-position-greeks.ts
@apps/web/src/components/system/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract number-format helpers + Row/ExpiryCell types to lib/position-format.ts (refactor, keep green)</name>
  <files>apps/web/src/lib/position-format.ts, apps/web/src/lib/position-format.test.ts, apps/web/src/screens/Overview.tsx</files>
  <read_first>
    - apps/web/src/screens/Overview.tsx — the four local, non-exported helpers `signed(v, dp=3)`,
      `signedUsd(v, dp=3)`, `usd(v, dp=3)`, `signClass(v)` and the local `type Row` / `type ExpiryCell`
      (Row = { key; label; expiry: ExpiryCell; legs: ReadonlyArray<BrokerPositionResponse> }). Note every
      current call site of these helpers/types inside Overview.tsx so the re-import replaces them 1:1.
    - .claude/rules/tdd.md "Scope → Exempt: … glue code … styling-only": a pure move with no behavior
      change is a refactor — the gate is that the FULL existing Overview suite stays green and typecheck
      passes, plus a tiny example test pinning the moved helpers' output (they are money/number
      formatting — the ponytail one-runnable-check rule).
    - 35-RESEARCH.md "Recommended Project Structure" — new files land beside what they extend; a shared
      `lib/` module is the right home for helpers now used by two files.
  </read_first>
  <action>
    Create `apps/web/src/lib/position-format.ts` exporting `usd`, `signed`, `signedUsd`, `signClass`
    (moved verbatim from Overview.tsx, same default `dp` args and exact output) and the `Row` +
    `ExpiryCell` types (moved verbatim, keeping `BrokerPositionResponse` import). In Overview.tsx, delete
    the local definitions and import the four helpers + the two types from `../lib/position-format.ts`
    (use `import type` for Row/ExpiryCell). This is a pure move — no output/behavior change, no rounding
    change. Add `position-format.test.ts` with a small set of example assertions pinning each helper
    (e.g. `usd`, `signedUsd` sign + `$` formatting, `signClass` returns the up/down/neutral token) so a
    future edit that breaks money formatting fails loudly. No `any`/`as`/`!`.
  </action>
  <verify>
    <automated>cd apps/web && bunx vitest run src/lib/position-format.test.ts src/screens/Overview.test.tsx && cd ../.. && bun run typecheck && bun run lint</automated>
  </verify>
  <done>usd/signed/signedUsd/signClass and Row/ExpiryCell live in lib/position-format.ts, imported by Overview.tsx with no behavior change; the full Overview suite plus a new format example test are green; typecheck and lint clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PositionCard component — collapsed/expanded, checkbox + expand wiring (RED→GREEN)</name>
  <files>apps/web/src/components/PositionCard.tsx, apps/web/src/components/PositionCard.test.tsx</files>
  <read_first>
    - 35-UI-SPEC.md "### 3. `PositionCard` — new component" — the full component body and prop list
      (`row, spot, liveGreeks, liveStatus, ivNa, verdict, marketSession, expanded, onSelect, included,
      onToggleIncluded`), the collapsed vs expanded layout, the `<button aria-expanded={expanded}>` body
      + sibling checkbox `<label>` (44px hit-area via `min-h-11 min-w-11`), the IV n/a `Badge`, the
      `Stat` Net val/Unreal + expanded Δ/Γ/Θ/Vega grid, and the "no live-cell flash on the card"
      deliberate simplification. Also the checkbox `aria-label` = `` `Include ${row.label} in risk
      profile & total` `` (verbatim parity with the desktop table).
    - 35-UI-SPEC.md "## Accessibility Notes" — `aria-expanded` IS set manually (plain button, not a
      native disclosure); checkbox is a SIBLING of the expand button (nested interactive elements are
      invalid HTML); focus-visible inherited, not overridden.
    - apps/web/src/screens/Overview.tsx `PositionsTable` row render — the source of truth for how a row's
      values are computed today: `resolveLivePositionRow(r.legs, spot, liveGreeks)` → `{ netVal, unreal,
      greeks, liveTs }`; verdict via `verdictByRowKey.get(r.label)`; expanded = `expandedRowKey === r.key
      && verdict !== null`. Mirror this exactly so the card and table agree.
    - apps/web/src/screens/HeldPositionsPanel.tsx — `export function VerdictChip({ row, marketSession })`
      (import it here; HeldPositionsPanel does not import PositionCard, so no cycle).
    - apps/web/src/lib/position-format.ts (from Task 1) for usd/signed/signedUsd/signClass + the Row type;
      apps/web/src/lib/live-position-greeks.ts for resolveLivePositionRow; components/system for Stat;
      @/components/ui for Badge.
  </read_first>
  <behavior>
    - Collapsed: renders `row.label`, the IV n/a `Badge` ONLY when `ivNa` is true, `row.expiry.line1` and
      `row.expiry.line2`, a "Net val" Stat = `usd(netVal)`, an "Unreal" Stat = `signedUsd(unreal)` (or
      "—" when null) sign-colored, and the `VerdictChip` when `verdict !== null`. The Δ/Γ/Θ/Vega grid is
      NOT present.
    - Expanded (`expanded` true): additionally renders the four-cell Δ/Γ/Θ/Vega grid.
    - The expand `<button>` has `aria-expanded={expanded}` and calls `onSelect(row.key)` on click; the
      checkbox is a sibling (not inside the button), reflects `included`, calls `onToggleIncluded(row.key)`
      on change, and carries the exact `aria-label`. Clicking the checkbox does NOT call `onSelect`.
    - When `!included`, the card carries the `opacity-40` dim (same convention as the excluded table row).
  </behavior>
  <action>
    STEP 1 (RED): write PositionCard.test.tsx FIRST covering: collapsed renders label/expiry/Net val/
    Unreal/verdict and NO greeks grid; IV n/a badge appears only when `ivNa`; passing `expanded` reveals
    the Δ/Γ/Θ/Vega grid; clicking the expand button fires `onSelect(row.key)` with `aria-expanded`
    reflecting state; toggling the checkbox fires `onToggleIncluded(row.key)` and does NOT fire onSelect;
    the checkbox `aria-label` matches `Include <label> in risk profile & total`. Build a minimal `Row`
    fixture + empty `liveGreeks` map + a stub verdict. RUN it; fails because PositionCard does not exist.
    Show the RED run.
    STEP 2 (GREEN): implement PositionCard.tsx exactly per the UI-SPEC section 3 (typed
    `PositionCardProps`, no `any`/`as`/`!`; compute values via `resolveLivePositionRow`; import
    VerdictChip, Stat, Badge, and the format helpers from their real modules). Do NOT port the desktop
    per-cell `.live-cell` flash (deliberate simplification per the UI-SPEC). Run GREEN.
  </action>
  <verify>
    <automated>cd apps/web && bunx vitest run src/components/PositionCard.test.tsx && cd ../.. && bun run typecheck</automated>
  </verify>
  <done>PositionCard renders collapsed/expanded states, wires the expand button (aria-expanded + onSelect) and a sibling checkbox (onToggleIncluded + exact aria-label) with no cross-talk, reusing the shared Row/format/VerdictChip/resolveLivePositionRow; its test is green and typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Dual-render positions — table hidden lg:table + card list lg:hidden (RED→GREEN)</name>
  <files>apps/web/src/screens/Overview.tsx, apps/web/src/screens/Overview.test.tsx</files>
  <read_first>
    - 35-UI-SPEC.md "### 3. PositionCard → Mount" — the dual render: `<table className="hidden w-full
      lg:table">` (existing markup unchanged) plus `<div className="flex flex-col gap-2 lg:hidden">`
      mapping the same `rows` to `<PositionCard>` (verdict = `verdictByRowKey.get(r.label) ?? null`,
      `expanded={selectedRowKey === r.key && verdict !== null}`, sharing the same props the table row
      uses). And "### Table ↔ card accessibility strategy" — must be `display:none`-based (`hidden
      lg:table` / `lg:hidden`), never sr-only/opacity.
    - apps/web/src/screens/Overview.tsx `PositionsTable` — it already computes `rows` (via `buildRows`)
      and owns `excluded`, `onToggleExcluded`, `verdictByRowKey`, `ivNaByRowKey`, `expandedRowKey`,
      `onSelectRow`, `spot`, `liveGreeks`, `liveStatus`, `verdictMarketSession`. Add the card branch HERE
      (same component, same data) so there is no prop re-threading. The current `<table className="w-full
      border-collapse font-mono text-[11px] tabular-nums">` gains `hidden lg:table`.
    - apps/web/src/components/PositionCard.tsx (Task 2).
  </read_first>
  <behavior>
    - The existing `<table>` gains `hidden lg:table` (present in the DOM, `display:none` below lg).
    - A new `lg:hidden` card-list container renders one `PositionCard` per row in `rows` (assert the card
      count equals the row count and that a known row's label appears in a card).
    - Each card receives the same `spot`/`liveGreeks`/`liveStatus`/`ivNa`/`verdict`/`marketSession`/
      `expanded`/`onSelect`/`included`/`onToggleIncluded` values the table row computes for that row.
    - The Net-total row and all existing table behavior are unchanged (still inside the `lg:table`).
  </behavior>
  <action>
    STEP 1 (RED): extend Overview.test.tsx to assert both branches exist: the positions `<table>` has
    `hidden lg:table`, and a `lg:hidden` card container renders a `PositionCard` per row (e.g. assert the
    number of `position-card-*` testids equals the number of `position-row-*` testids, and a known label
    appears in a card). RUN it; fails today (table only). Show the RED run.
    STEP 2 (GREEN): inside `PositionsTable`'s return, add `hidden lg:table` to the `<table>` className and
    add a sibling `<div className="flex flex-col gap-2 lg:hidden">` mapping `rows` to `<PositionCard>`
    with the per-row values already computed in the same scope (mirror the table row's `resolved`/`ivNa`/
    `verdict`/`expanded`/`included` derivations, passing the shared handlers). Do NOT re-thread props from
    Overview — the data is already here. No `any`/`as`/`!`. Run GREEN, then run the full Overview suite to
    confirm the table path (Net total, exclude, verdict expand) still passes.
  </action>
  <verify>
    <automated>cd apps/web && bunx vitest run src/screens/Overview.test.tsx && cd ../.. && bun run typecheck && bun run lint</automated>
  </verify>
  <human-check>
    390px (chrome-devtools): positions render as cards (the `<table>` computes `display:none`); tapping a
    card body expands Δ/Γ/Θ/Vega; the checkbox toggles include/exclude without expanding; no h-scroll.
    1024px+: the 9-column table renders and the card list is `display:none` (absent from the a11y tree);
    a screen reader announces each position once, not twice.
  </human-check>
  <done>Positions dual-render — cards below lg, the untouched table at lg — off the same rows, using display:none hiding; card count matches row count; the desktop table path is unchanged; Overview suite green, typecheck and lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | A second presentational render of already-fetched position rows; no new inputs, endpoints, auth, or data-layer surface (RESEARCH Security Domain) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-04 | Information Disclosure | dual table/card render | low | mitigate | The card branch consumes the identical Row[]/resolved values as the table (no new data path); `display:none` hiding keeps exactly one branch in the a11y tree so no position is announced twice or leaked in a hidden-but-reachable node — asserted by the dual-render test |
| T-35-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs this phase (RESEARCH Package Legitimacy Audit = N/A, zero new deps) — no supply-chain surface to mitigate |
</threat_model>

<verification>
- `cd apps/web && bunx vitest run src/lib/position-format.test.ts src/components/PositionCard.test.tsx src/screens/Overview.test.tsx` all green.
- `bun run typecheck` and `bun run lint` clean (no Overview→PositionCard cycle; no `any`/`as`/`!`).
- Manual (end-of-phase UAT, per 35-06): cards below lg with tap-expand + independent checkbox; table at
  lg unchanged; each position announced once.
</verification>

<success_criteria>
- Positions read as tap-expandable cards below lg and as the untouched 9-column table at lg, off one
  Row[] source (CONTEXT item 3, D-04).
- Expand and include/exclude reuse the existing state — no second mechanism (D-05).
- No runtime import cycle; desktop table a11y/semantics preserved (Pattern 1).
</success_criteria>

<output>
Create `.planning/phases/35-mobile-experience-redesign-the-phone-view-is-desktop-panels-/35-04-SUMMARY.md` when done
</output>
