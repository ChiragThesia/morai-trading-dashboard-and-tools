---
phase: 35-mobile-experience-redesign
plan: 06
type: execute
wave: 4
depends_on: [35-01, 35-02, 35-03, 35-04, 35-05]
files_modified:
  - apps/web/src/screens/Overview.test.tsx
autonomous: true
requirements: [MOBILE-01, MOBILE-02, MOBILE-06]
user_setup: []
must_haves:
  truths:
    - "The full apps/web test suite, typecheck, and lint are green after all five prior plans land"
    - "One automated smoke assertion proves no horizontal overflow at a mobile width for the mounted Overview screen (document scrollWidth does not exceed the viewport)"
    - "The desktop regression tripwire checklist (10 items) and the 390px chrome-devtools UAT checklist are recorded for the end-of-phase verify step, since jsdom cannot evaluate real layout (wrap/clip/scroll/order/touch-size are viewport-only)"
  artifacts:
    - apps/web/src/screens/Overview.test.tsx
  key_links:
    - "This is the phase-final gate: it does not add features — it proves the aggregate is green and enumerates the manual checks the orchestrator's UAT (gsd-verify-work) must run at 390px and 1024px+"
    - "MOBILE-06 (desktop unchanged) has no jsdom equivalent — it is a side-by-side screenshot spot-check in the tripwire list; the automated portion guards only what jsdom can see"
---

<objective>
Close the phase with the aggregate gate: run the full `apps/web` suite + typecheck + lint green, add one
automated no-horizontal-overflow smoke assertion for the Overview mount, and record the two manual
checklists jsdom cannot cover — the 10-item Desktop Regression Tripwire list (MOBILE-06) and the 390px
chrome-devtools UAT checklist (MOBILE-01/02) — for the end-of-phase verify step.

Purpose: RESEARCH's Validation Architecture is explicit that layout-in-viewport claims (wrap, clip,
scroll, `order`, touch-target size, sticky count, chart tooltip-on-tap) are jsdom-blind by construction
and MUST go through a rendered-browser checklist. This plan is the single place that (a) asserts the
whole suite still passes as one unit and (b) hands the orchestrator the exact manual UAT + desktop
tripwire steps to run, so nothing viewport-only is silently assumed.

Output: a green aggregate gate plus the enumerated manual UAT/tripwire checklists in this plan's
verification block for `gsd-verify-work`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-mobile-experience-redesign-the-phone-view-is-desktop-panels-/35-CONTEXT.md
@.planning/phases/35-mobile-experience-redesign-the-phone-view-is-desktop-panels-/35-RESEARCH.md
@.planning/phases/35-mobile-experience-redesign-the-phone-view-is-desktop-panels-/35-UI-SPEC.md
@apps/web/src/screens/Overview.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: No-horizontal-overflow smoke assertion + full aggregate gate (RED→GREEN)</name>
  <files>apps/web/src/screens/Overview.test.tsx</files>
  <read_first>
    - 35-RESEARCH.md "Validation Architecture → Phase Requirements → Test Map" (MOBILE-02 row: the
      `document.body.scrollWidth === window.innerWidth` smoke check is the ONE automatable slice of the
      no-h-scroll claim) and "Wave 0 Gaps" / "Sampling Rate" (per-wave full suite, phase gate = full
      suite + manual checklist).
    - 35-UI-SPEC.md "## Acceptance Criteria" and "## Desktop Regression Tripwires" — the source of the two
      manual checklists reproduced in this plan's verification block.
    - apps/web/src/screens/Overview.test.tsx — the existing suite and its render helper / hook mocks;
      jsdom reports `window.innerWidth` = 1024 and does not compute real layout, so this smoke assertion
      is a shallow guard (it catches a gross unconditional min-width overflow, not true wrap/clip) — its
      value is regression tripwire, not proof; the real proof is the manual checklist.
  </read_first>
  <behavior>
    - A new Overview test renders the screen and asserts `document.documentElement.scrollWidth` does not
      exceed `document.documentElement.clientWidth` (no gross horizontal overflow introduced by a
      min-width/overflow bug). This is a shallow jsdom guard, documented as such in a comment.
    - The full `apps/web` workspace suite, typecheck, and lint all pass.
  </behavior>
  <action>
    STEP 1 (RED then GREEN for the smoke test): add the no-overflow smoke assertion to Overview.test.tsx
    with a comment noting jsdom's layout blindness (it guards against a gross regression only; real
    wrap/clip/scroll verification is the manual checklist below). If the assertion is trivially true under
    jsdom today, keep it as a regression tripwire (it is allowed to pass immediately — it exists to fail
    if a future edit introduces an unconditional wider-than-viewport element); note this in the SUMMARY
    rather than forcing an artificial red.
    STEP 2 (aggregate gate): run the FULL apps/web suite, then `bun run typecheck` and `bun run lint`
    across the workspace. All must be green. If anything from plans 35-01..35-05 regressed, STOP and fix
    the offending plan's code before marking done (do not disable or skip a failing test).
    STEP 3: record the two manual checklists (below) verbatim in 35-06-SUMMARY.md so the orchestrator's
    end-of-phase verify (gsd-verify-work) runs them at 390px and 1024px+.
  </action>
  <verify>
    <automated>cd apps/web && bunx vitest run src/screens/Overview.test.tsx && cd ../.. && bun run test && bun run typecheck && bun run lint</automated>
  </verify>
  <human-check>
    Run the two checklists in this plan's &lt;verification&gt; block via chrome-devtools mobile emulation
    (390×844) and a 1024px+/1440px desktop width. All items must pass before the phase is verified;
    Recharts tooltip-on-tap (Pitfall 4) and real-iOS-Safari sticky (Pitfall 5) are log-a-follow-up items,
    not silent regressions.
  </human-check>
  <done>Full apps/web suite + typecheck + lint green with the added no-overflow smoke guard; both manual checklists recorded in the SUMMARY for end-of-phase UAT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Aggregate verification gate + one test assertion; no product code, no new inputs, endpoints, auth, or data-layer surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-06 | Repudiation | phase gate evidence | low | mitigate | The gate records concrete evidence (green suite/typecheck/lint output + completed manual checklists in the SUMMARY) so "desktop unchanged / no h-scroll" is proven, not asserted; nothing is marked done on "should work" |
| T-35-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs this phase (RESEARCH Package Legitimacy Audit = N/A, zero new deps) — no supply-chain surface to mitigate |
</threat_model>

<verification>

## Automated gate
- `cd apps/web && bun run test` (full workspace suite) green — includes every test added by plans 35-01..35-05.
- `bun run typecheck` clean (no `any`/`as`/`!` anywhere in the phase's changes).
- `bun run lint` clean (no import cycle, no boundary violation).

## Desktop Regression Tripwires (chrome-devtools 1024px+ and 1440px) — MOBILE-06
- [ ] PillHeader: all 10 chips render in the single existing row, unwrapped; still `sticky` under Shell's header.
- [ ] Positions: `<table>` visible with all 9 columns + checkbox column; the PositionCard list is absent (`lg:hidden` → `display:none`).
- [ ] MarketRail: fully expanded as the 320px left column regardless of the `<details>` runtime open state; the "Market" summary is not visible (`lg:hidden`).
- [ ] Overview 3-column grid: `320px / minmax(0,1fr) / 360px` — unchanged widths; MarketRail left, hero center, GEX right.
- [ ] PayoffControls: `flex flex-wrap` row, unchanged desktop wrap behavior (never scroll-snap at ≥lg).
- [ ] PayoffControls' 5 buttons render at their EXACT pre-phase box at ≥lg (`px-[7px] py-0.5 text-[9px]`, `min-h-0`) — devtools-diff a `size="touch"` button's computed box model against an untouched `xs` button on the same page (catches any drift in the hand-typed `SIZE_CLASS.touch` `lg:` triplet).
- [ ] Analyzer: scorecard banner full-width above the 300px/1fr/330px grid (via `order-none` + inner `lg:grid`), exact original DOM nesting.
- [ ] Journal: 250px/1fr/290px three-pane layout with independent per-column scroll inside a fixed-height viewport, exactly as today.
- [ ] Shell nav tabs: `min-h-8` (32px) height at ≥lg, unchanged.
- [ ] No visual diff in a side-by-side screenshot vs the pre-phase baseline at 1280px.

## Mobile UAT checklist (chrome-devtools 390×844) — MOBILE-01 / MOBILE-02
- [ ] Overview 390px: priority KPI row (SPX / net γ/1% / VIX / book) on one line, no wrap, no page h-scroll; `document.body.scrollWidth === window.innerWidth`.
- [ ] Overview: secondary chip rail scrolls + snaps, last chip peeks at the right edge before scroll input.
- [ ] Overview: payoff hero chart visible above the collapsed MarketRail; chart full-bleed; no "view-only · Analyzer →".
- [ ] Overview: PayoffControls scrolls instead of wrapping; date-step/Today/toggle buttons ≥44px tall (box model).
- [ ] Overview: positions render as PositionCards (`<table>` computes `display:none`); tapping a card body expands Δ/Γ/Θ/Vega; the checkbox toggles independently without expanding.
- [ ] Overview: MarketRail loads COLLAPSED (no `open`); tapping "▸ Market" expands it AFTER the positions in scroll order; tab from nav lands on the "Market" summary before the hero (one stop).
- [ ] Analyzer 390px: no h-scroll; visual order rail → scorecard → chart → term → right; inner container computed `display: contents`, scorecard wrapper `order: 2`, rail wrapper `order: 1`.
- [ ] Journal 390px: no h-scroll, no clipped/invisible content; Trades → Lifecycle → reactive rail in normal document flow; selecting a trade reveals its lifecycle below.
- [ ] Cross-cutting: only ONE sticky element while scrolling at 390px (Shell's header); PillHeader scrolls normally.
- [ ] Cross-cutting: tap the payoff chart at 390px — tooltip/crosshair responds (Pitfall 4/OQ2; if broken, log a follow-up, not a silent regression).
- [ ] Real iOS Safari (not emulator, if available): header does not jitter/misplace on scroll (Pitfall 5).
</verification>

<success_criteria>
- The full apps/web suite, typecheck, and lint are green as one unit after all six plans.
- Both manual checklists are recorded in the SUMMARY and available for gsd-verify-work.
- Desktop is confirmed pixel-identical (MOBILE-06) and the mobile acceptance bars (MOBILE-01/02) are
  enumerated for the rendered-browser UAT that jsdom cannot perform.
</success_criteria>

<output>
Create `.planning/phases/35-mobile-experience-redesign-the-phone-view-is-desktop-panels-/35-06-SUMMARY.md` when done
</output>
