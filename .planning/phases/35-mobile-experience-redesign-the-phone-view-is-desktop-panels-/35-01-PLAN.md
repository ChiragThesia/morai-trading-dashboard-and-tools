---
phase: 35-mobile-experience-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/system/ChipRail.tsx
  - apps/web/src/components/system/ChipRail.test.tsx
  - apps/web/src/components/system/Button.tsx
  - apps/web/src/components/system/system.test.tsx
  - apps/web/src/components/charts/PayoffControls.tsx
  - apps/web/src/components/charts/PayoffControls.test.tsx
autonomous: true
requirements: [MOBILE-02, MOBILE-03]
user_setup: []
must_haves:
  truths:
    - "Below lg (1024px) a row of chips scrolls horizontally with per-chip snap and a right-edge peek; at lg and up the same row falls back byte-for-byte to today's flex-wrap behavior (no scroll-snap, no clipping)"
    - "A Button rendered with size=\"touch\" is at least 44px tall below lg and reverts to the exact xs box (px-[7px] py-0.5 text-[9px], min-h-0) at lg and up"
    - "PayoffControls' date/toggle strip no longer wraps below lg — it scrolls as one ChipRail unit with 44px-tall date-step/Today/toggle buttons — and is visually identical to today at lg and up (both Overview and Analyzer mounts benefit from this one edit)"
  artifacts:
    - apps/web/src/components/system/ChipRail.tsx
    - apps/web/src/components/system/ChipRail.test.tsx
  key_links:
    - "ChipRail is exported from components/system so PillHeader (plan 35-03) and PayoffControls both consume the one primitive — no bespoke scroll wrapper per surface (RESEARCH Pattern 2 / D-07)"
    - "SIZE_CLASS.touch's lg: triplet is a hand-typed duplicate of xs's classes; the desktop tripwire in plan 35-06 devtools-diffs a touch button against an untouched xs button to catch drift"
---

<objective>
Build the two shared mobile-chrome primitives every later plan reuses, and apply the first of them to
the shared chart control strip. Deliver (1) `ChipRail` — a native CSS scroll-snap wrapper that becomes
`flex-wrap` at desktop — and (2) a `"touch"` entry in `Button`'s `SIZE_CLASS` map that is ≥44px tall
below `lg` and reverts to the exact `xs` box at `lg`. Then convert `PayoffControls`' `flex-wrap` row —
the literal cause of CONTEXT item 4 ("chart chrome wraps") — into a `ChipRail` with `size="touch"`
buttons, fixing the wrap for BOTH the Overview and Analyzer chart mounts with one edit.

Purpose: RESEARCH D-07 / Pattern 2 and the Don't-Hand-Roll table call for one scroll-snap mechanism and
one touch-size lookup extension reused at every call site, not a per-surface fix. Landing them first
unblocks the Overview PillHeader split (35-03) and keeps the desktop revert a guaranteed class triplet,
not "probably never wraps."

Output: `ChipRail` exported from `components/system`, `Button` gaining `size="touch"`, and
`PayoffControls` scrolling-not-wrapping below `lg` — all `lg:`-reverted to today's exact desktop classes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-mobile-experience-redesign-the-phone-view-is-desktop-panels-/35-CONTEXT.md
@.planning/phases/35-mobile-experience-redesign-the-phone-view-is-desktop-panels-/35-RESEARCH.md
@.planning/phases/35-mobile-experience-redesign-the-phone-view-is-desktop-panels-/35-UI-SPEC.md
@apps/web/src/components/system/Button.tsx
@apps/web/src/components/system/index.tsx
@apps/web/src/components/system/system.test.tsx
@apps/web/src/components/charts/PayoffControls.tsx
@apps/web/src/components/charts/PayoffControls.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ChipRail — shared native scroll-snap wrapper (RED→GREEN)</name>
  <files>apps/web/src/components/system/ChipRail.tsx, apps/web/src/components/system/ChipRail.test.tsx, apps/web/src/components/system/index.tsx</files>
  <read_first>
    - 35-UI-SPEC.md "### `ChipRail` — one shared scroll-snap primitive, two call sites" — the exact
      component body, className string, role/aria-label contract, and the `lg:` revert triplet.
    - 35-UI-SPEC.md "## Spacing Scale" rows "chip-rail item gap" (`gap-2`) and "chip-rail edge peek"
      (`pr-6`) — the peek is deliberately less than one chip so the next chip visibly peeks (the
      zero-JS "more to scroll" affordance).
    - 35-RESEARCH.md "Pattern 2: Horizontal scroll-snap chip rail" — the mechanism and why native
      `scroll-snap` replaces a JS carousel (Don't-Hand-Roll).
    - apps/web/src/components/system/index.tsx — the molecule barrel; `ChipRail` is re-exported here
      beside `Panel`/`Stat`/`MetricChip`. Confirm `cn` import path (`@/lib/utils`).
    - apps/web/src/components/system/system.test.tsx — the co-located test style for this folder
      (render + class/role assertions, no snapshot).
  </read_first>
  <behavior>
    - Renders a `role="group"` container with the passed `ariaLabel` and its `children`.
    - Container carries the mobile scroll classes (`snap-x snap-mandatory overflow-x-auto pr-6`) AND the
      `lg:` revert triplet (`lg:flex-wrap lg:overflow-visible lg:snap-none lg:pr-0`) so desktop is a
      guaranteed `flex-wrap` fallback, not incidental.
    - A caller-passed `className` is merged (via `cn`) after the base classes so call sites can add
      `mt-2`, `lg:hidden`, etc. without losing the base contract.
    - All children passed are rendered (assert a couple of sentinel children appear).
  </behavior>
  <action>
    STEP 1 (RED): write ChipRail.test.tsx FIRST, rendering `<ChipRail ariaLabel="test rail">` with two
    sentinel child nodes, and asserting: the `role="group"` element resolves by its accessible name;
    both children render; the group's className contains `snap-x`, `overflow-x-auto`, `pr-6`, and the
    four `lg:` revert tokens; a passed `className` (e.g. `lg:hidden`) is present on the same element.
    RUN it and confirm it fails because `ChipRail` does not yet exist (a real module-not-found / render
    failure, then a genuine assertion once stubbed — per .claude/rules/tdd.md show the RED run).
    STEP 2 (GREEN): create ChipRail.tsx exactly per the UI-SPEC "ChipRail" section (typed props
    `children: React.ReactNode`, `ariaLabel: string`, `className?: string`; return a single `<div
    role="group" aria-label=... className={cn(...)}>`); no `any`/`as`/`!`. Re-export `ChipRail` from
    components/system/index.tsx beside the other molecules. Run the test GREEN.
    Note: each call site (PillHeader, PayoffControls) is responsible for adding `snap-start shrink-0` to
    its own children — ChipRail owns the container, not the child chips.
  </action>
  <verify>
    <automated>cd apps/web && bunx vitest run src/components/system/ChipRail.test.tsx && cd ../.. && bun run typecheck</automated>
  </verify>
  <done>ChipRail renders a role=group scroll-snap container with the lg: flex-wrap revert, merges a caller className, and is exported from components/system; its test is green and typecheck is clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Button SIZE_CLASS "touch" entry — ≥44px below lg, lg:-reverts to xs (RED→GREEN)</name>
  <files>apps/web/src/components/system/Button.tsx, apps/web/src/components/system/system.test.tsx</files>
  <read_first>
    - 35-UI-SPEC.md "### Touch targets (≥44px below `lg:`)" item 2 — the EXACT `SIZE_CLASS` map with the
      new `touch: "min-h-11 px-3 py-1.5 text-[11px] lg:min-h-0 lg:px-[7px] lg:py-0.5 lg:text-[9px]"`
      entry; the `lg:` tail is a byte-for-byte duplicate of `xs`'s `px-[7px] py-0.5 text-[9px]` plus
      `min-h-0`.
    - apps/web/src/components/system/Button.tsx — `ButtonSize` union (currently `"xs" | "sm"`),
      `SIZE_CLASS` record, and `buttonClass()` which composes `BASE + SIZE_CLASS[size] + variant`.
    - apps/web/src/components/system/system.test.tsx — existing Button/buttonClass assertions to extend.
  </read_first>
  <behavior>
    - `ButtonSize` gains `"touch"` as a third member; `SIZE_CLASS` gains the matching entry so the
      `Record<ButtonSize, string>` stays exhaustive (typecheck proves no missing key).
    - `buttonClass({ size: "touch" })` returns a string containing `min-h-11` (the ≥44px mobile height)
      AND the `lg:` revert tokens (`lg:min-h-0`, `lg:px-[7px]`, `lg:py-0.5`, `lg:text-[9px]`).
    - `xs` and `sm` entries are unchanged — every existing Button call site keeps its exact classes
      (assert `buttonClass({ size: "xs" })` still equals today's `px-[7px] py-0.5 text-[9px]` fragment).
  </behavior>
  <action>
    STEP 1 (RED): add to system.test.tsx a test asserting `buttonClass({ size: "touch" })` contains
    `min-h-11` and the four `lg:` revert tokens, plus a guard test that `buttonClass({ size: "xs" })`
    still contains `px-[7px] py-0.5 text-[9px]`. RUN it; the touch test fails (type error / missing key
    — `"touch"` is not yet assignable to `ButtonSize`). Show the RED run.
    STEP 2 (GREEN): extend `ButtonSize` to `"xs" | "sm" | "touch"` and add the `touch` entry to
    `SIZE_CLASS` exactly per the UI-SPEC string. Do not alter `xs`/`sm`. No `any`/`as`/`!`. Run GREEN.
  </action>
  <verify>
    <automated>cd apps/web && bunx vitest run src/components/system/system.test.tsx && cd ../.. && bun run typecheck</automated>
  </verify>
  <done>Button exposes size="touch" (≥44px mobile height) that reverts to the exact xs box at lg; xs/sm untouched; system.test.tsx green, typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PayoffControls — flex-wrap row becomes a ChipRail with touch buttons (RED→GREEN)</name>
  <files>apps/web/src/components/charts/PayoffControls.tsx, apps/web/src/components/charts/PayoffControls.test.tsx</files>
  <read_first>
    - 35-UI-SPEC.md "### 1. Overview — mobile stack order → **Chart chrome**" bullet on `PayoffControls`
      ("its row becomes a `ChipRail` … date buttons get `size=\"touch\"`, the whole strip scrolls
      together as one unit"), and the `ChipRail` `aria-label` for this mount: `"Chart date and series
      controls"`.
    - 35-RESEARCH.md D-07 (one `ChipRail`, two call sites) and Pitfall 3 (touch-target sizing).
    - apps/web/src/components/charts/PayoffControls.tsx — the current outer `<div className="mb-2 flex
      flex-wrap items-center gap-2 font-mono text-[9px] text-dim">` wrapping the `Date:` label, the
      `‹`/`›`/`Today` `Button`s, the date `<input>`, the divider span, and the four toggle `Button`s.
    - apps/web/src/components/charts/PayoffControls.test.tsx — existing render assertions to extend.
  </read_first>
  <behavior>
    - The control strip renders inside a `ChipRail` (a `role="group"` with accessible name "Chart date
      and series controls") instead of the bare `flex flex-wrap` div — so below lg it scrolls-not-wraps,
      and at lg it reverts to flex-wrap (ChipRail's lg: triplet).
    - The five `Button`s (`‹`, `›`, `Today`, and the toggle chips) render with `size="touch"` (≥44px
      tall below lg). The existing `data-testid`/`aria-pressed`/`aria-label`/handler wiring on every
      button and the date `<input>` is unchanged (assert an existing toggle testid still resolves and a
      step handler still fires).
    - The item gap/label/order is preserved; each direct child that should snap gets `snap-start
      shrink-0` per the ChipRail contract.
  </behavior>
  <action>
    STEP 1 (RED): extend PayoffControls.test.tsx to assert the strip is wrapped in a `role="group"` named
    "Chart date and series controls" and that a toggle button's class string includes the touch height
    token (`min-h-11`). RUN it; it fails (today the wrapper is a plain div and buttons are default `xs`).
    Show the RED run.
    STEP 2 (GREEN): replace the outer `flex flex-wrap` div with `<ChipRail ariaLabel="Chart date and
    series controls" className="mb-2 items-center font-mono text-[9px] text-dim">` (import ChipRail from
    ../system/index.tsx); add `size="touch"` to the `‹`, `›`, `Today`, and each toggle `Button`; add
    `snap-start shrink-0` to the direct children per the ChipRail child contract. Keep the date `<input>`
    and all testids/handlers exactly as they are. No `any`/`as`/`!`. Run GREEN, and run the existing
    PayoffChart/Analyzer/Overview suites for this component to confirm no consumer regressed.
    Do NOT touch `PayoffChart.tsx` or any chart internals (D-10) — only the control strip changes.
  </action>
  <verify>
    <automated>cd apps/web && bunx vitest run src/components/charts/PayoffControls.test.tsx && cd ../.. && bun run typecheck</automated>
  </verify>
  <human-check>
    At 390px (chrome-devtools) on Overview and Analyzer: the date/toggle strip scrolls horizontally
    instead of wrapping; date-step/Today/toggle buttons measure ≥44px tall (box-model). At 1024px+: the
    strip is flex-wrap exactly as before (never scroll-snap).
  </human-check>
  <done>PayoffControls scrolls-not-wraps below lg via ChipRail with 44px-tall buttons, desktop flex-wrap unchanged; component test green, typecheck clean; both chart mounts inherit the fix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Presentation-only primitives — no new inputs, endpoints, auth, or data-layer surface (RESEARCH Security Domain) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-01 | Tampering | ChipRail/Button class strings | low | accept | Pure Tailwind class composition over trusted internal props; no user-controlled input reaches a class name or style; malformed props are a compile-time type error, not a runtime injection |
| T-35-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs this phase (RESEARCH Package Legitimacy Audit = N/A, zero new deps) — no supply-chain surface to mitigate |
</threat_model>

<verification>
- `cd apps/web && bunx vitest run src/components/system/ChipRail.test.tsx src/components/system/system.test.tsx src/components/charts/PayoffControls.test.tsx` all green.
- `bun run typecheck` clean (no `any`/`as`/`!`; `SIZE_CLASS` exhaustive over the widened `ButtonSize`).
- `bun run lint` clean for the changed files.
- Manual (end-of-phase UAT, per 35-06): PayoffControls scrolls-not-wraps at 390px on both Overview and
  Analyzer; touch buttons ≥44px; flex-wrap unchanged at 1024px+.
</verification>

<success_criteria>
- `ChipRail` and `Button size="touch"` exist as shared primitives with guaranteed `lg:` reverts to
  today's exact desktop classes.
- `PayoffControls`' wrap bug (CONTEXT item 4) is fixed for both chart mounts through the one shared edit.
- Every changed control keeps its existing testids/handlers/aria wiring; desktop is byte-identical.
</success_criteria>

<output>
Create `.planning/phases/35-mobile-experience-redesign-the-phone-view-is-desktop-panels-/35-01-SUMMARY.md` when done
</output>
