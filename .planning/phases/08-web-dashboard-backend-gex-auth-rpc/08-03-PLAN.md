---
phase: 08-web-dashboard-backend-gex-auth-rpc
plan: 03
type: tdd
wave: 2
depends_on: ["08-02"]
files_modified:
  - packages/core/src/analytics/domain/gex.ts
  - packages/core/src/analytics/domain/gex.test.ts
autonomous: true
requirements: [GEX-01]
must_haves:
  truths:
    - "GEX domain math reproduces the playground-v3 oracle on equivalent fixture input: γ-flip ≈ 7488, netGammaAtSpot ≈ -47 $Bn/1%, callWall = 7600, putWall = 7400 (SC-1/SC-2 numerics)"
    - "findFlip returns null when the cumulative gamma profile never crosses zero (no false flip)"
  artifacts:
    - path: "packages/core/src/analytics/domain/gex.ts"
      provides: "Pure GEX math: dollarGamma, strikeGex, findFlip, buildProfile"
      contains: "export function findFlip"
      min_lines: 40
  key_links:
    - from: "packages/core/src/analytics/domain/gex.ts"
      to: "packages/core/src/journal/domain/bsm.ts"
      via: "buildProfile re-prices gamma across the spot grid via bsmGreeks (reuse, do NOT re-derive)"
      pattern: "bsmGreeks"
  prohibitions:
    - "BSM gamma math is NOT re-derived — buildProfile reuses bsmGreeks from journal/domain/bsm.ts"
    - "gex.ts imports ONLY @morai/shared + the intra-core bsm.ts — NOT drizzle/zod/pg-boss/contracts (hexagon §2)"
    - "callWall and putWall are NOT both computed as max-absolute-value — callWall = argmax(positive GEX), putWall = argmin(most-negative GEX)"
---

<objective>
Implement the pure GEX domain math that turns the canonical RED oracle test (08-02 Task 3) GREEN.

GEX is the only file in this phase with no exact analog — the reference is the playground-v3
prototype (oracle algorithm) and the gex-snapshot.json / gex-profile.json fixtures (oracle values).
The math reuses the existing, property-tested BSM greeks engine for the profile-grid re-pricing; it
does not re-derive gamma.

Purpose: A pure, property-tested domain that the compute use-case (08-05) composes — flip level,
walls, per-strike GEX, and the net-gamma profile across a spot grid.
Output: `packages/core/src/analytics/domain/gex.ts` with `dollarGamma`, `strikeGex`, `findFlip`,
`buildProfile`, turning `gex.test.ts` GREEN against the oracle.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-web-dashboard-backend-gex-auth-rpc/08-RESEARCH.md
@.planning/phases/08-web-dashboard-backend-gex-auth-rpc/08-PATTERNS.md
@packages/core/src/journal/domain/bsm.ts
@packages/core/src/analytics/domain/gex.test.ts
@mockups/gex-snapshot.json
@mockups/gex-profile.json
@mockups/playground-v3.html
@.claude/rules/architecture-boundaries.md
@.claude/rules/typescript.md
@.claude/rules/tdd.md
</context>

## Artifacts this phase produces

This plan produces the GEX domain functions `dollarGamma`, `strikeGex`, `findFlip`, `buildProfile`
in `packages/core/src/analytics/domain/gex.ts`. (Full-phase artifact list in 08-02.)

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: GEX domain math — turn the oracle RED test GREEN (GEX-01)</name>
  <files>packages/core/src/analytics/domain/gex.ts, packages/core/src/analytics/domain/gex.test.ts</files>
  <read_first>
    - packages/core/src/analytics/domain/gex.test.ts (the RED scaffold from 08-02 — the locked GREEN target; read its exact assertions + fixture wiring before implementing)
    - packages/core/src/journal/domain/bsm.ts (the bsmGreeks signature to reuse: bsmGreeks(spot, strike, dte, iv, r, q, type) → BsmGreeks; the pure-function math style, no class)
    - packages/core/src/analytics/domain/risk-reversal.ts (intra-analytics domain function style — pure exports, readonly inputs, Result-free pure math)
    - mockups/playground-v3.html (the oracle algorithm — gamma sum, spot-grid profile, flip zero-crossing, wall argmax/argmin)
    - mockups/gex-snapshot.json (per-strike oracle: callWall {k:7600, gex:1230277553}, putWall {k:7400, gex:-5974395559}) + mockups/gex-profile.json (flip 7488, atSpot -47)
    - .planning/phases/08-web-dashboard-backend-gex-auth-rpc/08-RESEARCH.md (Pattern 5 algorithm + Code Examples "GEX domain computation sketch" + Pitfalls 3/4/5)
  </read_first>
  <behavior>
    - dollarGamma(gamma, oi, spot) returns gamma × oi × spot² × 0.01 × 100 / 1e9 ($Bn/1%); monotonic increasing in oi for fixed positive gamma/spot (fast-check property).
    - strikeGex(contracts, spot) returns a per-strike net-GEX map where calls contribute positive and puts negative; aggregating the oracle fixture yields callWall = 7600 (argmax positive) and putWall = 7400 (argmin / most-negative).
    - buildProfile(contracts, spotGrid) re-prices net gamma at each grid spot via bsmGreeks and returns the {s, g} grid; the curve reproduces netGammaAtSpot ≈ -47 at the current spot.
    - findFlip(grid) returns the linear-interpolated zero-crossing ≈ 7488 on the oracle grid, and returns null when the profile never changes sign (Pitfall 5).
  </behavior>
  <action>
    Implement gex.ts as pure exported functions (no class, no side effects). `dollarGamma(gamma:
    number, oi: number, spot: number): number` per the RESEARCH formula (SPX multiplier 100, spot²×0.01
    scaling, ÷1e9 for $Bn/1%). `strikeGex(contracts: ReadonlyArray<ContractObs>, spot: number):
    Map<number, number>` summing signed contributions (calls +, puts −) keyed by strike. `findFlip(grid:
    ReadonlyArray<{ s: number; g: number }>): number | null` scanning adjacent pairs for a sign change
    and linearly interpolating the zero-crossing; null when none (Pitfall 5). `buildProfile(contracts:
    ReadonlyArray<ContractObs>, spotGrid: ReadonlyArray<number>): ReadonlyArray<{ s: number; g: number }>`
    calling `bsmGreeks(S, K, T, bsmIv, r, q, type)` per contract per grid spot and summing dollar gamma —
    reuse bsmGreeks from ../../journal/domain/bsm.ts (intra-core, same package — allowed by §2; confirm
    eslint boundaries permit the intra-package import). Define a local `ContractObs` input row type
    (contractType, strike in points, expiration, openInterest, bsmGamma, bsmIv, dte). Walls: callWall =
    argmax over strikes with positive net GEX; putWall = argmin (most negative) — keep them distinct
    (Pitfall 4). Import ONLY @morai/shared + the bsm.ts intra-core path. Run the 08-02 RED test; show it
    flips to GREEN. Add any missing fast-check property assertions to gex.test.ts if the scaffold left
    them as TODO (fc.float bounds need Math.fround per the v4 precedent).
  </action>
  <verify>
    <automated>bun run test packages/core/src/analytics/domain/gex.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test packages/core/src/analytics/domain/gex.test.ts` exits 0; the oracle assertions (flip ≈ 7488, netGammaAtSpot ≈ -47, callWall 7600, putWall 7400) pass within tolerance.
    - `rg -n "bsmGreeks" packages/core/src/analytics/domain/gex.ts` matches (BSM reused, not re-derived).
    - `rg -v '^//' packages/core/src/analytics/domain/gex.ts | rg -c "from \"drizzle|from \"zod|from \"pg-boss|@morai/contracts"` returns 0 (hexagon §2: only @morai/shared + intra-core bsm).
    - `findFlip([... all-negative grid ...])` returns null (tested).
    - `bun run typecheck && bun run lint` exit 0 (including the intra-core bsm import passing boundary rules).
    - GREEN-after-RED is shown: the 08-02 scaffold failed on the missing module before this task, passes after (record in SUMMARY).
  </acceptance_criteria>
  <done>gex.ts implements dollarGamma/strikeGex/findFlip/buildProfile reproducing the playground-v3 oracle (flip 7488, net -47, walls 7600/7400); reuses bsmGreeks; imports only @morai/shared + intra-core bsm; the 08-02 RED domain test is GREEN; typecheck + lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — pure domain) | gex.ts is pure math in the hexagon; it crosses no I/O or network boundary. Inputs arrive already-parsed from the use-case. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Tampering | GEX numeric correctness | mitigate | Oracle fixture test + fast-check property test lock the math to the validated playground-v3 numbers; a regression that mis-signs walls or drops the flip-null case fails the suite. |
| T-08-04 | Denial of Service | profile-grid re-pricing cost | accept | ~500 contracts × ~55 grid points ≈ 28ms per run (RESEARCH Pitfall 3); in-memory after a single read; acceptable at this scale. |

ASVS L1; block on HIGH. No HIGH-severity threats — pure domain, no untrusted input crossing here.
</threat_model>

<verification>
- `bun run test packages/core/src/analytics/domain/gex.test.ts` green against the oracle.
- `bun run typecheck && bun run lint` green (intra-core bsm import allowed; no forbidden imports).
</verification>

<success_criteria>
GEX domain math (GEX-01 compute numerics) reproduces the oracle; the canonical RED test is GREEN;
the compute use-case (08-05) can now compose it.
</success_criteria>

<output>
Create `.planning/phases/08-web-dashboard-backend-gex-auth-rpc/08-03-SUMMARY.md` when done
</output>
