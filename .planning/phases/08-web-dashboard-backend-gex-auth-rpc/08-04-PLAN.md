---
phase: 08-web-dashboard-backend-gex-auth-rpc
plan: 04
type: execute
wave: 2
depends_on: ["08-02"]
files_modified:
  - packages/adapters/src/postgres/migrations/0008_gex_snapshot.sql
  - packages/adapters/src/postgres/migrations/meta/_journal.json
  - packages/adapters/src/postgres/migrations/meta/0008_snapshot.json
autonomous: false
requirements: [GEX-01]
must_haves:
  truths:
    - "A Drizzle migration 0008_gex_snapshot.sql creates the gex_snapshots table with cycle_time PK + JSONB array columns + RLS enabled"
    - "The migration applies cleanly to a real Postgres (testcontainers in 08-05; live Supabase per operator)"
  artifacts:
    - path: "packages/adapters/src/postgres/migrations/0008_gex_snapshot.sql"
      provides: "CREATE TABLE gex_snapshots (cycle_time PK, numeric scalars, jsonb arrays) + ENABLE ROW LEVEL SECURITY"
      contains: "gex_snapshots"
  key_links:
    - from: "packages/adapters/src/postgres/migrations/0008_gex_snapshot.sql"
      to: "packages/adapters/src/postgres/schema.ts"
      via: "drizzle-kit generate emits SQL from the gexSnapshots table definition (08-02)"
      pattern: "gex_snapshots"
  prohibitions:
    - "The migration is NOT generated before the gexSnapshots schema (08-02) exists"
    - "profile/strikes/by_expiry columns are NOT split into separate rows — jsonb columns only"
---

<objective>
Generate and apply the Drizzle migration for the `gex_snapshots` table (GEX-01 / SC-2 idempotency
anchor). This is the [BLOCKING] schema-push task: `bun run typecheck`/build pass WITHOUT the live
table (types come from the Drizzle schema, not the live DB), so without an explicit migration +
push step the phase enters a false-positive verification state.

Purpose: Materialize the `gex_snapshots` table so the repo integration test (08-05) and the live
endpoint have a real table backing the `onConflictDoNothing` idempotency on `cycle_time`.
Output: `0008_gex_snapshot.sql` + updated drizzle meta journal; the migration applied to a real
Postgres (verified via testcontainers in 08-05; live Supabase push gated to the operator).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-web-dashboard-backend-gex-auth-rpc/08-PATTERNS.md
@packages/adapters/src/postgres/schema.ts
@packages/adapters/src/postgres/migrations/0007_analytics_observations.sql
@.claude/rules/workflow.md
</context>

## Artifacts this phase produces

This plan produces `0008_gex_snapshot.sql` + the drizzle meta journal entry materializing the
`gex_snapshots` table (`cycle_time` PK; `spot`/`net_gamma_at_spot` numeric NOT NULL; `flip` numeric,
`call_wall`/`put_wall` integer nullable; `profile`/`strikes`/`by_expiry` jsonb NOT NULL; RLS
enabled). (Full-phase artifact list in 08-02.)

<tasks>

<task type="auto">
  <name>Task 1: Generate 0008_gex_snapshot.sql from the gexSnapshots schema</name>
  <files>packages/adapters/src/postgres/migrations/0008_gex_snapshot.sql, packages/adapters/src/postgres/migrations/meta/_journal.json, packages/adapters/src/postgres/migrations/meta/0008_snapshot.json</files>
  <read_first>
    - packages/adapters/src/postgres/schema.ts (the gexSnapshots table from 08-02 — the source drizzle-kit generates from)
    - packages/adapters/src/postgres/migrations/0007_analytics_observations.sql (the exact migration analog — CREATE TABLE + `--> statement-breakpoint` + `ENABLE ROW LEVEL SECURITY` form)
    - packages/adapters/src/postgres/migrations/meta/_journal.json (the journal format; 0007 is the latest entry — the new entry tags 0008)
    - .planning/phases/08-web-dashboard-backend-gex-auth-rpc/08-PATTERNS.md (migration section — the expected gex_snapshots DDL)
    - MEMORY: Phase 6 06-02 + Phase 5 precedent — drizzle-kit emits a random-word filename; rename to 0008_gex_snapshot.sql and update the meta journal tag to match.
  </read_first>
  <action>
    Run the project's drizzle-kit generate step to emit a migration from the gexSnapshots schema
    (08-02). Following the Phase 6 06-02 / Phase 5 precedent: rename the drizzle-kit-generated
    random-word SQL file to `0008_gex_snapshot.sql` and update `meta/_journal.json` (and the generated
    `meta/0008_snapshot.json`) so the journal tag matches the renamed file. Verify the emitted SQL is:
    `CREATE TABLE "gex_snapshots" ("cycle_time" timestamp with time zone PRIMARY KEY, "spot" numeric
    NOT NULL, "flip" numeric, "call_wall" integer, "put_wall" integer, "net_gamma_at_spot" numeric NOT
    NULL, "profile" jsonb NOT NULL, "strikes" jsonb NOT NULL, "by_expiry" jsonb NOT NULL);` followed by
    `--> statement-breakpoint` and `ALTER TABLE "gex_snapshots" ENABLE ROW LEVEL SECURITY;`. If the meta
    snapshot for the prior migration is missing (the Phase 5→6 06-02 gotcha where 0006's snapshot json
    was absent), reconstruct only what drizzle-kit needs so 0008 contains ONLY the gex_snapshots table
    — no spurious diffs from other tables.
  </action>
  <verify>
    <automated>rg -n "gex_snapshots|ENABLE ROW LEVEL SECURITY|jsonb" packages/adapters/src/postgres/migrations/0008_gex_snapshot.sql</automated>
  </verify>
  <acceptance_criteria>
    - `packages/adapters/src/postgres/migrations/0008_gex_snapshot.sql` exists and contains `CREATE TABLE "gex_snapshots"` with `cycle_time` PRIMARY KEY, three `jsonb` columns, and `ENABLE ROW LEVEL SECURITY`.
    - The migration contains ONLY the gex_snapshots table (no unrelated table diffs) — `rg -c "CREATE TABLE" packages/adapters/src/postgres/migrations/0008_gex_snapshot.sql` returns 1.
    - `meta/_journal.json` has a new entry tagged for 0008 matching the renamed filename.
    - `bun run typecheck` exits 0.
  </acceptance_criteria>
  <done>0008_gex_snapshot.sql defines the gex_snapshots table (cycle_time PK, jsonb arrays, RLS) and the drizzle meta journal is updated; the migration is single-table and clean.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Apply the migration (push)</name>
  <files>packages/adapters/src/postgres/migrations/0008_gex_snapshot.sql</files>
  <read_first>
    - apps/worker/src/migrate.ts (the project migrator entrypoint — `bun run apps/worker/src/migrate.ts`; this project does NOT use `npx drizzle-kit push`)
    - MEMORY: Phase 1 deploy note — live Supabase migrations apply via the session pooler (port 5432, max:1), DDL-safe; the transaction pooler (6543) is NOT migration-safe.
    - MEMORY: Phases 03/04/05/06 precedent — live-production Supabase migrate is DEFERRED to the operator; the migration is validated against a postgres:16 testcontainer in tests. The repo idempotency integration test in 08-05 exercises 0008 against an ephemeral Postgres.
  </read_first>
  <action>
    Apply the 0008 migration. The testcontainers integration test in 08-05 already applies all
    migrations (including 0008) against an ephemeral postgres:16 and is the automated proof the
    migration is well-formed + idempotent. For the LIVE Supabase database, follow the established
    Phases 03/04/05/06 precedent: live-production migrate is operator-gated (this task is
    `autonomous: false`). When applying live, use `bun run apps/worker/src/migrate.ts` against the
    SESSION pooler (port 5432, max:1) — NEVER `npx drizzle-kit push`, never the transaction pooler.
    The migrator is idempotent — re-running is a no-op. Record in the SUMMARY whether the live push
    was performed now or deferred to the operator (Deferred Items table), mirroring the 06-02 entry.
  </action>
  <verify>
    <automated>rg -n "gex_snapshots" packages/adapters/src/postgres/migrations/0008_gex_snapshot.sql && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The migration file is present and well-formed: `rg -n "gex_snapshots" packages/adapters/src/postgres/migrations/0008_gex_snapshot.sql` matches (this is the proof the migration exists — typecheck alone passes whether or not the migration ran, since types come from the Drizzle schema, not the live DB; the testcontainers idempotency proof stays in 08-05).
    - Either: (a) the migrator ran successfully against the target DB and `gex_snapshots` exists (operator confirms), OR (b) the live push is explicitly recorded as DEFERRED to the operator in the SUMMARY + Deferred Items, with the testcontainers run (08-05) standing as the automated migration proof — matching the 03/04/05/06 precedent.
    - The migrator command documented in the SUMMARY is `bun run apps/worker/src/migrate.ts` (NOT drizzle-kit push).
    - `bun run typecheck` exits 0.
  </acceptance_criteria>
  <done>The 0008 migration is applied (or explicitly operator-deferred per precedent); the testcontainers run in 08-05 is the automated migration+idempotency proof; the live-push status is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migrator → live Postgres | DDL against the live Supabase DB; must use the DDL-safe session pooler, operator-gated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Tampering | live migration application | mitigate | Live push is operator-gated (autonomous:false) via the DDL-safe session pooler; idempotent migrator means re-run is a no-op (no accidental double-apply). |
| T-08-06 | Information Disclosure | gex_snapshots RLS | mitigate | Table created with `ENABLE ROW LEVEL SECURITY` matching every other table; no anon read path. |
| T-08-FP | Repudiation | false-positive verification | mitigate | This BLOCKING task exists precisely because typecheck/build pass without the live table; explicit push + testcontainers proof closes the false-green gap. |

ASVS L1; block on HIGH. The live-DDL threat is mitigated by operator gating + session-pooler
discipline; no HIGH-severity unmitigated threat.
</threat_model>

<verification>
- `0008_gex_snapshot.sql` is single-table, well-formed, RLS-enabled.
- The 08-05 testcontainers run applies 0008 and proves insert + read + idempotency.
- Live push performed or operator-deferred per precedent, recorded in SUMMARY.
</verification>

<success_criteria>
The gex_snapshots table is materialized (testcontainers proven; live operator-gated) — SC-2's
idempotency anchor (`cycle_time` PK) exists in a real Postgres.
</success_criteria>

<output>
Create `.planning/phases/08-web-dashboard-backend-gex-auth-rpc/08-04-SUMMARY.md` when done
</output>
