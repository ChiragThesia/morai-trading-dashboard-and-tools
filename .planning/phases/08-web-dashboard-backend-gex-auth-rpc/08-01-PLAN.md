---
phase: 08-web-dashboard-backend-gex-auth-rpc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture/stack-decisions.md
  - docs/TOPIC-MAP.md
autonomous: true
requirements: [AUTH-01]
must_haves:
  truths:
    - "stack-decisions.md records Supabase Auth + CORS as a DECIDED stack decision (no longer deferred)"
    - "The decision documents HS256 JWT verification via hono/jwt (offline, no Supabase SDK coupling) and exact-origin CORS"
  artifacts:
    - path: "docs/architecture/stack-decisions.md"
      provides: "Un-deferred Supabase Auth decision (D18 extension or new ADR row)"
      contains: "Supabase Auth"
  key_links: []
  prohibitions:
    - "No auth or CORS CODE is written in this plan — docs only (D-02a ordering gate)"
    - "The Supabase-Auth 'revisit trigger / deferred' wording must NOT remain as the live status for Auth"
---

<objective>
Un-defer the Supabase-Auth decision in the architecture source-of-truth BEFORE any auth code lands.

Per the project workflow rule (docs-before-architecture) and CONTEXT decision D-02a, the
Supabase-Auth deferral recorded in `docs/architecture/stack-decisions.md` (D18 + the
deferred-features note whose "revisit trigger" was *a concrete need for Supabase Auth*) is being
un-deferred this phase. This plan updates the decision record so the auth-code plan (08-07) can
cite a DECIDED decision rather than contradict a deferred one.

Purpose: Satisfy the mandatory docs-before-code ordering gate. 08-07 (auth middleware) `depends_on`
this plan — auth code that contradicts the live docs is a bug per workflow.md Order of Authority.
Output: An extended/added decision in stack-decisions.md recording Supabase Auth + CORS as decided,
with the HOW (HS256 offline JWT verify, exact-origin CORS) and swap-cost note; TOPIC-MAP unchanged
unless a new file is added.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-web-dashboard-backend-gex-auth-rpc/08-CONTEXT.md
@.planning/phases/08-web-dashboard-backend-gex-auth-rpc/08-RESEARCH.md
@.claude/rules/workflow.md
@.claude/rules/docs.md
</context>

## Artifacts this phase produces

This is the documentation-gate plan; it produces no code symbols. The full phase produces:
`AppType` (apps/server), `gexWallEntry`/`gexSnapshotEntry`/`gexSnapshotResponse` + types
(packages/contracts/src/gex.ts), `gex_snapshots` table (`cycle_time` PK, `spot`, `flip`,
`call_wall`, `put_wall`, `net_gamma_at_spot`, `profile`/`strikes`/`by_expiry` JSONB),
`computeGexSnapshot`/`getGex` use-cases + ports (`ForReadingLegObsForGex`, `ForReadingGexSnapshot`,
`ForPersistingGexSnapshot`, `ForRunningComputeGexSnapshot`, `ForRunningGetGex`), GEX domain
functions (`dollarGamma`, `strikeGex`, `findFlip`, `buildProfile`), the `compute-gex-snapshot`
job, the `get_gex` MCP tool, the Supabase-Auth JWT middleware group + CORS middleware, and new env
vars `SUPABASE_JWT_SECRET` + `WEB_ORIGIN`.

<tasks>

<task type="auto">
  <name>Task 1: Un-defer Supabase Auth in stack-decisions.md</name>
  <files>docs/architecture/stack-decisions.md, docs/TOPIC-MAP.md</files>
  <read_first>
    - docs/architecture/stack-decisions.md (the file being modified — read D18 section body around line 171, D19 around line 28/202, and the deferred-features note with the "revisit trigger: a concrete need for Supabase Auth" wording)
    - .planning/phases/08-web-dashboard-backend-gex-auth-rpc/08-CONTEXT.md (D-02, D-02a — the source decision)
    - .planning/phases/08-web-dashboard-backend-gex-auth-rpc/08-RESEARCH.md (Pattern 8 "docs-before-code update" + Pattern 1 for the HS256 rationale and swap-cost framing)
    - .claude/rules/docs.md (single-source-of-truth, Hemingway prose, no duplication)
  </read_first>
  <action>
    Extend the D18 section body (and/or add a new dated decision entry following the existing
    decision-row style) in docs/architecture/stack-decisions.md to record that Supabase Auth is now
    DECIDED for Phase 8: what changed (un-defer Auth on the read endpoints), why (the web dashboard
    on Vercel reaches the previously-unauthenticated brokerage/journal/analytics data over CORS — that
    data becomes internet-reachable, so login gates it; single trader account, signups closed), and
    HOW it is implemented (offline HS256 JWT verification via Hono's built-in `hono/jwt` middleware
    using the Supabase project JWT secret — NOT the Supabase SDK, NOT RLS, NOT a per-request network
    call; CORS via `hono/cors` restricted to an exact `WEB_ORIGIN`, never wildcard-with-credentials).
    Record the swap cost as low (still just a stateless JWT verify behind a middleware seam,
    swappable to any auth provider). Reference Phase 8 and CONTEXT D-02. Remove or supersede the
    "deferred / revisit trigger: a concrete need for Supabase Auth" status for Auth so the live doc
    no longer reads as deferred. Keep the D5/D11/D18 "Supabase = just Postgres for the DB connection"
    statements intact — only the Auth deferral is being lifted. Prose follows Hemingway style.
    If a standalone ADR file is added instead of extending D18, add its row to docs/TOPIC-MAP.md per
    docs.md; if only D18 is extended in place, TOPIC-MAP needs no change.
  </action>
  <verify>
    <automated>rg -n "Supabase Auth" docs/architecture/stack-decisions.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Supabase Auth" docs/architecture/stack-decisions.md` returns at least one match in a DECIDED context (not in a "deferred" sentence).
    - `rg -n "HS256" docs/architecture/stack-decisions.md` matches (the HOW is recorded).
    - The phrase asserting Auth is deferred / "revisit trigger: a concrete need for Supabase Auth" no longer describes the current Auth status (grep the old wording; it is removed or explicitly marked superseded).
    - `bun run lint` and `bun run typecheck` still pass (docs-only change — no code touched).
  </acceptance_criteria>
  <done>stack-decisions.md records Supabase Auth + CORS as a decided Phase-8 stack decision with HS256-offline-verify + exact-origin-CORS rationale and low swap cost; the prior deferral wording for Auth is gone or superseded; no code changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — docs only) | This plan edits documentation; it crosses no runtime trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-DOC | Repudiation | architecture decision record | mitigate | Record the auth decision (who/why/how) in the source-of-truth so the later auth code is traceable to a decided rationale (workflow.md docs-before-code). |

ASVS L1; block on HIGH. No HIGH-severity runtime threats in a docs-only change. The load-bearing
auth/CORS threats (unauthenticated brokerage read, JWT forging, CORS-wildcard-with-credentials, JWT
secret in logs, stale-data leak) are mitigated by the code plans 08-05/08-07 and enumerated there.
</threat_model>

<verification>
- `rg -n "Supabase Auth" docs/architecture/stack-decisions.md` matches in a decided context.
- `bun run typecheck && bun run lint` pass (no code changed).
</verification>

<success_criteria>
The Supabase-Auth deferral is lifted in the architecture source-of-truth, unblocking the auth-code
plan (08-07) which depends on this plan. (Maps to SC-4 prerequisite.)
</success_criteria>

<output>
Create `.planning/phases/08-web-dashboard-backend-gex-auth-rpc/08-01-SUMMARY.md` when done
</output>
