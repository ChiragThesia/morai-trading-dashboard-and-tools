---
phase: 08-web-dashboard-backend-gex-auth-rpc
plan: 06
type: tdd
wave: 4
depends_on: ["08-05"]
files_modified:
  - apps/worker/src/handlers/compute-gex-snapshot.ts
  - apps/worker/src/handlers/compute-gex-snapshot.test.ts
  - apps/worker/src/handlers/compute-analytics.ts
  - apps/worker/src/schedule.ts
  - apps/worker/src/main.ts
autonomous: true
requirements: [GEX-01]
must_haves:
  truths:
    - "A compute-gex-snapshot pg-boss job is registered and chain-triggered serially after compute-analytics each RTH snapshot cycle (D-01)"
    - "The handler applies the RTH + NYSE-holiday gate before computing — never writes a GEX row outside RTH or on a holiday"
    - "compute-gex-snapshot appears in the worker's registered queues (10 queues total)"
  artifacts:
    - path: "apps/worker/src/handlers/compute-gex-snapshot.ts"
      provides: "makeComputeGexSnapshotHandler — thin pg-boss adapter over the computeGexSnapshot use-case"
      contains: "makeComputeGexSnapshotHandler"
  key_links:
    - from: "apps/worker/src/handlers/compute-analytics.ts"
      to: "apps/worker/src/handlers/compute-gex-snapshot.ts"
      via: "compute-analytics boss.send(compute-gex-snapshot) on success (serial chain, D-01)"
      pattern: "compute-gex-snapshot"
    - from: "apps/worker/src/main.ts"
      to: "packages/core (makeComputeGexSnapshotUseCase) + packages/adapters (gex repo)"
      via: "composition root wires the use-case + repo into the handler"
      pattern: "makeComputeGexSnapshotUseCase|GexSnapshot"
  prohibitions:
    - "compute-gex-snapshot has NO cron schedule — chain-triggered ONLY by compute-analytics (not a standalone timer)"
    - "The handler contains ZERO business logic — array-guard → RTH/holiday gate → use-case call → throw-on-err (architecture-boundaries §3)"
    - "The existing fetch-schwab-chain / sync-fills / refresh-tokens schedules are NOT altered"
---

<objective>
Add the `compute-gex-snapshot` pg-boss job: a thin handler over the 08-05 compute use-case, chained
serially after `compute-analytics` (so BSM gamma is filled by the time GEX computes), behind the
existing RTH + holiday gate. Wire it in the worker composition root and register its queue.

Per D-01 the GEX snapshot is produced by the scheduled job chain, not on read. Per RESEARCH Open
Question 2 the chain position is serial after `compute-analytics` (the current terminal job): the
linear chain becomes fetch-chain → bsm-greeks → snapshot-calendars → compute-analytics →
compute-gex-snapshot.
Output: `compute-gex-snapshot.ts` handler (+ test), the chain trigger added to
`compute-analytics.ts`, the queue + work registration in `schedule.ts`, and the use-case+repo+handler
wiring in worker `main.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-web-dashboard-backend-gex-auth-rpc/08-RESEARCH.md
@.planning/phases/08-web-dashboard-backend-gex-auth-rpc/08-PATTERNS.md
@apps/worker/src/handlers/compute-analytics.ts
@apps/worker/src/handlers/snapshot-calendars.ts
@apps/worker/src/schedule.ts
@apps/worker/src/main.ts
@.claude/rules/architecture-boundaries.md
@.claude/rules/tdd.md
</context>

## Artifacts this phase produces

This plan produces the `compute-gex-snapshot` job: `makeComputeGexSnapshotHandler`
(apps/worker/src/handlers/compute-gex-snapshot.ts), the `compute-gex-snapshot` queue registration in
schedule.ts (10th queue, chain-triggered, no cron), and the worker composition-root wiring. (Full
artifact list in 08-02.)

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: compute-gex-snapshot handler (RTH gate + use-case call)</name>
  <files>apps/worker/src/handlers/compute-gex-snapshot.ts, apps/worker/src/handlers/compute-gex-snapshot.test.ts</files>
  <read_first>
    - apps/worker/src/handlers/compute-analytics.ts (the EXACT analog — array-guard, RTH+holiday gate, use-case call, throw-on-err; this is a terminal-style handler, so no boss.send inside it unless chaining further — GEX is the new terminal, so no boss.send)
    - apps/worker/src/handlers/snapshot-calendars.ts (the chain-trigger shape if a future chain is needed — NOT needed here; GEX is terminal)
    - packages/core/src/analytics/application/computeGexSnapshot.ts (08-05 — ForRunningComputeGexSnapshot the handler wraps)
    - .planning/phases/08-web-dashboard-backend-gex-auth-rpc/08-PATTERNS.md (compute-gex-snapshot.ts handler section — copy compute-analytics verbatim with the rename)
    - MEMORY: Phase 3 execution lesson — executor may grep-verify per plan rather than show a literal RED commit; still run the test and show it red→green.
  </read_first>
  <behavior>
    - Handler no-ops (warn, no use-case call) when `now` is outside RTH or on an NYSE holiday (gate before the use-case).
    - On an in-RTH non-holiday job, the handler calls computeGexSnapshotUseCase exactly once.
    - When the use-case returns err, the handler throws (signals pg-boss failure for retry/alerting).
    - Handles the pg-boss v12 undefined array element (array-guard).
  </behavior>
  <action>
    Create makeComputeGexSnapshotHandler(deps) copying compute-analytics.ts verbatim, renaming
    computeAnalytics → computeGexSnapshot. deps = { computeGexSnapshotUseCase: ForRunningComputeGexSnapshot;
    now: () => Date }. Keep the array-guard (`if (job === undefined) return`), the `!isWithinRth(now) ||
    isNyseHoliday(now)` gate with a `compute-gex-snapshot: skipping — outside RTH or NYSE holiday` warn,
    the single use-case call, and `throw new Error(result.error.message)` on err. GEX is the NEW terminal
    job — no boss.send inside. Write the test first (RED) injecting a fake now() (in-RTH and off-RTH) +
    a spy use-case asserting call-count 0 (gated) / 1 (in-RTH) and throw-on-err; show RED→GREEN.
  </action>
  <verify>
    <automated>bun run test apps/worker/src/handlers/compute-gex-snapshot.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test apps/worker/src/handlers/compute-gex-snapshot.test.ts` exits 0 with the gate (call-count 0 off-RTH), call (call-count 1 in-RTH), and throw-on-err assertions passing.
    - `rg -n "isWithinRth|isNyseHoliday" apps/worker/src/handlers/compute-gex-snapshot.ts` matches (gate present).
    - `rg -c "boss.send" apps/worker/src/handlers/compute-gex-snapshot.ts` returns 0 (terminal — no further chain).
    - `bun run typecheck && bun run lint` exit 0.
  </acceptance_criteria>
  <done>The compute-gex-snapshot handler mirrors compute-analytics (array-guard + RTH/holiday gate + single use-case call + throw-on-err), is terminal (no boss.send), and its test is green.</done>
</task>

<task type="auto">
  <name>Task 2: Chain trigger + queue registration + worker wiring</name>
  <files>apps/worker/src/handlers/compute-analytics.ts, apps/worker/src/schedule.ts, apps/worker/src/main.ts</files>
  <read_first>
    - apps/worker/src/handlers/compute-analytics.ts (the file being modified — add the fire-and-forget boss.send to compute-gex-snapshot on success; copy the snapshot-calendars.ts boss.send pattern: void deps.boss.send("compute-gex-snapshot", {}, { singletonKey: "triggered-by-analytics" }).catch(...). This requires adding a `boss: BossForChainHandler` dep to compute-analytics, mirroring snapshot-calendars — it is no longer terminal.)
    - apps/worker/src/handlers/snapshot-calendars.ts (the exact boss.send chain-trigger + BossForChainHandler import + .catch warn pattern to copy)
    - apps/worker/src/schedule.ts (the file being modified — AllHandlers type lines 45-55; the createQueue list lines 68-77; the work list lines 126-134; add the 10th queue compute-gex-snapshot in the createQueue phase, NO schedule cron, and a work registration; update the boot warn string from "9 queues" to "10 queues")
    - apps/worker/src/main.ts (the file being modified — the computeAnalytics use-case + handler wiring block ~lines 188-235; the registerAllJobs call ~line 404; wire makeComputeGexSnapshotUseCase with the gex repo's readLegObs+persist, build makeComputeGexSnapshotHandler, add boss to the compute-analytics handler deps, and add computeGexSnapshot to the registerAllJobs handler map)
    - packages/adapters exports (the gex repo factory name from 08-05 — e.g. makePostgresGexSnapshotRepo — for the @morai/adapters import) + packages/core export makeComputeGexSnapshotUseCase
    - .planning/phases/08-web-dashboard-backend-gex-auth-rpc/08-RESEARCH.md (Open Question 2 — serial chain after compute-analytics) + 08-PATTERNS.md (schedule.ts section)
  </read_first>
  <action>
    Three additive changes. (1) compute-analytics.ts: add a `boss: BossForChainHandler` dep (import the
    type from ./fetch-cboe-chain.ts like snapshot-calendars does) and, on use-case success, fire a
    `void deps.boss.send("compute-gex-snapshot", {}, { singletonKey: "triggered-by-analytics" }).catch(...)`
    — compute-analytics is no longer terminal; compute-gex-snapshot is the new last step (RESEARCH Open
    Question 2). Update its handler test if it asserts terminality. (2) schedule.ts: add
    `computeGexSnapshot: PgBossHandler` to AllHandlers; add `await boss.createQueue("compute-gex-snapshot");`
    in the createQueue phase (all createQueue before schedule/work — CR-01); add `await
    boss.work("compute-gex-snapshot", POLLING_INTERVAL, handlers.computeGexSnapshot);` in the work phase;
    NO schedule() cron (chain-triggered only). Update the boot warn count 9→10 queues. (3) main.ts: build
    the gex repo (makePostgres...GexSnapshot... from @morai/adapters), wire makeComputeGexSnapshotUseCase({
    readLegObs, persist, now }), build makeComputeGexSnapshotHandler({ computeGexSnapshotUseCase, now }),
    add `boss` to the existing computeAnalyticsHandler deps, and add `computeGexSnapshot:
    computeGexSnapshotHandler` to the registerAllJobs handler map. Composition only — no business logic
    in main.ts (architecture-boundaries §3; main.ts is TDD-exempt wiring).
  </action>
  <verify>
    <automated>bun run typecheck && bun run test apps/worker/src/handlers/compute-analytics.test.ts && rg -c "compute-gex-snapshot" apps/worker/src/schedule.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "compute-gex-snapshot" apps/worker/src/schedule.ts` shows BOTH a createQueue and a work registration, and NO schedule() cron line for it.
    - `rg -n "compute-gex-snapshot" apps/worker/src/handlers/compute-analytics.ts` matches (the chain trigger fires after compute-analytics).
    - `rg -n "computeGexSnapshot" apps/worker/src/main.ts` shows the use-case + handler wired and added to the registerAllJobs handler map.
    - `bun run test apps/worker/src/handlers/compute-analytics.test.ts` exits 0 (the added chain trigger does not break the existing handler test).
    - The boot warn string reads 10 queues (was 9): `rg -n "10 queues" apps/worker/src/main.ts` matches.
    - `bun run typecheck && bun run lint` exit 0.
  </acceptance_criteria>
  <done>compute-analytics chain-triggers compute-gex-snapshot on success (serial, D-01); the 10th queue is registered with no cron; the worker composition root wires the GEX use-case + repo + handler; existing handler test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pg-boss queue → handler | Job payloads are empty (`{}`); the trust concern is scheduling/gating, not payload content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | Denial of Service | duplicate chain enqueues | mitigate | The chain trigger uses `singletonKey: "triggered-by-analytics"` (snapshot-calendars precedent) so rapid re-runs collapse to one enqueue; the repo PK then dedupes the row (SC-4). |
| T-08-11 | Tampering | off-RTH / holiday writes | mitigate | The handler RTH+NYSE-holiday gate precedes the use-case call — no GEX row is written outside market hours or on holidays. |
| T-08-12 | Denial of Service | chain-trigger failure cascades | accept | The boss.send is fire-and-forget with `.catch` warn (snapshot-calendars precedent) — a send failure logs but does not fail the analytics job. |

ASVS L1; block on HIGH. No HIGH-severity unmitigated threat — gating + singletonKey dedupe are the
controls; empty job payloads carry no untrusted input.
</threat_model>

<verification>
- `bun run test apps/worker/src/handlers/compute-gex-snapshot.test.ts apps/worker/src/handlers/compute-analytics.test.ts` green.
- `rg -n "compute-gex-snapshot" apps/worker/src/schedule.ts` shows createQueue + work, no cron.
- `bun run typecheck && bun run lint` green.
</verification>

<success_criteria>
GEX-01 job half: a scheduled (chain-triggered) job computes the GEX snapshot each RTH cycle and is
registered in the worker (SC-2 produce-one-row-per-cycle wiring; SC-4 idempotency enforced at the
repo from 08-05).
</success_criteria>

<output>
Create `.planning/phases/08-web-dashboard-backend-gex-auth-rpc/08-06-SUMMARY.md` when done
</output>
