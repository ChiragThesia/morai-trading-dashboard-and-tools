---
phase: 08-web-dashboard-backend-gex-auth-rpc
plan: 05
type: tdd
wave: 3
depends_on: ["08-02", "08-03", "08-04"]
files_modified:
  - packages/core/src/analytics/application/computeGexSnapshot.ts
  - packages/core/src/analytics/application/computeGexSnapshot.test.ts
  - packages/core/src/analytics/application/getGex.ts
  - packages/core/src/analytics/application/getGex.test.ts
  - packages/adapters/src/postgres/gex-snapshot.repo.ts
  - packages/adapters/src/postgres/gex-snapshot.repo.contract.test.ts
  - packages/adapters/src/memory/gex-snapshot.ts
  - packages/adapters/src/memory/gex-snapshot.contract.test.ts
autonomous: true
requirements: [GEX-01, "SC-4"]
must_haves:
  truths:
    - "makeComputeGexSnapshotUseCase reads the latest leg_observations cycle, computes the GEX payload via the 08-03 domain, and persists exactly one gex_snapshot row stamped with the resolved cycle_time (SC-2)"
    - "makeGetGexUseCase returns the latest stored snapshot row, or ok(null) when none exists (SC-1 read-from-stored-row)"
    - "Re-running the persist within the same cycle produces 0 duplicate rows (onConflictDoNothing on cycle_time PK) — proven against real Postgres (SC-4)"
    - "The Postgres repo JOINs leg_observations with contracts on occ_symbol to read contractType/strike/expiration (Pitfall 2)"
  artifacts:
    - path: "packages/core/src/analytics/application/computeGexSnapshot.ts"
      provides: "makeComputeGexSnapshotUseCase factory returning ForRunningComputeGexSnapshot"
      contains: "makeComputeGexSnapshotUseCase"
    - path: "packages/core/src/analytics/application/getGex.ts"
      provides: "makeGetGexUseCase factory returning ForRunningGetGex"
      contains: "makeGetGexUseCase"
    - path: "packages/adapters/src/postgres/gex-snapshot.repo.ts"
      provides: "Postgres impl of ForReadingLegObsForGex + ForPersistingGexSnapshot + ForReadingGexSnapshot"
      contains: "onConflictDoNothing"
    - path: "packages/adapters/src/memory/gex-snapshot.ts"
      provides: "In-memory twin of the three GEX driven ports"
  key_links:
    - from: "packages/adapters/src/postgres/gex-snapshot.repo.ts"
      to: "packages/adapters/src/postgres/schema.ts"
      via: "insert(gexSnapshots).values(row).onConflictDoNothing() + JOIN leg_observations/contracts"
      pattern: "gexSnapshots"
    - from: "packages/core/src/analytics/application/computeGexSnapshot.ts"
      to: "packages/core/src/analytics/domain/gex.ts"
      via: "use-case composes the domain math (strikeGex/buildProfile/findFlip)"
      pattern: "buildProfile|strikeGex|findFlip"
  prohibitions:
    - "GEX is NOT recomputed on read — getGex is a pure DB read of the stored row (D-01)"
    - "computeGexSnapshot does NOT call now() as the stamped cycle_time — cycle_time derives from the resolved leg_observations DATA cycle (idempotency, 06-06 precedent)"
    - "The in-memory twin is NOT omitted — it ships in the SAME plan as the Postgres adapter (architecture-boundaries §8)"
---

<objective>
Implement the GEX vertical slice through the application + driven-adapter layers: the compute and
read use-cases (turning the 08-02 RED scaffolds GREEN), the Postgres repo implementing the three
driven ports, and its in-memory twin — with a testcontainers integration test proving the
`onConflictDoNothing` idempotency on `cycle_time` (SC-4) against real Postgres.

Purpose: SC-1 (read serves a stored row, never recomputed), SC-2 (compute writes one row per cycle),
SC-4 (re-run = 0 duplicate rows). The compute use-case composes the 08-03 domain; the repo JOINs
leg_observations with contracts to get per-contract metadata; idempotency is enforced at the repo
on the `cycle_time` PK.
Output: `computeGexSnapshot.ts` + `getGex.ts` (GREEN), `gex-snapshot.repo.ts` (Postgres) +
`gex-snapshot.ts` (memory twin), both with contract tests; the testcontainers idempotency test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-web-dashboard-backend-gex-auth-rpc/08-RESEARCH.md
@.planning/phases/08-web-dashboard-backend-gex-auth-rpc/08-PATTERNS.md
@packages/core/src/analytics/application/computeAnalytics.ts
@packages/core/src/analytics/application/getSkew.ts
@packages/core/src/analytics/application/ports.ts
@packages/core/src/analytics/domain/gex.ts
@packages/adapters/src/postgres/repos/risk-reversal-observations.ts
@packages/adapters/src/postgres/leg-observations.ts
@packages/adapters/src/postgres/repos/risk-reversal-observations.contract.test.ts
@packages/adapters/src/memory/risk-reversal-observations.ts
@.claude/rules/architecture-boundaries.md
@.claude/rules/typescript.md
@.claude/rules/tdd.md
</context>

## Artifacts this phase produces

This plan produces `computeGexSnapshot`/`getGex` use-cases (`makeComputeGexSnapshotUseCase`,
`makeGetGexUseCase`), the Postgres GEX repo (`gex-snapshot.repo.ts`: ForReadingLegObsForGex +
ForPersistingGexSnapshot + ForReadingGexSnapshot), and the in-memory twin (`gex-snapshot.ts`).
(Full-phase artifact list in 08-02.)

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: computeGexSnapshot + getGex use-cases (turn 08-02 RED scaffolds GREEN)</name>
  <files>packages/core/src/analytics/application/computeGexSnapshot.ts, packages/core/src/analytics/application/computeGexSnapshot.test.ts, packages/core/src/analytics/application/getGex.ts, packages/core/src/analytics/application/getGex.test.ts</files>
  <read_first>
    - packages/core/src/analytics/application/computeGexSnapshot.test.ts + getGex.test.ts (the RED scaffolds from 08-02 — the locked GREEN targets; read their port-double wiring + assertions first)
    - packages/core/src/analytics/application/computeAnalytics.ts (the compute use-case analog: factory shape makeXxx(deps) → ForRunningXxx; now() bounds resolution ONLY, never a stamped value — 06-06 CR-01/CR-02 design; Result<void,StorageError>)
    - packages/core/src/analytics/application/getSkew.ts + getTermStructure.ts (the read use-case analog — thin forwarder over a read port)
    - packages/core/src/analytics/domain/gex.ts (08-03 — dollarGamma/strikeGex/findFlip/buildProfile to compose)
    - packages/core/src/analytics/application/ports.ts (08-02 — the five ports + LegObsForGex/GexSnapshotRow; reuse StorageError)
    - .planning/phases/08-web-dashboard-backend-gex-auth-rpc/08-RESEARCH.md (Pattern 3 cycle-time flooring snapCycleTime; the idempotency design — cycle_time derived from data, not now())
  </read_first>
  <behavior>
    - computeGexSnapshot: reads leg-obs via ForReadingLegObsForGex (in-memory double), parses bsmGamma/bsmIv numeric strings (skip 'NaN'-stamped rows), computes spot/flip/walls/netGammaAtSpot/profile/strikes/byExpiry via the 08-03 domain, and persists exactly ONE GexSnapshotRow via ForPersistingGexSnapshot; cycle_time is the resolved data cycle (the latest leg-obs `time`, snapped), NOT now().
    - computeGexSnapshot: when the read returns no usable rows, it writes no row and returns ok (no crash, no NaN row).
    - getGex: returns the latest GexSnapshotRow from ForReadingGexSnapshot; returns ok(null) when the store is empty (no-snapshot).
    - Both propagate the read/persist Result.err as their own err (no throw).
  </behavior>
  <action>
    Implement computeGexSnapshot.ts as `makeComputeGexSnapshotUseCase(deps): ForRunningComputeGexSnapshot`
    where deps = { readLegObs: ForReadingLegObsForGex, persist: ForPersistingGexSnapshot, now: () =>
    Date }. Resolve the cycle_time from the DATA (latest leg-obs `time`, floored to the 30-min slot via
    the RESEARCH snapCycleTime helper) — now() bounds resolution only, never the stamped value (06-06
    precedent; idempotency depends on this). Convert DB strike (×1000 int) to points for the domain.
    Compose strikeGex → derive callWall (argmax positive) / putWall (argmin) / netGammaAtSpot at spot,
    buildProfile over a spot grid (spot ±600 step 20) → findFlip. Build the GexSnapshotRow and persist
    once. Implement getGex.ts as `makeGetGexUseCase(deps): ForRunningGetGex` — a thin forwarder over
    ForReadingGexSnapshot (D-01: pure read, no recompute). Both import only @morai/shared + local
    ports/domain. Run the 08-02 RED scaffolds; show GREEN.
  </action>
  <verify>
    <automated>bun run test packages/core/src/analytics/application/computeGexSnapshot.test.ts packages/core/src/analytics/application/getGex.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test packages/core/src/analytics/application/computeGexSnapshot.test.ts packages/core/src/analytics/application/getGex.test.ts` exits 0 — the 08-02 RED scaffolds are now GREEN.
    - `rg -n "buildProfile|strikeGex|findFlip" packages/core/src/analytics/application/computeGexSnapshot.ts` matches (composes the 08-03 domain, no re-derivation).
    - `rg -n "Date.now\\(\\)|now\\(\\)" packages/core/src/analytics/application/computeGexSnapshot.ts` shows now() used only for cycle resolution, never as the persisted cycle_time (assert the test stamps cycle_time from the data cycle).
    - getGex contains no GEX math import — `rg -c "buildProfile|strikeGex|bsmGreeks" packages/core/src/analytics/application/getGex.ts` returns 0 (D-01 read-only).
    - `bun run typecheck && bun run lint` exit 0.
  </acceptance_criteria>
  <done>Both use-cases are implemented and the 08-02 RED scaffolds are GREEN; compute composes the domain and stamps cycle_time from data; getGex is a pure read; hexagon-clean imports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Postgres GEX repo + in-memory twin + idempotency testcontainers test (SC-4)</name>
  <files>packages/adapters/src/postgres/gex-snapshot.repo.ts, packages/adapters/src/postgres/gex-snapshot.repo.contract.test.ts, packages/adapters/src/memory/gex-snapshot.ts, packages/adapters/src/memory/gex-snapshot.contract.test.ts</files>
  <read_first>
    - packages/adapters/src/postgres/repos/risk-reversal-observations.ts (the exact repo analog — imports, the onConflictDoNothing insert pattern lines 44-57, the catch→err({kind:"storage-error"}) wrap lines 58-62)
    - packages/adapters/src/postgres/leg-observations.ts (the JOIN-to-contracts read pattern + the ×1000 strike / numeric-string handling for bsmGamma/bsmIv)
    - packages/adapters/src/postgres/repos/risk-reversal-observations.contract.test.ts (the testcontainers contract-test style: applies migrations against ephemeral postgres:16, asserts insert + read + idempotency)
    - packages/adapters/src/memory/risk-reversal-observations.ts (the in-memory twin style — required by architecture-boundaries §8)
    - packages/core/src/analytics/application/ports.ts (08-02 — the three ports the repo + twin implement)
    - .planning/phases/08-web-dashboard-backend-gex-auth-rpc/08-RESEARCH.md (Pitfall 2 JOIN leg_observations↔contracts; the JSONB pass-as-JS-object insert) + 08-PATTERNS.md (gex-snapshot.repo.ts section)
    - MEMORY: Phase 3 P06 cross-test contamination note — testcontainers GEX repo test must use distinct cycle_time values + clean its own rows to avoid the flaky re-persist/idempotency contamination seen before.
  </read_first>
  <behavior>
    - ForReadingLegObsForGex (Postgres): SELECT lo.*, c.strike, c.expiration, c.contract_type FROM leg_observations lo JOIN contracts c ON lo.contract = c.occ_symbol filtered to the latest cycle `time`; returns LegObsForGex[] (bsmGamma/bsmIv as numeric strings or 'NaN').
    - ForPersistingGexSnapshot (Postgres): insert one gex_snapshots row with profile/strikes/byExpiry passed as JS objects into the jsonb columns + numeric scalars as strings; `.onConflictDoNothing()` on the cycle_time PK.
    - SC-4 idempotency (testcontainers): persisting the SAME cycle_time twice leaves exactly 1 row (`SELECT count(*) ... WHERE cycle_time = $t` = 1).
    - ForReadingGexSnapshot (Postgres): ORDER BY cycle_time DESC LIMIT 1; returns the row or null when empty.
    - The in-memory twin satisfies the identical contract test (read latest, persist-idempotent on cycle_time, JOIN behavior emulated).
  </behavior>
  <action>
    Implement gex-snapshot.repo.ts (the three driven ports) following risk-reversal-observations.ts:
    same @morai/shared ok/err imports, the onConflictDoNothing insert, the catch→err storage-error
    wrap. The leg-obs read JOINs leg_observations with contracts on occ_symbol for the latest cycle
    `time` (Pitfall 2 — strike/expiration/contractType live on contracts). JSONB columns are passed as
    JS objects (Drizzle jsonb handles serialization); numeric scalars as strings. Persist uses
    `.onConflictDoNothing()` on the cycle_time PK (SC-4). Implement the in-memory twin gex-snapshot.ts
    satisfying the same three ports (Map keyed by cycle_time for idempotency). Write the testcontainers
    contract test (gex-snapshot.repo.contract.test.ts) that applies migrations (including 0008) against
    an ephemeral postgres:16, then proves: insert+read round-trip, idempotent re-insert on the same
    cycle_time = 1 row (SC-4), and the JOIN read returns contract metadata. Write the memory contract
    test mirroring it. Use distinct cycle_time values + per-test cleanup to dodge the Phase-3 cross-test
    contamination flakiness. RED-first per file where practical (twin test before twin impl).
  </action>
  <verify>
    <automated>cd packages/adapters && bun run test src/postgres/gex-snapshot.repo.contract.test.ts src/memory/gex-snapshot.contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages/adapters && bun run test src/postgres/gex-snapshot.repo.contract.test.ts` exits 0; the idempotency assertion (re-insert same cycle_time → count = 1) passes against real Postgres (SC-4).
    - `cd packages/adapters && bun run test src/memory/gex-snapshot.contract.test.ts` exits 0 (twin satisfies the same contract — §8).
    - `rg -n "onConflictDoNothing" packages/adapters/src/postgres/gex-snapshot.repo.ts` matches on the persist path.
    - `rg -n "leg_observations|contracts|occ_symbol|JOIN|innerJoin" packages/adapters/src/postgres/gex-snapshot.repo.ts` shows the JOIN read (Pitfall 2).
    - `bun run typecheck && bun run lint` exit 0.
  </acceptance_criteria>
  <done>The Postgres GEX repo (JOIN read + onConflictDoNothing persist + latest read) and its in-memory twin both pass their contract tests; the testcontainers run proves SC-4 idempotency against real Postgres; the migration (08-04) is exercised end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| leg_observations (DB) → use-case | Stored chain data read into the compute use-case; numeric strings (incl. 'NaN') must be parsed defensively. |
| use-case → gex_snapshots (DB) | Computed payload persisted; idempotency keyed on cycle_time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Tampering | duplicate snapshot rows | mitigate | `onConflictDoNothing` on the `cycle_time` PK guarantees 0 duplicates on re-run; proven by the testcontainers idempotency test (SC-4). |
| T-08-08 | Information Disclosure | repo error messages | mitigate | The catch→err wrap returns `{kind:"storage-error", message}` to core; the HTTP/MCP edge (08-07) maps it to a flat error — DB internals never reach the client. |
| T-08-09 | Tampering | stale / wrong cycle stamping | mitigate | cycle_time derives from the resolved leg-obs DATA cycle, never now() (06-06 precedent); re-runs collapse onto the same key rather than minting a fresh duplicate. |
| T-08-SC | Tampering | npm installs | mitigate | No new packages this plan (RESEARCH audit: all deps present); slopcheck N/A. |

ASVS L1; block on HIGH. No HIGH-severity unmitigated threat — idempotency + error-flattening are
the load-bearing controls and both are tested.
</threat_model>

<verification>
- Core: `bun run test packages/core/src/analytics/application/computeGexSnapshot.test.ts packages/core/src/analytics/application/getGex.test.ts` green.
- Adapters (testcontainers): `cd packages/adapters && bun run test src/postgres/gex-snapshot.repo.contract.test.ts src/memory/gex-snapshot.contract.test.ts` green incl. SC-4 idempotency.
- `bun run typecheck && bun run lint` green.
</verification>

<success_criteria>
SC-1 read-from-stored-row (getGex), SC-2 compute-one-row-per-cycle (computeGexSnapshot), and SC-4
re-run-0-duplicates (repo onConflictDoNothing, testcontainers-proven) are all satisfied; the
in-memory twin ships alongside (§8).
</success_criteria>

<output>
Create `.planning/phases/08-web-dashboard-backend-gex-auth-rpc/08-05-SUMMARY.md` when done
</output>
