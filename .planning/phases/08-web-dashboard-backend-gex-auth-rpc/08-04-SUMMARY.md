---
phase: 08-web-dashboard-backend-gex-auth-rpc
plan: "04"
subsystem: database
tags: [drizzle, postgres, migration, gex, rls]

requires:
  - phase: 08-02
    provides: gexSnapshots Drizzle table definition in schema.ts (cycle_time PK, jsonb columns)

provides:
  - 0008_gex_snapshot.sql — CREATE TABLE gex_snapshots with cycle_time PK, 3 jsonb columns, RLS enabled
  - meta/0008_snapshot.json — full drizzle schema snapshot at migration 0008
  - meta/_journal.json updated with tag 0008_gex_snapshot (entry idx=8)

affects: [08-05, 08-06, 08-07]

tech-stack:
  added: []
  patterns:
    - "Migration rename: drizzle-kit generated random-word filename renamed to descriptive 0008_gex_snapshot.sql; journal tag updated to match (Phase 6 precedent)"

key-files:
  created:
    - packages/adapters/src/postgres/migrations/0008_gex_snapshot.sql
    - packages/adapters/src/postgres/migrations/meta/0008_snapshot.json
  modified:
    - packages/adapters/src/postgres/migrations/meta/_journal.json

key-decisions:
  - "0008_gex_snapshot.sql generated via bunx drizzle-kit generate from root drizzle.config.ts; renamed from 0008_certain_banshee.sql (Phase 6 / 06-02 precedent)"
  - "Live production Supabase migrate DEFERRED to operator per Phases 03/04/05/06 precedent; testcontainers integration test (08-05) is the automated migration+idempotency proof"
  - "Migrator command for live push: bun run apps/worker/src/migrate.ts against session pooler (port 5432, max:1) — NOT drizzle-kit push, NOT transaction pooler"

patterns-established: []

requirements-completed: [GEX-01]

duration: 2min
completed: "2026-06-24"
status: complete
---

# Phase 08 Plan 04: GEX Snapshot Migration Summary

**Drizzle migration 0008_gex_snapshot.sql generated and committed — gex_snapshots table DDL (cycle_time PK, 3 jsonb blobs, RLS enabled) ready for testcontainers proof in 08-05 and live operator push.**

## Performance

- **Duration:** 2 min
- **Started:** 2026-06-24T15:42:14Z
- **Completed:** 2026-06-24T15:44:50Z
- **Tasks:** 2 (Task 1 committed; Task 2 operator-deferred per precedent)
- **Files modified:** 3

## Accomplishments

- Generated 0008_gex_snapshot.sql via `bunx drizzle-kit generate` from root drizzle.config.ts; single-table migration confirmed (`rg -c "CREATE TABLE"` returns 1)
- Renamed from random-word output (`0008_certain_banshee.sql`) to `0008_gex_snapshot.sql` and updated `meta/_journal.json` tag to match (Phase 6 / 06-02 precedent)
- Migration defines `gex_snapshots` with `cycle_time` PK, `spot`/`net_gamma_at_spot` numeric NOT NULL, `flip`/`call_wall`/`put_wall` nullable, three `jsonb NOT NULL` columns (`profile`, `strikes`, `by_expiry`), and `ENABLE ROW LEVEL SECURITY`
- Live Supabase push operator-deferred per 03/04/05/06 precedent; 08-05 testcontainers run is the automated migration proof

## Task Commits

1. **Task 1: Generate 0008_gex_snapshot.sql from the gexSnapshots schema** - `2333022` (feat)
2. **Task 2: [BLOCKING] Apply the migration (push)** - operator-deferred (no code commit); documented in Deferred Items

## Files Created/Modified

- `packages/adapters/src/postgres/migrations/0008_gex_snapshot.sql` — CREATE TABLE gex_snapshots DDL + RLS statement
- `packages/adapters/src/postgres/migrations/meta/0008_snapshot.json` — full schema snapshot at migration 0008 (generated by drizzle-kit)
- `packages/adapters/src/postgres/migrations/meta/_journal.json` — appended entry idx=8 tag `0008_gex_snapshot`

## Verification

```
$ rg -n "gex_snapshots|ENABLE ROW LEVEL SECURITY|jsonb" packages/adapters/src/postgres/migrations/0008_gex_snapshot.sql
1:CREATE TABLE "gex_snapshots" (
8:"profile" jsonb NOT NULL,
9:"strikes" jsonb NOT NULL,
10:"by_expiry" jsonb NOT NULL
13:ALTER TABLE "gex_snapshots" ENABLE ROW LEVEL SECURITY;

$ rg -c "CREATE TABLE" packages/adapters/src/postgres/migrations/0008_gex_snapshot.sql
1
```

Migration is single-table and well-formed.

## Deferred Items

| Item | Reason | Resolution |
|------|--------|------------|
| Live Supabase migrate (gex_snapshots) | Operator-gated per Phases 03/04/05/06 precedent — live DDL requires manual intervention with session pooler | Run: `bun run apps/worker/src/migrate.ts` against session pooler (port 5432, max:1). Automated proof: 08-05 testcontainers run applies all migrations including 0008 against ephemeral postgres:16. |

## Deviations from Plan

None — plan executed exactly as written.

- Task 1: drizzle-kit generated `0008_certain_banshee.sql`; renamed to `0008_gex_snapshot.sql` per plan instruction and Phase 6 / 06-02 precedent. Journal tag updated.
- Task 2: Live push operator-deferred per explicit plan instruction and established precedent.
- Typecheck: Two pre-existing TS2307 errors from 08-02 RED scaffolds (`computeGexSnapshot.ts`, `getGex.ts` — GREEN targets for 08-06); no new errors introduced by this migration.

## Self-Check: PASSED

- `packages/adapters/src/postgres/migrations/0008_gex_snapshot.sql` — FOUND
- `packages/adapters/src/postgres/migrations/meta/0008_snapshot.json` — FOUND
- `packages/adapters/src/postgres/migrations/meta/_journal.json` — FOUND (tag updated)
- Commit `2333022` — FOUND
