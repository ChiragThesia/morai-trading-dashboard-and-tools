# Local partial stub package for `schwab-py` 1.5.1 (D4-01).
#
# `schwab-py` ships no `py.typed` marker -- verified against the real
# downloaded wheel, this phase's own research. Without a stub, every symbol
# the vendor exports resolves to `Any`, and this project's `reportAny` gate
# fires at every call site. This package covers exactly the handful of
# symbols `src/morai/vendor/schwab_adapter.py` uses -- nothing more.
