# Sidecar service pinned dependencies
# All packages verified against authoritative sources in 11-RESEARCH.md § Package Legitimacy Audit.
# schwab-py pinned to exactly 1.5.1 (D22 — never upgrade without research review).

# ── Core ──────────────────────────────────────────────────────────────────────
schwab-py==1.5.1
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
psycopg2-binary>=2.9
pydantic>=2.0
pydantic-settings>=2.0

# ── Dev / test ────────────────────────────────────────────────────────────────
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
