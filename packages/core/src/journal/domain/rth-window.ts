/**
 * rth-window — promoted verbatim to @morai/shared (2026-07-08; percentile-rank precedent)
 * so the picker context can label snapshots rth/after-hours without a cross-context domain
 * import. This module stays as the journal-context re-export.
 */
export { isWithinRth } from "@morai/shared";
