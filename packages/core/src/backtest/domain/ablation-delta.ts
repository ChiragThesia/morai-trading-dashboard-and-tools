/**
 * ablation-delta — rank delta between baseline and leave-one-rule-out ablated rankings
 * (BT-04). RED stub — see ablation-delta.test.ts for the behavior contract.
 */

export function ablationDelta(
  _baselineRanked: ReadonlyArray<string>,
  _ablatedRanked: ReadonlyArray<string>,
  _candidateId: string,
): number | null {
  throw new Error("not implemented");
}
